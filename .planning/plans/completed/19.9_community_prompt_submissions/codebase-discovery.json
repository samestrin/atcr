{
  "generated": "2026-07-10",
  "refined": "2026-07-10",
  "discovery_method": "semantic",
  "queries": [
    "persona submit fixture gate test",
    "personas CLI subcommand install list search",
    "persona source community built-in status field",
    "github fork pull request creation via gh cli",
    "persona template fixture runner",
    "persona name validation path escape",
    "injectable seam package var test",
    "cobra RunE output stdout stderr",
    "atomic file write persona unit"
  ],
  "match_count": 33,
  "build_from": {
    "primary_file": "cmd/atcr/personas.go",
    "reason": "Hosts the `personas` cobra command tree (`install`, `list`, `search`, `remove`, `test`, `upgrade`) built by `newPersonasCmd()`. A new `submit` subcommand slots in the same way: a `newPersonasSubmitCmd()` function registered via `cmd.AddCommand(...)` alongside the existing six (line 106-113).",
    "suggested_approach": "Add `newPersonasSubmitCmd()` following the existing subcommand pattern (cobra.Command with Args/RunE), reuse `commpersonas.TestPersona(name, personasFixtureRunner)` (already used by `newPersonasTestCmd()` at line 298-328) as the local fixture gate before any GitHub call, then shell out to `gh` (fork + create branch + commit + `gh pr create`) since the repo has no existing `gh` CLI invocation and the existing `internal/ghaction.Client` is a token-based REST client built for the GitHub Action / --auto-fix bot flow, not an end-user fork-on-their-own-account flow."
  },
  "semantic_matches": [
    {
      "file": "cmd/atcr/personas.go",
      "symbol": "newPersonasCmd",
      "type": "function",
      "line": 94,
      "score": 1.0,
      "query": "personas CLI subcommand install list search",
      "relevance": "Parent command that registers all `personas` subcommands; `submit` is added here"
    },
    {
      "file": "cmd/atcr/personas.go",
      "symbol": "newPersonasTestCmd",
      "type": "function",
      "line": 298,
      "score": 1.0,
      "query": "persona submit fixture gate test",
      "relevance": "Existing subcommand that calls TestPersona/personasFixtureRunner — the exact fixture-gate call `submit` should reuse before opening a PR"
    },
    {
      "file": "internal/personas/test.go",
      "symbol": "TestPersona",
      "type": "function",
      "line": 195,
      "score": 1.0,
      "query": "persona template fixture runner",
      "relevance": "`TestPersona(name string, runner FixtureRunner) (FixtureOutcome, error)` — the exact call signature `submit`'s local gate calls before allowing a PR to open; a failing/absent fixture must block submission per AC1"
    },
    {
      "file": "internal/personas/test.go",
      "symbol": "TemplateFixtureRunner",
      "type": "struct",
      "line": 33,
      "score": 1.0,
      "query": "persona template fixture runner",
      "relevance": "Production FixtureRunner used by `personas test`; resolves built-in vs. on-disk installed vs. embedded community-library fixtures without an LLM/network call"
    },
    {
      "file": "internal/personas/list.go",
      "symbol": "PersonaMeta",
      "type": "struct",
      "line": 19,
      "score": 0.99,
      "query": "persona source community built-in status field",
      "relevance": "`Source string // \"built-in\" | \"community\" | \"project\"` (line 22; project set at line 113) — the existing provenance axis AC2 says stays untouched; the new `submitted` status is orthogonal and must not be folded into this field or its string values"
    },
    {
      "file": "internal/personas/list.go",
      "symbol": "List",
      "type": "function",
      "line": 38,
      "score": 0.99,
      "query": "persona source community built-in status field",
      "relevance": "Builds PersonaMeta rows for `personas list`; if a `submitted` status needs to surface in the CLI (e.g. a future `list --status`), this is the aggregation point to extend without changing Source semantics"
    },
    {
      "file": "internal/personas/paths.go",
      "symbol": "validatePersonaName",
      "type": "function",
      "line": 42,
      "score": 1.0,
      "query": "persona name validation path escape",
      "relevance": "`submit` must validate the provided `<name>` before resolving its on-disk path or forking; reuses the same `[a-zA-Z0-9_/-]` guard and `..`/absolute-path rejection already applied to install/search/remove"
    },
    {
      "file": "internal/personas/paths.go",
      "symbol": "personaPath",
      "type": "function",
      "line": 72,
      "score": 1.0,
      "query": "persona name validation path escape",
      "relevance": "Maps a validated persona name to its `.yaml` path under a personas directory and confirms the cleaned result stays within the directory; `submit` uses this (directly or via PersonasDir) to locate the unit to copy into the fork"
    },
    {
      "file": "internal/personas/unit.go",
      "symbol": "writeFileAtomic",
      "type": "function",
      "line": 180,
      "score": 0.98,
      "query": "atomic file write persona unit",
      "relevance": "Sibling-temp-then-rename write with symlink refusal and 0600 perms; if `submit` persists any local status/attribution marker, it should reuse this helper or `internal/atomicfs.WriteFileAtomic` rather than invent a new write path"
    },
    {
      "file": "internal/atomicfs/atomic.go",
      "symbol": "WriteFileAtomic",
      "type": "function",
      "line": 24,
      "score": 0.96,
      "query": "atomic file write persona unit",
      "relevance": "Cross-package atomic file writer (sibling temp + rename, 0644); an alternative to `internal/personas/unit.go:writeFileAtomic` if the submit status marker lives outside the personas package"
    },
    {
      "file": "internal/ghaction/client.go",
      "symbol": "Client.CreateBranch",
      "type": "method",
      "line": 192,
      "score": 0.97,
      "query": "github fork pull request creation via gh cli",
      "relevance": "Existing token-based Git Data API wrapper (CreateBranch/CreateCommit/CreatePullRequest/FindOpenPullRequest) used by the --auto-fix bot flow (Epic 17.0). Built for a single configured Token against one repo, not a fork-from-the-user's-own-account flow — `personas submit` likely needs `gh` CLI (fork + push + `gh pr create`) instead of extending this client, since forking under an arbitrary user's account is not something a fixed bot Token can do."
    },
    {
      "file": "internal/ghaction/client.go",
      "symbol": "Client.CreatePullRequest",
      "type": "method",
      "line": 345,
      "score": 0.87,
      "query": "github fork pull request creation via gh cli",
      "relevance": "Shows the existing PR-creation contract/shape (Head/Base/Title/Body, redaction, 422 handling) as a reference even if `submit` uses `gh pr create` instead of this REST client directly"
    },
    {
      "file": "docs/personas-authoring.md",
      "symbol": "Contribution checklist",
      "type": "doc-section",
      "line": 162,
      "score": 0.97,
      "query": "persona submit fixture gate test",
      "relevance": "Existing manual pre-PR checklist (YAML validity, fixture presence/pass, no secrets) that `personas submit`'s automated local gate effectively codifies for AC1"
    },
    {
      "file": "docs/personas-install.md",
      "symbol": "The six subcommands",
      "type": "doc-section",
      "line": 40,
      "score": 1.0,
      "query": "personas CLI subcommand install list search",
      "relevance": "Documents each existing subcommand's contract/output; AC4 requires a new `### atcr personas submit <name>` section here following the same format, updating the heading from \"six\" to \"seven subcommands\""
    },
    {
      "file": "cmd/atcr/personas_test.go",
      "symbol": "executeSplit",
      "type": "function",
      "line": 35,
      "score": 0.95,
      "query": "cobra RunE output stdout stderr",
      "relevance": "Existing CLI test helper that separates stdout/stderr; submit's tests can follow this pattern to assert PR-URL output on stdout and diagnostic precondition errors on stderr"
    }
  ],
  "existing_patterns": [
    {
      "name": "Cobra subcommand registration",
      "description": "Each `personas` verb is a `newPersonas<Verb>Cmd() *cobra.Command` function added via `cmd.AddCommand(...)` in `newPersonasCmd()`; `RunE` closures call into `internal/personas` package functions and format output with `fmt.Fprintf` to `cmd.OutOrStdout()`/`cmd.ErrOrStderr()`",
      "files": ["cmd/atcr/personas.go"],
      "follow_for": "The new `submit` subcommand's cobra wiring, argument validation (`usageArgs(cobra.ExactArgs(1))`), and output formatting"
    },
    {
      "name": "Injectable seams for testability",
      "description": "Package-level vars (`personasDir`, `personasClient`, `personasFixtureRunner`) hold the production implementation but are swapped in tests via `withFixtureRunner`-style helpers, keeping CLI tests free of real network/filesystem calls",
      "files": ["cmd/atcr/personas.go", "cmd/atcr/personas_test.go"],
      "follow_for": "Any new external dependency `submit` introduces (a `gh` CLI invoker, a GitHub client) should be an injectable package var/interface so submit's tests can stub it out rather than shelling out for real"
    },
    {
      "name": "FixtureRunner interface + local-only render check",
      "description": "`TemplateFixtureRunner.RunFixture` renders the persona template against its committed patch fixture and asserts no unrendered `{{ }}` markers remain — zero network, zero LLM calls",
      "files": ["internal/personas/test.go"],
      "follow_for": "The exact gate `personas submit` must run before opening a PR (AC1: \"a failing fixture blocks submission with a clear error\")"
    },
    {
      "name": "GitHub REST client with redaction, retry, and typed APIError",
      "description": "`internal/ghaction.Client` wraps GitHub's REST API with exponential backoff on 5xx/429, secret redaction on outgoing title/body, and a typed `*APIError` for non-retriable failures",
      "files": ["internal/ghaction/client.go"],
      "follow_for": "Reference implementation quality bar (retry/redaction/error typing) if `submit` ends up needing any direct REST call rather than shelling out to `gh` entirely"
    },
    {
      "name": "Persona name validation and safe path resolution",
      "description": "`internal/personas/paths.go` centralizes name validation (`validatePersonaName`, regex `[a-zA-Z0-9_/-]`, rejects `..`/absolute paths) and maps a name to its on-disk path under a directory with a relative-path guard; `internal/registry/persona.go:validateName` mirrors the same rules for the resolver",
      "files": ["internal/personas/paths.go", "internal/registry/persona.go"],
      "follow_for": "`personas submit <name>` must validate `name` and resolve its `.yaml` (and optional `.md`) path before any fork/PR work; reuse these helpers rather than reimplementing traversal guards"
    },
    {
      "name": "Atomic file writes with symlink/rollback guards",
      "description": "`internal/personas/unit.go:writeFileAtomic` stages to a sibling temp file, refuses symlinked destinations, chmods 0600, and renames into place; `internal/atomicfs/atomic.go:WriteFileAtomic` provides the same pattern cross-package for 0644 artifacts",
      "files": ["internal/personas/unit.go", "internal/atomicfs/atomic.go"],
      "follow_for": "If `submit` persists a local `submitted` status marker or attribution file, use one of these atomic writers instead of a bare `os.WriteFile`; both already handle temp-file cleanup and symlink TOCTOU"
    },
    {
      "name": "CLI precondition-check pattern",
      "description": "`skill/SKILL.md` documents the `atcr` skill's pre-flight checks: the binary must be on PATH, the cwd must be a git work tree, and `gh` must be authenticated for PR resolution, with clear halt messages before any expensive work",
      "files": ["skill/SKILL.md"],
      "follow_for": "Mirror this pattern for `personas submit`: check `gh` presence and `gh auth status` (or equivalent) before forking, with an actionable error message if `gh` is missing or unauthenticated"
    }
  ],
  "related_files": [
    {
      "path": "cmd/atcr/personas.go",
      "relevance": "high",
      "purpose": "Hosts the personas subcommand tree; new submit subcommand registers here",
      "likely_modification": "extend"
    },
    {
      "path": "cmd/atcr/personas_test.go",
      "relevance": "high",
      "purpose": "Existing CLI-level tests for install/list/search/remove/test/upgrade (e.g. TestPersonasTest_DefaultRunnerBuiltinFixture at line 669) and the `executeSplit` stdout/stderr helper",
      "likely_modification": "extend"
    },
    {
      "path": "cmd/atcr/main_test.go",
      "relevance": "medium",
      "purpose": "Shared `execute(t, args...)` helper for combined stdout+stderr CLI tests and subcommand-count assertions; submit tests may use either `execute` or `executeSplit`",
      "likely_modification": "reference-only"
    },
    {
      "path": "internal/personas/test.go",
      "relevance": "high",
      "purpose": "TestPersona + TemplateFixtureRunner — the fixture gate submit must run locally before any GitHub call",
      "likely_modification": "reference-only (call, do not modify)"
    },
    {
      "path": "internal/personas/list.go",
      "relevance": "high",
      "purpose": "PersonaMeta/Source (built-in|community|project) — the existing provenance axis the new `submitted` status must stay orthogonal to",
      "likely_modification": "extend only if the CLI surfaces submitted status in list output; otherwise reference-only"
    },
    {
      "path": "internal/personas/paths.go",
      "relevance": "high",
      "purpose": "Persona name validation and safe path resolution under personasDir; submit must reuse this before locating the unit to submit",
      "likely_modification": "reference-only"
    },
    {
      "path": "internal/personas/unit.go",
      "relevance": "medium",
      "purpose": "Atomic persona-unit write helpers and C3 fetched-prompt guardrails; reference for atomic-write and symlink-guard conventions if submit persists local markers",
      "likely_modification": "reference-only"
    },
    {
      "path": "internal/personas/search.go",
      "relevance": "medium",
      "purpose": "PersonaIndexEntry schema for `personas/community/index.json`; a submitted persona that graduates into the library will need a matching index entry",
      "likely_modification": "reference-only"
    },
    {
      "path": "internal/registry/persona.go",
      "relevance": "medium",
      "purpose": "Resolver-side name validation (`validateName`) and C3 prompt guardrails; mirrors `internal/personas/paths.go` rules for the review-time resolver",
      "likely_modification": "reference-only"
    },
    {
      "path": "internal/atomicfs/atomic.go",
      "relevance": "low",
      "purpose": "Cross-package atomic file writer; alternative to `internal/personas/unit.go:writeFileAtomic` for any submit status marker written outside `internal/personas`",
      "likely_modification": "reference-only"
    },
    {
      "path": "internal/ghaction/client.go",
      "relevance": "medium",
      "purpose": "Existing token-based GitHub REST client (branch/commit/PR) for the --auto-fix bot flow (Epic 17.0)",
      "likely_modification": "reference-only, unless submit reuses its retry/redaction helpers"
    },
    {
      "path": "docs/personas-authoring.md",
      "relevance": "high",
      "purpose": "Contribution checklist (line 162) a submitted persona must satisfy; the fixture/format/location rules submit's local gate enforces",
      "likely_modification": "modify (cross-reference the new automated submit flow and the submitted→graduated curation model)"
    },
    {
      "path": "docs/personas-install.md",
      "relevance": "high",
      "purpose": "Documents the six existing subcommands in a consistent format",
      "likely_modification": "modify (add a seventh `submit` subcommand section, per AC4)"
    },
    {
      "path": "skill/SKILL.md",
      "relevance": "medium",
      "purpose": "Precondition-check pattern for the atcr skill, including `gh` presence/authentication checks before PR resolution; risk mitigation in plan.md references this pattern",
      "likely_modification": "reference-only"
    },
    {
      "path": "personas/community/",
      "relevance": "medium",
      "purpose": "The vetted community persona library a submission graduates into",
      "likely_modification": "reference-only (graduation target directory)"
    },
    {
      "path": "personas/community/index.json",
      "relevance": "medium",
      "purpose": "Community library index; a graduated submission will need an entry here with provider/model matching its YAML",
      "likely_modification": "reference-only (graduation adds an entry, not part of the automated submit path)"
    }
  ],
  "integration_points": [
    {
      "location": "cmd/atcr/personas.go:newPersonasCmd",
      "type": "entry-point",
      "description": "`cmd.AddCommand(newPersonasSubmitCmd())` alongside the existing six subcommands"
    },
    {
      "location": "internal/personas/test.go:TestPersona",
      "type": "hook",
      "description": "Local fixture gate called before any fork/PR network call; a non-passing outcome aborts submission with a clear error (AC1)"
    },
    {
      "location": "internal/personas/paths.go:validatePersonaName / personaPath",
      "type": "guard",
      "description": "Name and path validation before resolving the on-disk persona unit to copy into the fork"
    },
    {
      "location": "cmd/atcr/personas.go:newPersonasSubmitCmd (gh precondition check)",
      "type": "guard",
      "description": "Mirror `skill/SKILL.md`'s precondition pattern: verify `gh` is on PATH and authenticated before any fork/branch/commit work, halting with a clear actionable message (risk mitigation)"
    },
    {
      "location": "internal/personas/submit.go (proposed)",
      "type": "persistence",
      "description": "If a local `submitted` status marker or attribution file is persisted, it must be written atomically and kept separate from the persona's Source provenance field (AC2)"
    }
  ],
  "reusable_components": [
    {
      "name": "TestPersona / TemplateFixtureRunner",
      "path": "internal/personas/test.go",
      "can_extend": true,
      "usage_example": "outcome, err := commpersonas.TestPersona(name, personasFixtureRunner); if err != nil || outcome.Passed != outcome.Total { return fmt.Errorf(\"fixture gate failed for %q\", name) }"
    },
    {
      "name": "validatePersonaName / personaPath",
      "path": "internal/personas/paths.go",
      "can_extend": false,
      "usage_example": "yamlPath, err := commpersonas.PersonasDir(); then use internal helpers or reuse PersonasDir + validatePersonaName to locate the unit"
    },
    {
      "name": "writeFileAtomic",
      "path": "internal/personas/unit.go",
      "can_extend": false,
      "usage_example": "use writeFileAtomic(markerPath, data) if submit persists any local status/attribution marker"
    }
  ],
  "architecture_notes": [
    "No existing code shells out to the `gh` CLI (`grep -rn \"exec.Command(\\\"gh\\\"\" --include=\"*.go\"` returns nothing); `personas submit` would be the first such integration point in the codebase, so its `gh` invocation wrapper should be an injectable seam (interface or package var) matching the existing `personasClient`/`personasFixtureRunner` testability pattern rather than a bare `exec.Command` call inline in the cobra RunE.",
    "The existing `internal/ghaction.Client` is architecturally a poor fit for `personas submit`: it authenticates with one fixed bot Token against one configured repo (the --auto-fix GitHub Action flow, Epic 17.0), whereas `submit` needs to fork `samestrin/atcr` under the *invoking user's own* GitHub identity and open a PR from their fork — a flow `gh` CLI (`gh repo fork`, `gh pr create`) handles natively via the user's local `gh auth` session, with no bot token plumbing required.",
    "`Source` (`internal/personas/list.go:22`, comment says \"built-in\"|\"community\" but `listProject` at line 113 also sets it to \"project\") is a provenance axis, not a closed two-value field. AC2's `submitted` status must NOT be added as another `Source` value — it needs its own field/concept (e.g., a status marker file, a YAML frontmatter key, or a separate manifest) so `Source` keeps meaning only resolver provenance, per the epic's terminology-collision resolution.",
    "The contribution checklist in `docs/personas-authoring.md` (line 162) already documents everything a human contributor manually verifies pre-PR (YAML validity, fixture presence/pass, no secrets, index entry consistency); `personas submit`'s automated local gate is effectively an automated subset of this checklist (specifically the fixture-pass item) and AC4's docs update should cross-reference rather than duplicate it.",
    "`personas submit <name>` should validate `name` through the existing `internal/personas/paths.go:validatePersonaName` guard (or the equivalent `internal/registry/persona.go:validateName`) before resolving the on-disk `.yaml`/`.md` unit. This avoids reimplementing path-traversal protection and keeps the submit path consistent with install/search/remove.",
    "If a local `submitted` status marker or attribution file is introduced, persist it with the existing atomic-write helper (`internal/personas/unit.go:writeFileAtomic` for 0600 persona-adjacent files, or `internal/atomicfs/atomic.go:WriteFileAtomic` for 0644 artifacts), never with a bare `os.WriteFile`. Keep the marker outside the vetted `personas/community/` tree so graduation remains a maintainer review action, not an automated copy.",
    "Precondition checking for `gh` should follow the pattern in `skill/SKILL.md` (used by the atcr skill for PR resolution): verify the binary is on PATH and authenticated, and halt with a clear, actionable error before any fork/branch/commit work begins. This directly addresses the risk-mitigation note in plan.md."
  ],
  "files_to_modify": [
    {
      "path": "cmd/atcr/personas.go",
      "reason": "Add `newPersonasSubmitCmd()` and register it in `newPersonasCmd()`",
      "scope": "major"
    },
    {
      "path": "docs/personas-install.md",
      "reason": "Document the new `submit` subcommand (AC4) following the existing six-subcommand format",
      "scope": "minor"
    },
    {
      "path": "docs/personas-authoring.md",
      "reason": "Cross-reference the automated submit flow against the manual contribution checklist; document the submitted → graduated two-tier model (AC2, AC4)",
      "scope": "minor"
    }
  ],
  "files_to_create": [
    {
      "path": "cmd/atcr/personas_submit_test.go or extend cmd/atcr/personas_test.go",
      "purpose": "Unit/CLI tests for the submit subcommand: fixture-gate pass/fail, gh invocation stubbed via an injectable seam",
      "based_on": "cmd/atcr/personas_test.go (existing TestPersonasTest_* pattern and executeSplit helper)"
    },
    {
      "path": "internal/personas/submit.go (naming TBD)",
      "purpose": "Package-level logic for the submit flow: fixture gate call, name/path resolution, submitted-status marker, attribution capture, gh wrapper interface — kept out of cmd/atcr per the existing internal/personas vs cmd/atcr split",
      "based_on": "internal/personas/test.go, internal/personas/paths.go, internal/personas/unit.go"
    },
    {
      "path": "internal/personas/submit_test.go (if logic lives in internal/personas/submit.go)",
      "purpose": "Unit tests for the internal submit logic: stub fixture runner, stub gh wrapper, name validation, status-marker read/write",
      "based_on": "internal/personas/*_test.go patterns (stubRunner in personas_test.go:425)"
    }
  ],
  "test_patterns": {
    "framework": "go test (testing package, testify not used per existing files)",
    "test_location": "cmd/atcr/personas_test.go, internal/personas/*_test.go",
    "naming_convention": "Test<Subject>_<Scenario> e.g. TestPersonasTest_DefaultRunnerBuiltinFixture",
    "example_test": "cmd/atcr/personas_test.go:669 (TestPersonasTest_DefaultRunnerBuiltinFixture); cmd/atcr/personas_test.go:35 (executeSplit)"
  }
}
