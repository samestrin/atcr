# Documentation Source Index

**Generated:** July 20, 2026 10:50:33PM
**Refined:** July 21, 2026 (by /refine-docs — sources populated, relevance table corrected)
**Plan:** /Users/samestrin/Documents/GitHub/atcr/.planning/plans/active/32.3_sandbox_ephemeral_copy_overlay/plan.md

---

## Source Paths

<!-- SOURCES:START -->
@.planning/specifications/packages/standard-library.md
@.planning/specifications/packages/testify.md
@docs/auto-fix.md
@docs/execution.md
@internal/sandbox/sandbox.go
@internal/verify/autofix_exec.go
@https://docs.docker.com/engine/storage/tmpfs/
@https://docs.docker.com/reference/cli/docker/container/run/#read-only
@https://docs.docker.com/engine/storage/bind-mounts/
<!-- SOURCES:END -->

---

## Source Details

| Path | Relevance | Type | Reason |
|------|-----------|------|--------|
| `.planning/specifications/packages/standard-library.md` | Medium | Global spec | Documents atcr's `os/exec` and `context` conventions — the exact stdlib surface plan.md names as its Framework/Technology (driving the `docker` CLI, run timeouts). The original `/find-documentation` pass scored no spec >= 5/10 for the sandbox topic itself, but this file covers the plan's implementation idioms. |
| `.planning/specifications/packages/testify.md` | Medium | Global spec | Assertion framework (`assert`/`require`) used by the existing `internal/sandbox` tests and required for T5's new tests; plan.md confirms testify is already a project dependency. |
| `docs/auto-fix.md` | High | Project doc | Carries the three stale read-only-`/work` claims T6 must update (line 47, lines 55-60, lines 62-71). Excerpted in [current-sandbox-guarantees.md](current-sandbox-guarantees.md). |
| `docs/execution.md` | High | Project doc | Pins `--exec`'s read-only-`/work` guarantee (lines 51-62, 86-90) that must remain true; `Writable:false` keeps it accurate with no edit. |
| `internal/sandbox/sandbox.go` | High | Code (package doc) | Package doc's hard MUST ("a read-only view of the snapshot") and `RunSpec`/`validate()` — the struct T1 extends and the injection guard that must survive. |
| `internal/verify/autofix_exec.go` | Medium | Code (doc comment) | `ResolveAutoFixSandbox`'s doc comment (lines 47-55) duplicates the stale EROFS limitation; in T6's scope. |
| Docker tmpfs mounts docs | High | External | `--tmpfs` syntax/options (`rw`, `exec`, `size=`), ephemerality, Linux-only — the mechanism behind the new `/work` mount. Excerpted in [docker-tmpfs-and-read-only-mounts.md](docker-tmpfs-and-read-only-mounts.md). |
| Docker `docker run` `--read-only` reference | High | External | Rootfs read-only semantics — why `/work` has no writable backing without an explicit tmpfs. |
| Docker bind mounts docs | Medium | External | `:ro` bind-mount behavior for the `/src` (Writable:true) and `/work` (Writable:false) snapshot mounts. |

**Specification inventory note (corrected):** `.planning/specifications/` contains three subdirectories, not two as the generated table originally stated — `design-concepts/` (UI/branding, unrelated), `local-services/` (a Docker Compose dev-infra stub, unrelated), and `packages/` (two relevant files listed above). No specification covers sandbox/Docker/container mount internals directly; the authoritative references for those are the external Docker docs listed above.

---

## Next Step

Documentation excerpts now exist (created by `/refine-docs`):
[docker-tmpfs-and-read-only-mounts.md](docker-tmpfs-and-read-only-mounts.md) and
[current-sandbox-guarantees.md](current-sandbox-guarantees.md) — see
[README.md](README.md) for the index.

Continue the plan pipeline with:

`/create-user-stories @/Users/samestrin/Documents/GitHub/atcr/.planning/plans/active/32.3_sandbox_ephemeral_copy_overlay/`

If the documentation set needs to be regenerated or expanded from the source list
above, run:
`/create-documentation @/Users/samestrin/Documents/GitHub/atcr/.planning/plans/active/32.3_sandbox_ephemeral_copy_overlay/`

---

**Generated by:** /find-documentation (refined by /refine-docs)
