# Documentation Source Index

**Generated:** July 18, 2026 08:00:42AM
**Plan:** /Users/samestrin/Documents/GitHub/atcr/.planning/plans/active/31.0_axi_compliance/plan.md

---

## Source Paths

<!-- SOURCES:START -->
.planning/specifications/packages/cobra.md
.planning/specifications/design-concepts/adversarial-verification-interface.md
.planning/specifications/packages/jsonschema-go.md
.planning/specifications/packages/mcp-go-sdk.md
.planning/specifications/packages/mcp-sdk.md
docs/findings-format.md
docs/payload-modes.md
https://toonformat.dev/
https://toonformat.dev/reference/syntax-cheatsheet.html
<!-- SOURCES:END -->

---

## Source Details

| Path | Relevance | Type | Reason |
|------|-----------|------|--------|
| `.planning/specifications/packages/cobra.md` | 8/10 | File | Documents `cmd.OutOrStdout()`/`SetOut()/SetErr()` and `main()`-level exit-code routing — the exact mechanism plan.md's Implementation Strategy specifies for gating `--axi` output and reconciling exit codes. |
| `.planning/specifications/design-concepts/adversarial-verification-interface.md` | 7/10 | File | Epic 3.0's `atcr verify` design already establishes and documents the identical 0=clean/1=gate-failure/2=usage-error exit-code contract this plan must reconcile with, plus a CLI/MCP dual-surface pattern directly analogous to the FormatAXI/MCP-parity question. |
| `.planning/specifications/packages/jsonschema-go.md` | 7/10 | File | Documents how `internal/mcp/tools.go` derives the `atcr_report` tool's format enum/schema from Go types — directly informs whether a new `FormatAXI` constant auto-propagates to MCP (the open design question noted in plan.md's Technical Planning Notes). |
| `.planning/specifications/packages/mcp-go-sdk.md` | 6/10 | File | Notes "stdio transport owns stdout — all human/log output ... must go to stderr," the same stdout/stderr discipline `--axi` must enforce, and documents the typed-schema tool-registration pattern behind MCP format-enum exposure. |
| `.planning/specifications/packages/mcp-sdk.md` | 6/10 | File | Full API reference for the MCP SDK's typed `Tool`/`AddTool` schema generation, underlying the MCP-parity mechanism (`report.FormatList()` feeding the `atcr_report` schema enum) that plan.md's FormatAXI decision depends on. |
| `docs/findings-format.md` | 7/10 | File | Canonical `atcr-findings/v1` specification — the existing versioned, machine-parseable findings contract (hard header gate, additive-only evolution) that plan.md's Risk Mitigation requires reviewing before finalizing the axi/TOON schema. Added during `/refine-docs`. |
| `docs/payload-modes.md` | 6/10 | File | Byte-budget and deterministic-truncation contract (largest-first drops, every drop recorded) behind the `truncated`/`files_dropped` status.json fields whose naming the AXI 500-line cap reuses. Added during `/refine-docs`. |
| `https://toonformat.dev/` | 6/10 | URL | TOON project home — the epic's named candidate encoding is a real spec-driven format with advertised multi-language implementations including Go; design must evaluate the Go implementation vs. plan.md's hand-rolled-formatter stance. Added during `/refine-docs`. |
| `https://toonformat.dev/reference/syntax-cheatsheet.html` | 7/10 | URL | Normative JSON→TOON syntax mapping: tabular arrays (the findings shape), declared array lengths, pipe/tab alternative delimiters (convergent with `atcr-findings/v1`'s pipe grammar), must-quote rules, and the five valid escape sequences. Added during `/refine-docs`. |

---

## Next Step

Run: `/create-documentation @.planning/plans/active/31.0_axi_compliance/`

The command will automatically read source paths from this file.

To include additional sources (URLs, other repos), append them:
`/create-documentation @.planning/plans/active/31.0_axi_compliance/ @https://docs.example.com @../other-repo/docs/`

---

**Generated by:** /find-documentation
