# SUPERSEDED — Docs-Only Scope (retired 2026-07-07)

This is the plan bundle (`plan.md`, user-stories, acceptance-criteria, sprint-design, test-planning-matrix, codebase-discovery) for the **retired** docs-only framing of Epic 19.6 — "recommend a default majors persona pack in the docs." It was generated by an earlier `/init-plan` → `/design-sprint` → `/create-sprint` run and is preserved here for traceability only.

## Why superseded

Epic 19.6 was rescoped on 2026-07-07 into a real in-repo feature (community-canonical persona distribution + model-aware discovery + a human-named model-indexed library + onboarding-hierarchy docs). The current requirements live in the epic:

- `.planning/epics/active/19.6_community_registry_hub.md` (see its `Rescope & Pivot (2026-07-07)` section)

The matching superseded sprint is at:

- `.planning/sprints/superseded/19.6_community_registry_hub_docs-only/`

## Do not

- Do not treat these artifacts as current. A fresh plan bundle will be regenerated by re-running `/init-plan` on the rewritten epic.
