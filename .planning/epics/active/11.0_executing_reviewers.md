# Epic Plan 11.0: Executing Reviewers

**Estimated Durations**: 3 weeks
**Tasks/Components:** 5/3
**Execution:** /init-plan (COMPONENT_COUNT=3 exceeds /execute-epic's ≤2 limit; detailed design deferred)
**Status:** Draft (outline only; depends on Epics 2.0–6.0)

## Objective

Let agents *demonstrate* findings instead of asserting them: an opt-in execution mode where designated agents can run the project's test suite and minimal repro scripts inside a sandbox, attaching command + output as evidence. The EVIDENCE column becomes executable proof.

## Context

Stages 2–4 ground findings in read code and adversarial challenge, but a reproduced failure is a categorically stronger deliverable: it cannot be a hallucination, and it hands the resolver a failing command to start from. Execution requires real sandboxing — untrusted-model-authored code running on the user's machine — which is why this is the last rung and strictly opt-in.

## Problem Statement

1. Even verified findings are static analysis; some bug classes (race conditions, environment-dependent behavior, integration failures) only show under execution.
2. Resolvers spend time reconstructing repro steps that the reviewing agent already reasoned out.
3. Arbitrary execution of model-authored code without containment is an unacceptable security posture for a shareable tool.

## Proposed Solution (outline — detailed design deferred until 2.0–3.0 field experience)

- **Opt-in only:** `atcr review --exec` / `verify --exec`; off by default; refuses to run without an explicit sandbox backend configured. *(Modifies: `cmd/atcr/review.go`, `cmd/atcr/verify.go`)*
- **Sandbox backend:** container-based (Docker/Podman) executor: snapshot mounted read-only, writable scratch overlay, no network, CPU/memory/time caps, non-root. Backend pluggable; bare-metal execution intentionally unsupported.
  *(Note: this new code-execution sandbox is a distinct component from `internal/verify/executor.go`, the existing LLM agent runner — that file is NOT in scope for this epic.)*
- **New tools (execution-enabled agents only):** `run_tests(target?)` (project test command from config), `run_script(content, timeout)` (repro scripts in scratch). Outputs captured, truncated to budget, attached to the transcript.
- **Findings integration:** `evidence_exec` block in findings JSON — `{command, exit_code, output_excerpt}`; report renders "Reproduced" badges; confidence: reproduced findings are VERIFIED by definition. *(Modifies: `internal/reconcile/emit.go`, `internal/report/render.go`)*
- **Primary consumers:** Epic 3.0 skeptics (refute or confirm by running) and a dedicated `repro` role for high-severity findings. *(Tool registration: `internal/mcp/handlers.go`, `internal/tools/dispatch.go`)*

## Success Criteria (outline)

- [ ] Execution unavailable unless explicitly enabled and a sandbox backend passes a preflight check.
- [ ] Sandboxed runs are network-isolated, resource-capped, and leave the snapshot unmodified.
- [ ] A planted bug fixture is reproduced end-to-end: skeptic writes a repro, runs it, attaches failing output, finding is marked Reproduced.
- [ ] Execution evidence flows through findings.json → report.md → gate semantics.

## Out of Scope

- Auto-fix / patch generation; performance benchmarking; bare-metal execution; long-running service testing.

## Dependencies

Epics 2.0–6.0; container runtime present on the host; project test command configured in `.atcr/config.yaml`.

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Sandbox escape / resource abuse by model-authored code | Low | Critical | Container isolation, no network, caps, read-only snapshot, opt-in; security review before release |
| Flaky tests poison evidence | High | Medium | Repro must be deterministic (re-run twice rule); flaky results downgraded to `unverifiable` |
| Setup friction (container runtime) limits adoption | High | Low | Strictly optional; all prior stages work without it |

## Clarifications

- **Q: Why last?** A: It is the only stage where model-authored code executes; the security bar is categorically higher, and stages 2–4 deliver most of the precision gain without it. (2026-06-10)

## Refinements (2026-06-25)

This section records findings from `/refine-epic` run on June 25, 2026 07:34:27AM. It is additive — original plan content above is preserved.

### Auto-applied corrections (0)

None.

### Items needing user confirmation (3)

✅ **C1 — Contradiction: Execution mode vs. plan status** *(applied 2026-06-25)*

The plan header states `**Execution:** execute-epic --strong` but also states `**Status:** Draft (outline only; depends on Epics 2.0–6.0)`. These are incompatible: a plan with unresolved design (deferred until Epics 2.0–3.0 field experience), outstanding prerequisites (Epics 2.0–6.0), and COMPONENT_COUNT=3 (which exceeds /execute-epic's ≤2 components hard limit) cannot run through /execute-epic. Suggested action: update the `Execution` field to `/init-plan` and only change `Status` from Draft once prerequisites are met and the detailed design is resolved.

✅ **C2 — Naming conflict: "sandbox executor" vs. existing `internal/verify/executor.go`** *(applied 2026-06-25)*

The plan's proposed "sandbox/executor module" component collides in name with the existing LLM agent executor at `internal/verify/executor.go` (which invokes Claude agents, not user code in a container). Spot-check confirmed this file exists and is active. Suggested action: rename the new component explicitly in the plan — e.g., "sandbox code-runner" or "code-execution sandbox" — and add a note that `internal/verify/executor.go` (the LLM executor) is NOT in scope for this epic.

✅ **C3 — Missing file references for T1, T4, and T5** *(applied 2026-06-25)*

Tasks T1 (add `--exec` flag), T4 (add `evidence_exec` block to findings JSON + render badges), and T5 (wire gate semantics) will necessarily modify existing files that the plan does not mention:
- `cmd/atcr/review.go` — CLI flag addition for `--exec`
- `cmd/atcr/verify.go` — CLI flag addition for `verify --exec`
- `internal/reconcile/emit.go` — where findings fields are emitted (evidence field additions)
- `internal/report/render.go` — where "Reproduced" badges would be rendered

Additionally, `internal/mcp/handlers.go` and `internal/tools/dispatch.go` are the likely registration points for new MCP tools (`run_tests`, `run_script`). Including these pushes the true component count to 5+, further past /execute-epic's ≤2 limit. Suggested action: enumerate these files explicitly in the task descriptions and update COMPONENTS_TOUCHED accordingly.

### Advisory observations (3)

ℹ️ **I1 — Scope-guard violation (already signaled in C1)**

Derived TASK_COUNT=5 (within limit), COMPONENT_COUNT=3 (exceeds limit of 2). This plan will be rejected by /execute-epic's Phase 1 Step 4 scope guard. It should run through `/init-plan @.planning/epics/active/11.0_executing_reviewers.md` for the full sprint pipeline. Refining alone will not unblock /execute-epic.

ℹ️ **I2 — All cited symbols are net-new**

`run_tests`, `run_script`, and `evidence_exec` have zero matches in the implementation codebase (confirmed via grep). Only the epic plan file itself contains these names. This confirms the plan is entirely additive — no existing interfaces need breaking changes for the core sandbox functionality. The plan's dependency declaration ("depends on Epics 2.0–6.0") aligns with this: the hooks these symbols need are delivered by prior epics, not yet present in the codebase.

ℹ️ **I3 — MCP and tools dispatch layers omitted from COMPONENTS_TOUCHED**

Spot-check surfaced `internal/mcp/handlers.go` and `internal/tools/dispatch.go` as highly likely integration points for any new agent-callable tools. If `run_tests` and `run_script` follow the established pattern (they should, per codebase conventions), they will need registration in both files. This is a fifth component area beyond the three the plan names, reinforcing that /init-plan is the correct pipeline.

### Verification context

- Refinement depth: spot-check
- Derived TASK_COUNT: 5 (limit: 6)
- Derived COMPONENT_COUNT: 3 (limit: 2)
- COMPONENTS_TOUCHED: CLI/commands layer, sandbox/executor module, findings/report layer
- VISUAL_SURFACE: false
- HAS_GATED_WORK: false
- HAS_CROSS_SYSTEM: true (sandbox crosses into Docker/Podman runtime; findings schema change)
- Cited references checked: 1 file path (`.atcr/config.yaml`), 4 symbols (`run_tests`, `run_script`, `evidence_exec`, `repro`)
- Codebase search queries (spot-check): ["sandbox|container|executor|docker|podman", "exec_mode|exec_enabled|--exec|execution.mode", "findings\.json|evidence_exec|output_excerpt|exit_code"]
