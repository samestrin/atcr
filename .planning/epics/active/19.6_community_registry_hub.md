# Epic Plan 19.6: Default Community Registry Hub

- **Estimated time**: TBD
- **Tasks/Components**: TBD
- **Execution**: init-plan

## Objective

Create a default, out-of-the-box community registry hosted on `atcr.dev` that provides optimized personas, prompts, and default models based on official prompting guides (e.g., Anthropic's Claude guidelines, OpenAI's GPT-4 guidelines) for specific tasks.

## Context

Epic 19.2 enabled `ATCR_REGISTRY_URL` for remote fetching, meaning users can pull a registry from anywhere. However, without a default, community-curated registry, new users are forced to write their own personas and prompt instructions from scratch. A default community registry provides immediate value by shipping with battle-tested prompts tailored to the strengths of specific models for code review tasks.

## Problem Statement

Users need an out-of-the-box experience where `atcr` uses highly effective, model-specific prompts (following official prompting guidelines) without requiring manual configuration. The project currently lacks a central, curated registry to host these assets.

## Proposed Solution

1. **Curate Model-Specific Personas**: Design a set of default personas (e.g., `default-claude`, `default-gpt4`) whose system instructions strictly adhere to the official prompting guides for those respective models (e.g., XML tagging for Claude, specific tone/formatting rules).
2. **Define the Community `registry.yaml`**: Create a `registry.yaml` that maps tasks (like `resolve-td`, `review`) to the best-performing models and personas by default.
3. **Host on `atcr.dev`**: Publish the `registry.yaml` and its associated prompt markdown files to a stable public URL on `atcr.dev` (e.g., `https://atcr.dev/registry.yaml`).
4. **Documentation**: Update `atcr`'s documentation to point to this default registry and explain how users can extend or override it.

## Acceptance Criteria

- [ ] A `registry.yaml` exists containing curated personas for at least 1-2 major models (e.g., Claude 3.5 Sonnet).
- [ ] Prompts are demonstrably aligned with the official prompting guides for their target models.
- [ ] The registry is hosted publicly on `atcr.dev` and fetchable via `ATCR_REGISTRY_URL`.
- [ ] The default install/quickstart documentation instructs users to use this registry (or sets it as the default in code if unset).

## Out of Scope

- An interactive registry "marketplace" website or UI — this is purely a static file distribution initially.
- Dynamic registry generation.

## Dependencies / Related

- Epic 19.2 (Shared Registry Remote Fetch) — provides the engine mechanism to fetch this registry.
