# Epic Plan 36.0: Enterprise Protected Path Overrides (Code Owners Enforcement)

- **Estimated time**: TBD
- **Tasks/Components**: TBD
- **Execution**: init-plan

## Objective

Implement a `.atcr-protected` (or support native `.codeowners`) configuration that identifies high-risk paths within a repository. Changes touching these paths will trigger an enforced "human gate" mode where AI autonomous approval is bypassed and explicit human sign-off is mandated.

## Context

While AI scales the checking of code, it does not own the judgment or accountability. Enterprise organizations have highly sensitive files (e.g., CI/CD workflows, authentication logic, infra-as-code, and billing controllers) where an AI hallucination or a bypass of a "low-risk" assessment could be catastrophic. ATCR needs a mechanism to acknowledge that AI is a sensor, but humans are the accountable owners.

## Proposed Solution

1. **Configuration File:**
   - Introduce `.atcr-protected` schema allowing teams to define glob patterns (e.g., `.github/workflows/**`, `src/auth/**`).
   - Alternatively, parse standard `CODEOWNERS` files.
2. **Review Execution Logic:**
   - When ATCR analyzes the git diff, it checks for intersections with protected paths.
   - If an intersection is found, the Reconciler explicitly flags the PR as `REQUIRES_HUMAN_APPROVAL`, bypassing any automatic merge behavior, regardless of how clean the AI panel's review is.
3. **Integration with CI/CD:**
   - Output specific exit codes or GitHub Check Run statuses (e.g., `action_required`) to halt the merge queue until a human intervenes.

## Acceptance Criteria

- [ ] ATCR correctly parses `.atcr-protected` glob patterns.
- [ ] Diffs containing protected files result in a mandatory human review block.
- [ ] AI review panel still runs on protected files to provide context to the human reviewer, but cannot autonomously approve the PR.

## Components Touched

- `samestrin/atcr-enterprise/internal/config/` (Schema for protected paths)
- `samestrin/atcr-enterprise/internal/git/` (Diff analysis against protected patterns)
- `samestrin/atcr` (Epic 39.0 EnterpriseHooks trigger)

## Refinements (2026-07-17)

This section records findings from `/refine-epic` run on July 17, 2026 11:25:00AM. It is additive — original plan content above is preserved.

### Auto-applied corrections (0)

### Items needing user confirmation (6)

- ⏸️ **Task count mismatch:** Plan claims TBD task(s); derived 3. Either the task list is incomplete or has redundant entries. Suggested actions: review Proposed Solution and define tasks explicitly. Proposed text: "Tasks/Components: 3/3" (was: "TBD")
- ⏸️ **Stale path:** `samestrin/atcr-enterprise/internal/config/` cited in plan but does not exist. Suggested action: update plan to point at `internal/config` if extending OSS, or confirm this is a separate private repository.
- ⏸️ **Stale path:** `samestrin/atcr-enterprise/internal/git/` cited in plan but does not exist. Suggested action: update plan to point at `internal/git` if extending OSS, or confirm this is a separate private repository.
- ⏸️ **Discovery surfaced primary file the plan ignores:** Discovery identified `internal/payload/ignore.go` as the best starting point for OBJECTIVE (reason: Provides the core `ignoreMatcher` implementation which uses `github.com/sabhiram/go-gitignore` to load and match glob patterns. This serves as the reference pattern matching implementation for `.atcr-protected` path rules.). The epic plan does not mention this file. Suggested action: either add it to TASKS / files_to_modify, or document why the plan deliberately avoids it.
- ⏸️ **Reusable component available:** Discovery found `ignoreMatcher` at `internal/payload/ignore.go` (Uses `MatchesPath` to test a relative path against compiled globs.). The plan's T1 "Introduce `.atcr-protected` schema allowing teams to define glob patterns (e.g., `.github/workflows/**`, `src/auth/**`). Alternatively, parse standard `CODEOWNERS` files." appears to create equivalent functionality from scratch. Suggested action: either extend the existing component, or document why a new implementation is needed.
- ⏸️ **Missing file in TASKS:** Discovery recommends modifying `internal/reconcile/gate.go` (reason: Needs to trigger enterprise hooks to check for protected path overrides and flag PRs as REQUIRES_HUMAN_APPROVAL., scope: minor). The plan's TASKS do not mention this file. Suggested action: either add a task for this file, or confirm it's intentionally out of scope.

### Advisory observations (3)

- ℹ️ **Scope-guard violation:** Derived TASK_COUNT=3, COMPONENT_COUNT=3 — exceeds the execute-epic skill's ≤6 tasks / ≤2 components limit. This plan will be rejected by the execute-epic skill and should run through `the init-plan skill @.planning/epics/active/36.0_enterprise_protected_paths.md` for the full sprint pipeline. Refining alone will not unblock /execute-epic.
- ℹ️ **Integration point hint:** Discovery identified `internal/reconcile/gate.go:RunReconcile` as a entry-point (Reconciliation runner where the enterprise plugin trigger (Epic 39.0 EnterpriseHooks) will check for protected path overrides.). The plan's T2 touches this file but doesn't explicitly reference how it connects. May be worth documenting.
- ℹ️ **Test pattern guidance:** Codebase uses go test with tests at internal/reconcile/gate_test.go (naming: *_test.go). Example: internal/payload/ignore_test.go. Confirm the plan's test approach matches.

### Verification context

- Refinement depth: deep
- Derived TASK_COUNT: 3 (limit: 6)
- Derived COMPONENT_COUNT: 3 (limit: 2)
- COMPONENTS_TOUCHED: `samestrin/atcr-enterprise/internal/config/`, `samestrin/atcr-enterprise/internal/git/`, `samestrin/atcr`
- VISUAL_SURFACE: false
- HAS_GATED_WORK: false
- HAS_CROSS_SYSTEM: true
- Cited references checked: 3
- Deep discovery method: semantic
- Deep discovery queries: ["parse gitignore glob patterns", "reconcile human approval block", "check diff paths intersection", "github status check run"]
- Deep discovery match count: 40
- Deep discovery snapshot: /Users/samestrin/Documents/GitHub/atcr/.planning/.temp/refine-epic/codebase-discovery.json (temp-only — not committed)
