# Sprint Plan 15.2: Documentation & Reviewer Personas

- **Estimated time**: 4 days
- **Tasks**: 4
- **Components**: 1
- **Execution**: execute-epic --strong

## Context

A command-line developer utility is only as good as its documentation. Developers need to know:
1. How to run reviews and reconcile findings.
2. How the config schemas are defined.
3. Who the individual reviewer personas are (their specializations, focal points, and limitations).
This sprint addresses porting and refining the `docs.html`, `faq.html`, and `personas.html` layouts.

## Problem Statement

We need to provide:
1. Fully populated and structured CLI instruction documents covering standard commands (`atcr review`, `atcr reconcile`, `atcr scorecard`).
2. An interactive FAQ page handling frequently asked questions regarding offline capabilities, costs, and token consumption.
3. A beautiful "Reviewer Directory" showcasing all 9 panel members (Greta, Bruce, Kai, Mira, Otto, Dax, Idiomatic, Sentinel, Tracer) with consistent visual design cues (a shared top-keyline accent color and code sample outputs).

## Proposed Solution

1. Port mockup structures for [docs.html](file:///Users/samestrin/Documents/GitHub/atcr.dev/the-artifact/mockups/docs.html) and [faq.html](file:///Users/samestrin/Documents/GitHub/atcr.dev/the-artifact/mockups/faq.html) to the production source.
2. Port and format [personas.html](file:///Users/samestrin/Documents/GitHub/atcr.dev/the-artifact/mockups/personas.html) structure, extending the mockup's 6 dossier cards with 3 additional cards (`idiomatic`, `sentinel`, `tracer`) to cover the full current 9-persona roster.
3. Ensure absolute styling consistency with the shared CSS layout tokens (IBM Plex fonts and HSL colors).

## User Personas

### Primary: Lead Architect (Alistair)
- Needs to review configuration structures and custom rules.
- Wants to understand which reviewer personas (e.g. Mira for security, Otto for architecture) should be included in their team review panels.

## User Journeys

### Reviewing Personas & Reading Docs
1. Navigates to the "Personas" page.
2. Reads the dossiers for each model persona.
3. Navigates to the "Docs" page to see how to enable specific personas in their `.atcr/config.yaml` configuration file.
4. Copies example configuration values from the documentation block.

## Acceptance Criteria

- [ ] AC1: `docs.html` contains detailed references for command execution, configuration parameters, and environment keys, sourced from the current `docs/` tree (`docs/README.md` and linked pages), not mockup placeholder copy.
- [ ] AC2: `faq.html` provides a clean accordion-style layout or clear text block segments matching the design theme.
- [ ] AC3: `personas.html` renders all 9 reviewers (`bruce`, `greta`, `kai`, `mira`, `otto`, `dax`, `idiomatic`, `sentinel`, `tracer`) as distinct cards with a top color-keyline in the shared `--persona` accent color (per the mockup's design system).
- [ ] AC4: Canonical findings on the persona cards are rendered using the dark code artifact block styling.

## Success Criteria

### Functional
- [ ] Tab and anchor links navigate correctly to page segments without offset errors.
- [ ] High-contrast readability is preserved for long-form prose.

## Technical Constraints

- HTML files must use standard relative links (`index.html`, `docs.html`, etc.) for portability.
- Avoid external CSS libraries; styling details must reside purely in `styles.css`.

## Out of Scope

- Integrating live leaderboard data (covered in Sprint 15.3).
- Deploying the site to production host environments (covered in Sprint 15.3).

## Dependencies

- Roster definitions of reviewers from Epic 9.0.
- Mockup pages from [mockups directory](file:///Users/samestrin/Documents/GitHub/atcr.dev/the-artifact/mockups/).
- Current CLI/config reference content from [`docs/README.md`](file:///Users/samestrin/Documents/GitHub/atcr/docs/README.md) and its linked pages (`docs/architecture.md`, `docs/personas-authoring.md`, `docs/personas-install.md`) — realigned as the single source of truth by completed Epic 15.0; `docs.html` content must be sourced from here, not invented or copied from mockup placeholder text.

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Configuration drift between codebase and docs | High | Medium | Verify that code snippets in `docs.html` match the active CLI flags and config structures in the main codebase |

## References

- [personas.html mockup](file:///Users/samestrin/Documents/GitHub/atcr.dev/the-artifact/mockups/personas.html)
- [docs.html mockup](file:///Users/samestrin/Documents/GitHub/atcr.dev/the-artifact/mockups/docs.html)

## Refinements (2026-07-02)

This section records findings from `/refine-epic` run on July 02, 2026 08:40:49AM. It is additive — original plan content above is preserved.

### Auto-applied corrections (4)

- ✅ **Stale sprint numbering (title):** "# Sprint Plan 14.2: Documentation & Reviewer Personas" → "# Sprint Plan 15.2: Documentation & Reviewer Personas". Confirmed against the parent index (`.planning/epics/plans/15.0_website_launch_index.md`, which lists this file as "Sprint 15.2") and the filename itself (`15.2_docs_and_personas.md`).
- ✅ **Stale sprint numbering (Out of Scope, leaderboard):** "(covered in Sprint 14.3)" → "(covered in Sprint 15.3)" for the leaderboard-data bullet. Confirmed: `.planning/epics/active/15.3_leaderboard_and_deployment.md` is the actual sibling sprint that covers leaderboard integration.
- ✅ **Stale sprint numbering (Out of Scope, deployment):** "(covered in Sprint 14.3)" → "(covered in Sprint 15.3)" for the deployment bullet, same evidence as above.
- ✅ **Stale config file citation:** User Journeys step 3, `` `atcr.yaml` `` → `` `.atcr/config.yaml` ``. Verified against `cmd/atcr/init.go:51,77` — the CLI writes and reads `.atcr/config.yaml`, never a bare `atcr.yaml`. This is the exact drift the plan's own Risks table already warns about ("Configuration drift between codebase and docs").

### Items needing user confirmation (1 open, 5 applied)

- ⏸️ **OPEN — Cross-repo deliverable target is undefined and likely blocks `/execute-epic`:** The plan's actual deliverable (`docs.html`, `faq.html`, `personas.html`, `styles.css`) must land in the **`atcr.dev`** repository, not this repo (`atcr`). Evidence: the parent index `.planning/epics/plans/15.0_website_launch_index.md` states "AC1: All 3 sprints completed and integrated into the `atcr.dev` repository," and sibling sprint 15.3 states "AC5: Merge events to the `main` branch of the `atcr.dev` repo trigger a GitHub Action." `atcr.dev` is a separate git repository (`/Users/samestrin/Documents/GitHub/atcr.dev`, own `.git` and `.planning/`) — not a subdirectory or submodule of this repo. No website/site source tree (`docs.html`, `faq.html`, `personas.html`, `styles.css`, or any `site/`/`www/`/`public/` directory) exists anywhere in this repo. `/execute-epic` operates entirely within the current git repo — branch, commit, PR, and merge all happen in `atcr` — and has no mechanism to write files into or open a PR against a sibling repository. Suggested action: either (a) move this epic plan into `atcr.dev/.planning/epics/active/` and run `/execute-epic` from that repo instead, or (b) if the site is genuinely meant to be built inside this repo first and published later, add an explicit destination path to Proposed Solution / COMPONENTS_TOUCHED. Still unresolved — this is the one item that needs a decision before `/execute-epic` can run.

- ✅ **APPLIED — AC3's per-persona CSS variables didn't exist in the source mockup.** AC3 now reads "...with a top color-keyline in the shared `--persona` accent color (per the mockup's design system)" instead of citing invented `--src-greta`/`--src-mira` variables, matching `the-artifact/mockups/styles.css:24,66,278` and `design-system.md:100`.

- ✅ **APPLIED — Persona roster mismatch (mockup had 6, codebase has 9).** AC3, Problem Statement #3, and Proposed Solution item 2 now name all 9 reviewers (`bruce`, `greta`, `kai`, `mira`, `otto`, `dax`, `idiomatic`, `sentinel`, `tracer`) and Proposed Solution item 2 explicitly calls for 3 additional dossier cards beyond the mockup's 6.

- ✅ **APPLIED — Task count mismatch.** Header "Tasks: 5" corrected to "Tasks: 4" to match the derived count and Proposed Solution's actual scope.

- ✅ **APPLIED — Components count mismatch.** Header "Components: 3" corrected to "Components: 1" (all site files are co-located).

- ✅ **APPLIED — Missing dependency on the existing markdown docs tree.** Dependencies now cites `docs/README.md` and its linked pages (`docs/architecture.md`, `docs/personas-authoring.md`, `docs/personas-install.md`) as the required source for `docs.html` content, and AC1 now states content must be sourced from the current `docs/` tree, not mockup placeholder copy.

Applied 2026-07-02 per explicit user follow-up to the `/refine-epic` run above; see the plan body (header, Context, Proposed Solution, Acceptance Criteria, Dependencies) for the resulting text.

### Advisory observations (3)

- ℹ️ **Sibling sprint has the identical stale-numbering bug:** `.planning/epics/active/15.3_leaderboard_and_deployment.md` also opens with "# Sprint Plan 14.3: Leaderboard & Production Deployment" (should be 15.3). Out of scope for this run (only `15.2` was targeted) — consider running `/refine-epic @.planning/epics/active/15.3_leaderboard_and_deployment.md` next.
- ℹ️ **`--visual` recommended once the destination is resolved:** VISUAL_SURFACE derives to true (the entire deliverable is user-visible HTML/CSS), but the plan doesn't state this explicitly. Once the cross-repo destination question above is resolved, run `/execute-epic --visual` (from whichever repo actually holds the site source) for browser-based verification of the ported pages.
- ℹ️ **`--strong` appears unnecessary at current derived complexity:** The plan declares `Execution: execute-epic --strong`, which raises the scope guard to ≤12 tasks / ≤3 components. Derived TASK_COUNT=4 and COMPONENT_COUNT=1 comfortably fit the *default* ≤6/≤2 guard. Not a defect — just noting `--strong` isn't buying anything here; the cross-repo issue above is the actual blocker regardless of this flag.

### Verification context

- Refinement depth: spot-check
- Derived TASK_COUNT: 4 (limit: 6 default / 12 with `--strong`)
- Derived COMPONENT_COUNT: 1 (limit: 2 default / 3 with `--strong`)
- COMPONENTS_TOUCHED: atcr.dev site source (`docs.html`, `faq.html`, `personas.html`, `styles.css` — co-located; destination repo/path unresolved, see findings above)
- VISUAL_SURFACE: true (derived; not stated by plan)
- HAS_GATED_WORK: false
- HAS_CROSS_SYSTEM: true (deliverable target is a separate git repository, `atcr.dev`)
- Cited references checked: 12 (3 mockup file links, `styles.css`, `index.html`, `atcr.yaml`/`.atcr/config.yaml`, `--src-greta`/`--src-mira` CSS vars, `atcr review`/`atcr reconcile`/`atcr scorecard` CLI commands, Epic 9.0 reference)
- Codebase search queries (spot-check): ["atcr.dev the-artifact mockups destination", "persona reviewer color keyline css variables", "atcr config yaml vs .atcr/config.yaml CLI commands"]
