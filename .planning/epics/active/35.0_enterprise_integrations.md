# Epic Plan 35.0: Enterprise Ticketing and PR Integrations

- **Estimated time**: TBD
- **Tasks/Components**: TBD
- **Execution**: init-plan

## Objective

Build native integrations with enterprise ticketing systems (Jira, Linear) and CI/CD version control platforms (GitHub, GitLab). This fulfills "Layer 4: Enterprise Integrations", allowing `atcr` findings to seamlessly enter existing enterprise workflows without manual copy-pasting.

## Context

A major blocker for adopting custom or CLI-based AI code review tools is the lack of integration with developer workflows. While `atcr` deduplicates findings and scores their confidence brilliantly (Epic 13.0 / Reconciler), developers still have to manually translate the `td-stream-merged.txt` output into actionable Jira tickets or PR comments.

To compete with "Buy" platforms that cost millions of dollars, `atcr` needs lightweight but powerful hooks to push its reconciled findings directly into the systems where developers already live.

## Proposed Solution

1. **Ticketing Integration (Enterprise Repo):**
   - Create `internal/integrations/tickets` in the private repo.
   - Add functionality to parse `td-stream-merged.txt` and automatically create Linear or Jira issues for findings above a certain confidence threshold.
   - Include links back to the git commit and file/line number in the ticket body.
2. **Advanced PR Commenting Hooks (Enterprise Repo):**
   - Create `internal/integrations/vcs` in the private repo.
   - Build GitLab CI and Bitbucket templates/wrappers. (Basic GitHub PR comments remain in the public free tier via Epic 7.3 to drive adoption).
3. **Configuration Driven:**
   - Allow teams to map `atcr` personas or severity levels to specific Jira labels, Linear projects, or PR comment behaviors via `.atcr/config.yaml`.

## Acceptance Criteria

- [ ] `atcr` can authenticate and create tickets in Jira and Linear based on config settings.
- [ ] High-confidence technical debt findings are automatically pushed to the configured ticketing board.
- [ ] Templates exist for GitHub Actions and GitLab CI to post findings as inline comments on Pull Requests.
- [ ] Integration behavior (thresholds, labels, target projects) is fully configurable.

## Components Touched

- `samestrin/atcr-enterprise/internal/integrations/` (New packages for tickets and vcs)
- `samestrin/atcr-enterprise/cmd/atcr-enterprise/` (CLI hooks)
- `.atcr/config.yaml` (New schema for integration targets)

## Refinements (2026-07-17)

This section records findings from `/refine-epic` run on July 17, 2026 08:31:30AM. It is additive — original plan content above is preserved.

### Auto-applied corrections (0)

None.

### Items needing user confirmation (5)

- ⏸️ **Missing Tasks section**: The epic plan lists "Tasks/Components: TBD" and does not contain a discrete task backlog (`## Tasks` or `## Work Items` section). To run with `/execute-epic`, a task list of 1-6 concrete, sequential tasks is required.
- ⏸️ **Header metadata mismatch**: Plan claims "TBD" tasks/components but has 3 components touched and implies at least 6 implementation tasks. Propose replacing "Tasks/Components: TBD" with "6 Tasks / 3 Components".
- ⏸️ **GitHub Actions templates overlap**: The Proposed Solution states "Basic GitHub PR comments remain in the public free tier via Epic 7.3" but the Acceptance Criteria lists "Templates exist for GitHub Actions and GitLab CI to post findings as inline comments on Pull Requests." If GitHub Actions templates are already built in the public tier, it should be clarified whether this epic extends them or if GitHub CI is out-of-scope for the private tier.
- ⏸️ **Ignored primary file**: Discovery identified [github.go](file:///Users/samestrin/Documents/GitHub/atcr/cmd/atcr/github.go) as the best starting point/reference pattern for implementing VCS and ticketing integration entry points, but the plan does not reference it.
- ⏸️ **Reusable component available**: Discovery found [finding.go](file:///Users/samestrin/Documents/GitHub/atcr/internal/reconcile/finding.go#L1) (reconcile.JSONFinding) as the structured format for reconciled findings. The ticketing integration should consume this or `td-stream-merged.txt` rather than parsing from scratch.

### Advisory observations (3)

- ℹ️ **Scope-guard violation**: Derived TASK_COUNT=6, COMPONENT_COUNT=3 — exceeds the execute-epic skill's ≤6 tasks / ≤2 components limit. This plan will be rejected by the execute-epic skill and should run through `/init-plan` for the full sprint pipeline.
- ℹ️ **Integration point hint**: Discovery identified [newGithubCmd](file:///Users/samestrin/Documents/GitHub/atcr/cmd/atcr/github.go#L48) as the CLI command entry point. Ticketing and VCS CLI integration hooks in `cmd/atcr-enterprise/` should follow this command structure.
- ℹ️ **Test pattern guidance**: Codebase uses `go test` with tests at `samestrin/atcr-enterprise/internal/integrations/...` (naming: `*_test.go`). Example: [client_test.go](file:///Users/samestrin/Documents/GitHub/atcr/internal/ghaction/client_test.go). The plan should document the testing strategy (TDD) for the integrations.

### Verification context

- Refinement depth: deep
- Derived TASK_COUNT: 6 (limit: 6)
- Derived COMPONENT_COUNT: 3 (limit: 2)
- COMPONENTS_TOUCHED: `samestrin/atcr-enterprise/internal/integrations/`, `samestrin/atcr-enterprise/cmd/atcr-enterprise/`, `.atcr/config.yaml`
- VISUAL_SURFACE: false
- HAS_GATED_WORK: false
- HAS_CROSS_SYSTEM: true
- Cited references checked: 1
- Codebase search queries (spot-check): ["jira linear ticketing", "gitlab bitbucket pr integration", "reconciled findings parse"]
- Deep discovery method: semantic
- Deep discovery queries: ["create jira linear ticketing issue", "parse td-stream-merged.txt findings", "gitlab CI bitbucket PR commenting templates", "atcr config mapping labels severity"]
- Deep discovery match count: 40
- Deep discovery snapshot: /Users/samestrin/Documents/GitHub/atcr/.planning/.temp/refine-epic/codebase-discovery.json (temp-only — not committed)

