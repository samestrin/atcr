# Epic Plan 22.2: Extract Shared Wasm Guest ABI Across Parsers

- **Estimated time**: 1 day
- **Tasks/Components**: 2/1

## Objective

Extract the duplicated alloc/free/emit/pins Wasm guest ABI boilerplate — currently copy-pasted across `goparser`, `pyparser`, and `braceparser` — into one shared internal guest package, now that the project's own documented extraction threshold ("parser count > 2") has been crossed.

## Context

Three separate TD rows (`braceparser/main.go:1`, `braceparser/main.go:20`, `goparser/main.go:39`) flag the same duplication from three different sprints (13.1, 13.3, 13.4), each citing an epic plan that turns out to be unrelated or nonexistent for this specific concern. The underlying ABI code — alloc/free/emit/pins guest boilerplate, plus the unsafe pointer-packing that assumes a non-moving Go GC — is duplicated three times and was explicitly flagged as "revisit if parser count grows," which has now happened.

## Task Breakdown

1. Extract the shared alloc/free/emit/pins guest ABI into one internal guest package, imported by `goparser`, `pyparser`, and `braceparser`'s `main.go` (via `go.mod` replace directives coordinated in `build.sh`, matching the existing Wasm build convention).
2. Add a build-time note (doc comment) pinning the non-moving-GC pointer-packing assumption in the shared package, so a future moving GC change is a documented, discoverable risk rather than a silent one.

## Acceptance Criteria

- [ ] `goparser`, `pyparser`, and `braceparser` each import the shared guest ABI package instead of defining their own alloc/free/emit/pins boilerplate.
- [ ] The non-moving-GC pointer-packing assumption is documented once, in the shared package, not repeated per-parser.
- [ ] `go build ./...` succeeds for all three parser Wasm modules; existing `internal/astgroup` tests pass unchanged.

## Components Touched

- `internal/astgroup/parsers/src/goparser`
- `internal/astgroup/parsers/src/pyparser`
- `internal/astgroup/parsers/src/braceparser`
- `build.sh`

## Out of Scope

- Switching to a moving GC or an explicitly reserved arena — documenting the assumption is in scope, changing the allocation strategy is not.
- Any change to parser-specific structural-hash logic (see epic 22.3 for the separate `pyparser` quote-awareness gap).

## Dependencies

- None.

## Source TD Items

`.planning/technical-debt/README.md` — `internal/astgroup/parsers/src/braceparser/main.go:1`, `internal/astgroup/parsers/src/braceparser/main.go:20`, `internal/astgroup/parsers/src/goparser/main.go:39`.
