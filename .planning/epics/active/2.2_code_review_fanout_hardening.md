# Epic Plan 2.2: Code Review Fan-out Hardening

- **Estimated time**: 2-3 days

## Context

The code review fan-out skill uses `~/.config/llm-tools/agents/registry.yaml` to orchestrate multi-agent code reviews. Each agent has a specialized role (correctness, performance, style, etc.) defined in its `system_prompt`. The fan-out sends the same diff to all agents, collects their `td-stream.txt` outputs, and merges them into a unified findings list.

Recent reconciles revealed two critical failures in this pipeline:

1. **Reviewer name hallucination**: The fan-out trusts the model's self-reported reviewer name (from the REVIEWER column in `td-stream.txt`) instead of stamping the registry agent key. When `bob2-backup` (NVIDIA-Nemotron-3-Super-120B-A12B) ran, it wrote "ace" as its reviewer name instead of "bob2-backup", making it impossible to trace findings back to the actual model.

2. **Role hallucination / runaway output**: `bob2-backup` was configured as a performance reviewer but produced 521 findings (498 blank-line style items), flooding the merged stream with noise. All other reviewers produced 0-4 findings each. The 498 style findings from one weak model drowned out the 3 HIGH and 8 MEDIUM findings from stronger models.

## Problem Statement

The fan-out pipeline has no guardrails against:
- **Unconstrained output volume**: A single agent can produce 500+ findings, overwhelming the merge/dedupe step and the TD README.
- **Role drift**: Models ignore their `system_prompt` role specialization and review outside their scope.
- **Identity drift**: Models invent reviewer names instead of using their assigned identity.

These failures waste tokens (generating findings that get discarded), clutter the TD README (486 blank-line items), and make it impossible to attribute findings to the correct model for quality tracking.

## Proposed Solution

Three-layer defense:

### Layer 1: Fan-out post-processing (enforcement)
- **Stamp reviewer name**: After collecting each agent's `td-stream.txt`, overwrite the REVIEWER column with the agent key from `registry.yaml`. Do not trust the model's self-reported name.
- **Enforce output caps**: Read `max_findings` from registry (if set) and truncate the agent's output to that many rows. Log a warning if truncated.
- **Enforce severity floor**: Read `min_severity` from registry (if set) and drop findings below that threshold. Log dropped count.

### Layer 2: Registry configuration (per-agent constraints)
Add optional fields to `registry.yaml`:

```yaml
agents:
  bob2-backup:
    provider: litellm
    model: NVIDIA-Nemotron-3-Super-120B-A12B
    temperature: 0.3
    lane: parallel
    scope: ["performance", "efficiency"]  # Restrict review focus
    min_severity: "MEDIUM"                # Drop LOW findings
    max_findings: 20                      # Cap output volume
    system_prompt: |
      You are Bob2, a code review agent specializing in performance.
      ...
```

| Field | Type | Purpose | Default |
|-------|------|---------|---------|
| `scope` | `string[]` | Categories this agent should focus on. Injected into prompt. | All categories |
| `min_severity` | `enum` | Minimum severity to report. Findings below this are dropped in post-processing. | `LOW` |
| `max_findings` | `int` | Maximum findings this agent can emit. Output truncated if exceeded. | Unlimited |

### Layer 3: Model selection (avoid weak models for open-ended review)
- Document which models are suitable for open-ended code review vs. narrow scopes.
- `bob2-backup` (Nemotron) demonstrated it cannot follow role instructions — remove from fan-out or constrain to `min_severity: HIGH` + `max_findings: 5`.
- Strong models (qwen-3.7-plus, deepseek-4, kimi-k2.6) can handle broader scopes.

## Requirements

### Functional
- Fan-out stamps reviewer name from `registry.yaml` agent key onto each `td-stream.txt` row after collection.
- Fan-out reads `scope`, `min_severity`, `max_findings` from registry (if present) and enforces them in post-processing.
- Fan-out logs warnings when output is truncated or findings are dropped.
- Reconcile skill correctly attributes findings to the registry agent name, not the model's self-reported name.

### Non-Functional
- Post-processing adds < 1 second per agent (deterministic filtering, no LLM calls).
- No breaking changes to existing `td-stream.txt` format — new columns are optional.
- Registry.yaml remains backward-compatible — new fields are optional with sensible defaults.

## Technical Approach

### Option A: Post-processing in fan-out skill (recommended)
- After the fan-out collects each agent's `td-stream.txt`, run a post-processing step:
  1. Parse each row.
  2. Overwrite REVIEWER column with agent key.
  3. Filter by `min_severity` (if set).
  4. Truncate to `max_findings` (if set).
  5. Write back to `td-stream.txt`.
- Pros: No changes to agent prompts or model behavior. Deterministic. Works with any model.
- Cons: Wastes tokens generating findings that get discarded.

### Option B: Prompt injection (scope enforcement)
- Read `scope` from registry and inject into the agent's system prompt before sending to the model.
- Example: "You are Bob2, specializing in performance and efficiency. Only report findings in these categories. Do not report style, naming, or formatting issues."
- Pros: Reduces wasted tokens (model doesn't generate out-of-scope findings).
- Cons: Model-dependent — weak models may still ignore the instruction.

### Option C: Both A + B (defense in depth)
- Inject scope into prompt (Option B) to reduce wasted tokens.
- Post-process to enforce caps and stamp reviewer name (Option A) as a safety net.
- Pros: Best of both worlds. Prompt guides the model; post-processing enforces hard limits.
- Cons: Slightly more complex fan-out logic.

**Recommended: Option C**

## Implementation Components

1. **Fan-out post-processing module**
   - Function: `postprocess_td_stream(agent_key, td_stream_path, registry_config)`
   - Steps:
     1. Parse `td-stream.txt` into rows.
     2. For each row: overwrite REVIEWER column with `agent_key`.
     3. Filter rows by `min_severity` (if set in registry).
     4. Truncate to `max_findings` (if set).
     5. Write back to `td-stream.txt`.
   - Log: "Agent {agent_key}: {original_count} findings → {filtered_count} after post-processing (dropped {dropped_count}, truncated {truncated_count})"

2. **Registry schema extension**
   - Add optional fields: `scope`, `min_severity`, `max_findings`.
   - Update registry.yaml parser to read these fields.
   - Defaults: `scope = []` (all categories), `min_severity = "LOW"`, `max_findings = null` (unlimited).

3. **Prompt injection hook**
   - Before sending agent's system prompt to the model, check if `scope` is set.
   - If set, append: "Focus exclusively on these categories: {scope}. Do not report findings outside these categories."
   - This is a soft constraint — post-processing enforces the hard limit.

4. **Reconcile skill update**
   - No changes needed — the reconcile skill already reads the REVIEWER column from `td-stream.txt`. Since the fan-out now stamps the correct reviewer name, the reconcile skill will attribute findings correctly.

5. **Registry.yaml updates**
   - Add `scope`, `min_severity`, `max_findings` to `bob2-backup`:
     ```yaml
     bob2-backup:
       scope: ["performance", "efficiency"]
       min_severity: "MEDIUM"
       max_findings: 20
     ```
   - Optionally add constraints to other weak models (gemma-4-31b, nemotron-3-nano).

## Constraints

- Must remain backward-compatible with existing `registry.yaml` files that don't have the new fields.
- Post-processing must be deterministic (no LLM calls) — it's a simple filter/truncate operation.
- The fan-out skill must not break if `scope`, `min_severity`, or `max_findings` are missing.

## Out of Scope

- Changes to the reconcile skill's deduplication logic (it already works correctly once reviewer names are stamped).
- Changes to the TD README format (it already supports arbitrary reviewer names).
- Model-specific prompt tuning (that's a separate effort — this epic adds the infrastructure to enforce constraints, not the prompts themselves).
- Removing weak models from the registry (that's a policy decision — this epic adds the guardrails to constrain them).

## Acceptance Criteria

- [ ] AC1: Fan-out stamps the registry agent key onto the REVIEWER column of each `td-stream.txt` row after collection (verified by inspecting `td-stream.txt` — no model-invented reviewer names).
- [ ] AC2: Fan-out reads `min_severity` from registry (if set) and drops findings below that threshold (verified by setting `min_severity: "MEDIUM"` on a test agent and confirming no LOW findings in output).
- [ ] AC3: Fan-out reads `max_findings` from registry (if set) and truncates output to that many rows (verified by setting `max_findings: 5` on a test agent and confirming ≤ 5 findings in output).
- [ ] AC4: Fan-out logs warnings when output is truncated or findings are dropped (verified by checking fan-out logs for "dropped X findings" or "truncated to Y findings" messages).
- [ ] AC5: Registry.yaml parser accepts the new `scope`, `min_severity`, `max_findings` fields without errors (verified by loading a registry with these fields).
- [ ] AC6: Reconcile skill correctly attributes findings to the registry agent name, not the model's self-reported name (verified by running a reconcile on a fan-out with a model that previously hallucinated its name — findings should be attributed to the registry agent key).
- [ ] AC7: `bob2-backup` is configured with `scope: ["performance", "efficiency"]`, `min_severity: "MEDIUM"`, `max_findings: 20` (verified by inspecting `registry.yaml`).

## Success Criteria

- [ ] No model-invented reviewer names in `td-stream.txt` (all reviewer names match registry agent keys).
- [ ] No single agent produces > 50 findings in a fan-out (enforced by `max_findings`).
- [ ] Reconcile output correctly attributes findings to registry agents (verified by reconcile summary).
- [ ] TD README is not flooded with style findings from non-style reviewers (enforced by `scope` + `min_severity`).

## Risks

| Risk | Mitigation |
|------|------------|
| Post-processing breaks existing fan-out workflows | New fields are optional with sensible defaults; post-processing is a no-op if fields are missing. |
| Prompt injection weakens model output quality | Scope is a soft constraint; post-processing enforces hard limits. If prompt injection causes issues, disable it and rely on post-processing only. |
| Weak models still produce low-quality output even with constraints | Document model suitability in registry comments; recommend removing chronically underperforming models from the fan-out. |

## Rollback Plan

Post-processing is additive. If issues occur:
1. Remove `scope`, `min_severity`, `max_findings` from registry.yaml (fan-out falls back to defaults: no constraints).
2. Disable reviewer name stamping (fan-out uses model-reported names as before).
3. Existing fan-out workflows continue unchanged.

## References

- Registry location: `~/.config/llm-tools/agents/registry.yaml`
- Fan-out skill: `/code-review --multi-agent`
- Reconcile skill: `/reconcile-code-review`
- Example of failure: `.planning/epics/code-reviews/2.1_manifest_review_stage_recording/multi-agent/raw/bob2-backup/td-stream.txt` (521 findings, 498 blank-line, reviewer name "ace" instead of "bob2-backup")

## Clarifications

### Recorded 2026-06-13 (rubber duck — /execute-epic)

**Key Decisions:**
- Implement the guardrails in the atcr Go codebase. The epic's terminology borrowed from the old llm-tools skill, but the problems (identity drift, role drift, volume floods) are real atcr problems. atcr uses `~/.config/atcr/registry.yaml` and `findings.txt`; the three registry fields (`scope`, `min_severity`, `max_findings`) are absent from `internal/registry/config.go` and must be added in Go.
- AC1 and AC6 are already structurally satisfied — NO new work. The engine hard-stamps REVIEWER from the registry agent key (`internal/fanout/artifacts.go:141`); `ParseModelOutput` enforces 7 model columns only, so a model can never self-attribute REVIEWER (`internal/stream/parser.go:75-76`); overflow folds into EVIDENCE not REVIEWER (`internal/stream/parser.go:103-108`).
- `scope` is a SOFT prompt-injection constraint only — no post-processing hard drop of out-of-category findings. Only `min_severity` and `max_findings` are enforced in post-processing.
- Enforcement lives in the fan-out per-source path inside `findingsFor()` (`internal/fanout/artifacts.go`), immediately after `ParseModelOutput()` stamps REVIEWER and before per-agent `findings.txt` is written. The reconciler stays source-agnostic.
- `max_findings` truncation: sort by severity descending (CRITICAL>HIGH>MEDIUM>LOW) then keep top N, reusing the canonical `severityRank` ordering (`internal/reconcile/merge.go:17`, `sortMerged` at `internal/reconcile/reconcile.go:104-116`). Preserve-emission-order is rejected — it would repeat the motivating incident (498 LOW findings before 3 HIGH ones).
- AC7 example config goes in `docs/registry.md` as a fenced block on an atcr-named agent (e.g. `bruce-backup`). No in-repo `registry.yaml` is created (the user registry is external and untracked).

**Scope Boundaries:**
- IN scope: add `scope` / `min_severity` / `max_findings` to `AgentConfig` + load-time validation; soft scope prompt injection into persona prompts; fan-out per-source enforcement of `min_severity` (drop below floor) and `max_findings` (severity-sorted truncate) with warning logs; `docs/registry.md` field docs + example block; tests for all of the above.
- OUT of scope: AC1/AC6 reviewer-name stamping (already done); reconcile-stage changes; in-repo `registry.yaml`; hardening of the external llm-tools skill files; a hard category drop for `scope`.

**Technical Approach:**
- New `AgentConfig` fields: `Scope []string` (`yaml:"scope,omitempty"`), `MinSeverity string` (`yaml:"min_severity,omitempty"`), `MaxFindings *int` (`yaml:"max_findings,omitempty"`). Validate `min_severity` against the severity rubric (CRITICAL/HIGH/MEDIUM/LOW) and `max_findings` as a positive int. Backward-compatible: all optional, no-op when unset.
- Post-processing in the fan-out per-source path filters by `min_severity` then truncates to `max_findings` (severity-sorted), logging dropped/truncated counts.
- Scope injection appends a soft focus instruction into the persona prompt when `scope` is set.

**Original Questions and Answers:**
1. Q: Target system — atcr Go codebase, or wrong repo (external llm-tools skills)?
   A: atcr Go codebase. Add the three fields to `internal/registry/config.go`. (95%)
2. Q: Treat AC1/AC6 as already done and focus on min_severity/max_findings/scope?
   A: Yes — AC1/AC6 structurally satisfied, no new work. (99%)
3. Q: scope semantics — prompt-inject + hard drop / prompt-inject only / defer?
   A: (b) prompt-inject only, no hard drop. No AC requires a category drop. (82%)
4. Q: Enforcement location — fan-out per-source path or reconcile?
   A: Fan-out per-source path inside `findingsFor()` after REVIEWER stamping. (97%)
5. Q: AC7 example config — skip / doc example / name a real agent?
   A: (b) doc example block in `docs/registry.md` on an atcr-named agent. (93%)
6. Q: max_findings truncation order — severity-sorted or emission order?
   A: Severity-sorted (CRITICAL>HIGH>MEDIUM>LOW), keep top N. (97%)
