# Epic Plan 34.0: Enterprise Immutable Audit Logging

- **Estimated time**: TBD
- **Tasks/Components**: TBD
- **Execution**: init-plan

## Objective

Implement strict, SIEM-compatible JSON Lines (JSONL) audit logging for every `atcr` execution. This satisfies "Layer 5: Governance and Audit" enterprise requirements, allowing organizations in highly regulated environments (e.g., FedRAMP, HIPAA) to adopt `atcr` by guaranteeing a tamper-evident record of all AI interactions.

## Context

Enterprises evaluating the "Build vs. Buy" path for AI code review often reject "Buy" solutions because commercial vendors cannot provide the level of granular audit logging required for compliance. When engineers run AI models against proprietary codebases, compliance teams need to know exactly what was sent, to which model, by whom, and what the model returned.

Currently, `atcr` outputs developer-friendly artifacts (`review.md`, `td-stream.txt`) but lacks a structured, machine-readable audit trail. By generating a definitive JSONL audit log, `atcr` provides the necessary observability for enterprise SIEM ingestion (e.g., Splunk, Datadog), making it enterprise-ready out of the box.

## Proposed Solution

1. **Audit Logger Package:** Create `internal/audit` to handle structured JSON logging.
2. **Execution Context Capture:** For every model invocation, log a JSON payload containing:
   - Timestamp and Execution ID
   - User/Runner Identity (from environment/git context)
   - Code Context Sent (file paths, block hashes, or exact snippets)
   - Model Provider and Model Name
   - Exact Prompt Provided
   - Exact Model Response
   - Token Usage and Estimated Cost
3. **Log Sinks:** Configure the audit logger to write to a dedicated, append-only file (e.g., `.atcr/audit.log`) or stream to a configurable endpoint.
4. **Log Masking:** Ensure sensitive environment variables or explicit secrets (though already filtered by `atcrignore`) are categorically excluded from the audit log payload.

## Acceptance Criteria

- [ ] Every model invocation generates a discrete, structured JSONL entry.
- [ ] Log entries include execution identity, model metadata, full prompt/response payloads, and token telemetry.
- [ ] The audit log can be successfully ingested and parsed by standard SIEM tools (e.g., Splunk).
- [ ] Sensitive secrets are structurally omitted or masked from the log payloads.

## Components Touched

- `samestrin/atcr-enterprise/internal/audit/` (New package)
- `samestrin/atcr` (Epic 39.0 EnterpriseHooks trigger)
- `samestrin/atcr-enterprise/cmd/atcr-enterprise/` (Configuration for audit log path/destination)

## Refinements (2026-07-17)

This section records findings from `/refine-epic` run on 2026-07-17T08:31:00-07:00. It is additive — original plan content above is preserved.

### Auto-applied corrections (0)

### Items needing user confirmation (5)

- ⏸️ **Task count mismatch:** Plan claims TBD task(s); derived 4. Either the task list is incomplete or has redundant entries. Suggested actions: review Proposed Solution and define tasks explicitly. Proposed text: "Tasks/Components: 4/3" (was: "TBD/TBD")
- ⏸️ **Ambiguous T4:** "Ensure sensitive environment variables or explicit secrets..." — too vague to derive a RED→GREEN test. Suggested rewrite: include the specific file:line, the concrete change, and the observable success criterion.
- ⏸️ **Stale path:** `samestrin/atcr-enterprise/internal/audit/` cited in plan but does not exist. Suggested action: update plan to point at `internal/audit` if extending OSS, or confirm this is a separate private repository.
- ⏸️ **Stale path:** `samestrin/atcr-enterprise/cmd/atcr-enterprise/` cited in plan but does not exist. Suggested action: clarify if this is a net-new private repo or typo for `cmd/atcr`.
- ⏸️ **Reusable component available:** Discovery found `internal/audit` already handles an append-only JSONL ledger for Epic 19.1. The plan's T1 appears to create equivalent functionality from scratch. Suggested action: either extend the existing component, or document why a new enterprise-only implementation is needed.

### Advisory observations (1)

- ℹ️ **Scope-guard violation:** Derived TASK_COUNT=4, COMPONENT_COUNT=3 — exceeds the execute-epic skill's ≤6 tasks / ≤2 components limit. This plan will be rejected by the execute-epic skill and should run through `/init-plan` for the full sprint pipeline. Refining alone will not unblock /execute-epic.

### Verification context

- Refinement depth: deep
- Derived TASK_COUNT: 4 (limit: 6)
- Derived COMPONENT_COUNT: 3 (limit: 2)
- COMPONENTS_TOUCHED: `samestrin/atcr-enterprise/internal/audit/`, `samestrin/atcr`, `samestrin/atcr-enterprise/cmd/atcr-enterprise/`
- VISUAL_SURFACE: false
- HAS_GATED_WORK: false
- HAS_CROSS_SYSTEM: true
- Cited references checked: 6
- Deep discovery method: keyword
- Deep discovery queries: `audit, JSONL, execution context, internal/audit, atcr-enterprise, SIEM`
- Deep discovery match count: 6
- Deep discovery snapshot: /Users/samestrin/Documents/GitHub/atcr/.planning/.temp/refine-epic/codebase-discovery.json (temp-only — not committed)
