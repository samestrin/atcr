# Epic Plan 38.0: Enterprise Two-Repo Infrastructure & Module Setup

- **Estimated time**: TBD
- **Tasks/Components**: 5/2
- **Execution**: init-plan

## Objective

Pivot ATCR to a True Open Core (Two-Repo) architecture. Create the private `samestrin/atcr-enterprise` repository, set up its Go module imports to consume the public `atcr` core, and establish the CI/CD pipeline for building the enterprise binary.

## Context

To protect ATCR's enterprise IP (audit logging, ticketing integrations, protected paths) from being trivially bypassed in a public repository, the "license key" model has been abandoned in favor of a dual-repo structure. 

The public repository (`samestrin/atcr`) remains 100% open-source (MIT). The proprietary features will live exclusively in a private repository (`samestrin/atcr-enterprise`). This guarantees that non-paying users physically do not possess the compiled code for enterprise features, making piracy impossible and eliminating the need for offline cryptography checks.

## Proposed Solution

1. **Private Repository Initialization:**
   - Create the private `samestrin/atcr-enterprise` repository.
   - Initialize `go.mod` with `module github.com/samestrin/atcr-enterprise`.
2. **Module Importing:**
   - Import the public core engine: `require github.com/samestrin/atcr vX.Y.Z`.
3. **Core CLI Export (Public Seam):**
   - Refactor `cmd/atcr/main.go` in the public repository to export the root Cobra command constructor (e.g., `NewRootCmd()`) so the enterprise wrapper can import it.
4. **Main Wrapper:**
   - Create `cmd/atcr-enterprise/main.go`. This file will serve as the exact entrypoint for the enterprise binary. It will import the public CLI bootstrapping logic, but inject the proprietary implementations for the `EnterpriseHooks` (defined in Epic 39.0).
5. **CI/CD Pipeline Syncing & Unified Release:**
   - **Continuous Sync Check:** Set up a GitHub Action to automatically test `atcr-enterprise` against the `main` branch of the public `atcr` repo to catch API breakages before they are released.
   - **Unified Release Automation:** Set up a workflow where tagging a release in the public repo (e.g., `v1.2.0`) automatically triggers the private repo to update its `go.mod` dependency to the new tag, compile the `atcr-enterprise` binary, and push it to the private distribution channel.

## Acceptance Criteria

- [ ] The public CLI `cmd/atcr/main.go` exports `NewRootCmd` to allow external import.
- [ ] The `atcr-enterprise` repo successfully builds a binary that runs the full ATCR review engine.
- [ ] The enterprise binary's `go.mod` correctly imports the public `samestrin/atcr` module.
- [ ] A GitHub Action validates that the private repo continues to compile against public updates.
- [ ] A unified release workflow automatically builds and distributes the enterprise binary whenever the public repo is tagged.

## Components Touched

- `samestrin/atcr-enterprise` (New Repository)
- `cmd/atcr-enterprise/main.go` (Entrypoint)

## Refinements (2026-07-17)

This section records findings from `/refine-epic` run on July 17, 2026 11:32:05AM. It is additive — original plan content above is preserved.

### Auto-applied corrections (0)

None.

### Items needing user confirmation (6)

- ✅ **Resolved:** **Task count mismatch:** Metadata updated to `5/2` (5 tasks, 2 components).
- ✅ **Resolved:** **Filename Mismatch:** File renamed to `38.0_enterprise_two_repo_infrastructure.md`.
- ✅ **Confirmed:** **Stale path:** `cmd/atcr-enterprise/main.go` lives in the private repository.
- ✅ **Confirmed:** **Stale path:** `samestrin/atcr-enterprise` refers to the private repository.
- ✅ **Confirmed:** **Symbol not found:** `EnterpriseHooks` dependency on Epic 39.0 noted.
- ✅ **Resolved:** **Discovery surfaced primary file the plan ignores:** Task 3 added to Proposed Solutions to export the root command, and corresponding Acceptance Criteria added.

### Advisory observations (3)

- ℹ️ **Semantic-related code:** `internal/boundaries_test.go` (score 0.98) is semantically related to boundaries/modules but is not in COMPONENTS_TOUCHED. It defines allowed package import rules, which may need to be respected when creating dependencies.
- ℹ️ **Semantic-related code:** `action.yml` (score 0.99) is semantically related to the action execution but is not in COMPONENTS_TOUCHED.
- ℹ️ **Semantic-related code:** `.github/workflows/release.yml` (score 0.71) is semantically related to release automation but is not in COMPONENTS_TOUCHED. The private repo's unified release pipeline should follow the same pattern.

### Verification context

- Refinement depth: deep
- Derived TASK_COUNT: 4 (limit: 6)
- Derived COMPONENT_COUNT: 2 (limit: 2)
- COMPONENTS_TOUCHED: `samestrin/atcr-enterprise`, `cmd/atcr-enterprise/main.go`
- VISUAL_SURFACE: false
- HAS_GATED_WORK: false
- HAS_CROSS_SYSTEM: true
- Cited references checked: 3
- Codebase search queries (spot-check): ["go module imports core", "cli bootstrapping main", "reconcile hook entrypoint"]
- Deep discovery method: semantic
- Deep discovery queries: ["cli bootstrapping main cobra command", "go module imports dependencies", "github actions build workflow go", "enterprise hooks plugin dependency injection"]
- Deep discovery match count: 10
- Deep discovery snapshot: /Users/samestrin/Documents/GitHub/atcr/.planning/.temp/refine-epic/codebase-discovery.json (temp-only — not committed)
