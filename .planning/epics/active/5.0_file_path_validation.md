# Epic 5.0: File Path Validation and Correction

**Status:** Draft
**Created:** 2026-06-14
**Priority:** High (correctness issue)

## Problem

When reviewing code, models sometimes hallucinate file paths — they reference files that don't exist in the repository. The current parser (`internal/stream/parser.go`) extracts file paths from findings but **never validates that the files actually exist**.

This causes two problems:

1. **Lost findings**: A good TD item with a hallucinated path (e.g., `internal/auth/validator.go` when the actual file is `internal/auth/validate.go`) becomes useless because you can't navigate to the file to fix it.

2. **Silent failure**: The finding is parsed successfully, written to `findings.txt`, and appears in reports — but the file path is wrong. Users only discover this when they try to open the file and it doesn't exist.

This is especially problematic in the legacy code review system where TD items are reconciled from multiple sources. If a path is wrong, the TD item is effectively lost.

## Root Cause

The `splitFileLine` function in `internal/stream/parser.go` (lines 214-228) extracts the file path from the FILE:LINE column but performs no validation:

```go
func splitFileLine(s string) (string, int) {
    idx := strings.LastIndex(s, ":")
    if idx < 0 {
        return s, 0
    }
    suffix := s[idx+1:]
    if suffix == "" {
        return s[:idx], 0
    }
    n, err := strconv.Atoi(suffix)
    if err != nil {
        return s, 0
    }
    return s[:idx], n
}
```

The function splits on the last colon, parses the line number, and returns the file path — but never checks if the file exists.

## Solution

Add a **file path validation step** to the reconcile pipeline that:

1. **Validates file existence** after parsing findings
2. **Flags findings with non-existent paths** (adds a warning or metadata field)
3. **Optionally attempts path correction** (fuzzy matching, similar file names)
4. **Preserves the finding** even if the path is wrong (don't discard it)

### Validation Strategies

#### 1. File Existence Check
After parsing, check if the file exists:
```go
if _, err := os.Stat(finding.File); os.IsNotExist(err) {
    finding.PathValid = false
    finding.PathWarning = "file not found"
}
```

#### 2. Fuzzy Matching (Optional)
If the file doesn't exist, try to find a similar file:
- Same directory, similar name (e.g., `validator.go` → `validate.go`)
- Same name, different directory (e.g., `internal/auth/validator.go` → `pkg/auth/validator.go`)
- Use Levenshtein distance or similar algorithm

If a match is found with high confidence, correct the path and note the correction.

#### 3. Metadata Tracking
Add fields to the `Finding` struct:
```go
type Finding struct {
    // ... existing fields ...
    PathValid    bool   // true if file exists, false otherwise
    PathWarning  string // warning message if path is invalid
    PathCorrected string // original path if corrected, empty otherwise
}
```

### Integration Points

The validation should run:
1. **After parsing** (in `ParseSource` and `ParseModelOutput`)
2. **Before reconciliation** (so invalid paths are flagged early)
3. **In the report generator** (so users see warnings)

## Scope

### In Scope
- File existence validation for all findings
- Path warning metadata (flag invalid paths)
- Optional fuzzy matching for path correction
- Report generation updates (show warnings)
- Unit tests for validation logic
- Integration test with known hallucinated paths

### Out of Scope
- Automatic path correction without user confirmation (too risky)
- Validation of line numbers (out of scope for this epic)
- Validation of file contents (out of scope)
- Integration with tool-using reviewers (Epic 2.0 already addresses this for live reviews)

## Acceptance Criteria

- [ ] AC1: All findings are validated for file existence after parsing
- [ ] AC2: Findings with non-existent file paths are flagged with `PathValid=false` and `PathWarning="file not found"`
- [ ] AC3: Reports display a warning for findings with invalid paths (e.g., "⚠️ File not found: internal/auth/validator.go")
- [ ] AC4: Findings with invalid paths are **not discarded** — they are preserved in the output with warnings
- [ ] AC5: Unit tests cover: valid paths, invalid paths, paths with typos, paths in wrong directories
- [ ] AC6: Integration test with a known hallucinated path (from a real review) shows the warning in the report
- [ ] AC7: (Optional) Fuzzy matching corrects obvious typos with high confidence (>90% similarity)

## Success Criteria

- [ ] Zero findings lost due to hallucinated file paths
- [ ] All findings with invalid paths are clearly flagged in reports
- [ ] Users can quickly identify and correct path issues
- [ ] No regression in parsing performance (<5% slowdown)

## Implementation Plan

### Phase 1: Validation (1 week)
1. Add `PathValid` and `PathWarning` fields to `Finding` struct
2. Implement file existence check in parser
3. Add unit tests for validation logic
4. Update report generator to show warnings

### Phase 2: Fuzzy Matching (Optional, 1 week)
1. Implement fuzzy matching algorithm (Levenshtein distance or similar)
2. Add path correction logic with confidence threshold
3. Add `PathCorrected` field to track corrections
4. Add unit tests for fuzzy matching

### Phase 3: Integration (3 days)
1. Integration test with known hallucinated paths
2. Update documentation
3. Performance testing (ensure <5% slowdown)

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Fuzzy matching corrects to wrong file | Medium | High | Only correct with high confidence (>90%); show original path in warning |
| Performance slowdown | Low | Medium | Only validate after parsing (not during); cache file existence checks |
| False positives (file exists but is wrong) | Low | Low | Validation only checks existence, not correctness; users still review findings |
| Breaking existing workflows | Low | Medium | Warnings are additive; existing findings still work; no breaking changes |

## Relationship to Other Epics

- **Epic 2.0 (Tool-using reviewers)**: Tool-using reviewers can read files, so they catch hallucinated paths during the review. This epic addresses the gap for **static reviews** (no tool access) and **legacy reviews** (already completed).
- **Epic 2.2 (Fanout hardening)**: Addressed reviewer name hallucination and runaway output. This epic addresses **file path hallucination** — the third category of hallucination.
- **Epic 3.0 (Adversarial verification)**: Skeptics could potentially validate file paths, but that's overkill. This epic provides a lightweight, deterministic validation.

## Open Questions

1. **Should we attempt path correction by default, or only when explicitly requested?**
   - Option A: Always attempt correction (convenient, but risky)
   - Option B: Only correct with `--correct-paths` flag (safe, but less convenient)
   - Recommendation: Option B (safe by default)

2. **What confidence threshold for fuzzy matching?**
   - Option A: 80% (more corrections, but more false positives)
   - Option B: 90% (fewer corrections, but safer)
   - Option C: 95% (very safe, but few corrections)
   - Recommendation: Option B (90%)

3. **Should we validate line numbers too?**
   - Out of scope for this epic (line numbers are less critical than file paths)
   - Could be a future enhancement

## References

- `internal/stream/parser.go` — current parser implementation
- `internal/stream/parser_test.go` — existing parser tests
- Epic 2.2 — addressed other hallucination types
- Epic 2.0 — tool-using reviewers (live validation)
