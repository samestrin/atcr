# Epic 14.1: Verification & Grounding

- **Estimated time**: 1 week
- **Tasks/Components**: 3/2
- **Execution**: execute-epic

## Context

During a multi-review using `atcr`, the system can fan out to multiple LLM personas. When presented with large amounts of code, the LLMs frequently hallucinate "Technical Debt" (TD) items that do not exist, flagging unchanged code, completely fabricated snippets, or irrelevant issues.

## Problem Statement

A user recently received 523 TD items, of which 513 were complete hallucinations. This runaway generation destroys trust in the review process and makes the `atcr` tool effectively useless for large codebases. We must force the LLMs to ground their responses in the actual diff provided.

## Proposed Solution

Implement a robust grounding mechanism that prevents an LLM from successfully submitting a hallucinated TD item. 

1. **Require Exact Quotes/Snippets**: Update the persona prompts to mandate that every TD item cite the exact line numbers or the exact code snippet from the diff.
2. **Programmatic Validation**: Implement a new Go function that runs before passing the persona's findings to the reconciler. This function will check if the referenced snippet/lines actually exist in the git patch. If not, the TD item is silently dropped.
3. **Limit Scope to Patch**: Update the prompt constraints so the LLMs are explicitly forbidden from commenting on code outside the "added" or "modified" lines of the diff.

## User Personas

### Primary: Software Engineer
- Wants actionable, real feedback on their PRs.
- Has zero tolerance for reviewing 500+ hallucinated items.

## User Journeys

### Code Review execution
1. User runs `atcr review`.
2. The fanned-out personas generate TD items and cite exact line numbers.
3. The programmatic validator parses the response, cross-references it with the patch, and drops hallucinations.
4. Only grounded, real TD items are passed to the Reconciler.

## Acceptance Criteria

- [ ] AC1: Persona prompts are updated to strictly require `file` and `line` properties (or equivalent).
- [ ] AC2: A validation step drops any TD item whose `file:line` reference cannot be found in the diff.
- [ ] AC3: Prompts are updated to explicitly reject analyzing code that is not part of the `+` or `-` lines in the patch.

## Technical Constraints

- Must intercept the review items before they hit the reconciler pipeline.
- Must accurately map line numbers from the LLM response to the actual git patch line numbers.

## Non-Functional Requirements

### Performance
- The patch-validation step should add negligible overhead (<500ms) to the pipeline.

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| LLMs provide "approximate" lines | High | High | Use fuzzy matching or snippet text matching if line numbers are slightly off, to avoid dropping real issues. |

## Clarifications

### Recorded 2026-06-30 (rubber duck — /execute-epic)

**Key Decisions:**
- Ungrounded TD items are HARD-DROPPED in the fan-out stage (per AC2), but every drop is observable via a per-agent stderr count, following the existing `enforceConstraints` precedent (`internal/fanout/postprocess.go`) — not truly silent.
- Findings tagged `category=out-of-scope` are EXEMPT from the grounding drop. Out-of-scope is a deliberate, AC-traced (AC 06-04) feature (`internal/payload/scope.go`, `internal/reconcile/gate.go`, `internal/reconcile/merge.go`) — annotated, not promoted — and must survive untouched.
- Grounding match: keep a finding if its cited line falls within a changed head range expanded by ±3 lines (matching the existing `lineProximity = 3` dedupe constant in `internal/reconcile/cluster.go`); if the line misses, fall back to matching the finding's EVIDENCE snippet against the patch's added/modified text; drop only when BOTH fail.
- The validator runs in the FAN-OUT phase only — per-file changed line ranges are threaded from `PrepareReview` into `WritePool`/`findingsFor` (`internal/fanout/review.go`, `internal/fanout/artifacts.go`). This is the only point with live base/head diff access and matches the Technical Constraint to intercept before the reconciler.

**Scope Boundaries:**
- IN scope: `personas/_base.md` grounding rules (AC1, AC3); a new pure-Go patch-line validator reusing the existing `lineRange` machinery from `internal/payload/diff.go`; wiring it into the fan-out phase before the reconciler (AC2).
- OUT of scope: the reconciler core / dedupe / merge logic; the existing Epic 5.0/5.4 path-EXISTENCE validation (a separate, flag-only concern); grounding at reconcile time (`atcr reconcile <dir>` has no live diff — documented, accepted gap).

**Technical Approach:**
- Reuse existing head-side changed-range computation (`changedHeadRanges`/`parseHeadRanges`/`lineRange`, `internal/payload/diff.go`) rather than re-parsing diffs.
- Non-git / binary / no-range cases fail OPEN (keep the finding) to avoid dropping real issues, matching the codebase's conservative path-validation degradation.
- Pure deletions (no head line) rely on the EVIDENCE-snippet fallback; a finding citing only deleted code with no evidence match is dropped.

**Original Questions and Answers:**
1. Q: Hard-drop ungrounded items, or extend the existing flag-and-filter (Epic 5.0/5.4) approach?
   A: Hard-drop in fan-out (per AC2), but observable via per-agent stderr count following the `enforceConstraints` precedent. Epic 5.0/5.4 flag-only behavior is a deliberate exception for path-existence checks, not a project-wide anti-drop policy.
2. Q: Drop `out-of-scope` findings uniformly, or exempt them?
   A: Exempt them (option b). Out-of-scope is a deliberate, AC-traced (AC 06-04), gate-integrated feature; dropping it would silently delete existing tested behavior outside this epic's scope.
3. Q: Is ±3 the right tolerance, and enable the evidence-snippet fallback?
   A: Yes to both. ±3 matches the existing `lineProximity = 3` constant; the evidence-snippet fallback is the epic's own named Risk-table mitigation and reuses the existing `Evidence` field with no schema change.
4. Q: Validator in fan-out only, or also at reconcile time?
   A: Fan-out only. It is the only point with live base/head diff access and matches the epic's Technical Constraint; `atcr reconcile <dir>` has no live diff — an accepted, documented gap.
