# Sprint Plan 15.1: Core Landing Page & Design System

- **Estimated time**: 5 days
- **Tasks**: 6
- **Components**: 2
- **Execution**: execute-epic

## Context

The primary landing page of `atcr.dev` represents the first impression developers have of the tool. It needs to reflect the design system specified in [design-system.md](file:///Users/samestrin/Documents/GitHub/atcr.dev/the-artifact/design-system.md): dual-theme, paper-like colors, IBM Plex typography, and a mechanical, high-trust documentation aesthetic. The core visual asset is "The Artifact Viewer" itself.

## Problem Statement

We need to construct the foundation of the site:
1. Build a responsive layout supporting device sizes from mobile viewports (320px) up to widescreen.
2. Implement theme switching automatically using `prefers-color-scheme`.
3. Support the interactive file tab switching of the artifact view panel (`report.md` / `findings.txt` / `findings.json`) without layout shifts or text wrapping.
4. Establish copy-to-clipboard command pill logic.

## Proposed Solution

1. Port mockup styles to `styles.css` matching colors, typography rules, layout widths, and key components.
2. Port index HTML structure to `index.html` ensuring semantic element tags (`<nav>`, `<section>`, `<footer >`).
3. Port Javascript animations and events (tab toggles, clipboard copy, scroll reveals) to `script.js`.
4. Package assets (such as `favicon.svg` and brand logo marks).

## User Personas

### Primary: Software Engineer (Devin)
- Wants to understand what ATCR does in 5 seconds.
- Looks for command-line instructions (`go install`).
- Evaluates the readability of output file formats.

## User Journeys

### Visitor Landing & Command Copy
1. Visits `atcr.dev`.
2. Scans the hero message ("A review panel, not a reviewer") and watches the scroll reveal animations.
3. Clicks through the "Artifact Viewer" tabs (`report.md`, `findings.txt`, `findings.json`) to see differences.
4. Clicks the "copy" button on the command pill to copy the installation command.

## Acceptance Criteria

- [ ] AC1: Page structure utilizes semantic HTML5 tags and has a single H1.
- [ ] AC2: All colors (backgrounds, borders, text, severe warnings) strictly map to HSL/Hex specs in the design system.
- [ ] AC3: The Artifact Viewer tabs are keyboard-focusable and switch active panes instantly without layout reflows.
- [ ] AC4: Clicking the copy button copies `$ go install github.com/samestrin/atcr/cmd/atcr@latest` to the system clipboard and displays a visual check/state change.
- [ ] AC5: The design changes themes automatically between light/dark depending on the user's OS settings.

## Success Criteria

### Functional
- [ ] Initial load time of the landing page is under 1.0 second on standard connections.
- [ ] Javascript payload is under 5KB and requires zero dependencies.
- [ ] Page layout displays correctly on Safari, Chrome, and Firefox.

### Business
- [ ] Provide clear, accessible CTA buttons leading directly to Docs and Team Edition validation program signups.

## Technical Constraints

- Vanilla HTML, CSS, and JS only (no React, Svelte, or tailwind utility pollution).
- Responsive breakpoint configurations must prevent code wrap in the artifact view panels (using horizontal scrolls).

## Non-Functional Requirements

### Performance
- Total size of HTML/CSS/JS/SVG asset files must be less than 150KB.
- Lighthouse performance score must be > 95.

### Security
- All scripts are inline or local; no external script calls (e.g. CDNs) to protect visitor privacy.

## Out of Scope

- Model Leaderboard and Personas pages details (covered in Sprint 15.2 and 15.3).
- Integration of backend lead tracking databases.

## Dependencies

- Selected mockup code files from [the-artifact mockups](file:///Users/samestrin/Documents/GitHub/atcr.dev/the-artifact/mockups/).

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Font flash (FOUT) | Medium | Low | Use standard system monospaced fonts as fallbacks for IBM Plex Mono |

## References

- [design-system.md](file:///Users/samestrin/Documents/GitHub/atcr.dev/the-artifact/design-system.md)
- [index.html mockup](file:///Users/samestrin/Documents/GitHub/atcr.dev/the-artifact/mockups/index.html)

## Refinements (2026-07-02)

This section records findings from `/refine-epic` run on July 02, 2026 08:40:38AM. It is additive — original plan content above is preserved.

### Auto-applied corrections (1)

- ✅ **Stale sprint reference:** Out of Scope cited "Sprint 14.1 and 14.2" as the owners of the Model Leaderboard and Personas pages. Sprint 14.1 (`verification_grounding`) and 14.2 (`reconciliation_layer`) are unrelated, already-completed engine-internals sprints. The parent Epic Index (`.planning/epics/plans/15.0_website_launch_index.md`) shows the leaderboard and personas work is actually owned by **Sprint 15.3** (`leaderboard_and_deployment`) and **Sprint 15.2** (`docs_and_personas`) respectively. Corrected to "Sprint 15.2 and 15.3."

### Items needing user confirmation (4)

- ⏸️ **Missing target repository / cross-repo execution mismatch (most severe finding):** None of the four Proposed Solution steps ("Port mockup styles to `styles.css`", "Port index HTML structure to `index.html`", "Port Javascript... to `script.js`", "Package assets") state a destination path or repository. This epic file lives in the `atcr` repo's `.planning/epics/active/`, so `/execute-epic` will resolve REPO_ROOT to `atcr` (a Go CLI codebase with zero HTML/CSS/site infrastructure today) and will branch/commit/PR *within that repo*. But the parent Epic Index 15.0 (`.planning/epics/plans/15.0_website_launch_index.md`) AC1 requires the work be "integrated into the `atcr.dev` repository" — a wholly separate git repository (`~/Documents/GitHub/atcr.dev`) that today only contains design *concepts* (`cyber-devtools/`, `tribunal/`, `signal-lab/`, `workshop/`, `the-artifact/`, `consensus/`), not a production site skeleton. `/execute-epic`'s single-repo branch → TDD → PR → merge flow cannot commit into a second, unrelated git repository, and COMPONENTS_TOUCHED cannot be verified against real paths until a destination is named. Suggested action: state explicitly where in `atcr.dev` the production `index.html`/`styles.css`/`script.js`/assets should land (e.g., repo root, superseding the current concept-comparison README), and confirm whether execution should instead run as a session scoped to the `atcr.dev` repo itself (e.g., a copy of this plan placed under `atcr.dev/.planning/`), or route this epic through `/init-plan` if cross-repo output can't be avoided.
- ⏸️ **Missing font-asset sourcing task:** Context and the Risks section both treat IBM Plex as the core typography, but the mockup source (`the-artifact/mockups/styles.css`) only declares `font-family: 'IBM Plex Sans'/'IBM Plex Mono'` with system-font fallbacks — no `.woff`/`.woff2` files exist anywhere under `the-artifact/`, and the Security NFR explicitly forbids external CDN calls (ruling out Google Fonts as a loading mechanism). Without a task to source and self-host the actual font files, most visitors will only ever see the FOUT-fallback font, not IBM Plex. Suggested action: add a task (or fold into Proposed Solution step 4 "Package assets") to source IBM Plex `.woff2` files and self-host them via `@font-face`.
- ⏸️ **"Brand logo marks" dependency not cited:** Proposed Solution step 4 requires packaging "`favicon.svg` and brand logo marks," but the Dependencies section only cites `the-artifact/mockups/` (which has `favicon.svg` but no brand-mark files). A codebase search found likely candidates at `atcr.dev/image-concepts/Brand Mark.svg` and `Brand Mark.png` — outside the cited mockups directory and not referenced anywhere in this plan. Suggested action: add the Brand Mark files to Dependencies, or clarify that "brand logo marks" is out of scope / already satisfied by `favicon.svg` alone.
- ⏸️ **Task count mismatch:** Header claims "Tasks: 6," but the Proposed Solution section explicitly enumerates only 4 discrete steps (style port, HTML port, JS port, asset packaging). It's not stated whether the other 2 tasks are implied sub-splits (e.g., JS work broken into tab-toggle / clipboard-copy / scroll-reveal as separate tasks) or missing entirely. Suggested action: either expand Proposed Solution to explicitly enumerate all 6 tasks, or correct the header to "Tasks: 4" (or 5, if the font-asset task above is added).

Note that these were NOT applied automatically — see the copy-paste block emitted at the end of the skill run.

### Advisory observations (0)

None.

### Verification context

- Refinement depth: spot-check
- Derived TASK_COUNT: 4 (limit: 6) — see task-count-mismatch finding above
- Derived COMPONENT_COUNT: 2 (accepted from plan header; unverifiable — see cross-repo finding above)
- COMPONENTS_TOUCHED: unresolved pending target-repository clarification (see finding above)
- VISUAL_SURFACE: true (recommend `/execute-epic --visual` once the target-repo question is resolved)
- HAS_GATED_WORK: false
- HAS_CROSS_SYSTEM: true (deliverables target the separate `atcr.dev` git repository)
- Cited references checked: 6 (design-system.md, index.html mockup, mockups directory, styles.css/index.html/script.js/favicon.svg as named output targets)
- Codebase search queries (spot-check): ["landing page atcr.dev", "prefers-color-scheme clipboard", "IBM Plex"]
