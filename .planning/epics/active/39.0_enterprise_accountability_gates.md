# Epic Plan 39.0: Enterprise Accountability Gates (Digital Signatures)

- **Estimated time**: TBD
- **Tasks/Components**: 4 tasks / 2 components
- **Execution**: init-plan

## Objective

Create an "Accountability Gate" integration that explicitly requires a traceable human digital signature (or a verifiable `/approve` command) for pull requests flagged as HIGH/CRITICAL severity or touching protected paths. 

## Context

In high-compliance environments (e.g., SOC2, ISO 27001, FedRAMP), "looks good to me" by a bot isn't enough. When a change involves significant risk, auditors need proof of human authorization. Currently, AI tools speed up the development process, but often blur the lines of authorship and approval. ATCR needs a mechanism to enforce clear accountability so teams can stand behind what they merge.

## Proposed Solution

1. **VCS Integration (GitHub/GitLab):**
   - ATCR integrates via webhooks or actions into the Pull Request workflow.
2. **Dynamic Review Requirements:**
   - For routine PRs (LOW/MEDIUM severity findings, no protected paths), ATCR outputs findings and allows standard merge conditions.
   - For high-risk PRs (HIGH/CRITICAL severity findings, or paths matching Epic 38.0), ATCR locks the PR via a required status check.
3. **Signature Verification:**
   - The status check can only be cleared if a designated Code Owner leaves an explicit, traceable comment (e.g., `/atcr approve`) or uses a formal digital signature mechanism provided by the VCS.

## Acceptance Criteria

- [ ] ATCR can enforce a locking status check on GitHub/GitLab PRs based on finding severity.
- [ ] The lock can be cleared by an authorized human interaction (e.g., command, or digital signature).
- [ ] The accountability event is logged (tying into Epic 36.0 Enterprise Audit Logging).

## Tasks

- **T1**: Implement VCS status check lock/unlock API integration (GitHub/GitLab) in [internal/ghaction/client.go](file:///Users/samestrin/Documents/GitHub/atcr/internal/ghaction/client.go).
- **T2**: Implement severity-based status locking logic for HIGH/CRITICAL findings.
- **T3**: Implement webhook handler in `samestrin/atcr-enterprise/internal/integrations/vcs/` to parse `/atcr approve` comments, verify authorized human signatures (with `app.slug` verification to prevent spoofing), and unlock status checks.
- **T4**: Integrate signature verification events with Epic 36.0 (Enterprise Audit Logging) using [internal/audit](file:///Users/samestrin/Documents/GitHub/atcr/internal/audit/).

## Components Touched

- `samestrin/atcr-enterprise/internal/integrations/vcs/` (Handling status checks and webhooks)
- `samestrin/atcr` (Epic 35.0 EnterpriseHooks trigger)

## Refinements (2026-07-17)

This section records findings from `/refine-epic` run on July 17, 2026 11:25:13AM. It is additive — original plan content above is preserved.

### Auto-applied corrections (0)

None. Mark each with ✅.

### Items needing user confirmation (4)

- ⏸️ **Missing task list / TBD metadata:** The epic plan currently has no explicit tasks list and has `Tasks/Components: TBD` in the metadata header. Suggested action: Add an explicit 4-task implementation backlog (T1-T4) to detail VCS status checks, severity checks, `/atcr approve` comment webhook handlers, and audit logging integration.
- ⏸️ **Private repository component path:** The component path `samestrin/atcr-enterprise/internal/integrations/vcs/` does not exist in the current workspace. Suggested action: Confirm that this path refers to the private `samestrin/atcr-enterprise` repository under the Open Core modular setup (as established in Epic 34.0), or update the path if it is a typo.
- ⏸️ **Discovery surfaced primary file the plan ignores:** Discovery identified [client.go](file:///Users/samestrin/Documents/GitHub/atcr/internal/ghaction/client.go) as the best starting point for OBJECTIVE because it houses the HTTP client wrapper and API methods for creating/listing check runs and reviews. The epic plan does not mention this file. Suggested action: Add a task to modify `internal/ghaction/client.go` to support status check locking/unlocking methods.
- ⏸️ **Missing file in TASKS:** Discovery recommends modifying [client.go](file:///Users/samestrin/Documents/GitHub/atcr/internal/ghaction/client.go) to add helper methods for lock status checks and comments parsing. The plan's tasks do not mention this file. Suggested action: Add this file to the task list/scope of modifications.

### Advisory observations (2)

- ℹ️ **Required check-run security (app.slug verification):** Based on [kb-2026-07-10-9846c6.md](file:///Users/samestrin/Documents/GitHub/atcr/.planning/.knowledge/kb-2026-07-10-9846c6.md), required check-run gates can be bypassed if they only match by check-run name. To make the accountability gate secure, status check verification must validate the creator's `app.slug` (e.g., verifying it is `github-actions` or the authorized `atcr-enterprise` app) or verify the `check_suite.id` matches the actual CI run.
- ℹ️ **Test pattern guidance:** Codebase uses `go test` with tests at [internal/ghaction/](file:///Users/samestrin/Documents/GitHub/atcr/internal/ghaction/) (naming: `*_test.go`). Example: [client_test.go](file:///Users/samestrin/Documents/GitHub/atcr/internal/ghaction/client_test.go). The plan should follow this pattern for testing status checks and comments webhook handlers.

### Verification context

- Refinement depth: deep
- Derived TASK_COUNT: 4 (limit: 6)
- Derived COMPONENT_COUNT: 2 (limit: 2)
- COMPONENTS_TOUCHED: `samestrin/atcr-enterprise/internal/integrations/vcs/`, `samestrin/atcr`
- VISUAL_SURFACE: false
- HAS_GATED_WORK: false
- HAS_CROSS_SYSTEM: true
- Cited references checked: 0
- Codebase search queries (spot-check): ["status check pull request", "digital signature approve", "severity gate webhook"]
- Deep discovery method: semantic
- Deep discovery queries: ["github check run status creation", "pr comment webhook signature handler", "reconcile findings audit log event", "vcs integration pull request status"]
- Deep discovery match count: 10
- Deep discovery snapshot: `/Users/samestrin/Documents/GitHub/atcr/.planning/.temp/refine-epic/codebase-discovery.json` (temp-only — not committed)
