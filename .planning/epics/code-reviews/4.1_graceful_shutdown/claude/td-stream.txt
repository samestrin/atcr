MEDIUM|internal/fanout/status.go:209|ReadReviewStatus returns the stale/in_progress inference (early return on missing or corrupt summary.json) BEFORE the m.Interrupted check, so an interrupted run whose summary.json never landed (e.g. WritePool failed under a SIGINT) is reported as stale despite the durable manifest.interrupted=true marker. The comment at status.go:212 ("Applied last so it overrides both branches") overstates the guarantee, and no test covers interrupted=true with an absent summary.|Move the m.Interrupted check above the corrupt-summary/stale early-return in ReadReviewStatus so the durable interrupt marker wins regardless of summary presence; correct the comment; add a status_test case with interrupted=true and no summary.json.|correctness|45|internal/fanout/status.go:180-218|claude
MEDIUM|cmd/atcr/main.go:64|handleSignals has no completion channel: after a signal it unconditionally sleeps gracefulShutdownTimeout then calls forceExit(1). On a fast clean interrupt the process exit is a goroutine race between main's os.Exit(code) and the timer, and the misleading "Graceful shutdown timed out, forcing exit" notice can print even when shutdown was prompt. The time.After timer is also never stopped.|Have main close a done channel after root.ExecuteContext returns and select it against a time.NewTimer (defer Stop) in the goroutine; on done, return without printing the timeout notice or calling forceExit. Add a test: signal then complete under the grace period, assert forceExit not called and no timeout notice.|correctness|60|cmd/atcr/main.go:59-68|claude
MEDIUM|cmd/atcr/main.go:61|Only the first SIGINT is consumed; a second Ctrl-C is swallowed (buffered cap 1) with no immediate-exit fast path, so a user cannot escape the 10s grace wait - the opposite of the intended UX. The current "first signal only" contract is also untested.|Loop on sigCh in a select against the grace timer; on a second signal print a notice and forceExit(1) immediately. Add a test sending two signals asserting a single force-exit with code 1.|maintainability|30|cmd/atcr/main.go:40-67|claude
MEDIUM|cmd/atcr/main.go:64|The unconditional 10s force-exit (os.Exit) can fire while fanout.WritePool is still flushing, stranding a review dir with per-agent artifacts but no summary.json/manifest - exactly the partial results the epic promises to preserve (an in-flight provider call can run to the 120s llmclient timeout). Combined with the status.go precedence gap this run then reports stale, not interrupted. NOTE: the epic explicitly accepted force-exit as a backstop (Risk table: Low impact), so this is hardening, not a regression.|Persist results incrementally or write a partial summary+manifest marker before the long drain so force-exit cannot strand artifacts, or document the data-loss window. Verify by cancelling mid-WritePool and asserting atcr status reads a coherent partial.|correctness|240|cmd/atcr/main.go:62-66 internal/fanout/review.go:339-378|claude
MEDIUM|cmd/atcr/review.go:281|interruptMessage prints "partial results saved to <dir>" and "0/0 agents completed" even when result==nil - the case where ExecuteReview was cancelled before producing any result and likely nothing usable was written - then points the user at "atcr status <id>" which may report stale/in-progress. Contradicts the sibling interruptedBeforeFanout path which correctly says "no partial results to save."|When result==nil or Succeeded==0, render the no-results variant instead of claiming saved results. Add a test asserting the nil-result message does not claim saved partials.|correctness|30|cmd/atcr/review.go:278-287|claude
LOW|cmd/atcr/main.go:36|No test exercises a SIGINT/SIGTERM during atcr serve, although the epic clarification states signal handling applies uniformly to serve via the shared root context. A regression leaving serve blocked after a signal would pass all current tests.|Add a serve test that injects a cancellable context (via the serveFn seam) and asserts the handler returns promptly on cancel; assert serve plumbs cmd.Context() rather than context.Background().|testing|60|cmd/atcr/main.go:36-51|claude
LOW|internal/fanout/interrupt_test.go:13|The comment claims MaxParallel=1 "serializes the fan-out" making the 2/2 split deterministic, but the slots do not set Serial:true, so they run in the parallel lane bounded by a sem of 1; determinism actually comes from cancel() firing before the sem is released, not from serial execution. A maintainer trusting the comment could break the test.|Either set Serial:true on the slots to genuinely serialize, or correct the comment to describe the real invariant (sem-of-1 + cancel-before-release).|testing|15|internal/fanout/interrupt_test.go:48-66|claude
LOW|cmd/atcr/main_test.go:248|TestHandleSignals_CancelsContextOnSignal never asserts ctx is alive before the signal is sent, so a handler that cancels unconditionally at startup would still pass - the test proves "ctx ends cancelled," not "cancelled because of the signal" (weakens AC8). TestHandleSignals_ForceExitsAfterGracePeriod likewise does not prove the exit waited for the grace period.|Add require.NoError(t, ctx.Err()) before sending the signal; in the force-exit test record start time and assert code==1 observed no earlier than ~the configured timeout.|testing|15|cmd/atcr/main_test.go:248-281|claude
LOW|internal/fanout/review.go:337|interrupted is computed once right after Run returns and reused for both manifest writes, so a SIGINT arriving during WritePool's I/O is missed: WritePool can succeed and the run be marked interrupted=false / reported completed. The parent-vs-child ctx distinction also silently depends on the root ctx never being given a deadline by a future caller.|Re-evaluate errors.Is(ctx.Err(), context.Canceled) at the manifest-write site (OR it into the earlier capture); document the precondition that interrupted==Canceled is only meaningful because the root ctx is cancelled solely by the signal handler.|correctness|30|internal/fanout/review.go:331-365|claude
LOW|cmd/atcr/review.go:268|Maintainability nits: the interrupt ctx-check + codedError construction is duplicated across three return sites (the post-fanout branch hand-builds the same codedError that interruptedBeforeFanout returns, risking drift); the two interrupt notices emit a raw warning emoji that can render as mojibake on non-UTF8 consoles, inconsistent with main.go's plain-text notice.|Extract a shared interruptedError() helper used by both paths; drop the emoji or gate it behind a TTY/UTF-8 check.|maintainability|20|cmd/atcr/review.go:120-122,164-166,196-199,268,284|claude
