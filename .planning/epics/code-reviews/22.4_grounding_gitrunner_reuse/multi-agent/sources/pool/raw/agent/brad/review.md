HIGH|internal/payload/diff.go:462|Lazy cache population in zeroCtxChunks and rangeChunks lacks synchronization causing data races and potential panics on concurrent map writes when the same gitRunner is accessed by multiple goroutines|Guard cache fields with a sync.Mutex or use sync.Once per cache population path|concurrency|25|zeroCtxChunks checks s.zeroCtx != nil then assigns s.zeroCtx = m without locks; rangeChunks does the same for s.lineRanges; concurrent calls from payload and grounding will race