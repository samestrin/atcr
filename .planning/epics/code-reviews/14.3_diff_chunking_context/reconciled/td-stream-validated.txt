MEDIUM|internal/fanout/chunker.go:19|diffFileMarker is hardcoded to 'diff --git a/' (with the a/ prefix), but internal/payload splits on 'diff --git ' (space) and explicitly handles --no-prefix headers (ingest.go:407-419 headPathFromGitHeader). If a user has git config diff.noprefix=true (which also affects atcr's own git diff payload calls, since atcr never forces --src-prefix), headers become 'diff --git <old> <new>' with no a/, so splitDiffFiles/countDiffFiles match zero markers, chunkDiff returns the whole diff as one chunk, and review_strategy=chunked silently degrades to bulk while internal/payload still parses the diff correctly. This is a new trigger on the same hardcoded marker already tracked for combined diffs (TD README combined-diff item).|Change diffFileMarker to 'diff --git ' to match payload's convention and add a chunker test with a --no-prefix fixture asserting the correct file count. Consider consolidating with the existing combined-diff marker TD item since both share the chunker.go:15-19 root cause.|correctness|30|code-review|claude|MEDIUM
LOW|internal/fanout/chunker.go:25|countLines counts only newline characters, so a file segment whose final line lacks a trailing newline is undercounted by one. This propagates into bin-packing (curLines+fl > maxLines at line 98 can over-pack by a line) and into the oversize-warning check (countLines(ct) > ml at review.go:849 can miss a genuinely over-budget lone file). Git's '\ No newline at end of file' case makes this reachable; the comment calls it 'soft' but for the oversize warning it means a real over-budget file can go unwarned.|Count strings.Count(s,newline) plus 1 when s is non-empty and does not end in a newline, or drop the oversize warning's precision claim. Verify with a no-trailing-newline fixture sized exactly at the boundary.|error-handling|15|code-review|claude|MEDIUM
MEDIUM|internal/fanout/chunker.go:82|chunkDiff places no ceiling on the number of chunks (count ~= totalLines/maxLines). totalLines derives from mp.Text, bounded by payload_byte_budget EXCEPT that 0 is the documented 'unlimited' escape hatch. With payload_byte_budget=0 + review_strategy=chunked, a huge diff produces an unbounded number of chunks and thus unbounded Slots; Engine.Run eagerly spawns one goroutine per non-serial slot (the semaphore bounds concurrency, not goroutine/API-call count), amplifying into unbounded goroutines and provider calls — a cost/DoS vector on the exact feature meant to control cost.|Cap chunks per persona (e.g. a MaxChunksPerAgent constant) and error or coalesce beyond it, or refuse chunked when payload_byte_budget==0. Verify a synthetic multi-thousand-file diff produces a bounded slot count.|security|60|code-review|claude|MEDIUM
LOW|internal/fanout/chunker.go:182|In mergeResultGroup, FallbackFrom is overwritten by the last chunk that used a fallback (if r.FallbackUsed { out.FallbackFrom = r.FallbackFrom }). When different chunks fall back from different primaries, the merged telemetry arbitrarily reports one chunk's substitution and silently discards the others. FallbackUsed=true correctly latches, but the paired FallbackFrom becomes non-deterministic/misleading.|Accumulate the distinct fallback-from values (or document that FallbackFrom reflects one representative fallback, not all). Verify with a group where chunk 0 and chunk 2 fall back from different primaries.|maintainability|15|code-review|claude|MEDIUM
HIGH|internal/fanout/chunker.go:212|mergeResultGroup sets Status=StatusOK whenever ANY chunk succeeded (anyOK at line 216). When a persona is split into N chunks and only some succeed (e.g. 2 of 3 fail on provider errors), the merged result reports OK and contributes findings from only the surviving chunk(s); the files in the failed chunks were never reviewed. The only signal is a stderr Fprintf (line 213) — nothing lands in summary.json, the manifest, or the Result status, and the 14.2 consensus filter counts the persona as a full voter over the whole diff. For a review tool feeding a CI gate, this is a false coverage guarantee: the gate can pass green while part of the diff went unreviewed and could hide a CRITICAL.|Record partial coverage in the machine-readable result (e.g. add a PartialChunks/UnreviewedFileCount field to Result surfaced in summary.json), or introduce a distinct partial status so downstream gating can react. Keep the existing stderr warning but do not rely on it as the sole signal. Verify summary.json reflects the failed-chunk count on a run where a persona has a failing chunk.|correctness|120|code-review|claude|MEDIUM
MEDIUM|internal/fanout/review.go:840|The chunked branch runs chunkDiff on mp.Text for whatever payload mode resolved. In payload_mode=files the payload body is '=== FILE: <path> ===' sentinel-delimited content (builder.go:172-199) with NO 'diff --git' line, so splitDiffFiles returns the whole multi-file payload as one segment, chunkDiff yields a single chunk, and review_strategy=chunked becomes a silent no-op. Worse, the oversize-warning loop at line 849 sees countDiffFiles(ct)==0 (<=1); if the whole files payload exceeds max_context_lines it prints 'a single file's diff (N lines) exceeds max_context_lines', mislabeling the entire multi-file payload as one file.|Reject or warn when review_strategy==chunked is combined with a non-diff payload mode (files) at config-resolve time, and gate the oversize warning on countDiffFiles(ct)==1 (not <=1) so a zero-marker payload does not fire the single-file message. Verify a chunked+files run emits no misleading single-file warning.|correctness|60|code-review|claude|MEDIUM
LOW|internal/fanout/review.go:847|In the chunked branch EffectiveMaxContextLines() is invoked twice (line 841 as the chunkDiff arg, line 847 into ml), countDiffFiles(ct) is computed in the warning loop (lines 849-850) and AGAIN when passed to renderAgent (line 855), and each countDiffFiles/splitDiffFiles/countLines call does a full strings.SplitAfter/strings.Count pass over the chunk. For a large diff this is several redundant O(n) scans per chunk.|Hoist ml once and compute countDiffFiles(ct)/countLines(ct) once per chunk into locals reused by both the warning and renderAgent. Verify behavior unchanged via existing chunker tests.|performance|15|code-review|claude|MEDIUM
MEDIUM|internal/fanout/review.go:941|After the T3 refactor extracted renderAgent, the add closure re-implements mode resolution (forceMode/EffectivePayloadMode, payload lookup, not-found/no-payload errors) at review.go:818-831, but buildAgent (line 941) still contains the identical resolution logic and now has NO production caller — only tests (retry_wiring_test.go, review_scope_inject_test.go, review_test.go, review_tools_test.go) reference it (verified: grep buildAgent internal/ excluding _test shows only the definition). This is dead production code plus two divergent copies of the same mode/payload-resolution logic that will silently drift.|Either delete buildAgent and update the tests to drive buildSlots/renderAgent, or have the add closure call buildAgent for the single-chunk/bulk path so there is one resolution site. Verify no non-test caller exists before deleting and that fallback-wiring tests still pass.|maintainability|60|code-review|claude|MEDIUM
LOW|internal/registry/config.go:712|The comment justifying the max_context_lines <= 0 rejection claims 'A zero or negative cap would make bin-packing degenerate (every file its own chunk, or worse)'. That is factually inverted: chunkDiff at chunker.go:87 treats maxLines <= 0 as an explicit 'disable chunking, return the whole diff as one chunk' sentinel — the opposite of 'every file its own chunk'. The validation is correct; the stated rationale misleads future maintainers about downstream behavior.|Rewrite the comment to say a non-positive value is rejected because it collides with the chunker's <=0 'disable chunking' sentinel (a misconfigured 0 would silently fall back to bulk-like behavior for that persona). Keep the bounds check as-is.|maintainability|15|code-review|claude|MEDIUM
LOW|internal/registry/config.go:942|max_context_lines is validated only in validateAgent (load-time YAML path). A directly-constructed AgentConfig with MaxContextLines set to a negative or zero value never passes validateAgent, and EffectiveMaxContextLines() returns the raw value with no re-check. This contrasts the retry tunables, which ResolveSettings explicitly re-validates for directly-constructed reg values 'so the engine never receives them' (precedence.go:229-236). The only thing preventing degenerate behavior is the chunker's incidental <=0 guard, which silently disables chunking with no error.|Add a defensive clamp/re-validation where EffectiveMaxContextLines is consumed in chunked dispatch, or explicitly document that non-positive values silently fall back to bulk for that persona. Given the retry-bounds precedent, a re-check at dispatch is the consistent choice.|correctness|30|code-review|claude|MEDIUM
LOW|internal/registry/precedence.go:153|review_strategy is documented as 'mirroring payload_mode' (config.go:399-401) and Settings advertises a 'CLI flag > project config > registry > embedded default' chain (precedence.go:78-79), but review_strategy has NO CLI tier: CLIOverrides (precedence.go:114-119) has no ReviewStrategy field and it is resolved at registry+project tiers only. A user cannot flip it for a one-off run the way --payload overrides payload mode. This was by-design per the epic clarification (Settings/Registry/project only), so it is a doc/consistency gap rather than a functional bug.|If a CLI override is desired, add ReviewStrategy *string to CLIOverrides and overlay+validate it after the project tier (mirroring the payload_mode block at precedence.go:193-200). If intentionally omitted, soften the 'mirrors payload_mode' phrasing so it does not imply CLI parity.|maintainability|30|code-review|claude|MEDIUM
LOW|internal/registry/project.go:47|LoadProjectConfig validates payload_mode, max_parallel, and cache_max_bytes at load (project.go:138-145) but does NOT validate review_strategy. It is not a correctness hole — ResolveSettings backstops it at precedence.go:227 — but the resulting error is strictly worse: an invalid project review_strategy surfaces late from ResolveSettings with no file-path context, whereas an invalid payload_mode fails at load with the config path. Defense-in-depth is inconsistent within the same struct and epic.|Add an 'if !reviewStrategyValid(cfg.ReviewStrategy)' check returning a base-prefixed error alongside the existing payloadModeValid check in LoadProjectConfig, matching the payload_mode treatment.|error-handling|15|code-review|claude|MEDIUM
