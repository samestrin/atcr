# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/31.1_content_first_home_view.md
# TARGET_TYPE: epic
# CREATED: 2026-07-19 07:09:36
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
LOW|cmd/atcr/main.go:234|The Args-block comment states "the RunE keeps bare atcr printing help with exit 0", but epic 31.1 replaced cmd.Help() with runHome(cmd) at main.go:262-264. The comment now documents behavior that no longer exists and will mislead a maintainer reading the root command's invariants about what a bare atcr invocation does.|Update the comment at main.go:230-234 to say the RunE renders the home view (not help) with exit 0. Verify by reading lines 230-264 for internal consistency after the edit.|maintainability|5|cmd/atcr/main.go:230-264|claude
LOW|cmd/atcr/home.go:72|resolveHomeState classifies any errors.Is(err, os.ErrNotExist) as a true first-run, but os.ReadFile returns an ErrNotExist-matching error also for a dangling symlink target (ENOENT) or an ENOTDIR path component (e.g. .atcr is a regular file). In those cases a broken .atcr/latest pointer exists yet renders "No reviews yet" — the honesty gap the function's own doc comment (home.go:63-68) claims to avoid.|Optionally os.Lstat .atcr/latest before classifying: a present-but-broken pointer routes to the unavailable state, reserving first-run for a Lstat that itself returns ErrNotExist. Add a test symlinking .atcr/latest to a missing target asserting unavailable. Documenting the known limitation is an acceptable low-cost alternative.|correctness|30|cmd/atcr/home.go:69-85|claude
LOW|cmd/atcr/home.go:44|relHome is a third hand-rolled copy of the "relativize against home, fall back to verbatim on .." idiom, alongside internal/tools/dispatch.go relDisplay and internal/log/redact.go relativizePaths (the code comment openly admits copying them). Three copies of the rel==".." vs ".."+sep boundary guard mean a fix to one will not propagate — the maintainability cost the existing expandHome TD in quickstart.go already gestures at.|Extract a single shared pathrel helper (e.g. internal/pathutil) that all three call sites use, or at minimum add a cross-reference TD linking them and expandHome. Verify TestRelHome still passes against the shared helper.|maintainability|60|cmd/atcr/home.go:37-57|claude
LOW|cmd/atcr/home.go:93|The AXI renderer routes every field through toonQuote (strips control bytes / invalid UTF-8), but the human renderHomeView prints relHome(execPath) verbatim via fmt.Fprintln. A binary installed at a path containing an ANSI escape or newline reaches the terminal unfiltered. Exposure is marginal (requires an attacker-controlled install path of atcr's own binary) and matches existing verbatim path-display views, but the two renderers are asymmetric on a control-byte-safety property the AXI side treats as load-bearing.|If parity is wanted, run the human exec path through the same sanitize helper other human views use before Fprintln; test with an exec path containing an ESC byte asserting it is stripped. Otherwise document that the human view trusts the local binary path.|security|15|cmd/atcr/home.go:92-98|claude
LOW|cmd/atcr/home.go:56|relHome joins with filepath.Separator, so on Windows it emits a backslash form (~\go\bin\atcr) while the code comment (home.go:40-43) anchors the contract to axi.md Principle 8's ~/rel example. For the agent-facing AXI payload this is a spec divergence: a consumer parsing the documented forward-slash convention would not match the backslash form.|Decide whether the axi.md path contract is OS-native or forward-slash-canonical; if canonical, filepath.ToSlash the rel and join with "/" for the AXI path, with a ToSlash-based test assertion. If OS-native is intended, amend the comment to stop citing ~/rel as the literal shape.|correctness|15|cmd/atcr/home.go:37-57|claude

