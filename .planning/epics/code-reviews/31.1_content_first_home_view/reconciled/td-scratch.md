# Technical Debt Backlog

Items from code review. Use `/resolve-td --group=N` to fix by group.
Use `/promote-tech-debt` to graduate items to formal TD sprint plans.

### [2026-07-19] From Sprint: 31.1_content_first_home_view

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| <nil> | [ ] | LOW | cmd/atcr/main.go:234 | The Args-block comment states "the RunE keeps bare atcr printing help with exit 0", but epic 31.1 replaced cmd.Help() with runHome(cmd) at main.go:262-264. The comment now documents behavior that no longer exists and will mislead a maintainer reading the root command's invariants about what a bare atcr invocation does. | Update the comment at main.go:230-234 to say the RunE renders the home view (not help) with exit 0. Verify by reading lines 230-264 for internal consistency after the edit. | maintainability | 5 | code-review | claude | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/home.go:72 | resolveHomeState classifies any errors.Is(err, os.ErrNotExist) as a true first-run, but os.ReadFile returns an ErrNotExist-matching error also for a dangling symlink target (ENOENT) or an ENOTDIR path component (e.g. .atcr is a regular file). In those cases a broken .atcr/latest pointer exists yet renders "No reviews yet" — the honesty gap the function's own doc comment (home.go:63-68) claims to avoid. | Optionally os.Lstat .atcr/latest before classifying: a present-but-broken pointer routes to the unavailable state, reserving first-run for a Lstat that itself returns ErrNotExist. Add a test symlinking .atcr/latest to a missing target asserting unavailable. Documenting the known limitation is an acceptable low-cost alternative. | correctness | 30 | code-review | claude | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/home.go:44 | relHome is a third hand-rolled copy of the "relativize against home, fall back to verbatim on .." idiom, alongside internal/tools/dispatch.go relDisplay and internal/log/redact.go relativizePaths (the code comment openly admits copying them). Three copies of the rel==".." vs ".."+sep boundary guard mean a fix to one will not propagate — the maintainability cost the existing expandHome TD in quickstart.go already gestures at. | Extract a single shared pathrel helper (e.g. internal/pathutil) that all three call sites use, or at minimum add a cross-reference TD linking them and expandHome. Verify TestRelHome still passes against the shared helper. | maintainability | 60 | code-review | claude | MEDIUM |
| <nil> | [ ] | MEDIUM | cmd/atcr/home.go:44 | relHome has no direct tests for edge cases (path under home, path outside home, home dir error); the integration test relies on the real home directory, making relativization nondeterministic | Add unit tests for relHome with mocked homeUserDir covering: path under home, path equals home, path outside home, home dir error, empty home dir | testing | 20 | code-review | dax | MEDIUM |
| <nil> | [ ] | MEDIUM | cmd/atcr/home.go:46 | relHome constructs paths using filepath.Separator, which produces backslashes on Windows, potentially breaking downstream TOON parsers or scripts that expect forward slashes in the exec_path field | Normalize path separators to forward slashes before rendering to ensure consistent cross-platform TOON payloads | portability | 10 | code-review | brad | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/home.go:53 | relHome uses a bespoke string concatenation for the home separator | Use filepath.Join("~", rel) or a consistent separator constant | style | 5 | code-review | otto | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/home.go:56 | relHome joins with filepath.Separator, so on Windows it emits a backslash form (~\go\bin\atcr) while the code comment (home.go:40-43) anchors the contract to axi.md Principle 8's ~/rel example. For the agent-facing AXI payload this is a spec divergence: a consumer parsing the documented forward-slash convention would not match the backslash form. | Decide whether the axi.md path contract is OS-native or forward-slash-canonical; if canonical, filepath.ToSlash the rel and join with "/" for the AXI path, with a ToSlash-based test assertion. If OS-native is intended, amend the comment to stop citing ~/rel as the literal shape. | correctness | 15 | code-review | claude | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/home.go:93 | The AXI renderer routes every field through toonQuote (strips control bytes / invalid UTF-8), but the human renderHomeView prints relHome(execPath) verbatim via fmt.Fprintln. A binary installed at a path containing an ANSI escape or newline reaches the terminal unfiltered. Exposure is marginal (requires an attacker-controlled install path of atcr's own binary) and matches existing verbatim path-display views, but the two renderers are asymmetric on a control-byte-safety property the AXI side treats as load-bearing. | If parity is wanted, run the human exec path through the same sanitize helper other human views use before Fprintln; test with an exec path containing an ESC byte asserting it is stripped. Otherwise document that the human view trusts the local binary path. | security | 15 | code-review | claude | MEDIUM |
| <nil> | [ ] | MEDIUM | cmd/atcr/home.go:115 | runHome uses a hardcoded fallback "atcr" for the executable path | Inject the command name from cmd.Name() to avoid hardcoding the binary name | structure | 5 | code-review | otto | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/home.go:126 | runHome falls back to the literal string "atcr" when os.Executable() fails, which may not match the actual binary name or path, causing misleading exec_path output in edge cases (e.g., renamed binaries or embedded environments) | Use cmd.CalledAs() or os.Args[0] as a more accurate fallback when os.Executable() fails | reliability | 5 | code-review | brad | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/home.go:127 | "unavailable" with empty reviewID uses ambiguous message | See note in findings — the message "Latest review pointer is unreadable" is technically correct but conflates "pointer file unreadable" with "review directory unreadable"; the user may not understand whether they need to fix the pointer or simply run a new review | correctness | 10 | code-review | bruce | MEDIUM |
| <nil> | [ ] | HIGH | cmd/atcr/home.go:148 | No integration test exercises the `atcr --axi` bare invocation path; the AXI home view is only unit-tested at the renderer level, not end-to-end (AC4) | Add a test in `main_test.go` that runs `execute(t, "--axi")` and asserts the TOON header `home[1│]{...}:` and key fields | testing | 20 | code-review | dax | MEDIUM |
| <nil> | [ ] | HIGH | cmd/atcr/main_test.go:96 | AC5 requires a golden/snapshot test pinning the non-`--axi` home-view output, but no such test exists; the existing test only checks substrings, not the exact output | Add a golden test with mocked `homeExecutable`/`homeUserDir` asserting the full byte-for-byte output for the first-run state | testing | 20 | code-review | dax | MEDIUM |
| <nil> | [ ] | HIGH | cmd/atcr/main_test.go:96 | TestRootCmd_BareInvocationShowsHomeView is not isolated from the filesystem; its output depends on whether `.atcr/latest` exists in the working directory, making it unreliable | Set up a temp directory with no `.atcr/latest` or mock `anchorDir` to ensure deterministic first-run state | testing | 30 | code-review | dax | MEDIUM |
| <nil> | [ ] | MEDIUM | cmd/atcr/home.go:81 | resolveHomeState error paths (anchorDir non-ErrNotExist, ReadReviewStatus failure) have zero test coverage; the "unavailable" degrade is never exercised | Add table-driven tests for resolveHomeState covering anchorDir permission error, corrupt pointer, and ReadReviewStatus failure | testing | 25 | code-review | dax | MEDIUM |
| U | [ ] | MEDIUM | internal/report/home_test.go:63 | Golden test hardcodes Unix forward-slash paths but relHome uses platform filepath.Separator — golden passes while real code path produces backslash paths on Windows | Use filepath.FromSlash("~/go/bin/atcr") for ExecPath in golden test data; use filepath.ToSlash on want string before assert, or compare parsed struct fields rather than raw string | portability | 30 | code-review | mira | MEDIUM |
