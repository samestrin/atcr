# atcr-findings/v1
HIGH|cmd/atcr/home.go:148|No integration test exercises the `atcr --axi` bare invocation path; the AXI home view is only unit-tested at the renderer level, not end-to-end (AC4)|Add a test in `main_test.go` that runs `execute(t, "--axi")` and asserts the TOON header `home[1|]{...}:` and key fields|0|20/`runHome` calls `report.RenderHomeViewAXI` under `if axiFromContext(ctx)` but no test in `main_test.go` executes `atcr --axi`|dax
HIGH|cmd/atcr/main_test.go:96|AC5 requires a golden/snapshot test pinning the non-`--axi` home-view output, but no such test exists; the existing test only checks substrings, not the exact output|Add a golden test with mocked `homeExecutable`/`homeUserDir` asserting the full byte-for-byte output for the first-run state|testing|20|`TestRootCmd_BareInvocationShowsHomeView` uses `Contains`/`NotContains` instead of exact output match; AC5 mandates a golden test|dax
HIGH|cmd/atcr/main_test.go:96|TestRootCmd_BareInvocationShowsHomeView is not isolated from the filesystem; its output depends on whether `.atcr/latest` exists in the working directory, making it unreliable|Set up a temp directory with no `.atcr/latest` or mock `anchorDir` to ensure deterministic first-run state|testing|30|`execute(t)` runs in the current directory without isolating `.atcr/latest`; the test only checks for description and absence of "Usage:"|dax
MEDIUM|cmd/atcr/home.go:81|resolveHomeState error paths (anchorDir non-ErrNotExist, ReadReviewStatus failure) have zero test coverage; the "unavailable" degrade is never exercised|Add table-driven tests for resolveHomeState covering anchorDir permission error, corrupt pointer, and ReadReviewStatus failure|testing|25|`resolveHomeState` returns `homeState{unavailable: true}` on non-ErrNotExist errors and on ReadReviewStatus failure, but no test triggers these branches|dax
MEDIUM|cmd/atcr/home.go:44|relHome has no direct tests for edge cases (path under home, path outside home, home dir error); the integration test relies on the real home directory, making relativization nondeterministic|Add unit tests for relHome with mocked homeUserDir covering: path under home, path equals home, path outside home, home dir error, empty home dir|testing|20|`relHome` branches on `filepath.Rel` result and home dir error; no test pins these paths|dax
