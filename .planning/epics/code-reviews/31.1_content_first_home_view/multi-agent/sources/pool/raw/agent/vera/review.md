[truncated — reasoning incomplete; raise max_tokens]

xt". That is fine.

Now, potential breaking: In home.go, they import "github.com/samestrin/atcr/internal/fanout". That is fine.

Now, potential breaking: In home.go, they import "github.com/samestrin/