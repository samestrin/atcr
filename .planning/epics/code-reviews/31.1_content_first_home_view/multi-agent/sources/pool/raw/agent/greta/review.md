CRITICAL|cmd/atcr/main.go:243|PersistentPreRunE unconditionally reads root's --axi flag now that it's registered, defaulting to false and overwriting context, which breaks atcr review --axi|Guard the context propagation with cmd.Flags().Changed("axi") or check if cmd.Parent() == nil before reading the root-local flag|correctness|15|root.PersistentPreRunE runs for all subcommands; cmd.Flags().Lookup("axi") now always succeeds, reading root's default false and poisoning the context for review --axi