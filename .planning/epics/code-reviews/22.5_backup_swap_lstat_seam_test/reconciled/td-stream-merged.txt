LOW|internal/fanout/reviewdir_test.go:455|The withLstatStub doc comment (reviewdir_test.go:452-461) and the new test's inline comment (reviewdir_test.go:536-539) justify the selective stub by claiming the .bak.old/.bak.new RemoveAll legs run on the real os.Lstat and must be passed through. Those legs use os.RemoveAll (reviewdir.go:322,325) which does NOT route through lstatFn, so the stub's else-branch (return os.Lstat(name)) is never invoked in this test — the probe at line 341 returns immediately. The comment overstates the live mechanism.|Reword the comments to describe the selective delegation as defensive future-proofing (e.g. in case the belt-and-suspenders os.Lstat at reviewdir.go:362 is ever rerouted through lstatFn) rather than a live requirement of this test's RemoveAll legs. Verify reviewdir.go:341 is the only lstatFn call site via grep.|maintainability|5|code-review|claude|MEDIUM
LOW|internal/fanout/reviewdir_test.go:527|The TestBackupExisting_LstatProbeFailureSurfaces doc comment hardcodes source line numbers (reviewdir.go:341 probe, reviewdir.go:346-347). These references rot when any line above shifts; a future reader chasing them may land on unrelated code. Note this line-number convention is pre-existing house style (seam var comment, epic plan), not new to this change.|Reference the branch by symbol/behavior (the non-ErrNotExist arm of the prior-backup probe returning the wrapped 'checking prior backup' error) instead of hardcoded line numbers.|maintainability|5|code-review|claude|MEDIUM
