# atcr-findings/v1
MEDIUM|internal/fanout/reviewdir_test.go:458|No timeout on provider call|Derive per-call context with deadline from config|performance|15|http.Client zero value has no Timeout|mira
MEDIUM|internal/fanout/reviewdir_test.go:456|Package-level lstatFn mutation in withLstatStub lacks synchronization; concurrent test execution (t.Parallel) will race on the global variable, causing flaky failures or data races|Guard seam mutations with a sync.Mutex or switch to a per-test config struct to avoid global state|concurrency|15|lstatFn = stub and t.Cleanup(...) modify a package var without locking; unsafe for parallel test runs|brad
HIGH|internal/fanout/reviewdir_test.go:454|Copy-paste error restores removePathFn instead of lstatFn in cleanup leaking the stub to subsequent tests|Change removePathFn = orig to lstatFn = orig in withLstatStub's t.Cleanup callback|copy-paste|3|t.Cleanup(func() { removePathFn = orig })|archer
LOW|internal/fanout/reviewdir_test.go:458|Comment claims permissions cannot isolate the branch, but the test uses a stub; the phrasing is a bit redundant given the task|Simplify comment to focus on the necessity of the seam for deterministic failure injection|style|2|`Permissions cannot isolate this branch...`|otto
