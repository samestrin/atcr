LOW|internal/circuitbreaker/breaker.go:128|Potential probe leak on panic|The probeInFlight flag is set to true in Allow() but only released in RecordSuccess, RecordFailure, or ReleaseProbe. If the logic between Allow() and these calls panics, the circuit remains half-open indefinitely.|Wrap the call site in internal/llmclient/client.go with a defer that calls ReleaseProbe if no verdict is recorded|correctness|15|probeInFlight is set in Allow() but released in separate methods; a panic in the caller wedges the circuit|otto