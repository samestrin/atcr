# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/3.5_severity-rank-consolidation.md
# TARGET_TYPE: epic
# CREATED: 2026-06-16 12:48:27
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
MEDIUM|internal/reconcile/disagree.go:282|Epic 3.5 normalized severity lookups only at the scoped boundary sites (merge.go:104, grayZoneItem disagree.go:338/360) but left disagree.go's sibling finding-level lookups raw: spreadFromDisagreement (disagree.go:248-249, TrimSpace only, no ToUpper), severitySplitItem (:282), soloItem (:298), verificationItem (:322), and the sortDisagreements tiebreak (:463) all key SeverityRank[f.Severity] / SeverityRank[TrimSpace(...)] without stream.NormalizeSeverity. These consume the SAME reconciled findings.json that render.go:122 normalizes defensively, and the codebase itself documents that surface as potentially hand-edited or externally produced (render.go:116-118). Severity is never normalized at parse time (parser.go:193 stores f[0] verbatim). On mixed-case input a real HIGH solo scores rank 0 and sorts dead-last, and the radar-JSON ordering diverges from the normalized report-view ordering. Not a happy-path bug (in-pipeline reconciled severities are canonical via mergeSeverity), but a latent desync on the exact non-canonical-casing surface the epic set out to eliminate.|Wrap each raw SeverityRank lookup in disagree.go in stream.NormalizeSeverity: lines 248-249 (replace strings.TrimSpace with stream.NormalizeSeverity, which trims and upper-cases), 282, 298, 322, and 463. Add a radar test feeding a lowercase-severity JSONFinding and asserting a non-zero solo score plus an ordering that matches render.go. Either normalize all reconcile lookup sites or none — do not leave the file half-migrated.|correctness|30|internal/reconcile/disagree.go:243-330|claude
LOW|internal/reconcile/reconcile.go:108|sortMerged (reconcile.go:108) and AtOrAbove (gate.go:45,49) do raw SeverityRank[...] lookups. These are correct today only because mergeSeverity now canonicalizes Merged.Severity upstream (merge.go:116) before sortMerged/gate run, making the merge.go normalization silently load-bearing for two other files. Every other consumer (fanout/postprocess, verify, report) normalizes at the lookup site; reconcile's own sort/gate are the lone outliers trusting upstream canonicalization, an invisible coupling a future simplification of mergeSeverity would break.|Normalize at the lookup site in sortMerged (reconcile.go:108) and AtOrAbove (gate.go:49, and threshold at :45) using SeverityRank[stream.NormalizeSeverity(...)] so each consumer is self-defending. Verify the gate test with a lowercase merged severity. This makes mergeSeverity's normalization a convenience rather than a hidden correctness dependency.|maintainability|30|internal/reconcile/gate.go:44-50|claude
LOW|internal/stream/severity.go:23|The NormalizeSeverity doc-comment (stream/severity.go:22-26) claims "Every consumer normalizes through this single helper so their lookups stay identical and the fan-out/reconcile casing asymmetry cannot reappear." That invariant is not held: disagree.go (248,282,298,322,463), reconcile.go (108), and gate.go (45,49) still do raw, un-normalized SeverityRank lookups. A misleading comment is worse than none — a future reader will trust the guarantee and skip defensive normalization.|Either make the claim true by normalizing the remaining raw lookup sites, or soften the wording to "consumers SHOULD normalize." Verify by grepping SeverityRank\[ for any key not wrapped in NormalizeSeverity. Prefer making it true (bundle with the disagree.go and reconcile/gate items).|docs|15|internal/stream/severity.go:22-26|claude
LOW|internal/report/render.go:204|writeSummaryGrid buckets findings with counts[f.Severity] (render.go:204) using the raw severity against a map keyed by canonical reconcile.SevCritical/SevHigh/... constants. A mixed-case "high" from hand-edited findings.json misses its bucket and falls into the OTHER row, even though severityRankOf (same file, same input) ranks it correctly as HIGH. The summary grid and the findings list disagree on what counts as a known severity — the consolidation touched severityRankOf for exactly this casing reason but left its sibling counter un-normalized.|Normalize the bucket key: c, ok := counts[canonicalize(f.Severity)] — a canonicalize helper already exists at render.go:411 and matches NormalizeSeverity. Add a render test with a lowercase severity asserting it lands in its canonical bucket, not OTHER.|correctness|15|internal/report/render.go:200-219|claude
LOW|internal/report/render.go:122|severityRankOf now calls stream.NormalizeSeverity (ToUpper+TrimSpace, allocating a new string) and it is invoked inside the sort.SliceStable comparator (render.go:122) on both operands per comparison, O(n log n) times. The pre-3.5 version did a bare reconcile.SeverityRank[s] lookup with no allocation, so the consolidation introduced a minor per-compare allocation regression in the render hot path.|Decorate-sort: precompute rank := severityRankOf(f.Severity) once per finding into a parallel slice (or a small struct) before sorting, then compare cached ints in the closure. Verify existing render tests still pass; optional micro-benchmark on a few-hundred-finding slice. Low priority — only matters at high finding counts.|performance|15|internal/report/render.go:119-124|claude

