# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/2.1_manifest_review_stage_recording.md
# TARGET_TYPE: epic
# CREATED: 2026-06-13 18:22:41
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
LOW|internal/fanout/review.go:660|snapshotManifestFields infers live-vs-worktree mode purely from root==repo string equality. This is correct only because NewSnapshotManager stores repoRoot verbatim and SnapshotFor's fast path returns that exact string. The call site encodes implicit knowledge of SnapshotFor's internals: if SnapshotFor is later changed to canonicalize repoRoot (EvalSymlinks/filepath.Abs, as snapshotCleanupGuard already does for its temp guard), a clean-HEAD live snapshot misclassifies as worktree and the repo root leaks into snapshot_worktree_path. No integration test drives the real SnapshotFor fast path through to the manifest — the e2e only exercises the worktree slow path and the unit tests pass literal strings — so the coupling is unguarded.|Make the live/worktree distinction explicit at the boundary rather than inferred from path equality: have SnapshotFor return a mode/sentinel (or a small SnapshotResult struct with Mode and WorktreePath) that ExecuteReview records directly. Alternatively add an integration test that runs ExecuteReview against a clean HEAD so the real fast path asserts snapshot_mode==live and snapshot_worktree_path=="" on disk, guarding the root==repo assumption.|maintainability|30|internal/fanout/review.go:656-665|claude

