MEDIUM|internal/fanout/resume.go:287|Sprint plan constraint dropped on resume|Load and pass constraint from req.SprintPlanPath in PrepareResume, or load it from persisted slots|maintainability|15|buildSlots called with empty scopeConstraint during resume; SCOPE CONSTRAINT may not survive resume|mira
MEDIUM|internal/fanout/resume.go:287|Sprint plan SCOPE CONSTRAINT not applied during resume|Thread scopeConstraint from req.SprintPlanPath through PrepareResume → buildSlots, or document that slots persist the constraint|correctness|15|buildSlots receives "" for scopeConstraint during resume; constraint may be lost if payloads rebuilt from raw diff|mira
MEDIUM|internal/fanout/resume.go:287|Sprint plan SCOPE CONSTRAINT not threaded through PrepareResume → buildSlots|If payloads are loaded from persisted slots (which contain the constraint), this is fine. If payloads are rebuilt from raw diff, the constraint is lost on resume. Verify and either thread the constraint from req.SprintPlanPath or document that slots are loaded with constraint already applied.|correctness|15|buildSlots called with "" for scopeConstraint during resume; sprint plan constraint may be lost if payloads rebuilt from raw diff instead of persisted slots|mira
MEDIUM|internal/fanout/resume.go:287|Sprint plan SCOPE CONSTRAINT may not survive resume|Verify how `payloads` is loaded in PrepareResume and either thread scopeConstraint from req.SprintPlanPath or rely on persisted slot prompts (which would already carry the constraint)|correctness|15|buildSlots called with "" for scopeConstraint; SprintPlanPath is on req but not threaded through|mira
