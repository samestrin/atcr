MEDIUM|.github/workflows/refresh-synthetic-manifest.yml:21|actions/checkout@v4 and actions/setup-go@v5 are pinned to mutable major-version tags, not commit SHAs, in a workflow granted contents:write and pull-requests:write that pushes branches and opens PRs. If either tag is repointed to a malicious commit (tag-hijack supply-chain attack) that code runs with write access and can push arbitrary commits.|Pin both uses: lines to full commit SHAs (actions/checkout@<sha> # v4), and consider scoping the write permissions to the Open PR job rather than the whole workflow. Verify the workflow still runs after pinning.|security|15|code-review|claude|MEDIUM
LOW|cmd/atcr/quickstart.go:114|scaffoldWorkflow and writeSyntheticRegistry (line 285) check existence with os.Lstat and later write with os.WriteFile (O_CREATE|O_TRUNC, follows symlinks). A file or symlink appearing between the check and the write is silently overwritten/followed despite the never-overwrite-without-force contract. Low real-world risk for a local interactive wizard but it technically violates the no-clobber guard under a race or a pre-planted symlink.|Replace check-then-write with an atomic create: os.OpenFile(path, O_CREATE|0|code-review|security|MEDIUM
LOW|cmd/atcr/quickstart.go:154|readLine returns "" for both clean EOF and any scanner.Scan()==false caused by a read error or a line exceeding bufio.MaxScanTokenSize (64KB); scanner.Err() is never inspected. A read failure on the key prompt is indistinguishable from the user pressing Enter, so the wizard silently proceeds down the no-key branch and reports success having done nothing, with no diagnostic surfaced.|Check scanner.Err() at end of keyEnvFlow and surface a non-nil error to o.errOut, and have readLine return (string, bool) to distinguish EOF from error. Verify with a test injecting a reader that errors mid-stream and asserting a diagnostic is emitted.|error-handling|15|code-review|claude|MEDIUM
LOW|cmd/atcr/quickstart.go:192|The ~/ expansion is implemented twice - appendExport (line 193) returns the os.UserHomeDir() error while resolveProfilePath (line 234) swallows it and falls through to filepath.Abs("~/...") producing a bogus $cwd/~/... path the guard then compares. Neither expands a bare ~ (no trailing slash). The two copies can drift and the guard can end up validating a different path than the one written.|Extract a single expandHome(path) (string, error) helper used by both appendExport and resolveProfilePath; handle bare ~ and propagate the home-dir error consistently. Verify guard and writer resolve identical absolute paths for the same input via a shared unit test.|maintainability|30|code-review|claude|MEDIUM
LOW|cmd/atcr/quickstart.go:200|os.OpenFile(..., 0o600) only applies 0o600 when the file is created. If the named profile already exists at 0o644 (common for ~/.zshrc) the append leaves the secret API key in a group/world-readable file, since OpenFile never chmods an existing file. The invariant permits writing to a user-named profile but silently degrading the key's on-disk protection is a real exposure.|After a successful append, os.Chmod(profile, 0600), or at minimum warn if the existing mode has group/other bits set. Verify by pre-creating the target at 0644 and asserting the resulting mode is 0600 or a warning is printed.|security|15|code-review|claude|MEDIUM
HIGH|cmd/atcr/quickstart.go:213|profileIsAtcrOwned canonicalizes the user-named shell-profile path with filepath.Abs only (lexical Clean resolves .. but NOT symlinks), while appendExport opens it with os.OpenFile which follows symlinks. A profile path that is a symlink into ./.atcr/ resolves lexically to itself so the guard returns false (not owned), then appendExport writes the raw API key through the symlink into an atcr-owned file, defeating the key-never-in-atcr-file invariant this function exists to enforce.|In resolveProfilePath/profileIsAtcrOwned resolve symlinks before comparison via filepath.EvalSymlinks on the deepest existing ancestor, or have appendExport open the final component O_NOFOLLOW and stat-compare against the .atcr tree. Add a test that symlinks a named profile into .atcr/ and asserts the append is refused.|security|60|code-review|claude|MEDIUM
MEDIUM|cmd/atcr/quickstart.go:219|profileIsAtcrOwned uses case-sensitive strings.HasPrefix and abs==regAbs comparisons. On darwin/APFS (case-insensitive by default) naming the profile ./.ATCR/config.yaml yields abs /proj/.ATCR/... which does not HasPrefix /proj/.atcr/, so the guard returns false, yet appendExport writes to .ATCR/config.yaml which the OS maps to the same inode as .atcr/config.yaml. The key lands in an atcr-owned file with only a case change, no symlink required.|Normalize case for the comparison on case-insensitive platforms (EqualFold-style prefix check) or compare via os.SameFile on the resolved parent dirs instead of string prefixes. Verify by running the guard against ./.ATCR/config on macOS and asserting it reports owned.|security|30|code-review|claude|MEDIUM
MEDIUM|internal/quickstart/manifest.go:59|validate() rejects control characters only inside m.Models (line 78) but checks Provider.Name/BaseURL/APIKeyEnv for non-emptiness only. Those three fields are emitted verbatim into registry.yaml (lines 113-115); a newline in provider.name forges entirely new YAML keys/agents - a strictly worse injection than the model-id case the code went out of its way to block. The asymmetry defeats the stated defense-in-depth against a hostile refresh.|In validate() run the same unicode.IsControl scan (and the YAML-significant-char scan) over Provider.Name, BaseURL, and APIKeyEnv. Verify with a manifest whose provider.name contains a newline - it must be rejected by LoadManifest.|security|30|code-review|claude|MEDIUM
MEDIUM|internal/quickstart/manifest.go:78|The control-char check is the only content check on model ids, but ids render as bare `model: <id>` (line 128). A controller of the /models response can return an id like `gpt: evil` (YAML rejects a plain scalar containing colon-space, breaking every generated registry.yaml - a DoS) or `gpt # x` (# silently truncates the value). Control chars are blocked so the JSON diff looks clean while the YAML breakage only surfaces later at quickstart time.|In validate() reject model ids and provider fields that are not safe plain YAML scalars, or render values through a real YAML encoder (gopkg.in/yaml.v3) instead of fmt.Fprintf string concatenation in RegistryYAML/WorkflowYAML. Verify by feeding an id `a: b` through BuildManifestFromModels then RegistryYAML and confirming the output parses.|security|60|code-review|claude|MEDIUM
LOW|internal/quickstart/manifest.go:89|validate() never checks SignupURL, so a manifest with an empty/garbage signup_url passes and the wizard shows a broken link. Additionally SignupLink appends the referral by checking only for ?; if the URL has a fragment (https://x/#frag) the referral is appended after the fragment (...#frag?referral=y) placing the query inside the fragment where it is inert.|Add a non-empty (ideally url.Parse-valid) check for SignupURL in validate(), and build the link with url.Parse + u.Query().Set so query/fragment ordering is correct. Verify with a signup_url containing a # fragment.|error-handling|30|code-review|claude|MEDIUM
LOW|internal/quickstart/manifest.go:122|The len(m.Models)==0 guard protects the modulo but there is no guard on an empty roster. With a non-empty model set and an empty roster the code writes the agents: header (line 122) and the loop adds nothing, producing a trailing agents: with a null value that a strict registry loader may parse as agents: null or reject, yielding a confusing failure for a caller who passed no personas.|In RegistryYAML skip the agents: header when len(roster)==0 (return after the provider block, mirroring the empty-models guard). Verify by calling RegistryYAML(m, nil) and confirming no bare agents: line is emitted.|error-handling|15|code-review|claude|MEDIUM
MEDIUM|internal/quickstart/workflow.go:41|WorkflowYAML emits `go install github.com/samestrin/atcr/cmd/atcr@latest` into every user's .github/workflows/atcr.yml. Every CI run pulls whatever @latest resolves to at that moment, so builds are non-reproducible and a compromised or brand-new atcr release silently runs with the user's synthetic API-key secret in scope. Shipping this pattern to every quickstart adopter multiplies the blast radius.|Emit a pinned version (@v1 or a specific tag) instead of @latest, or point users at the published composite action already referenced in the header comment. Verify the generated scaffold pins a concrete ref.|security|15|code-review|claude|MEDIUM
LOW|internal/quickstart/workflow.go:45|run: atcr review --base "origin/${{ github.base_ref }}" inlines a GitHub expression into the shell command, which GitHub hardening guidance explicitly warns against (the value is substituted before the shell runs). base_ref is relatively low-risk but propagating this anti-pattern to every quickstart user is the wrong default; a base branch name with shell metacharacters would break or inject.|Pass the ref via an env: variable and reference "$BASE_REF" in the run script instead of inlining the expression. Verify the generated scaffold no longer contains a ${{ expression inside a run: line.|security|15|code-review|claude|MEDIUM
