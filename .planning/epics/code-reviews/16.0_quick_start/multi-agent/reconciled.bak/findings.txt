# atcr-findings/v1
HIGH|cmd/atcr/quickstart.go:97|runQuickstart silently discards writeSyntheticRegistry error when runInit fails|Return the error from writeSyntheticRegistry or wrap it; currently the error is discarded and the function falls through to keyEnvFlow|testing|10|`if err := writeSyntheticRegistry(o, manifest, builtins.Names()); err != nil { return err }` — the error is returned, but the earlier `runInit` error path at line 97 returns without calling writeSyntheticRegistry, so this is a false read. The real gap: no test exercises runInit failure followed by the rest of the flow.|dax|MEDIUM
HIGH|internal/quickstart/manifest.go:83|Unquoted fmt.Fprintf for provider name and base_url allows YAML structure injection if the refreshed API returns values containing ':', '#', or other special characters|Use a proper YAML encoder (e.g. yaml.Marshal) or quote all interpolated values with single quotes and escape embedded quotes|security|20|fmt.Fprintf(&b, "  %s:\n", m.Provider.Name) and fmt.Fprintf(&b, "    base_url: %s\n", m.Provider.BaseURL) construct YAML via string concatenation without escaping or quoting|brad|MEDIUM
MEDIUM|cmd/atcr/quickstart.go:152|keyEnvFlow scanner.Scan() failure (e.g. EOF on stdin) returns empty string with no error surfaced|Return an error when scanner.Err() is non-nil after the loop, or check it after each prompt; a truncated stdin silently yields empty key/profile|style|15|[otto] `os.MkdirAll(filepath.Dir(wfPath), 0o755)` and `os.WriteFile(wfPath, ..., 0o644)` / [dax] `readLine` returns `""` on both empty input and scanner failure; the caller treats both as "skip" with no distinction (disagreement: LOW vs MEDIUM)|dax,otto|HIGH
MEDIUM|cmd/atcr/quickstart.go:173|resolveProfilePath uses filepath.Abs which does not resolve symlinks, allowing a user to bypass the atcr-owned file guard by passing a symlink pointing into .atcr/ or the registry|Use filepath.EvalSymlinks on both the profile path and the atcr/registry paths before comparison|security|10|abs, err := filepath.Abs(profile) in resolveProfilePath does not dereference symlinks, so a symlink to an atcr-owned file bypasses the prefix/equals checks in profileIsAtcrOwned|brad|MEDIUM
MEDIUM|cmd/atcr/quickstart.go:258|shellSingleQuote does not escape control characters other than single-quote|A key containing a newline or escape sequence injected via paste can break the export line or inject shell commands when the user sources the profile|security|15|`strings.ReplaceAll(s, "'", `'\''`)` only handles single quotes; a newline in the key would produce a multi-line export that a shell would execute|dax|MEDIUM
MEDIUM|internal/quickstart/manifest.go:72|validate rejects control characters in model ids but not in provider Name, BaseURL, or APIKeyEnv|Add control-character checks for Provider.Name, Provider.BaseURL, and Provider.APIKeyEnv; a newline in BaseURL would forge YAML structure in the generated registry just like a model id|security|10|Only `m.Models` loop checks `unicode.IsControl`; Provider fields are validated only for whitespace-trimmed emptiness|dax|MEDIUM
