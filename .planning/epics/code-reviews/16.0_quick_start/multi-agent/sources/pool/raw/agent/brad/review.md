HIGH|internal/quickstart/manifest.go:83|Unquoted fmt.Fprintf for provider name and base_url allows YAML structure injection if the refreshed API returns values containing ':', '#', or other special characters|Use a proper YAML encoder (e.g. yaml.Marshal) or quote all interpolated values with single quotes and escape embedded quotes|security|20|fmt.Fprintf(&b, "  %s:\n", m.Provider.Name) and fmt.Fprintf(&b, "    base_url: %s\n", m.Provider.BaseURL) construct YAML via string concatenation without escaping or quoting
MEDIUM|cmd/atcr/quickstart.go:173|resolveProfilePath uses filepath.Abs which does not resolve symlinks, allowing a user to bypass the atcr-owned file guard by passing a symlink pointing into .atcr/ or the registry|Use filepath.EvalSymlinks on both the profile path and the atcr/registry paths before comparison|security|10|abs, err := filepath.Abs(profile) in resolveProfilePath does not dereference symlinks, so a symlink to an atcr-owned file bypasses the prefix/equals checks in profileIsAtcrOwned
MEDIUM|internal/quickstart/refresh.go:58|io.ReadAll reads the entire stdin into memory without an upper bound, allowing a malicious or misconfigured API response to OOM the refresh process|Wrap in with io.LimitReader(in, 10*1024*1024) before reading|resource|5|apiResp, err := io.ReadAll(in) allocates a buffer matching the full input size with no cap
LOW|cmd/atcr/quickstart.go:114|bufio.NewScanner uses a default 64KB token limit, silently truncating or failing on API keys or profile paths longer than 64KB|Increase the buffer with scanner.Buffer(make([]byte, 0, 1024*1024), 1024*1024) or switch to bufio.Reader.ReadString|robustness|10|scanner := bufio.NewScanner(o.in) relies on the default MaxScanTokenSize of 64KB for readLine
LOW|.github/workflows/refresh-synthetic-manifest.yml:52|git checkout -b fails if the branch already exists from a concurrent or retried run, causing the workflow to abort without cleanup|Use git checkout -B to force-create/reset the branch, or add git branch -D before checkout|reliability|5|git checkout -b "$branch" will error if the branch exists, and the script has no fallback or cleanup