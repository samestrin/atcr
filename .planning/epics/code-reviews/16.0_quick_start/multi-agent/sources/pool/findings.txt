# atcr-findings/v1
HIGH|cmd/atcr/quickstart.go:97|runQuickstart silently discards writeSyntheticRegistry error when runInit fails|Return the error from writeSyntheticRegistry or wrap it; currently the error is discarded and the function falls through to keyEnvFlow|testing|10|`if err := writeSyntheticRegistry(o, manifest, builtins.Names()); err != nil { return err }` — the error is returned, but the earlier `runInit` error path at line 97 returns without calling writeSyntheticRegistry, so this is a false read. The real gap: no test exercises runInit failure followed by the rest of the flow.|dax
MEDIUM|cmd/atcr/quickstart.go:153|keyEnvFlow scanner.Scan() failure (e.g. EOF on stdin) returns empty string with no error surfaced|Return an error when scanner.Err() is non-nil after the loop, or check it after each prompt; a truncated stdin silently yields empty key/profile|testing|15|`readLine` returns `""` on both empty input and scanner failure; the caller treats both as "skip" with no distinction|dax
MEDIUM|cmd/atcr/quickstart.go:175|appendExport failure is logged to errOut but the error is swallowed — caller gets nil|Return the error so the caller can decide; a disk-full or permission-denied on the shell profile is silently ignored|error-handling|10|`if err := appendExport(profile, env, key); err != nil { _, _ = fmt.Fprintf(o.errOut, ...); return nil }`|dax
MEDIUM|cmd/atcr/quickstart.go:258|shellSingleQuote does not escape control characters other than single-quote|A key containing a newline or escape sequence injected via paste can break the export line or inject shell commands when the user sources the profile|security|15|`strings.ReplaceAll(s, "'", `'\''`)` only handles single quotes; a newline in the key would produce a multi-line export that a shell would execute|dax
MEDIUM|internal/quickstart/manifest.go:72|validate rejects control characters in model ids but not in provider Name, BaseURL, or APIKeyEnv|Add control-character checks for Provider.Name, Provider.BaseURL, and Provider.APIKeyEnv; a newline in BaseURL would forge YAML structure in the generated registry just like a model id|security|10|Only `m.Models` loop checks `unicode.IsControl`; Provider fields are validated only for whitespace-trimmed emptiness|dax
MEDIUM|internal/quickstart/refresh.go:39|BuildManifestFromModels filters empty-trimmed ids but does not reject control characters in ids|Add the same control-character rejection as manifest.validate() so the refresh job cannot emit a manifest that LoadManifest would reject (defense-in-depth gap)|testing|10|`if strings.TrimSpace(d.ID) != ""` accepts ids with embedded newlines; validate() later catches them but the refresh job should reject early|dax
LOW|cmd/atcr/quickstart_test.go:1|No test exercises the error path when runInit fails (e.g. permission-denied on .atcr/)|Add a test with a read-only target directory and assert the quickstart fails before registry/worfklow steps run|testing|15|All tests use writable temp dirs; the `runInit` error return at line 97 is uncovered|dax
LOW|cmd/atcr/quickstart_test.go:1|No test exercises the scanner error path (closed stdin, /dev/null input)|Add a test with an io.Reader that returns an error and assert keyEnvFlow surfaces it|testing|10|`quickstartInput = "\n\n"` always succeeds; scanner.Err() path is uncovered|dax
LOW|internal/quickstart/workflow_test.go:1|TestWorkflowYAML_ReviewOnlySynthetic does not assert the generated YAML is valid YAML|Parse the output with yaml.v3 and assert no error; a stray unquoted colon or brace could produce invalid CI YAML|testing|5|Only string-contains assertions; no structural validation|dax
HIGH|internal/quickstart/manifest.go:83|Unquoted fmt.Fprintf for provider name and base_url allows YAML structure injection if the refreshed API returns values containing ':', '#', or other special characters|Use a proper YAML encoder (e.g. yaml.Marshal) or quote all interpolated values with single quotes and escape embedded quotes|security|20|fmt.Fprintf(&b, "  %s:\n", m.Provider.Name) and fmt.Fprintf(&b, "    base_url: %s\n", m.Provider.BaseURL) construct YAML via string concatenation without escaping or quoting|brad
MEDIUM|cmd/atcr/quickstart.go:173|resolveProfilePath uses filepath.Abs which does not resolve symlinks, allowing a user to bypass the atcr-owned file guard by passing a symlink pointing into .atcr/ or the registry|Use filepath.EvalSymlinks on both the profile path and the atcr/registry paths before comparison|security|10|abs, err := filepath.Abs(profile) in resolveProfilePath does not dereference symlinks, so a symlink to an atcr-owned file bypasses the prefix/equals checks in profileIsAtcrOwned|brad
MEDIUM|internal/quickstart/refresh.go:58|io.ReadAll reads the entire stdin into memory without an upper bound, allowing a malicious or misconfigured API response to OOM the refresh process|Wrap in with io.LimitReader(in, 10*1024*1024) before reading|resource|5|apiResp, err := io.ReadAll(in) allocates a buffer matching the full input size with no cap|brad
LOW|cmd/atcr/quickstart.go:114|bufio.NewScanner uses a default 64KB token limit, silently truncating or failing on API keys or profile paths longer than 64KB|Increase the buffer with scanner.Buffer(make([]byte, 0, 1024*1024), 1024*1024) or switch to bufio.Reader.ReadString|robustness|10|scanner := bufio.NewScanner(o.in) relies on the default MaxScanTokenSize of 64KB for readLine|brad
LOW|.github/workflows/refresh-synthetic-manifest.yml:52|git checkout -b fails if the branch already exists from a concurrent or retried run, causing the workflow to abort without cleanup|Use git checkout -B to force-create/reset the branch, or add git branch -D before checkout|reliability|5|git checkout -b "$branch" will error if the branch exists, and the script has no fallback or cleanup|brad
LOW|cmd/atcr/quickstart.go:152|`scaffoldWorkflow` uses a hardcoded 0o755 for directories and 0o644 for files|Use a constant or a configuration for default file permissions to ensure consistency across the tool|style|5|`os.MkdirAll(filepath.Dir(wfPath), 0o755)` and `os.WriteFile(wfPath, ..., 0o644)`|otto
LOW|cmd/atcr/quickstart.go:238|`appendExport` uses a hardcoded 0o600 for shell profiles|Ensure this permission is appropriate for all target OSs or make it configurable|style|5|`os.OpenFile(profile, os.O_APPEND/os.O_CREATE/os.O_WRONLY, 0o600)`|otto
MEDIUM|internal/quickstart/manifest.go:105|`RegistryYAML` uses `fmt.Fprintf` for a simple string builder|Use `b.WriteString` for static strings to avoid the overhead of fmt parsing|idiom|5|`fmt.Fprintf(&b, "  %s:\n", m.Provider.Name)`|otto
LOW|internal/quickstart/workflow.go:36|`WorkflowYAML` uses `go install ...@latest` in a generated CI scaffold|Pin the version to a specific tag (e.g., @v1.0.0) to prevent breaking changes from silently entering consumer CI pipelines|style|10|`run: go install github.com/samestrin/atcr/cmd/atcr@latest`|otto
