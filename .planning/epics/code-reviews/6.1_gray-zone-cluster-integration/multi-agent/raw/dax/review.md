MEDIUM|internal/debate/cluster.go:120|Drift recovery may absorb unrelated finding if exactly one unmatched record at member location|Add stricter check: verify unmatched record's reviewer or evidence matches cluster member|correctness|15|"if len(idxs) == 1 { matched[idxs[0]] = true }" without verifying relation to cluster|bruce
LOW|internal/debate/cluster.go:53|filterMergedClusters uses location only, not problem, could filter out a different gray-zone cluster at same location|Use full FindingKey (File+Line+Problem) for filtering|maintainability|5|"mergedLocs[locationKey(it.File, it.Line)]" ignores Problem, risking collision|bruce
LOW|internal/reconcile/merge.go:300|mergeVerification may include empty skeptic strings in output|Filter out empty skeptics before joining|maintainability|5|"seen[v.Skeptic]" adds empty string if Skeptic is empty|bruce