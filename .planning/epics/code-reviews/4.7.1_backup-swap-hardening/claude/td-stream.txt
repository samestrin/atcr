# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/4.7.1_backup-swap-hardening.md
# TARGET_TYPE: epic
# CREATED: 2026-06-20 05:32:38
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
MEDIUM|internal/fanout/reviewdir.go:318|backupExisting() unconditionally RemoveAll's <path>.bak.old and <path>.bak.new at entry, but forceBackupOutputDir() only calls guardForeignBackup(dir+".bak") — the two staging siblings are not guarded. On an arbitrary --output-dir a user who owns <output-dir>.bak.old or <output-dir>.bak.new has it silently deleted by --force, re-opening the Epic 4.7 "never silently delete user data" contract. NOTE: epic clarification Q4 deliberately scoped the guard to .bak only (atcr-internal suffixes); this finding re-raises that decision as worth reconsidering, not a defect against the recorded scope.|In forceBackupOutputDir() (reviewdir.go:440) extend the guard to guardForeignBackup(dir+".bak.old") and guardForeignBackup(dir+".bak.new") before backupExisting, or refuse if either exists non-empty. Add a test pre-creating a foreign <output-dir>.bak.old with user content and assert --force errors instead of deleting it.|security|60|internal/fanout/reviewdir.go:317-323,436-444|claude
MEDIUM|internal/fanout/reviewdir.go:388|The EXDEV fallback's own failure legs are untested: backupCrossDevice's CopyPath error (line 392), inner os.Rename(backupNew,backup) swap error (395), and RemoveAll(path) vacate error (398), plus the restorePriorBackup(...) call wired on backupCrossDevice failure (line 343). The "EXDEV falls back without data loss" AC is proven only for the success path; no test proves the prior .bak is restored when the cross-device copy fails.|Add a test that forces renameFn to return syscall.EXDEV and makes the copy fail (a copy seam, or unreadable src), staging a prior .bak first; assert the prior .bak content is restored intact, the live tree survives, and .bak.new is cleaned up. Cover the copy-failure leg at minimum.|testing|120|internal/fanout/reviewdir.go:388-402; internal/fanout/reviewdir_test.go:465-493|claude
MEDIUM|internal/fanout/reviewdir.go:373|The advertised "an interrupted swap never leaves the user with neither generation" invariant is conditional on a best-effort SILENT restore succeeding. If restorePriorBackup's os.Rename(backupOld,backup) fails (e.g. backup partially recreated, or perms change), the only surviving copy is stranded under .bak.old — which the very next run's entry-time RemoveAll(backupOld) (line 318) then deletes. Same pattern at the copy site swapStagedBackup (atomic.go:159-167) feeding atomic.go:97 RemoveAll(bakOld).|Surface restore failures loudly (log/return) instead of dropping them, and/or do not auto-RemoveAll .bak.old at entry when .bak is absent — treat a lone .bak.old as the surviving generation to recover. Add a test where restore fails and a subsequent run is asserted not to delete the only surviving copy.|correctness|120|internal/fanout/reviewdir.go:373-378,318-320; internal/atomicfs/atomic.go:159-167,97-99|claude
LOW|internal/fanout/reviewdir.go:380|Doc overclaim: backupCrossDevice's comment says it replicates "os.Rename(path,backup)'s postcondition", but the fallback is two non-atomic steps (rename backupNew over backup, then RemoveAll(path)). A crash between them leaves the tree duplicated at both path and path.bak; entry-time reconcile only clears .bak.old/.bak.new, not a leftover live path. The move it emulates is genuinely atomic; the fallback cannot match that and the comment should not claim it does.|Reword the comment at reviewdir.go:380-387 to state the cross-device fallback is non-atomic by necessity and note the duplicated-tree window. Optionally detect a stale post-rename path duplicate on the next entry.|maintainability|30|internal/fanout/reviewdir.go:380-387,395-400|claude
LOW|internal/fanout/reviewdir.go:388|backupCrossDevice's inner os.Rename(backupNew,backup) relies on backupNew and backup sharing a filesystem, an invariant that holds only by naming coincidence (both are siblings of path). If anyone later relocates backupNew under path, the inner rename silently becomes cross-device and returns a raw EXDEV to the user.|Make the same-fs invariant structural rather than naming-coincidental: compute backupNew via filepath.Join(filepath.Dir(backup), ...) or add an assertion/comment binding backupNew and backup to the same directory. Add a test asserting the two paths share a parent.|maintainability|15|internal/fanout/reviewdir.go:388-397|claude
LOW|internal/atomicfs/atomic.go:180|Two independent package-level vars named renameFn with identical doc comments now exist — one in atomicfs (atomic.go:180), one in fanout (reviewdir.go:366). A maintainer patching fault-injection in one may assume it affects the other; worse, backupCrossDevice's inner os.Rename (reviewdir.go:395) bypasses BOTH seams, so cross-device inner-swap atomicity is not test-injectable through either var. The asymmetry is undocumented.|Rename one var (e.g. fanoutRenameFn) or add a comment noting the two are independent and that backupCrossDevice's inner os.Rename bypasses both seams.|maintainability|15|internal/atomicfs/atomic.go:177-180; internal/fanout/reviewdir.go:361-366,395|claude
LOW|internal/fanout/reviewdir_test.go:458|Test-coverage gaps in the failed-swap/cleanup paths: TestBackupExisting_FailedSwapPreservesPriorBak asserts no .bak.old straggler but not .bak.new; the non-ErrNotExist Lstat(backup) error branch (reviewdir.go:333-335) is untested; the entry-time RemoveAll straggler-cleanup failure legs (reviewdir.go:318-323) are untested. Each is a real error branch a regression could silently break.|Add assert.NoDirExists for src+".bak.new" at reviewdir_test.go:458; add a perms-based test forcing Lstat(backup) to fail with a non-ErrNotExist error; add a test where .bak.old cannot be removed and assert the typed "clearing stale staging backup" error. Skip on root/CI where perms are not enforced.|testing|30|internal/fanout/reviewdir_test.go:434-521|claude
LOW|internal/fanout/reviewdir.go:328|TOCTOU window between os.Lstat(backup) and the later os.Rename(backup,backupOld) at both swap sites (reviewdir.go:328-329, atomic.go:150-151). Another process or a symlink swap could change what backup is between check and use. Window is tiny and atcr is a single-user dev tool, but the epic's threat model names symlink/TOCTOU, so the accepted risk should be documented.|Document the accepted window as a comment given the single-user trust model, or use renameat/RENAME_NOREPLACE semantics where available.|security|30|internal/fanout/reviewdir.go:328-329; internal/atomicfs/atomic.go:150-151|claude

