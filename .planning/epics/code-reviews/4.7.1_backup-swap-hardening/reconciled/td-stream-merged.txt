HIGH|internal/atomicfs/atomic.go:75|Missing error check on Lstat result|Check errors.Is for fs.ErrNotExist|correctness|5|code-review|REVIEWER|MEDIUM
HIGH|internal/atomicfs/atomic.go:85|Unsafe error handling|Check errors.Is for syscall.EXDEV|correctness|5|code-review|REVIEWER|MEDIUM
LOW|internal/atomicfs/atomic.go:89|bakOld RemoveAll error aborts entire backup even if prior .bak.old doesn't exist|Skip RemoveAll error when bakOld doesn't exist (ENOENT) to avoid unnecessary failure|error-handling|5|code-review|bruce|MEDIUM
MEDIUM|internal/atomicfs/atomic.go:94|Missing entry-time cleanup of .bak.tmp-* stragglers|Add glob/read-dir to remove .bak.tmp-* siblings at start|correctness|10|code-review|bruce|MEDIUM
LOW|internal/atomicfs/atomic.go:110|Unused error variable|Add explicit else branch that sets staged or returns error for non-regular/non-directory|maintainability|10|code-review|REVIEWER|MEDIUM
MEDIUM|internal/atomicfs/atomic.go:110|staged variable may be used uninitialized if info.IsDir() and info.Mode().IsRegular() are both false|Add explicit else branch that sets staged or returns error for non-regular/non-directory|maintainability|10|code-review|bruce|MEDIUM
LOW|internal/atomicfs/atomic.go:120|Unused variable in error message|Remove unused 'got' variable|maintainability|5|code-review|REVIEWER|MEDIUM
LOW|internal/atomicfs/atomic.go:135|Redundant error check|Log or return error if restore fails|maintainability|15|code-review|REVIEWER|MEDIUM
MEDIUM|internal/atomicfs/atomic.go:135|Potential data loss on restore failure|Log or return error if restore fails|maintainability|15|code-review|otto|MEDIUM
MEDIUM|internal/atomicfs/atomic.go:149|swapStagedBackup does not clean up the staged temp file/dir on failure|Remove staged temp on failed swap to avoid leaking .bak.tmp-* artifacts|correctness|15|code-review|bruce|MEDIUM
MEDIUM|internal/atomicfs/atomic.go:155|Staged temp file/dir leaks on swap failure|Add os.RemoveAll(staged) before returning error|error-handling|5|code-review|bruce|MEDIUM
LOW|internal/atomicfs/atomic.go:180|Two independent package-level vars named renameFn with identical doc comments now exist — one in atomicfs (atomic.go:180), one in fanout (reviewdir.go:366). A maintainer patching fault-injection in one may assume it affects the other; worse, backupCrossDevice's inner os.Rename (reviewdir.go:395) bypasses BOTH seams, so cross-device inner-swap atomicity is not test-injectable through either var. The asymmetry is undocumented.|Rename one var (e.g. fanoutRenameFn) or add a comment noting the two are independent and that backupCrossDevice's inner os.Rename bypasses both seams.|maintainability|15|code-review|claude|MEDIUM
MEDIUM|internal/fanout/reviewdir.go:280|Missing error validation|Validate path before Rename|error-handling|5|code-review|REVIEWER|MEDIUM
HIGH|internal/fanout/reviewdir.go:295|Unsafe path concatenation|Use filepath.Join for backup paths|security|5|code-review|REVIEWER|MEDIUM
LOW|internal/fanout/reviewdir.go:300|Inefficient string operations|Use fmt.Sprintf for error messages|maintainability|4|code-review|REVIEWER|MEDIUM
LOW|internal/fanout/reviewdir.go:305|backupOld/backupNew RemoveAll errors abort backup even if stragglers don't exist|Skip RemoveAll errors for non-existent paths to be resilient|error-handling|5|code-review|bruce|MEDIUM
MEDIUM|internal/fanout/reviewdir.go:310|Missing error context|Add context to RemoveAll error|error-handling|5|code-review|REVIEWER|MEDIUM
MEDIUM|internal/fanout/reviewdir.go:318|backupExisting() unconditionally RemoveAll's <path>.bak.old and <path>.bak.new at entry, but forceBackupOutputDir() only calls guardForeignBackup(dir+".bak") — the two staging siblings are not guarded. On an arbitrary --output-dir a user who owns <output-dir>.bak.old or <output-dir>.bak.new has it silently deleted by --force, re-opening the Epic 4.7 "never silently delete user data" contract. NOTE: epic clarification Q4 deliberately scoped the guard to .bak only (atcr-internal suffixes); this finding re-raises that decision as worth reconsidering, not a defect against the recorded scope. (intent_note: deferred per epic §Clarifications Q4 — extending guardForeignBackup to .bak.old/.bak.new is out of scope)|In forceBackupOutputDir() (reviewdir.go:440) extend the guard to guardForeignBackup(dir+".bak.old") and guardForeignBackup(dir+".bak.new") before backupExisting, or refuse if either exists non-empty. Add a test pre-creating a foreign <output-dir>.bak.old with user content and assert --force errors instead of deleting it.|security|60|code-review|claude|MEDIUM
LOW|internal/fanout/reviewdir.go:318|Inefficient directory cleanup|In forceBackupOutputDir() (reviewdir.go:440) extend the guard to guardForeignBackup(dir+".bak.old") and guardForeignBackup(dir+".bak.new") before backupExisting, or refuse if either exists non-empty. Add a test pre-creating a foreign <output-dir>.bak.old with user content and assert --force errors instead of deleting it.|security|60|code-review|REVIEWER|MEDIUM
LOW|internal/fanout/reviewdir.go:328|TOCTOU window between os.Lstat(backup) and the later os.Rename(backup,backupOld) at both swap sites (reviewdir.go:328-329, atomic.go:150-151). Another process or a symlink swap could change what backup is between check and use. Window is tiny and atcr is a single-user dev tool, but the epic's threat model names symlink/TOCTOU, so the accepted risk should be documented.|Document the accepted window as a comment given the single-user trust model, or use renameat/RENAME_NOREPLACE semantics where available.|security|30|code-review|claude|MEDIUM
MEDIUM|internal/fanout/reviewdir.go:332|backupCrossDevice does not clean up backupNew on copy failure|Remove backupNew on copyTree/CopyPath failure to avoid stale staging artifact|correctness|10|code-review|bruce|MEDIUM
MEDIUM|internal/fanout/reviewdir.go:340|backupCrossDevice vacate failure triggers rollback, overwriting successful new backup|Document or handle partial state; backup exists but path not vacated|correctness|15|code-review|greta|MEDIUM
MEDIUM|internal/fanout/reviewdir.go:340|backupCrossDevice leaves path unvacated if RemoveAll(path) fails after successful backup swap|Document or handle partial state; backup exists but path not vacated|correctness|15|code-review|bruce|MEDIUM
MEDIUM|internal/fanout/reviewdir.go:365|Potential data loss on restore failure|Log or return error if restore fails|correctness|15|code-review|otto|MEDIUM
MEDIUM|internal/fanout/reviewdir.go:373|The advertised "an interrupted swap never leaves the user with neither generation" invariant is conditional on a best-effort SILENT restore succeeding. If restorePriorBackup's os.Rename(backupOld,backup) fails (e.g. backup partially recreated, or perms change), the only surviving copy is stranded under .bak.old — which the very next run's entry-time RemoveAll(backupOld) (line 318) then deletes. Same pattern at the copy site swapStagedBackup (atomic.go:159-167) feeding atomic.go:97 RemoveAll(bakOld).|Surface restore failures loudly (log/return) instead of dropping them, and/or do not auto-RemoveAll .bak.old at entry when .bak is absent — treat a lone .bak.old as the surviving generation to recover. Add a test where restore fails and a subsequent run is asserted not to delete the only surviving copy.|correctness|120|code-review|claude|MEDIUM
LOW|internal/fanout/reviewdir.go:380|Doc overclaim: backupCrossDevice's comment says it replicates "os.Rename(path,backup)'s postcondition", but the fallback is two non-atomic steps (rename backupNew over backup, then RemoveAll(path)). A crash between them leaves the tree duplicated at both path and path.bak; entry-time reconcile only clears .bak.old/.bak.new, not a leftover live path. The move it emulates is genuinely atomic; the fallback cannot match that and the comment should not claim it does.|Reword the comment at reviewdir.go:380-387 to state the cross-device fallback is non-atomic by necessity and note the duplicated-tree window. Optionally detect a stale post-rename path duplicate on the next entry.|maintainability|30|code-review|claude|MEDIUM
MEDIUM|internal/fanout/reviewdir.go:388|The EXDEV fallback's own failure legs are untested: backupCrossDevice's CopyPath error (line 392), inner os.Rename(backupNew,backup) swap error (395), and RemoveAll(path) vacate error (398), plus the restorePriorBackup(...) call wired on backupCrossDevice failure (line 343). The "EXDEV falls back without data loss" AC is proven only for the success path; no test proves the prior .bak is restored when the cross-device copy fails.|Add a test that forces renameFn to return syscall.EXDEV and makes the copy fail (a copy seam, or unreadable src), staging a prior .bak first; assert the prior .bak content is restored intact, the live tree survives, and .bak.new is cleaned up. Cover the copy-failure leg at minimum.|testing|120|code-review|claude|MEDIUM
LOW|internal/fanout/reviewdir.go:388|backupCrossDevice's inner os.Rename(backupNew,backup) relies on backupNew and backup sharing a filesystem, an invariant that holds only by naming coincidence (both are siblings of path). If anyone later relocates backupNew under path, the inner rename silently becomes cross-device and returns a raw EXDEV to the user.|Add a test that forces renameFn to return syscall.EXDEV and makes the copy fail (a copy seam, or unreadable src), staging a prior .bak first; assert the prior .bak content is restored intact, the live tree survives, and .bak.new is cleaned up. Cover the copy-failure leg at minimum.|testing|120|code-review|claude|MEDIUM
LOW|internal/fanout/reviewdir_test.go:458|Test-coverage gaps in the failed-swap/cleanup paths: TestBackupExisting_FailedSwapPreservesPriorBak asserts no .bak.old straggler but not .bak.new; the non-ErrNotExist Lstat(backup) error branch (reviewdir.go:333-335) is untested; the entry-time RemoveAll straggler-cleanup failure legs (reviewdir.go:318-323) are untested. Each is a real error branch a regression could silently break.|Add assert.NoDirExists for src+".bak.new" at reviewdir_test.go:458; add a perms-based test forcing Lstat(backup) to fail with a non-ErrNotExist error; add a test where .bak.old cannot be removed and assert the typed "clearing stale staging backup" error. Skip on root/CI where perms are not enforced.|testing|30|code-review|claude|MEDIUM
