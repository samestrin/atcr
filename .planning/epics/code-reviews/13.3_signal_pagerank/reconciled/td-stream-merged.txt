MEDIUM|reconcile/merge.go:17|The Confidence doc comment still reads HIGH = 2+ distinct reviewers, MEDIUM = single reviewer. After epic 13.3 promoteByAuthority (reconcile/pagerank.go:189) raises a single-reviewer finding to HIGH when its model is above the authority baseline, so HIGH no longer strictly means multiple models corroborated. Any downstream reader trusting this comment as the HIGH contract is now misled.|Update the comment at reconcile/merge.go:17-18 to state HIGH = 2+ distinct reviewers OR a single above-baseline-authority reviewer (epic 13.3), and cross-reference promoteByAuthority. Grep for other docs/gates that assume the old HIGH semantics.|maintainability|15|code-review|claude|MEDIUM
MEDIUM|reconcile/pagerank.go:43|O(n^2) allocation per addAgreement due to seen map|Preallocate seen map with capacity len(reviewers)|performance|2|code-review|dax|MEDIUM
MEDIUM|reconcile/pagerank.go:50|addAgreement re-sorts distinct reviewers implicitly via slice order|Explicitly sort distinct reviewers for deterministic edge addition order|correctness|3|code-review|dax|MEDIUM
MEDIUM|reconcile/pagerank.go:51|Unbounded quadratic loop in addAgreement could enable CPU exhaustion|Enforce maximum distinct reviewers per group or cap slice length|security|15|code-review|greta|MEDIUM
MEDIUM|reconcile/pagerank.go:98|outWeight map uses int for edge weights but pageRank converts to float64|Use float64 for outWeight to avoid repeated int-to-float conversions|performance|2|code-review|dax|MEDIUM
LOW|reconcile/pagerank.go:105|Float accumulation may vary across CPU architectures breaking byte-identical guarantee|Use fixed-point arithmetic or enforce single-arch CI|correctness|10|code-review|greta|MEDIUM
MEDIUM|reconcile/pagerank.go:116|Potential floating point precision drift in delta calculation|Use math.Abs for calculating delta to ensure consistency|correctness|5|code-review|otto|MEDIUM
LOW|reconcile/pagerank.go:119|The dangling-mass redistribution block (lines 119-127 and the danglingShare term at 137) is unreachable for any graph this package builds: addEdge always increments both endpoints to weight >=1 and nodes only come into existence via addEdge, so every node has outWeight > 0 and dangling is always 0. The block plus its comment imply a code path that cannot occur, costing reader time.|Either delete the dangling block and danglingShare term (the agreement graph guarantees no dangling nodes), or keep it as defense for hypothetical reuse of the unexported pageRank() but add a one-line comment marking it unreachable for agreement graphs. Test suite is unaffected (never exercised this path).|maintainability|15|code-review|claude|MEDIUM
LOW|reconcile/pagerank.go:142|Variable name `any` shadows Go 1.18+ builtin type alias|Rename to e.g. `hasAgreement` or `found` to avoid shadowing `any`|maintainability|2|code-review|bruce|MEDIUM
LOW|reconcile/pagerank.go:143|delta accumulation uses manual abs; math.Abs is clearer|Replace manual abs with math.Abs(d)|maintainability|1|code-review|dax|MEDIUM
MEDIUM|reconcile/pagerank.go:152|modelAuthority sets any=true even when addAgreement adds no edges (e.g. distinctReviewers returns 2+ but all but one are empty)|Guard any on actual edge addition or check g.nodes() length before returning pageRank|correctness|10|code-review|archer|MEDIUM
MEDIUM|reconcile/pagerank.go:152|Redundant deduplication: distinctReviewers and addAgreement both filter empties/dupes, risking divergence if implementations differ|Single source of truth for distinct-reviewer extraction|maintainability|5|code-review|archer|MEDIUM
LOW|reconcile/pagerank.go:158|Implicit zero-value map access for missing reviewer authority|Add explicit map lookup check before comparison|correctness|5|code-review|greta|MEDIUM
LOW|reconcile/pagerank.go:158|Baseline recalculated per finding|Precompute baseline once per run|maintainability|2|code-review|brad|MEDIUM
LOW|reconcile/pagerank.go:159|distinctReviewers(group) is computed twice per merge group: once in modelAuthority (reconcile/pagerank.go:163) to build the graph and again inside Merge(g) (reconcile/merge.go:54) during the second pass. For large runs this is duplicated O(findings) work plus a per-call map allocation that the run-global two-pass restructuring made avoidable. Not a hot-loop catastrophe at current input sizes.|Optional optimization: have the first pass cache distinct reviewers per group and thread them into Merge to avoid recomputation. Existing tests confirm no behavior change.|performance|30|code-review|claude|MEDIUM
MEDIUM|reconcile/pagerank.go:188|promoteByAuthority uses a bare strict comparison authority[reviewer] > baseline with no float margin, and the epic's backward-compat invariant (symmetric/uniform agreement = no promotion) relies on the PageRank fixed point landing at exactly 1.0/N. For K_n every node computes an identical expression so all land equal, but f(1/N) can round one ULP ABOVE 1.0/float64(N) for some N (K4 rounds to 0.24999999999999997 below by luck; K5/K6/K7 unproven). If that happens, every participating model exceeds baseline and any isolated single-reviewer finding from one of them silently flips MEDIUM->HIGH in a fully-corroborated symmetric run, violating the stated invariant. Blast radius is bounded (promote-only, never suppresses) but it breaks a claimed correctness guarantee.|Add a margin to the threshold in promoteByAuthority: compare against baseline + pageRankEpsilon (or baseline*(1+eps)) so sub-noise differences never promote. Add symmetric K5/K6/K7 and C4/C5 reconcile tests asserting no promotion to lock the invariant independent of rounding direction.|correctness|60|code-review|claude|MEDIUM
MEDIUM|reconcile/pagerank_confidence_test.go:42|Invariant-critical cases are untested: (1) symmetric graphs beyond K3 (K4+/cycles) where the float baseline boundary actually matters; (2) disconnected components bridged only by the teleport term; (3) the common path where an isolated finding's model never agreed with anyone so authority[reviewer] is the 0 zero-value (no test pins it stays MEDIUM); (4) no full Reconcile() run-twice determinism assertion with the authority feature wired in (determinism is only pinned at the bare pageRank() level).|Add table tests for K4/K5 and a 4-cycle asserting no promotion, a disconnected-graph pageRank test, a never-agreed-model isolated-finding-stays-MEDIUM reconcile test, and a Reconcile() run-twice deep-equal determinism test. Verify with go test ./... inside the reconcile module.|testing|60|code-review|claude|MEDIUM
LOW|reconcile/pagerank_test.go:128|BenchmarkModelAuthority comment claims it validates the epic's <5ms NFR, but a Go benchmark never fails — it only reports ns/op. There is no assertion or CI gate that fails on regression, so the NFR is documented but unguarded; a future O(n^2)-in-loop change passes CI silently.|Either reword the comment from validates to characterizes, or add a real timed test that t.Fatal's above a threshold (or testing.Benchmark with a NsPerOp ceiling). Verify by artificially slowing modelAuthority and confirming the check fails.|testing|30|code-review|claude|MEDIUM
LOW|reconcile/reconcile.go:21|Dead code: ambiguous slice never used after loop|Remove or use ambiguous variable|maintainability|5|code-review|vera|MEDIUM
HIGH|reconcile/reconcile.go:36|Undefined variable 'merged' used|Define merged before use|error-handling|10|code-review|vera|MEDIUM
LOW|reconcile/reconcile.go:61|Dead code: outOfScope variable unused|Remove outOfScope variable|maintainability|5|code-review|vera|MEDIUM
MEDIUM|reconcile/reconcile.go:82|allGroups slice grows unbounded without preallocation|Preallocate allGroups with len(clusters) or known capacity|performance|1|code-review|dax|MEDIUM
