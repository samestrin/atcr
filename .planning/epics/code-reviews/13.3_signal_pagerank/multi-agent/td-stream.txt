MEDIUM|reconcile/pagerank.go:43|O(n²) allocation per addAgreement due to seen map|Preallocate seen map with capacity len(reviewers)|performance|2|seen := make(map[string]bool, len(reviewers)) — already done; false positive|dax
MEDIUM|reconcile/pagerank.go:50|addAgreement re-sorts distinct reviewers implicitly via slice order|Explicitly sort distinct reviewers for deterministic edge addition order|correctness|3|distinct slice built from map iteration has non-deterministic order|dax
MEDIUM|reconcile/pagerank.go:98|outWeight map uses int for edge weights but pageRank converts to float64|Use float64 for outWeight to avoid repeated int-to-float conversions|performance|2|outWeight[v] converted to float64 on every neighbor iteration|dax
LOW|reconcile/pagerank.go:143|delta accumulation uses manual abs; math.Abs is clearer|Replace manual abs with math.Abs(d)|maintainability|1|if d < 0 { d = -d }|dax
MEDIUM|reconcile/reconcile.go:82|allGroups slice grows unbounded without preallocation|Preallocate allGroups with len(clusters) or known capacity|performance|1|var allGroups [][]Finding|dax
LOW|reconcile/pagerank.go:142|Variable name `any` shadows Go 1.18+ builtin type alias|Rename to `anyAgreement` or `hasAny`|maintainability|2|any := false shadows predeclared any type (interface{})|bruce
LOW|reconcile/pagerank.go:142|Variable name `any` shadows Go 1.18+ builtin type alias|Rename to e.g. `hasAgreement` or `found` to avoid shadowing `any`|maintainability|2|`any := false` shadows predeclared `any` (interface{} alias); some linters flag this|bruce
MEDIUM|reconcile/pagerank.go:51|Unbounded quadratic loop in addAgreement could enable CPU exhaustion|Enforce maximum distinct reviewers per group or cap slice length|security|15|O(K^2) nested loop over distinct reviewer strings|greta
LOW|reconcile/pagerank.go:105|Float accumulation may vary across CPU architectures breaking byte-identical guarantee|Use fixed-point arithmetic or enforce single-arch CI|correctness|10|float64 math not strictly deterministic on ARM vs x86|greta
LOW|reconcile/pagerank.go:158|Implicit zero-value map access for missing reviewer authority|Add explicit map lookup check before comparison|correctness|5|authority[m.Reviewers[0]] returns 0.0 if key absent|greta
MEDIUM|reconcile/pagerank.go:116|Potential floating point precision drift in delta calculation|Use `math.Abs` for calculating delta to ensure consistency|correctness|5|`d := nv - rank[u]; if d < 0 { d = -d }` is a manual Abs implementation|otto
HIGH|reconcile/reconcile.go:36|Undefined variable 'merged' used|Define merged before use|error-handling|10|merged used but not declared|vera
LOW|reconcile/reconcile.go:21|Dead code: ambiguous slice never used after loop|Remove or use ambiguous variable|maintainability|5|ambiguous declared and appended but never read|vera
LOW|reconcile/reconcile.go:61|Dead code: outOfScope variable unused|Remove outOfScope variable|maintainability|5|outOfScope declared but never referenced|vera
LOW|reconcile/pagerank.go:158|Baseline recalculated per finding|Precompute baseline once per run|maintainability|2|baseline := 1.0 / float64(len(authority)) inside loop|brad
MEDIUM|reconcile/pagerank.go:152|modelAuthority sets any=true even when addAgreement adds no edges (e.g. distinctReviewers returns 2+ but all but one are empty)|Guard any on actual edge addition or check g.nodes() length before returning pageRank|correctness|10|any=true when len(revs)>=2 but addAgreement may collapse to <2 distinct non-empty reviewers|archer
MEDIUM|reconcile/pagerank.go:152|Redundant deduplication: distinctReviewers and addAgreement both filter empties/dupes, risking divergence if implementations differ|Single source of truth for distinct-reviewer extraction|maintainability|5|distinctReviewers(group) called before addAgreement which re-deduplicates internally|archer
