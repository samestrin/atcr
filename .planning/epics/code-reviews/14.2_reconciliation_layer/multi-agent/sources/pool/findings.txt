# atcr-findings/v1
HIGH|internal/reconcile/consensus_filter_test.go:1|No test exercises the `panelReviewers` path when a source has findings but all have empty Reviewer strings|Add a test case where a source with findings carries only `Reviewer: ""` and assert `panelReviewers` returns 0|testing|10|`panelReviewers` ignores empty reviewers but no test proves it; `TestConsensusSingleton_And_PanelReviewers` only tests the unattributed finding mixed with attributed ones|dax
MEDIUM|reconcile/consensus.go:52|`consensusExempt` dead-code branch for `Verification.Verdict == VerdictConfirmed` is exercised only by a direct unit test, never through `Reconcile`|Keep the forward-looking guard but add a comment that `TestConsensusExempt_Predicate` is the sole coverage|testing|5|`consensusExempt` confirmed-verdict branch cannot fire through `Reconcile` (Merge nils input Verification); only `TestConsensusExempt_Predicate` hits it|dax
MEDIUM|reconcile/consensus.go:91|`consensusNoiseCluster` sets `Similarity` to 0 but no test asserts the field value|Assert `Similarity == 0` in any test that inspects the sidecar cluster produced by the filter|testing|5|`consensusNoiseCluster` returns `AmbiguousCluster` with zero-value `Similarity`; `inAmbiguousSingleton` only checks `File` and `len(Findings)`|dax
MEDIUM|reconcile/consensus_test.go:1|No test for `consensusSingleton` on a `ConfVerified` merged finding|Add a case: `consensusSingleton(Merged{Finding{Confidence: ConfVerified}})` must be `false`|testing|5|`consensusSingleton` keys on `!ConfidenceAtOrAbove(_, ConfHigh)`, which covers `ConfVerified` transitively, but no explicit case guards against a regression that reorders the confidence enum|dax
LOW|reconcile/consensus.go:1|`categorySecurity` constant is exported implicitly (lowercase) but `CategoryOutOfScope` is used directly in the same switch; inconsistency may confuse future readers|Add a comment that `categorySecurity` is unexported because it's internal to the filter, while `CategoryOutOfScope` is the canonical public constant|style|5|`consensusExempt` switches on `categorySecurity` (local const) and `CategoryOutOfScope` (exported); no comment explains the asymmetry|dax
LOW|reconcile/reconcile_test.go:25|The sort-order test comment explains the reviewer-sharing fix but the test still uses `recAt()` which sets a fixed time — the comment doesn't mention that `recAt()` is irrelevant to the fix|Remove the `recAt()` call or note that it's orthogonal to the reviewer-sharing fix|style|2|`TestReconcile_SortedBySeverityThenLocation` calls `recAt()` but the comment only explains the reviewer-sharing change|dax
