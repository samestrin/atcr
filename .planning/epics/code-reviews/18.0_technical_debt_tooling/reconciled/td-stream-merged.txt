LOW|cmd/atcr/debt.go:58|--sync is registered on the read-oriented list and dashboard commands, and loadRecords calls debt.SyncShards (which writes shard files to disk) before reading. So `atcr debt dashboard --check --sync` — an invocation whose entire purpose is read-only drift verification for CI/pre-commit — mutates the working tree as a side effect.|Skip SyncShards when --check is set (verification should read shards as-committed), or emit a warning when --sync is combined with --check. Verify `dashboard --check --sync` leaves items/ unchanged.|maintainability|15|code-review|claude|MEDIUM
LOW|cmd/atcr/debt.go:146|truncate computes string(r[:n-1]) after the `len(r) <= n` guard. For n==0 with a non-empty string this indexes r[:-1] and panics; for n==1 it yields just the ellipsis. Only called with the constant 60 today so it cannot fire, but it is an unexported general-looking helper with a latent panic and no guard.|Add an `if n <= 0 { return "" }` (or n<2) guard at the top of truncate. Verify with a unit test calling truncate("abc", 0).|maintainability|5|code-review|claude|MEDIUM
LOW|cmd/atcr/debt_add.go:74|--date is only checked for non-emptiness (Section.validate); no format check happens on the CLI path. A malformed value like `--date 07-03-2026` is accepted and written into the section header. Downstream age sorting and the dashboard monthHistogram parse dates as YYYY-MM, so a malformed date silently mis-buckets or drops the item from the by-month rollup — a data-quality defect in the authoritative store with no error surfaced.|Validate date against time.Parse("2006-01-02", date) in runDebtAdd and the wizard before building the Section, returning a usage error on failure. Verify `debt add --date garbage ...` exits non-zero.|error-handling|15|code-review|claude|MEDIUM
LOW|cmd/atcr/debt_add.go:100|Severity is case-normalized (strings.ToUpper) at both flag and wizard construction, but status and source-type are passed verbatim to validators that enforce exact-case enums. So `--severity high` works while `--status Open` or `--source-type sprint` fail validation. The asymmetric normalization is a surprising, inconsistent input contract for otherwise-identical enum fields.|Normalize status (lower) and source-type (title-case) at the same boundary, or drop severity normalization for consistency and document that enums are case-sensitive. Verify `debt add ... --status Open` succeeds or all three fail identically.|maintainability|15|code-review|claude|MEDIUM
MEDIUM|cmd/atcr/debt_add.go:104|runDebtAdd's switch selects flag mode only when ALL five flags (sev && file && problem && fix && category) are set. When a user supplies some-but-not-all on an interactive terminal, control falls through to the TTY case and launches the wizard from scratch; the already-provided flag values are silently discarded because wizardDefaults carries date/label/group/status/source/est but NOT severity/file/problem/fix/category.|Detect partial-flag state (any of the five set but not all) and return a usage error naming the missing flags instead of dropping into the wizard, or seed the wizard defaults from the provided flags. Verify `debt add --severity HIGH --file x.go` on a TTY errors rather than re-prompting for severity.|maintainability|30|code-review|claude|MEDIUM
LOW|cmd/atcr/debt_add.go:112|The non-interactive missing-flags error lists all five required flags generically rather than naming which specific flags were empty. In a scripted/CI context (the command's stated primary contract) this forces the caller to eyeball their invocation instead of being told exactly what to add.|Accumulate the names of the empty required flags and interpolate them into the error message. Verify that `debt add --severity HIGH` with echo piped to stdin names --file --problem --fix --category.|error-handling|15|code-review|claude|MEDIUM
LOW|cmd/atcr/debt_add.go:142|promptEntry treats every sc.Scan()==false as end-of-input and never inspects sc.Err(). A pasted answer longer than the 1 MB buffer triggers bufio.ErrTooLong, which Scan reports by returning false — indistinguishable from EOF. The wizard then either aborts with the misleading "input ended before required field" or silently substitutes a default and collapses every subsequent prompt.|After the prompt loop or inside ask, check sc.Err() and surface a distinct "input too long / read error" message rather than the EOF path. Verify by piping a >1 MB single line into a required field.|error-handling|15|code-review|claude|MEDIUM
LOW|cmd/atcr/debt_dashboard.go:52|The dashboard action switch orders `case check:` before `case toStdout:`, so `--check --stdout` silently runs check mode and ignores --stdout with no warning. Both are boolean flags with no declared precedence in the help text.|Reject the --check --stdout combination with a usage error, or document the precedence in flag help. Verify `debt dashboard --check --stdout` returns a clear error or documented behavior.|maintainability|15|code-review|claude|MEDIUM
HIGH|internal/debt/add.go:162|AppendItem performs an unsynchronized read-modify-write of the authoritative README (os.ReadFile -> insertRow -> os.WriteFile) with no advisory lock. The rest of the TD tooling (the group_td mkdir-flock path used by execute-sprint/reconcile) serializes README writes because multiple concurrent sessions share the branch. Two concurrent `atcr debt add` runs both read the same base content and the second write silently clobbers the first item.|Wrap the read-through-write (lines 163-189) in the same mkdir/flock lock the rest of the TD tooling uses, or an os.OpenFile O_EXCL sentinel. Verify by launching two adds in parallel against one README and asserting both rows survive.|correctness|120|code-review|claude|MEDIUM
LOW|internal/debt/add.go:162|A single AppendItem re-reads and re-parses the README three times and writes it twice, and SyncShards regenerates the entire shard store via a full migrate. Bulk-adding N items in a loop is effectively O(N) full-store rewrites and 2N shard-directory rewrites.|Expose a batch-append that mutates the README once, syncs shards once, and refreshes stats once for a batch; keep the single-item path for the interactive case. Verify by benchmarking a 100-item batch against 100 single adds.|performance|120|code-review|claude|MEDIUM
LOW|internal/debt/add.go:178|The comment at lines 171-177 claims "no partial-write window," but that only covers the pre-write parse check. After the README is written (line 178), a failure in SyncShards (181) or RefreshStats (187) returns an error while the README already contains the new row — README and shard store diverge until a later successful sync. The success contract is oversold.|Either roll the README back to the pre-write bytes on a post-write step failure, or soften the comment to state the README is authoritative and shards/stats are best-effort re-synced. Verify by forcing SyncShards to fail and asserting Load state matches the documented guarantee.|error-handling|30|code-review|claude|MEDIUM
MEDIUM|internal/debt/add.go:198|RefreshStats replaces the entire span content[statsIdx:lmLineEnd] (from `## Stats` through the end of the Last Modified line), guarding only lmIdx<statsIdx. Any content between the Stats table and the Last Modified line (another section, notes, a second table) is silently deleted. Unlike insertRow, RefreshStats writes to disk with no ParseREADME re-validation, so corruption is committed silently and shards regenerate from the mangled README on the next sync.|Constrain the replaced span to the actual Stats block (stop at the first blank line after the table), and add a tdmigrate.ParseREADME(updated) guard before os.WriteFile at line 224. Verify with a README that has an unrelated section between Stats and Last Modified and assert it survives.|correctness|30|code-review|claude|MEDIUM
LOW|internal/debt/aggregate.go:22|Component treats any single-segment path containing a space as free-text prose and buckets it under (unscoped). A legitimate filename with a space is misclassified as unscoped rather than its own component. The space heuristic conflates prose with filenames containing spaces.|Prefer a stronger signal (presence of a known extension, or absence of multiple whitespace-delimited words) at aggregate.go:22, or document the limitation. Verify Component("my file.go:3") expects "my file.go".|maintainability|15|code-review|claude|MEDIUM
MEDIUM|internal/debt/dashboard.go:79|RenderDashboard prints r.File, r.Severity, r.EstMinutes raw in the Top Priority table and c.Component raw in By Component (line 54); only r.Problem passes through sanitizeCell. Item.Validate never checks File for pipes/newlines and Load reads File verbatim from YAML, so a File containing a literal / or newline breaks the Markdown row column count, corrupting the deterministic render and producing a false positive under `atcr debt dashboard --check`.|Wrap r.File (dashboard.go:79-80) and c.Component (line 54) in sanitizeCell(...) mirroring the Problem cell. Verify with a record whose File contains a pipe/newline and assert the rendered row still has the expected pipe count.|correctness|15|code-review|claude|MEDIUM
LOW|internal/debt/dashboard.go:102|monthHistogram derives the month bucket as r.Date[:7] whenever len>=7 with no format check. A malformed date like 2026-7x or 2026/07 yields a bogus month label that becomes its own histogram row instead of unknown. ageDays (aggregate.go:86) correctly uses time.Parse for the same field; the two disagree on what a valid date is.|Parse with time.Parse("2006-01-02", ...) or at least "2006-01" and bucket parse failures under unknown, matching ageDays. Verify Date="2026-7x" expects the unknown bucket.|maintainability|15|code-review|claude|MEDIUM
LOW|internal/debt/debt.go:92|Filter.Match is inconsistent about case: Severity and Status use EqualFold, Category uses lowercased Contains, but Group is exact case-sensitive ==. `--group foo` returns nothing if the stored group is Foo, with no error. Component is a bare HasPrefix with no path-boundary check, so `--component cmd` also matches cmder/... .|Make Group matching case-insensitive (or document the asymmetry), and consider a path-segment-boundary check for Component at debt.go:95. Verify a Group-cased mismatch and a cmd vs cmder component prefix test.|maintainability|15|code-review|claude|MEDIUM
LOW|internal/debt/debt.go:145|The Sort doc comment says ties are "broken by File then Date," but SortSeverity and SortAge break ties by Date then File, and SortEst breaks only by File. The comment misdescribes the implemented tiebreak order that a maintainer relies on when reasoning about deterministic output.|Correct the comment at debt.go:145 to match the real per-key tiebreak chains, or unify the chains. Verify by reading each less closure against the comment.|maintainability|5|code-review|claude|MEDIUM
