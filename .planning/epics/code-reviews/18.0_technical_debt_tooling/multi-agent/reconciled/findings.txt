# atcr-findings/v1
MEDIUM|internal/debt/add.go:181|AppendItem does an unlocked read-modify-write of the authoritative README|Wrap the README read-modify-write in the shared mkdir-flock used by the TD flush path or serialize TD writes through a single utility|concurrency|30|`data, err := os.ReadFile(readmePath)` followed by `os.WriteFile(readmePath, ...)` without synchronization|otto|MEDIUM
LOW|cmd/atcr/debt_add.go:96|Interactive wizard does not seed defaults from provided flags|Seed `wizardDefaults` from flags passed to the command so partial flag input carries into the interactive prompts|style|20|`promptEntry` is called with `def` but `sev`, `file`, `problem`, `fix`, and `category` are checked via `mustFlag` and ignored if the wizard is triggered|otto|MEDIUM
LOW|internal/debt/add.go:195|AppendItem writes the README then runs SyncShards; a failure in SyncShards leaves shards lagging the README|Regenerate shards to a temp dir and swap, or document that post-write sync failure is recovered by re-running `atcr debt list --sync`|error-handling|20|`os.WriteFile(readmePath, ...)` occurs before `SyncShards(readmePath, itemsDir, stderr)`|otto|MEDIUM
