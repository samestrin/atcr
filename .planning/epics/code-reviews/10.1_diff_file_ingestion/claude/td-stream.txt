# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/active/10.1_diff_file_ingestion.md
# TARGET_TYPE: epic
# CREATED: 2026-06-25 09:53:14
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---

HIGH|internal/payload/ingest.go:179|looseSectionStarts consumes each hunk body by its declared old/new line counts, then exits the inner loop the instant both counters reach zero. A `\ No newline at end of file` marker — which git and `diff -u` emit for any file lacking a trailing newline — sits AFTER the last counted body line, so the loop never reaches the `\`-prefix branch at line 185 to consume it. The outer walk then sees the unconsumed `\` line, looseHeaderAt returns false, and BuildEntriesFromDiff hard-errors with "unexpected content at line N". Confirmed empirically: both a trailing `\ No newline` after the final hunk AND an interior one between two loose files cause a parse rejection. This rejects a large class of valid loose unified diffs — the exact suite-fixture format this ingestion path exists to serve (Epic 10.2 benchmark Case.Diff). The git-format path is immune (it splits only on `diff --git`), so the gap is loose-format-specific.|In looseSectionStarts (internal/payload/ingest.go:176-194), after the counted-body inner loop exits, drain any immediately-following `\`-prefixed line(s) into the current hunk before re-checking for the next `@@`/file header. Add fixtures carrying `\ No newline at end of file` on (a) the last hunk line of the final file and (b) an interior file of a multi-file loose diff; assert both round-trip verbatim.|correctness|60|internal/payload/ingest.go:176-194|claude
MEDIUM|internal/payload/ingest.go:179|looseSectionStarts trusts the declared hunk counts as exact ground truth. A hunk header that over-declares its line count (e.g. `@@ -1,5 +1,5 @@` with only one body line before the next file's `--- ` header) makes the body-consumption loop swallow the next file's `--- `/`+++ `/`@@ ` header lines as if they were this hunk's body. Confirmed empirically: a two-file loose diff with an over-counted first hunk yields ONE FileEntry under the first file's path (nentries=1), silently losing the second file's separate identity and misattributing its content. Because the bytes are still preserved, the round-trip contract test passes and the corruption is invisible to existing tests — but per-file path provenance and truncation are wrong. This is reachable from malformed/hostile externally-sourced diffs, which the epic explicitly anticipates ingesting.|In looseSectionStarts, bound body consumption so it cannot cross a line that is itself a valid looseHeaderAt(i) start: treat a header appearing mid-budget as early hunk termination (malformed-count tolerance) or hard-error rather than swallowing it. Add a two-file loose fixture with an over-counted first hunk asserting len(entries)==2 and correct per-file paths.|correctness|120|internal/payload/ingest.go:179-194|claude
LOW|internal/payload/ingest.go:163|The trailing-empty-line tolerance `if lines[i] == "" && i == n-1` in looseSectionStarts handles exactly ONE trailing empty line. A loose diff ending in `\n\n` (a final blank context line plus the terminating newline) produces two empty trailing lines; the guard skips only the last, then looseHeaderAt runs on the penultimate empty line and the parse errors with "unexpected content". Confirmed empirically: a diff ending `...+b\n\n` is rejected. A blank trailing line is benign and should round-trip, not abort ingestion.|In looseSectionStarts, loop the trailing-empty-line skip to consume all trailing "" lines (not just i==n-1), or treat a blank line inside an open hunk as a context line. Add a fixture ending in `\n\n` asserting successful round-trip.|error-handling|15|internal/payload/ingest.go:162-174|claude
LOW|internal/payload/ingest.go:307|headPathFromGitHeader extracts the head path by taking everything after the LAST ` b/` token via strings.LastIndex. For a path that itself contains ` b/` (e.g. `diff --git a/my b/dir.go b/my b/dir.go`) it selects the wrong boundary and returns "dir.go" instead of "my b/dir.go". Confirmed empirically. Impact is narrow: it only affects header-only git sections (binary/mode-only) with no `+++ b/...` line — the `+++` path is preferred when present — and spaced paths containing ` b/` are uncommon, but the file is silently mislabeled in the review.|For the header-only fallback, the `diff --git` format is genuinely ambiguous for spaced paths; at minimum document the limitation in the diagnostic, or split the header at its midpoint using the matching `a/` prefix length. Low priority given rarity. Add a binary-section fixture with a spaced ` b/` path.|correctness|30|internal/payload/ingest.go:301-311|claude
LOW|internal/payload/ingest.go:234|splitLinesWithOffsets builds two parallel slices (lines and offsets) sized to the full line count of a diff up to DefaultMaxDiffBytes (10 MiB) with no pre-sized capacity, so both grow by repeated append/realloc; diffSectionPath then re-runs strings.Split(section, "\n") per file, allocating a third whole-section copy. The work is linear and cap-bounded (not a DoS), but it is an avoidable allocation spike on the ingestion hot path for large multi-file diffs.|Pre-size the slices with bytes.Count(s, "\n")+1, and restructure diffSectionPath to scan only the section's leading header-triple lines instead of splitting the entire section body. Verify allocation count via a benchmark on a large multi-file diff.|performance|30|internal/payload/ingest.go:234-246|claude
