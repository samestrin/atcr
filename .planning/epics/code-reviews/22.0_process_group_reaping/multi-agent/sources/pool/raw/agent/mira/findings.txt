# atcr-findings/v1
HIGH|internal/verify/localvalidate_pgroup_unix.go:26|Cancel returns non-ESRCH kill errors directly to exec.CommandContext, which ignores cancel errors and proceeds to Wait; the kill error is discarded and never reaches the caller, so a failed group kill (e.g. EPERM from a privilege-dropped child) is silent and indistinguishable from a clean kill|Return nil on non-ESRCH errors so the cancel is acknowledged but don't surface the kill failure; surface the failure separately via a result field (e.g. ReapError) or log it at minimum|error-handling|20|Cancel returns err directly; exec.CommandContext.cancel does not propagate cancel errors|mira
MEDIUM|internal/verify/localvalidate_pgroup_unix.go:26|The group SIGKILL fires only when runCtx.Done() triggers Cancel; if the command exits 0 or non-zero before the timeout, Cancel never fires and any orphaned grandchildren survive indefinitely|Acceptable within the timeout-only epic scope but leaves a leak path if a validation command backgrounds children and exits; document the limitation explicitly|under-engineering|15|No mechanism for the clean-exit case|mira
LOW|internal/verify/localvalidate_pgroup_unix.go:33|errors.Is not used — bare `err == syscall.ESRCH` works today but breaks if Kill wraps the error in a wrapped sentinel (e.g. os.(*PathError) wrapping syscall.Errno)|Use errors.Is(err, syscall.ESRCH) and add a comment that this must track any future wrapping|error-paths|5|bare == comparison on line 33|mira
MEDIUM|internal/verify/localvalidate_pgroup_unix_test.go:47|processAlive uses bare PID without any uniqueness guarantee across the 5s Eventually window — OS PID recycling can flip the assertion to a false-negative failure or, if a new process happens to appear at that PID, a false-positive pass|Use kill(-pid, 0) to probe the whole group rather than a single recycled PID, or capture the child start-time via os.FindProcess and compare to confirm it's the original process|edge-cases|15|processAlive probes a single PID that may be recycled|mira
