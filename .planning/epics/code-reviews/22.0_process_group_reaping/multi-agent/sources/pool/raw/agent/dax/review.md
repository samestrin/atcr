

LOW|internal/verify/localvalidate_pgroup_unix.go:29|Non-ESRCH errors from syscall.Kill (e.g., EPERM if a group member drops privileges) propagate up through cmd.Run() and bypass the TimedOut branch in RunConfiguredValidation, masking a failed reap as a clean timeout|Log or record the error when it is neither nil nor ESRCH so RunConfiguredValidation can surface reap failures instead of masking them|OBSERVABILITY|15|err := syscall.Kill(-cmd.Process.Pid, syscall.SIGKILL) ... if err == syscall.ESRCH { return os.ErrProcessDone } return err
LOW|internal/verify/localvalidate_pgroup_unix_test.go:47|processAlive relies on the reaped grandchild PID not being recycled by the OS within the 5s Eventually window; PID reuse flips the assertion to a false-negative failure|Accept the low probability as documented, or additionally probe the whole group (kill(-pid, 0)) or capture the child start time to detect reuse|EDGE_CASES|15|return syscall.Kill(pid, syscall.Signal(0)) != syscall.ESRCH
MEDIUM|internal/verify/localvalidate_pgroup_unix_test.go:68|The happy-path test only covers exit-0; AC2 requires pass/fail runs to be unaffected, but no test verifies that a clean non-zero exit preserves ExitCode and sets Passed()=false under the new cancel override|Add a case running a command that exits non-zero (e.g., sh -c "exit 1") and assert ExitCode matches and Passed() is false|testing|10|require.False(t, res.TimedOut, "a command that exits within the timeout must not be marked timed out")
LOW|internal/verify/localvalidate_pgroup_unix.go:22|If the context is cancelled before cmd.Start() finishes, cmd.Process is nil. The override returns nil, but this race condition is untested and could mask start errors or interact unpredictably with future Go versions that call Cancel differently|Add a test that cancels the context immediately after invoking RunConfiguredValidation to verify no panic and correct handling of pre-start cancellation|testing|10|if cmd.Process == nil { return nil }