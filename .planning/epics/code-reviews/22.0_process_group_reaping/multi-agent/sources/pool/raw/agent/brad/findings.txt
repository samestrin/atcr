# atcr-findings/v1
MEDIUM|internal/verify/localvalidate.go:108|Timeout path returns res, nil unconditionally when context is done, silently discarding runErr — if the process-group Cancel fails (e.g. EPERM from setuid grandchildren or LSM restrictions), the kill is incomplete and orphans survive without any signal|Inspect runErr on the timeout branch or log non-nil/non-ESRCH cancel errors so incomplete reaps are observable and can fail-closed|correctness|20|if errors.Is(runCtx.Err(), context.DeadlineExceeded) // errors.Is(runCtx.Err(), context.Canceled) { res.TimedOut = true; return res, nil } masks the error returned by cmd.Run() which includes Cancel failures|brad
LOW|internal/verify/localvalidate_pgroup_unix_test.go:53|250ms timeout races with shell scheduling under CI load: sh may be preempted before echo $! executes, leaving res.Stdout empty and causing require.NotEmpty to fail before the Eventually reaping assertion can run|Increase the fixture timeout to 1s or add a synchronous barrier before wait to guarantee PID emission within the deadline|testing|10|sh -c "sleep 100 & echo $! ; wait" with 250ms deadline can be interrupted before the background job is registered and PID printed|brad
