LOW|internal/verify/localvalidate_pgroup_unix_test.go:27|processAlive decides liveness purely by PID via syscall.Kill(pid, 0). After the group SIGKILL reaps the grandchild sleep and init reaps its zombie, the OS may recycle that PID. If an unrelated process on the shared CI box claims the same PID inside the 5s require.Eventually window (:47-50, :70-73), processAlive returns true forever and the test fails even though the reap succeeded. Low probability but a rare false-failure that erodes CI trust on a busy multi-runner host.|PID probing cannot fully eliminate this; cheapest mitigation is to shorten exposure with a tighter require.Eventually window (e.g. 2s) since the reap happens within tens of ms. A fully robust fix would confirm the PID start-time/identity (likely over-engineering here); documenting the residual risk in the test comment is acceptable. Verify by stress-running under high process churn.|testing|15|code-review|claude|MEDIUM
MEDIUM|internal/verify/localvalidate_pgroup_unix_test.go:59|The two timeout tests (TestRunConfiguredValidation_TimeoutReapsGrandchild at :38 and TestRunConfiguredValidation_TimeoutReapsWholeGroup at :59) give the sh fixture only 250ms before the deadline SIGKILLs the group, yet they hard-assert on stdout captured before the kill (require.NotEmpty at :43, require.Len(...,2) at :64). The whole-group test needs two separate echo writes ($$ then $!) to fork and flush to the captured pipe within 250ms. On the shared 4-way-parallel gauntlet CI box, sh cold-start plus two echoes can approach or exceed 250ms, leaving res.Stdout empty or single-line and failing the run spuriously.|Raise the fixtures' timeout from 250*time.Millisecond to ~1*time.Second at lines 38 and 59. The PIDs are echoed as the first shell action, so a longer kill deadline widens the capture window without meaningfully lengthening the test (the command is still killed, TimedOut still true). Verify by running both tests under -race -count=50 on a loaded machine.|testing|15|code-review|claude|MEDIUM
