LOW|cmd/atcr/benchmark.go:93|The --out/--output flags document the symlink replace-not-follow behavior of writeExportFile, but --checkpoint routes through the SAME helper and omits that note. An operator pointing --checkpoint at a symlink gets the same replace-not-follow behavior undocumented; the shared helper temp prefix is .scorecard-export-*.tmp even when writing a checkpoint, minor naming coupling that confuses anyone debugging stray temp files.|Add the symlink note to the --checkpoint help string for parity; optionally parameterize the temp prefix in writeExportFile.|maintainability|15|code-review|claude|MEDIUM
MEDIUM|cmd/atcr/benchmark.go:113|Unvalidated CLI flag path passed directly to file I/O allows arbitrary read/write via traversal|Sanitize path with filepath.Clean and restrict to allowed directories or validate against base path|security|10|code-review|greta|MEDIUM
LOW|cmd/atcr/benchmark_checkpoint.go:62|checkpointReviewer.Expected stores c.ExpectedCategories per-reviewer per-case, but Expected is identical for every reviewer of a case. With R reviewers this inflates the checkpoint payload by ~Rx on that field, paid on every whole-file rewrite, and it is redundant with the ReproHash guard which already pins expected categories so the stored copy is never the source of truth on a valid resume.|Hoist Expected to checkpointCase (one copy per case) and pass it through replayCheckpointCase, or drop it and re-read c.ExpectedCategories on replay (the hash guard guarantees it unchanged). Verify replay still byte-identical.|performance|30|code-review|claude|MEDIUM
LOW|cmd/atcr/benchmark_checkpoint.go:85|loadCheckpoint reads the checkpoint with a bare os.ReadFile and json.Unmarshal with no size cap, inconsistent with the codebase defensive posture elsewhere on the resume/hash paths: ReproHashManifest caps diffs at MaxDiffBytes and fanout rebuild caps per-agent files via readFileLimited. The checkpoint is operator-supplied/untrusted JSON; a pathologically large or crafted file exhausts memory with no guard.|Stat-and-cap the checkpoint read (reuse the readFileLimited pattern with a sane ceiling) before unmarshal, and add a test that an oversize checkpoint returns a bounded error rather than allocating it.|security|15|code-review|claude|MEDIUM
LOW|cmd/atcr/benchmark_checkpoint.go:89|loadCheckpoint returns nil on missing file, but caller may treat nil as valid empty state|Document that nil means "no checkpoint" vs "empty checkpoint" in function comment|maintainability|5|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark_checkpoint.go:100|MarshalIndent inflates checkpoint file size|Use json.Marshal for compact storage and faster serialization|performance|5|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark_checkpoint.go:104|AC1 claims a process killed mid-write leaves the previous valid checkpoint intact via temp+rename, but the only failure test (failAfterCompleter) is a clean abort AFTER case 0 checkpoint was fully written — it never exercises an interrupted save. The atomicity guarantee (prior valid checkpoint survives a crash during the N+1th write) is asserted nowhere.|Add a test that writes a valid 1-case checkpoint, forces saveCheckpoint/writeExportFile to fail mid-operation (inject write error or pre-create temp path as a directory), and asserts the on-disk file still parses to the prior valid 1-case checkpoint. Reference the writeExportFile durability test if one exists.|testing|30|code-review|claude|MEDIUM
LOW|cmd/atcr/benchmark_checkpoint.go:114|doneIndex builds map[int]checkpointCase and on duplicate Index entries (corrupt or hand-edited checkpoint) the last one silently wins, dropping the earlier completed case from replay with no signal. Nothing validates index uniqueness, index bounds, or non-empty CaseID after unmarshal — loadCheckpoint trusts the JSON wholesale, and the suite-identity hash guard does not cover internal checkpoint self-consistency.|After unmarshal, reject duplicate or out-of-range indices and empty CaseIDs with a checkpoint is corrupt error, mirroring the fail-loud-on-parse-error posture already present. Add a duplicate-index checkpoint fixture test.|error-handling|30|code-review|claude|MEDIUM
LOW|cmd/atcr/benchmark_checkpoint.go:122|validateCheckpointRoster accepts a nil cp.Roster as matching any current roster|Return errCheckpointRosterMismatch when cp.Roster is nil so a checkpoint lacking the roster field cannot silently resume against a changed panel (AC4 fail-closed)|security|5|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark_checkpoint.go:165|shortHash duplicates fanout's shortRef helper and the sprint plan explicitly says to "share helpers where practical" rather than introducing parallel mechanisms|Move shortHash to a shared internal utility (or export fanout.shortRef) and import it here|maintainability|15|code-review|claude|MEDIUM
LOW|cmd/atcr/benchmark_checkpoint_test.go:25|TestCheckpoint_RoundTrips uses assert.Equal on structs and includes non-zero cost/latency (good) but leaves Roster nil. Roster is a load-bearing AC4 guard field, yet its serialization round-trip is never asserted with populated values, so a JSON tag typo or omission on roster would not be caught here.|Populate want.Roster with a couple of agent=model entries and assert it round-trips through save/load.|testing|5|code-review|claude|MEDIUM
LOW|cmd/atcr/benchmark_checkpoint_test.go:30|Round-trip test omits Roster field|Add Roster field to want struct to verify serialization|testing|5|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark_run.go:67|errCheckpointSuiteMismatch sentinel is reused for the per-index CaseID drift guard, conflating two distinct failure modes under one error name and making it harder for callers (or future telemetry) to distinguish suite-identity drift from index-id drift|Introduce a separate errCheckpointCaseMismatch sentinel for the per-index CaseID check so the two failure modes are distinguishable via errors.Is|correctness|5|code-review|claude|MEDIUM
MEDIUM|cmd/atcr/benchmark_run.go:75|errCheckpointSuiteMismatch sentinel reused for per-index CaseID drift|Introduce a separate errCheckpointCaseMismatch sentinel so callers can distinguish suite-identity drift from per-index case-id drift|correctness|10|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark_run.go:178|saveCheckpoint rewrites the full JSON on every case, giving O(N^2) total write bytes; after every scored case the full cp.Cases slice is re-marshaled and the entire file rewritten, so cumulative bytes written across an n-case suite is O(n^2).|Acquire an exclusive sidecar lockfile (O_EXCL) next to the checkpoint, released on exit, or at minimum document that a checkpoint path must not be shared by concurrent runs. Verify two runs at one path: second aborts or no completed case is ever lost.|performance|120|code-review|bruce,claude|HIGH
LOW|cmd/atcr/benchmark_run.go:178|Two benchmark run --checkpoint same.json processes share one checkpoint path with no lockfile. Each holds its own in-memory cp and rewrites the whole file atomically per case; the on-disk checkpoint flaps to whichever wrote last and can regress to FEWER completed cases than were actually finished. A later resume then re-executes (and re-pays for) already-done cases, defeating the feature. The fanout precedent does not lock either, but it operates on a per-review directory, not a single shared file the operator is invited to point two runs at.|Acquire an exclusive sidecar lockfile (O_EXCL) next to the checkpoint, released on exit, or at minimum document that a checkpoint path must not be shared by concurrent runs. Verify two runs at one path: second aborts or no completed case is ever lost.|performance|120|code-review|claude|MEDIUM
MEDIUM|cmd/atcr/benchmark_run.go:178|Checkpoint save failure after a successful case loses the just-scored case work: cp.Cases is appended in memory before saveCheckpoint, but on save error the function returns and the on-disk checkpoint lacks this case, so resume re-executes (and re-pays for) it — partially defeating the purpose|Acquire an exclusive sidecar lockfile (O_EXCL) next to the checkpoint, released on exit, or at minimum document that a checkpoint path must not be shared by concurrent runs. Verify two runs at one path: second aborts or no completed case is ever lost.|performance|120|code-review|claude|MEDIUM
MEDIUM|cmd/atcr/benchmark_run.go:212|rosterSignature dereferences cfg.Registry.Agents[n].Model without a nil/existence check, panicking if a configured project agent is missing from the registry — exactly the cfg-drift scenario AC4's roster guard exists to handle gracefully; rosterSignature dereferences cfg.Registry.Agents[n].Model without nil-checking the map entry; rosterSignature assumes cfg.Registry.Agents[n] is non-nil|Include persona in rosterSignature (e.g. agent=model=persona) and in the recorded Roster so validateCheckpointRoster fails closed on persona drift. Add a test that mutates only persona between checkpoint write and resume and asserts errCheckpointRosterMismatch.|correctness|30|code-review|claude,bruce|HIGH
MEDIUM|cmd/atcr/benchmark_run.go:212|rosterSignature builds the AC4 resume guard from agent=model only, omitting persona. Persona is resolved from the registry (reviewerPersona), locked at first-sighting in applyReviewerOutcome, and is a public-board identity column AND the secondary Score sort key (internal/benchmark/score.go:63-67). If an operator changes an agent persona but not its model, validateCheckpointRoster passes, replay locks the OLD persona for that reviewer, and freshly executed cases inherit it too — so the resumed RunResult silently carries a different reviewer identity/sort order than an equivalent fresh run. This is the exact silent-panel-drift hole AC4 claims to close.|Include persona in rosterSignature (e.g. agent=model=persona) and in the recorded Roster so validateCheckpointRoster fails closed on persona drift. Add a test that mutates only persona between checkpoint write and resume and asserts errCheckpointRosterMismatch.|correctness|30|code-review|claude,bruce|HIGH
LOW|cmd/atcr/benchmark_run.go:220|rosterSignature packs sig[i] = n + "=" + model, which is ambiguous if an agent name or model id contains =: agent a=b/model c and agent a/model b=c both render a=b=c, so a roster change across that boundary could pass the fail-closed guard. The fanout precedent sidesteps this by comparing name SETS, never a packed string. Agent names with = are unlikely so this is theoretical, but it is a silent-equality hole in a guard whose whole job is to fail closed.|Use a delimiter that cannot appear in either component, or compare structured (agent,model) pairs rather than a joined string. Add a test using an agent name containing =.|correctness|15|code-review|claude|MEDIUM
MEDIUM|cmd/atcr/benchmark_run.go:220|Missing nil check on registry agent lookup causes panic if agent lacks config|Use a delimiter that cannot appear in either component, or compare structured (agent,model) pairs rather than a joined string. Add a test using an agent name containing =.|correctness|15|code-review|greta|MEDIUM
MEDIUM|cmd/atcr/benchmark_run_resume_test.go:18|countingCompleter has unprotected int field|Use sync/atomic or mutex for calls counter|correctness|5|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark_run_resume_test.go:46|All checkpoint tests run against the same 2-case suite-valid fixture. There is no coverage for an empty suite (zero cases should write a checkpoint with empty Cases and resume to a zero-cost re-run) nor an explicit single-case boundary, so the doneIndex/replay loop boundaries at 0 and N are untested at the extremes.|Add a zero-case and a one-case fixture; assert the checkpointed run completes, the checkpoint records the expected entry count, and a re-run makes zero Completer calls and is byte-identical to baseline.|testing|30|code-review|claude|MEDIUM
MEDIUM|cmd/atcr/benchmark_run_resume_test.go:99|TestExecuteBenchmarkRun_NoCheckpointPathWritesNothing creates dir via t.TempDir() but never passes dir to executeBenchmarkRun (it passes the empty checkpoint path). The function writes its work artifacts to its own os.MkdirTemp("", ...) (benchmark_run.go:77), not to dir, so assert.Empty over dir is vacuously true and would pass even if checkpointing wrote a file elsewhere. The test proves nothing about no-path = no-file.|Point a candidate checkpoint path inside dir (filepath.Join(dir, "ckpt.json")), still invoke with empty path, then assert that specific file does not exist after the run, coupling the assertion to the path the function could have written.|testing|15|code-review|claude|MEDIUM
MEDIUM|cmd/atcr/benchmark_run_resume_test.go:115|The usage-gated cost/latency replay path — the central AC3 claim that applyReviewerOutcome keeps the float sum and latency median byte-identical across replay — is never exercised with non-zero values. stubCompleter only implements Complete with no usage, so usageReported is always false and CostUSD/LatencyMS are always 0 (benchmark_run_test.go:80-81 even asserts stub reports no usage). Every AC3 byte-identical assertion would still pass if the cost-sum order or latency-median replay were broken, because there is nothing but zeros to sum or median.|Add a stub implementing the UsageCompleter interface (CompleteWithUsage) returning distinct non-zero usage per reviewer/case, then re-run the full + partial resume scenarios with >=2 usage-reporting reviewers over 2 cases and assert resumed CostUSD and LatencyP50MS equal the uninterrupted baseline.|testing|60|code-review|claude|MEDIUM
LOW|cmd/atcr/benchmark_run_resume_test.go:124|Vacuous test checks unrelated directory|Assert no checkpoint write to cwd or mock saveCheckpoint|testing|10|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark_run_resume_test.go:139|AC3 is stated as byte-identical RunResult and the implementation comments claim byte-identity, but the tests assert JSONEq, which compares semantically — normalizing key ordering, whitespace, and numeric representation. A real byte-level divergence (float formatting, map key reordering) would be masked, so the test proves only semantic equivalence, not the stated contract.|Compare raw marshaled bytes with assert.Equal(string(jBaseline), string(j2)) (and rr1) instead of JSONEq; or keep JSONEq only if formatting drift is intentionally allowed and then soften the byte-identical wording in the AC/comments.|testing|15|code-review|claude|MEDIUM
LOW|cmd/atcr/benchmark_run_resume_test.go:156|Ignored json.Marshal error in test|Use assert.Equal on marshaled byte slices|error-handling|5|code-review|bruce|MEDIUM
MEDIUM|cmd/atcr/benchmark_run_resume_test.go:156|JSONEq does not assert byte-identical output|Use assert.Equal on marshaled byte slices|error-handling|5|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark_run_resume_test.go:189|Ignored json.Marshal error in test|Use assert.Equal on marshaled byte slices|error-handling|5|code-review|bruce|MEDIUM
MEDIUM|cmd/atcr/benchmark_run_resume_test.go:189|JSONEq does not assert byte-identical output|Use assert.Equal on marshaled byte slices|error-handling|5|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark_run_resume_test.go:240|TestExecuteBenchmarkRun_RejectsCheckpointCaseIDDrift never asserts resume.calls == 0|Add assert.Equal(t, 0, resume.calls) to prove the per-index guard fires before any LLM call, matching the roster-drift tests|correctness|3|code-review|bruce|MEDIUM
