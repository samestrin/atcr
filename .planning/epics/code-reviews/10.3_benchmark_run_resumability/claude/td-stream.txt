# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/10.3_benchmark_run_resumability.md
# TARGET_TYPE: epic
# CREATED: 2026-06-25 19:11:38
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
MEDIUM|cmd/atcr/benchmark_run.go:215|rosterSignature builds the AC4 resume guard from agent=model only, omitting persona. Persona is resolved from the registry (reviewerPersona), locked at first-sighting in applyReviewerOutcome, and is a public-board identity column AND the secondary Score sort key (internal/benchmark/score.go:63-67). If an operator changes an agent persona but not its model, validateCheckpointRoster passes, replay locks the OLD persona for that reviewer, and freshly executed cases inherit it too — so the resumed RunResult silently carries a different reviewer identity/sort order than an equivalent fresh run. This is the exact silent-panel-drift hole AC4 claims to close.|Include persona in rosterSignature (e.g. agent=model=persona) and in the recorded Roster so validateCheckpointRoster fails closed on persona drift. Add a test that mutates only persona between checkpoint write and resume and asserts errCheckpointRosterMismatch.|correctness|30|cmd/atcr/benchmark_run.go:215-223|claude
MEDIUM|cmd/atcr/benchmark_run_resume_test.go:115|The usage-gated cost/latency replay path — the central AC3 claim that applyReviewerOutcome keeps the float sum and latency median byte-identical across replay — is never exercised with non-zero values. stubCompleter only implements Complete with no usage, so usageReported is always false and CostUSD/LatencyMS are always 0 (benchmark_run_test.go:80-81 even asserts stub reports no usage). Every AC3 byte-identical assertion would still pass if the cost-sum order or latency-median replay were broken, because there is nothing but zeros to sum or median.|Add a stub implementing the UsageCompleter interface (CompleteWithUsage) returning distinct non-zero usage per reviewer/case, then re-run the full + partial resume scenarios with >=2 usage-reporting reviewers over 2 cases and assert resumed CostUSD and LatencyP50MS equal the uninterrupted baseline.|testing|60|cmd/atcr/benchmark_run_resume_test.go:115-141|claude
MEDIUM|cmd/atcr/benchmark_run_resume_test.go:99|TestExecuteBenchmarkRun_NoCheckpointPathWritesNothing creates dir via t.TempDir() but never passes dir to executeBenchmarkRun (it passes the empty checkpoint path). The function writes its work artifacts to its own os.MkdirTemp("", ...) (benchmark_run.go:77), not to dir, so assert.Empty over dir is vacuously true and would pass even if checkpointing wrote a file elsewhere. The test proves nothing about no-path = no-file.|Point a candidate checkpoint path inside dir (filepath.Join(dir, "ckpt.json")), still invoke with empty path, then assert that specific file does not exist after the run, coupling the assertion to the path the function could have written.|testing|15|cmd/atcr/benchmark_run_resume_test.go:99-110|claude
LOW|cmd/atcr/benchmark_run.go:180|Two benchmark run --checkpoint same.json processes share one checkpoint path with no lockfile. Each holds its own in-memory cp and rewrites the whole file atomically per case; the on-disk checkpoint flaps to whichever wrote last and can regress to FEWER completed cases than were actually finished. A later resume then re-executes (and re-pays for) already-done cases, defeating the feature. The fanout precedent does not lock either, but it operates on a per-review directory, not a single shared file the operator is invited to point two runs at.|Acquire an exclusive sidecar lockfile (O_EXCL) next to the checkpoint, released on exit, or at minimum document that a checkpoint path must not be shared by concurrent runs. Verify two runs at one path: second aborts or no completed case is ever lost.|error-handling|60|cmd/atcr/benchmark_run.go:47-75,180-185|claude
LOW|cmd/atcr/benchmark_checkpoint.go:85|loadCheckpoint reads the checkpoint with a bare os.ReadFile and json.Unmarshal with no size cap, inconsistent with the codebase defensive posture elsewhere on the resume/hash paths: ReproHashManifest caps diffs at MaxDiffBytes and fanout rebuild caps per-agent files via readFileLimited. The checkpoint is operator-supplied/untrusted JSON; a pathologically large or crafted file exhausts memory with no guard.|Stat-and-cap the checkpoint read (reuse the readFileLimited pattern with a sane ceiling) before unmarshal, and add a test that an oversize checkpoint returns a bounded error rather than allocating it.|security|15|cmd/atcr/benchmark_checkpoint.go:85-98|claude
LOW|cmd/atcr/benchmark_checkpoint.go:114|doneIndex builds map[int]checkpointCase and on duplicate Index entries (corrupt or hand-edited checkpoint) the last one silently wins, dropping the earlier completed case from replay with no signal. Nothing validates index uniqueness, index bounds, or non-empty CaseID after unmarshal — loadCheckpoint trusts the JSON wholesale, and the suite-identity hash guard does not cover internal checkpoint self-consistency.|After unmarshal, reject duplicate or out-of-range indices and empty CaseIDs with a checkpoint is corrupt error, mirroring the fail-loud-on-parse-error posture already present. Add a duplicate-index checkpoint fixture test.|error-handling|30|cmd/atcr/benchmark_checkpoint.go:85-98,114-120|claude
LOW|cmd/atcr/benchmark_run.go:220|rosterSignature packs sig[i] = n + "=" + model, which is ambiguous if an agent name or model id contains =: agent a=b/model c and agent a/model b=c both render a=b=c, so a roster change across that boundary could pass the fail-closed guard. The fanout precedent sidesteps this by comparing name SETS, never a packed string. Agent names with = are unlikely so this is theoretical, but it is a silent-equality hole in a guard whose whole job is to fail closed.|Use a delimiter that cannot appear in either component, or compare structured (agent,model) pairs rather than a joined string. Add a test using an agent name containing =.|correctness|15|cmd/atcr/benchmark_run.go:215-223|claude
LOW|cmd/atcr/benchmark_checkpoint.go:62|checkpointReviewer.Expected stores c.ExpectedCategories per-reviewer per-case, but Expected is identical for every reviewer of a case. With R reviewers this inflates the checkpoint payload by ~Rx on that field, paid on every whole-file rewrite, and it is redundant with the ReproHash guard which already pins expected categories so the stored copy is never the source of truth on a valid resume.|Hoist Expected to checkpointCase (one copy per case) and pass it through replayCheckpointCase, or drop it and re-read c.ExpectedCategories on replay (the hash guard guarantees it unchanged). Verify replay still byte-identical.|performance|30|cmd/atcr/benchmark_run.go:161-174,benchmark_checkpoint.go:58-67|claude
LOW|cmd/atcr/benchmark_run.go:182|After every scored case the full cp.Cases slice is re-marshaled and the entire file rewritten (saveCheckpoint to writeExportFile temp+rename), so cumulative bytes written across an n-case suite is O(n^2). Acceptable at the curated suite sizes today (tens of cases, single-digit MB cumulative) — flagged only because it is genuinely quadratic and would degrade for a large externally-authored suite, compounding the redundant-Expected storage.|No change required at current sizes. If suites grow, switch to an append-only JSONL checkpoint (one line per completed case) so each write is O(1); verify resume replays a JSONL checkpoint identically.|performance|120|cmd/atcr/benchmark_run.go:180-185|claude
LOW|cmd/atcr/benchmark.go:93|The --out/--output flags document the symlink replace-not-follow behavior of writeExportFile, but --checkpoint routes through the SAME helper and omits that note. An operator pointing --checkpoint at a symlink gets the same replace-not-follow behavior undocumented; the shared helper temp prefix is .scorecard-export-*.tmp even when writing a checkpoint, minor naming coupling that confuses anyone debugging stray temp files.|Add the symlink note to the --checkpoint help string for parity; optionally parameterize the temp prefix in writeExportFile.|maintainability|15|cmd/atcr/benchmark.go:92-93,leaderboard.go:179-191|claude
LOW|cmd/atcr/benchmark_run_resume_test.go:139|AC3 is stated as byte-identical RunResult and the implementation comments claim byte-identity, but the tests assert JSONEq, which compares semantically — normalizing key ordering, whitespace, and numeric representation. A real byte-level divergence (float formatting, map key reordering) would be masked, so the test proves only semantic equivalence, not the stated contract.|Compare raw marshaled bytes with assert.Equal(string(jBaseline), string(j2)) (and rr1) instead of JSONEq; or keep JSONEq only if formatting drift is intentionally allowed and then soften the byte-identical wording in the AC/comments.|testing|15|cmd/atcr/benchmark_run_resume_test.go:136-166|claude
LOW|cmd/atcr/benchmark_checkpoint.go:104|AC1 claims a process killed mid-write leaves the previous valid checkpoint intact via temp+rename, but the only failure test (failAfterCompleter) is a clean abort AFTER case 0 checkpoint was fully written — it never exercises an interrupted save. The atomicity guarantee (prior valid checkpoint survives a crash during the N+1th write) is asserted nowhere.|Add a test that writes a valid 1-case checkpoint, forces saveCheckpoint/writeExportFile to fail mid-operation (inject write error or pre-create temp path as a directory), and asserts the on-disk file still parses to the prior valid 1-case checkpoint. Reference the writeExportFile durability test if one exists.|testing|30|cmd/atcr/benchmark_checkpoint.go:100-110|claude
LOW|cmd/atcr/benchmark_run_resume_test.go:46|All checkpoint tests run against the same 2-case suite-valid fixture. There is no coverage for an empty suite (zero cases should write a checkpoint with empty Cases and resume to a zero-cost re-run) nor an explicit single-case boundary, so the doneIndex/replay loop boundaries at 0 and N are untested at the extremes.|Add a zero-case and a one-case fixture; assert the checkpointed run completes, the checkpoint records the expected entry count, and a re-run makes zero Completer calls and is byte-identical to baseline.|testing|30|cmd/atcr/benchmark_run_resume_test.go:46-75|claude
LOW|cmd/atcr/benchmark_checkpoint_test.go:25|TestCheckpoint_RoundTrips uses assert.Equal on structs and includes non-zero cost/latency (good) but leaves Roster nil. Roster is a load-bearing AC4 guard field, yet its serialization round-trip is never asserted with populated values, so a JSON tag typo or omission on roster would not be caught here.|Populate want.Roster with a couple of agent=model entries and assert it round-trips through save/load.|testing|5|cmd/atcr/benchmark_checkpoint_test.go:27-49|claude

