MEDIUM|internal/reconcile/emit.go:135|The epic back-compat invariant (omitempty keeps findings without a warning byte-identical to pre-7.0 JSON) has no guarding test, unlike the precedent verification-field test (render_test.go:298-302). Nothing asserts an empty FixWarning emits no fix_warning key nor that a set value round-trips. A future struct edit dropping omitempty would silently break every pre-7.0 fixture undetected.|Add a render_test.go case asserting Render(FormatJSON) of a finding with empty FixWarning NotContains fix_warning, and one asserting a set value round-trips through json.Unmarshal.|testing|15|code-review|claude|MEDIUM
MEDIUM|internal/report/render_test.go:23|The markdown golden test TestRender_GoldenFiles drives only sample() which has no finding with FixWarning set, so testdata/report.md contains zero Fix warning lines. The byte-exact golden (the codebase drift guard) never locks the 7.1 FixWarning rendering — indentation, glyph, position. A regression reformatting or moving the warning line would pass all tests.|Add a finding with FixWarning set to sample() or a second golden case so the warning line is captured byte-for-byte in testdata/report.md, then regenerate with -update.|testing|15|code-review|claude|MEDIUM
MEDIUM|internal/report/render_test.go:36|TestRenderMarkdown_ShowsFixWarning supplies a benign FixWarning with no HTML metacharacters, backticks, newlines, or >500-rune body, so it never exercises escTrunc on FixWarning (render.go:167). If escTrunc were replaced with raw f.FixWarning the test stays green, despite FixWarning carrying parser-derived text that can echo attacker-controlled fix content. Every other free-text field has an injection/truncation test; FixWarning does not.|Add a case with FixWarning containing a script tag, a backtick, an embedded newline-heading, and a >500-rune body; assert no raw script tag, no injected heading, and presence of the truncation ellipsis. Mirror TestRender_HTMLInjectionEscapedInMarkdown.|testing|15|code-review|claude|MEDIUM
LOW|internal/verify/executor.go:80|generateFixes nil-checks ex and complete (line 73) but dereferences reg.Providers[ex.Provider] at line 80 with no nil check on reg itself. The in-memory direct-call/test path the doc comment cites can pass a nil reg and panic with a nil-pointer deref, crashing the verify run instead of the advertised graceful no-op. Pre-existing Epic 7.0 code adjacent to the 7.1 change.|Add reg == nil to the early-return guard at line 73. Add a unit test calling generateFixes with reg nil asserting no panic.|error-handling|15|code-review|claude|MEDIUM
LOW|internal/verify/executor.go:144|validateGoFixSyntax is called on the raw fix with no length cap and parseGoFix runs up to three full parser.ParseFile passes per fix, concurrently across the worker pool. A pathological multi-megabyte completion is parsed three times per eligible finding driven by untrusted model output (regex passes are cheap; the cost is the triple AST build).|Short-circuit validateGoFixSyntax with a size cap (e.g. return nil above ~256KB) before parsing. Add a large-input benchmark asserting bounded time and allocations.|performance|15|code-review|claude|MEDIUM
MEDIUM|internal/verify/executor.go:144|Parser error includes empty filename prefix causing messy warning|Short-circuit validateGoFixSyntax with a size cap (e.g. return nil above ~256KB) before parsing. Add a large-input benchmark asserting bounded time and allocations.|performance|15|code-review|bruce|MEDIUM
LOW|internal/verify/syntaxguard.go:10|The header comment calls this a syntax/compile guard but go/parser performs zero type/semantic checking — a func returning an undefined identifier parses cleanly. A maintainer trusting the compile label assumes far more coverage than exists.|Strike compile from the line 10 comment; it is a syntax/parse guard only.|maintainability|5|code-review|claude|MEDIUM
LOW|internal/verify/syntaxguard.go:23|fenceRe character class excludes '#' (c#, f#)|Add '#' to character class: [A-Za-z0-9_+#-]*|correctness|5|code-review|bruce|MEDIUM
MEDIUM|internal/verify/syntaxguard.go:30|fenceRe body capture is non-greedy so the first triple-backtick INSIDE the fenced body closes the block. Valid Go containing a triple-backtick string literal is truncated mid-expression, fails to parse, and because hadFence is true the guard flags VALID code as invalid_syntax — the exact false-positive trust degradation the guard exists to avoid.|Require the closing fence on its own line by anchoring the close with (?m) and a leading newline so an inline backtick run mid-line cannot close the block. Add a fixture containing a backtick-bearing Go string literal asserting NoError.|correctness|30|code-review|claude|MEDIUM
LOW|internal/verify/syntaxguard.go:30|fenceRe matches exactly three backticks. Against a standard CommonMark 4-backtick fence (used when the body itself contains triple backticks) the regex still matches but slices at the inner three backticks, capturing a stray backtick line into the body and producing garbage that may false-pass or false-flag depending on parse luck.|Require the closing fence on its own line by anchoring the close with (?m) and a leading newline so an inline backtick run mid-line cannot close the block. Add a fixture containing a backtick-bearing Go string literal asserting NoError.|correctness|30|code-review|claude|MEDIUM
MEDIUM|internal/verify/syntaxguard.go:30|Fence regex misses spaces after backticks|Require the closing fence on its own line by anchoring the close with (?m) and a leading newline so an inline backtick run mid-line cannot close the block. Add a fixture containing a backtick-bearing Go string literal asserting NoError.|correctness|30|code-review|bruce|MEDIUM
MEDIUM|internal/verify/syntaxguard.go:38|`blockOpenRe` regex is overly broad|Use `(?m)^[ \t]*[^{]*\{[ \t]*$`|correctness|10|code-review|bruce|MEDIUM
HIGH|internal/verify/syntaxguard.go:45|Regex for markdown fence doesn't allow whitespace after backticks|Update regex to allow optional whitespace|maintainability|30|code-review|a123456,bob2|HIGH
HIGH|internal/verify/syntaxguard.go:137|Logic error in `looksLikeGoCode` vs Test case|The test `TestValidateGoFixSyntax_InvalidShortAssign` expects `x := func( {` to be flagged, but `looksLikeGoCode` only checks for `declKeywordRe`, `blockOpenRe` (ends with `{`), and `blockCloseRe` (starts with `}`). The `:=` operator is not checked in `looksLikeGoCode`, so this specific input will return `nil` and not be flagged, causing the test to fail.|Add a regex check for `:=` to `looksLikeGoCode`|60|code-review|`looksLikeGoCode` lacks check for `:=` but test `TestValidateGoFixSyntax_InvalidShortAssign` requires it|MEDIUM
MEDIUM|internal/verify/syntaxguard.go:137|extractFencedCode uses fenceRe.FindStringSubmatch and validates ONLY the first markdown fence. A common multi-block LLM response shaped as a non-Go fence (e.g. a text fence) followed by a broken Go fence extracts the first non-Go block, matches nonGoFenceLangs, and returns nil — the broken Go in the second fence is never parsed, a silent false negative.|The test `TestValidateGoFixSyntax_InvalidShortAssign` expects `x := func( {` to be flagged, but `looksLikeGoCode` only checks for `declKeywordRe`, `blockOpenRe` (ends with `{`), and `blockCloseRe` (starts with `}`). The `:=` operator is not checked in `looksLikeGoCode`, so this specific input will return `nil` and not be flagged, causing the test to fail.|Add a regex check for `:=` to `looksLikeGoCode`|60|code-review|claude|MEDIUM
LOW|internal/verify/syntaxguard_test.go:1|The intentional conservative-recall false negative — unfenced single-line broken Go with no block structure passes unflagged — has no characterization test. A maintainer cannot distinguish the intended trade-off from an accidental hole and could loosen looksLikeGoCode, reintroducing the false-positive class the guard avoids.|Add TestValidateGoFixSyntax_BrokenUnfencedNoBlockStructureNotFlagged asserting NoError on an unfenced broken one-liner with a comment marking it the documented conservative-recall boundary.|testing|15|code-review|claude|MEDIUM
HIGH|internal/verify/syntaxguard_test.go:56|Unused err variable causes compilation failure|Use err in require.Error call|correctness|2|code-review|bruce|MEDIUM
LOW|internal/verify/syntaxguard_test.go:60|TestValidateGoFixSyntax_InvalidShortAssign comment claims := is a strong Go signal justifying the flag, but looksLikeGoCode keys only on declKeyword/blockOpen/blockClose — there is no := signal. The input is flagged solely because it ends in an open brace. The test passes for a reason unrelated to its stated rationale, giving false confidence that := detection exists.|Fix the comment to cite the real signal (trailing open brace), or change the input so its only signal is := (which will correctly NOT flag, honestly exposing the recall gap).|testing|5|code-review|claude|MEDIUM
