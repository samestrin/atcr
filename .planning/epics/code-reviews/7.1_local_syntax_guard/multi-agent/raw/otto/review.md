HIGH|internal/verify/syntaxguard.go:137|Logic error in `looksLikeGoCode` vs Test case|The test `TestValidateGoFixSyntax_InvalidShortAssign` expects `x := func( {` to be flagged, but `looksLikeGoCode` only checks for `declKeywordRe`, `blockOpenRe` (ends with `{`), and `blockCloseRe` (starts with `}`). The `:=` operator is not checked in `looksLikeGoCode`, so this specific input will return `nil` and not be flagged, causing the test to fail.|Add a regex check for `:=` to `looksLikeGoCode`|correctness|15|`looksLikeGoCode` lacks check for `:=` but test `TestValidateGoFixSyntax_InvalidShortAssign` requires it|otto