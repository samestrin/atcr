# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/19.5_response_truncation_failover.md
# TARGET_TYPE: epic
# CREATED: 2026-07-06 13:19:44
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---

HIGH|internal/fanout/chunker.go:189|mergeResultGroup byte-copies the memoized parsedFindingCount/parsedFindingCountSet fields from chunk[0] (out := g[0]) and rebuilds out.Content from ALL chunks (line 251), but its reset block (tokens/turns/toolbytes) never invalidates the memo. When a chunked persona's FIRST chunk truncated to zero findings, the epic failover gate (engine.go:570) already set the memo to count=0/set=true and demoted it; that stale zero memo rides into the merged persona result, so findingsFor short-circuits (artifacts.go:214) and silently drops EVERY finding the persona's other chunks produced — re-creating the exact silent clean review the epic prevents, for the chunked strategy. Introduced by the concurrent TD-019 memo refactor (commits 1c36a190/361da806), not the epic merge.|In mergeResultGroup reset the memo after rebuilding out.Content: set out.parsedFindingCount=0 and out.parsedFindingCountSet=false alongside the other per-chunk resets. Add a test: chunk[0] truncated+zero (memo set) plus chunk[1] yielding a finding must persist that finding, not an empty result.|correctness|30|internal/fanout/chunker.go:189-251,internal/fanout/artifacts.go:214,internal/fanout/engine.go:569-570|claude
MEDIUM|internal/fanout/chunker.go:189|mergeResultGroup never aggregates ResponseTruncated across the chunk group — the merged persona inherits only g[0].ResponseTruncated. If a later chunk (not chunk[0]) hit finish_reason=length the persona-level response_truncated marker is lost (under-report); if chunk[0] truncated but later chunks were clean the whole persona over-reports as truncated. Every other cross-chunk signal (tokens/turns/fallback/UnreviewedChunks) is aggregated here; the epic Task-4 truncation telemetry was missed for the chunked strategy.|In the per-chunk loop add out.ResponseTruncated = out.ResponseTruncated OR r.ResponseTruncated (after resetting it to false with the other accumulators). Test that a persona with one truncated chunk reports response_truncated=true in status.json.|correctness|15|internal/fanout/chunker.go:189-251|claude
MEDIUM|internal/fanout/engine.go:570|The failover demotion gate uses the RAW ParsedFindingCount() (stream.ParseModelOutput), but the persisted FindingsCount and the run-level truncated_zero_findings tally (artifacts.go:134) use the GROUNDED count after groundFindings + enforceConstraints. A truncated response that raw-parses >=1 finding but has them all dropped as ungrounded/below-min-severity stays StatusOK (guard does not fire) yet is tallied as a runaway; the comment at artifacts.go:38-43 claims the tally is the rate the guard reacts to, which is false. Same divergence already deferred as the epic independent-review MEDIUM; corroborated by both reviewers.|Gate the failover on the same effective/grounded finding set findingsFor uses (thread grounding into invokeSlot), or correct the artifacts.go:38-43 comment to say the tally measures truncated agents with zero GROUNDED findings at final-status granularity. Needs an engine API change; deferred.|correctness|60|internal/fanout/engine.go:569-575,internal/fanout/artifacts.go:38-43,126-135|claude
LOW|internal/fanout/artifacts.go:214|The TD-019 memo short-circuit only fires for parsedFindingCount==0, so a truncated-with-findings result (memo set to >=1 by the failover gate) re-runs stream.ParseModelOutput at line 217 — the redundant parse the refactor aimed to remove, though bounded to the rare truncated-with-findings path.|Cache the parsed []Finding slice on Result rather than only its length so findingsFor can reuse it, or accept the bounded cost.|performance|15|internal/fanout/artifacts.go:207-217|claude
LOW|internal/verify/executor.go:181|On the snippet path a fix that truncated (finish_reason=length) AND emitted nothing comes back from callExecutor as empty-content/truncated=true/SystemError; generateFixes discards the truncated flag on the err!=nil branch and sets the generic 'fix generation failed' warn, never reaching the truncated branch, so the specific executor_truncated_fix warning / truncated FixWarning is not emitted. Refusal is still non-silent (observability only).|In the snippet else-branch let the truncated check run before the generic empty/error handling, or fold truncation into the warn message when tr is true.|error-handling|15|internal/verify/executor.go:180-201,internal/llmclient/client.go:316-322|claude
LOW|internal/fanout/status.go:305|AgentStatus.ResponseTruncated uses omitempty (absent when false) but the adjacent Epic 2.2 counters (DroppedByMinSeverity/TruncatedByMaxFindings) are deliberately always-present so 0/false is distinguishable from an older status.json predating the field; a consumer cannot tell response_truncated:false from a pre-19.5 file. Defensible per the code comment but divergent.|Drop omitempty if field-presence detection matters for this marker as it did for the 2.2 counters, else keep as-is (comment already justifies it).|maintainability|5|internal/fanout/status.go:298-309|claude
