# atcr-findings/v1
HIGH|cmd/atcr/debt_resolve.go:216|atcr debt resolve output changed from "Marked %s resolved." to "Marked %s %s." breaking scripts that parse the output string|Restore the original output string or document the change and update consumers|compatibility|30|[brad] recs, err := localdebt.ReadAll(dir, localdebt.ReadOpts{Writer: cmd.ErrOrStderr()}) scales linearly with history size and risks OOM on long-lived repos / [vera] fmt.Fprintf(..., "Marked %s %s.\n", id, status) (disagreement: MEDIUM vs HIGH)|brad,vera|HIGH
HIGH|cmd/atcr/debt_resolve.go:250|(markDebtResolved) No length validation on --reason allows multi-MB justification strings to exhaust disk and trigger OOM on subsequent ReadAll calls|Enforce a max length or truncate before assignment|exhaustion|10|rec.Justification = r accepts unbounded input directly from CLI flag|brad|MEDIUM
MEDIUM|cmd/atcr/debt_resolve.go:64|(runDebtResolve) The `cmd.Flags().Changed("status")` guard correctly rejects `--status` supplied without `--resolve`, but the error message says "--status/--reason require --resolve <id>" which conflates the two flags. If only `--status` was provided (no `--reason`), the error still mentions `--reason`, which is confusing. The error should mention only the flags that were actually supplied, not all gated flags.|Emit a targeted error listing only the flags that were provided without `--resolve`, e.g. "flag(s) require --resolve <id>" with a list of the offending flags|testing|10|[dax] TestDebtResolve_StatusOrReasonWithoutResolveIsUsageError only tests `--status wontfix`; the guard condition `cmd.Flags().Changed("status")` is value-agnostic. / [mira] return usageError(fmt.Errorf("--status/--reason require --resolve <id>"))|dax,mira|HIGH
