HIGH|cmd/atcr/debt_resolve.go:216|atcr debt resolve output changed from "Marked %s resolved." to "Marked %s %s." breaking scripts that parse the output string|Restore the original output string or document the change and update consumers|compatibility|10|fmt.Fprintf(..., "Marked %s %s.\n", id, status)
HIGH|cmd/atcr/debt_resolve.go:144|status/--reason flags now require --resolve, otherwise a usage error is returned instead of silently ignoring them, breaking scripts that passed --status or --reason without --resolve|Add a guard that allows status/--reason to be passed without --resolve (e.g., ignore them or provide a default) or update scripts to include --resolve|compatibility|10|if id == "" && (cmd.Flags().Changed("status") || strings.TrimSpace(mustFlag(cmd, "reason")) != "") { return usageError(...) }