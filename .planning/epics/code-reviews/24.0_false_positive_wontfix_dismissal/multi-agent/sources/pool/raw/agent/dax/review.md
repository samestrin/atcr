HIGH|cmd/atcr/reconcile_test.go:519|TestPersistLocalDebt_WontfixSuppressesReappend only asserts the wontfix finding is suppressed; it does not add a second, different finding to verify that suppression does not block appending of other new findings. A regression in dedup could silently drop all findings when a wontfix record exists, and this test would still pass.|Extend the test to include an additional finding with a different ID and assert it is appended alongside the suppressed wontfix record.|testing|15|The test body only creates one seed wontfix record and one matching finding; it asserts len(recs)==1 and never introduces a second finding.
MEDIUM|cmd/atcr/debt_resolve.go:240|The already-closed message hardcodes "already resolved" regardless of the existing terminal status (e.g., wontfix). The new wontfix status path is untested for idempotent marks; the pre-existing TestDebtResolve_MarkResolvedIsIdempotent only covers resolved→resolved.|Add a test that marks a wontfix item as resolved (or wontfix again) and asserts the message reflects the actual status, or update the message to be status-aware.|testing|15|Line `fmt.Fprintf(cmd.OutOrStdout(), "%s is already resolved; nothing to do.\n", id)` uses a static string; no test covers re-marking a wontfix item.
MEDIUM|cmd/atcr/debt_resolve.go:64|The guard rejecting --status/--reason without --resolve is only tested with --status wontfix. The case where the user explicitly passes the default value `--status resolved` without --resolve is untested, so a future change could break that error path.|Add a test case with `--status resolved` (no --resolve) and assert a usage error.|testing|10|TestDebtResolve_StatusOrReasonWithoutResolveIsUsageError only tests `--status wontfix`; the guard condition `cmd.Flags().Changed("status")` is value-agnostic.
LOW|cmd/atcr/debt_resolve.go:67|The status validation rejects empty string, but no test covers `--status ""`.|Add a test with an empty --status value and assert a usage error.|testing|5|The validation `!resolveStatuses[status]` will reject empty string; only "bogus" is tested.
LOW|cmd/atcr/debt_resolve.go:260|The reason trimming logic treats whitespace-only reason as empty (preserving existing justification), but no test covers `--reason "   "`.|Add a test with whitespace-only --reason and verify that an existing justification is preserved.|testing|10|Code uses `strings.TrimSpace(reason)`; whitespace-only yields empty string, which falls through to preserve existing.