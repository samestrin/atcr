# Technical Debt Backlog

Items from code review. Use `/resolve-td --group=N` to fix by group.
Use `/promote-tech-debt` to graduate items to formal TD sprint plans.

### [2026-07-14] From Sprint: 24.0_false_positive_wontfix_dismissal

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| <nil> | [ ] | MEDIUM | cmd/atcr/debt_resolve.go:54 | Reason-only guard skips validation and silently falls through: `cmd.Flags().Changed("status")` does not guard --reason, so `--reason "text" --resolve` is required but `--reason "text"` alone bypasses the guard and proceeds to markDebtResolved which then errors on missing id (user gets an unhelpful "no open item" error instead of a clear usage hint). The --status case correctly uses `Changed("status")`. Align --reason guard with the --status pattern: `cmd.Flags().Changed("status") | Align --reason guard with the --status pattern: cmd.Flags().Changed("status") |  | 0 | correctness | 10 | code-review |
| <nil> | [ ] | MEDIUM | cmd/atcr/debt_resolve.go:64 | The guard rejecting --status/--reason without --resolve is only tested with --status wontfix. The case where the user explicitly passes the default value `--status resolved` without --resolve is untested, so a future change could break that error path. | Emit a targeted error listing only the flags that were provided without `--resolve`, e.g. "flag(s) require --resolve <id>" with a list of the offending flags | testing | 10 | code-review | dax | MEDIUM |
| <nil> | [ ] | MEDIUM | cmd/atcr/debt_resolve.go:64 | The `cmd.Flags().Changed("status")` guard correctly rejects `--status` supplied without `--resolve`, but the error message says "--status/--reason require --resolve <id>" which conflates the two flags. If only `--status` was provided (no `--reason`), the error still mentions `--reason`, which is confusing. The error should mention only the flags that were actually supplied, not all gated flags. | Emit a targeted error listing only the flags that were provided without `--resolve`, e.g. "flag(s) require --resolve <id>" with a list of the offending flags | testing | 10 | code-review | mira | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/debt_resolve.go:64 | The status validation rejects empty string, but no test covers `--status ""`. | Emit a targeted error listing only the flags that were provided without `--resolve`, e.g. "flag(s) require --resolve <id>" with a list of the offending flags | testing | 10 | code-review | dax | MEDIUM |
| <nil> | [ ] | MEDIUM | cmd/atcr/debt_resolve.go:71 | The error message for invalid `--status` value uses `mustFlag(cmd, "status")` (original casing) while the lookup uses the lowercased `status` variable, so a user typing "--status RESOLVED" sees "invalid --status 'RESOLVED'" rather than the canonical lowercase form. Not a crash, but inconsistent with the lowercasing that actually controls the logic. | Use the already-lowercased `status` variable in the error message: fmt.Errorf("invalid --status %q: expected resolved/wontfix", status) | usability | 5 | code-review | mira | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/debt_resolve.go:77 | --status wontfix is accepted with no --reason and no pre-existing justification, producing a terminal wontfix record whose Justification is empty. The epic's premise is capturing WHY a false positive was dismissed so the decision is auditable; permitting a reasonless permanent dismissal defeats that for the wontfix path specifically (resolved, where code was actually fixed, is a weaker case for requiring text). | Require a non-empty --reason (or an existing justification) when --status wontfix, returning a usage error otherwise; leave resolved unchanged. Verify with a test that '--resolve <id> --status wontfix' with no reason and no prior justification is a usage error. | error-handling | 30 | code-review | claude | MEDIUM |
| <nil> | [ ] | HIGH | cmd/atcr/debt_resolve.go:216 | atcr debt resolve output changed from "Marked %s resolved." to "Marked %s %s." breaking scripts that parse the output string | Restore the original output string or document the change and update consumers | compatibility | 30 | code-review | vera | MEDIUM |
| <nil> | [ ] | MEDIUM | cmd/atcr/debt_resolve.go:216 | markDebtResolved loads the entire append-only store into memory to locate a single finding ID | Restore the original output string or document the change and update consumers | compatibility | 30 | code-review | brad | MEDIUM |
| <nil> | [ ] | MEDIUM | cmd/atcr/debt_resolve.go:240 | Epic 24.0 added a second terminal status (wontfix) alongside resolved, but markDebtResolved's no-lock TD-004 comment (lines 240-249) still claims concurrent invocations produce only harmless duplicate bloat. Two concurrent 'atcr debt resolve --resolve <id>' calls (one --status resolved, one --status wontfix) both pass the alreadyClosed==false check and each append a divergent terminal record. selectOpenDebt folds the item out either way, but the durable audit trail now disagrees on whether the finding was fixed vs dismissed as a false positive — the exact distinction this epic exists to record. | Update the comment to acknowledge concurrent invocations can now write conflicting statuses (not identical duplicates), and make the downstream status reader deterministic — define a rule (e.g. wontfix outranks resolved, or first-terminal-wins) and select the effective terminal record accordingly rather than relying on shard read order. Verify with a test that appends both a resolved and a wontfix record for one id and asserts a single well-defined effective status. | correctness | 60 | code-review | claude | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/debt_resolve.go:247 | When an id already carries any terminal record, markDebtResolved hardcodes the message '<id> is already resolved; nothing to do.' (line 247). Post-Epic 24.0 the existing terminal may be wontfix, so a user attempting to --resolve an item that was dismissed as a false positive is told it is 'already resolved', which is factually wrong and hides that no re-classification is possible. | Look up the existing terminal record's actual status and report it (e.g. 'already closed as %s'). Verify with a test that wontfixes an id, then runs --resolve <id> and asserts the message names wontfix, not resolved. | maintainability | 15 | code-review | claude | MEDIUM |
| <nil> | [ ] | HIGH | cmd/atcr/debt_resolve.go:247 | No length validation on --reason allows multi-MB justification strings to exhaust disk and trigger OOM on subsequent ReadAll calls | Look up the existing terminal record's actual status and report it (e.g. 'already closed as %s'). Verify with a test that wontfixes an id, then runs --resolve <id> and asserts the message names wontfix, not resolved. | maintainability | 15 | code-review | brad | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/debt_resolve.go:260 | The reason trimming logic treats whitespace-only reason as empty (preserving existing justification), but no test covers `--reason "   "`. | Append/annotate rather than overwrite when the record already has a distinct justification, or explicitly document that --reason replaces existing justification. Verify with a test that resolves an item carrying enrichment justification with a --reason and asserts the intended precedence. | testing | 15 | code-review | dax | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/debt_resolve.go:260 | A supplied --reason overwrites rec.Justification (lines 263-265) for BOTH statuses, including --status resolved. When the open record already carried reconcile-enrichment justification (Epic 18.3 source_report), passing --reason on a plain resolve silently discards that enrichment with no warning. The comment only frames the empty-reason case and does not acknowledge that a non-empty reason clobbers pre-existing enrichment; the flag help also implies it is wontfix-scoped when it applies to resolved too. | Append/annotate rather than overwrite when the record already has a distinct justification, or explicitly document that --reason replaces existing justification. Verify with a test that resolves an item carrying enrichment justification with a --reason and asserts the intended precedence. | testing | 15 | code-review | claude | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/debt_resolve_test.go:223 | The new tests (`TestDebtResolve_WontfixStatusFoldsItemOutOfOpenList`, `TestDebtResolve_MarkWontfixSetsStatusAndFoldsOut`, etc.) cover the happy path for wontfix but do not include a test for the idempotency case: resolving an already-closed item with `--status wontfix` when a `resolved` record already exists (or vice versa). The existing `TestDebtResolve_MarkResolvedIsIdempotent` covers this for `--status resolved`, but the wontfix variant is missing, leaving the "already resolved" hardcoded-message regression (finding 1) untested for wontfix closures. | Add `TestDebtResolve_MarkWontfixIsIdempotentAgainstResolved` (and symmetric) to mirror the existing resolved idempotency test | test-coverage | 20 | code-review | mira | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/debt_resolve_test.go:355 | `TestDebtResolve_NoReasonPreservesExistingJustification` verifies that omitting `--reason` preserves an existing justification, but does not test the edge case where the original record has an empty `Justification` and `--reason` is also omitted — does the new record get a blank `Justification` (zero-value field) or does it remain unset? This matters for clients that distinguish between "no justification provided" (nil/empty) and "justification is empty string". | Add assertion that `terminal.Justification == ""` (not uninitialized) when no `--reason` is supplied and original had no justification | test-coverage | 10 | code-review | mira | MEDIUM |
| <nil> | [ ] | MEDIUM | cmd/atcr/reconcile.go:205 | persistLocalDebt seeds its dedup 'seen' set from ALL existing records regardless of status (lines 205-207), then skips any finding whose id is already present (line 254). This is status-blind: a finding previously marked resolved (genuinely fixed) that later genuinely regresses — same file/line/problem, same id — is silently dropped and never re-surfaced. For wontfix this is the intended suppression; for resolved it means real regressions of previously-fixed items vanish from the backlog. | When seeding 'seen', treat only permanent-terminal records (wontfix) as hard suppressors and let a resolved id re-open on re-detection (or re-surface with a 'regressed' marker). Verify with a test that appends a resolved record, re-detects the same finding, and asserts it re-appears. | correctness | 120 | code-review | claude | MEDIUM |
| <nil> | [ ] | LOW | cmd/atcr/reconcile_test.go:569 | TestPersistLocalDebt_WontfixSuppressesReappend is tagged 'Epic 24.0 AC #3' and asserts recs[0].Status == wontfix, implying it locks wontfix-driven suppression. But persistLocalDebt dedups purely by id-presence and never inspects status — the test passes identically if the seed status were resolved or empty, and would still pass if wontfix were removed from isClosedStatus. The status assertion only confirms the seed row was not mutated; the test's own comment concedes suppression is independent of the terminal status value, so the AC#3 attribution overstates what is locked. | Add an assertion that depends on wontfix semantics — after seeding wontfix and re-detecting, run 'debt resolve --list' and assert the finding is absent (that path routes through isClosedStatus), so deleting wontfix from the closed set would fail. Verify by temporarily removing wontfix from isClosedStatus and confirming a test goes red. | testing | 30 | code-review | claude | MEDIUM |
| U | [ ] | LOW | internal/localdebt/store.go:67 | The store is append-only with no compaction; every resolve/wontfix appends a full record copy, and every ReadAll/selectOpenDebt/persistLocalDebt re-reads and folds the entire history on each invocation. Combined with the line-sensitive id (record.go StampID), a churny false positive accumulates: each line drift appends a fresh open record plus a fresh wontfix record, growing shards unbounded while every command pays the full O(total-records) scan. There is no cap or GC path. | Add a compaction/GC command (or periodic fold-and-rewrite that drops superseded closed records) so shard size tracks live findings, not total history. Verify by simulating many resolve cycles and asserting bounded on-disk size after compaction. | performance | 240 | code-review | claude | MEDIUM |
