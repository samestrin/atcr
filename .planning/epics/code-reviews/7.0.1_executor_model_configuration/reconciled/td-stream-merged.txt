LOW|internal/registry/config.go:501|The control-character guard `r < 32` (used for persona, name, rules, scope) is documented as blocking CR/LF prompt-line forgery, but it misses U+0085 (NEL), U+2028 (LINE SEPARATOR), U+2029 (PARAGRAPH SEPARATOR), and DEL (127). U+2028/U+2029/U+0085 are treated as line breaks by many renderers and tokenizers, so the guard only partially meets its own stated goal for personas/rules interpolated mid-prompt.|Replace `r < 32` with `unicode.IsControl(r)` plus explicit checks for U+2028, U+2029, and U+0085 (or a shared helper) applied consistently to persona, name, rules, and scope. Add a test embedding U+2028 in a rule asserting rejection.|security|30|code-review|claude|MEDIUM
MEDIUM|internal/registry/config.go:521|validateExecutor guards executor temperature with `*e.Temperature < 0` or `*e.Temperature > 2`. In Go all NaN comparisons are false, so a YAML `temperature: .nan` unmarshals to a real NaN *float64 and PASSES validation, violating the epic's stated 0.0..2.0 contract. The NaN then flows EffectiveExecutorTemperature -> callExecutor -> llmclient.Invocation.Temperature -> json.Marshal, which errors `json: unsupported value: NaN`, so every fix-eligible finding fails generation at runtime with a FixWarning instead of the config being rejected once at load. (disagreement: LOW vs MEDIUM)|In validateExecutor add `math.IsNaN(*e.Temperature)` and `math.IsInf(*e.Temperature, 0)` checks to the temperature guard. Add a test feeding `temperature: .nan` asserting LoadRegistry returns a temperature error. The same gap exists in validateAgent (~config.go:592) and should be fixed in the same pass.|correctness|30|code-review|claude,code-reviewer|HIGH
LOW|internal/registry/config.go:524|Potential prompt injection via system_prompt|Add control character validation to SystemPrompt|security|15|code-review|otto|MEDIUM
LOW|internal/registry/config.go:529|system_prompt is length-capped (MaxExecutorSystemPromptLen=4096) but, unlike persona/rules, is NOT control-char filtered, and buildFixPrompt writes it verbatim at the top of the prompt followed by finding metadata. This is an intentional documented tradeoff (multi-line framing is legitimate; registry is self-authored; output only lands in the Fix column), but it is inconsistent with the surrounding hardening where persona rejects newlines. If a registry is ever templated from semi-trusted input, system_prompt is the easiest injection surface with no structural guard.|Decision item: if the threat model trusts system_prompt, document it explicitly; otherwise strip/reject only `\r` (allow `\n` for legitimate multi-line) so a CRLF cannot forge the metadata lines that follow. Add a test asserting the chosen behavior.|security|15|code-review|claude|MEDIUM
MEDIUM|internal/registry/config.go:535|validateExecutor caps the length of each rule (MaxExecutorRuleLen=512) but imposes no cap on the NUMBER of rules. buildFixPrompt interpolates every rule verbatim into the fix prompt (executor.go:235-241), so a templated or generated registry with thousands of <=512-char rules sends N x 512 bytes of free text on every fix call, defeating the per-rule cap's stated prompt-stuffing rationale. Rules is the one unbounded untrusted-text vector in the executor block.|Add a MaxExecutorRules constant (e.g. 64) beside MaxExecutorRuleLen and reject len(e.Rules) > MaxExecutorRules in validateExecutor. Add a test supplying MaxExecutorRules+1 rules asserting load failure, mirroring the existing bounding-cap pattern.|security|30|code-review|greta,claude|HIGH
LOW|internal/registry/executor_config_test.go:287|No test exercises the NaN/Inf temperature path or the rules-count boundary. The out-of-range temperature test only checks 3.5, which the `>2` branch trivially catches, giving false confidence in temperature-validation robustness; the NaN gap survived precisely because no test exercises it.|Add TestExecutor_TemperatureNaNRejected (`temperature: .nan`) and TestExecutor_TemperatureInfRejected plus a rules-count cap test alongside the validation fixes. Verify they fail before the validation change and pass after.|testing|15|code-review|claude|MEDIUM
HIGH|internal/verify/executor.go:158|Taking address of loop variable temp|Store temperature in a variable before taking its address|correctness|20|code-review|executor,bob2|HIGH
HIGH|internal/verify/executor.go:198|Potential nil pointer dereference in buildFixPrompt when ex is nil|Add nil check for ex at function start|error-handling|5|code-review|bruce|MEDIUM
MEDIUM|internal/verify/executor.go:225|Prompt injection risk via unescaped config fields|Add explicit delimiters or instruction boundaries between system_prompt/rules and finding data|security|15|code-review|greta|MEDIUM
LOW|internal/verify/executor.go:229|buildFixPrompt re-derives the persona default (`if strings.TrimSpace(persona) == "" { persona = registry.DefaultExecutorPersona }`) even though applyDefaults already sets r.Executor.Persona = DefaultExecutorPersona at load (config.go:713-714). This duplicates default-resolution in a second place - the drift surface the epic eliminated elsewhere via single resolvers (EffectiveFixMinSeverity, EffectiveExecutorTemperature). No EffectiveExecutorPersona resolver exists.|Add an EffectiveExecutorPersona() resolver on ExecutorConfig mirroring the other Effective* methods and call it from buildFixPrompt, or drop the redundant inline fallback if the loaded-registry invariant is trusted. Verify existing prompt tests pass.|maintainability|15|code-review|claude|MEDIUM
