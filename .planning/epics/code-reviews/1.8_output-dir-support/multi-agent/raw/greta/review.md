```
MEDIUM|internal/fanout/reviewdir.go:233|Symlink traversal — os.ReadDir follows symlinks, so a symlink at the --output-dir path redirecting to an empty sensitive directory will cause review tree creation inside the symlink target|Resolve symlinks before the emptiness check: evaluate existing parent components with filepath.EvalSymlinks and reconstruct the path, or use os.Lstat to detect and reject symlinks at the final component|security|20|switch entries, err := os.ReadDir(dir); { // ReadDir follows symlinks — a symlink to an empty dir passes the check|greta
LOW|internal/fanout/reviewdir.go:233|TOCTOU race between os.ReadDir emptiness check and os.MkdirAll creation — another process can populate the directory in the gap, causing review files to be written alongside unvalidated content|Use os.OpenFile with O_EXCL|O_CREATE for the leaf directory to make creation atomic, or document the accepted race window|security|15|os.ReadDir(dir) ... // gap ... os.MkdirAll(dir, 0o755)|greta
```