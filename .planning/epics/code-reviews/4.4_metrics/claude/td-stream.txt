# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/4.4_metrics.md
# TARGET_TYPE: epic
# CREATED: 2026-06-19 10:26:41
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
MEDIUM|cmd/atcr/review_summary.go:26|The CLI summary line "Agents: X/Y succeeded, F failed, T timed out" mixes granularities: the numerator counters (atcr_agents_succeeded/failed/timed_out) are incremented once per invokeAgent attempt in engine.go:430-434 — which runs for the primary AND every fallback in a slot chain — while the denominator Y is result.Summary.Total, the slot count. A slot whose primary fails and fallback succeeds yields succeeded+failed greater than Y (e.g. "1/1 succeeded, 1 failed"). The CLI denominator also diverges from the atcr_agents_total the MCP atcr_metrics tool exports.|Source the denominator from reg.Counter(metrics.NameAgentsTotal).Value() so numerator and denominator share the per-attempt registry granularity the MCP tool exports, or move the succeeded/failed/timeout tally to per-slot in invokeSlot. Pick one unit and document it.|correctness|60|cmd/atcr/review_summary.go:22-28; internal/fanout/engine.go:369-391,430-434|claude
MEDIUM|internal/fanout/review.go:355|recordReviewOutcome is called at each explicit terminal return (review.go:392,408,417,420) but is NOT deferred, while atcr_reviews_total.Inc() (355) and the atcr_review_duration_seconds histogram (357-359) run via defer. A panic between Inc and a terminal return increments reviews_total and observes duration but fires no outcome counter, silently breaking the invariant reviews_total == succeeded+failed+interrupted. The recovered-agent-panic path in the engine likewise increments atcr_agents_total (engine.go:430) without recording an outcome or duration.|Record review and agent terminal outcomes from a defer so they fire on every exit including panic; route resultFromPanic results through recordAgentOutcome and the duration Observe.|correctness|60|internal/fanout/review.go:355-421; internal/fanout/engine.go:289-295,430-435|claude
MEDIUM|cmd/atcr/review.go:225|The Epic 4.4 AC3 end-of-review summary prints only on the success path. The `if err != nil { return err }` guard at review.go:217-219 returns before writeReviewSummary at 225-226, so an all-agents-failed review (exit 1, artifacts preserved) prints no duration/agents/API/findings summary — the run an operator most needs the breakdown for. The interrupt path (reportInterrupt) and the entire --resume path (resume.go) also never call writeReviewSummary.|Move writeReviewSummary above the error-return guard (it already guards result != nil) and add an equivalent call to runResume after its success line, sourcing elapsed from the resume StartedAt.|correctness|60|cmd/atcr/review.go:213-227; cmd/atcr/resume.go:146-159|claude
MEDIUM|internal/fanout/metrics.go:56|recordFindingMetrics(merged) is called on the post-enforcement finding set: merged derives from findingsFor(r) AFTER enforceConstraints drops min-severity findings and truncates to max_findings (artifacts.go:81-92,166-176). Its doc comment claims these are "raw per-agent findings ... emitted by agents". For any roster using min_severity or max_findings, atcr_findings_total and the severity breakdown undercount what agents actually emitted, and the comment is false.|Decide the intended semantic: count the pre-enforcement findings slice if "emitted by agents" is intended, otherwise correct the comment to state it counts kept (post-guardrail) findings.|correctness|30|internal/fanout/metrics.go:52-65; internal/fanout/artifacts.go:81-92,166-176|claude
MEDIUM|internal/metrics/prometheus.go:96|WritePrometheus holds the registry mutex for the entire render and calls h.Percentile() four times per histogram; each call independently copies and sorts the full (up to 10k) sample window under the histogram mutex — four redundant O(n log n) sorts of identical data per histogram per scrape, serializing concurrent Observe/Inc during a scrape. It also reads each family's quantile/_sum/_count lines under separate lock acquisitions, so a family snapshot can be internally inconsistent under a concurrent Observe. Impact is small at the current metric scale but the cost grows with labeled-histogram cardinality.|Add a histogram method returning (sum,count,percentiles[]) under one lock with a single sort; snapshot registry keys and metric pointers under r.mu, then render outside the registry lock (counters are atomic).|performance|60|internal/metrics/prometheus.go:61-104; internal/metrics/metrics.go:88-141|claude
LOW|cmd/atcr/review.go:226|writeReviewSummary computes elapsed = time.Since(now) where now (review.go:154) is captured before config load, PrepareReview, payload build, and fan-out, yet review_summary.go:25 labels it "Review completed in %.1fs". This is total wall-clock, not the fan-out window the atcr_review_duration_seconds histogram (exported by the MCP tool) measures, so the CLI and MCP report two divergent durations for the same review.|Relabel the line to "Total elapsed", or source the duration from the atcr_review_duration_seconds histogram so CLI and MCP agree; document which window each represents.|maintainability|30|cmd/atcr/review.go:154,226; cmd/atcr/review_summary.go:25|claude
LOW|internal/fanout/metrics.go:62|atcr_findings_by_severity is keyed on stream.NormalizeSeverity (ToUpper+TrimSpace, no validation), so a finding parsed with an empty or junk severity produces a counter keyed {severity=""} or {severity="BANANA"}. The CLI severityOrder renders only CRITICAL/HIGH/MEDIUM/LOW (review_summary.go:13), so such findings count in atcr_findings_total but vanish from the breakdown (breakdown no longer reconciles to the total) and add unbounded label cardinality to the serve-mode export.|Clamp unknown/empty severities to an explicit UNKNOWN bucket (added to severityOrder) or validate against the canonical set before keying the counter.|correctness|30|internal/fanout/metrics.go:61-64; cmd/atcr/review_summary.go:13|claude
LOW|internal/metrics/prometheus.go:28|escapeLabelValue escapes backslash, double-quote, and newline but NOT carriage return (\r), while its doc comment calls the escaping "complete" and "a security boundary". Currently reachable label values (normalized severity, integer HTTP status) cannot contain \r so this is latent, but the safety claim is inaccurate and a future model-derived label could carry a CR that corrupts the exposition stream for some parsers.|Add strings.ReplaceAll(v, "\r", backslash-r) after the \n replacement (order after the backslash escape is preserved); add a test feeding "a\rb" through Key.|security|15|internal/metrics/prometheus.go:25-33|claude
LOW|internal/fanout/metrics.go:36|recordAgentOutcome special-cases only Turns==0 (set to 1) but the comment claims max(1,Turns) semantics; a negative Turns from a corrupt Result would Add a negative delta to the monotonic atcr_api_calls_total counter. Separately it keys atcr_api_errors_total on strconv.Itoa(he.Status) with no guard, so a zero or negative HTTPStatusError.Status emits an uninformative {status="0"} bucket an operator cannot map to a failure class.|Use `if apiCalls < 1 { apiCalls = 1 }` to match the documented max; guard he.Status > 0 before recording, or bucket non-positive codes under {status="unknown"}.|error-handling|15|internal/fanout/metrics.go:36-45; internal/llmclient/client.go:374|claude
LOW|internal/metrics/metrics.go:71|histogram.Observe does not guard NaN/+Inf/-Inf: a single NaN permanently poisons h.sum (Mean and the rendered _sum become NaN forever) and corrupts sort order in Percentile (sort.Float64s NaN ordering is not total). The package exposes a general Observe(v) call-site API with no documented finite-value contract; current callers pass finite time.Since().Seconds() so the hazard is latent.|Drop non-finite inputs at the top of Observe (math.IsNaN/IsInf check) or document that Observe requires finite values; add a test asserting Observe(NaN) leaves Sum/Mean/Percentile finite.|error-handling|15|internal/metrics/metrics.go:71-86|claude
LOW|internal/metrics/metrics.go:76|Two robustness/clarity nits in otherwise-correct, 100%-covered code: (1) the histogram ring-buffer full transition relies on `len(values) == maxHistogramSamples` exactly, an implicit invariant that holds only because append grows by one; (2) ceilDiv computes a ceiling-of-product (not a division) despite its name, via float arithmetic that could in principle return an off-by-one-low rank at exact-boundary percentiles.|Use `>=` for the full check; rename ceilDiv to nearestRank (or use math.Ceil) and add a wrap-boundary test observing maxHistogramSamples+1 values.|maintainability|30|internal/metrics/metrics.go:71-116|claude
LOW|internal/mcp/server.go:128|Minor maintainability: atcr_metrics is registered without a custom input schema (relying on SDK inference from MetricsArgs struct{}) while atcr_report has an explicit reportInputSchema, with no comment explaining the difference. handleMetrics also does not honor ctx mid-render after the initial ctx.Err() check — acceptable for the current tiny metric set but worth a note as cardinality grows.|Add a one-line comment at the atcr_metrics registration noting it takes no args so it needs no schema; revisit ctx handling only if labeled-metric cardinality grows.|maintainability|5|internal/mcp/server.go:97-128; internal/mcp/handlers.go:425-430|claude

