MEDIUM|internal/fanout/reviewdir.go:345|summary.json trusted without integrity verification — a tampered file with FailureMarker:true and Succeeded>0 forces partial=true; clearing the marker hides an incomplete run|Add HMAC or signature verification of summary.json, or restrict file permissions to prevent tampering|security|30|if ps.FailureMarker && ps.Succeeded > 0 { return true } — attacker-controlled JSON fields drive reconciliation logic|greta
MEDIUM|internal/fanout/artifacts.go:126|writeFailureSummary silently swallows write error — if the failure-summary write itself fails, no marker is left on disk and ReadManifestPartial falls back to manifest, which may report partial:false for an incomplete run, hiding the failure|Return the error from writeJSON so the caller can escalate or retry|security|15|if err := writeJSON(...); err != nil { fmt.Fprintf(os.Stderr, "atcr: warning: ...") }|greta
LOW|internal/fanout/artifacts.go:126|Non-atomic write of summary.json — a crash mid-write leaves a truncated file; json.Unmarshal fails and ReadManifestPartial falls back to manifest, potentially hiding a partial run|Use atomic write pattern (write to temp file, then os.Rename)|security|20|writeJSON(filepath.Join(poolDir, summaryFile), ps)|greta
LOW|internal/fanout/reviewdir.go:342|Succeeded/Failed/Total fields from untrusted JSON are not bounds-checked — a tampered summary.json with Succeeded:999999999 could pass the >0 check and force partial; negative values could also produce unexpected logic|Validate that count fields are non-negative and consistent (Total == Succeeded + Failed) before trusting them|security|10|if ps.FailureMarker && ps.Succeeded > 0|greta