HIGH|internal/fanout/status.go:152|Contract drift between the two summary.json readers. ReadManifestPartial (reviewdir.go:348) forces partial=true for a FailureMarker summary with Succeeded>0, but ReadReviewStatus reads st.Partial = ps.Partial raw with no FailureMarker awareness, and sets Status=RunCompleted whenever Succeeded>0. So for a write-aborted review whose failure marker WAS written, `atcr status` (and the atcr_status MCP handler) reports partial:false + completed for the exact run reconcile reports partial:true. The ReadManifestPartial doc comment explicitly claims both readers share one source of truth and never drift; they now disagree. The epic's out-of-scope note only covers the no-summary stale-inference case, not the marker-summary-present case.|Extract a single method func (ps PoolSummary) EffectivePartial() bool returning ps.Partial || (ps.FailureMarker && ps.Succeeded > 0) and call it from BOTH ReadManifestPartial (reviewdir.go) and ReadReviewStatus (status.go:152) so the rule cannot drift. Add a status test on a FailureMarker,Succeeded>0 summary asserting st.Partial==true.|correctness|30|code-review|claude|MEDIUM|intent_note: deferred per epic — out-of-scope states "Changes to the atcr status / ReadReviewStatus path"
MEDIUM|internal/fanout/reviewdir.go:348|The partial guard keys off ps.Succeeded (computed by writeFailureSummary from in-memory summarize(results), artifacts.go:125), not the artifacts actually flushed to disk. summarize() buckets StatusTimeout and unknown statuses as Failed, not Succeeded (outcome.go). A timed-out agent that produced partial Content still has findings parsed by findingsFor and flushed by writeAgentArtifacts before a WritePool fault, yet contributes Succeeded=0. If every surviving agent is non-Succeeded (e.g. all timed out with content), the guard returns ps.Partial (false) and reconcile emits a non-partial verdict over the surviving findings subset — the same class of bug the epic set out to prevent, reached via a different status path. The comment asserts zero successes means no surviving subset, but never verifies it.|Gate partiality on the presence of surviving source artifacts rather than the in-memory success count, OR force partial whenever FailureMarker is set and Total>0 (any failure-path write is by definition an untrustworthy roster). Add a test: a StatusTimeout result carrying Content, flushed before a WritePool fault, asserting reconcile reports partial.|correctness|60|code-review|claude|MEDIUM|
MEDIUM|internal/fanout/reviewdir.go:341|ReadManifestPartial treats json.Unmarshal(...)==nil as trustworthy, but a summary.json that is valid JSON yet semantically empty (the literals {} or null or 0) unmarshals without error into a zero-value PoolSummary (Partial=false, FailureMarker=false, Succeeded=0). The function then returns partial:false, treating a corrupt-but-parseable or truncated-to-prefix summary as a clean non-partial run. The success path is protected by atomic temp+rename, but writeFailureSummary write errors are only logged to stderr, so a partially written marker summary could leave a parseable-but-wrong file.|After unmarshal, sanity-check the decoded record (e.g. require ps.Total>0 and ps.Total==ps.Succeeded+ps.Failed) before trusting ps.Partial; on invariant failure fall through to the manifest. Verify with tests feeding {} and null summaries asserting the manifest fallback fires.|error-handling|30|code-review|claude|MEDIUM|
MEDIUM|internal/fanout/reviewdir_test.go:310|No integration test covers the behavior the epic exists to guarantee end-to-end: a real WritePool I/O fault -> writeFailureSummary -> EnsureReviewComplete passes -> RunReconcile emits partial:true. Existing tests verify ReadManifestPartial in isolation against hand-written summaries and writeFailureSummary's marker stamping, but never the integrated path through the reconcile gate — which is exactly where the status-reader drift (see status.go:152 finding) hides, since EnsureReviewComplete keys off ReadReviewStatus.|Add an integration test that forces WritePool to fail mid-flush (read-only sub-dir or injected writer error), runs ExecuteReview, then asserts BOTH ReadReviewStatus and a real reconcile report partial. It should fail today against the status reader.|testing|60|code-review|claude|MEDIUM|
LOW|internal/fanout/reviewdir.go:339|ReadManifestPartial returns a bare bool and swallows every error: unreadable summary (permission denied, I/O error — not just absence), corrupt summary, unreadable manifest, and corrupt manifest all collapse to false. ReadReviewStatus, reading the identical files, surfaces corruption as an explicit summary.json is corrupt error. So a corrupt summary that would block a status read silently yields partial:false on the reconcile path. Currently masked because reconcile is gated by EnsureReviewComplete (which validates first), but a latent footgun if that gate is ever reordered or bypassed.|Document the precondition that this function is only called after EnsureReviewComplete validated both files, OR change the signature to (bool, error) and let the caller decide. Lowest-effort: add the precondition to the doc comment.|maintainability|15|code-review|claude|MEDIUM|
LOW|internal/fanout/reviewdir.go:340|ReadManifestPartial does os.ReadFile + json.Unmarshal on sources/pool/summary.json with no size cap, and PoolSummary.Agents []AgentStatus has no element cap. Via --output-dir (documented at reviewdir.go as accepting arbitrary orchestrator-shaped trees) or a hand-assembled review anchored by path, a multi-gigabyte or millions-of-agents summary.json is slurped whole into memory before unmarshal. Developer tool with a stated trust boundary, so low severity, but the read is genuinely unbounded — same exposure in ReadReviewStatus.|Cap the read with an io.LimitReader (a real summary is kilobytes) before unmarshal, shared with ReadReviewStatus. Verify with a test feeding an oversized summary asserting a bounded-read error rather than an OOM.|security|30|code-review|claude|MEDIUM|
