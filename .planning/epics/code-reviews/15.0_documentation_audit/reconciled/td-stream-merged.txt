LOW|cmd/atcr/docs_audit_test.go:71|atcrInvocations only scans inline code spans and fenced code blocks. A removed/renamed command or flag mentioned in plain prose without backticks (e.g. "run atcr frobnicate to seed the pool") is never captured, so it escapes all three command/flag guards. Since docs/ is the single source of truth for a website build, prose references are exactly what a reader follows. Defensible boundary but undocumented in the helper contract and it widens the drift the guards silently tolerate.|Note the code-span-only scope explicitly in the helper comment, or additionally scan prose `atcr <verb>` occurrences with a stricter word-boundary regex. Verify with a prose fixture referencing a fake command.|testing|30|code-review|claude|MEDIUM
LOW|cmd/atcr/docs_audit_test.go:181|The subcommand group check (lines 181-185) validates only tokens[1] and only when isWord(tokens[1]) is true. `atcr benchmark --json frobnicate` tokenizes to [benchmark,--json,frobnicate]; because tokens[1] is --json (not a word) the subcommand check is skipped entirely and the bogus frobnicate at tokens[2] is never validated. Any flags-before-subcommand form or nesting deeper than one level bypasses subcommand validation. Low blast radius today (shallow command tree) but the guard does not enforce what its comment claims.|Walk the documented tokens skipping --flag/values to find the first bare word after a group and validate it against the group's children; recurse for deeper groups. Verify with a fixture `atcr benchmark --json frobnicate` asserting a failure.|testing|30|code-review|claude|MEDIUM
MEDIUM|cmd/atcr/docs_audit_test.go:188|canonicalFlags() (lines 140-158) flattens every flag on every command into one global union set, and the validation loop (lines 187-191) checks a documented --flag only against that union while the error string claims per-command scope ("references --%s on an atcr %s command"). Empirically confirmed: checkpoint (a `benchmark run`-only flag) and json (a `doctor`-only flag) resolve true globally, so a doc writing `atcr review --checkpoint` or `atcr init --json` (a flag attached to the WRONG command) passes the guard silently. This is exactly the CLI-instruction drift the epic exists to catch.|Build a per-command flag set (walk each command, collect local+inherited+persistent flags keyed by command path) and validate a documented flag against the flags reachable from name0/its subcommand, not the global union. Verify by adding fixture line `atcr review --checkpoint` and asserting the test now fails.|testing|60|code-review|claude|MEDIUM
LOW|cmd/atcr/docs_audit_test.go:226|TestReconcilerConfigSurfaceDocumented only asserts strings.Contains(registry.md, "persona:"/"debate:"/"verify:"/"executor:"). These tokens appear dozens of times as incidental YAML and prose, so deleting the entire Executor documentation section while leaving the word executor: anywhere in the file still passes. It guards vocabulary presence, not that the config block is actually documented.|Anchor each block to a real heading or a fenced YAML key at column 0 (e.g. require a ##/### header whose text names the block), not a bare Contains. Verify by deleting the Executor section body and confirming the test fails.|testing|30|code-review|claude|MEDIUM
LOW|cmd/atcr/docs_audit_test.go:312|TestArchitectureDocDescribesReconciler lowercases the whole doc and asserts 8 common substrings ("review","reconcile","cluster","dedupe","confidence","verify","debate","persona") each appear somewhere. It cannot detect an inaccurate architecture: stating the merge threshold as 0.9 instead of 0.7, or reordering the stages, still passes because all 8 words remain present. It guards against a stub, not against drift in the substantive claims (dedupe cutoffs, tier order, env-var name) the doc actually makes.|Either accept it as a stub-only guard (say so in the comment) or assert the load-bearing facts directly (0.7/0.4 and ATCR_DISABLE_AST_GROUPING appear, cross-checked against reconcile/dedupe.go and internal/reconcile/gate.go). Verify by mutating a threshold in the doc and confirming failure.|testing|60|code-review|claude|MEDIUM
LOW|internal/registry/persona.go:33|The epic removed the fictional --task-message CLI flag from docs/registry.md (now correctly "not exposed as a CLI flag", line 162) but persona.go:33 still documents level 1 as "taskMessage (the --task-message flag)". Grep confirms no such flag is registered anywhere in cmd/. The exact drift the epic was chartered to eliminate still lives in the source comment, contradicting the corrected doc, and no guard test covers code comments so it will not be caught.|Edit persona.go:31-38 to describe taskMessage as an internal/programmatic resolution seam (matching registry.md line 162), dropping the "--task-message flag" phrasing. Verify with `grep -rn task-message internal/` returning only the resolution-source label and tests.|correctness|5|code-review|claude|MEDIUM
