LOW|cmd/atcr/docs_audit_test.go:216|Link regex captures trailing quoted titles (e.g. x.md "title") causing .md suffix check to fail, silently skipping valid index links from coverage validation|Strip trailing quoted titles from captured targets before suffix/anchor checks|correctness|15|re := regexp.MustCompile(`\]\(([^)]+)\)`) captures x.md "title", strings.HasSuffix(target, ".md") rejects it
LOW|cmd/atcr/docs_audit_test.go:176|strings.Contains matches config block names anywhere in prose, so a doc mentioning persona: in a sentence passes without actually documenting the YAML structure|Anchor checks to fenced-yaml lines or require start-of-line block pattern|correctness|15|strings.Contains(ref, block) matches prose occurrences of persona:, debate:, etc.
LOW|cmd/atcr/docs_audit_test.go:262|Test only asserts 8 substrings exist anywhere in the file, allowing a buzzword-sprinkled stub to pass without describing actual pipeline stages|Strengthen assertion to verify stage ordering, headings, or structural markers instead of bare substring presence|correctness|10|for _, term := range []string{"review", "reconcile"...} { if !strings.Contains(lower, term) ... }
LOW|cmd/atcr/docs_audit_test.go:71|Fenced-block state toggles on any line starting with ``` or ~~~, so a code example containing a line like echo "```" incorrectly closes the fence and exposes subsequent prose to command parsing|Require the fence line to match ^(```|~~~)[a-zA-Z]*$ or track fence delimiter length/type to avoid toggling on inner content|correctness|20|if strings.HasPrefix(trimmed, "```") || strings.HasPrefix(trimmed, "~~~") { inFence = !inFence } toggles on any matching prefix regardless of context
LOW|cmd/atcr/docs_audit_test.go:53|Root README.md is audited alongside docs/, so unrelated future README edits (e.g., adding a new atcr example) could fail CI in a docs-audit test and surprise contributors|Scope drift-token and command checks to docs/ only, or explicitly document the coupling in the test header|testing|10|paths := append(docs, filepath.Join(root, "README.md")) folds root README into auditedMarkdown used by all audit tests