MEDIUM|cmd/atcr/docs_audit_test.go:79|Fence toggle uses inFence=!inFence without tracking info-strings or handling unbalanced fences, leaving state true for EOF and parsing prose as commands|Track fence depth or match explicit closing fences to prevent state leakage on malformed input|correctness|15|if strings.HasPrefix(trimmed, "```") || strings.HasPrefix(trimmed, "~~~") { inFence = !inFence; continue }
LOW|cmd/atcr/docs_audit_test.go:216|Link regex \]\(([^)]+)\) captures trailing quoted titles (e.g. file.md "title"), causing the .md suffix check to fail and silently skip valid titled links|Strip trailing quoted title from captured target before suffix/anchor checks|correctness|10|re := regexp.MustCompile(`\]\(([^)]+)\)`)
LOW|cmd/atcr/docs_audit_test.go:176|strings.Contains matches config block tokens in arbitrary prose rather than anchoring to YAML block syntax, risking false positives on descriptive sentences|Anchor checks to fenced-yaml lines or start-of-line block pattern|correctness|10|if !strings.Contains(ref, block) { t.Errorf("docs/registry.md does not document the %s config block", block) }
LOW|cmd/atcr/docs_audit_test.go:262|Architecture validation only asserts 8 keyword substrings exist anywhere, allowing a buzzword-sprinkled stub to pass without verifying structural ordering|Strengthen check to assert stage ordering/structure or require keyword proximity|correctness|10|for _, term := range []string{"review", "reconcile", "cluster", "dedupe", "confidence", "verify", "debate", "persona"} { if !strings.Contains(lower, term) { ... }
LOW|cmd/atcr/docs_audit_test.go:63|Inline code regex runs over the entire document before the fence loop, so inline spans inside fenced blocks are captured twice, violating single-pass correctness|Exclude fenced regions from the inline regex pass or merge into a single parser state machine|correctness|10|for _, m := range regexp.MustCompile("`([^`\n]+)`").FindAllStringSubmatch(md, -1) { capture(m[1]) }