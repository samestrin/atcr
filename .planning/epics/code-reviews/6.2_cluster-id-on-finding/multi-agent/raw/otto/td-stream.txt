LOW|internal/debate/cluster.go:66|Potential for silent re-debate on problem drift|The check `clusterIdx[FindingKey{...}]` will fail if the problem text drifts, causing a re-debate. While documented as intended, it differs from the previous `locationKey` behavior which was more resilient to drift.|maintainability|15|`if c, ok := clusterIdx[FindingKey{...}]; ok && mergedIDs[c.ID]`|otto
