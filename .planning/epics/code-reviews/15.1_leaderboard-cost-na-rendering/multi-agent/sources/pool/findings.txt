# atcr-findings/v1
HIGH|internal/scorecard/export_test.go:381|TestExport_ClampsNegativeMetrics sets FindingsCorroborated=-2 which clamps to 0 at ingestion, so CostPerCorroboratedFindingUSD is always nil and the nil-guard assertion on that field is dead code, never exercising negative-CostUSD clamping through the non-nil pointer path.|Add a second case with positive FindingsCorroborated and negative CostUSD so the clamp is exercised through the non-nil pointer branch.|testing|15|rec.FindingsCorroborated = -2; if r.CostPerCorroboratedFindingUSD != nil { assert.GreaterOrEqual(t, *r.CostPerCorroboratedFindingUSD, 0.0) } — the nil-guard is always taken, the GreaterOrEqual assertion never executes.|dax
MEDIUM|internal/scorecard/export.go:99|reviewerAcc.finalize() always calls costPer and assigns the result, but costPer now returns nil when corroborated==0 — the nil pointer is assigned and omitempty drops the key, which is correct, but there is no test asserting the nil-assignment path through finalize (the only test of the nil case goes through Export→parseEnvelope, not the accumulator directly).|Add a unit test calling finalize on an acc with corroborated==0 and asserting CostPerCorroboratedFindingUSD is nil.|testing|10|pr.CostPerCorroboratedFindingUSD = costPer(a.costTotal, a.corroborated) — no test of finalize with corroborated==0 directly.|dax
MEDIUM|internal/benchmark/score.go:115|scoreOne now sets CostPerCorroboratedFindingUSD to a *float64 when matchedFindings>0, but the zero-cost-with-matches path (CostUSD==0, matchedFindings>0) yields a non-nil pointer to 0.0 — the benchmark scorer has no test asserting this specific disambiguation case (free+matched vs paid+unmatched).|Add a test case: CostUSD=0, matchedFindings>0 → assert NotNil and *v==0.0.|testing|10|TestScore_CostPerCorroboratedNilWhenPaidButUnmatched covers the nil path but no test covers the non-nil-zero path.|dax
LOW|internal/scorecard/export_test.go:103|TestExport_CostPerCorroboratedFinding uses a fixed record with FindingsCorroborated=7 and CostUSD=0.04 — the test name and assertion verify the division but the record's FindingsCorroborated is hardcoded in exportRec, making the test fragile to changes in the helper.|Extract the expected value from the record's own fields (0.04/7.0) or document the dependency on exportRec's internals.|testing|5|exportRec("bruce", "claude-sonnet-4-6", 1) — FindingsCorroborated=7 is an implicit contract of exportRec.|dax
LOW|.planning/technical-debt/README.md:67|TD item for `internal/scorecard/export_test.go:381` is listed as `LOW` but describes a dead-code/test-gap scenario that could mask regressions in clamping logic|Update severity to MEDIUM or refine the "Problem" to emphasize the risk of missing regressions|style|5|`TestExport_ClampsNegativeMetrics` sets FindingsCorroborated=-2 which clamps to 0... assertion on that field dead code`|otto
