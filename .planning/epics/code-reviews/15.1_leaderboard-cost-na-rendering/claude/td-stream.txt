# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/15.1_leaderboard-cost-na-rendering.md
# TARGET_TYPE: epic
# CREATED: 2026-07-02 10:31:37
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
MEDIUM|docs/scorecard.md:292-294|The epic updated the detailed field-reference table (docs/scorecard.md:242) to say cost_per_corroborated_finding_usd is omitempty, documenting exactly when it's omitted, but did NOT update the parallel "Preserved (allowlist)" summary a few lines down. That summary annotates survived_skeptic_rate with "(omitted when no verification ran)" but lists cost_per_corroborated_finding_usd with no annotation at all, implying it is always present -- contradicting the field two lines away and the sole per-record change this epic makes. The doc's own footer (docs/scorecard.md:320-322) states the privacy model must match internal/scorecard/export.go or it's a documentation bug.|Add the same style of parenthetical to the cost_per_corroborated_finding_usd bullet at docs/scorecard.md:294, e.g. "cost_per_corroborated_finding_usd (omitted when zero corroborated findings), latency_p50_ms".|documentation|5|docs/scorecard.md:289-294|claude
MEDIUM|internal/scorecard/export.go:275-281|costPer's doc comment and docs/scorecard.md both assert the emitted value is never Inf/NaN once non-nil. That guarantee is false under adversarial/corrupted input: clampNonNegF (export.go:180-185) only screens each individual record's CostUSD for NaN/Inf before adding it to a.costTotal (export.go:82) -- it never re-clamps the running sum. A group with enough large-but-finite per-run costs (or a tampered JSONL line with a huge cost_usd) can overflow a.costTotal to +Inf, which costPer then divides through and returns as a non-nil pointer to +Inf. encoding/json.Marshal cannot encode Inf/NaN floats and would fail with an undocumented error class.|Clamp a.costTotal (and/or the costPer result) with a clampNonNegF-style guard after the division in costPer, or clamp the accumulator itself after each += in add() (export.go:82). Add a test seeding two CostUSD values near math.MaxFloat64 in the same group asserting Export still returns a finite value or a clean error.|error-handling|30|internal/scorecard/export.go:78-98,180-185,275-281|claude
LOW|internal/scorecard/export_test.go:115-143|TestExport_CostPerCorroboratedAbsentWhenNoCorroboration and TestExport_CostPerCorroboratedPresentAndZeroWhenGenuinelyFree only ever exercise a single-record group. costPer is actually called with the GROUP total (a.costTotal, a.corroborated), summed across every record in the (persona, model) bucket (export.go:109), but no test aggregates 2+ records where the summed corroborated count is zero or where per-record corroborated counts differ from the group total. A regression that switched costPer's inputs from group totals to a single record's fields would pass every current test.|Add a test with 2+ exportRec calls for the same (persona, model), all with FindingsCorroborated=0, asserting the key stays omitted; and one with a mixed group (one record corroborated=0, another corroborated>0) asserting the key is present using the summed value.|testing|15|internal/scorecard/export_test.go:105-143|claude
LOW|internal/scorecard/export.go:275-278|costPer's guard is "if corroborated <= 0", but corroborated is always the result of summing clampNonNeg(r.FindingsCorroborated) into a.corroborated, which by construction can never go negative. The <= reads as if a negative corroborated is a real, reachable case being defended against, misleading a future maintainer about what inputs actually reach this function.|Change the check to "== 0" to accurately reflect the only reachable degenerate case, or add a one-line comment noting the <= is purely defensive against a hypothetical direct caller.|maintainability|5|internal/scorecard/export.go:275-281|claude
LOW|internal/scorecard/export.go:229-231|Export groups records by the scrubbed persona/model, and scrubField's absolute-path regexes can reduce an entirely path-like input string to empty. Two genuinely distinct reviewer identities that both happen to be path-like scrub to the same empty string and silently merge into one aggregated group, blending the costPer/SurvivedSkepticRate/FindingsRaisedAvg values this epic's change is meant to make trustworthy. Adjacent to, not caused by, this epic, but feeds the same grouping key costPer's group totals depend on.|Key groups on the pre-scrub identity (scrub only at final string-emission time), or fall back to a stable non-empty placeholder when scrubbing empties out an identity field. Add a test seeding two records whose Reviewer values are both pure absolute paths and assert they do NOT silently merge.|security|60|internal/scorecard/export.go:225-239,343-345|claude
HIGH|cmd/atcr/benchmark_run_resume_test.go:269-302|TestExecuteBenchmarkRun_ResumeReportsReplayedCount swaps the process-global os.Stderr for a pipe write-end at line 282, then only restores it at line 296 -- after a require.NoError(t, err) at line 292 that can call t.FailNow()/runtime.Goexit() and abort the goroutine before line 294-296 ever run. If the resumed executeBenchmarkRun call ever errors, os.Stderr is never restored and the pipe write-end is never closed: the io.Copy goroutine leaks forever, and every subsequent test in the same package/process silently writes to a dangling pipe instead of real stderr, corrupting diagnostics for the rest of the test run.|Restore os.Stderr and close w unconditionally via defer or t.Cleanup immediately after the swap at line 282, e.g. t.Cleanup(func() { os.Stderr = oldStderr }), before any assertion that can abort the test.|maintainability|15|cmd/atcr/benchmark_run_resume_test.go:278-296|claude
MEDIUM|internal/benchmark/score.go:120-123|scoreOne divides r.CostUSD by matchedFindings with zero validation on CostUSD itself. ReviewerScore.CostUSD is a plain public field the caller sets directly, not exclusively sourced through llmclient.ComputeCostUSD which guarantees non-negativity in production. A negative, NaN, or +Inf CostUSD silently produces a negative or non-finite CostPerCorroboratedFindingUSD that reaches a public leaderboard submission -- ScrubPublicRecord only touches Model/Persona, never the numeric fields. A NaN/Inf value will also make encoding/json.Marshal fail at export time with an opaque error far from the actual cause.|Add a guard mirroring clamp01 (e.g. if !math.IsNaN(v) && !math.IsInf(v,0) && v >= 0 { pr.CostPerCorroboratedFindingUSD = &v }) or reject/clamp negative CostUSD before dividing. Add a test with CostUSD=-1 and one with CostUSD=math.Inf(1) asserting the record stays sane instead of silently propagating.|error-handling|15|internal/benchmark/score.go:88-123|claude
MEDIUM|cmd/atcr/benchmark_run_test.go:141-152|TestBenchmarkRun_RoundTripsThroughExport is the one integration test that drives the real benchmark export CLI command end-to-end, but its decode struct has no CostPerCorroboratedFindingUSD/cost_per_corroborated_finding_usd field at all. It therefore provides zero coverage at the CLI/export boundary of the exact omit-vs-0.0 distinction this epic exists to implement; a regression that flips the key back to always-present (or drops it even when it should be present) would pass this test silently.|Add CostPerCorroboratedFindingUSD *float64 `json:"cost_per_corroborated_finding_usd"` to the anonymous decode struct and assert require.NotNil(...) plus the expected value; optionally also assert the raw string contains the key, mirroring internal/scorecard/export_test.go:125,139.|testing|15|cmd/atcr/benchmark_run_test.go:141-163|claude
LOW|internal/benchmark/score.go:91-110|normalizeSet(c.Raised) at line 92 already computes normalize(cat) for every raised finding once to build a set, but the matchedFindings loop at lines 106-110 re-runs normalize(cat) per finding again instead of reusing raised. Purely cosmetic at benchmark-suite scale, but it's duplicated work for no reason and slightly obscures that raised and the matched-findings count are computed from the same normalization.|Reuse the already-normalized raised set: iterate c.Raised, normalize once, and check membership against expected using the same normalized value.|performance|15|internal/benchmark/score.go:91-110|claude
LOW|internal/benchmark/score.go:38-40|The doc comment explicitly flags that a CaseScore with empty Expected silently drags down CorroborationRate (counts in the denominator, contributes nothing to the numerator), but the code does nothing to prevent or surface this beyond a comment. Score/scoreOne are exported/callable directly with hand-built ReviewerScore/CaseScore values that never pass through benchmark.Validate's "at least one expected category" suite-level check, so this known footgun is enforced only by convention.|Either have scoreOne skip/flag cases with empty Expected (excluding them from the denominator) or return an error/panic in a debug-asserted path, and add a test pinning the current documented-but-unenforced behavior.|error-handling|30|internal/benchmark/score.go:38-40,95-103|claude

