HIGH|internal/astgroup/merkle.go:14|MerkleHash uses unbounded recursion on AST depth|Replace with iterative post-order traversal or add depth limit to prevent stack overflow on deeply nested code|correctness|30|Recursive MerkleHash(n Node) calls itself for each child without depth bound
MEDIUM|internal/astgroup/host.go:177|Hardcoded 8MB source limit rejects large files|Make maxSourceBytes configurable via Host option or increase to 64MB with documentation|performance|10|const maxSourceBytes = 1 << 23 caps guest memory but may fall back to proximity on legitimate files
MEDIUM|internal/astgroup/parsers/src/pyparser/main.go:107|scanTripleQuotes heuristic doesn't handle escaped quotes|Add escape-sequence handling or document known limitation for PoC parser|correctness|20|strings.HasPrefix(line[i:], `"""`) misses \"\"\" inside strings causing false block detection
LOW|internal/astgroup/grouper.go:58|Path traversal check uses OS-specific separator|Use filepath.ToSlash(rel) and check for "../" prefix for cross-platform consistency|security|10|strings.HasPrefix(rel, ".."+string(filepath.Separator)) fails on Windows paths with backslash
MEDIUM|internal/astgroup/benchmark_test.go:85|Benchmark requires zero false positives (astFP == 0)|Relax to allow small FP rate (e.g., <1%) or document as strict AC3 gate for PoC|maintainability|5|require.Equal(t, 0, astFP) is brittle for heuristic parsers; may cause flaky CI
MEDIUM|internal/astgroup/cover.go:45|coveringChain returns early on first covering child without verifying non-overlap|Add assertion or comment that AST children have non-overlapping line ranges|correctness|15|for i := range n.Children returns on first match; assumes valid AST with disjoint child spans
LOW|internal/reconcile/grouper.go:54|File-level findings (Line<=0) grouped into single cluster per file|Verify this matches legacy proximity behavior for Line<=0 findings|correctness|10|clusters = append(clusters, fileLevel) groups all file-level findings regardless of line distance