MEDIUM|go.mod:13|wazero marked as // indirect but is a direct dependency|Move wazero to the direct require block|maintainability|5|require github.com/tetratelabs/wazero v1.12.0 // indirect — astgroup imports it directly|
MEDIUM|internal/astgroup/host.go:185|maxSourceBytes check bypassed on empty src via alloc(1)|Guard maxSourceBytes before empty-src alloc adjustment|correctness|3|if len(src) > maxSourceBytes { return error } then n=1 for empty src|
LOW|internal/astgroup/host.go:163|Parser pointer-stability not guaranteed if initErr is non-nil after first success|Cache parser before returning on subsequent calls|correctness|5|if h.initErr != nil after a successful parser was cached, Parser returns error instead of cached parser|
LOW|internal/astgroup/cover.go:55|coveringChain descends through non-block nodes but blockIdx increments for skipped blocks|blockIdx should only increment when isBlock(ch) and ch covers the line|correctness|10|blockIdx++ occurs before the line-range check; should move inside the covering branch|
LOW|internal/astgroup/grouper.go:93|MerkleHash called on block node which still contains line numbers in its children|Strip line numbers before hashing or ensure MerkleHash ignores them|correctness|5|MerkleHash recurses into children which carry StartLine/EndLine — hash invariance depends on field exclusion|
LOW|internal/astgroup/merkle.go:25|MerkleHash includes children's StartLine/EndLine via recursive hash of full Node|Children hashes include line fields, breaking drift invariance|correctness|10|MerkleHash(Node) recurses into Node.Children which embed StartLine/EndLine in their own hash|