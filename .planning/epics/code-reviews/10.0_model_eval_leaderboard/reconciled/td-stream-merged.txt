LOW|cmd/atcr/benchmark.go:56|runBenchmarkVerify parses and validates suite manifest twice|Use the returned error or document the safe-to-ignore assumption|performance|5|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark.go:56|Ignored GetString error for required flag|Use the returned error or document the safe-to-ignore assumption|performance|5|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark.go:56|Ignored error from cmd.Flags().GetString|Use the returned error or document the safe-to-ignore assumption|performance|5|code-review|td-reviewer|MEDIUM
MEDIUM|cmd/atcr/benchmark.go:82|Using current time for submitted_at instead of run-result's generated_at|Use rr.GeneratedAt parsed as time|correctness|10|code-review|reviewer|MEDIUM
LOW|cmd/atcr/benchmark.go:94|Ignored GetString error for required flag|Apply strings.TrimSpace to the suite fields to match Manifest.Validate, and reject an empty Reviewers slice with a clear error before BuildSubmission.|error-handling|15|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark.go:94|Ignored GetString error for optional flag|Apply strings.TrimSpace to the suite fields to match Manifest.Validate, and reject an empty Reviewers slice with a clear error before BuildSubmission.|error-handling|15|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark.go:94|Ignored errors from two GetString calls|Apply strings.TrimSpace to the suite fields to match Manifest.Validate, and reject an empty Reviewers slice with a clear error before BuildSubmission.|error-handling|15|code-review|td-reviewer|MEDIUM
LOW|cmd/atcr/benchmark.go:94|writeExportFile not defined in this file|Apply strings.TrimSpace to the suite fields to match Manifest.Validate, and reject an empty Reviewers slice with a clear error before BuildSubmission.|error-handling|15|code-review|bruce|MEDIUM
LOW|cmd/atcr/benchmark.go:94|benchmark export accepts degenerate run-results. It checks rr.Suite/rr.SuiteVersion are non-empty but a whitespace-only value (" ") passes (asymmetric with Manifest.Validate which trims), and an empty/nil Reviewers slice is accepted, so BuildSubmission emits a valid-looking suite submission with zero reviewers.|Apply strings.TrimSpace to the suite fields to match Manifest.Validate, and reject an empty Reviewers slice with a clear error before BuildSubmission.|error-handling|15|code-review|claude|MEDIUM
LOW|cmd/atcr/benchmark.go:105|runBenchmarkExport calls time.Now().UTC() directly in the handler, defeating BuildSubmission's injectable submittedAt param (designed for reproducibility, benchmark.go:216-217). The command's submitted_at is not deterministically testable. Consistent with the existing leaderboard.go:160 CLI pattern, so low priority.|Thread a clock seam through the command (package-level var now = time.Now overridable in tests, or a hidden test-only flag) so the CLI timestamp is controllable, matching the library's reproducibility design.|maintainability|15|code-review|claude|MEDIUM
LOW|cmd/atcr/benchmark.go:110|--output flag advertises symlink-following behavior|Verify writeExportFile's symlink behavior and consider rejecting symlink destinations or warn the user|security|10|code-review|td-reviewer|MEDIUM
LOW|docs/benchmark.md:158|Privacy model admits no re-scrub at export time|Track as tech debt but document clearly|maintainability|5|code-review|bruce|MEDIUM
LOW|internal/benchmark/benchmark.go:71|Load uses os.Stat without verifying regular file|Add check that os.FileMode.IsRegular() is true after Stat|error-handling|10|code-review|bruce|MEDIUM
LOW|internal/benchmark/benchmark.go:71|isSafeRelPath doesn't handle symlink escape|Add check that os.FileMode.IsRegular() is true after Stat|error-handling|10|code-review|bruce|MEDIUM
MEDIUM|internal/benchmark/benchmark.go:103|isSafeRelPath ignores symlinks allowing path traversal|Use os.Lstat to reject symlinks or EvalSymlinks to verify target stays in suite dir|security|10|code-review|bruce|MEDIUM
LOW|internal/benchmark/benchmark.go:108|Validate only checks len(ExpectedCategories)==0; individual empty or duplicate category strings pass. A case with expected_categories:["",""] validates and ReproHash hashes empty/duplicate strings, and the Epic 10.1 scorer would silently receive them. The contract comment implies meaningful categories.|In the case loop, reject any strings.TrimSpace(cat)=="" and reject (or de-dup) duplicate categories per case, mirroring the case-ID uniqueness check.|correctness|15|code-review|claude|MEDIUM
LOW|internal/benchmark/benchmark.go:115|Path validator permits dot path causing ReproHash to fail on directories|Reject clean == "." in isSafeRelPath|correctness|5|code-review|bruce|MEDIUM
MEDIUM|internal/benchmark/benchmark.go:119|isSafeRelPath does not resolve symlinks. It rejects absolute paths and lexical '..' escapes but filepath.Clean does not follow links, so a suite whose diff path is a symlink to an outside target (e.g. /etc/passwd) passes validation and Load's os.Stat (line 71) plus ReproHash's os.ReadFile (line 158) follow it outside suitePath. Benchmark suites are fetched from an external repo (untrusted input) and the guard comment claims it prevents reading arbitrary files. Current impact is limited (content is hashed not disclosed; benchmark run is deferred to 10.1) but the containment guarantee is real and broken.|In Load, after joining each diff path, filepath.EvalSymlinks the result and confirm it is still within an EvalSymlinks'd suitePath (filepath.Rel + leading-'..' check); reuse in ReproHash. Add a Load-level test creating an in-suite symlink pointing outside and asserting rejection, plus table cases for isSafeRelPath ('.', './x', '..').|security|60|code-review|claude|MEDIUM
LOW|internal/benchmark/benchmark.go:128|ReproHash re-reads diff files already stat'd by Load|Cache the diff bytes or pass the loaded manifest through|performance|5|code-review|td-reviewer|MEDIUM
LOW|internal/benchmark/benchmark.go:132|ReproHash calls Load which re-reads manifest|Consider passing Manifest to avoid double I/O|performance|5|code-review|bruce|MEDIUM
MEDIUM|internal/benchmark/benchmark.go:149|Potential for memory exhaustion via large diff files|The `ReproHash` function reads the entire diff file into memory using `os.ReadFile` and then converts it to a string, which can lead to OOM for very large benchmark cases.|Use `io.Copy` to stream the file content directly into the SHA-256 hasher.|Use `io.Copy` to stream the file content directly into the SHA-256 hasher.|0|code-review|`diffBytes, err := os.ReadFile(filepath.Join(suitePath, c.Diff))` followed by `writeField(h, string(diffBytes))`|MEDIUM
MEDIUM|internal/benchmark/benchmark.go:153|writeField ignores write errors|Stream the file into the hash with io.Copy(h, f) after a length prefix from f.Stat().Size(), and/or enforce a sane per-file size cap during Load. Avoid string(diffBytes). Verify with an oversized fixture that the cap rejects it.|error-handling|30|code-review|bruce|MEDIUM
MEDIUM|internal/benchmark/benchmark.go:153|PII leakage via unscrubbed run-result input|Stream the file into the hash with io.Copy(h, f) after a length prefix from f.Stat().Size(), and/or enforce a sane per-file size cap during Load. Avoid string(diffBytes). Verify with an oversized fixture that the cap rejects it.|error-handling|30|code-review|greta|MEDIUM
HIGH|internal/benchmark/benchmark.go:153|Path traversal vulnerability in ReproHash|Stream the file into the hash with io.Copy(h, f) after a length prefix from f.Stat().Size(), and/or enforce a sane per-file size cap during Load. Avoid string(diffBytes). Verify with an oversized fixture that the cap rejects it.|error-handling|30|code-review|bruce|MEDIUM
MEDIUM|internal/benchmark/benchmark.go:153|ReproHash reads each diff with unbounded os.ReadFile then converts to string (line 162), doubling the transient footprint. A hostile or accidental multi-GB diff in an externally-sourced suite makes atcr benchmark verify a memory-exhaustion DoS; there is no per-file size cap.|Stream the file into the hash with io.Copy(h, f) after a length prefix from f.Stat().Size(), and/or enforce a sane per-file size cap during Load. Avoid string(diffBytes). Verify with an oversized fixture that the cap rejects it.|error-handling|30|code-review|claude|MEDIUM
HIGH|internal/benchmark/benchmark.go:184|Missing error check on fmt.Fprintf/WriteString|Check write errors or use io.WriteString with error handling|error-handling|5|code-review|bruce|MEDIUM
MEDIUM|internal/benchmark/benchmark.go:227|BuildSubmission copies reviewers verbatim without re-scrub (disagreement: LOW vs MEDIUM)|Add defense-in-depth re-scrub or enforce at producer|security|15|code-review|bruce|MEDIUM
MEDIUM|internal/scorecard/export.go:103-110|NaN returned when verification data exists but verdict counts and stored rates are absent, producing invalid JSON|null check on storedRates before division|correctness|10|code-review|bruce|MEDIUM
LOW|internal/scorecard/export.go:115|scrubField called twice for same identity|Scrub once at ingestion, already done but verify no double-scrub|maintainability|5|code-review|bruce|MEDIUM
LOW|internal/scorecard/export.go:119|Survived-skeptic stored-rate fallback uses an unweighted mean. When verified+refuted==0 but storedRates is non-empty (a corrupt record carrying a rate pointer with nil count pointers), finalize averages the per-run rates unweighted (lines 119-125), reintroducing exactly the per-run-average bias the count-based path (lines 117-118) exists to avoid. Only triggers on the rare corrupt shape.|Document the fallback as a knowingly-unweighted best-effort path for corrupt records, or omit the survived_skeptic_rate key entirely when verdict counts are absent rather than emitting a biased mean. Confirm intended behavior with the spec owner.|correctness|30|code-review|claude|MEDIUM
MEDIUM|internal/scorecard/export.go:135|SurvivedSkepticRate falls through to 0.0 in degenerate verification case|Guard the hasVerification branch so the pointer is only emitted when at least one of verified+refuted>0 or len(storedRates)>0|error-handling|10|code-review|td-reviewer|MEDIUM
MEDIUM|internal/scorecard/export.go:160|Non-finite floats crash the entire public export. clampNonNegF (line 160) guards only negatives, and clampRate (line 167) falls through to default for NaN since NaN<0 and NaN>1 are both false. A scorecard Record with CostUSD = +Inf/NaN, or a SurvivedSkepticRate pointer carrying NaN, flows through aggregation into json.MarshalIndent which errors on non-finite values, returning a nil body and blanking the whole submission. This violates the stated contract (lines 142-145) that a corrupt-but-parseable record yields a dropped value, not a crash.|In clampNonNegF return 0 when math.IsNaN(f) or math.IsInf(f,0); in clampRate add a leading case math.IsNaN(f): return 0. Add table tests exporting a Record with CostUSD=math.Inf(1) and a SurvivedSkepticRate of NaN, asserting err==nil and finite output fields.|error-handling|15|code-review|claude|MEDIUM
LOW|internal/scorecard/export.go:195|ExportedAt/SubmittedAt uses time.Now() in tests only|Ensure production code uses passed exportedAt, not time.Now()|correctness|5|code-review|bruce|MEDIUM
LOW|internal/scorecard/export.go:213|Group key uses scrubbed identity which can collapse distinct reviewers|Add a test asserting two pre-scrub distinct personas that scrub to the same value merge intentionally|correctness|10|code-review|td-reviewer|MEDIUM
MEDIUM|internal/scorecard/export.go:218|Division by zero risk in costPer when corroborated=0|Already guarded but add explicit test for zero corroborated|correctness|10|code-review|bruce|MEDIUM
HIGH|internal/scorecard/export.go:218|Returning pointer to local variable|Already guarded but add explicit test for zero corroborated|correctness|10|code-review|reviewer|MEDIUM
MEDIUM|internal/scorecard/export.go:218|SurvivedSkepticRate fallback averages per-run rates|Already guarded but add explicit test for zero corroborated|correctness|10|code-review|bruce|MEDIUM
MEDIUM|internal/scorecard/export.go:238|medianInt64 integer division loses precision for even counts|Document that p50 is integer median (floor of average)|correctness|5|code-review|bruce|MEDIUM
LOW|internal/scorecard/export.go:258|medianInt64 even-count branch computes (sorted[n/2-1] + sorted[n/2]) / 2 which can overflow int64 if two latencies sum past MaxInt64. Latencies are milliseconds so this requires ~4.6e18 ms values and is not realistically reachable, but it is a silent wrong-answer path and the doc comment does not warn of it.|Compute as sorted[n/2-1] + (sorted[n/2]-sorted[n/2-1])/2 to avoid the sum. Verify with two near-MaxInt64 inputs.|correctness|15|code-review|claude|MEDIUM
LOW|internal/scorecard/export.go:264|medianInt64 truncates even-count medians via integer division|Use float division with math.Round or add 1 before dividing|correctness|5|code-review|bruce|MEDIUM
MEDIUM|internal/scorecard/export.go:285|scrubField defense-in-depth backstop has denylist gaps. scrubEmbeddedPath (line 304) only strips five FHS roots (/etc/ /Users/ /home/ /var/ /tmp/) while scrubAbsPath (line 306) deliberately preserves an alnum-preceded '/', so glued absolute paths under /opt/ /srv/ /mnt/ /root/ /private/ /usr/ survive into the public model/persona field; scrubWinPath (line 296) requires a drive letter so UNC paths (\\\\host\\share) pass; scrubEmail (307) needs a TLD dot so user@localhost passes; scrubKey (308) uses sk- (hyphen) and misses sk_live_ underscore keys and AIza Google keys. The allowlist (PublicRecord) is the primary guarantee and model/persona are controlled identities, so realistic leak risk is low, but the backstop is weaker than its comment claims.|Either replace the path denylist with a positive model-id allowlist (strip any '/'-bearing token not matching ^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$), or extend the FHS root denylist and add UNC (\\\\[^\\\\s]+\\\\S*), no-TLD email, and sk_/AIza key shapes. Add a table test enumerating the glued-root and UNC forms.|security|60|code-review|claude|MEDIUM
