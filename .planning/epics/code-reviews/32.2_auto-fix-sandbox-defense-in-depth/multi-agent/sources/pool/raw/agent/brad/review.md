MEDIUM|internal/sandbox/docker.go:232|Directly checking runCtx.Err() after cmd.Run() races with successful process exit when parent context cancels concurrently, misclassifying valid runs as TimedOut|Check runErr for context cancellation errors first, or only classify as timeout when runErr != nil|reliability|10|if errors.Is(runCtx.Err(), context.DeadlineExceeded) || errors.Is(runCtx.Err(), context.Canceled) { res.TimedOut = true ...
LOW|internal/sandbox/docker.go:240|Timeout/cancel kill command uses unbounded context.Background() and is not gated by b.sem, allowing cancellation storms to spawn unbounded docker kill subprocesses that can exhaust PIDs if the daemon is slow|Use ctx or a shared semaphore for the kill command, or bound it with a tighter timeout and track active kills|resource|15|killCtx, killCancel := context.WithTimeout(context.Background(), 5*time.Second) ... exec.CommandContext(killCtx, b.cfg.DockerPath, "kill", containerName).Run()
LOW|cmd/atcr/autofix.go:387|Fail-closed default path wraps revert failure with %w, masking the original refusal reason and leaving the tree in an undefined partial-revert state without a clear operator recovery path|Return a structured error or separate the refusal and revert failures, and log the original refusal explicitly|state|10|return fmt.Errorf("auto-fix: refusing unsandboxed host validation without --no-sandbox AND revert failed: %w", rerr)