# atcr-findings/v1
HIGH|cmd/atcr/autofix.go:380|Fail-closed default branch reverts patch and returns error but no test exercises this path|Add a unit test for runAutoFix with sandboxBackend=nil and noSandbox=false proving the revert+refuse message|testing|20|default branch at :380-387 is unreachable in existing tests because all host-path tests now set noSandbox=true|dax
MEDIUM|internal/sandbox/docker_test.go:243|TestDockerBackend_Run_CancellationClassIsTimedOut uses a fake docker script that sleeps 5s but the test only waits 50ms before cancel — the fake `run` subprocess may not have started yet, making the cancellation racy|Add a ready-signal file or pipe to the fake script so the test cancels only after `run` has started|testing|15|fake script: `if [ "$1" = "run" ]; then sleep 5; fi` — cancel fires at 50ms, but exec.CommandContext may still be in fork/exec; no synchronization proves the cancellation landed during Run|dax
LOW|internal/sandbox/docker.go:232|errors.Is check for context.Canceled folded into TimedOut but the kill-on-timeout path still runs `docker kill` for a cancellation — killing a container that may have already exited on cancel is harmless but wastes a subprocess spawn|Consider checking if the container still exists before issuing kill on a pure cancellation (not deadline)|performance|5|killCtx block at :235-239 runs unconditionally for both DeadlineExceeded and Canceled|dax
