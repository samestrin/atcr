


LOW|internal/sandbox/docker.go:198|Parent-context cancellation during semaphore wait exits early with raw error|The parent-context cancellation case inside the `select` on `b.sem <- struct{}{}` (line 198) returns `ctx.Err()` directly without creating `runCtx`. `res.TimedOut` is never set and a non-nil error is returned. Both are cancellation-class ends that should be folded into `TimedOut` per the epic comment. Test `TestDockerBackend_Run_CancellationClassIsTimedOut` does not cover this because its "context canceled" case has a 5-second timeout with a 50ms cancel, and with semaphore capacity 4 the goroutine never blocks on the select.|Initialize `runCtx` with the parent ctx's timeout before the select, or in the `ctx.Done()` branch construct a result with `TimedOut=true` and return it instead of the raw error|observability|15|The cancel fires at 50ms; the test timeout is 5s; semaphore capacity is 4; the goroutine never waits, so the parent cancel arrives inside cmd.Run() and is caught by the existing runCtx.Err() check. The semaphore-wait path (no slot available) is not exercised