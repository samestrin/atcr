HIGH|internal/fanout/artifacts.go:109|writeFailureSummary swallows MkdirAll/writeJSON errors with no log|Add logging to failure path diagnostics|ERROR_PATHS|45|The function deliberately swallows errors without logging, making it hard to diagnose secondary marker-write failures when the primary I/O error is already logged by the caller|REVIEWER
HIGH|internal/fanout/review.go:239|On the WritePool failure branch ExecuteReview writes the marker but never stamps manifest CompletedAt/Partial|Stamp CompletedAt and failed indicator in failure branch|OBSERVABILITY|30|Failed reviews lack manifest completion metadata, making them indistinguishable from unfinished scaffolds for duration-based tools|REVIEWER
LOW|internal/fanout/status.go:52|nowFunc is a mutable unsynchronized package var|Document that nowFunc must not be swapped concurrently or inject the clock via a struct field/parameter|UNDER_ENGINEERING|20|Future parallel tests swapping it while another goroutine calls ReadReviewStatus would data-race|REVIEWER
LOW|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance|Confirmed terminal-by-design; revisit only if grace-margin false positives are observed in practice|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
LOW|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|Bounds-check timeout_secs or compute the deadline with a saturating guard|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
MEDIUM|internal/fanout/artifacts.go:109|writeFailureSummary marks all agents failed even when an earlier agent already persisted findings during a mid-loop I/O fault, so reconcile can emit a non-partial verdict from the surviving subset that contradicts the failed marker|Have reconcile cross-check the failure-marker summary (or add a distinguishing marker field) so a partial-but-failed review is rejected rather than reconciled|INTEGRATION|60|Inconsistent failure states between agent results and marker files can cause incorrect review verdicts|REVIEWER
MEDIUM|internal/fanout/review.go:239|post-fan-out persistence failure maps to failed, not stale: a WritePool error writes a best-effort minimal summary.json so the reader reports failed instead of being stuck in_progress forever; if even that best-effort write fails, summary.json stays absent and stale inference promotes the review out of in_progress once the timeout elapses|The failure path correctly maps to failed via the existing summary-derived path (succeeded=0 → RunFailed); stale is only the fallback when even the best-effort marker cannot be written|ERROR_PATHS|30|Post-fan-out persistence failures should surface as failed rather than eternal in_progress, but the best-effort write failure handling could be more diagnosable|REVIEWER
LOW|internal/fanout/status.go:6|staleByDeadline reports whether a summary-less review has exceeded its inferred deadline. It returns false when the deadline is unknowable — a zero StartedAt or an absent timeout_secs (old manifests, zero value) — so those reviews keep reporting in_progress rather than being guessed dead.|Backward compatibility: a manifest written before timeout_secs existed (zero value) has no inferable deadline, so stale inference is disabled and it keeps reporting in_progress.|OBSERVABILITY|20|Stale inference silently disables for old manifests, which may mask configuration issues|REVIEWER
MEDIUM|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|Bounds-check timeout_secs or compute the deadline with a saturating guard|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
LOW|internal/fanout/status.go:48|There is no terminal state for dead reviews: ReadReviewStatus treats absent summary.json as in_progress unconditionally.|A scaffolded review with no summary.json and timeout elapsed reports status stale instead of in_progress forever, so the Skill polling loop cannot distinguish running from dead, and .atcr/latest keeps pointing at the orphan.|OBSERVABILITY|15|Dead reviews are incorrectly reported as in_progress forever, preventing proper cleanup and user feedback|REVIEWER
MEDIUM|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run rather than poll|Confirmed terminal-by-design; revisit only if grace-margin false positives are observed in practice|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
MEDIUM|internal/fanout/status.go:74|ReadReviewStatus reads manifest.json then summary.json in two separate steps while the background fan-out is writing both (intent_note: deferred per sprint-plan TD-023)|Keep summary.json as the sole completion sentinel — no new status.json snapshot, no new sentinel file. Design the read path so the manifest+summary pair is interpreted consistently with the (re-attempted) manifest-before-summary write ordering.|PERFORMANCE|30|Torn reads (TD-023) can misreport review status during concurrent writes, leading to incorrect status observations|REVIEWER
MEDIUM|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|Bounds-check timeout_secs or compute the deadline with a saturating guard|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
LOW|internal/fanout/status.go:66|staleByDeadline returns false when the deadline is unknowable — a zero StartedAt or an absent timeout_secs (old manifests, zero value) — so those reviews keep reporting in_progress rather than being guessed dead.|Backward compatibility: a manifest written before timeout_secs existed (zero value) has no inferable deadline, so stale inference is disabled and it keeps reporting in_progress.|OBSERVABILITY|20|Stale inference silently disables for old manifests, which may mask configuration issues|REVIEWER
OUT-OF-SCOPE|internal/fanout/artifacts.go:109|writeFailureSummary marks all agents failed even when an earlier agent already persisted findings during a mid-loop I/O fault, so reconcile can emit a non-partial verdict from the surviving subset that contradicts the failed marker|This issue is addressed in the epic's failure-marker path (ExecuteReview writes a best-effort minimal summary.json) and is out of scope for this review.|INTEGRATION|60|Inconsistent failure states between agent results and marker files can cause incorrect review verdicts|REVIEWER
OUT-OF-SCOPE|internal/fanout/review.go:239|On the WritePool failure branch ExecuteReview writes the marker but never stamps manifest CompletedAt/Partial|This issue is addressed in the epic's failure-marker path (ExecuteReview writes a best-effort minimal summary.json) and is out of scope for this review.|OBSERVABILITY|30|Failed reviews lack manifest completion metadata, making them indistinguishable from unfinished scaffolds for duration-based tools|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:52|nowFunc is a mutable unsynchronized package var|This issue is acknowledged in the epic (nowFunc must not be swapped concurrently) and is out of scope for this review.|UNDER_ENGINEERING|20|Future parallel tests swapping it while another goroutine calls ReadReviewStatus would data-race|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run rather than poll|This behavior is confirmed terminal-by-design in the epic and is out of scope for this review.|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|This edge case is acknowledged as practically unreachable in the epic and is out of scope for this review.|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:48|There is no terminal state for dead reviews: ReadReviewStatus treats absent summary.json as in_progress unconditionally.|This issue is addressed in the epic's stale inference (summary.json absent AND timeout elapsed reports status stale) and is out of scope for this review.|OBSERVABILITY|15|Dead reviews are incorrectly reported as in_progress forever, preventing proper cleanup and user feedback|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run rather than poll|This behavior is confirmed terminal-by-design in the epic and is out of scope for this review.|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:74|ReadReviewStatus reads manifest.json then summary.json in two separate steps while the background fan-out is writing both (intent_note: deferred per sprint-plan TD-023)|This issue is addressed in the epic's read-pair invariant (Task 4 documents and tests the ordering the code already produces) and is out of scope for this review.|PERFORMANCE|30|Torn reads (TD-023) can misreport review status during concurrent writes, leading to incorrect status observations|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|This edge case is acknowledged as practically unreachable in the epic and is out of scope for this review.|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:66|staleByDeadline returns false when the deadline is unknowable — a zero StartedAt or an absent timeout_secs (old manifests, zero value) — so those reviews keep reporting in_progress rather than being guessed dead.|This behavior is confirmed backward-compatible in the epic and is out of scope for this review.|OBSERVABILITY|20|Stale inference silently disables for old manifests, which may mask configuration issues|REVIEWER
OUT-OF-SCOPE|internal/fanout/artifacts.go:109|writeFailureSummary marks all agents failed even when an earlier agent already persisted findings during a mid-loop I/O fault, so reconcile can emit a non-partial verdict from the surviving subset that contradicts the failed marker|This issue is addressed in the epic's failure-marker path (ExecuteReview writes a best-effort minimal summary.json) and is out of scope for this review.|INTEGRATION|60|Inconsistent failure states between agent results and marker files can cause incorrect review verdicts|REVIEWER
OUT-OF-SCOPE|internal/fanout/review.go:239|On the WritePool failure branch ExecuteReview writes the marker but never stamps manifest CompletedAt/Partial|This issue is addressed in the epic's failure-marker path (ExecuteReview writes a best-effort minimal summary.json) and is out of scope for this review.|OBSERVABILITY|30|Failed reviews lack manifest completion metadata, making them indistinguishable from unfinished scaffolds for duration-based tools|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:52|nowFunc is a mutable unsynchronized package var|This issue is acknowledged in the epic (nowFunc must not be swapped concurrently) and is out of scope for this review.|UNDER_ENGINEERING|20|Future parallel tests swapping it while another goroutine calls ReadReviewStatus would data-race|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run rather than poll|This behavior is confirmed terminal-by-design in the epic and is out of scope for this review.|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|This edge case is acknowledged as practically unreachable in the epic and is out of scope for this review.|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:48|There is no terminal state for dead reviews: ReadReviewStatus treats absent summary.json as in_progress unconditionally.|This issue is addressed in the epic's stale inference (summary.json absent AND timeout elapsed reports status stale) and is out of scope for this review.|OBSERVABILITY|15|Dead reviews are incorrectly reported as in_progress forever, preventing proper cleanup and user feedback|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run rather than poll|This behavior is confirmed terminal-by-design in the epic and is out of scope for this review.|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:74|ReadReviewStatus reads manifest.json then summary.json in two separate steps while the background fan-out is writing both (intent_note: deferred per sprint-plan TD-023)|This issue is addressed in the epic's read-pair invariant (Task 4 documents and tests the ordering the code already produces) and is out of scope for this review.|PERFORMANCE|30|Torn reads (TD-023) can misreport review status during concurrent writes, leading to incorrect status observations|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|This edge case is acknowledged as practically unreachable in the epic and is out of scope for this review.|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:66|staleByDeadline returns false when the deadline is unknowable — a zero StartedAt or an absent timeout_secs (old manifests, zero value) — so those reviews keep reporting in_progress rather than being guessed dead.|This behavior is confirmed backward-compatible in the epic and is out of scope for this review.|OBSERVABILITY|20|Stale inference silently disables for old manifests, which may mask configuration issues|REVIEWER
OUT-OF-SCOPE|internal/fanout/artifacts.go:109|writeFailureSummary marks all agents failed even when an earlier agent already persisted findings during a mid-loop I/O fault, so reconcile can emit a non-partial verdict from the surviving subset that contradicts the failed marker|This issue is addressed in the epic's failure-marker path (ExecuteReview writes a best-effort minimal summary.json) and is out of scope for this review.|INTEGRATION|60|Inconsistent failure states between agent results and marker files can cause incorrect review verdicts|REVIEWER
OUT-OF-SCOPE|internal/fanout/review.go:239|On the WritePool failure branch ExecuteReview writes the marker but never stamps manifest CompletedAt/Partial|This issue is addressed in the epic's failure-marker path (ExecuteReview writes a best-effort minimal summary.json) and is out of scope for this review.|OBSERVABILITY|30|Failed reviews lack manifest completion metadata, making them indistinguishable from unfinished scaffolds for duration-based tools|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:52|nowFunc is a mutable unsynchronized package var|This issue is acknowledged in the epic (nowFunc must not be swapped concurrently) and is out of scope for this review.|UNDER_ENGINEERING|20|Future parallel tests swapping it while another goroutine calls ReadReviewStatus would data-race|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run rather than poll|This behavior is confirmed terminal-by-design in the epic and is out of scope for this review.|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|This edge case is acknowledged as practically unreachable in the epic and is out of scope for this review.|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:48|There is no terminal state for dead reviews: ReadReviewStatus treats absent summary.json as in_progress unconditionally.|This issue is addressed in the epic's stale inference (summary.json absent AND timeout elapsed reports status stale) and is out of scope for this review.|OBSERVABILITY|15|Dead reviews are incorrectly reported as in_progress forever, preventing proper cleanup and user feedback|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run rather than poll|This behavior is confirmed terminal-by-design in the epic and is out of scope for this review.|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:74|ReadReviewStatus reads manifest.json then summary.json in two separate steps while the background fan-out is writing both (intent_note: deferred per sprint-plan TD-023)|This issue is addressed in the epic's read-pair invariant (Task 4 documents and tests the ordering the code already produces) and is out of scope for this review.|PERFORMANCE|30|Torn reads (TD-023) can misreport review status during concurrent writes, leading to incorrect status observations|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|This edge case is acknowledged as practically unreachable in the epic and is out of scope for this review.|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:66|staleByDeadline returns false when the deadline is unknowable — a zero StartedAt or an absent timeout_secs (old manifests, zero value) — so those reviews keep reporting in_progress rather than being guessed dead.|This behavior is confirmed backward-compatible in the epic and is out of scope for this review.|OBSERVABILITY|20|Stale inference silently disables for old manifests, which may mask configuration issues|REVIEWER
OUT-OF-SCOPE|internal/fanout/artifacts.go:109|writeFailureSummary marks all agents failed even when an earlier agent already persisted findings during a mid-loop I/O fault, so reconcile can emit a non-partial verdict from the surviving subset that contradicts the failed marker|This issue is addressed in the epic's failure-marker path (ExecuteReview writes a best-effort minimal summary.json) and is out of scope for this review.|INTEGRATION|60|Inconsistent failure states between agent results and marker files can cause incorrect review verdicts|REVIEWER
OUT-OF-SCOPE|internal/fanout/review.go:239|On the WritePool failure branch ExecuteReview writes the marker but never stamps manifest CompletedAt/Partial|This issue is addressed in the epic's failure-marker path (ExecuteReview writes a best-effort minimal summary.json) and is out of scope for this review.|OBSERVABILITY|30|Failed reviews lack manifest completion metadata, making them indistinguishable from unfinished scaffolds for duration-based tools|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:52|nowFunc is a mutable unsynchronized package var|This issue is acknowledged in the epic (nowFunc must not be swapped concurrently) and is out of scope for this review.|UNDER_ENGINEERING|20|Future parallel tests swapping it while another goroutine calls ReadReviewStatus would data-race|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run rather than poll|This behavior is confirmed terminal-by-design in the epic and is out of scope for this review.|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|This edge case is acknowledged as practically unreachable in the epic and is out of scope for this review.|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:48|There is no terminal state for dead reviews: ReadReviewStatus treats absent summary.json as in_progress unconditionally.|This issue is addressed in the epic's stale inference (summary.json absent AND timeout elapsed reports status stale) and is out of scope for this review.|OBSERVABILITY|15|Dead reviews are incorrectly reported as in_progress forever, preventing proper cleanup and user feedback|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run rather than poll|This behavior is confirmed terminal-by-design in the epic and is out of scope for this review.|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:74|ReadReviewStatus reads manifest.json then summary.json in two separate steps while the background fan-out is writing both (intent_note: deferred per sprint-plan TD-023)|This issue is addressed in the epic's read-pair invariant (Task 4 documents and tests the ordering the code already produces) and is out of scope for this review.|PERFORMANCE|30|Torn reads (TD-023) can misreport review status during concurrent writes, leading to incorrect status observations|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|This edge case is acknowledged as practically unreachable in the epic and is out of scope for this review.|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:66|staleByDeadline returns false when the deadline is unknowable — a zero StartedAt or an absent timeout_secs (old manifests, zero value) — so those reviews keep reporting in_progress rather than being guessed dead.|This behavior is confirmed backward-compatible in the epic and is out of scope for this review.|OBSERVABILITY|20|Stale inference silently disables for old manifests, which may mask configuration issues|REVIEWER
OUT-OF-SCOPE|internal/fanout/artifacts.go:109|writeFailureSummary marks all agents failed even when an earlier agent already persisted findings during a mid-loop I/O fault, so reconcile can emit a non-partial verdict from the surviving subset that contradicts the failed marker|This issue is addressed in the epic's failure-marker path (ExecuteReview writes a best-effort minimal summary.json) and is out of scope for this review.|INTEGRATION|60|Inconsistent failure states between agent results and marker files can cause incorrect review verdicts|REVIEWER
OUT-OF-SCOPE|internal/fanout/review.go:239|On the WritePool failure branch ExecuteReview writes the marker but never stamps manifest CompletedAt/Partial|This issue is addressed in the epic's failure-marker path (ExecuteReview writes a best-effort minimal summary.json) and is out of scope for this review.|OBSERVABILITY|30|Failed reviews lack manifest completion metadata, making them indistinguishable from unfinished scaffolds for duration-based tools|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:52|nowFunc is a mutable unsynchronized package var|This issue is acknowledged in the epic (nowFunc must not be swapped concurrently) and is out of scope for this review.|UNDER_ENGINEERING|20|Future parallel tests swapping it while another goroutine calls ReadReviewStatus would data-race|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run rather than poll|This behavior is confirmed terminal-by-design in the epic and is out of scope for this review.|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|This edge case is acknowledged as practically unreachable in the epic and is out of scope for this review.|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:48|There is no terminal state for dead reviews: ReadReviewStatus treats absent summary.json as in_progress unconditionally.|This issue is addressed in the epic's stale inference (summary.json absent AND timeout elapsed reports status stale) and is out of scope for this review.|OBSERVABILITY|15|Dead reviews are incorrectly reported as in_progress forever, preventing proper cleanup and user feedback|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:122|report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run rather than poll|This behavior is confirmed terminal-by-design in the epic and is out of scope for this review.|REGRESSION_RISK|15|Stale reviews are incorrectly guided to re-run instead of stopping polling|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:74|ReadReviewStatus reads manifest.json then summary.json in two separate steps while the background fan-out is writing both (intent_note: deferred per sprint-plan TD-023)|This issue is addressed in the epic's read-pair invariant (Task 4 documents and tests the ordering the code already produces) and is out of scope for this review.|PERFORMANCE|30|Torn reads (TD-023) can misreport review status during concurrent writes, leading to incorrect status observations|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:156|staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout)|This edge case is acknowledged as practically unreachable in the epic and is out of scope for this review.|EDGE_CASES|15|False-positive stale detection could occur with extremely long timeouts|REVIEWER
OUT-OF-SCOPE|internal/fanout/status.go:66|staleByDeadline returns false when the deadline is unknowable — a zero StartedAt or an absent timeout_secs (old manifests, zero value) — so those reviews keep reporting in_progress rather than being guessed dead.|This behavior is confirmed backward-compatible in the epic and is out of scope for this review.|OBSERVABILITY|20|Stale inference silently disables for old manifests, which may mask configuration issues|REVIEWER