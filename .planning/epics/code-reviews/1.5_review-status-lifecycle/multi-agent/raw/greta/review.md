MEDIUM|internal/fanout/status.go:120|Stale inference bypassed for non-ErrNotExist I/O errors (e.g. permission denied on pool dir), causing dead reviews to report in_progress indefinitely past timeout|Apply staleByDeadline check regardless of error type when summary is unreadable, or return an error for non-ErrNotExist I/O failures instead of silently reporting in_progress|security|30|`if errors.Is(serr, os.ErrNotExist) && staleByDeadline(m)` — only ErrNotExist triggers stale; a permission-denied error on sources/pool/ skips stale inference entirely, returning in_progress even when the deadline has elapsed|greta
LOW|internal/fanout/status.go:158|Integer overflow in deadline computation with pathological timeout_secs can produce a negative Duration, causing immediate false-positive stale (DoS on review status)|Bounds-check timeout_secs before computing the deadline (e.g. reject values > math.MaxInt64/1e9 - staleGraceSecs) or use saturating Duration arithmetic|security|15|`time.Duration(m.TimeoutSecs+staleGraceSecs) * time.Second` — addition or Duration multiplication overflows int64 nanoseconds when TimeoutSecs approaches ~2.9e8|greta