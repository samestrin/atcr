MEDIUM|internal/fanout/status.go:122|ReadReviewStatus only infers stale when errors.Is(serr, os.ErrNotExist). A non-not-exist read error on summary.json (permission denied, EIO, dir-where-file-expected) falls through and reports in_progress forever — the exact eternal-in_progress failure the epic claims to remove. A dead fan-out whose pool dir is corrupt/permission-broken never reaches a terminal state.|In ReadReviewStatus, treat a persistent non-ErrNotExist read error past the deadline like absent for stale inference, or surface it as an explicit error rather than masking as in_progress. Verify with a test that chmods the pool dir unreadable after the deadline and asserts a terminal/error result.|error-handling|60|code-review|claude|MEDIUM
MEDIUM|internal/fanout/artifacts.go:109|writeFailureSummary writes PoolSummary{Total: roster, Failed: roster, Agents: nil}, fabricating an all-failed record. The real per-agent results (in memory at the ExecuteReview call site) are discarded, so a run where some agents produced valid findings before the WritePool I/O error is reported as a total failure with zero findings and empty agents list, discarding recoverable work and corrupting post-hoc analysis.|Pass the real summarize(results) tally (or the results slice) into the failure marker so Succeeded/Failed/Partial reflect what actually happened instead of hard-coding all-failed. Verify with a test where WritePool fails after a partial-success run and assert the marker preserves the succeeded count.|error-handling|30|code-review|claude|MEDIUM
LOW|internal/fanout/status.go:90|Torn-read invariant comment asserts the manifest fields are byte-identical across the finalizing rewrite, but ExecuteReview rewrites the manifest with mutated Partial and a fresh CompletedAt, so the file is NOT byte-identical. The claim survives only because the status path happens not to read those two fields — a fragile coupling the comment papers over; a future edit reading Partial/CompletedAt would silently violate the guarantee while the comment still claims safety.|Reword the comment to the actual invariant: the fields consumed here (roster, StartedAt, timeout_secs) are stable across the rewrite; Partial and CompletedAt change but are not read here. Verify by grep that no status-path code reads m.Partial/m.CompletedAt.|maintainability|15|code-review|claude|MEDIUM
LOW|internal/fanout/artifacts.go:112|writeFailureSummary swallows both the writeJSON and os.MkdirAll errors with no logging. When the best-effort marker fails AND the manifest has no timeout_secs (old manifests / TimeoutSecs<=0), stale never fires, so the review is stuck in_progress permanently and silently with zero operator signal — an undiagnosable hang.|Emit at least one stderr/log line on marker-write failure naming the review dir, so a permanently-in_progress review is traceable. Verify the log fires by stubbing writeJSON to fail in a test.|error-handling|30|code-review|claude|MEDIUM
LOW|internal/fanout/status.go:52|nowFunc is a package-level mutable var with no synchronization. The documented concurrent-read invariant and -race torn-read test call staleByDeadline -> nowFunc() from 8 goroutines; the withNow helper reassigns nowFunc from the test goroutine. Today no test overlaps a swap with live readers so -race passes, but the design is one test away from a data race on the package var.|Document that nowFunc may only be swapped before reader goroutines start and restored after they join, or guard it behind atomic.Value. Verify the concurrent test never overlaps a swap with live readers.|maintainability|15|code-review|claude|MEDIUM
LOW|internal/fanout/status.go:48|staleGraceSecs=60 is a hard-coded magic constant unanchored to the write cost it claims to absorb. atomicWriteFile does a temp Sync + dir Sync per file; for a large roster with slow fsyncs, finalization could exceed 60s and falsely report a still-finishing run as stale. The grace is a guess not tied to roster size or measured latency. intent_note: deferred per epic clarification Q2 — flat 60s adjudicated as sufficient|Document the bound (max roster x per-file fsync budget < 60s) with justification, or scale the grace with roster size. Verify by timing WritePool finalization for the largest supported roster and confirming it stays under the grace.|error-handling|30|code-review|claude|MEDIUM
LOW|internal/fanout/review.go:239|writeFailureSummary(poolDir, len(p.Slots)) uses len(p.Slots) as roster count, but WritePool derives Total from summarize(results) over the actual results slice. If results count differs from slot count, the failure marker Total/Failed disagree with what a successful WritePool would record — a second independent source of truth for the agent count instead of reusing results already in hand.|Pass results (or summarize(results)) to the failure path so the marker totals come from the same source as the success path. Verify by asserting marker Total equals len(results) in a forced-failure test.|error-handling|15|code-review|claude|MEDIUM
