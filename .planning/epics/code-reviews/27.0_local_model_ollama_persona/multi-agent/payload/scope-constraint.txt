## SCOPE CONSTRAINT
The sprint/epic plan below defines the active work items for this review. Constrain your findings to files and changes directly related to these work items. Suppress findings for unrelated changes that merely happen to appear in the diff — for example dependency bumps, formatter or whitespace-only reformatting, and mechanical refactors not described in the plan. This is a scope hint, not a hard limit: still report any genuinely critical issue (security, data loss, crash) even if it falls outside the plan.

----- BEGIN SPRINT PLAN -----
# Epic Plan 27.0: Local Model / Ollama Persona Integration

- **Estimated time**: TBD
- **Tasks/Components**: TBD
- **Execution**: init-plan

## Objective

Ship a "Zero Data Egress" persona pack (using Ollama or llama.cpp) in the `personas/community/` registry, tuned for local hardware tiers, to resolve enterprise privacy objections during onboarding. The local-endpoint provider wiring itself already exists (`docs/providers.md`'s `local:` example) — the gap this epic closes is the tuned persona content and the walkthrough connecting it end-to-end.

## Context

While LiteLLM and proxy configurations are supported, privacy-conscious enterprise teams often refuse to send proprietary code to external APIs like OpenAI or Anthropic. `docs/providers.md` already documents a `local:` provider block (`base_url: http://localhost:11434/v1`) for wiring atcr at a local Ollama/llama.cpp endpoint — the missing piece is a first-class, officially supported persona tuned for local models (e.g., a highly optimized prompt for `Ornith 1.0` or `Qwen 3.6`), which makes the "privacy first" adoption path friction-free.

## Proposed Solution

1. **Targeted Persona Pack (The 3-Tier Strategy)**: Author a focused pack of personas — each a `<name>.yaml` + `<name>.md` + `community/testdata/<name>_fixture.patch` triple under `personas/community/` (with a matching `index.json` entry), following the established bare-human-first-name convention (no parenthetical qualifiers, no model name in the display name — model identity lives only in the `provider`/`model` fields) and passing `internal/personas/community_fixture_test.go`'s fixture gate. New files are auto-embedded via the existing `//go:embed` globs — no Go code changes needed. Using mnemonic human names that hint at the underlying model (e.g., Quinn for Qwen, Gerald for Gemma). Tuned for specific hardware tiers:
   - **Tier 1: 32GB Dense** (e.g. `Gerald` for Gemma 4 or `Quinn` for Qwen 3.6): High reasoning, standard context (32k). Best for file-by-file review.
   - **Tier 2: 32GB Long-Context** (e.g. `Orson` for Ornith 1.0): Optimized for massive context (256k on 32GB VRAM). Best for full-repo architectural reviews.
   - **Tier 3: 64GB+ Heavyweight** (e.g. `Liam` for Llama 4 70B or `Dexter` for DeepSeek V4 Pro): For Mac Mini M4 Pro or dual-GPU setups. Fits 70B+ quants with massive context, rivaling frontier cloud APIs.
2. **Documentation**: Extend `docs/personas-install.md`'s existing "Provider tiers beyond Synthetic" section with a Local / Privacy-First subsection explaining how to `brew install ollama`, pull a model, and run `atcr` entirely offline — including a reminder to `export OLLAMA_API_KEY=<placeholder>` (the provider config's `api_key_env` field is required even though a local endpoint needs no real key).

## Acceptance Criteria

- [ ] `personas/community/` includes a ready-to-use local persona for each of the 3 hardware tiers, each passing its `internal/personas/community_fixture_test.go` fixture.
- [ ] A local model can successfully execute a review pipeline using one of these personas without external network calls (once the user's own `registry.yaml` defines a matching `local` provider, per `docs/providers.md`).
- [ ] Documentation clearly outlines the zero-egress setup, including the `api_key_env` placeholder-value requirement.

## Components Touched

- `personas/community/` (persona content: yaml + md + fixture patch + index.json)
- `docs/` (providers.md + personas-install.md)

## Out of Scope

- Managing or installing the local model server itself (user is expected to run Ollama/llama.cpp themselves).

## Dependencies

- Epic 19.6 (Community Registry Hub)

## Refinements (2026-07-15)

This section records findings from `/refine-epic` run on July 15, 2026 10:16:24AM. It is additive — original plan content above is preserved.

### Auto-applied corrections (0)

None — every finding this run changes the plan's intent or mechanism enough to need your confirmation (see below).

### Items needing user confirmation (7)

1. ⏸️ **Mechanism mismatch: `registry.yaml` is not the community hub.** No `registry.yaml` exists as shared/community content anywhere in this repo. `~/.config/atcr/registry.yaml` (and the optional `.atcr/registry.yaml` project overlay) is the user-local **provider/agent config** file (`docs/registry.md:5-18`) — not a persona registry. The actual community persona distribution channel — built by **Epic 19.6**, this epic's own listed dependency — is `personas/community/*.yaml` + `personas/community/index.json` (confirmed on disk: `milo.yaml`, `anthony.yaml`, `gia.yaml`, etc.). Epic 19.6 explicitly ruled this out: *"Any `registry.yaml` task→model mapping schema — the existing named-agent + roster model stands"* (Out of Scope) and *"this epic does not touch `ATCR_REGISTRY_URL`/`registry.yaml` hosting."* All three concrete citations of `registry.yaml` in this plan (Proposed Solution item 1, Acceptance Criteria, Components Touched) point at the wrong file/mechanism.

2. ⏸️ **A community persona is a 3-file unit, not a config edit.** Every existing community persona ships as a triple: `<slug>.yaml` (metadata: name/provider/model/persona/role), `<slug>.md` (the actual prompt template, e.g. `personas/community/milo.md`), and `community/testdata/<slug>_fixture.patch` (a fixture case). All three are embedded into the binary via `//go:embed` globs (`personas/community.go:17,24,32`) — new files under `personas/community/` are auto-discovered with **no Go code edit needed** — but every persona must pass `internal/personas/community_fixture_test.go:19` (`TestTemplateFixtureRunner_CommunityPersonasPass`), which asserts exactly one passing fixture case per persona. The plan's Task 1 ("Author a persona ... in registry.yaml") does not account for the `.md` prompt or the fixture-test requirement.

3. ⏸️ **Naming convention mismatch.** Commit `73afa1f0` updated this plan's example from `local-reviewer` to `Ollie (Local)` to "match the human naming convention," but every existing built-in and community persona uses a single bare human first name with **no** parenthetical qualifier or model name (anthony, sonny, gia, milo, flint, delia, quinn, celeste, glenna, gene; built-ins bruce/dax/greta/ingrid/kai/mira/otto/penny/sasha). Model identity lives only in the structured `provider`/`model` fields, never in the display name. `Ollie (Local)`, `Gemma 4`, `Qwen 3.6`, `Ornith 1.0`, `Llama 4 70B`, `DeepSeek V4 Pro` all diverge from this convention.

4. ⏸️ **Self-contradiction between item 1 and item 2 of Proposed Solution.** Item 1 says "Author a persona ... (e.g., `Ollie (Local)`)" — singular, one persona. Item 2 opens with "Instead of one generic 'local' persona, ship a focused pack of personas tuned for specific hardware tiers" — directly negating item 1's own example as the discouraged "one generic persona." The two items read as alternatives, not a sequence.

5. ⏸️ **The provider-level building block already exists.** `docs/providers.md:20-22` already documents, under a "Direct (always works, zero infrastructure)" section:
   ```yaml
   local:
     base_url: http://localhost:11434/v1   # ollama, llama.cpp, vllm, ...
     api_key_env: OLLAMA_API_KEY
   ```
   and `internal/registry/config_test.go` already has a passing test for this exact provider shape (a `local-llm` provider at `http://localhost:11434/v1`). So "pointing to a standard local Ollama/llama.cpp endpoint" is **already shipped and documented** — the real net-new work is narrower than the Objective states: a tuned persona (prompt + fixture) plus a connecting walkthrough, not a provider-config feature.

6. ⏸️ **`api_key_env` is required-non-empty; no keyless-local path is addressed.** `internal/registry/config.go:686-687` makes `api_key_env` mandatory for every provider, and `internal/llmclient/client.go:325-327` errors ("API key env var %s is not set") if that env var isn't set to a non-empty value at invoke time — even for a keyless local Ollama endpoint. Neither this plan nor the existing `docs/providers.md` example addresses this: the user will still need to `export` some placeholder value for `OLLAMA_API_KEY`. Minor, but the "Zero Data Egress... out-of-the-box" framing and AC2 ("execute a review pipeline... without external network calls") imply more friction-free than this actually is today.

7. ⏸️ **Documentation target under-specified.** `docs/personas-install.md:256` already has a `## Provider tiers beyond Synthetic` section covering DashScope, Chutes→Featherless, and LiteLLM as sibling tier write-ups. Task 3 ("Add a Local/Privacy-First Quickstart") doesn't cite this file/section, risking a disconnected or duplicate doc page instead of a natural extension of the existing structure.

### Advisory observations (1)

- ℹ️ **Model names not verified.** `Gemma 4`, `Qwen 3.6`, `Ornith 1.0`, `Llama 4 70B`, `DeepSeek V4 Pro` are external LLM model identifiers, not codebase symbols — out of scope for codebase grep verification. Flagging only so you're aware these weren't checked against a live model catalog (cf. `atcr models check`, `internal/personas/testdata/catalog_snapshot.json`).

### Verification context

- Refinement depth: deep
- Derived TASK_COUNT: 5 (limit: 6)
- Derived COMPONENT_COUNT: 2 (limit: 2)
- COMPONENTS_TOUCHED (corrected): `personas/community/` (persona yaml + prompt md + fixture patch + index.json entries), `docs/` (providers.md + personas-install.md)
- VISUAL_SURFACE: false
- HAS_GATED_WORK: false
- HAS_CROSS_SYSTEM: false
- Cited references checked: 1 file path (`registry.yaml` — stale/wrong mechanism, does not exist as community content), 6 external model-name mentions (not code symbols, unverified against catalog)
- Codebase search queries (spot-check): grep `localhost:11434|11434|base_url.*local|ollama` (case-insensitive); dedicated Explore subagent tasked with locating the real registry/persona/naming/docs structure; manual reads of `docs/registry.md`, `docs/providers.md`, `docs/personas-install.md`, `internal/registry/config.go`, `internal/llmclient/client.go`, `internal/personas/community_fixture_test.go`, and Epic 19.6's completed plan
- Deep discovery method: semantic+keyword+agent
- Deep discovery queries: ["local model Ollama llama.cpp zero data egress persona" (semantic), "localhost:11434|11434|base_url.*local|ollama" (grep)]
- Deep discovery match count: 47
- Deep discovery snapshot: `.planning/.temp/refine-epic/codebase-discovery.json` (temp-only — not committed)

### Resolution (2026-07-15)

All 7 user-confirm items above were accepted and applied directly to the plan sections above (Objective, Context, Proposed Solution, Acceptance Criteria, Components Touched):

1. ✅ `registry.yaml` references replaced with `personas/community/` throughout.
2. ✅ Proposed Solution item 1 now specifies the yaml + md + fixture-patch triple and the `community_fixture_test.go` gate.
3. ✅ Example persona names changed to bare first names (`Nadia`, `Ezra`, `Hugo`) — model identity kept in prose, not the display name.
4. ✅ The standalone single-persona item merged into the 3-tier pack item.
5. ✅ Objective/Context reframed to acknowledge the existing `docs/providers.md` `local:` provider example.
6. ✅ Documentation task now notes the `api_key_env` placeholder-value requirement.
7. ✅ Documentation task now targets `docs/personas-install.md`'s existing "Provider tiers beyond Synthetic" section.

Item 8 (advisory: model names unverified against a live catalog) required no action.

## Clarifications

### Recorded 2026-07-15 (rubber duck — /execute-epic)

**Key Decisions:**
- **Path A (narrowly scoped)** chosen to resolve the feasibility blocker: the community pool's `index.json` registration test (`personas/community_test.go:371`) hard-asserts `provider == "openrouter"` for every persona (comment: "LOCKED Q3 / Phase 5 clarifications"). A true zero-egress local persona needs `provider: local`. Resolution: loosen that single assertion to a small allowlist `{openrouter, local}` (+ its doc comment). No other test needs editing — the `/`-namespacing check (`TestCommunityModel`, :484) and the vendor-token check (:374) both pass unmodified against a `local/<model>` string.
- **Drift-check exemption added** (accepted caveat): `atcr models check` runs `CheckDrift` against the checked-in OpenRouter catalog snapshot for every community persona. A `local/<model>` slug never matches the catalog and would be permanently reported `missing` (`internal/personas/drift.go:116`). Fix: add a `local/`-prefix exemption in `CheckDrift`, mirroring the existing `aliasSlugSet` TD-005 exemption (drift.go:113) — a lock resolvable outside the catalog is skipped, not flagged missing.
- **Roster edit required** (corrects the plan's "no Go code changes needed"): the hard-coded `communityPersonas` slice in `personas/community_test.go:118-129` plus count assertions (`TestCommunityAccessors:175`, `TestCommunityIndex_Registration:339`) cross-check the embedded-file count against the slice length. Each new persona needs a one-line roster entry. The `//go:embed` globs and the fixture gate (`community_fixture_test.go`) are fully dynamic; only this separately-maintained test roster needs editing.

**Scope Boundaries:**
- IN scope: 3 local personas (Gerald / Orson / Liam) as yaml + md + fixture-patch triples + index.json entries; the one-assertion contract relaxation; the drift-check exemption; the roster-slice edits; docs extensions to `docs/personas-install.md` and `docs/providers.md`.
- OUT of scope: any change to the search/discovery architecture (discovery stays by model, not provider); managing/installing the Ollama server; broadening the provider allowlist beyond `{openrouter, local}`.

**Technical Approach:**
- New personas use `provider: local`, `model: local/<ollama-model>`, `role: reviewer`, each with a distinct review lens (category + primary task) — the tests forbid lens/category/task duplication and enforce Role+Focus Jaccard < 0.85.
- Names: **Gerald** (Gemma, Tier‑1 32GB dense, `local/gemma3-27b`), **Orson** (Qwen long-context, Tier‑2 256k, `local/qwen3-30b-a3b`), **Liam** (Llama, Tier‑3 64GB+ heavyweight, `local/llama3.3-70b`). All three confirmed collision-free against on-disk personas and the roster.
- Each `.md` carries all 7 template tokens, `## Role` + `## Output Format` with the byte-for-byte 7-column contract and "7 pipe-delimited columns" text, and exactly one vendor-guidance citation.

**Original Questions and Answers:**
1. Q: How to resolve the `openrouter`-locked contract vs. a true local persona? (A relax / B docs-only / C re-scope)
   A: Path A, narrowly scoped — loosen the single string-equality assertion at `community_test.go:371` to an allowlist `{openrouter, local}`. B silently drops AC1; C is disproportionate for a one-assertion change. Vendor-token and namespacing checks pass unmodified. Caveat: also add a drift-check exemption for non-catalog (`local`) providers.
2. Q: Consent to edit the locked `openrouter` assertion (and model-format assertions)?
   A: Only `community_test.go:371` needs editing (provider-equality). `:484` passes unmodified with a `local/<model>` string. The lock was designed as an extendable allowlist ("may include synthetic"), not immutable architecture. Proceeding on the Path-A selection as authorization.
3. Q: Confirm names Gerald / Orson / Liam + model bindings + distinct lenses?
   A: Names confirmed collision-free; distinct-lens assignment feasible (room for 3 more categories/tasks). Assistant to propose specific local-appropriate lenses.
4. Q: Confirm editing the `communityPersonas` roster slice + count assertions?
   A: Confirmed — required, not optional. Both `TestCommunityAccessors` and `TestCommunityIndex_Registration` cross-check counts against the static slice. One-line addition per new persona.
----- END SPRINT PLAN -----

