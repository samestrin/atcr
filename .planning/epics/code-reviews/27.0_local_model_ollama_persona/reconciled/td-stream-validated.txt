LOW|docs/personas-install.md:306|The Local/Privacy-First section shows "atcr personas search --model gemma  # or qwen / llama", implying model-search surfaces the local persona. orson reuses the vendor token qwen, already held by the cloud persona quinn (qwen/qwen3-coder-plus), so search --model qwen returns both — a user hunting the local qwen persona sees an openrouter result mixed in. The --provider local path disambiguates cleanly; the model hint is imprecise.|In docs/personas-install.md:302-307, steer users to search --provider local as the primary discovery path and note the qwen model token is shared with the cloud quinn persona. Verify against the two qwen rows in personas/community/index.json (quinn and orson).|docs|5|code-review|claude|MEDIUM
LOW|internal/personas/drift.go:90|isLocalProviderSlug exempts ANY slug beginning "local/" from the missing check via strings.HasPrefix — an open prefix, unlike the closed aliasSlugSet it cites as its model. A typo'd tag, a hypothetical mirror model named under local/, or a non-local persona whose model happens to start local/ would be silently and permanently exempted. CheckDrift only sees Model so it cannot confirm the lock is genuinely local. By-design (discovery keys on model) but the comment claims parity with a closed set it does not have.|Tighten the comment at internal/personas/drift.go:81-85 to state the exemption is an open-namespace convention, not a validated membership set. If stricter behavior is wanted, gate the exemption on a validated local/<non-empty> shape and add a false-negative case to drift_test.go.|correctness|15|code-review|claude|MEDIUM
LOW|personas/community/gerald.md:9|The gerald Role text asserts an unconditional guarantee: "You are the zero-egress gate ... the review itself sends nothing to a remote API." Zero-egress depends entirely on the user's registry.yaml base_url pointing at localhost, which the persona binding cannot verify. A user who points provider:local at a remote base_url gets a reviewer falsely reassuring them nothing left the host. The docs correctly hedge this ("once the provider points at localhost"); the prompt does not.|Soften personas/community/gerald.md:8-10 to condition the claim on a localhost endpoint, mirroring the honest wording in docs/providers.md. No test impact — the category word "secret" still renders in the prompt.|docs|15|code-review|claude|MEDIUM
LOW|personas/community_test.go:110|The communityPersona.Tier field comment reads "// flagship / fallback / open" but Epic 27.0 added three rows with Tier "local", so the enum comment is stale. Tier is also written in all 13 roster literals but never read by any test (only Slug/VendorToken/Category are asserted), making it dead metadata that asserts nothing about the new local personas.|Either extend the comment at personas/community_test.go:110 to "// flagship / fallback / open / local", or delete the unused Tier field entirely and drop it from all 13 rows. Verify with grep -rn '\.Tier' personas/ returning no field reads.|maintainability|15|code-review|claude|MEDIUM
LOW|personas/community_test.go:296|TestCommunityPersonas_DistinctCategories doc comment says "the 10 personas' finding-category words must all be distinct" but the roster grew to 13 in Epic 27.0 (gerald/orson/liam appended). The sibling C(13,2)=78 comment at line 272 was updated; this count was missed. Assertions use len(communityPersonas) dynamically so behavior is correct — comment only.|Edit the comment at personas/community_test.go:296 to say "13 personas" (or make it count-agnostic). Re-verify the ~0.168 observed-Jaccard figure still holds across 13 rows or drop the stale number. Verify by reading the comment against the 13-row roster at lines 118-132.|maintainability|5|code-review|claude|MEDIUM
LOW|personas/community_test.go:377|Two mechanisms added in Epic 27.0 are not coupled by any assertion: the provider allowlist relaxation keys on the provider field (openrouter / local) at TestCommunityIndex_Registration, while the drift missing-exemption keys on the model's local/ prefix (drift.go isLocalProviderSlug). Nothing asserts they agree, so a future local persona authored as provider:local + model:gemma3-27b (no local/ prefix) would pass every test yet be falsely reported missing by models check. The three shipped personas are correct; this is a latent authoring trap.|In TestCommunityIndex_Registration (personas/community_test.go), add: when e.Provider == "local" require strings.HasPrefix(e.Model, "local/") and vice-versa. Verify the three shipped personas still pass.|testing|15|code-review|claude|MEDIUM
