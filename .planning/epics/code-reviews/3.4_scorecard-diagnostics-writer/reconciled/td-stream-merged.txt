MEDIUM|internal/mcp/handlers.go:220|The MCP engine carries a structured *slog.Logger (e.log) used for every other diagnostic in handleReconcile (e.g. the require_verified warning at line ~225), but scorecard diagnostics are routed to raw os.Stderr, so MCP-path scorecard write-failures/malformed-record/orphan-verdict warnings emit as unstructured plaintext that bypasses the logger's level filtering, formatting, and sink redirection. NOTE: this was a DELIBERATE, documented epic decision (Clarifications Q2: supply os.Stderr at the call site; adapting e.log to an io.Writer was explicitly OUT of scope), so this is enhancement debt, not a regression. (intent_note: deferred per epic Clarifications Q2 — adapting e.log to io.Writer is out of scope)|If MCP observability is later desired, adapt e.log into an io.Writer shim (slog-backed at Warn level) and pass it as Diag instead of os.Stderr, so MCP-path scorecard diagnostics flow through the same structured pipeline as the rest of the handler. Defer unless/until structured MCP diagnostics are required.|error-handling|30|code-review|claude|MEDIUM
LOW|internal/scorecard/reconcile.go:20|EmitForReconcile's doc comment still asserts "the MCP handler passes a zero EmitOpts so the agentic path keeps emitting", but Epic 3.4 changed the MCP handler (handlers.go:220) to pass EmitOpts{Diag: os.Stderr}. The comment is now factually wrong and will mislead the next reader about what the MCP path actually sends.|Update the doc comment at internal/scorecard/reconcile.go:18-24 to state the MCP handler passes EmitOpts{Diag: os.Stderr} (explicit stderr, the documented default for the cmd-less path). Verify the comment matches handlers.go:220.|maintainability|5|code-review|claude|MEDIUM
MEDIUM|internal/scorecard/scorecard.go:118|Error from diagWriter is silently discarded|Log or return the error from fmt.Fprintf|error-handling|5|code-review|dax|MEDIUM
MEDIUM|internal/scorecard/scorecard.go:199|Error from diagWriter is silently discarded|Log or return the error from fmt.Fprintf|error-handling|5|code-review|dax|MEDIUM
MEDIUM|internal/scorecard/scorecard.go:235|Error from diagWriter is silently discarded|Log or return the error from fmt.Fprintf|error-handling|5|code-review|dax|MEDIUM
MEDIUM|internal/scorecard/scorecard.go:248|Error from diagWriter is silently discarded|Log or return the error from fmt.Fprintf|error-handling|5|code-review|dax|MEDIUM
MEDIUM|internal/scorecard/scorecard.go:276|Error from diagWriter is silently discarded|Log or return the error from fmt.Fprintf|error-handling|5|code-review|dax|MEDIUM
LOW|internal/scorecard/scorecard_test.go:291|Epic 3.4 added unit tests proving EmitOpts.Diag / ReadOpts.Writer routing at the scorecard layer, but there is no test asserting the wiring itself — that the MCP handler passes a non-default writer or that the three CLI entry points pass cmd.ErrOrStderr(). This plumbing can silently regress (a future refactor swaps cmd.ErrOrStderr() back to a default and no test fails).|Add a CLI-level test that drives a scorecard command against a deliberately-malformed store and asserts the read diagnostic reaches the command's ErrOrStderr buffer; once MCP diagnostics route through e.log, add a handler-level test asserting against a captured logger buffer.|testing|60|code-review|claude|MEDIUM
LOW|internal/scorecard/store.go:25|The new ReadOpts.Writer and EmitOpts.Diag fields accept a caller-controlled io.Writer but document no concurrency contract. os.Stderr writes are individually atomic, but an arbitrary caller-supplied writer (e.g. a shared bytes.Buffer) is not goroutine-safe; if two ReadAll/FindByRunID calls ever share one unsynchronized writer the diagnostic Fprintf calls would interleave. Latent (no current caller triggers it), but the field invites an unsafe caller with no warning.|Add one sentence to the ReadOpts.Writer and EmitOpts.Diag doc comments stating the writer must be safe for the caller's concurrency model (the package does not synchronize writes to it). Doc-only; no code change.|maintainability|15|code-review|claude|MEDIUM
HIGH|internal/scorecard/store.go:30|diagWriter misses typed-nil io.Writer (non-nil interface wrapping nil pointer)|Use reflect.ValueOf(w).IsNil() check or recover wrapper|correctness|15|code-review|greta|MEDIUM
LOW|internal/scorecard/store.go:114|Several diagnostics now routable to a caller-controlled writer embed absolute store paths (which can contain a username via ~/.config/atcr/...) and raw %v error strings (e.g. "skipping malformed record in %s: %v"; "verification read failed: %v"). For current callers this lands on stderr with no change in exposure, but the threading made these messages caller-routable without noting the path/username content. If Epic 10.0 ever routes this writer into a leaderboard submission or a remote-facing MCP response, those paths leak.|No action for current callers. When the writer is wired to any non-local sink, scrub absolute paths (use base names) and avoid echoing raw %v errors. Add a note to the diagnostics or the Writer/Diag doc that the sink is assumed local/trusted.|security|15|code-review|claude|MEDIUM
MEDIUM|internal/scorecard/store.go:114|Error from diagWriter is silently discarded|Log or return the error from fmt.Fprintf|error-handling|5|code-review|dax|MEDIUM
MEDIUM|internal/scorecard/store.go:145|Error from diagWriter is silently discarded|Log or return the error from fmt.Fprintf|error-handling|5|code-review|dax|MEDIUM
MEDIUM|internal/scorecard/store.go:155|Error from diagWriter is silently discarded|Log or return the error from fmt.Fprintf|error-handling|5|code-review|dax|MEDIUM
LOW|internal/scorecard/store.go:194|Redundant call to diagWriter|Remove diagWriter call and use opts.Writer directly since ReadRecords already resolves it|performance|2|code-review|otto|MEDIUM
LOW|internal/scorecard/store.go:208|Inconsistent writer resolution in FindByRunID|Use the already-resolved `w` variable instead of calling `diagWriter(opts.Writer)` again|maintainability|5|code-review|bob|MEDIUM
MEDIUM|internal/scorecard/store.go:211|Error from diagWriter is silently discarded|Log or return the error from fmt.Fprintf|error-handling|5|code-review|dax|MEDIUM
