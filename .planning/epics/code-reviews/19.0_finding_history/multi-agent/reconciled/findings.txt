# atcr-findings/v1
MEDIUM|internal/history/capture.go:44|RecordReview deduplicates findings by id within a run using first-occurrence-wins, but the pool findings.txt is a concatenation of per-reviewer rows with no guaranteed ordering; the "first" record's severity/category may not be the consensus severity|Document that first-occurrence-wins is intentional (the ledger stores run-time severity, not reconciled severity) or use the majority severity when multiple reviewers disagree|correctness|30|`seen[id] = true` skips subsequent occurrences; no severity-resolution logic|dax|MEDIUM
MEDIUM|internal/history/filter.go:25|ParseSince parses "2w" as 14 days using float64 multiplication (n * float64(per)), which can produce sub-nanosecond drift for large week counts due to IEEE 754 rounding|Use integer arithmetic (n * per) after parsing n as an integer, or accept the drift as negligible and document it|correctness|15|`d = time.Duration(n * float64(per))` at filter.go:35|dax|MEDIUM
MEDIUM|internal/history/writer.go:21|Append assumes O_APPEND atomicity for multi-KB batches|Use a file lock (flock) or a temporary-swap pattern to prevent interleaved JSONL lines during concurrent review runs|idiom|30|The whole batch is serialized to memory first, then written with a single O_APPEND write() call.|otto|MEDIUM
MEDIUM|internal/history/writer.go:32|Append serializes all records to memory then writes with a single O_APPEND call, claiming POSIX atomicity; POSIX guarantees atomicity only for writes up to PIPE_BUF (typically 4096 bytes) on pipes/FIFOs, not regular files|Document the non-atomicity assumption or add advisory flock around the append, mirroring the mkdir-flock pattern used for the TD README|correctness|45|`// On a local POSIX filesystem that append is atomic` comment at writer.go:32 is incorrect for regular files|dax|MEDIUM
LOW|cmd/atcr/history.go:43|runHistory resolves .atcr relative to cwd (`filepath.Join(".", ".atcr", ...)`) so running from a subdirectory silently reports no history; consistent with other commands but undocumented|Document the cwd requirement in the command help or resolve the repo root once for all commands|integration|15|[otto] histPath := filepath.Join(".", ".atcr", "findings-history.jsonl") / [dax] `histPath := filepath.Join(".", ".atcr", "findings-history.jsonl")` at history.go:44|dax,otto|HIGH
LOW|internal/history/capture_test.go:92|TestRecordReview_AppendsOneRecordPerPoolFinding asserts record count but does not verify that the second record's fields (Package, Severity, File, Category, ID) match the input|Add assertions for the second record's fields, mirroring the first-record assertions at lines 93-98|testing|10|Only recs[0] fields are asserted; recs[1] is only length-checked|dax|MEDIUM
LOW|internal/history/reader.go:28|Load skips malformed JSON lines silently with `continue`, so a ledger corrupted by a partial write silently drops records with no diagnostic; the caller (cmd/atcr/history.go) has no way to know data was lost|Log a warning or return a count of skipped lines alongside the records so the CLI can surface a notice|observability|20|`if err := json.Unmarshal(raw, &rec); err != nil { continue }` at reader.go:43|dax|MEDIUM
LOW|internal/history/render.go:35|normalizeSeverity is a duplicate of reconcile.NormalizeSeverity|Import and use reconcile.NormalizeSeverity to ensure consistent casing logic|consistency|5|func normalizeSeverity(s string) string { return strings.ToUpper(strings.TrimSpace(s)) }|otto|MEDIUM
LOW|internal/history/render.go:46|RenderTable normalizes empty severity to "UNKNOWN" but the canonical column set is fixed to CRITICAL/HIGH/MEDIUM/LOW; an "UNKNOWN" severity column is appended as an extra, but the normalizeSeverity call on line 46 is unreachable if the caller always passes a non-empty severity|Remove the dead normalization branch or add a test proving it is reachable with an empty severity record|testing|10|`if sev == "" { sev = "UNKNOWN" }` has no test coverage|dax|MEDIUM
