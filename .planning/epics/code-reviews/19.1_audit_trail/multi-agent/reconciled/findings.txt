# atcr-findings/v1
HIGH|cmd/atcr/audit_report.go:47|--pr 0 matches every no-PR run because omitempty serializes PR=0 records without a "pr" field, so the loaded records all carry PR=0 and the filter r.PR == pr (pr=0) selects all of them|Reject pr <= 0 after GetInt via a usageError; MarkFlagRequired only checks presence not value|edge_cases|10|filter `if r.PR == pr { forPR = append(forPR, r) }` with pr=0 returns every local (no-PR) record under a bogus "PR #0" report|mira|MEDIUM
HIGH|cmd/atcr/review.go:387|cross-directory write/read split: review writes via filepath.Join(req.Root,...) where req.Root="." (CWD-relative) while audit-report reads via repoRoot() walk-up — a review launched from a subdirectory of the git root writes records the report will never find|Resolve auditPath through repoRoot() in runReview/runResume, matching audit-report|integration|60|auditPath uses req.Root="."; audit_report.go:34 uses repoRoot(). On CI run from <root>/subdir, the two paths diverge and AC1/AC2 silently fail|mira|MEDIUM
MEDIUM|cmd/atcr/review.go:115|prNumberFromFlags() uses os.Getenv() directly|Pass the environment map or a getter to the function to make it testable without global state|structure|10|return prFromGitHubRef(os.Getenv("GITHUB_REF"))|otto|MEDIUM
MEDIUM|cmd/atcr/review.go:390|audit write failure is swallowed as Warn and review still exits 0, so a systematically broken compliance ledger (full disk, read-only fs, .atcr/ perm denied) leaves runs reporting success with only a log line that CI rarely surfaces|Mirror the warn on stderr from the CLI so the compliance-write failure is visible regardless of log-level capture|observability|15|log.FromContext(ctx).Warn("failed to append audit record", "error", aerr) then control falls through to the gate path and a successful exit|mira|MEDIUM
MEDIUM|internal/audit/reader.go:60|oversized-line recovery uses unbounded bufio.NewReader.ReadString('\n') which grows the buffer until a newline or EOF — a tampered or corrupted ledger with a multi-GB run-on line OOMs the audit-report command|Read fixed-size chunks and split on '\n' with a per-line cap (e.g. 4MiB) so any further oversized line is truncated + skipped with a stderr note|performance|20|for { line, rerr := r.ReadString('\n'); ... } allocates without limit on tampered input|mira|MEDIUM
MEDIUM|internal/audit/writer.go:46|Append does not fsync, so a crash or power loss between f.Write and Close loses the most recent record — undermining the "tamper-evident compliance ledger" framing in record.go and CHANGELOG|Add an explicit f.Sync() before Close||0||mira|MEDIUM
LOW|cmd/atcr/audit_pr_test.go:45|intToStr() is a manual implementation of strconv.Itoa|Use strconv.Itoa|idiom|2|manual byte-slice loop for integer conversion|otto|MEDIUM
LOW|cmd/atcr/audit_report.go:39|RunE ignores error from fmt.Fprint|Use a proper logger or handle the return error if the output stream is closed|style|2|_, _ = fmt.Fprint(cmd.OutOrStdout(), audit.RenderReport(forPR, pr, time.Now()))|otto|MEDIUM
LOW|internal/audit/capture.go:75|finding key uses null bytes as separators|Use a structured key or a joined string with a standard delimiter like a pipe|style|5|key := f.File + "\x00" + strconv.Itoa(f.Line) + "\x00" + f.Problem|otto|MEDIUM
LOW|internal/audit/render.go:118|RenderReport uses a map for counts without ensuring key existence for canonical columns|The logic is correct but relies on the zero-value of int; a more explicit check or default value would improve readability|style|5|n := counts[c]|otto|MEDIUM
