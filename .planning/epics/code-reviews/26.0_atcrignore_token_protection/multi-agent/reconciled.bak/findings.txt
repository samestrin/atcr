# atcr-findings/v1
HIGH|internal/fanout/resume.go:268|Resume rebuilds payloads using the caller's NoIgnore flag instead of the original run's value, violating payload consistency for pending agents|Persist NoIgnore in the review manifest and validate it matches the resume request before rebuilding payloads|correctness|20|payloads, rb, err := buildPayloads(ctx, cfg, req.Repo, req.Range.Base, req.Range.Head, req.NoIgnore)|brad|HIGH
HIGH|internal/payload/diff.go:236|(applyIgnore) Unbounded exclude pathspec list exceeds OS execve ARG_MAX limits and crashes git diff|Filter ignored files in Go after retrieving the full diff or batch excludes into a temp file passed via stdin|reliability|45|exclude = append(exclude, ":(exclude,literal)"+f.path) passed directly to git args at line 395|brad|HIGH
HIGH|internal/payload/ignore.go:84|(loadAtcrignore) Reading .atcrignore with os.ReadFile allows a malicious multi-gigabyte file to OOM the process|Use bufio.Scanner with a strict total size limit or io.LimitReader before parsing|correctness|15|[brad] data, err := os.ReadFile(path) / [greta] lines = append(lines, ln) preserves carriage returns from CRLF endings so gitignore patterns never match forward-slash paths (disagreement: MEDIUM vs HIGH)|brad,greta|HIGH
MEDIUM|internal/payload/diff.go:208|(changedFilesMemo) Lazy initialization of ignore matcher lacks synchronization, causing data races if gitRunner is ever shared or called concurrently|Use `fmt.Sprintf` or a constant prefix for clarity, though string concatenation is performant here|concurrency|10|[brad] if !g.ignoreReady { g.ignore = newIgnoreMatcher(g.dir, g.log()); g.ignoreReady = true } / [otto] line 209 (disagreement: LOW vs MEDIUM)|brad,otto|HIGH
