# atcr-findings/v1
HIGH|internal/payload/ignore.go:85|loadAtcrignore retains trailing \r on Windows files causing pattern mismatches|Append strings.TrimSpace(ln) instead of raw ln|correctness|10|lines = append(lines, ln) preserves carriage returns from CRLF endings so gitignore patterns never match forward-slash paths|greta
HIGH|internal/payload/diff.go:236|Unbounded exclude pathspec list exceeds OS execve ARG_MAX limits and crashes git diff|Filter ignored files in Go after retrieving the full diff or batch excludes into a temp file passed via stdin|reliability|45|exclude = append(exclude, ":(exclude,literal)"+f.path) passed directly to git args at line 395|brad
HIGH|internal/fanout/resume.go:268|Resume rebuilds payloads using the caller's NoIgnore flag instead of the original run's value, violating payload consistency for pending agents|Persist NoIgnore in the review manifest and validate it matches the resume request before rebuilding payloads|correctness|20|payloads, rb, err := buildPayloads(ctx, cfg, req.Repo, req.Range.Base, req.Range.Head, req.NoIgnore)|brad
MEDIUM|internal/payload/diff.go:208|Lazy initialization of ignore matcher lacks synchronization, causing data races if gitRunner is ever shared or called concurrently|Replace the boolean flag with sync.Once to guard the one-time load|concurrency|10|if !g.ignoreReady { g.ignore = newIgnoreMatcher(g.dir, g.log()); g.ignoreReady = true }|brad
MEDIUM|internal/payload/ignore.go:84|Reading .atcrignore with os.ReadFile allows a malicious multi-gigabyte file to OOM the process|Use bufio.Scanner with a strict total size limit or io.LimitReader before parsing|security|15|data, err := os.ReadFile(path)|brad
LOW|internal/payload/ignore.go:67|`loadAtcrignore` uses `strings.Split(string(data), "\n")`|Use `bufio.Scanner` or `bytes.Split` to avoid converting the entire file to a string before splitting|style|5|line 67|otto
LOW|internal/payload/diff.go:209|`exclude = append(exclude, ":(exclude,literal)"+f.path)`|Use `fmt.Sprintf` or a constant prefix for clarity, though string concatenation is performant here|style|2|line 209|otto
