HIGH|internal/payload/ignore.go:85|loadAtcrignore retains trailing \r on Windows files causing pattern mismatches|Append strings.TrimSpace(ln) instead of raw ln|correctness|10|lines = append(lines, ln) preserves carriage returns from CRLF endings so gitignore patterns never match forward-slash paths
HIGH|internal/payload/builder.go:130|ChangedFileCount applies ignore filter unconditionally even when --no-ignore is passed|Thread noIgnore into ChangedFileCount or reuse the RangeBuilder's filtered state|correctness|20|g.changedFilesMemo(base, head) always filters because the fresh gitRunner defaults noIgnore=false, breaking the plan's requirement that count and payload agree