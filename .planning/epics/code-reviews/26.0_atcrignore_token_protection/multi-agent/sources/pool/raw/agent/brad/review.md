HIGH|internal/payload/diff.go:236|Unbounded exclude pathspec list exceeds OS execve ARG_MAX limits and crashes git diff|Filter ignored files in Go after retrieving the full diff or batch excludes into a temp file passed via stdin|reliability|45|exclude = append(exclude, ":(exclude,literal)"+f.path) passed directly to git args at line 395
HIGH|internal/fanout/resume.go:268|Resume rebuilds payloads using the caller's NoIgnore flag instead of the original run's value, violating payload consistency for pending agents|Persist NoIgnore in the review manifest and validate it matches the resume request before rebuilding payloads|correctness|20|payloads, rb, err := buildPayloads(ctx, cfg, req.Repo, req.Range.Base, req.Range.Head, req.NoIgnore)
MEDIUM|internal/payload/diff.go:208|Lazy initialization of ignore matcher lacks synchronization, causing data races if gitRunner is ever shared or called concurrently|Replace the boolean flag with sync.Once to guard the one-time load|concurrency|10|if !g.ignoreReady { g.ignore = newIgnoreMatcher(g.dir, g.log()); g.ignoreReady = true }
MEDIUM|internal/payload/ignore.go:84|Reading .atcrignore with os.ReadFile allows a malicious multi-gigabyte file to OOM the process|Use bufio.Scanner with a strict total size limit or io.LimitReader before parsing|security|15|data, err := os.ReadFile(path)