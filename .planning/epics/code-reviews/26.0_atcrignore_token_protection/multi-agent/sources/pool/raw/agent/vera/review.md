HIGH|internal/fanout/review.go:70|Adding NoIgnore field to exported ReviewRequest struct changes its layout, breaking callers that construct the struct using positional composite literals; such callers will fail to compile and lose the intended field ordering|Document that ReviewRequest is a public type and recommend using named field literals; consider adding a deprecation notice or providing a constructor that preserves backward compatibility|compatibility|20|ReviewRequest struct now includes NoIgnore bool at line 70, altering its field order and breaking positional composite literal usage.