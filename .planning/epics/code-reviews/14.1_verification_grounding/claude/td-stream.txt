# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/14.1_verification_grounding.md
# TARGET_TYPE: epic
# CREATED: 2026-06-30 18:28:12
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---

MEDIUM|internal/fanout/grounding.go:110|The evidence-fallback floor evidenceMinMatch=12 is too low and the code comment falsely claims boilerplate like "if err != nil {" (15 chars) cannot ground a finding; a hallucinated finding on a REAL changed file with a wrong out-of-range line plus generic EVIDENCE passes via the bidirectional strings.Contains, and because len() counts bytes not runes a ~4-rune multibyte quote also clears 12. The line-level precision of the gate is defeated by the most common line in Go.|Raise the floor (~24+) and require a distinctive match (evidence approximates a full changed line, not any common substring); count runes via utf8.RuneCountInString; correct the misleading comment; add a test that "if err != nil {" on an out-of-range line is DROPPED.|correctness|60|internal/fanout/grounding.go:100-124|claude
MEDIUM|internal/payload/scope.go:43|scopeFiles (files mode) uses soft "focus on the changed regions / you may note a pre-existing issue" language and never states that the grounding gate HARD-DROPS out-of-range findings. Files mode deliberately shows whole-file head content, so a reviewer is far more likely to cite a genuine issue on an unchanged-but-visible line; without the out-of-scope tag that finding is silently discarded — the real-finding loss the epic set out to prevent.|Bring scopeFiles to parity with scopeChangedOnly: state explicitly that any finding whose FILE:LINE is outside the changed lines is discarded unless tagged CATEGORY out-of-scope.|correctness|15|internal/payload/scope.go:43-47|claude
MEDIUM|internal/fanout/artifacts.go:186|The grounding drop count is written to stderr only; the comment claims parity with enforceConstraints, but enforceConstraints drops ARE persisted to status.json (DroppedByMinSeverity/TruncatedByMaxFindings) whereas grounding drops never reach status.json or summary.json. For an anti-hallucination gate that permanently removes findings there is no audit trail, and MCP background reviews frequently do not capture stderr.|Add a DroppedByGrounding field to AgentStatus, thread the ungrounded count through findingsResult/statusFor, and aggregate into PoolSummary; then fix or remove the misleading "matching the enforceConstraints precedent" comment.|maintainability|60|internal/fanout/artifacts.go:178-190|claude
MEDIUM|internal/fanout/grounding.go:29|groundFindings fail-opens only when changed==nil; a non-nil but EMPTY ChangedLines map (a BuildChangedLines derivation that diverges from the payload builder, or an unexpected empty diff) is treated as an active gate, so every finding misses the map and ALL findings are dropped run-wide, visible only per-agent on stderr. computeGroundingData returns nil only on git-error/range-less paths, not on empty-map success.|Treat len(changed)==0 the same as nil (fail open) in groundFindings, or return nil from computeGroundingData when BuildChangedLines yields an empty map; warn when the map is empty but payloads were non-empty (derivation mismatch).|error-handling|30|internal/fanout/grounding.go:28-42|claude
LOW|internal/fanout/artifacts.go:60|WritePool/findingsFor/writeResumedAgents accept grounding via a variadic changed ...payload.ChangedLines, so a zero-arg call silently disables the anti-hallucination gate with no compiler help; existing test callers already use the 2-arg form, proving the footgun is live.|Make changed a required nil-able parameter (a nil map still fails open, but explicitly) and update the handful of test call sites to pass nil.|maintainability|30|internal/fanout/artifacts.go:60|claude
LOW|internal/fanout/grounding.go:84|normalizeFindingPath unconditionally strips a leading a/ or b/, so in a repo with a real top-level directory named a or b a correctly-cited finding (a/main.go) is normalized to main.go, misses the changed map, and is dropped as a fabricated file.|Try an exact changed-map hit before stripping; only strip a//b/ when the unstripped key is absent AND the stripped key is present.|correctness|30|internal/fanout/grounding.go:81-88|claude
LOW|internal/fanout/grounding.go:114|evidenceMatches runs strings.Contains of a model-controlled evidence string against every changed line with no cap on len(evidence) or len(changed), and collapseSpaces/ToLower allocate 2-3x up front; a degenerate model emitting multi-MB EVIDENCE creates CPU/allocation amplification per finding.|Cap evidence length (truncate to a few KB) before matching and short-circuit.|performance|15|internal/fanout/grounding.go:109-124|claude
LOW|internal/fanout/grounding.go:15|groundingTolerance=3 and reconcile/cluster.go lineProximity=3 must stay equal but are coupled only by a comment; tuning the reconciler clustering distance silently desyncs the gate, with no compile-time or test coupling.|Export one shared constant referenced by both packages, or add a test asserting groundingTolerance == reclib lineProximity.|maintainability|15|internal/fanout/grounding.go:11-20|claude
LOW|internal/payload/grounding.go:67|parseFileChange flips inHunk on HasPrefix(line,"@@") while parseHeadRanges uses the strict ^@@ -d+ ... @@ regex; a malformed or combined (@@@) hunk header harvests +/- text into ChangedText while contributing zero Ranges, producing text-without-ranges that can only ground via the weak evidence path. The two parsers of the same chunk disagree on what a hunk is.|Reuse hunkHeaderRe.MatchString in parseFileChange so range parsing and text harvesting share one hunk definition.|correctness|15|internal/payload/grounding.go:60-82|claude
LOW|internal/fanout/review.go:401|A single transient git failure in computeGroundingData disables the anti-hallucination gate for the ENTIRE run (all agents, all findings) with only a Warn log; there is no signal in summary.json/manifest that grounding was skipped, so one git hiccup lets every hallucination through unnoticed.|Record a grounding_enabled (or grounding_disabled_reason) flag in summary.json/manifest; keep the fail-open behavior but make it auditable.|error-handling|30|internal/fanout/review.go:396-406|claude
LOW|internal/fanout/grounding.go:60|isGrounded keys the changed map by head-side path, but a diff-mode reviewer citing a RENAMED file's OLD (base-side) path shown in the diff header misses the map and is dropped as a fabricated file; normalizeFindingPath does not map old->new.|Include base-side (old) rename paths as aliases in the changed map, or normalize renamed-file citations to their head path before lookup; at minimum document that old-path citations are dropped.|correctness|60|internal/fanout/grounding.go:56-88|claude
LOW|internal/payload/scope.go:39|On the diff-ingestion path (PrepareReviewFromDiff) req.Range is empty so grounding is disabled, yet the forced-diff-mode scopeChangedOnly still asserts out-of-range findings "will be discarded before it reaches the report" — a false promise to ingestion reviewers, nothing is discarded there.|Make the discard clause conditional on grounding being active, or document in scope.go that it only holds for live-range (git-range) reviews.|correctness|15|internal/payload/scope.go:39-43|claude
