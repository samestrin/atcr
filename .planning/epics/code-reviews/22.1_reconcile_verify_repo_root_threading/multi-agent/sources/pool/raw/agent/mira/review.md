

MEDIUM|cmd/atcr/reconcile.go:89-97|--repo path not validated: strings.TrimSpace normalizes empty "" to "." but the resulting repoRoot is passed directly to reconcile.RunReconcile without a stat check. A mistyped --repo /no/such/path silently degrades to flagging every finding "file not found" instead of failing fast. TD-022 documents this as deferred-to-epic-independent; the empty-string normalization comment (§2) demonstrates awareness of silent-degradation risk while leaving the nonexistent-path case unaddressed. (Note: TD-022 explicitly defers this to a future epic; flagging per findings-only output requirement and the explicit doc-vs-code gap.)|Add os.Stat(repoRoot) and return usageError when it does not exist or is not a directory, consistent with the resolveReviewDir pattern|operational-hazard|15|repoRoot, _ := cmd.Flags().GetString("repo"); if strings.TrimSpace(repoRoot) == "" { repoRoot = "." }; res, err := reconcile.RunReconcile(..., Root: repoRoot)
MEDIUM|cmd/atcr/verify.go:89-97|--repo path not validated: same gap as reconcile.go. The empty-string normalization (§3) handles "" explicitly but passes repoRoot directly to verify.Verify and absRoot to the redactor without any existence check. A non-existent --repo would be silently absorbed. TD-022 applies identically.|Add os.Stat(repoRoot) before filepath.Abs so a nonexistent --repo fails with usageError (exit 2), matching the deferred TD-022 scope|operational-hazard|15|repoRoot, _ := cmd.Flags().GetString("repo"); if strings.TrimSpace(repoRoot) == "" { repoRoot = "." }; absRoot, _ := filepath.Abs(repoRoot)
LOW|cmd/atcr/verify.go:101|absRoot passed to NewRedactor is computed from repoRoot (possibly relative), while internal/redactor likely needs the absolute path for file I/O. If --repo is passed as a relative path, absRoot=filepath.Abs(repoRoot) resolves it relative to CWD; this works but is inconsistent with the original pattern (Abs(".") always resolves to the CWD's absolute path regardless of CWD). In practice identical for valid relative paths, but worth noting if redactor ever adds directory-existence guards.|Document the absRoot semantics or clarify that --repo must be absolute for the redactor|operational-hazard|5|absRoot, _ := filepath.Abs(repoRoot); Redactor: log.NewRedactor(absRoot, ...)