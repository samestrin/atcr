# atcr-findings/v1
MEDIUM|cmd/atcr/reconcile.go:96|runReconcile reads --repo and normalizes empty/whitespace to "." but does not call filepath.Abs before passing repoRoot to reconcile.RunReconcile — unlike verify.go:87 which explicitly computes absRoot via filepath.Abs(repoRoot). A relative --repo path (e.g., --repo ../other-repo) is passed as-is, creating inconsistent behavior between the two entry points that share the same Root contract. The pre-22.1 code hardcoded "." without Abs in both places, so this is not a new crash, but the verify side now diverges.|Call filepath.Abs(repoRoot) before the RunReconcile call, consistent with verify.go's pattern; add a test that passes a relative --repo path and asserts path validation still works|correctness|30|repoRoot, _ := cmd.Flags().GetString("repo") ... Root: repoRoot vs verify.go absRoot, _ := filepath.Abs(repoRoot)|bruce
LOW|cmd/atcr/verify.go:101|absRoot passed to NewRedactor is computed from repoRoot (possibly relative), while internal/redactor likely needs the absolute path for file I/O. If --repo is passed as a relative path, absRoot=filepath.Abs(repoRoot) resolves it relative to CWD; this works but is inconsistent with the original pattern (Abs(".") always resolves to the CWD's absolute path regardless of CWD). In practice identical for valid relative paths, but worth noting if redactor ever adds directory-existence guards.|Document the absRoot semantics or clarify that --repo must be absolute for the redactor|operational-hazard|5|absRoot, _ := filepath.Abs(repoRoot); Redactor: log.NewRedactor(absRoot, ...)|mira
MEDIUM|cmd/atcr/reconcile_test.go:601|TestReconcileCmd_RepoFlagValidatesAgainstOtherRepo does not cover an invalid/nonexistent --repo path, leaving the silent-degradation path untested.|Add a test case with --repo pointing to a nonexistent directory and assert that the command exits with a usage error or that path warnings are still produced, to ensure the error is surfaced.|testing|15|The test only covers a valid other repo and the default/empty normalization; no test for a path that does not exist.|dax
MEDIUM|cmd/atcr/verify_test.go:208|TestVerifyCmd_RepoFlagThreadsReviewedRoot does not verify that the --repo flag actually changes the repo root used by verify.Verify; the test passes even if the flag is ignored.|Add a test that verifies the repoRoot is used for path validation or snapshot building, e.g., by providing a file in the other repo and asserting no path warning, or by injecting a spy on the snapshot builder.|testing|20|The test uses an empty otherRepo and expects unverifiable verdict because no skeptic; the flag's functional effect is not asserted.|dax
LOW|cmd/atcr/reconcile.go:96|The --repo flag value is not validated for existence or directory type; a mistyped path causes silent failure (all findings flagged file-not-found) instead of a clear error.|Stat the path and return a usageError if it does not exist or is not a directory.|error_paths|15|The code only normalizes empty string; no existence check.|dax
LOW|cmd/atcr/reconcile.go:65|repoRoot normalization logic is repetitive|Move string trimming and empty-check to a helper or use a cobra default value that handles empty strings|style|5|`if strings.TrimSpace(repoRoot) == "" { repoRoot = "." }`|otto
LOW|cmd/atcr/verify.go:77|repoRoot normalization logic is repetitive|Move string trimming and empty-check to a helper or use a cobra default value that handles empty strings|style|5|`if strings.TrimSpace(repoRoot) == "" { repoRoot = "." }`|otto
