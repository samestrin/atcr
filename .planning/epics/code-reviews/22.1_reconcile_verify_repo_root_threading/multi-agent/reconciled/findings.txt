# atcr-findings/v1
MEDIUM|cmd/atcr/reconcile.go:65|(runReconcile) runReconcile reads --repo and normalizes empty/whitespace to "." but does not call filepath.Abs before passing repoRoot to reconcile.RunReconcile — unlike verify.go:87 which explicitly computes absRoot via filepath.Abs(repoRoot). A relative --repo path (e.g., --repo ../other-repo) is passed as-is, creating inconsistent behavior between the two entry points that share the same Root contract. The pre-22.1 code hardcoded "." without Abs in both places, so this is not a new crash, but the verify side now diverges.|Call filepath.Abs(repoRoot) before the RunReconcile call, consistent with verify.go's pattern; add a test that passes a relative --repo path and asserts path validation still works|correctness|30|[otto] `if strings.TrimSpace(repoRoot) == "" { repoRoot = "." }` / [bruce] repoRoot, _ := cmd.Flags().GetString("repo") ... Root: repoRoot vs verify.go absRoot, _ := filepath.Abs(repoRoot) / [dax] The code only normalizes empty string; no existence check. (disagreement: LOW vs MEDIUM)|bruce,dax,otto|HIGH
LOW|cmd/atcr/verify.go:77|(runVerify) absRoot passed to NewRedactor is computed from repoRoot (possibly relative), while internal/redactor likely needs the absolute path for file I/O. If --repo is passed as a relative path, absRoot=filepath.Abs(repoRoot) resolves it relative to CWD; this works but is inconsistent with the original pattern (Abs(".") always resolves to the CWD's absolute path regardless of CWD). In practice identical for valid relative paths, but worth noting if redactor ever adds directory-existence guards.|Move string trimming and empty-check to a helper or use a cobra default value that handles empty strings|operational-hazard|5|[otto] `if strings.TrimSpace(repoRoot) == "" { repoRoot = "." }` / [mira] absRoot, _ := filepath.Abs(repoRoot); Redactor: log.NewRedactor(absRoot, ...)|mira,otto|HIGH
