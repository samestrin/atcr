# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/7.2_radar-renderer-consolidation.md
# TARGET_TYPE: epic
# CREATED: 2026-06-22 15:53:07
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
MEDIUM|internal/reconcile/disagree.go:442|The epic's stated parity oracle is "existing golden tests", but no byte-exact golden fixture exists for the rendered radar. Every radar test uses assert.Contains/ordering (TestWriteRadarSection_EmitsHeaderThenItemsVerbatim, TestRenderMarkdown_DisagreementsRadar at disagree_test.go:288), so a future edit to spacing, the "· score" separator, or a trailing newline in the now-single WriteRadarItems would pass CI silently. The byte-output-preserving claim is asserted, not proven by the suite.|Add one golden test in internal/reconcile/disagree_test.go that pins the FULL expected string of WriteRadarItems for the all-fields markerItem() (built at line 603) via assert.Equal on the exact rendered block. Verify by mutating a single space in the Fprintf at disagree.go:444 and confirming the test fails.|testing|30|internal/reconcile/disagree_test.go:619-645|claude
MEDIUM|internal/reconcile/disagree.go:456|WriteRadarItems and WriteRadarSection dereference the injected renderText RadarTextRenderer at lines 456/459/468 with no nil guard, and both functions are exported. The pre-refactor code hardcoded esc/escTrunc, so a nil renderer was structurally impossible; the consolidation converts that impossible state into a latent nil-call panic for any current or future caller passing nil. An item with no Problem/Detail/Positions skips the call, masking the bug until a populated item appears.|In WriteRadarItems guard at entry: if renderText == nil { renderText = esc } (sane verbatim default), or document the precondition explicitly. Verify with WriteRadarItems(&b, []DisagreementItem{markerItem()}, "### ", nil) asserting no panic.|error-handling|15|internal/reconcile/disagree.go:442-472|claude
LOW|internal/reconcile/emit.go:469|The consolidation collapsed the duplicated radar renderer but left two byte-identical esc functions (and identical newlineFlattener Replacers) in reconcile/emit.go and report/render.go. The radar renderer was unified; the escaper it relies on for parity was not. If one esc drifts (e.g. someone escapes "[" in only one package) the parity this epic just established silently breaks and no test catches it (see the golden-test finding). Pre-existing duplication the epic surfaced but left in place.|Have report.esc delegate to an exported reconcile.Esc (report already imports reconcile), or document why the duplication is intentional. Verify the report display escaping tests still pass.|maintainability|30|internal/report/render.go:417-422|claude
LOW|internal/reconcile/disagree.go:476|formatScore uses if s == float64(int64(s)) with no guard for NaN/+-Inf; int64(NaN) is implementation-defined and the equality is false, so a NaN score would render as "NaN" into the report via FormatFloat. Scores are never NaN today (no division in the scoring path), so this is theoretical and pre-existing, copied verbatim into the now-single source of truth. Recorded as a known non-issue, not an action item.|Only if the scoring path ever divides: add if math.IsNaN(s) || math.IsInf(s,0) handling in formatScore. No fix or verification needed today.|correctness|5|internal/reconcile/disagree.go:476-481|claude

