We need to review the diff and produce findings about test coverage and error paths. The payload is a diff for the SARIF output integration plan documentation. No actual code changes (Go files) are shown. The diff is purely planning documents: README.md, metadata.md, plan files, acceptance criteria, documentation, sprint-knowledge.yaml. There are no changed Go source files. So the scope is only these markdown/yaml files. The task is to review test coverage and error paths. But these are documentation files, not test code. The instructions say "Review only the changed regions. The payload shows you the change in context, but a finding whose FILE:LINE falls outside the changed lines will be discarded... If you must flag a genuine pre-existing issue in unchanged code, give it CATEGORY out-of-scope so the reconciler annotates rather than discards it. Stay on the diff." The diff is only addition of new files. There is no test code. So there's no test coverage to review. The findings should be about missing tests? But the files are not test files. The sprint plan is about SARIF output integration. The actual implementation (Go code) is not in the diff. So there's nothing to review for test coverage. The task says "Review what the tests do NOT cover: untested error paths, asserts that prove nothing, fixtures that hide real behavior." But there are no tests in the diff. So we should emit nothing. However, maybe we can flag that there are no tests at all for the SARIF implementation, but that's not in the diff. The scope constraint says "Constrain your findings to files and changes directly related to these work items. Suppress findings for unrelated changes ... still report any genuinely critical issue (security, data loss, crash) even if it falls outside the plan." The diff only contains planning documents. No code changes. So no test coverage issues. The only potential finding is that there are no tests for the SARIF code, but that's not in the diff. So we should output nothing.