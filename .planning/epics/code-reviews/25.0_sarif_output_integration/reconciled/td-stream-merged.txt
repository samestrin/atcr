LOW|internal/mcp/tools.go:234|The atcr_report format enum is hand-written as a literal slice of FormatMarkdown/FormatJSON/FormatChecklist/FormatSarif at tools.go:234 while descReport (line 216-218) derives its advertised list from report.Formats(). Adding a 5th format to Formats()/ValidFormat and forgetting line 234 makes the MCP schema reject a format its own auto-derived description advertises. The comment framing Formats() as the single source of truth overpromises for the enforcing enum.|Add an exported report.FormatList() []string and build both Formats() and the MCP p.Enum from it so the schema enum and the description cannot drift; or at minimum reword the comment so the hand-maintained enum is explicit.|maintainability|30|code-review|claude|MEDIUM
LOW|internal/report/sarif.go:105|renderSarif sets ruleId=f.Category and sarifRules sets rule id=f.Category; an empty Category (a tested case, TestSarif_RulesEmptyCategory) yields ruleId '' referencing a rule whose id is also ''. The local schema-conformance test passes (the SARIF schema has no minLength) but GitHub Code Scanning may reject a blank reportingDescriptor.id, and an empty ruleId is a weak identifier for dedup/suppression.|Map empty Category to a sentinel id (e.g. uncategorized) in both renderSarif and sarifRules so ruleId always references a real non-empty rule; update TestSarif_RulesEmptyCategory to assert the sentinel.|correctness|30|code-review|claude|MEDIUM
LOW|internal/report/sarif.go:190|sarifLevel writes its unrecognized-severity diagnostic to the package-global sarifDiag (os.Stderr). On the MCP path (atcr_report over the server) that stderr belongs to the server process, not the client, so the corruption signal the diagnostic exists to surface is invisible to the MCP caller, and a batch of corrupt findings emits one stderr line each.|Route the corruption signal through a returned/accumulated warning that ReportResult carries back to the MCP client, or at least de-duplicate so each distinct bad token is reported once per render.|error-handling|60|code-review|claude|MEDIUM
LOW|internal/report/sarif.go:199|sarifDiag is a mutable package-level io.Writer read by sarifLevel during rendering and reassigned by tests (sarif_test.go). No production race exists today (prod never reassigns it and renderSarif is not called concurrently), but adding t.Parallel() to any test in this package, or ever invoking renderSarif concurrently, creates a genuine data race on the interface value.|Thread the diagnostic sink through a parameter/struct instead of a package global, or document that renderSarif is not concurrency-safe while the sink is swapped. Verify with go test -race after adding a parallel render test.|maintainability|30|code-review|claude|MEDIUM
MEDIUM|internal/report/sarif.go:218|sarifLocation sets artifactLocation.uri = f.File verbatim with no guard against empty, absolute, or ..-traversal paths. GitHub Code Scanning rejects the entire SARIF upload when any artifactLocation.uri is empty or not repo-root-relative, so one malformed finding (hand-edited findings.json, or a reviewer emitting /etc/passwd or an empty file) drops the whole report. AC 03-02 mandates uri=f.File unmodified, so the guard belongs at export time or in upstream path normalization, not by changing the anchoring mapping.|Treat a blank f.File as a non-empty placeholder (e.g. UNKNOWN) mirroring sarifMessageText's fallback, and reject or clean absolute/traversal URIs so the emitted uri is always a non-empty repo-relative path. Add table cases for File=='', '/abs', and '../x' asserting a safe uri.|correctness|60|code-review|claude|MEDIUM
