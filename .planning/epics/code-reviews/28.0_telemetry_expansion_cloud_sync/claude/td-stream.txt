# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/active/28.0_telemetry_expansion_cloud_sync.md
# TARGET_TYPE: epic
# CREATED: 2026-07-16 16:21:57
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
HIGH|internal/registry/telemetry_setting.go:153|withConfigLock stale-lock reclamation is not atomic: a waiter that sees a stale ownerFile calls os.RemoveAll(lockDir) unconditionally without re-verifying staleness. If a second process reclaims first (RemoveAll then Mkdir then writes a fresh owner and enters fn), the first process's pending RemoveAll deletes the freshly-acquired valid lock and both run fn() concurrently, defeating serialization and causing a last-writer-wins lost update on config.yaml.|Reclaim via compare-and-swap: rename the stale lockDir to a unique name and only proceed if the rename succeeds, or re-read and re-confirm the epoch immediately before removal. Never issue an unconditional RemoveAll on a dir another racer may have just legitimately acquired.|correctness|120|internal/registry/telemetry_setting.go:153-160|claude
MEDIUM|internal/scorecard/cloudsync.go:36|cloudHTTPClient is &http.Client{CheckRedirect: noRedirect} with a nil Transport, so it falls back to the shared http.DefaultTransport; the comment's claim that cloud-sync's connection pool and Transport are isolated from the process is false (only redirect policy is isolated). telemetry.Client shares the same nil-Transport while internal/llmclient actually sets a Transport, so the cited matching rationale is inconsistent.|Either set an explicit Transport (&http.Transport{...}) to make the isolation claim true (matching llmclient), or correct the comment to state the client reuses http.DefaultTransport and only isolates redirect policy.|maintainability|15|internal/scorecard/cloudsync.go:36-43|claude
MEDIUM|internal/registry/telemetry_setting.go:139|withConfigLock acquisition deadline is 60s but the stale-reclamation threshold is 300s. A process that crashed while holding the lock between 61s and 299s ago can neither be waited out (times out at 60s) nor reclaimed (not yet 300s stale), so every `config set` fails with timeout acquiring config lock for up to ~4 minutes with no recovery path.|Make the stale threshold <= the acquisition deadline (or extend the deadline past the stale threshold) so any lock old enough to time out is also old enough to reclaim; emit an actionable error naming the lock path.|correctness|30|internal/registry/telemetry_setting.go:139-166|claude
MEDIUM|internal/registry/telemetry_setting.go:56|SetTelemetrySetting symlink guard runs os.Lstat BEFORE withConfigLock and long before the os.Rename (spanning Stat/ReadFile/marshal/temp-write/fsync) - a TOCTOU: the path can be swapped to a symlink between check and rename. The guard is also skipped whenever Lstat returns any error (the err == nil gate), so a transiently-erroring path is treated as safe.|Perform the symlink check inside the lock immediately before the rename, or open the existing file O_NOFOLLOW and operate on the fd; treat a non-ErrNotExist Lstat error as a hard failure rather than proceeding.|security|60|internal/registry/telemetry_setting.go:56-60|claude
MEDIUM|cmd/atcr/review.go:420|The review telemetry ping computes status from err at review.go:421-424, but there err is only the fanout.ExecuteReview result; the --fail-on/--verify/--debate gate runs later (~lines 501-584). A run where all agents succeed but findings survive the gate (process exits 1) sends telemetry status=success, contradicting the reconcile path which was deliberately moved to send AFTER the gate (TD-009).|Move the review telemetry Send to after the one-shot reconcile/verify/debate/gate resolves the final outcome (mirroring reconcile.go), or compute the telemetry status from the finalized returned error rather than the pre-gate ExecuteReview error.|correctness|60|cmd/atcr/review.go:415-464|claude
MEDIUM|cmd/atcr/main.go:52|Telemetry Send dispatches a detached goroutine (WithoutCancel + 3s timeout) but nothing drains it before process exit: main.go calls os.Exit immediately after the command returns and production never calls client.Wait (Wait/wg exist but are test-only). Send fires at the very end of the run right before return then os.Exit, so once a real endpoint is wired the 3s request almost never completes and pings are dropped on essentially every run (latent now only because defaultTelemetryEndpoint is empty). Found by two independent reviewers.|Wire a bounded telemetryClient.Wait() (short cap ~1-2s) into main() before os.Exit (or the shutdown path) so an in-flight ping can complete without blocking indefinitely; the existing Wait()/wg seam was built for exactly this.|correctness|60|cmd/atcr/main.go:47-53 + internal/telemetry/client.go:99-100|claude
LOW|internal/telemetry/client.go:100|Client.Send spawns an unbounded detached goroutine per call with no concurrency cap or backpressure. Client is an exported reusable type; a future caller invoking Send in a loop (e.g. the MCP server path) against a slow/hung endpoint would accumulate goroutines up to (call-rate x 3s). Bounded to one goroutine at today's single-call sites, so latent not active.|Add a small buffered semaphore or bounded worker so Send drops the ping when N sends are already in flight rather than always spawning; assert goroutine count stays bounded under a burst against a blocking doRequest stub.|performance|30|internal/telemetry/client.go:95-101|claude
LOW|internal/telemetry/client.go:140|The drain uses io.LimitReader(resp.Body, 1<<16) and the comment claims this lets the keep-alive connection be reused; if a response body exceeds 64KB it is only partially read before Close, so the connection is NOT reused - the opposite of the stated intent. Self-correcting and acks are tiny so impact is negligible, but the comment overstates the guarantee.|Either fully drain (telemetry responses are trivially small) or soften the comment to note the cap intentionally sacrifices reuse on oversized bodies.|maintainability|15|internal/telemetry/client.go:140-141|claude
LOW|cmd/atcr/telemetry.go:116|dominantLang returns the extension of the single file with the most changed lines; if that dominant file has no extension (Makefile, Dockerfile) it returns empty even when other changed files carry a clear language (e.g. several .go files), and the doc comment says empty only occurs when no file is present or has an extension, misrepresenting this case and yielding a misleading empty lang label.|Fall back to the highest-line file that DOES have an extension, or tighten the comment to state the label is empty whenever the single dominant file lacks an extension.|maintainability|15|cmd/atcr/telemetry.go:104-121|claude
LOW|internal/telemetry/client.go:118|The if timeout == 0 guard in send() is dead code: requestTimeout is unexported and New (the only constructor) always sets it to defaultRequestTimeout, so the branch can only run for a zero-value Client{} that nothing constructs. Reads as if a zero timeout is a supported configuration when it is not.|Remove the guard and read c.requestTimeout directly, or document the field is intentionally defended for zero-value use.|maintainability|5|internal/telemetry/client.go:117-120|claude
LOW|internal/scorecard/cloudsync.go:102|NewCloudSyncRecord empty-agent gate trims (strings.TrimSpace(a.Agent) == "") but the next line hashes the untrimmed value (HashPersonaID(a.Agent)), and HashPersonaID does no normalization. An agent name with surrounding whitespace passes the non-empty gate yet hashes to a different digest than the clean name, fragmenting the Persona Leaderboard into two buckets for the same identity.|Compute name := strings.TrimSpace(a.Agent) once and use it in both the gate and HashPersonaID(name); if raw fidelity is deliberate, document why the gate trims but the hash does not.|correctness|15|internal/scorecard/cloudsync.go:101-113|claude
LOW|internal/scorecard/cloudsync.go:204|Push drain is capped at io.LimitReader(resp.Body, 1<<16) with a comment that this enables keep-alive reuse; a server body larger than 64KB leaves bytes unread so Go cannot return the connection to the pool and reuse is silently defeated. Good DoS guard but the reuse rationale only holds for sub-64KB bodies.|Keep the bounded read as a safety cap but adjust the comment to reflect reuse only holds for small bodies, or drain fully with a hard cap sized to the expected small ack.|performance|5|internal/scorecard/cloudsync.go:203-204|claude
LOW|internal/scorecard/cloudsync.go:179|Push re-validates the endpoint defensively but never validates that apiKey is non-empty. A direct caller (Push is exported) passing an empty key sends Authorization: Bearer with a trailing space, wasting a round-trip and potentially being treated as an anonymous request server-side. Defensive posture is asymmetric: endpoint guarded, credential not.|Add an early guard if strings.TrimSpace(apiKey) == "" return an error before building the request, mirroring the endpoint re-validation, so the exported function fails closed on a missing credential.|security|15|internal/scorecard/cloudsync.go:179-196|claude
LOW|internal/scorecard/telemetry.go:34|HashPersonaID uses unsafe.Slice(unsafe.StringData(raw), len(raw)) to avoid one []byte(raw) copy on a cold path (at most once per persona per run), so the saved allocation is negligible while the unsafe usage adds ongoing review burden and a footgun for maintainers. Correct for Go 1.25 but risk/benefit is inverted for a non-hot path.|Replace with plain sha256.Sum256([]byte(raw)); equally correct, trivial cost at this frequency, removes the unsafe import and its justification comment.|maintainability|15|internal/scorecard/telemetry.go:27-36|claude
LOW|internal/registry/telemetry_setting.go:145|The ownerFile WriteFile error is discarded; if it fails the lock is held with no epoch metadata, any concurrent waiter ReadFile(ownerFile) errors, the staleness branch is skipped, and the lock can never be reclaimed. If that holder crashes, every subsequent invocation times out at the 60s deadline until the directory is removed by hand.|Check the WriteFile error and abort acquisition on failure (releasing the lock), or treat an unreadable/absent ownerFile in the waiter as unconditionally stale after the deadline so a metadata-less lock is reclaimable.|correctness|30|internal/registry/telemetry_setting.go:143-161|claude
LOW|internal/registry/telemetry_setting.go:92|The comment claims the write Mirrors the trust-store write (trust.go), but trust.go Save() performs no fsync of the temp file or parent dir - it is Chmod/Write/Close/Rename only. This telemetry write is actually stronger (adds tmp.Sync + syncDir), so the reference misleads a reader into thinking trust.go is already crash-safe (it is not).|Correct the comment to state this write hardens beyond trust.go, and separately consider back-porting the fsync to trust.go so both paths are crash-safe.|maintainability|15|internal/registry/telemetry_setting.go:87-92|claude
LOW|internal/registry/telemetry_setting.go:190|configMapping treats any document with len(doc.Content) == 0 as empty and synthesizes a fresh mapping. A config file containing only comments unmarshals to a null document with text in HeadComment and empty Content, so the synthesized mapping discards those comments, contradicting the every other key and its comments survive promise for that stub case.|When Content is empty but the parsed doc carries head/foot comments, preserve them by attaching to the synthesized mapping node rather than replacing the document wholesale.|maintainability|30|internal/registry/telemetry_setting.go:189-201|claude
LOW|cmd/atcr/flags.go:54|addRangeFlags runs its own validation BEFORE the chained prev hook while addSyncCloudFlags runs prev BEFORE its own logic. Chaining correctly preserves both hooks (nothing dropped), but the opposite ordering is an undocumented easy-to-trip inconsistency: a future precondition needing a specific order will hit surprising behavior on a command that installs both.|Standardize the chain order across both helpers (prev-first or own-first) and document the chosen invariant in a comment.|maintainability|15|cmd/atcr/flags.go:20-31|claude
LOW|cmd/atcr/telemetry.go:61|telemetryGate calls registry.LoadTelemetrySetting("."), re-reading and re-parsing .atcr/config.yaml a second time per run. On the review path fanout.LoadReviewConfig(".") already parsed the full project config including Telemetry *bool (project.go:95), and on reconcile resolveGateThreshold already read the config; the already-parsed value is discarded and the file re-read.|Thread the already-loaded cfg.Project.Telemetry (review) / single config read (reconcile) into the gate and call telemetryEnabled(env, cfg.Project.Telemetry) directly, eliminating the redundant disk read and YAML parse.|performance|30|cmd/atcr/telemetry.go:59-66|claude
LOW|cmd/atcr/cloudsync.go:96|The final branch of resolveSyncCloudOutcome (preserve an already-coded failure, lines 96-100) is unreachable from both callers: review.go gates the deferred push behind if !cloudSyncPushable(err) return so only a non-coded err reaches it, and reconcile.go only passes nil or the plain exit-1 gateErr. The errors.As(runErr, &runCoded) check can never be true in production.|Drop the dead branch and document that callers guarantee a non-coded runErr, or keep it with a comment/test marking it a defensive backstop for a future caller.|maintainability|15|cmd/atcr/cloudsync.go:86-101|claude
