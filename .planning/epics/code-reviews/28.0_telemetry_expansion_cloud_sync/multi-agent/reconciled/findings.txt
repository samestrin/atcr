# atcr-findings/v1
HIGH|cmd/atcr/config.go:41|(newConfigSetCmd) No tests for runConfigSet error branches (unsupported key, invalid bool, repoRoot, SetTelemetrySetting) and success path|Change to return `nil` and call `cmd.Help()` or use a dedicated help command|idiom|45|[otto] `RunE: func(cmd *cobra.Command, _ []string) error { return cmd.Help() }` / [dax] runConfigSet has four error returns and one success output; no config_test.go in diff (disagreement: LOW vs HIGH)|dax,otto|HIGH
HIGH|internal/scorecard/telemetry.go:12|Empty string input to HashPersonaID not tested despite unsafe.StringData edge case|Validate input or use standard []byte(raw) conversion; the allocation savings are not worth the panic surface in a hash function called on untrusted scorecard data|boundary|15|[dax] Comment claims safety for empty string but no test confirms / [mira] unsafe.Slice(unsafe.StringData(raw), len(raw)) panics if raw is nil string (disagreement: MEDIUM vs HIGH)|dax,mira,pace|HIGH
HIGH|internal/scorecard/telemetry.go:12|HashPersonaID claims pseudonymization but unsalted SHA-256 over a small enumerable set is dictionary-reversible; the privacy contract is misleading and the Leaderboard cannot claim anonymized aggregation|Add HMAC-SHA256 with a provisioned, rotatable application secret (server-side keyed hash acceptable per KB entry); add TD-007 tracking label to the function|cryptography|120|[dax] HashPersonaID is a pure function with no tests; NewTelemetryPersonaRecord untested / [mira] SHA-256 without salt or HMAC over enumerable input set (persona names); digest is trivially reversible with a precomputed table / [kai] small, enumerable, often publicly-known set... UNSALTED digest does not defend against a dictionary/rainbow attack / [pace] HashPersonaID(r.Reviewer) is called for every Record in the scorecard, leading to redundant hash computations when the same reviewer appears multiple times (e.g., reviewing multiple models). For a pool of R reviewers and M models per reviewer, this does R*M hash computations instead of R. (disagreement: MEDIUM vs HIGH)|dax,kai,mira,pace|HIGH
MEDIUM|cmd/atcr/config.go:30|Long help text warns about ATCR_DISABLE_AST_GROUPING direction but does not document what exit code an I/O failure (missing config file) produces|Add exit-code expectation to the Long doc: "on I/O failure (missing/unwritable .atcr/config.yaml) returns exit 1"|observability|10|docs_audit_test.go already audits the Long text for env-var coverage; adding exit-code documentation closes the observability gap|mira|HIGH
MEDIUM|cmd/atcr/flags_test.go:28|(TestAddSyncCloudFlags_RegisteredOnReviewAndReconcile) No test verifies placeholder warning absent when --sync-cloud not set or endpoint overridden|Add test cases for warning absence to prevent false positive warnings|testing|15|TestAddSyncCloudFlags_DefaultEndpointWarns only checks positive case|dax|HIGH
MEDIUM|internal/scorecard/export_test.go:592|(TestRunLeaderboardExport_ByteForByteRegression) Byte-for-byte SHA-256 checksum pins the entire serialized output, making the test fail on any formatting, ordering, or whitespace change and obscuring the actual semantic contract|Replace checksum with structured assertions against parsed JSON fields|extensibility|30|wantExportChecksum = "96231aeede4bec24132992b35bcf0a5c069619248ad720f319372517ee39625a"|kai|HIGH
MEDIUM|internal/scorecard/telemetry.go:31|(HashPersonaID) HashPersonaID uses unsafe.StringData/unsafe.Slice to avoid a negligible allocation, trading memory safety guarantees for no meaningful performance gain on small strings|Use `[]byte(raw)` for readability; the allocation is negligible for persona IDs|correctness|10|[kai] unsafe.Slice(unsafe.StringData(raw), len(raw)) / [brad] unsafe.StringData(raw) used when raw == "" violates Go spec; sha256.Sum256 copies anyway so zero-copy is illusory / [mira] Comment notes HMAC hardening is deferred; no TD label in source for reconciler to act on / [otto] `unsafe.Slice(unsafe.StringData(raw), len(raw))` (disagreement: LOW vs MEDIUM)|brad,kai,mira,otto|HIGH
LOW|cmd/atcr/flags.go:39|Hardcoded production endpoint URL in binary is a maintenance hazard if the backend migrates|Make the default configurable via build-time ldflags or a well-documented constant; document the migration path in the inline comment|configuration|15|[mira] const defaultCloudEndpoint = "https://atcr.dev/dashboard" / [otto] `_, _ = fmt.Fprintf(cmd.ErrOrStderr(), "warning: ...")`|mira,otto|HIGH
LOW|internal/telemetry/event.go:1|Event struct has no test for exact JSON field allowlist (privacy regression guard)|Add test marshaling Event and asserting keys are exactly [event,lang,lines,status]|testing|10|Comment references TestClient_Send_PayloadHasExactlyFourAllowlistedKeys but no such test in diff|dax|HIGH
