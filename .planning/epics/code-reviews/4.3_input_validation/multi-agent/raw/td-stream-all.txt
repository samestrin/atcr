# Merged TD Stream from direct review
# SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER

MEDIUM|internal/validation/validation.go:56|FilePath rejects "/etc" but allows "/etc/passwd"|The guard path == sysDir handles bare "/etc"; the HasPrefix guard handles "/etc/". Both are covered.|maintainability|5|for _, sysDir := range []string{"/etc", "/proc", "/sys"} { if path == sysDir |dax| strings.HasPrefix(path, sysDir+"/") {
MEDIUM|cmd/atcr/report.go:41|Potential path traversal via symbolic links|The current validation checks the path string after `filepath.Abs`, but does not resolve symbolic links. A user could provide a path to a symlink that points to `/etc/passwd`, bypassing the prefix check.|Add `filepath.EvalSymlinks` before calling `validation.FilePath`|security|15|`abs, err := filepath.Abs(output)` followed by `validation.FilePath(abs)`|otto
MEDIUM|internal/validation/validation.go:60|FilePath rejects legitimate filenames containing ".."|Use filepath.Split-based component check or drop check (post-filepath.Abs callers have already resolved path traversal)|correctness|10|strings.Contains(path, "..") rejects /cwd/foo..bar, ..hidden, etc. — false positive|bruce
MEDIUM|internal/validation/validation.go:53|Dead path traversal check after filepath.Abs|Remove strings.Contains("..") check or validate against a canonical safe root|correctness|5|filepath.Abs calls Clean internally; ".." check never triggers post-Abs|greta
MEDIUM|cmd/atcr/report.go:41|Validation relies on dead ".." string check|Update validator to use filepath.Clean + boundary checks instead of string matching|security|10|strings.Contains("..") is bypassable and redundant after Abs resolution|greta
MEDIUM|internal/validation/validation.go:58|FilePath `..` check rejects legitimate filenames containing `..`|Check for `..` as path component, not substring|correctness|10|strings.Contains(path,"..") matches `/home/user..backup/` which is valid, not traversal|bruce
LOW|internal/validation/validation.go:31|GitRef doc says "no control chars" but only checks tab/newline|Fix comment or add \r and other control chars|maintainability|5|comment says "no control chars" but code only checks \t\n via ContainsAny|bruce
LOW|internal/validation/validation.go:49|Contains("..") falsely matches paths with "..."|Use boundary-aware check like strings.Contains(path, "/../")|correctness|3|strings.Contains(path, "..") triggers on ...|bruce
LOW|internal/validation/validation.go:83|Map allocated on every Severity invocation|Hoist valid map to package-level var|maintainability|2|valid := map[string]bool{...} inside func|bruce
