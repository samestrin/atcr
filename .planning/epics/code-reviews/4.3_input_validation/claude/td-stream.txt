# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/4.3_input_validation.md
# TARGET_TYPE: epic
# CREATED: 2026-06-19 07:34:30
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
MEDIUM|internal/validation/validation.go:62|FilePath's system-directory guard compares against the literal strings /etc, /proc, /sys, but on macOS (and many Unix layouts) /etc and /var are symlinks to /private/etc and /private/var. A caller-resolved absolute path of /private/etc/passwd reaches the same protected files yet passes the guard (no /etc prefix, no ..). Callers resolve via filepath.Abs only, not EvalSymlinks, so the real-path form bypasses the guard. Defense-in-depth only (atcr runs as the user, no privilege boundary), but the doc comment overstates the protection.|In validation.go FilePath, resolve symlinks before comparison or canonicalize the deny list — e.g. add /private/etc, /private/var equivalents, or have callers pass filepath.EvalSymlinks output. The review.go evalSymlinksFn seam (review.go:308) shows the resolution pattern already exists. Add a test asserting FilePath("/private/etc/passwd") errors.|security|60|internal/validation/validation.go:59-66|claude
MEDIUM|internal/validation/validation.go:56|FilePath's traversal guard uses strings.Contains(path, "..") — a substring test, not a path-segment test. It false-rejects legitimate paths where ".." is part of a name: my..file, release-1..2/out, a..b.md all return the "must not contain .." error. Because both callers validate the post-Abs/Clean absolute path (review.go:74, report.go:47) where Clean has already collapsed real traversal segments, this substring check at that point only catches innocent literal dots — the real traversal protection comes from Clean, making the check pure false-positive surface.|Either drop the ".." substring check on the post-Clean path (Clean already neutralizes traversal — the reason callers resolve first), or make it segment-aware: split on the path separator and compare each segment == "..". Add a test asserting FilePath("/home/user/my..file") returns nil.|correctness|30|internal/validation/validation.go:55-58|claude
LOW|cmd/atcr/report.go:111|runReport computes abs from --output and validates abs (line 47), then discards it: line 106 re-reads the raw output flag and line 111 writes to the raw path, not the validated abs. The two strings target the same inode today so it is not a live bypass, but the value validated is not the value used — fragile. Contrast review.go:77 which returns and threads the validated abs downstream. A future change to Abs resolution (e.g. adding EvalSymlinks for the symlink fix) would validate one path and write another.|Thread the validated abs to the write call: capture abs from the validation block and pass it to os.WriteFile at report.go:111 instead of the raw output string. Verify existing report tests pass and add one asserting the written file location matches the validated absolute path.|maintainability|15|cmd/atcr/report.go:106-115|claude
LOW|internal/validation/validation_test.go:37|TestFilePath exercises only raw inputs and never asserts the contract the callers depend on — that a post-Abs/Clean path validates correctly. No case for /private/etc (the macOS symlink bypass), no case for a legitimate ".."-in-name path being accepted, and no case for an absolute path with a collapsed traversal (/foo/../etc -> /etc) caught after cleaning. The suite passes while both FilePath defects are live, giving false confidence.|Add table cases to TestFilePath covering the symlink real-path (/private/etc/passwd), a benign ".."-substring filename, and the Clean-then-validate caller flow. The symlink case should fail before the validation.go fix and pass after.|testing|30|internal/validation/validation_test.go:44-57|claude
LOW|internal/validation/validation.go:33|GitRef's doc comment claims it rejects "control chars" but it only blocks tab, newline, and space — it accepts carriage return, vertical tab, form feed, DEL (0x7f), the metacharacters \\ ? * [, and a leading "-" (option injection), all of which git check-ref-format rejects. GitRef currently has no live callers (only FilePath is wired), so impact is dormant, but the comment overstates the guarantee and a future caller would inherit the gap. In scope only as a doc-vs-implementation discrepancy, not the out-of-scope --base/--head wiring decision.|Either narrow the doc comment to the actual subset checked, or tighten the guard to reject all bytes < 0x20, 0x7f, the shell/git metacharacters, and a leading "-". Add a test enumerating the control/metachar cases. Lowest priority — zero live callers.|maintainability|30|internal/validation/validation.go:25-41|claude

