HIGH|internal/fanout/status.go:122|Status JSON reserved byte counter ToolBytes is *int64 while the sibling registry byte field tool_budget_bytes is *int — two byte-quantity fields modeled with different integer widths (note: existing payload_byte_budget is *int64), an inconsistency a future stage may trip over.|Make registry ToolBudgetBytes a *int64 to match status ToolBytes and the existing payload_byte_budget for a consistent byte-count type across artifacts.|EDGE_CASES|15|The reconciler writes `status.json` with a `*int64` field `ToolBytes` while the registry config has a `*int` field `ToolBudgetBytes`, causing a type mismatch that could break future stages expecting consistent byte-count types.|bruce