# atcr-findings/v1
HIGH|cmd/atcr/resume.go:210|recordResumeHistory hardcodes cwd-relative ./.planning/history, so running atcr review --resume from a subdirectory writes shards to a location that atcr history (which uses repoRoot()) never reads|Pass repo root from runResume to recordResumeHistory to ensure shards are written to the project root rather than the current working directory|correctness|20|[greta] histPath := history.ShardPath(filepath.Join(".", ".planning", "history"), ts) / [brad] histPath := history.ShardPath(filepath.Join(".", ".planning", "history"), ts) / [otto] histPath := history.ShardPath(filepath.Join(".", ".planning", "history"), ts) (disagreement: MEDIUM vs HIGH)|brad,greta,otto|HIGH
HIGH|cmd/atcr/review.go:285|Multiple concurrent atcr review processes will append to the same YYYY-MM.jsonl simultaneously without synchronization, causing interleaved JSON lines and ledger corruption|Acquire a file lock (flock) on the shard or a dedicated lockfile before opening for append|concurrency|40|histPath := history.ShardPath(filepath.Join(req.Root, ".planning", "history"), now) / history.RecordReview(histPath, dir, now)|brad|HIGH
HIGH|internal/history/shard.go:33|LoadShards uses filepath.Glob on the directory path, so a repo path containing glob metacharacters (e.g. a [1] segment) is parsed as a character class and silently returns empty history instead of the real records|Replace filepath.Glob with os.ReadDir + strings.HasSuffix(name, ".jsonl") so the directory path is treated literally|edge_cases|20|`matches, err := filepath.Glob(filepath.Join(dir, "*.jsonl"))` treats metacharacters in dir as glob syntax|dax|MEDIUM
HIGH|internal/history/shard.go:43|LoadShards loads every monthly shard into memory before any --since filtering is applied, causing unbounded heap growth on repos with long histories even for narrow queries|Log-and-skip an unreadable individual shard so remaining shards stay queryable, mirroring Load's torn-line tolerance|error_paths|25|[brad] var all []Record / for _, path := range matches { recs, err := Load(path) ... all = append(all, recs...) } / [dax] `if err != nil { return nil, err }` inside the shard loop hard-fails on the first unreadable shard (disagreement: MEDIUM vs HIGH)|brad,dax|HIGH
MEDIUM|internal/history/shard.go:32|LoadShards loads every shard into memory before Filter applies --since, so a narrow time window still reads all shards (acceptable within 1-2yr bound but no month-prefix pruning)|Replace filepath.Glob with os.ReadDir and filter using strings.HasSuffix(name, ".jsonl") to treat the directory path literally|correctness|30|[brad] matches, err := filepath.Glob(filepath.Join(dir, "*.jsonl")) / [dax] `for _, path := range matches { recs, err := Load(path) ... }` loads all shards unconditionally / [greta] matches, err := filepath.Glob(filepath.Join(dir, "*.jsonl")) (disagreement: LOW vs MEDIUM)|brad,dax,greta|HIGH
MEDIUM|internal/history/shard.go:40|A single unreadable or corrupt shard aborts the entire LoadShards loop and fails atcr history, making one bad month brick all trend queries|Wrap Load in a conditional that logs the error and continues so remaining shards stay queryable|error|20|if err != nil { return nil, err } inside the for _, path := range matches loop|brad|HIGH
