MEDIUM|internal/history/shard.go:33|filepath.Glob treats the directory path as a pattern, so repo paths containing metacharacters like [ or ? are parsed as character classes and silently return zero matches|Use os.ReadDir(dir) and filter entries with strings.HasSuffix(name, ".jsonl")|correctness|15|matches, err := filepath.Glob(filepath.Join(dir, "*.jsonl"))
HIGH|cmd/atcr/resume.go:210|recordResumeHistory hardcodes cwd-relative ./.planning/history, so running atcr review --resume from a subdirectory writes shards to a location that atcr history (which uses repoRoot()) never reads|Resolve repoRoot() once and pass it to recordResumeHistory instead of hardcoding "."|correctness|10|histPath := history.ShardPath(filepath.Join(".", ".planning", "history"), ts)