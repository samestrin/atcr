HIGH|internal/history/shard.go:33|LoadShards uses filepath.Glob on the directory path, so a repo path containing glob metacharacters (e.g. a [1] segment) is parsed as a character class and silently returns empty history instead of the real records|Replace filepath.Glob with os.ReadDir + strings.HasSuffix(name, ".jsonl") so the directory path is treated literally|edge_cases|20|`matches, err := filepath.Glob(filepath.Join(dir, "*.jsonl"))` treats metacharacters in dir as glob syntax
HIGH|internal/history/shard.go:43|A single unreadable shard (permission/IO error) aborts LoadShards and fails `atcr history` with exit 2, so one bad monthly file bricks all trend queries|Log-and-skip an unreadable individual shard so remaining shards stay queryable, mirroring Load's torn-line tolerance|error_paths|20|`if err != nil { return nil, err }` inside the shard loop hard-fails on the first unreadable shard
MEDIUM|internal/history/shard.go:32|LoadShards loads every shard into memory before Filter applies --since, so a narrow time window still reads all shards (acceptable within 1-2yr bound but no month-prefix pruning)|Skip shard files whose YYYY-MM stem is entirely older than now-since before loading, to keep long histories fast|performance|30|`for _, path := range matches { recs, err := Load(path) ... }` loads all shards unconditionally
LOW|internal/history/shard_test.go:1|No test exercises an unreadable shard file (permission error) to verify LoadShards behavior|Add a test case with a 000-permission .jsonl file and assert the error or skip behavior|error_paths|15|TestLoadShards_MergesAllMonthlyFiles and siblings only test happy paths and absent dir
LOW|internal/history/shard_test.go:1|No test exercises LoadAll when the legacy file exists but is unreadable|Add a test with an unreadable legacy file and assert the error propagation|error_paths|15|TestLoadAll_MergesShardsAndLegacy only tests happy path; TestLoadAll_AbsentBothIsEmpty tests absent files
LOW|internal/history/shard_test.go:1|No test exercises LoadShards with a dir path containing glob metacharacters|Add a test with a dir containing [brackets] and verify records are still found|edge_cases|15|TestLoadShards_MergesAllMonthlyFiles uses plain t.TempDir() paths
LOW|cmd/atcr/history_test.go:84|TestHistoryCmd_MergesLegacyAndShards does not verify the merged order (shards before legacy) or that the legacy file is not mutated|Assert record ordering and verify legacy file content is unchanged after query|correctness|10|Test only asserts both packages appear in output, not order or non-mutation