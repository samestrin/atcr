LOW|internal/history/shard.go:32|filepath.Glob interprets metacharacters in dir as pattern syntax (e.g. [ ] or *) causing silent empty matches or unexpected file loads|Replace filepath.Glob with os.ReadDir and filter using strings.HasSuffix(name, ".jsonl") to treat the directory path literally|input|15|matches, err := filepath.Glob(filepath.Join(dir, "*.jsonl"))
MEDIUM|internal/history/shard.go:40|A single unreadable or corrupt shard aborts the entire LoadShards loop and fails atcr history, making one bad month brick all trend queries|Wrap Load in a conditional that logs the error and continues so remaining shards stay queryable|error|20|if err != nil { return nil, err } inside the for _, path := range matches loop
MEDIUM|internal/history/shard.go:43|LoadShards loads every monthly shard into memory before any --since filtering is applied, causing unbounded heap growth on repos with long histories even for narrow queries|Parse the YYYY-MM stem from the filename and skip files older than now-since before calling Load|memory|25|var all []Record / for _, path := range matches { recs, err := Load(path) ... all = append(all, recs...) }
HIGH|cmd/atcr/resume.go:211|recordResumeHistory hardcodes "." for the shard directory instead of resolving the repo root, so running atcr resume from a subdirectory writes to a path that atcr history never reads|Resolve repoRoot() in runResume and pass the absolute path to recordResumeHistory instead of using a relative dot|regression|20|histPath := history.ShardPath(filepath.Join(".", ".planning", "history"), ts)
HIGH|cmd/atcr/review.go:285|Multiple concurrent atcr review processes will append to the same YYYY-MM.jsonl simultaneously without synchronization, causing interleaved JSON lines and ledger corruption|Acquire a file lock (flock) on the shard or a dedicated lockfile before opening for append|concurrency|40|histPath := history.ShardPath(filepath.Join(req.Root, ".planning", "history"), now) / history.RecordReview(histPath, dir, now)