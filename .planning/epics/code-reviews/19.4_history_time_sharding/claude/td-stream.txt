MEDIUM|cmd/atcr/review.go:404|atcr review (review.go:404) and atcr resume (resume.go:213) write history shards to a CWD-relative .planning/history (req.Root is hard-coded to "." at review.go:302; resume uses "."), but the read path atcr history (history.go:52) resolves the shard dir via repoRoot() which walks up to the nearest .git/.atcr. Running review/resume from a subdirectory writes shards to <subdir>/.planning/history while history only reads <repoRoot>/.planning/history, so the run's findings are silently invisible and a stray .planning/history is created in the subdir. Pre-existing cwd==root assumption (the whole review command hard-codes Repo/Root="." per review.go:434), but Epic 19.4 makes the misrouted files git-tracked rather than gitignored. (Adversarial agent proposed HIGH; recorded MEDIUM as pre-existing and bounded by documented repo-root usage.)|Resolve the shard dir from repoRoot() in both write hooks (review.go and resume.go recordResumeHistory) so writes and the read path agree regardless of CWD; or thread the resolved repo root into req.Root. Verify by running review from a subdir then atcr history from root and confirming the run appears.|correctness|30|cmd/atcr/review.go:404 cmd/atcr/resume.go:213 cmd/atcr/history.go:52 cmd/atcr/root.go:14-32|claude
MEDIUM|internal/history/shard.go:41|LoadShards returns a hard error the moment Load fails on any single shard, aborting the entire atcr history query. This contradicts the resilience contract Load honors (reader.go: skip malformed lines, never brick history). One shard with a permission problem, or a directory matching *.jsonl (os.Open succeeds but Scanner errors EISDIR), takes down history reads for every other month including readable ones.|In the LoadShards loop (shard.go:40-46) log-and-skip a failed shard (warn to stderr) and continue accumulating from the rest, mirroring Load's line-level tolerance. Verify by chmod 000 on one shard and confirming atcr history still renders the other months instead of exiting 2.|error-handling|30|internal/history/shard.go:32-48 internal/history/reader.go:14-20|claude
MEDIUM|internal/history/shard.go:32|LoadAll/LoadShards read and JSON-unmarshal every monthly shard on disk, then Filter discards records older than the --since cutoff. The justification for monthly sharding is the YYYY-MM filenames, but the read path throws that structure away: a repo with years of history running the common --since 30d (or the 90d default) still parses every archived month it will immediately drop.|Prune glob results in LoadShards by comparing each shard's YYYY-MM stem against the cutoff month before calling Load; pass a lower-bound month into LoadShards/LoadAll (or add a windowed variant) so shards entirely before the window are never opened. Verify with a benchmark over 60 seeded shards and --since 30d reading at most 1-2 files.|performance|60|internal/history/shard.go:32-67 cmd/atcr/history.go:52-63 internal/history/filter.go:62-77|claude
LOW|internal/history/shard.go:66|LoadAll returns append(shards, legacy...), appending the legacy pre-19.4 ledger (oldest records) AFTER the newest shards. The LoadShards doc advertises records ordered chronologically by shard file name, but LoadAll silently breaks that ordering. Nothing downstream depends on order today (Filter/RenderTable aggregate), so it is latent, but a future reader trusting the doc who adds a most-recent-first feature will be quietly wrong.|Either prepend legacy (append(legacy, shards...)) to keep the chronological invariant the doc promises, or update the LoadAll/LoadShards docs to state merged order is not guaranteed chronological. Verify with a unit test asserting the intended ordering of a legacy+shard mix.|maintainability|15|internal/history/shard.go:25-28 internal/history/shard.go:57-67|claude
LOW|cmd/atcr/resume.go:212|recordResumeHistory (resume.go:212-219) is a near-verbatim duplicate of the inline history hook in review.go:404-409: same ShardPath construction, RecordReview call, and warn/debug logging, differing only in the root argument (. vs req.Root). This is the divergence that produced the anchoring inconsistency; the two copies must be kept in lockstep but already differ on root.|Extract a single recordHistory(ctx, root, dir, ts) helper used by both call sites so shard path, root resolution, and logging live in one place. Verify both commands still emit identical debug/warn output and target the same dir.|maintainability|15|cmd/atcr/resume.go:208-219 cmd/atcr/review.go:394-409|claude
LOW|internal/history/writer.go:39|Append does an unlocked O_APPEND write and documents that a large buffer split across multiple write() syscalls can be torn by a concurrent append. Epic 19.4 does not change the mechanism but changes exposure: history is now git-tracked and the project is run as multiple concurrent sessions appending to the same YYYY-MM.jsonl within a month. A torn line is silently dropped by Load's malformed-line skip, so lost findings are invisible; sharding by month did not reduce intra-month contention.|If a hard guarantee is wanted wrap the shard write in an advisory file lock (flock) consistent with the mkdir-flock pattern used in the planning tooling; otherwise document that concurrent same-month runs can lose records. Verify with a stress test of N concurrent Appends and a line-count assertion.|error-handling|60|internal/history/writer.go:16-51 internal/history/reader.go:43-45|claude
