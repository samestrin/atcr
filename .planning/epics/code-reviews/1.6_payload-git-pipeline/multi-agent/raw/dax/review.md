MEDIUM|internal/payload/diff.go:101|ensureRange guards five whole-range caches by one shared cacheKey; a future cache accessor that forgets to call ensureRange first would serve stale cross-range data|Centralize range reconciliation behind a single lazy keyed-access path so no cache can be read without first reconciling cacheKey|maintainability|30|ensureRange is called by each cache accessor individually; missing one call silently serves wrong-range data|bruce