```
MEDIUM|internal/payload/diff.go:232|chunkKey suffix match can mis-attribute nested paths|Require space or start-of-string before 'b/' in the suffix check|correctness|15|strings.HasSuffix(first, " b/"+h) matches " b/foo" in "diff --git a/x b/sub/foo" when h="foo", but the actual file is "sub/foo"; longest-match heuristic helps but doesn't fully prevent collision when one path is a suffix of another|bruce
MEDIUM|internal/payload/builder.go:147|Missing key-existence check before map access|Check if key exists in chunks map and return error or fallback if absent|correctness|10|chunks[headPathOf(f.pathspec())] returns empty string for missing keys; if a file appears in changedFiles but not in rawCache (e.g., mode-only change with no content diff), the caller gets an empty body silently instead of an error|bruce
LOW|internal/payload/diff.go:207|numstatNewPath doesn't handle all git rename formats|Add handling for git's percent-encoded paths and other rename notations|correctness|20|git may render rename paths with percent-encoding for special characters (e.g., spaces as %20); current code only handles " => " and "{old => new}" forms, potentially mis-keying binary status for files with special characters in names|bruce
LOW|internal/payload/diff.go:183|ensureRange uses string concatenation for cache key|Use a structured key type or separator that can't appear in branch names|correctness|5|cacheKey = base + ".." + head could collide if a branch name contains ".."; unlikely in practice but violates the invariant that cacheKey uniquely identifies the range|bruce
LOW|internal/payload/pipeline_test.go:137|TestBuildEntries_FilesModeOnlyShowScalesWithN assumes exact git process count|Assert >= 6 difference instead of == 6, or document the assumption|maintainability|5|The test asserts large-small == 6 exactly, but any future change that adds a git invocation (e.g., a new cache warm-up) would break this test; the sprint plan says "constant number... excluding show", so the delta should be >= 6|bruce
```