SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|SOURCE|REVIEWERS|CONFIDENCE
MEDIUM|internal/fanout/engine.go:450|Incorrect sentinel for retry override|Change `a.InitialBackoffMs > 0` to check if the budget was explicitly resolved|correctness|15|code-review|otto|MEDIUM
MEDIUM|internal/fanout/engine.go:456|The retry-override gate keys off a.InitialBackoffMs>0 as the configured sentinel, overloading one knob to also carry MaxRetries. A directly-constructed Agent that sets only max_retries=0 (single attempt, fail-fast) with no initial_backoff_ms is silently ignored. Config-loaded agents are unaffected because EffectiveInitialBackoffMs always inherits the resolved global (>=1), so this only bites bare/library Agent construction — but the struct comment presents a clean two-state contract when it is really three-state with a silent drop of MaxRetries. (disagreement: LOW vs MEDIUM)|Carry an explicit bool (e.g. RetryConfigured) or use a pointer on Agent to decouple apply-override from the backoff value, OR reword the comment to state MaxRetries is applied ONLY when InitialBackoffMs>0 so a caller setting MaxRetries alone knows it is ignored.|correctness|30|code-review|bruce,claude|HIGH
LOW|internal/llmclient/client.go:424|The added delay=clampBackoff(delay) before the loop is correct but its comment claims it guards a misconfigured initial_backoff OR a direct WithRetryOverride. In practice the registry caps initial_backoff_ms at exactly maxBackoff (30000ms == 30s), so for every config-sourced value the clamp is a strict no-op; the only input it can actually clamp is a hand-rolled WithRetryOverride exceeding 30s, which is untested. The comment implies broader coverage than exists.|Tighten the comment to state the only real source is a direct WithRetryOverride, and add a unit test passing WithRetryOverride(ctx, n, 60*time.Second) asserting the first sleep is capped at 30s (fails without line 424).|testing|15|code-review|claude|MEDIUM
MEDIUM|internal/llmclient/retry_override.go:26|WithRetryOverride clamps a negative maxRetries to 0 but performs no guard on initialBackoff, while its doc claims it applies the same guard as WithRetry. A negative or absurd duration from a direct caller flows into dispatch; clampBackoff only bounds the upper end (d > maxBackoff), so a negative base survives and jitter/sleepCtx treat it as fire-immediately. Production config is validated (>=1ms) so this only affects direct callers, but the guard is advertised and only half-applied. (disagreement: LOW vs MEDIUM)|Floor initialBackoff inside WithRetryOverride (clamp <0 to 0 or a 1ms minimum), symmetric with the maxRetries clamp. Add a unit test passing a negative duration and asserting a non-negative stored value.|error-handling|15|code-review|claude,greta,bruce|HIGH
LOW|internal/llmclient/retry_override.go:35|context.WithValue uses unexported key struct; safe but non-idiomatic|Consider using a typed context key variable for clarity|maintainability|5|code-review|bruce|MEDIUM
LOW|internal/registry/precedence.go:51|MaxInitialBackoffMs (30000) and DefaultInitialBackoffMs (500) are hand-copied to equal llmclient's maxBackoff (30s) and defaultInitialBackoff (500ms), bound only by a comment across a package boundary. If maxBackoff or defaultInitialBackoff changes in llmclient, the registry silently drifts: it would keep rejecting newly-valid bases or accept bases the engine clamps, contradicting the validation's stated purpose and the unchanged-behavior comment.|Export the llmclient constants and reference them, or add a registry test asserting MaxInitialBackoffMs == int(maxBackoff/time.Millisecond) and DefaultInitialBackoffMs == int(defaultInitialBackoff/time.Millisecond) so drift fails CI.|maintainability|30|code-review|claude|MEDIUM
LOW|internal/registry/precedence.go:172|ResolveSettings re-validates per-agent max_retries/initial_backoff_ms by walking reg.Agents, duplicating the identical range checks in validateAgent (config.go) and the global checks, now in three places with two different error-string formats (agent %q: vs agent '%s':). A future bound change must touch three sites and a caller sees inconsistent messages depending on which path rejects. The defense-in-depth intent (catch directly-constructed registries bypassing LoadRegistry) is valid but argues for one shared validator.|Extract a single validateRetryBounds(maxRetries, backoffMs *int, label string) helper and call it from validate(), validateAgent(), and ResolveSettings() with a consistent error prefix. Verify retry_config_test.go still passes and error strings unify.|maintainability|30|code-review|claude|MEDIUM
MEDIUM|internal/verify/invoke.go:118|buildSkepticAgent forwards derefInt(c.MaxRetries)/derefInt(c.InitialBackoffMs) raw rather than resolving through EffectiveMaxRetries/EffectiveInitialBackoffMs like buildAgent. A skeptic that omits retry config derefs both to 0, so invokeAgent's InitialBackoffMs>0 gate never fires and the skeptic silently uses the bare llmclient default budget (2 retries), NOT the resolved global default of 5 that review/fallback agents get. Two agent classes hitting the same provider/account use different rate-limit resilience; the verify package never receives resolved Settings (select.go SelectEligibleSkeptics) so it is structurally unable to inherit the global tier.|Thread resolved registry.Settings into SelectEligibleSkeptics/Skeptic and call c.EffectiveMaxRetries(s)/c.EffectiveInitialBackoffMs(s) in buildSkepticAgent, matching buildAgent. If the divergence is intentional, document it in docs/registry.md and assert it in a test. Verify an unconfigured skeptic dispatches with maxRetries=5 not 2.|maintainability|60|code-review|claude|MEDIUM
LOW|internal/verify/invoke_test.go:50|The only skeptic retry test (TestBuildSkepticAgent_ForwardsProviderAndBudgets) sets explicit values (4/200) and asserts they forward, but never tests the unset case — the exact path where the skeptic wrongly drops to client-default 2 instead of the global 5. The test locks in the current behavior by omission and gives false confidence the wiring is complete.|After resolving the skeptic-budget inheritance gap, add a test where the skeptic config has nil retry pointers and assert the built Agent carries the resolved global budget, proving parity with review agents.|testing|15|code-review|claude|MEDIUM
