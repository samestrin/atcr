# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/completed/7.5_syntax-guard-refinements.md
# TARGET_TYPE: epic
# CREATED: 2026-06-22 17:55:49
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
LOW|internal/verify/syntaxguard.go:62|jsonKeyLineRe (`^\s*"[^"]*"\s*:`) uses a non-escape-aware character class, so a JSON object whose ONLY member key contains an escaped quote (e.g. "a\"b": 1) does not match. looksLikeNonGoBraces then returns false, the block-brace signal fires, and the single-key escaped-quote JSON object is spuriously flagged invalid_syntax — the same false-positive class Epic 7.5 set out to close. Verified empirically: validateGoFixSyntax returns "illegal label declaration". Multi-key objects are unaffected because a sibling clean-key line still matches, so blast radius is narrow.|Widen jsonKeyLineRe to tolerate backslash-escapes in the key using an escape-aware character class (allow a non-quote/non-backslash char OR a backslash-escaped char between the quotes; RE2-safe, no backtracking). Add a single-key fixture {\n  "a\"b": 1\n} to syntaxguard_test.go asserting NoError. Alternatively accept and document the boundary as a known narrow limitation.|correctness|15|internal/verify/syntaxguard.go:62|claude
LOW|internal/verify/syntaxguard_test.go:274|The Epic 7.5 tests exercise only plain ASCII keys; they never probe escaped-quote, empty, or unicode keys — the regex boundary cases where jsonKeyLineRe behavior is non-obvious. Empty-key ("": 1) and unicode keys ("café": 1) suppress correctly, but the single-escaped-quote case silently regresses (see syntaxguard.go:62 finding) and nothing in the suite would catch a regression at this boundary.|Add table cases to TestLooksLikeNonGoBraces (syntaxguard_test.go:274) for "": 1 (expect true), "café": 1 (expect true), and "a\"b": 1 (pin the chosen behavior after the regex decision). Verify via go test ./internal/verify/.|testing|15|internal/verify/syntaxguard_test.go:274-279|claude
LOW|internal/verify/syntaxguard.go:190|The looksLikeNonGoBraces comment states it suppresses when there is "no Go declaration keyword", reading as if the JSON is structurally key-checked. In reality declKeywordRe is a coarse whole-input line-anchored scan, so a JSON value line beginning with a Go keyword (func/type/var/...) defeats suppression and re-arms the block-brace flagging. This is benign — it only re-arms pre-existing 7.1 behavior and never adds new flagging beyond 7.1 (AC2 intact) — but the comment over-promises and could mislead a future maintainer.|Tighten the comment at syntaxguard.go:183-189 to note the keyword check is a coarse whole-input line scan (not key-aware), so a JSON value line starting with a Go keyword defeats suppression — acceptable because it only re-arms 7.1 behavior, never adds flagging. No code change.|maintainability|5|internal/verify/syntaxguard.go:183-192|claude

