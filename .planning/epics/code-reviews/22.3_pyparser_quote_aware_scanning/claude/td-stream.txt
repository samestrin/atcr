# TD_STREAM - Technical Debt Items
# TARGET_ID: /Users/samestrin/Documents/GitHub/atcr/.planning/epics/active/22.3_pyparser_quote_aware_scanning.md
# TARGET_TYPE: epic
# CREATED: 2026-07-13 12:59:50
# Format: SEVERITY|FILE:LINE|PROBLEM|FIX|CATEGORY|EST_MINUTES|EVIDENCE|REVIEWER
# EST_MINUTES: estimated fix time
# EVIDENCE: Code snippet reference with file:line_start-line_end range (e.g., src/auth.ts:45-52)
# REVIEWER: 'claude' (this prompt), agent name (multi_review pool, e.g. bruce/greta)
# --- DATA BELOW THIS LINE ---
MEDIUM|internal/astgroup/host_test.go:718|scanLine's backslash-escape branch (main.go:201-203, 'case \\: i += 2') implements the epic-22.3 AC that a backslash escapes the next byte inside a single-line string, but no test exercises it. For input like if s == "a\"#b": the escaped quote keeps the string open so the trailing # is content and the : header survives; deleting the escape branch would let the second quote close the string, stripComment would cut at the in-string #, and the if-header would lose its : and stop nesting — with no test to catch the regression.|Add TestHost_PyParseEscapedQuoteInString parsing a header whose string holds an escaped quote then a # (e.g. if s == "a\"#b":) with a nested body, asserting the if node has children. Confirm it passes on HEAD and fails if main.go:201-203 is removed.|testing|30|internal/astgroup/parsers/src/pyparser/main.go:196-209|claude
MEDIUM|internal/astgroup/host_test.go:670|The three new epic-22.3 regression tests are all single-physical-line edge cases; no test feeds a genuine multi-line triple-quoted docstring containing a fake def/#/: and asserts those content lines are skipped (the startInString path at main.go:84-88 driven by a persisted delim). A regression breaking multi-line delim opening in scanLine would expose docstring content as real code while every existing test still passes.|Add a test parsing a func whose multi-line docstring body contains def fake(): and a # comment line, asserting collectFuncNames excludes 'fake' and includes the real following def. Verify it fails if delim-opening (main.go:210-219) is stubbed out.|testing|30|internal/astgroup/parsers/src/pyparser/main.go:83-88|claude
LOW|internal/astgroup/parsers/src/pyparser/main.go:115|Quote-aware stripComment changes bracketDelta counting on continuation lines containing an in-string #. significantLines feeds bracketDelta(stripComment(...)) at lines 98/102 and 115/119; the new stripComment keeps bytes after an in-string # that the old cut-at-first-# discarded, and because bracketDelta is deliberately not string-aware (main.go:151-155) the two can now disagree and flip continuation folding. Usually an improvement, but a direction-dependent, undocumented, untested behavior change.|Document the interaction in the significantLines comment, or route bracketDelta through scanLine-style state so stripComment and bracketDelta agree. Add a continuation-line test with an in-string bracket+# to pin the intended behavior.|correctness|60|internal/astgroup/parsers/src/pyparser/main.go:98-124|claude

