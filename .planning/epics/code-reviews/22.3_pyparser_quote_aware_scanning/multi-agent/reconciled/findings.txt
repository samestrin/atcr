# atcr-findings/v1
MEDIUM|internal/astgroup/parsers/src/pyparser/main.go:193|(scanLine) Missing test for triple-quote inside single-line string of the same quote type|Add test with `"text """` or `'text '''` to ensure same-quote triple-quote does not flip state|testing|20|`if q != 0` at line 193 guards against triple-quote detection; `TestHost_PyParseTripleQuoteInsideString` only tests `"` with `'''`, not `"` with `"""`|dax|MEDIUM
MEDIUM|internal/astgroup/parsers/src/pyparser/main.go:198|(scanLine) Escape handling inside single-line string (`case '\\': i += 2`) has no test coverage|Add test with escaped quote inside string containing `#` to exercise escape logic|testing|20|`case '\\': i += 2` at line 198 is never executed; `TestHost_PyParseHashInsideString` uses unescaped string|dax|MEDIUM
MEDIUM|internal/astgroup/parsers/src/pyparser/main.go:228|(scanLine) No test for `#` inside a triple-quoted (multi-line) string|Add test with a triple-quoted string containing `#` to verify it is not treated as comment|testing|20|`func scanTripleQuotes` at line 228 calls `scanLine` which ignores `#` inside `delim != ""`; no test exercises this|dax|MEDIUM
LOW|internal/astgroup/parsers/src/pyparser/main.go:171|`stripComment` now uses `scanLine` but no test for comment after a closed string|Add test like `x = "a" # comment` to verify comment stripping still works|style|15|[otto] `...is quote- and escape-aware (epic 22.3)` / [dax] `stripComment` at line 333 calls `scanLine`; no test verifies comment after closed string is stripped|dax,otto|HIGH
LOW|internal/astgroup/parsers/src/pyparser/main.go:199|(scanLine) Backslash escape `i += 2` may skip past end of line without bounds check; boundary untested|Add test with backslash at end of line inside string to verify safe handling|testing|15|`i += 2` at line 199 after `case '\\':`; no test for line ending with backslash inside string|dax|MEDIUM
LOW|internal/astgroup/parsers/src/pyparser/main.go:221|(scanLine) Comment restates the a-priori objective of the epic|Remove "(epic 22.3)" from the comment|style|2|`...appearing inside a # comment (epic 22.3)`|otto|MEDIUM
LOW|internal/astgroup/parsers/src/pyparser/main.go:234|Comment restates the a-priori objective of the epic|Remove "(epic 22.3)" from the comment|style|2|`...string span on this line is treated as string content, not a comment start (epic 22.3)`|otto|MEDIUM
