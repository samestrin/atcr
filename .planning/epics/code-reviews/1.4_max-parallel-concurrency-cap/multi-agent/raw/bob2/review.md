MEDIUM|internal/fanout/engine.go:125|Potential data race writing shared results slice concurrently|Serialize result writes with mutex or channel aggregation|correctness|15|results[i] = e.invokeSlot(ctx, s) is written by multiple goroutines while Run returns the slice|bruce
LOW|docs/registry.md:89|max_parallel docs do not state serial lane can add one extra provider call|Clarify cap is parallel-lane only; total may be N+1 with serial agents|maintainability|5|“Cap on concurrent parallel-lane agent calls… serial lane is unaffected” lacks N+1 clarification|bruce
LOW|internal/fanout/engine.go:111|Engine comment says “Each goroutine still spawns; only provider call is gated”|Add cancellation acquire bypass behavior to comment|maintainability|5|select on ctx.Done bypasses sem and invokes slot without token during cancellation|bruce