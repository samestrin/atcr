LOW|internal/fanout/engine.go:131|Under cancellation a queued goroutine that loses the acquire race bypasses the semaphore and calls invokeSlot without a token; correct for drain but the cap is not enforced during cancellation windows (only timeout results are produced, so no real provider burst occurs).|No code fix needed; behavior is correct since the bypassed path makes no provider call. Confirm intent in a code comment.|EDGE_CASES|5|Under cancellation a queued goroutine that loses the acquire race bypasses the semaphore and calls invokeSlot without a token; correct for drain but the cap is not enforced during cancellation windows (only timeout results are produced, so no real provider burst occurs).|bruce