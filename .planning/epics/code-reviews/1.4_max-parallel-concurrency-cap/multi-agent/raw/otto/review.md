```
HIGH|internal/fanout/engine.go:127|Potential deadlock or resource leak on context cancellation|The `defer func() { <-sem }()` is only registered if the `sem <- struct{}{}` case is selected. If the `<-ctx.Done()` case is selected, the goroutine returns immediately. However, the logic is correct for the `<-ctx.Done()` path. The real issue is that `results[i] = e.invokeSlot(ctx, s)` is called *inside* the `select` block for the cancellation path, but `invokeSlot` might still attempt to perform work or block. More importantly, if `sem` is nil (unbounded), the code proceeds to `results[i] = e.invokeSlot(ctx, s)`. If `sem` is NOT nil and the context is cancelled, it calls `invokeSlot` and returns. This is correct for the WaitGroup drain. However, there is a subtle race: if the context is cancelled *exactly* after the `sem <- struct{}{}` succeeds but before the `defer` is registered, the token is leaked. While the `defer` is immediate, the pattern is slightly risky. Actually, the primary concern is the `invokeSlot` call inside the `select` case: it is called with a cancelled context. If `invokeSlot` doesn't handle a cancelled context by returning immediately, the "drain" guarantee is compromised.|Ensure `invokeSlot` is strictly context-aware and consider moving the `results[i]` assignment to a single exit point to ensure consistency.|correctness|15|case <-ctx.Done(): results[i] = e.invokeSlot(ctx, s); return|otto
```