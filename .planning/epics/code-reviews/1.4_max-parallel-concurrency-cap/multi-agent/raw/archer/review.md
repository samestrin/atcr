HIGH|internal/fanout/engine.go:112|The cap bounds only the parallel lane; the concurrently-running serial lane adds one more in-provider call, so true peak provider concurrency is max_parallel+1 rather than max_parallel.|Clarify in docs/registry.md that the cap governs the parallel lane only and total concurrency may be N+1 with serial agents present.|correctness|5|The semaphore limits concurrent provider calls in the parallel lane to max_parallel, but the serial lane runs one additional provider call concurrently, resulting in a true peak concurrency of max_parallel+1 rather than max_parallel as implied by the cap.|REVIEWER|
HIGH|internal/fanout/engine.go:131|Under cancellation a queued goroutine that loses the acquire race bypasses the semaphore and calls invokeSlot without a token; correct for drain but the cap is not enforced during cancellation windows (only timeout results are produced, so no real provider burst occurs).|No code fix needed; behavior is correct since the bypassed path makes no provider call. Confirm intent in a code comment.|correctness|5|A goroutine that loses the semaphore acquire race during cancellation invokes Slot without a token, but since it does not make a provider call, the cap is not violated. The drain guarantee is preserved.|REVIEWER|
HIGH|internal/payload/manifest.go:22|The resolved max_parallel value is not recorded in manifest.json/status (only payload_mode is), so a throttled run gives no on-disk evidence of the active cap when diagnosing slow fan-outs in prod.|Record the effective max_parallel (and timeout_secs) in the manifest for post-hoc diagnosis, or accept parity with the existing un-recorded timeout_secs.|observability|15|The manifest.json file only records payload_mode and timeout_secs, but not max_parallel, so there is no on-disk evidence of the active concurrency cap when diagnosing slow fan-outs in production.|REVIEWER|
MEDIUM|internal/registry/precedence.go:399|Default max_parallel=10 silently changes behavior for existing rosters larger than 10 agents (previously all fired at once, now throttled to 10) with no migration notice.|Note the behavior change in CHANGELOG so >10-agent users understand the new throttle (otherwise acceptable per plan).|regression-risk|5|The default max_parallel=10 changes the behavior for existing rosters larger than 10 agents, previously all fired at once, now throttled to 10 concurrent provider calls, with no migration notice.|REVIEWER|
LOW|docs/registry.md:85|The serial lane is unaffected — it is already sequential. `max_parallel: 10` | Clarify in docs/registry.md that the cap governs the parallel lane only and total concurrency may be N+1 with serial agents present. |correctness|5|The serial lane runs one additional provider call concurrently, so true peak provider concurrency is max_parallel+1 rather than max_parallel as implied by the cap.|REVIEWER|
LOW|internal/registry/precedence.go:119|Default max_parallel=10 silently changes behavior for existing rosters larger than 10 agents (previously all fired at once, now throttled to 10) with no migration notice.|Note the behavior change in CHANGELOG so >10-agent users understand the new throttle (otherwise acceptable per plan).|regression-risk|5|The default max_parallel=10 changes the behavior for existing rosters larger than 10 agents, previously all fired at once, now throttled to 10 concurrent provider calls, with no migration notice.|REVIEWER|
LOW|internal/fanout/engine.go:124|The semaphore bounds concurrent provider calls (the work inside `Completer.Complete`), which is what the fake completer's peak counter measures and what Success Criterion 2 asserts — not the goroutine-spawn count. | No code fix needed; behavior is correct. |correctness|5|The semaphore bounds concurrent provider calls (the work inside `Completer.Complete`), which is what the fake completer's peak counter measures and what Success Criterion 2 asserts — not the goroutine-spawn count.|REVIEWER|