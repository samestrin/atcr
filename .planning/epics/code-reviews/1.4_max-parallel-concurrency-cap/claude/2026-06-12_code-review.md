# Code Review Report: 1.4_max-parallel-concurrency-cap

## 1. Executive Summary
- **Overall Result:** Pass
- **Items Checked:** 4 / 4
- **Approval Status:** Approved
- **Review Date:** June 12, 2026
- **Review Mode:** Epic (Acceptance Criteria + Adversarial + Tests)

## 2. Acceptance Criteria Verified

- **AC1 — `max_parallel` resolves through CLI > project > registry > embedded; negative fails with a usage error** — VERIFIED ✅
  - Evidence: `internal/registry/precedence.go:16,56,80-86`, `internal/registry/config.go:99,145-146`, `internal/registry/project.go:39,145-146`, `cmd/atcr/review.go:30,168-170`
- **AC2 — engine test proves peak concurrency never exceeds N for a roster larger than N** — VERIFIED ✅
  - Evidence: `internal/fanout/engine_test.go:306-319` (WithMaxParallel(2) on 5-agent roster, asserts peak ≤ 2); edge cases `:321`, `:333`
- **AC3 — serial-lane behavior + WaitGroup drain guarantee unchanged** — VERIFIED ✅
  - Evidence: `internal/fanout/engine.go:128-141` (ctx-aware acquire), `:147-176` (serial lane untouched); `internal/fanout/engine_test.go:345-360`
- **AC4 — registry.md documents the setting; `atcr init` template carries it** — VERIFIED ✅
  - Evidence: `docs/registry.md:90,43,79,128,134`; `internal/registry/project.go:64-65` (comment + active key); `cmd/atcr/init_test.go:38-39`

## 3. Evidence Map

- **Semaphore implementation:** `internal/fanout/engine.go:103-145` — buffered `chan struct{}` sized from `maxParallel`; `nil` when `<= 0` (unbounded). Ctx-aware acquire preserves the drain guarantee.
- **Settings threading:** `cfg.Settings.MaxParallel` → `PreparedReview.MaxParallel` (`fanout/review.go:210`) → `NewEngine(completer, WithMaxParallel(p.MaxParallel))` (`fanout/review.go:230`). Recorded into the manifest (`fanout/review.go:194`).
- **Precedence + validation:** `internal/registry/precedence.go:50-99` (`ResolveSettings`), negative rejected at all three settable tiers.

## 4. Remaining Unchecked Items

No remaining unchecked items — all 4 acceptance criteria verified.

## 5. Manual Review Status
- **Code Reviewed and Approved:** Checked
- **Rationale:** Implementation matches the epic's stated contract end-to-end. Three independent hostile reviewers (two with `-race` + source-mutation testing) confirmed the concurrency core is correct: token balance, WaitGroup drain under cancellation, and data-race safety all hold. Mutation tests proved the cap-bounds and drain tests genuinely fail when the semaphore/result-write logic is broken.

## 6. Coverage Analysis
- **Coverage:** 87.2%
- **Baseline:** 80%
- **Delta:** ↑7.2%
- **Status:** PASSING
- Per-package (epic-touched): `internal/fanout` 84.9%, `internal/registry` 88.0%, `cmd/atcr` 82.3%

## 7. Quality Checks
| Check | Status | Command |
|-------|--------|---------|
| Lint | PASSING | golangci-lint run |
| Types | PASSING | go vet ./... |
| Format | PASSING | gofmt -l . |

## 8. Adversarial Analysis
- **Files Reviewed:** 10
- **Issues Found:** 6 (Critical: 0, High: 0, Medium: 1, Low: 5)

### Issues by Severity
- **MEDIUM** `internal/payload/manifest.go:38` — `omitempty` on `MaxParallel` erases the `0`/unbounded value, defeating the field's documented post-hoc-diagnosis purpose. (correctness, ~15m)
- **LOW** `internal/registry/precedence.go:115` — `applyTier` trusts file tiers without re-validating; a non-loader caller could drive a negative (→ silently unbounded) into the engine. (error-handling, ~15m)
- **LOW** `internal/registry/precedence.go:16` — embedded default `10` vs the "0 = unbounded" messaging everywhere else; unset ≠ unbounded is undocumented in user-facing copy. (maintainability, ~5m)
- **LOW** `internal/fanout/engine_test.go:306` — cap tests use a 30ms delay rather than a barrier to force overlap (robust in practice; 0 flakes in 60+ runs). (testing, ~30m)
- **LOW** `internal/fanout/engine_test.go:345` — drain tests assert `Run` returns but don't independently verify zero goroutine leak (no goleak). (testing, ~30m)
- **LOW** `internal/fanout/engine_test.go:108` — serial-lane sequentiality test cannot detect a max_parallel regression; labeling clarity. (maintainability, ~5m)

> Dropped (already addressed): the "serial lane makes effective max = `max_parallel + 1`" observation is already documented at `docs/registry.md:90`.

## 9. Follow-ups
- Run `/reconcile-code-review @.planning/epics/completed/1.4_max-parallel-concurrency-cap.md` to merge these 6 findings into the technical-debt README.
- Consider fixing the MEDIUM manifest `omitempty` item, since it directly undermines the field added by this epic.

---
*Generated by /execute-code-review on June 12, 2026 01:25:43PM*
