# Feature Request: Semantic NCD Deduplication

- **Estimated time**: 4 days
- **Tasks**: 3
- **Components**: 1
- **Execution**: execute-epic

## Context

The reconciler deduplication pipeline currently relies on standard string-overlap algorithms to determine if two models have reported the same finding. 

## Problem Statement

Standard text-overlap algorithms (Jaccard, TF-IDF) fail when two models identify the same issue using entirely different vocabularies. This leaves duplicate findings in the final review, annoying the end-user.

## Proposed Solution

- Calculate Normalized Compression Distance: `NCD(x, y) = (C(x+y) - min(C(x), C(y))) / max(C(x), C(y))`.
- Use Go's fast `zstd` compression.
- Concatenating and compressing two review findings provides a deterministic, language-agnostic semantic similarity score.

## Acceptance Criteria

- [ ] AC1: NCD algorithm implemented using `zstd`.
- [ ] AC2: Replaces the existing `internal/reconcile` text-similarity threshold logic.
- [ ] AC3: Benchmark suite proves NCD correctly identifies duplicate findings that previous lexical algorithms missed due to vocabulary differences.

## Success Criteria

### Functional
- [ ] Benchmark processes 1,000 finding comparisons in <50ms.
- [ ] Deduplication catches semantically identical but lexically diverse findings.

### Business
- [ ] Eliminates 90% of visually redundant code review comments.

## Technical Constraints

- Must use native Go `zstd` library for performance and cross-compilation capability.

## Non-Functional Requirements

### Performance
- Algorithm must execute in under 1ms per pair to allow quadratic matching.

## Out of Scope
- LLM embeddings or local vector databases.

## Dependencies
- `github.com/atcr/reconcile` module extracted in Epic 8.0.

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| NCD compression overhead is too slow | Low | High | Benchmark `zstd` early; pre-allocate byte buffers to avoid GC pressure. |

## Clarifications

### Recorded 2026-06-27 (rubber duck — /execute-epic)

**Key Decisions:**
- NCD replaces Jaccard as the dedup algorithm AND drives the merge gate (Option Y), but the decision boundary stays deterministic: the NCD distance is integer-quantized before the threshold comparison (same two-tier "integer-authoritative, float-advisory" pattern the current cross-multiply already uses). The raw NCD float is advisory/display only.
- NCD is a distance (0 = identical, 1 = unrelated), so the merge/gray thresholds are inverted from the current Jaccard `inter*10 >= union*7` / `>= union*4` and must be calibrated against a labeled corpus.
- The real algorithm lives in the extracted in-tree module `github.com/samestrin/atcr/reconcile` (`reconcile/dedupe.go`), wired in via the `replace ... => ./reconcile` directive in the root `go.mod`. `internal/reconcile` only re-exports it.
- "Native Go zstd" = pure-Go (no CGO) for cross-compilation → `github.com/klauspost/compress/zstd`. stdlib has no zstd; gzip/flate are not substitutes (different ratios change every NCD score).
- NCD is computed over concatenated Problem + Fix + Evidence so the compressed input is long enough (50-150 words) to give reliable compression-distance signal.

**Scope Boundaries:**
- IN scope: NCD primitive + zstd dep; replace classify()/tokenize() with NCD-driven integer-quantized inverted thresholds; threshold calibration + labeled corpus (25-40 pairs); dedupe/grayPair test update; golden-corpus regeneration (`UPDATE_GOLDEN=1`); benchmark suite (AC3); doc-comment updates. 6 tasks (header's "Tasks: 3" superseded), 1 component.
- OUT of scope: changing AmbiguousHash / union-find adjudication / sidecar schema / internal/debate / downstream AmbiguousCluster consumers (they depend on cluster structure, not the similarity algorithm); LLM embeddings or vector DBs.

**Technical Approach:**
- Reuse a single zstd encoder + preallocated byte buffers across pair comparisons to hit `<1ms`/pair and `<50ms`/1000-comparison targets and avoid GC pressure.
- Hit the 1,000-comparison throughput figure by looping over the labeled pairs in the benchmark, not by authoring 1,000 distinct pairs.
- Keep NCD out of any serialized struct hashed by AmbiguousHash (or, if surfaced as a display field, a single controlled `UPDATE_GOLDEN=1` run suffices and does not recur across zstd upgrades).

**Original Questions and Answers:**
1. Q: How should NCD coexist with the float-free integer gate invariant?
   A: Option Y (revised) — NCD replaces Jaccard and drives the gate, kept deterministic by integer-quantizing the NCD distance at the boundary; raw NCD float advisory only.
2. Q: Modify the extracted reconcile/ module rather than internal/reconcile?
   A: Yes — modify reconcile/dedupe.go directly (in-tree via replace directive); internal/reconcile is a delegation wrapper.
3. Q: Full replace of Jaccard, or add NCD behind a flag?
   A: Full replace. Blast radius limited to classify()/tokenize(), grayPair() helper, golden corpus. 6 tasks, fits the skill cap.
4. Q: Add github.com/klauspost/compress/zstd as the native Go zstd library?
   A: Yes — pure-Go, satisfies the no-CGO cross-compilation constraint.
5. Q: Author the labeled corpus, and how many pairs are credible proof?
   A: Yes — 25-40 pairs (~20 true-dup lexically-diverse + 10-15 true-negative).
6. Q: Open to the benchmark concluding NCD doesn't beat Jaccard and stopping?
   A: Benchmark is the AC3 decider; efficacy risk low when NCD runs over Problem+Fix+Evidence. If it fails, AC3 is falsifiable and stopping is correct.

---

## Outcome / Findings (2026-06-27, /execute-epic)

**Status: NOT ADOPTED — AC3 falsified by the benchmark.** NCD does not reliably
identify lexically-diverse duplicate findings, and its error profile is worse than
the incumbent Jaccard gate. The T1 implementation was reverted; `reconcile/dedupe.go`
retains the Jaccard token-set algorithm and the `reconcile` module stays
stdlib-only (no `zstd` dependency added on `main`).

### What was built and tested
- T1: NCD over concatenated Problem+Fix+Evidence using pure-Go
  `klauspost/compress/zstd`, with an integer-quantized deterministic gate
  (committed, then reverted).
- A labeled calibration corpus of **33 pairs** — 20 same-issue/different-vocabulary
  duplicates + 13 unrelated pairs — preserved at
  `13.0_semantic_ncd_deduplication_evidence/ncd_corpus.json`.

### Benchmark result — no separating threshold exists
Best achievable accuracy at an *oracle* cutoff (chosen with knowledge of the
labels — not realizable in production), across text composition × zstd level:

| text | zstd level | best accuracy | worst-dup NCD | best-distinct NCD | separable |
|------|-----------|---------------|---------------|-------------------|-----------|
| problem only | Fastest | 64% | 0.889 | 0.243 | no |
| problem only | Default | 73% | 0.883 | 0.243 | no |
| problem only | Best | 67% | 0.685 | 0.571 | no |
| Problem+Fix+Evidence | Fastest | 73% | 0.959 | 0.478 | no |
| Problem+Fix+Evidence | Default | 76% | 0.781 | 0.508 | no |
| Problem+Fix+Evidence | Best | **82%** | 0.796 | 0.728 | no |

The duplicate and distinct NCD distributions fully overlap in every configuration.
At the chosen production config (Fastest, cutoff 0.55) only 17/20 duplicates merged
while 4/13 unrelated pairs were *wrongly* merged.

### Why it fails
Every finding shares the code-review register ("the X is Y; fix: use A; evidence:
the handler …"), so two *unrelated* findings in that template compress together
better than two genuinely different paraphrases of the *same* bug. NCD measures
shared surface structure/vocabulary — the very thing it was meant to beat — not
semantics. A cutoff loose enough to catch diverse duplicates merges nearly all
unrelated pairs (silent data loss); a cutoff tight enough to avoid false merges
catches almost no diverse duplicates. Unwinnable for this data.

### Acceptance criteria
- AC1 (NCD implemented via zstd): achieved, then reverted (not shipped).
- AC2 (replace text-similarity threshold logic): not done — replacement would regress quality.
- AC3 (benchmark proves NCD catches duplicates lexical algos missed): **FALSIFIED**.

### Downstream impact
| Epic | Impact |
|------|--------|
| 13.2 Bipartite + DBSCAN | HIGH — NCD was one of two edge-weight metrics; re-source from 13.1 AST isomorphism / token / embeddings. Annotated. |
| 13.1 AST plugin | LOW — only a stale "13.0 should be stable" dependency; void. 13.1 is independent and now the primary semantic signal. Annotated. |
| 14.2 Reconciliation layer | LOW — framed NCD *or* DBSCAN as alternatives; proceed on the DBSCAN path. Annotated. |
| 13.3 PageRank signal | NONE. |

### Recommendation
Pursue lexically-diverse dedup via 13.1 AST isomorphism + 13.2 bipartite matching
(a more principled approach), feeding the bipartite edge weights from a real
distance metric — token-set similarity, AST structural distance, or embeddings
(embeddings were explicitly OUT of scope for 13.0 and would need their own epic).
