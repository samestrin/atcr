# Epic Plan 19.8: Hermes Maintenance Agents — Mechanical, Judgment & Drafting

- **Estimated time**: TBD
- **Tasks/Components**: TBD
- **Execution**: init-plan

## Objective

Configure scheduled **hermes agents** to consume Epic 19.7's `atcr models check` primitives and offload the recurring persona-model maintenance across three clearly-separated roles — **mechanical** (deterministic catalog-diff → slug-bump PR, no LLM), **judgment** (classify drift; route minor vs. major/deprecation), and **drafting** (an LLM reads the vendor's updated prompting guide and drafts persona `.md` edits). Every change lands as a **pull request**, is **fixture-gated in CI**, and prompt changes require **human approval** — the agent is a contributor, never a committer to `main`.

## Context

**The real deliverable is the battle-tested prompt.** Model-tuned reviewer prompts are refined *in production*: they hallucinate, over-report, flag non-bugs, or miss real ones — and the maintainer tunes them over time until they earn trust. That human refinement loop is unavoidable and is 19.6's core value. Hermes automation does not replace it; it converts "write the re-tune from scratch" into "review a drafted re-tune," and it eliminates the purely mechanical slug/deprecation toil entirely.

**Split the labor by determinism.**

- **Mechanical work needs no LLM.** Detecting a newer family member, a deprecating `expiration_date`, or a missing slug, then bumping a lock, is pure string/timestamp comparison. An LLM here only *adds* hallucination risk (invented slugs) to a deterministic task. This layer is a scheduled wrapper over 19.7's `atcr models check --json` + a PR opener.
- **Judgment work is classification.** Minor bump → hand to mechanical. Major bump or deprecation → open a re-tune work item tagged with the vendor guide URL.
- **Drafting work is where an LLM helps, with a caveat.** Asking a flat-rate open model (Kimi K2.7 / GLM 5.2 / Qwen 3.7) to re-tune a prompt meant to embody *Anthropic's/OpenAI's/Google's own* prompting guidance is cross-vendor — the open model can mechanically transcribe "the guide now says X → change Y" but cannot judge fit for the target model. So its output is a **draft the maintainer reviews**, never an auto-merge.

**The guardrails already exist.** An auto-generated prompt is exactly 19.6's C3 threat model — untrusted input becoming a system prompt. 19.6 already ships the fixture gate + length cap + template allowlist that contain it. The hermes agent is just another untrusted contributor the existing machinery already handles.

## Proposed Solution

1. **Mechanical agent (no LLM).** Scheduled run of `atcr models check --json`; on a detected newer member / expiration / missing slug, open a **slug-bump PR** that advances the lock and lets CI's fixture gate verify. Auto-merge-on-green is an opt-in policy the maintainer sets.
2. **Judgment agent.** Classify each drift item: minor → mechanical PR; major or deprecation → open a **re-tune task** carrying the persona, the old/new model, and the vendor prompting-guide URL.
3. **Drafting agent (LLM).** On a re-tune task, fetch the vendor's current prompting guide, draft edits to the persona `.md`, and open a **separate** PR (never mixed with a mechanical slug bump). Backed by a configurable flat-rate reasoning model (Kimi/GLM/Qwen); output is explicitly a draft.
4. **Hard guardrails.**
   - PRs only — **never a direct commit to `main`** (rides the existing squash-to-main + CI releasability model).
   - **Separate PRs** for mechanical slug bumps vs. prompt re-tunes — a routine bump merges on green while a prompt change gets scrutiny, and a prompt hallucination never blocks a slug fix.
   - **Fixture gate CI-blocking** on both; **human approval required** on every prompt PR.
   - Reuse 19.6's fixture + length-cap + template-allowlist guardrails on any agent-authored prompt.
5. **Configuration surface.** How the maintainer assigns each hermes agent to a role and picks the drafting model — documented, with a default off/opt-in posture.
6. **Runtime provisioning on the hermes host.** Install the `atcr` binary and a working repo checkout (with GitHub auth) on nucleus.lan (`~/docker/hermes/`) so the mechanical `no_agent` cron job can run `atcr models check --json` and open PRs. Pull-before-run so drift checks run against the current personas/lock. Prerequisite for the mechanical agent (item 1).

## Acceptance Criteria

- [ ] AC1: A scheduled mechanical agent runs `atcr models check --json` and opens a slug-bump PR (no LLM) when drift/expiration is detected; the PR passes the CI fixture gate before it can merge.
- [ ] AC2: The judgment step classifies drift as minor (→ mechanical) vs. major/deprecation (→ re-tune task with the vendor guide URL).
- [ ] AC3: The drafting agent produces prompt-edit PRs from the vendor prompting guide, backed by a configurable flat-rate model, opened **separately** from mechanical PRs.
- [ ] AC4: No agent ever commits to `main`; every agent change is a PR; prompt PRs require explicit human approval; mechanical PRs may auto-merge on green per maintainer policy.
- [ ] AC5: Agent-authored prompts pass the reused 19.6 C3 guardrails (fixture gate + length cap + template allowlist) as a hard CI gate.
- [ ] AC6: The role→agent and drafting-model configuration is documented with a safe default (opt-in), and a dry-run/preview mode exists before any PR is opened.

## Components Touched

- Hermes agent configuration (scheduling + role assignment)
- `.github/workflows/` (PR automation, CI fixture gate wiring, auto-merge policy)
- `cmd/atcr` (`models check --json` consumption; any dry-run/report surface)
- nucleus.lan hermes host provisioning (`atcr` binary + repo checkout + `gh` auth on `~/docker/hermes/`)
- `docs/`

## Out of Scope

- **The resolution/lockfile/`models check` primitives themselves** — Epic 19.7 (this epic consumes them).
- **Community prompt submissions / crowd-sourced battle-tested prompts** — a later epic (under discussion); this epic is maintainer-side automation only.
- **Building the hermes agent framework itself** — hermes is already installed and live (nucleus.lan:`~/docker/hermes/`; profile-based agents brian/nolan/marcus/cole running and scheduled, confirmed 2026-07-08). This epic configures those existing agents against atcr's primitives; it never builds or modifies the hermes framework.
- **Auto-merging prompt changes** — always human-reviewed.

## Dependencies

- **Epic 19.7 (Live Model Resolution)** — provides `atcr models check`, the lock, and the drift/expiration/major-bump signals this epic's agents act on.
- **Epic 19.6 (Community Registry Hub)** — provides the C3 guardrails reused to contain agent-authored prompts.

## Hermes Integration Surface (confirmed 2026-07-08 via SSH probe of nucleus.lan)

Resolves the "is hermes external?" question: **hermes is installed and live** at `nucleus.lan:~/docker/hermes/` (all models front a LiteLLM proxy at `192.168.68.109:4000/v1`; cron ticker healthy). Every seam this epic needs already exists — the work is configuration, not construction.

**Agents are profiles** at `data/profiles/<name>/`, each carrying its own `config.yaml` (`model.default` + `provider: custom`), `SOUL.md`, `skills/`, `cron/jobs.json`, and `memories/`. Live agents relevant to this epic:

| Agent | `model.default` | Role fit |
|-------|-----------------|----------|
| brian | `glm-5.1` | ops; holds SSH keys for the whole fleet (`ops/fleet-ops` skill) |
| nolan | `glm-5.2` | senior coding |
| marcus | `openai/qwen-3.7-plus` | senior creative |
| cole | `kimi-k2.7-code` | junior coding (`devops/ssh-key-management` skill) |

These map 1:1 onto the epic's named flat-rate models (Kimi K2.7 / GLM 5.2 / Qwen 3.7). SSH keys live per-agent under `data/.ssh/` (e.g. `id_hermes_brian`) and can be added to any agent.

**Seam → epic role:**

- **Scheduling (mechanical trigger):** `data/profiles/<agent>/cron/jobs.json` — an array of jobs with `schedule.expr` (cron), `prompt`, `skills`, `script`, a **`no_agent`** flag, `deliver`, and `enabled`. The `no_agent: true` + `script` path expresses the deterministic **no-LLM mechanical** run natively: a scheduled `atcr models check --json` → PR-open needs no model. Reference pattern: brian's `fleet-sweep` job (`7 3 * * *`, SSHes the fleet and writes a status file).
- **PR opening / scrutiny:** shared skill `data/skills/github/github-pr-workflow` ("branch, commit, open, CI, merge" via `gh` or git+curl), plus `github-auth`, `github-code-review`, `github-issues`, `github-repo-management`. Agents can open and review PRs today.
- **Model / role assignment:** per-profile `model.default`. **Drafting default → marcus (`qwen-3.7-plus`)** — prompt re-tuning is prose/instruction work (not code), marcus is the prose-tuned "senior creative" profile, and its 1M context ingests a full vendor guide + persona + fixtures in one pass; drafting fires only on major bumps/deprecations so speed is irrelevant. **Fallback → nolan (`glm-5.2`)** if strict schema/template precision matters more than prose quality in practice. Rule out kimi (`kimi-k2.7-code`, coding variant — wrong for prose) and brian (`glm-5.1`, less context). Model stays configurable per the config surface. Judgment (light classification) → brian (`glm-5.1`) or cole (`kimi`); mechanical → a `no_agent` script job (no model).

**Scope consequence:** the "Hermes agent configuration" component lives entirely on nucleus.lan (cron `jobs.json` + a maintenance skill + role assignment) — **not in the atcr git repo**. The atcr-repo footprint of this epic therefore shrinks to `.github/workflows/` (fixture gate + agent-PR auto-merge policy) plus `docs/`; `cmd/atcr` needs nothing beyond 19.7's `models check`. Decompose 19.8 as three tracks: (a) hermes-side config on nucleus.lan (cron jobs + maintenance skill + role assignment), (b) a thin atcr-side CI/docs change, (c) one-time runtime provisioning of `atcr` + repo checkout + `gh` auth on the hermes host (prerequisite for track a's mechanical job).

## Refinements (2026-07-08)

This section records findings from `/refine-epic` run on July 08, 2026 10:45:12AM. It is additive — original plan content above is preserved.

### Auto-applied corrections (0)

None. Every cited component path (`cmd/atcr`, `.github/workflows/`, `docs/`) exists; there were no typo'd paths or mechanically-correctable gaps.

### Items needing user confirmation (1)

- ✅ **RESOLVED 2026-07-08 (SSH-confirmed): hermes is external and already live — configuration only.** The original grep found zero `hermes` footprint in the atcr repo; an SSH probe of nucleus.lan confirmed hermes is installed and running at `~/docker/hermes/` with profile-based agents (brian/nolan/marcus/cole) already scheduled. Framework-build is definitively out of scope. Out of Scope updated to a definite statement; concrete integration seams recorded in the new "Hermes Integration Surface" section above.

### Advisory observations (4)

- ℹ️ **Scope-guard exceeded — but that matches author intent.** Derived TASK_COUNT=6 (mechanical agent, judgment agent, drafting agent, CI/PR guardrails wiring, configuration surface, dry-run/preview) and COMPONENT_COUNT=4 (hermes config, `.github/workflows/`, `cmd/atcr`, `docs/`) exceed `/execute-epic`'s ≤6 tasks / ≤2 components limit. The plan header already declares `Execution: init-plan` and `Tasks/Components: TBD`, so this is expected — route through `/init-plan @.planning/epics/active/19.8_hermes_maintenance_agents.md` for the full sprint pipeline, not `/execute-epic`.
- ℹ️ **Consumed primitives do not exist yet (hard dependency on unbuilt 19.7).** `atcr models check`, `expiration_date`, and the lock have zero code footprint — they appear only in the 19.7/19.8 planning docs. This epic is a wrapper over 19.7's seams and cannot begin until 19.7 lands. Already stated in Dependencies; recorded here as verified.
- ℹ️ **19.6 C3 guardrails claim verified.** The plan's "the guardrails already exist" is accurate on the current branch: fixture gate (`internal/personas/community_fixture_test.go`, `internal/personas/community_schema_test.go`), length caps (`internal/tools/limits.go`), and registry validation (`internal/registry/validate.go`) are all present and reusable.
- ℹ️ **Persona-edit target lives in `internal/personas` / `internal/registry`.** The theme search for the drafting agent's work surfaced these components plus `internal/tools`, none of which are in COMPONENTS_TOUCHED. This is likely correct: the persona `.md` edits land in *agent-authored* PRs (not the epic's own diff), and the 19.6 guardrails are reused reference-only. Confirm the persona `.md` source-file location during `/init-plan` so the drafting agent's PR target is unambiguous.

### Verification context

- Refinement depth: spot-check
- Derived TASK_COUNT: 6 (limit: 6)
- Derived COMPONENT_COUNT: 4 (limit: 2)
- COMPONENTS_TOUCHED: hermes agent configuration, `.github/workflows/`, `cmd/atcr`, `docs/`
- VISUAL_SURFACE: false
- HAS_GATED_WORK: false
- HAS_CROSS_SYSTEM: true (GitHub PR automation + external hermes framework + CI gate; hard dependency on unbuilt 19.7)
- Cited references checked: component paths `cmd/atcr`, `.github/workflows/`, `docs/`; symbols `models check`, `expiration_date`, `hermes`, persona/slug/fixture
- Codebase search queries (spot-check): ["models check / expiration_date primitives", "fixture gate / length cap / template allowlist guardrails", "hermes agent configuration + persona slug"]
