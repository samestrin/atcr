# Tech Debt: Scorecard diagnostics writer-wiring tests (AC3/AC4)

- **Estimated time**: ~1 hour
- **Plan type**: test-remediation
- **Source**: Deferred from `/resolve-td` (Sprint 3.4_scorecard-diagnostics-writer code review). Renumbered from the suggested 3.5 to **3.6** because `3.5_severity-rank-consolidation` already owns 3.5.

## Current State

Epic 3.4 made the scorecard diagnostics sink injectable (`ReadOpts.Writer`, `EmitOpts.Diag`) and added unit tests proving the routing **at the scorecard package layer**:
- `TestEmit_OrphanVerdictDiagnosticRoutesToDiagWriter` (`internal/scorecard/scorecard_test.go:295`)
- the buffer-injection read-path test (`internal/scorecard/scorecard_test.go:322`)

These lock **AC1** (diagnostics route to the injected writer) and **AC2** (the default-to-`os.Stderr` resolution) at the package boundary.

## Pain Points

The **wiring** that connects the real call sites to those injectable fields has **no dedicated test**:

- **AC3 — CLI entry points** pass `cmd.ErrOrStderr()` into the scorecard read/emit paths (the three scorecard-touching commands). Nothing asserts this; a future refactor could swap `cmd.ErrOrStderr()` back to a default writer and no test would fail.
- **AC4 — MCP handler** (`internal/mcp/handlers.go:220`) passes `EmitOpts{Diag: os.Stderr}` on the agentic path. Nothing asserts the handler passes a non-default writer.

This is silent-regression risk: the plumbing can break without any failing test.

## Proposed Solution

Add targeted wiring tests that prove the real call sites pass a non-default writer through to the scorecard layer.

### In Scope

- **AC3 (CLI):** Drive a scorecard command against a deliberately-malformed store and assert the read diagnostic reaches the command's `cmd.ErrOrStderr()` buffer (set via `cmd.SetErr(&buf)`). Cover the three scorecard-touching CLI entry points.
- **AC4 (MCP):** Assert the MCP `handleReconcile` path constructs `EmitOpts` with a non-default `Diag` writer. If/when MCP diagnostics are routed through `e.log` (see the deferred `internal/mcp/handlers.go:220` structured-logging TD), assert against a captured logger buffer instead.

### Out of Scope

- Changing the diagnostics convention itself (the `_, _ = fmt.Fprintf` discard is the intentional, codebase-wide best-effort pattern — confirmed wontfix during the 3.4 TD review).
- Adapting `e.log` into an `io.Writer` shim (tracked separately under the `internal/mcp/handlers.go:220` deferred item).

## Files Affected

| File | Change |
|------|--------|
| `cmd/atcr/scorecard_test.go` (or equivalent CLI test) | Add AC3 wiring assertions for the scorecard CLI entry points |
| `internal/mcp/handlers_test.go` | Add AC4 assertion that the reconcile path passes a non-default `Diag` |

## Acceptance Criteria

- [x] AC1: A CLI-level test drives a scorecard command against a malformed store and asserts the read diagnostic lands in the command's `ErrOrStderr` buffer
- [x] AC2: The three scorecard-touching CLI entry points are each covered by an `ErrOrStderr`-wiring assertion
- [x] AC3: An MCP handler-level test asserts `handleReconcile` passes a non-default `EmitOpts.Diag` (or a captured logger buffer once routed through `e.log`)
- [x] AC4: A deliberate regression (swapping the wired writer back to a default) makes at least one new test fail

## Success Criteria

- [ ] Wiring from CLI + MCP call sites to the injectable scorecard writer is regression-protected by tests
- [ ] `go test ./...` passes

## Risks

| Risk | Mitigation |
|------|------------|
| MCP path may not yet expose `Diag` for assertion | Assert at the construction site; gate the logger-buffer variant behind the `e.log` routing work |
| CLI test coupling to store layout | Use a temp store dir (the existing test pattern pins `EmitOpts.Dir`) |

## Source TD items

- `internal/scorecard/scorecard_test.go:291` — "No test asserts the writer-wiring itself (MCP handler / CLI entry points pass a non-default writer)." Deferred to this plan per `/sprint-clarification` (confidence 75%): AC3 (`cmd.ErrOrStderr()` in CLI) and AC4 (`EmitOpts{Diag: os.Stderr}` in `internal/mcp/handlers.go:220`) have no dedicated coverage; multi-package scope (~60 min) exceeds an inline TD fix.

## References

- Sprint 3.4: `.planning/epics/completed/3.4_scorecard-diagnostics-writer.md`
- Package tests: `internal/scorecard/scorecard_test.go:295,322`

## Clarifications

### Recorded 2026-06-16 (rubber duck — /execute-epic)

**Key Decisions:**
- AC4 (MCP handler wiring) is implemented via **Option B**: add a minimal injectable `diag io.Writer` field to the `engine` struct (defaulting to `os.Stderr`) and change `internal/mcp/handlers.go:220` to pass it as `EmitOpts.Diag`. The handler test injects a `bytes.Buffer` and asserts the scorecard diagnostic lands there, satisfying the "non-default writer" assertion and the deliberate-regression criterion (a swap back to `os.Stderr`/`nil` fails the test).
- The "three scorecard-touching CLI entry points" are confirmed as `leaderboard` (ReadAll, read path), `scorecard` (FindByRunID, read path), and `reconcile` (EmitForReconcile, emit path).
- CLI wiring tests build each command with **separate stdout/stderr buffers** (not the combined `execCmdCapture`) so the assertion proves the diagnostic is routed to `cmd.ErrOrStderr()` specifically, not stdout.

**Scope Boundaries:**
- IN scope: test additions to `cmd/atcr/*_test.go` and `internal/mcp/handlers_test.go`, plus the minimal `engine.diag io.Writer` seam in `internal/mcp/handlers.go` (Option B). The Files Affected table is illustrative; the ~3-line production change is the only path that makes the MCP AC achievable.
- OUT of scope: changing the `_, _ = fmt.Fprintf` best-effort diagnostics convention; adapting `e.log` into an `io.Writer` shim (deferred separately).

**Technical Approach:**
- leaderboard + scorecard: seed a malformed JSONL line in the isolated store, run the command with separate out/err buffers, assert the "skipping malformed record" diagnostic is in the err buffer and absent from stdout.
- reconcile: drive `atcr reconcile` over a fixtureReview, force a scorecard write-failure by making the resolved store path a regular file (so `Append`'s `MkdirAll` fails), and assert the "scorecard: write failed" diagnostic reaches the command's err buffer.
- MCP: `engine` gains `diag io.Writer`; a `diagWriter()`-style resolution defaults `nil` to `os.Stderr`. Handler passes the resolved writer to `EmitOpts.Diag`.

**Original Questions and Answers:**
1. Q: For the MCP handler test (AC3/AC4), strictly test-only construction-site assertion (Option A) or a minimal injectable `diag io.Writer` seam on the engine (Option B)?
   A: Option B. AC4 explicitly requires asserting a "non-default writer," which Option A cannot satisfy (it only proves the os.Stderr default and cannot catch a regression to nil/io.Discard, both of which resolve back to os.Stderr inside the scorecard package). The Risk note's "assert at the construction site" maps to the injection point; the ~3-line production change is narrower than the deferred e.log shim and is the only path that makes the deliberate-regression AC achievable. (Confidence 88%)

---

**Merge Commit:** 9626b718b332651b60b0c4e13da22c6d4d66d7e4
