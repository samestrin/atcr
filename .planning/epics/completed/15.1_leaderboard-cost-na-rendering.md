# Tech Debt: Distinguish free vs paid-but-uncorroborated `cost_per_corroborated_finding_usd`

- **Estimated time**: 1 week
- **Tasks/Components**: 4/2

## Current State

`scorecard.PublicRecord.CostPerCorroboratedFindingUSD` is a plain non-pointer `float64` with no `omitempty` tag (internal/scorecard/export.go:40). Both the production leaderboard `--export` path and the benchmark scorer therefore emit `0.0` in two very different situations:

1. The reviewer genuinely cost $0 (free/stub model).
2. The reviewer cost real money but matched zero planted/defect categories.

Because the field has no absent/N/A state, these cases are byte-identical on the public leaderboard, making a paid-but-ineffective reviewer appear as best-in-class efficient.

## Pain Points

### Leaderboard Misreading
- `cost_per_corroborated_finding_usd == 0.0` is ambiguous: it could mean "free" or "expensive but useless".
- Readers who look only at cost efficiency cannot distinguish the two without cross-referencing `corroboration_rate` and `findings_raised_avg`.

### Schema Freeze Conflict
- Epic 10.2 froze `scorecard.PublicRecord` for byte-compatibility with production.
- Any change to the field type (pointer, omitempty, or companion flag) is a schema change that must be applied to both production and benchmark paths together, not patched locally into the benchmark scorer.

## Evidence

### TD item driving this plan:
- internal/benchmark/score.go:115-117 — "scoreOne sets CostPerCorroboratedFindingUSD only when matchedFindings>0. A reviewer with real CostUSD>0 but zero matched planted categories reports 0 — byte-identical to a genuinely free reviewer."

### Existing parity precedent:
- `internal/scorecard/export.go:272-274` — `costPer(totalCost, corroborated)` returns `0` when `corroborated <= 0`.
- `internal/benchmark/score.go:115-117` — the benchmark scorer uses `if matchedFindings > 0 { ... }`, producing the same `0.0`.
- `internal/scorecard/export_test.go:121` already asserts "no corroborated findings => 0, never Inf/NaN", confirming `0.0` is the intended sentinel today.
- `internal/scorecard/export.go:39` — `SurvivedSkepticRate *float64` with `omitempty` already uses the identical pattern (absent key = no data, present key = real value including 0.0) for a different field on the same struct.

## Proposed Solution

**Decided:** change `CostPerCorroboratedFindingUSD` to `*float64` with `omitempty` on `scorecard.PublicRecord`. A nil pointer omits the JSON key (absent = "no corroborated findings, cost-per is undefined"); a non-nil pointer carries the real value, including a genuine `0.0` should that ever occur. This mirrors `SurvivedSkepticRate *float64` (`internal/scorecard/export.go:39`), which already uses this exact pattern on the same struct to distinguish "no verification ran" (key absent) from "verification ran, rate is 0" (key present, value 0.0) — the identical shape of ambiguity this epic addresses for cost-per-corroborated.

Options considered:

1. **Chosen.** Change `CostPerCorroboratedFindingUSD` to `*float64` with `omitempty`. Consistent with the `SurvivedSkepticRate` precedent already on this struct; the field is genuinely optional data, not always-present.
2. Add a companion enum/boolean field (e.g., `cost_per_corroborated_finding_usd_available`) to disambiguate. Rejected: introduces a second convention for the same "value is optional" problem `SurvivedSkepticRate` already solves with a pointer — unnecessary duplication of an idiom the schema already has.
3. Keep `0.0` in the raw JSON but update the leaderboard UI/rendering layer to display "N/A" when `corroboration_rate == 0 && findings_raised_avg > 0`. Rejected: no in-repo renderer consumes the exported `PublicRecord` JSON for human display (see Files Affected) — this option has no addressable implementation target in this codebase.

## Scope

### In Scope
- Change `scorecard.PublicRecord.CostPerCorroboratedFindingUSD` to `*float64` with `omitempty` in `internal/scorecard/export.go` (Option 1, see Proposed Solution — decided, not open).
- Update production export path (`internal/scorecard/export.go`) to emit the new representation.
- Update benchmark scorer (`internal/benchmark/score.go`) to match production exactly.
- Update field-reference documentation (`docs/scorecard.md:242`, `docs/benchmark.md:128`) to describe the new absent/N/A representation.
- Update tests in both scorecard and benchmark packages.

### Out of Scope
- Changing benchmark scorer behavior before the schema decision is made.
- Altering other PublicRecord fields.
- New leaderboard features beyond the cost-per ambiguity fix.

## Files Affected

| File | Role |
|------|------|
| `internal/scorecard/export.go` | Production cost-per computation and PublicRecord schema |
| `internal/scorecard/export_test.go` | Existing parity tests |
| `internal/benchmark/score.go` | Benchmark cost-per computation |
| `internal/benchmark/score_test.go` | Benchmark scorer tests |
| `docs/scorecard.md` | Field-reference table entry (line 242) for `cost_per_corroborated_finding_usd` — must reflect the new absent/N/A representation |
| `docs/benchmark.md` | Same field-reference table (line 128) in the benchmark suite output doc |

## Acceptance Criteria

- [ ] AC1: `scorecard.PublicRecord` supports an explicit N/A/absent state for `cost_per_corroborated_finding_usd`.
- [ ] AC2: Production export emits the new N/A state when `cost > 0 && corroborated == 0`.
- [ ] AC3: Benchmark run emits the identical N/A state for the same condition.
- [ ] AC4: All existing scorecard and benchmark tests are updated and pass.
- [ ] AC5: `docs/scorecard.md` and `docs/benchmark.md` field-reference tables document the new absent/N/A representation for `cost_per_corroborated_finding_usd`, so a reader consulting either doc can distinguish "free" from "paid, caught nothing".

## Success Criteria

- [ ] No paid-but-uncorroborated reviewer is rendered identically to a free reviewer.
- [ ] Production and benchmark outputs remain byte-compatible where they were previously byte-compatible.
- [ ] No regressions in existing scorecard or benchmark test suites.

## Risks

| Risk | Mitigation |
|------|------------|
| Schema change breaks downstream consumers | Choose the least-breaking option; add migration note to CHANGELOG |
| Production and benchmark drift apart | Update both paths in the same PR; add a shared helper if possible |
| Documentation drifts from the new schema | Update `docs/scorecard.md` and `docs/benchmark.md` in the same PR as the schema change (AC5) |

## Rollback Plan

1. Revert the schema change and any documentation updates.
2. Restore the previous `0.0`-only behavior.
3. Re-open this epic for re-evaluation.

## References

- `internal/scorecard/export.go:40` — `CostPerCorroboratedFindingUSD` field definition
- `internal/scorecard/export.go:272-274` — `costPer` helper
- `internal/benchmark/score.go:115-117` — benchmark cost-per guard
- `.planning/technical-debt/README.md:62` — source TD row

## Source TD Items

- **Q1** `[internal/benchmark/score.go:109]` — "scoreOne sets CostPerCorroboratedFindingUSD only when matchedFindings>0..."  
  **Rationale:** The disambiguating signal already exists in sibling columns; the correct home for a real fix is the public-schema/leaderboard-rendering layer, applied to production and benchmark together, not patched divergently into the benchmark scorer. Epic 15.3 is already completed, so a new 15.x sub-plan is required.

## Refinements (2026-07-02)

This section records findings from `/refine-epic` run on July 02, 2026 08:42:17AM. It is additive — original plan content above is preserved.

### Auto-applied corrections (0)

None — no mechanical corrections met the SAFE_AUTOFIX bar.

### Items needing user confirmation (4)

**All 4 applied 2026-07-02** — user reviewed the recommendations and directly approved applying them to the plan body above (Proposed Solution, Scope, Files Affected, AC5, Risks, Rollback Plan, Evidence, References). Left below for provenance/audit trail.

⏸️ **1. Components mismatch — "Leaderboard/UI renderer" has no concrete file target.**
The plan header claims `Tasks/Components: 4/2`, but the "Leaderboard/UI renderer" row (Files Affected), AC5, and the Risks table all point at a third area with no named file. Investigation: the only in-repo human-readable renderer for a comparable ambiguity is `cmd/atcr/leaderboard.go`'s `renderLeaderboard()`, which operates on the **unrelated** `scorecard.LeaderboardRow` / `scorecard.Aggregate()` path — not `PublicRecord` / `Export()`. That renderer already solved an analogous ambiguity in a prior epic via `LeaderboardRow.HasCostPerCorroborated` (`internal/scorecard/aggregate.go:151-158`), rendering `-` instead of `$0.0000` whenever `FindingsCorroborated == 0` (`cmd/atcr/leaderboard.go:135-138`). There is no in-repo renderer for the exported `PublicRecord` JSON itself — the actual human-readable surface for this schema is the field-reference tables in `docs/scorecard.md:242` and `docs/benchmark.md:128`, which document today's `0.0`-when-none-corroborated semantics and will go stale once the schema changes, but neither doc file is listed in Files Affected.
Suggested action: replace the "Leaderboard/UI renderer" row with `docs/scorecard.md:242` + `docs/benchmark.md:128` and rescope AC5 to a documentation update — this keeps COMPONENT_COUNT at 2 (`internal/scorecard` + `internal/benchmark`), matching the plan's own claim. If genuine renderer code is intended instead, name the exact file/function; that would raise COMPONENT_COUNT to 3, exceeding `/execute-epic`'s ≤2-component scope guard (see INFO note below).

⏸️ **2. Risks table references a "frontend owner" that doesn't exist in this codebase.**
Row 3 of the Risks table reads: "Leaderboard UI not updated | Coordinate with frontend owner; add a dedicated AC." No `frontend/`, `web/`, or `site/` directory exists anywhere in this repository — this is a solo-maintained CLI tool with no separate frontend team.
Suggested action: remove this risk row, or rewrite it as "Coordinate doc updates in `docs/scorecard.md` and `docs/benchmark.md`" consistent with item 1's rescoping.

⏸️ **3. Hidden gated work — the schema-shape decision is unresolved.**
"Proposed Solution" lists 3 mutually-exclusive options (pointer+`omitempty`, companion bool field, or render-layer-only) and states "the preferred option should be chosen after weighing backward compatibility against clarity" — an undecided design gate, not flagged as `HAS_GATED_WORK`. `/execute-epic`'s linear TDD loop expects T1 ("Schema decision and change") to already have a concrete target before a RED→GREEN cycle can start.
Suggested action: decide the option now and rewrite T1 concretely — recommend Option 1 (`*float64` + `omitempty`), since Epic 10.0's `SurvivedSkepticRate` (`internal/scorecard/export.go:39`) already establishes exactly this pattern as in-repo precedent — or explicitly flag `HAS_GATED_WORK` and run the decision as a standalone step before `/execute-epic`.

⏸️ **4. Stale line citations for the benchmark cost-per guard.**
Cited twice, both drifted (comments were added elsewhere in the file since these citations were written): the Evidence section cites `internal/benchmark/score.go:109` and the References section cites `internal/benchmark/score.go:118-120`. Verified against the live file: line 109 is now a closing `}` (end of the inner category-match loop), and lines 118-120 are `return pr` / `}` (func close) / blank — neither location is the guard. The actual guard is:
```go
115: if matchedFindings > 0 {
116:     pr.CostPerCorroboratedFindingUSD = r.CostUSD / float64(matchedFindings)
117: }
```
Suggested action: update both citations to `internal/benchmark/score.go:115-117`.

### Advisory observations (2)

ℹ️ **Scope-guard risk (contingent).** If item 1's "Leaderboard/UI renderer" line resolves to real code changes in `cmd/atcr` rather than the docs-only rescoping recommended above, COMPONENT_COUNT becomes 3, exceeding `/execute-epic`'s ≤2-component limit — the plan would then need `/init-plan` instead.

ℹ️ **Related precedent already in the codebase.** `internal/scorecard/aggregate.go`'s `LeaderboardRow.HasCostPerCorroborated` already solved an analogous ambiguity for the local `atcr leaderboard` table (renders `-` instead of `0.0000` whenever `FindingsCorroborated == 0`, whether the group was free or paid-but-uncorroborated). This is a directly relevant working example the plan should reference when making the schema decision in item 3 — it demonstrates the "companion boolean" shape (Option 2) already shipped and tested (`internal/scorecard/aggregate_test.go`).

### Verification context

- Refinement depth: spot-check
- Derived TASK_COUNT: 4 (limit: 6)
- Derived COMPONENT_COUNT: 2 (limit: 2) — see item 1 for the contingent 3rd-component risk
- COMPONENTS_TOUCHED: internal/scorecard, internal/benchmark
- VISUAL_SURFACE: false
- HAS_GATED_WORK: true (see item 3)
- HAS_CROSS_SYSTEM: true (`PublicRecord` is a frozen public schema per Epic 10.2, consumed outside this repo)
- Cited references checked: 9 (4 file paths, 5 file:line/symbol citations)
- Codebase search queries (spot-check): ["leaderboard rendering human-readable table", "PublicRecord export schema cost per corroborated", "reviewerAcc benchmark score aggregation"]

---

**Merge Commit:** ce0853a315eb7d9284147cbf4c49017282791343
