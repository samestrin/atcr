date: "2026-06-18"
source_type: Sprint
label: epic-4.2
items:
  - group: U
    status: deferred
    severity: LOW
    file: internal/registry/attribution.go:55
    problem: attribute() recurses and reconstructs the join even for a single-error errors.Join (which still satisfies Unwrap() []error), a small avoidable cost on the load-time validation path
    fix: 'Optionally short-circuit when len(children)==1 if profiling ever flags it (WON''T-FIX 2026-06-18: trigger unmet — error-path only, never hit on normal load; no perf AC in epic 4.2)'
    category: INTEGRATION
    est_minutes: 5
    source: execute-epic-independent
