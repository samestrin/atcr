date: "2026-06-18"
source_type: Sprint
label: epic-4.3
items:
  - group: "1"
    status: deferred
    severity: LOW
    file: internal/validation/validation.go:62
    problem: 'System-dir denylist covers only /etc, /proc, /sys, leaving /boot, /dev, /root, /var and ~/.ssh writable via --output-dir / --output (deliberate Option-B permissive choice) (Won''t-fix: intended Option-B policy per epic 4.3 clarifications 2026-06-18; revisit only on a concrete isolation requirement)'
    fix: If stronger isolation is later required, switch to an allowlist anchored at the repo/.atcr root instead of a denylist
    category: SECURITY
    est_minutes: 30
    source: execute-epic-independent
  - group: "1"
    status: deferred
    severity: LOW
    file: internal/validation/validation.go:86
    problem: 'Severity and Enum validators are shipped but wired to nothing (ParseSeverity/ValidFormat remain the live paths); they exist only to satisfy AC5/AC7 and future use (Won''t-fix: intentionally public for AC5/AC7 per epic 4.3 clarifications; deletion breaks ACs, wire-in out of scope)'
    fix: Revisit and delete if no caller adopts them within a release, or wire them in where duplication can be removed
    category: OVER_ENGINEERING
    est_minutes: 15
    source: execute-epic-independent
