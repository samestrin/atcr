# Technical Debt Tracking

This file is a staging area for small technical debt items discovered during development. Items are triaged and moved to individual sprint documents in `sprints/` as they are prioritized.

## Stats

| Severity | Open | Deferred | Resolved |
|----------|------|----------|----------|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 0 | 0 | 22 |
| MEDIUM | 0 | 3 | 30 |
| LOW | 0 | 7 | 57 |


**Last Modified:** 2026-06-16 | **Open Items:** 0 | **Deferred Items:** 10 | **Resolved Items:** 109 | **Total Items:** 119

## Directory Structure

```
technical-debt/
├── README.md                    # This file (staging area)
├── CLAUDE.md                    # AI assistant guidelines
└── sprints/
    ├── active/                  # Currently being addressed
    ├── pending/                 # Prioritized, not yet started
    └── completed/               # Resolved items
```

## How to Use

1. **Small items**: Add to this README under "Staging Area" below
2. **Larger items**: Create a new document in `sprints/pending/`
3. **During sprint planning**: Move items from pending to active
4. **After resolution**: Move items from active to completed


### [2026-06-15] From Sprint: 3.3_per_run_scorecard

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| Solo | [x] | HIGH | internal/fanclient/client.go:86-95 | Cost is not being computed from provider responses. The `ComputeCostUSD` function is not called, so `cost_usd` will always be zero in the scorecard emitter. | security | 15 | 0 | code-review | archer | MEDIUM |
| 5 | [x] | HIGH | cmd/atcr/leaderboard.go:51 | Missing validation for --output flag combination (disagreement: MEDIUM vs HIGH) | Add explicit check and error for --output without --export | correctness | 5 | code-review | bruce | MEDIUM |
| 5 | [x] | HIGH | cmd/atcr/scoreboard.go:104-123 | Corrupted JSONL lines not handled | Skip malformed lines with warning, continue processing | error-handling | 5 | code-review | bruce | MEDIUM |
| 5 | [x] | HIGH | cmd/atcr/scorecard.go:104 | Path traversal possible via reviewDir argument (disagreement: MEDIUM vs HIGH) | Validate path is within expected bounds before reading | security | 5 | code-review | bruce | MEDIUM |
| 5 | [x] | MEDIUM | cmd/atcr/leaderboard.go:121 | Duplicate error message on export no-match | Remove pre-print or wrap error to suppress cobra's default printing | error-handling | 5 | code-review | bruce | MEDIUM |
| 5 | [x] | MEDIUM | cmd/atcr/leaderboard.go:128 | On empty-store export, Export returns ErrNoExportRecords; runLeaderboardExport prints the canonical guidance to stderr via Fprintln AND returns the same err, so main.go:20 prints "atcr: no records match the export filters" to stderr a second time. The user sees two stderr lines for one condition. | Return a sentinel that satisfies ExitCode()==1 but whose Error() is suppressed, or print guidance and return a codedError wrapping a nil-message error so main() skips the second print. Verify: run leaderboard --export on an empty store, assert exactly one stderr line and exit 1. | error-handling | 30 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | cmd/atcr/leaderboard.go:33 | --since defaults to "30d" with no way to disable the window, so leaderboard --export always silently truncates the public submission to the last 30 days even when the user expects "export everything". The only signal is the easy-to-miss filters.since field in the envelope. | Accept --since all (or 0) as an explicit no-window sentinel in ParseSince, or print a stderr note when the default 30d window is applied to an export. Verify by exporting a store with records older than 30 days. | error-handling | 30 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | cmd/atcr/leaderboard.go:70 | The edge-case contract says empty/missing store is a graceful "No records found" exit 0; the table path honors this at :74, but the --export branch at :70 runs before that check, so leaderboard --export on a brand-new install with zero records returns ErrNoExportRecords (exit 1) with a misleading "widen --since" message. Exit code contradicts the documented empty-store-is-exit-0 contract for the same store state. | In runLeaderboardExport distinguish len(records)==0 (empty store) from filtered-to-nothing, emit a distinct "no data yet, run reconcile" message, and decide+document whether empty-store export is exit 0 or 1. Verify with a zero-record store. | error-handling | 30 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | cmd/atcr/reconcile.go:107 | Two overlapping threshold resolvers exist: failOnThreshold (flag-only) and resolveGateThreshold/validateGate (config-precedence, used at :38). failOnThreshold appears to be dead code in reconcile.go (only resolveGateThreshold is invoked), and the duplication invites the two gates to drift in validation semantics — the exact "two layers fork" risk the comments claim to prevent. | Confirm failOnThreshold's only caller (grep -rn failOnThreshold cmd/); if it belongs to the review command, move it there or collapse both through one validation helper. Rebuild to confirm no unused-function lint. | maintainability | 30 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | cmd/atcr/scorecard.go:114 | runIDFromReviewDir reconstructs run_id as s.ReconciledAt + "-" + filepath.Base(clean) with no validation. If summary.json reconciled_at is malformed (corrupt/hand-edited), the bad run_id flows into FindByRunID/monthFromRunID which returns a generic "cannot derive month" error that leaks the reconstructed run_id and surfaces as "failed to read scorecard store" rather than a corrupt-summary diagnosis. (disagreement: LOW vs MEDIUM) | For the non-IsNotExist branch return a sanitized error that does not %w the PathError (e.g. fmt.Errorf with arg only). Verify by triggering a permission error on summary.json and asserting the resolved path is absent from stderr. | error-handling | 30 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | cmd/atcr/leaderboard.go:103 | renderLeaderboard discards every tabwriter Fprintln/Fprintf error and the tw.Flush() error (_,_=), propagating only the final w.Write; same in renderScorecard (scorecard.go:182). A tw.Flush() error would silently emit a malformed/truncated buffer. Low impact (tabwriter-to-bytes.Buffer effectively never errors) but the discarded Flush error is a latent hole the comment hand-waves. | Capture and propagate tw.Flush()'s error before w.Write(buf.Bytes()). | maintainability | 15 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | cmd/atcr/leaderboard.go:147 | writeExportFile accepts a fully user-controlled --output path and os.MkdirAll(dir,0700)+atomic rename. A symlink at the target path is replaced by the rename (TOCTOU on the os.Stat dir check). Blast radius is the user's own perms (local CLI), but the dir check is a non-atomic guard. | Acceptable for a local CLI; if hardening is desired use O_NOFOLLOW on the final target or document that --output follows symlinks. Verify by pointing --output at a symlink. | security | 30 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | cmd/atcr/reconcile.go:53 | runReconcile reads args[0] into arg with no strings.TrimSpace, unlike runScorecard (scorecard.go:39) which trims. A trailing-whitespace or quoted-blank arg is passed raw to resolveReviewDir, becoming a confusing path-resolution failure instead of a clean usage error. | Trim arg in reconcile.go for parity with scorecard and treat trimmed-empty as the default-anchor path. Verify with reconcile " ". | maintainability | 15 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | cmd/atcr/scorecard.go:188 | formatPercent computes int(rate*100+0.5) with no clamping, while the export path clamps via clampRate. A corrupt record with CorroborationRate or SurvivedSkepticRate outside [0,1] renders nonsense (1500%) or masks a negative as 0%. The table read path trusts the stored rate; inconsistent defensiveness vs export. | Clamp rate to [0,1] in formatPercent (or at the render boundary) to match export's clampRate. Verify with a record carrying corroboration_rate:5.0 asserting 100% not 500%. | error-handling | 15 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | cmd/atcr/scorecard.go:55 | Error from FindByRunID wrapped incorrectly | Remove redundant error wrapping or fix exit code mapping | error-handling | 2 | code-review | bruce | MEDIUM |
| 5 | [x] | LOW | cmd/atcr/scorecard.go:94 | looksLikePath treats any arg containing / or \\ as a path with no run_id-shape fallback: if resolution fails with IsNotExist the arg is never retried as a run_id, so a slash-bearing run_id-shaped arg fails with a confusing "no reconciled/summary.json" error instead of an invalid-run_id message. | When looksLikePath is true but path resolution fails IsNotExist AND the arg satisfies IsRunID, fall back to treating it as a run_id (or name both interpretations in the error). | error-handling | 15 | code-review | claude | MEDIUM |
| 6 | [x] | HIGH | internal/fanout/artifacts.go:107-127 | ReadPoolSummary missing for scorecard emitter | Add ReadPoolSummary to read pool/summary.json | correctness | 5 | code-review | bruce | MEDIUM |
| 6 | [x] | HIGH | internal/fanout/engine.go:125-142 | Single-shot path missing usage capture | Add UsageCompleter type assertion and token capture | correctness | 5 | code-review | bruce | MEDIUM |
| 6 | [x] | HIGH | internal/fanout/engine.go:134-137,chat.go:113 | Usage data is not being decoded from the provider response. The `usage` block is dropped silently, leading to empty `tokens_in` and `tokens_out` fields in the scorecard emitter. | security | 10 | 0 | code-review | archer | MEDIUM |
| 6 | [x] | HIGH | internal/fanout/loop.go:128,284 | Tool-loop token accumulation incomplete | Extend `internal/fanout/engine_e2e_test.go` with a tool-enabled agent whose fake ChatCompleter returns non-zero per-turn usage; assert the persisted `status.json` records the summed `tokens_in`/`tokens_out` and the model. | testing | 10 | execute-sprint | bruce | MEDIUM |
| 6 | [x] | HIGH | internal/fanout/status.go:286-296 | AgentStatus missing usage fields | Add Model, TokensIn, TokensOut to AgentStatus struct | correctness | 5 | code-review | bruce | MEDIUM |
| Solo | [x] | MEDIUM | internal/fanout/artifacts.go:226 | statusFor persists Model/TokensIn/TokensOut only when r.TokensIn>0 OR r.TokensOut>0, so a successful zero-token run (fully cached prompt + empty/refusal completion) loses its Model attribution in status.json — contradicting the "model recorded so a $0 cost remains auditable" comment. It also disagrees with the in-memory Result.Model (engine.go:435) which is always set, giving two different answers to "is the model recorded". | Gate Model persistence on r.Status==StatusOK (or r.Model != "") rather than on token counts so a successful run always records which model answered even at zero tokens; keep the token fields omitempty to preserve byte-identical legacy output. Won't-fix: byte-identical status.json is intentional; a zero-token run prices to $0 regardless of model (rates.go), so recording Model adds no auditability and risks phantom leaderboard rows (2026-06-16). | maintainability | 15 | code-review | claude | MEDIUM |
| 6 | [x] | LOW | internal/fanout/loop.go:128,284 | Tool-loop token accumulation (`addUsage` after every `Chat()` turn plus the final-answer call) is un | Extend `internal/fanout/engine_e2e_test.go` with a tool-enabled agent whose fake ChatCompleter returns non-zero per-turn usage; assert the persisted `status.json` records the summed `tokens_in`/`tokens_out` and the model. | testing | 10 | execute-sprint | execute-sprint | MEDIUM |
| 6 | [x] | LOW | internal/fanout/status.go:349 | atomicWriteFile chmods the temp to 0644 before rename, so status.json — now carrying model, tokens_in, tokens_out cost-bearing metadata added this sprint — is world-readable. On a multi-user host any local user can read which models/costs a review incurred. Token counts are not secrets, but the sprint widened a world-readable artifact without revisiting the mode. Pre-existing mode, sprint-added data. | No change if single-user (documented assumption). If multi-user matters, set pool dir 0700 and file 0600. Noted because the sprint added cost data to the file. Won't-fix: single-user assumption accepted per AC 01-03; token counts are not secrets (2026-06-15). | security | 5 | code-review | claude | MEDIUM |
| 7 | [x] | HIGH | internal/llmclient/chat.go:128 | Chat tool response usage is not being captured. The `Usage` field in `chatToolResponse` is not populated from the provider response, leading to empty `tokens_in` and `tokens_out` fields in the scorecard emitter. | security | 10 | 0 | code-review | archer | MEDIUM |
| 7 | [x] | HIGH | internal/llmclient/chat.go:86 | Chat response usage is not being captured. The `Usage` field in `ChatResponse` is not populated from the provider response, leading to empty `tokens_in` and `tokens_out` fields in the scorecard emitter. | security | 10 | 0 | code-review | archer | MEDIUM |
| 7 | [x] | HIGH | internal/llmclient/client.go:188-191 | Cost is not being computed from provider responses. The `CompleteWithUsage` function is not called, so `cost_usd` will always be zero in the scorecard emitter. | security | 15 | 0 | code-review | archer | MEDIUM |
| 7 | [x] | MEDIUM | internal/llmclient/chat.go:113 | chatToolResponse decodes usage at the top level only and the fanout loop SUMS resp.Usage per turn (loop.go:128). A provider/gateway that reports CUMULATIVE usage on every turn (some Anthropic-via-gateway shims) would be N-counted across the multi-turn loop — the opposite failure of TD-002. The per-turn-incremental assumption is silently trusted and undocumented. | Document the per-turn-incremental assumption as a hard contract at the decode site; if cumulative-reporting gateways are in scope, detect monotonic-increasing usage and diff instead of sum. At minimum add a guard/test asserting the assumption. | maintainability | 60 | code-review | claude | MEDIUM |
| 7 | [x] | MEDIUM | internal/llmclient/chat.go:85-93 (ChatResponse.Usage); internal/fanout/loop.go:116,273 | `ChatResponse.Usage` is per-turn. | add a usage accumulator on the fanout `Result` (sum `resp.Usage` after each `Chat()` in the loop and the final-answer call) before the scorecard emitter reads per-reviewer totals. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [x] | MEDIUM | internal/llmclient/client.go:151 | clampNonNegative does int(v) on a float64 after checking v<0 on the float, not the resulting int. A provider emitting an absurd prompt_tokens (1e20, or +Inf via 9e999 from json.Number.Float64()) overflows the int conversion to implementation-defined garbage that bypasses the negative clamp and flows into token sums and cost. | After the v<0 check also reject math.IsInf(v)/math.IsNaN(v) and cap v at a sane token ceiling (or math.MaxInt) before int(v). Return 0 on out-of-range to match the degrade-to-zero contract. | error-handling | 30 | code-review | claude | MEDIUM |
| 7 | [x] | MEDIUM | internal/llmclient/rates.go:19 | `modelRates` keys are bare model ids (e.g. `claude-sonnet-4-6`), but real-world model ids in use carry suffixes or provider prefixes (`claude-opus-4-8[1m]`, `anthropic/claude-...`, `us.anthropic.claude-...`). | normalize the model key before lookup (strip `[...]` suffix and known provider prefixes), or emit a one-time debug signal when a non-empty model misses the table so silent zero-cost is observable. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [x] | LOW | internal/llmclient/chat.go:157 | Chat's no-choices early guard returns before usage is read, so a provider that returns usage on an error-shaped 200 (no choices) but was billed for the call has its usage silently discarded, understating cost on billed-but-empty turns. | Acceptable if billed-failed turns are rare; document it, or capture usage before the no-choices early return so cost reflects provider-billed tokens. | testing | 15 | code-review | claude | MEDIUM |
| 7 | [x] | LOW | internal/llmclient/client.go:134 | UsageData.UnmarshalJSON swallows ALL unmarshal errors and returns nil (degrade to zero), but struct-level unmarshal fails atomically: a usage block with a valid prompt_tokens but a junk completion_tokens drops BOTH to zero, understating cost when one field was recoverable. | Decode the two fields independently (map[string]json.RawMessage or per-field json.Number with per-field tolerance) so one bad field does not discard the other. | error-handling | 30 | code-review | claude | MEDIUM |
| 7 | [x] | LOW | internal/llmclient/client.go:338 | Key redaction on error bodies uses strings.ReplaceAll(snippet,key,"[redacted]") — an exact-match scrub. If a provider echoes the key URL-encoded, base64'd, truncated, or case-folded, the literal key substring will not match and the raw secret leaks into HTTPStatusError.Snippet, now also on the usage/cost error path. Pre-existing, not sprint-introduced. | Defense-in-depth: also scrub a URL-encoded and prefix form of the key, or generically redact Bearer\s+\S+ and sk-\S+ shaped tokens in the snippet. | security | 30 | code-review | claude | MEDIUM |
| 7 | [x] | LOW | internal/llmclient/rates.go:51 | The cost formula divides each token count by 1_000_000 as a separate float64 multiply-and-add, rounding the division before multiplying and accumulating IEEE error across two terms; summed per-reviewer (scorecard.go:172) this compounds into a non-deterministic least-significant digit serialized to JSON as e.g. 0.30000000000000004. | Compute as (float64(tokensIn)*rate.input + float64(tokensOut)*rate.output)/1_000_000 (one division), and round cost_usd to a fixed number of decimals at the scorecard boundary for stable output. | error-handling | 30 | code-review | claude | MEDIUM |
| 8 | [x] | HIGH | internal/scorecard/aggregate.go:175-180 | Offset-form timestamps silently excluded from --since | Use RFC3339 regex that tolerates both Z and offset forms | correctness | 5 | code-review | bruce | MEDIUM |
| 8 | [x] | HIGH | internal/scorecard/export.go:175-180 | Email addresses not stripped from exported fields | Rewrite scrubField to strip paths not preceded by alphanumeric | security | 10 | code-review | bruce | MEDIUM |
| 8 | [x] | HIGH | internal/scorecard/export.go:238 | scrubAbsPath pattern (^ or non-alnum)/\S* only strips a /-run when the byte before / is non-alphanum | Do not rely on the preceding-byte guard alone. Strip any token containing a known path root (/etc/, /Users/, /home/, /var/, /tmp/) or >=3 slash-separated segments before the provider-prefix allowance, OR validate model strings against a known-model regex instead of scrubbing arbitrary content. Add a unit case for "host/etc/passwd". | security | 60 | code-review | claude | MEDIUM |
| 8 | [x] | HIGH | internal/scorecard/paths.go:19-21 | Month regex accepts impossible months | Constrain monthRe to 01-12 | correctness | 5 | code-review | bruce | MEDIUM |
| 8 | [x] | HIGH | internal/scorecard/paths.go:31-35 | Loose run_id validation accepts bare month prefix | Add RFC3339 'T' check to IsRunID | correctness | 5 | code-review | bruce | MEDIUM |
| 8 | [x] | HIGH | internal/scorecard/scorecard.go:116-120 | Double-logging on resolveDir failure | Remove error return, keep only stderr log | error-handling | 5 | code-review | bruce | MEDIUM |
| 8 | [x] | HIGH | internal/scorecard/store.go:17-23 | Atomicity rationale cites wrong invariant (PIPE_BUF) | Rewrite FindByRunID to union matches across boundary-adjacent months | maintainability | 10 | code-review | bruce | MEDIUM |
| 8 | [x] | HIGH | internal/scorecard/store.go:63 | ReadRecords never inspects r.SchemaVersion. The package doc (scorecard.go:19-22) and the design risk promise "leaderboard handles version negotiation", but a record with schema_version:2 is unmarshaled into the v1 struct and silently aggregated as if v1. A future field rename/semantic change corrupts leaderboard totals with no skip and no warning. This is the same gap flagged INCOMPLETE in Story 03 / AC03-05. (disagreement: MEDIUM vs HIGH) | In ReadRecords after unmarshal, branch on r.SchemaVersion: accept ==SchemaVersion, skip-with-stderr-warn anything greater (forward-incompatible), and route older versions through an explicit migration shim. At minimum warn-and-skip unknown versions so they cannot silently pollute aggregates. Add a table test with a schema_version:2 record. | error-handling | 60 | code-review | claude | MEDIUM |
| 8 | [x] | MEDIUM | internal/scorecard/aggregate.go:102 | Three different run_id parsing strategies coexist for one canonical format: ApplyFilters parses via | Centralize run_id parsing in one function returning (time.Time, ok) and derive month, day, and window-membership from it everywhere. Delete the positional slicing paths. | maintainability | 60 | code-review | claude | MEDIUM |
| 8 | [x] | MEDIUM | internal/scorecard/paths.go:44 | IsRunID has 0.0% test coverage (confirmed via go tool cover). It is the lookup-path validation gate at cmd/atcr/scorecard.go:86 credited in the risk profile as the traversal guard, yet neither the rejecting branch nor the T-required branch is exercised; a regression loosening it would ship green. Note the actual traversal block lives in monthFromRunID's prefix validation, so the IsRunID doc comment overstates its role. | Add table tests for IsRunID (valid RFC3339-prefixed true; bare month false; bad month false; short false; T-bearing true) in a paths_test.go, plus a negative test asserting a traversal-suffixed run_id resolves to month only. Tighten the doc comment to credit monthFromRunID. | testing | 15 | code-review | claude | MEDIUM |
| 8 | [x] | MEDIUM | internal/scorecard/store.go:100 | File permissions are not being set correctly. The `Append` function is not setting the file permissions to `0o600`, so the scorecard files may be readable by other users. | security | 10 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | MEDIUM | internal/scorecard/store.go:139 | monthsToScan reads runID[8:10] as day-of-month, only guarding len<10. monthFromRunID validates just | Parse the day from the validated RFC3339 prefix (reuse runIDTime and call .Day()) instead of fixed-offset string slicing, tying day to a real parsed timestamp. | error-handling | 30 | code-review | claude | MEDIUM |
| 8 | [x] | MEDIUM | internal/scorecard/store.go:139 | Directory permissions are not being set correctly. The `Append` function is not setting the director | Parse the day from the validated RFC3339 prefix (reuse runIDTime and call .Day()) instead of fixed-offset string slicing, tying day to a real parsed timestamp. | error-handling | 30 | code-review | archer | MEDIUM |
| 8 | [x] | MEDIUM | internal/scorecard/store.go:180 | File creation is not being handled correctly. The `Append` function is not handling file creation errors, so the scorecard files may not be created if there is an error. | correctness | 10 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | MEDIUM | internal/scorecard/store.go:220 | File reading is not being handled correctly. The `ReadRecords` function is not handling file reading errors, so the scorecard files may not be read if there is an error. | correctness | 10 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | MEDIUM | internal/scorecard/store.go:70 | ReadRecords accumulates every parsed Record into an in-memory slice and ReadAll concatenates every month file's full slice, so Aggregate over 10k+ records holds all records resident — load-entire-store, not the single-pass streaming fold the perf risk profile demanded. The scanner is streamed; the data is not. | Provide a streaming variant ReadRecordsFunc(path, func(Record) error) or fold into LeaderboardRow accumulators during the scan so peak memory is O(groups) not O(records). Tolerable today at 10k records but diverges from design intent. | performance | 120 | code-review | claude | MEDIUM |
| 8 | [x] | MEDIUM | internal/scorecard/store.go:85 | On a mid-file scanner error (sc.Err() non-nil, e.g. a line exceeding maxLineBytes -> bufio.ErrTooLong) ReadRecords returns (recs,err) and ReadAll returns (all,err), aborting the entire leaderboard. The risk profile says malformed JSONL should skip+warn+continue; instead a single oversized line in one month file kills aggregation across all months and bufio.Scanner cannot resume. | On bufio.ErrTooLong log+skip the offending line and continue (switch to a bufio.Reader.ReadString loop that can drain an over-long line) rather than propagating an error that aborts every month. | error-handling | 60 | code-review | claude | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/aggregate.go:102 | Corroboration rate is being calculated incorrectly. The formula `findings_corroborated / findings_ra | Centralize run_id parsing in one function returning (time.Time, ok) and derive month, day, and window-membership from it everywhere. Delete the positional slicing paths. | maintainability | 60 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/aggregate.go:130 | Cost per corroborated finding is being calculated incorrectly. The formula `total_cost_usd / findings_corroborated` is used, but it should be `total_cost_usd / (findings_corroborated + findings_refuted)` to match the standard definition of cost per corroborated finding. | correctness | 5 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/aggregate.go:154 | The sort comparator cross-multiplies int64(FindingsCorroborated)*int64(FindingsRaised) across two rows; these are sums over many runs, so with large Runs the products could in theory overflow int64. Realistic finding counts stay small, but the "exactness" claim is only conditionally true with no saturation. | Document the magnitude assumption (counts << 2^31 so the product << 2^62), or fall back to comparing the stored float rate when products would overflow. | error-handling | 15 | code-review | claude | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/aggregate.go:58 | ParseSince treats "Nm" (months) as a fixed 30-day duration, but monthsToScan/month-file rotation use real calendar months (28-31 days). So --since 1m and the on-disk month boundaries disagree at a month edge — two "month" definitions in one package. | Document the 30-day approximation at the call site, or use AddDate(0,-n,0) so "month" matches file rotation. | maintainability | 15 | code-review | claude | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/export.go:100 | Schema version is not being incremented. The `SchemaVersion` constant is set to 1, but it should be incremented to 2 to reflect changes in the scorecard record schema. | correctness | 10 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/export.go:150 | Exported at timestamp is not being formatted correctly. The `ExportedAt` field is set to `time.Now().UTC().Format(time.RFC3339)`, but it should be set to `time.Now().UTC().Format(time.RFC3339)` to match the expected format. | correctness | 5 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/export.go:200 | Filters are not being applied correctly. The `ApplyFilters` function is not being called, so the exported scorecard records may include findings that should be filtered out. | correctness | 5 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/export.go:206 | Filters: ExportFilters(opts) relies on ExportFilters and FilterOpts being struct-identical field-for-field and in order. FilterOpts (aggregate.go:15) has more fields than ExportFilters' three, so a future reorder of FilterOpts' first three fields would silently compile and emit a since/model/persona mismatch in the public envelope — a data-correctness bug with no compile error. | Replace the direct conversion with explicit field assignment: ExportFilters{Since: opts.Since, Model: opts.Model, Persona: opts.Persona} — equally terse and immune to field reordering. | maintainability | 5 | code-review | bruce, claude | HIGH |
| 8 | [x] | LOW | internal/scorecard/export.go:238 | SurvivedSkepticRate indistinguishable between no-verification and 0% | Document behavior or use sentinel value | maintainability | 2 | code-review | bruce | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/paths.go:110 | Directory permissions are not being set correctly. The `resolveDir` function is not setting the directory permissions to `0o700`, so the scorecard directory may be writable by other users. | security | 5 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/paths.go:50 | Directory path is not being resolved correctly. The `resolveDir` function is not resolving the directory path correctly, so the scorecard files may not be created in the expected location. | correctness | 5 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/paths.go:80 | Directory creation is not being handled correctly. The `resolveDir` function is not handling directory creation errors, so the scorecard directory may not be created if there is an error. | correctness | 5 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/reconcile.go:100 | Verification path is not being handled correctly. The `EmitForReconcile` function is not handling the verification path correctly, so the conditional skeptic fields may not be populated. | correctness | 5 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/reconcile.go:150 | No scorecard flag is not being handled correctly. The `EmitForReconcile` function is not handling the no scorecard flag correctly, so the scorecard files may be created even when they should be suppressed. | correctness | 5 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/reconcile.go:200 | Scorecard directory is not being created correctly. The `EmitForReconcile` function is not creating the scorecard directory correctly, so the scorecard files may not be created if there is an error. | correctness | 5 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/reconcile.go:23 | Pool summary read before NoScorecard check | Move suppression gate before any I/O | performance | 3 | code-review | bruce | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/reconcile.go:50 | Pool summary is not being read correctly. The `ReadPoolSummary` function is not reading the pool summary correctly, so the per-reviewer usage metadata may not be available. | correctness | 5 | 0 | code-review | archer | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/reconcile.go:66 | runID is built as res.Summary.ReconciledAt + "-" + filepath.Base(reviewDir) with no validation that ReconciledAt is a parseable YYYY-MM-prefixed value. If ReconciledAt is empty/malformed (corrupt summary), the emitted records carry a run_id whose month prefix monthFromRunID later rejects, silently dropping the run from all lookups/exports — written successfully but unfindable. | Validate res.Summary.ReconciledAt against the month prefix (reuse monthRe or monthFromRunID) before composing runID; if invalid, log and skip emission rather than writing an unfindable record. | error-handling | 15 | code-review | claude | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/scorecard.go:111 | Double logging on Emit errors | Remove redundant error logging in caller | maintainability | 2 | code-review | bruce | MEDIUM |
| Solo | [x] | LOW | internal/scorecard/scorecard.go:210 | reviewerCounts is O(reviewers x findings) and calls distinctCount per matching finding, allocating a fresh map per finding (scorecard.go:325-332); distinctCount(f.Reviewers) depends only on the finding yet is recomputed once per reviewer. Emission runs once per reconcile so not hot, but it is needless R x F work. | Precompute distinctCount per finding once (or build a map[reviewer]->(raised,corroborated) in a single pass over findings) instead of rescanning all findings per reviewer. | performance | 15 | code-review | claude | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/scorecard.go:232 | verdictTallies map overwrites reviewers for duplicate finding locations | Append reviewers to slice per key instead of direct assignment | correctness | 5 | code-review | bruce | MEDIUM |
| Solo | [x] | LOW | internal/scorecard/scorecard.go:245 | verdictTallies matches findings by findingKey(file,line,problem) — an exact byte-for-byte match on the full Problem string. Any reformatting of the LLM-emitted problem text between review and verify stages silently drops the verdict attribution, under-counting findings_verified/findings_refuted with no warning. normalizeVerdict normalizes the verdict but nothing normalizes the join key. | Match on the stable file+line pair (or a normalized/hashed problem snippet) and emit a warning when a verification finding matches no raised finding so silent under-counting is observable. | error-handling | 30 | code-review | claude | MEDIUM |
| 8 | [x] | LOW | internal/scorecard/scorecard.go:260 | normalizeVerdict comment says "trim surrounding" but strips all whitespace | Update comment to match actual behavior | maintainability | 2 | code-review | bruce | MEDIUM |
| Solo | [/] | LOW | internal/scorecard/store.go (FindByRunID adjacent-month notice; ReadRecords malformed-line notice); internal/scorecard/scorecard.go (Emit notices) | Store/emit diagnostics ("skipping malformed record", "run spans adjacent month files", "write failed") are written directly to the process-global `os.Stderr` rather than to a writer threaded from the cobra command (`cmd.ErrOrStderr()`). (Deferred: Epic Plan 3.4) | accept an `io.Writer` (or logger) on the store/emit entry points and have the CLI pass `cmd.ErrOrStderr()`, so warnings are capturable and redirectable. | ops | 0 | execute-sprint | execute-sprint | MEDIUM |
| Solo | [x] | LOW | internal/scorecard/store.go:23 | Concurrent same-file appends rely on a single `write()` to an `O_APPEND` regular file being atomic (contiguous, end-of-file seek + write). | gate concurrent writes with an OS advisory file lock (flock) around the append, or document the single-writer assumption for networked config dirs. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| Solo | [x] | LOW | internal/scorecard/store.go:42 | File reopened per record in batch | Consider batching for in-process appends — Won't-fix: batching is mutually exclusive with the per-write O_APPEND atomicity invariant (store.go:28-31) protecting concurrent reconcile writers (2026-06-16) | performance | 5 | code-review | bruce | MEDIUM |

### [2026-06-14] From Sprint: 3.2_disagreement_radar

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| Solo | [/] | LOW | internal/reconcile/disagree.go:280 | Duplicate writeRadarSection rendering logic risks divergent escaping across packages (Deferred: .planning/epics/active/15.0_radar-renderer-consolidation.md) | Consolidate radar rendering into report package; reconcile should call report.writeRadarSection | security | 20 | code-review | greta | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/disagree.go:350 | Duplicated radar section rendering logic (Deferred: Epic Plan 15.0) | Extract shared writeRadarSection to a common package | maintainability | 10 | code-review | bruce | MEDIUM |
| 4 | [/] | MEDIUM | internal/reconcile/disagree.go:354 | Duplicated radar markdown rendering diverges (Deferred: Epic Plan 15.0) | Extract shared writer or make reconcile use report package's escTrunc | maintainability | 10 | code-review | bruce | MEDIUM |
| Solo | [/] | LOW | internal/reconcile/disagree.go:413 | Redundant implementation of writeRadarSection (Deferred: .planning/epics/active/15.0_radar-renderer-consolidation.md) | Remove duplicate function from internal/reconcile and use internal/report | maintainability | 15 | code-review | otto | MEDIUM |
| 4 | [x] | LOW | internal/reconcile/disagree_test.go:131 | Gray-zone out-of-scope exclusion is untested -- neither branch of allOutOfScope (disagree.go:135-143,166-176) has coverage. No test feeds a cluster where every member is out_of_scope (expect skipped) nor a mixed cluster with one in-scope member (expect kept), so the fail-closed semantics are unverified. | Add two cases: (1) all members out_of_scope -> zero gray_zone items; (2) mixed cluster -> the gray_zone item survives. | testing | 30 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | internal/reconcile/disagree_test.go:142 | Determinism is under-tested against the actual risk. TestBuildDisagreements_DeterministicOrdering (and TestEmit_DeterministicOutput) call the builder twice on the SAME slice order, proving only referential transparency/idempotence -- not sort-stability under shuffled input, which is the AC3 determinism contract. A broken tie-break (dropping the kind-asc or problem-asc keys in sortDisagreements) would still pass; the lower tie-break tiers (line/kind/problem asc, disagree.go:449-456) are never exercised. | Build the same items in two different slice orders (or shuffle with a fixed seed) and assert identical df.Items, and add a case where two items share score+severity+file so line/kind/problem asc decide order. | testing | 30 | code-review | claude | MEDIUM |
| Solo | [x] | LOW | internal/reconcile/disagree_test.go:240 | No test pins the documented load-bearing contract that the snapshot disagreements.json EXCLUDES the verification_disagreement tier (disagree.go:69-71 claims it is surfaced only by the live radar). TestEmit_WritesDisagreementsJSON feeds only a severity split, so a future change letting a verification tie leak into the persisted snapshot would not fail. The same gap leaves the snapshot-vs-live-rebuild equivalence (emit.go:114 BuildDisagreements over r.Ambiguous vs the live LoadDisagreements re-read) without any invariant. | Add a test running Emit with a merged finding carrying an unverifiable 2-skeptic Verification block, read back disagreements.json, assert no item has Kind==KindVerificationDisagreement, while the same input through the live RenderDisagreements path does surface it. | testing | 30 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | internal/reconcile/disagree_test.go:286 | TestDisagreementsSchema_StableContract pins the Epic 6.0 handoff contract only with assert.Contains on each field-name substring. It does not pin JSON value types, nesting, or the positions/skeptics/detail keys, and cannot detect a field moving or being retyped (e.g. score becoming a string, line nesting under an object) -- Contains matches the quoted key regardless of structure. A downstream Epic 6.0 consumer could break while this test stays green. | Unmarshal into map[string]any (or a golden fixture) and assert the exact top-level and per-item key sets plus each value's Go type (score float64, line int, reviewers array). Drive a gray-zone input so the nested Position contract (reviewer/severity/problem) is pinned. | testing | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/reconcile/disagree_test.go:328 | TestBuildDisagreements_GrayZoneAllUnknownSeverityStillScoresAboveZero asserts only Score > 0.0, weaker than the impl/intent: the floor is exactly 1 (disagree.go:345-347) and the test comment claims it should rank "above a LOW solo (rank 1)" -- but a floor of 1 ties LOW rank 1, it is not above. The assertion would pass even at 0.0001, which would not achieve the stated ranking goal, and the comment is slightly inaccurate. | Assert the exact floor (Score == 1.0) and, better, add a LOW solo to the same input asserting the gray-zone cluster sorts at-or-above it; correct the comment to "at or above a LOW solo". | testing | 15 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/reconcile/emit.go:113 | BuildDisagreements(r.JSONFindings(), r.Ambiguous) runs twice per Emit -- once for the DisagreementsJSON writer (emit.go:114) and again inside RenderMarkdown (emit.go:264) -- each rebuilding the identical radar (JSONFindings allocation + gray-key map build + sort), redundant O(n log n) work on the reconcile path. | Build the radar once at the top of Emit and pass the DisagreementsFile to both the DisagreementsJSON writer and RenderMarkdown. | performance | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/reconcile/emit.go:207 | ReadDisagreements unmarshals disagreements.json without ever checking SchemaVersion, so a future 2.0 file would parse into the 1.0 struct with silent field loss -- exactly the drift the version constant exists to guard against. The version is written but never validated on read, making it decorative for the Epic 6.0 consumer it was added for. | Have ReadDisagreements validate df.SchemaVersion against DisagreementsSchemaVersion (at least major-version compatibility) and error on mismatch. | maintainability | 30 | code-review | claude | MEDIUM |
| 5 | [/] | MEDIUM | internal/report/disagree.go:47 | The radar markup is rendered by two divergent copies: writeRadarSection + formatScore in internal/report/disagree.go and a structurally different copy in internal/reconcile/disagree.go:389. They are intentionally not identical (report copy truncates via escTrunc + uses joinReviewers/reviewerOrUnknown; reconcile copy uses uncapped esc + joinOrNone), so a future markup change (new field, reordered bullets) must be made in both or the live `atcr report` radar and the archival reconciled/report.md silently drift. (Deferred: .planning/epics/active/15.0_radar-renderer-consolidation.md) | Extract one shared item renderer parameterized by a truncate-vs-verbatim flag and heading prefix; have both call sites delegate. Add a test that diffs the rendered markup of both paths on the same DisagreementsFile asserting the only intended difference is truncation. | correctness | 60 | code-review | bruce, claude | HIGH |
| Solo | [x] | MEDIUM | cmd/atcr/report.go:68 | Silent error suppression on ReadAmbiguousClusters hides corrupted or tampered ambiguous.json | Log warning or annotate report output when ambiguous.json read fails | security | 10 | code-review | greta | MEDIUM |
| Solo | [x] | LOW | cmd/atcr/report_test.go:70 | Incomplete ambiguous-sidecar test | Assert rendered output contains the gray-zone cluster content | maintainability | 3 | code-review | bruce | MEDIUM |
| Solo | [x] | LOW | internal/mcp/handlers.go:267 | MCP handleReport surfaces the radar only embedded in format==md; there is no MCP equivalent of the CLI --disagreements flag, so an MCP client cannot request the focused/ranked radar view or its JSON machine-contract (RenderDisagreementsJSON). The format-to-radar dispatch is also duplicated between cmd/atcr/report.go:57-89 and handlers.go:267-279 (the CLI has a third branch the MCP lacks), the root cause of the parity gap. Out of this epic's ACs but a CLI/MCP-parity TD. | Extract a shared report.RenderReport(w, findings, df, opts) both call sites invoke, and add a disagreements option to ReportArgs mirroring the CLI; or explicitly document the radar as markdown-embed-only over MCP. | maintainability | 120 | code-review | claude | MEDIUM |
| Solo | [x] | MEDIUM | internal/mcp/handlers.go:267 | Silent error suppression on ReadAmbiguousClusters hides corrupted or tampered ambiguous.json | Log warning or annotate report output when ambiguous.json read fails | security | 10 | code-review | greta | MEDIUM |

### [2026-06-14] From Sprint: 3.1_verify_audit_attribution

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:391 | Error path sets tripped to nil instead of empty slice, losing budget info from partial runs | Set tripped to a non-nil empty slice or nil consistently | error-handling | 3 | code-review | case-reviewer | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline.go:421 | In verifyFinding, when invokeSkeptic returns a programming-fault error the code synthesizes an unverifiable verdict but emits no log line, so the impossible nil-ctx/cc/disp case silently becomes a normal unverifiable record indistinguishable from a real budget trip. The operational signal that a programming fault occurred is lost. | Add logSkepticFailure(sk.Name, "programming_fault", ierr.Error()) in the ierr != nil branch so the impossible case is visible if it ever fires. Verify by injecting the fault in a test and asserting the stderr line. | maintainability | 15 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline.go:435 | winningAttribution with empty winner records all voters silently | Amend the verifyFinding comment to winners on a decisive vote, all participants on a tie (see winningAttribution) so it matches the actual contract. | error-handling | 5 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline.go:435 | The verifyFinding comment says attribute Model/TrippedBudgets to the winning skeptics only - not all of skeptics[0..n]. This is only half-true: winningAttribution's tie branch deliberately records every participant's model. A reader of verifyFinding alone will wrongly conclude a tie records nothing or only the lone unverifiable voter. The two comments describe the same behavior inconsistently. | Amend the verifyFinding comment to winners on a decisive vote, all participants on a tie (see winningAttribution) so it matches the actual contract. | error-handling | 5 | code-review | claude | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:472 | winningAttribution re-derives the decisive/tie outcome independently from aggregateVerdicts instead of being told the result, so the two can silently diverge. aggregateVerdicts builds its valid set by filtering to canonical verdicts, while winningAttribution counts all non-nil entries including any non-canonical verdict string. If a skeptic ever returned a non-canonical verdict, the two would disagree on decisive vs tie, flipping which models get attributed and making the audit Model disagree with the recorded verdict. | Either have aggregateVerdicts return the winners set (or a decisive flag) and pass it to winningAttribution, or make winningAttribution filter to canonical verdicts using the same switch aggregateVerdicts uses before counting. Verify with a test feeding a non-canonical verdict among canonical ones, asserting attribution matches the aggregated verdict's voters. | maintainability | 60 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline_test.go:208 | TestRunVerify_ToolHarnessUnavailable_RedactsDetail swaps os.Stderr to a pipe but never drains it in a goroutine while runVerify writes. If runVerify ever wrote more than the OS pipe buffer (~64KB) before w.Close(), the write would block and deadlock the test. Output is small today, but the pattern is fragile and duplicated in TestRunVerify_CorruptPriorNoWarningWhenNoSkippedFindings. | Drain the read end concurrently (start io.Copy in a goroutine before running, or use a bytes.Buffer-backed approach) so a larger-than-buffer stderr write cannot deadlock. Latent flake risk as logging grows. | testing | 15 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline_test.go:262 | alwaysChat and byModelChat both implement Complete, but every multi-skeptic test wires skeptics with SupportsFC=true so only the Chat path runs. The single-shot degrade path under multi-vote aggregation is never exercised; only the single-skeptic TestInvokeSkeptic_DegradesWhenNotFC covers degrade, and it returns empty->unverifiable. | Add a multi-skeptic test where one eligible skeptic has SupportsFC=false and the fake Complete returns a real verdict, asserting the non-FC skeptic's vote still participates in aggregation and its model is attributed. Confirms degrade interacts correctly with winningAttribution. | testing | 30 | code-review | claude | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline_test.go:281 | TestRunVerify_ThoroughMultiSkepticRecordsAllModels (AC2) asserts only Contains(Model, m-s2) and Contains(Model, m-skep). Since all three skeptics confirm here, every model is a winner, so it does not exercise winner-vs-loser discrimination and would pass even if the impl naively recorded all participating models regardless of verdict. It also never pins the exact value or ordering of the joined Model string. (disagreement: LOW vs MEDIUM) | Assert the exact joined string (assert.Equal m-s2, m-skep) to lock ordering/dedup, and rely on the TwoConfirmOneRefute test for the discrimination property. Add an exact-equality assertion so a stray extra/duplicate model is caught. | testing | 15 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline_test.go:281 | The multi-skeptic tests hard-code an assumption about SelectEligibleSkeptics ordering in comments (Alphabetically returns [s2, skep]) and rely on it implicitly without asserting it. The AC2 never-default-to-skeptics[0] guarantee is only argued in a comment, not asserted via a controlled skeptics[0] that is a loser. (disagreement: LOW vs MEDIUM) | Assert the exact joined string (assert.Equal m-s2, m-skep) to lock ordering/dedup, and rely on the TwoConfirmOneRefute test for the discrimination property. Add an exact-equality assertion so a stray extra/duplicate model is caught. | testing | 15 | code-review | claude | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline_test.go:330 | AC2 calls out both 2-confirm/1-refute AND 2-refute/1-confirm winning-model attribution, but only the confirmed-wins direction is tested. There is no test where two skeptics refute and one confirms asserting Model names only the two refuters and excludes the confirmer. The winningAttribution decisive/loser-exclusion logic is symmetric in code but only proven for one verdict polarity. | Add TestRunVerify_WinningModelAttribution_TwoRefuteOneConfirm mirroring the existing test with verdicts flipped, asserting verdict=refuted, Model contains the two refuters, NotContains the confirmer. Closes the untested polarity of AC2. | testing | 30 | code-review | claude | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline_test.go:355 | TestRunVerify_WinningModelAttribution_TwoConfirmOneRefute asserts Contains/NotContains on the Model substring. Model names m-s2/m-s3/m-skep happen to avoid substring overlap, but the assertion style is fragile: a future rename (e.g. m-s or m-s30) would make NotContains(m-s3) silently wrong, and substring checks pin neither ordering nor reject duplicate emission of a winning model. | Assert the exact expected string (assert.Equal m-s2, m-skep) rather than substring Contains/NotContains. Exact equality is robust to model-name overlaps, locks selection-order dedup, and still proves the refuting model is excluded. | testing | 15 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline_test.go:362 | 1+1 tie case (1 confirmed + 1 refuted) not covered by tests | Add Skeptic assertions to lock the joinSkeptics contract the docstring promises | maintainability | 10 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline_test.go:362 | Multi-vote tests assert Model but not Skeptic field | Add Skeptic assertions to lock the joinSkeptics contract the docstring promises | maintainability | 10 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline_test.go:382 | No test exercises the pipeline-level rule that tripped budgets attach only to unverifiable winners and are dropped for confirmed/refuted winners. TestRunVerify_ThreeWayTieRecordsAllParticipantModels checks models but never asserts vf.Findings[0].TrippedBudgets content, and there is no live confirmed-finding test asserting TrippedBudgets is empty even when a losing skeptic tripped. | In the tie test, script one voter's unverifiable from a budget trip (tool-loop turns + low MaxTurns) and assert verification.json TrippedBudgets contains the tripped budget. Add a confirmed-winner test asserting TrippedBudgets is empty even when a loser tripped. | testing | 60 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline_test.go:397 | TestRunVerify_SkipDropsMismatchedPriorMetadata covers only a verdict-string mismatch. The carry-forward guard also gates on priorLoadFailed (corrupt prior) and on a missing prior entry for the key. There is no test that a SKIPPED finding with a corrupt prior verification.json drops to zero metadata without carrying garbage (the existing corrupt-prior test covers the no-skipped-findings branch, the opposite case). | Add a test: an already-verified finding (skipped, no --fresh) plus a corrupt prior verification.json; assert the rewritten finding keeps its verdict but has empty Model/zero DurationMs/empty TrippedBudgets and that the prior-unreadable warning DOES fire. | testing | 30 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline_test.go:457 | TestRunVerify_SkipPreservesPriorMetadata asserts DurationMs==4242 is carried forward (skip path) but no test asserts the live (non-skip) path populates a non-zero DurationMs. Every live test uses scripted in-memory completers returning sub-millisecond, so int(time.Since(start).Milliseconds()) is routinely 0; a regression that always wrote 0 on the live path would pass every test. | Inject a clock into verifyFinding to assert a deterministic non-zero DurationMs, or add a mock turn delay and assert DurationMs > 0 on a live confirmed finding. Without one, live duration attribution is untested. | testing | 60 | code-review | claude | MEDIUM |
| 3 | [x] | HIGH | internal/verify/pipeline_test.go:569 | TestRunVerify_WorkerPoolPreservesOrder claims to verify that a MaxParallel>1 pool produces the same per-finding verdict ordering as a serial run, but it uses alwaysChat which returns an identical confirmed verdict for every finding. A pool bug that swapped result indices would be undetectable because every finding still reads confirmed/VERIFIED. The test cannot fail on the ordering property it advertises. | Script distinct per-finding verdicts (byModelChat keyed on file/model, or distinct findings credited to distinct reviewers so each gets a different verdict) and assert a.go->confirmed, b.go->refuted, c.go->unverifiable land on the correct keys after the parallel run. Only distinguishable results can detect a mis-ordered pool. | testing | 30 | code-review | claude | MEDIUM |

### [2026-06-14] From Sprint: 2.2_code_review_fanout_hardening

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| Solo | [x] | HIGH | src/auth.go:42 | Missing input validation (intent_note: src/auth.go is not within epic 2.2 scope — epic covers internal/fanout, internal/registry, and internal/payload only; this finding is likely a reviewer hallucination; verify file existence before acting) (WON'T FIX — false positive: src/auth.go does not exist in the repo, no src/ tree and no auth.go anywhere; reviewer hallucination confirmed 2026-06-16) | Add length check | security | 5 | code-review | bruce | MEDIUM |
| Solo | [/] | MEDIUM | internal/fanout/postprocess.go:14 | The severity-rank rubric {CRITICAL:4,HIGH:3,MEDIUM:2,LOW:1} is independently redefined in fanout/postprocess.go:17, reconcile/merge.go, verify, report, plus a set-form copy reviewSeverities in registry/config.go. postprocess looks up severityRank[strings.ToUpper(...)] while reconcile looks up the raw value, so a future severity change or non-canonical casing silently desyncs fan-out truncation from reconcile merging. The postprocess copy was newly added by Epic 2.2. (disagreement: LOW vs MEDIUM) (Deferred: Epic Plan 3.5) | Extract a single canonical severity package (or export from internal/stream) exposing the ordered rank map plus normalizeSeverity, and have registry/fanout/reconcile/verify/report consume it. Verify by deleting the local maps and confirming the suite passes with one source of truth. | maintainability | 120 | code-review | claude | MEDIUM |
| Solo | [x] | MEDIUM | internal/fanout/review.go:600 | buildFallbackAgent overrides the fallback's MinSeverity/MaxFindings with the primary's (review.go:603-604) and the fallback's Scope never reaches it (Prompt is copied from primary.Prompt which already baked in the primary's scope). A user who sets scope/min_severity/max_findings on an agent that only ever runs as a fallback gets them silently ignored. Zero test coverage across all four Epic 2.2 test files. | Add a test that builds a slot whose fallback declares its own min_severity/max_findings/scope and asserts the primary's constraints win. Optionally emit a load-time warning when a fallback-only agent sets these fields so the silent-ignore is visible. | testing | 60 | code-review | claude | MEDIUM |
| Solo | [x] | LOW | internal/payload/scope.go:22 | ScopeFocus concatenates each scope string verbatim into the persona prompt via strings.Join with no escaping or length bound. Validation rejects blank/whitespace entries but not content or length — a pathological multi-KB scope entry bloats every agent prompt, and the trust assumption (scope is operator-controlled input) is undocumented at the injection site. | Document at scope.go that scope is trusted operator input and intentionally not escaped. Optionally bound each entry's length (e.g. reject > 64 chars at config.go scope validation) to prevent accidental prompt bloat. | security | 30 | code-review | claude | MEDIUM |
| Solo | [x] | LOW | internal/payload/scope_focus_test.go:16 | TestScopeFocus only checks category substring presence; the soft-not-hard contract ("not a hard limit — still report any genuinely critical issue") is never asserted and could be deleted with green tests. The blank-skip assertion checks only for literal ", ," patterns, so a trailing blank entry would pass undetected. | Assert the exact rendered string for a known input to lock wording and join correctness; assert the "not a hard limit" clause is present in the output. | testing | 15 | code-review | claude | MEDIUM |

### [2026-06-14] From Review: llmclient OpenAI-compatible tool handling

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| U | [/] | LOW | internal/llmclient/chat.go:1 | No provider-conformance test matrix for the OpenAI-compatible surface. The client deliberately absorbs real wire divergence (string-encoded vs raw-object tool_call `arguments`, lenient finish_reason) but is exercised only against synthetic fixtures, so a regression against a specific provider's actual tool_call shape (OpenAI, litellm, Ollama, vLLM, Together) would not be caught. This is the robustness the official SDK is assumed to provide, achievable here without adopting it. | Add a recorded-fixture conformance suite: capture a real `tool_calls` response from each target provider and assert the parser (`ToolCallArguments`, `chatToolResponse` decode, finish_reason handling) yields identical engine-facing results. NOTE: scope is a few days, not a quick-win — consider promoting to a standalone test-remediation plan rather than resolving inline. | testing | 480 | review | claude | LOW |

### [2026-06-14] From Sprint: 3.0_adversarial_verification

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| 7 | [/] | LOW | internal/verify/pipeline.go:238 | The rich VerificationResult built in verifyFinding never populates TrippedBudgets (invokeSkeptic folds tripped budgets only into free-text Notes), base.Model is hard-coded to skeptics[0].Config.Model even when another skeptic produced the winning verdict, and the skip-already-verified rebuild (pipeline.go:199) re-synthesizes records from the compact on-disk block losing Model/DurationMs/TrippedBudgets — so verification.json's structured audit fields degrade or misattribute on multi-skeptic and no-op re-runs (extends the model-attribution gap of TD-011). Also: Model is empty on the no_eligible_skeptic / tool_harness_unavailable early-return paths. [intent: deferred per sprint-plan TD-011] | Thread the tripped-budget slice and the winning skeptic's model up from invokeSkeptic into base.TrippedBudgets/base.Model (join models when multiple voters agree, mirroring joinSkeptics), and carry Model/DurationMs/TrippedBudgets forward for skipped findings instead of synthesizing a lossy record. | correctness | 120 | execute-sprint | execute-sprint, claude | HIGH |

### [2026-06-13] From Sprint: 2.0_tool_using_reviewers

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| 5 | [/] | LOW | internal/tools/snapshot.go | AC 03-02 Scenario 5 and AC 03-03 Scenarios 4-5 require `manifest.json` `stages.review` to record `snapshot_mode` (live/worktree), `head_sha`, and `snapshot_worktree_path`. (intent_note: deferred per sprint-plan §2.5.A (manifest review-stage recording is Phase 5 work); Deferred to Epic Plan 2.1) | when wiring `SnapshotFor` into the agent loop, record `snapshot_mode`/`head_sha`/`snapshot_worktree_path` into `internal/payload/manifest.go` review stage and add the manifest assertion tests from AC 03-02/03-03. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
