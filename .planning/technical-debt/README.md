# Technical Debt Tracking

This file is a staging area for small technical debt items discovered during development. Items are triaged and moved to individual sprint documents in `sprints/` as they are prioritized.

## Stats

| Severity | Open | Deferred | Resolved |
|----------|------|----------|----------|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 29 | 1 | 0 |
| MEDIUM | 45 | 22 | 20 |
| LOW | 55 | 21 | 26 |

**Last Modified:** 2026-06-22 | **Open Items:** 129 | **Deferred Items:** 44 | **Resolved Items:** 46 | **Total Items:** 219

## Directory Structure

```
technical-debt/
├── README.md                    # This file (staging area)
├── CLAUDE.md                    # AI assistant guidelines
└── sprints/
    ├── active/                  # Currently being addressed
    ├── pending/                 # Prioritized, not yet started
    └── completed/               # Resolved items
```

## How to Use

1. **Small items**: Add to this README under "Staging Area" below
2. **Larger items**: Create a new document in `sprints/pending/`
3. **During sprint planning**: Move items from pending to active
4. **After resolution**: Move items from active to completed



### [2026-06-22] From Sprint: 7.5_syntax-guard-refinements

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 12 | [ ] | HIGH | internal/verify/syntaxguard.go:55 | jsonKeyLineRe (`^\s*"[^"]*"\s*:`) uses a non-escape-aware character class, so a JSON object whose ONLY member key contains an escaped quote (e.g. "a\"b": 1) does not match. looksLikeNonGoBraces then returns false, the block-brace signal fires, and the single-key escaped-quote JSON object is spuriously flagged invalid_syntax — the same false-positive class Epic 7.5 set out to close. Verified empirically: validateGoFixSyntax returns "illegal label declaration". Multi-key objects are unaffected because a sibling clean-key line still matches, so blast radius is narrow. (disagreement: LOW vs HIGH) | Widen jsonKeyLineRe to tolerate backslash-escapes in the key using an escape-aware character class (allow a non-quote/non-backslash char OR a backslash-escaped char between the quotes; RE2-safe, no backtracking). Add a single-key fixture {\n  "a\"b": 1\n} to syntaxguard_test.go asserting NoError. Alternatively accept and document the boundary as a known narrow limitation. | maintainability | 15 | code-review | agent, otto, greta, Residual FP for `{"a\"b":1}`; conservative-recall tolerant, bruce, claude, Regex `[^"]*` cannot match `\"` in keys like `"foo\"bar":` | HIGH |
| 12 | [ ] | LOW | internal/verify/syntaxguard.go:167 | jsonKeyLineRe does not match keys with escaped quotes | Either accept limitation or use `"(?:[^"\\] | \\.)*"` | 5 | code-review | `[^"]*` won't match `"key\"with\"escapes"` which is rare but valid JSON | MEDIUM |
| 12 | [ ] | LOW | internal/verify/syntaxguard.go:167 | Inefficient regex re-execution | Either accept limitation or use `"(?:[^"\\] | \\.)*"` | 5 | code-review | bruce | MEDIUM |
| 12 | [ ] | HIGH | internal/verify/syntaxguard.go:167 | looksLikeGoCode logic unclear without comments | Either accept limitation or use `"(?:[^"\\] | \\.)*"` | 5 | code-review | agent | MEDIUM |
| 12 | [ ] | HIGH | internal/verify/syntaxguard.go:172 | looksLikeNonGoBraces may suppress valid Go with string keys | Order is correct per design but could be clearer | maintainability | 5 | code-review | agent | MEDIUM |
| 12 | [ ] | MEDIUM | internal/verify/syntaxguard.go:172 | looksLikeNonGoBraces called before block brace check but after decl keyword | Order is correct per design but could be clearer | maintainability | 5 | code-review | bruce | MEDIUM |
| 12 | [x] | LOW | internal/verify/syntaxguard.go:190 | The looksLikeNonGoBraces comment states it suppresses when there is "no Go declaration keyword", reading as if the JSON is structurally key-checked. In reality declKeywordRe is a coarse whole-input line-anchored scan, so a JSON value line beginning with a Go keyword (func/type/var/...) defeats suppression and re-arms the block-brace flagging. This is benign — it only re-arms pre-existing 7.1 behavior and never adds new flagging beyond 7.1 (AC2 intact) — but the comment over-promises and could mislead a future maintainer. | Tighten the comment at syntaxguard.go:183-189 to note the keyword check is a coarse whole-input line scan (not key-aware), so a JSON value line starting with a Go keyword defeats suppression — acceptable because it only re-arms 7.1 behavior, never adds flagging. No code change. | maintainability | 5 | code-review | claude | MEDIUM |
| 12 | [ ] | LOW | internal/verify/syntaxguard.go:190 | Redundant declKeywordRe.MatchString — already checked at line 174 | Tighten the comment at syntaxguard.go:183-189 to note the keyword check is a coarse whole-input line scan (not key-aware), so a JSON value line starting with a Go keyword defeats suppression — acceptable because it only re-arms 7.1 behavior, never adds flagging. No code change. | maintainability | 5 | code-review | base-code-reviewer | MEDIUM |
| 12 | [ ] | HIGH | internal/verify/syntaxguard_test.go:218 | Missing header comment for Epic 7.5 tests | Add epic reference in test file header | maintainability | 2 | code-review | agent | MEDIUM |
| 12 | [ ] | HIGH | internal/verify/syntaxguard_test.go:238 | Missing test for JSON with trailing comma | Add test for trailing comma in JSON | maintainability | 5 | code-review | agent | MEDIUM |
| 12 | [ ] | LOW | internal/verify/syntaxguard_test.go:252 | Test name references AC4 but doesn't assert conservative-recall policy | Rename to TestValidateGoFixSyntax_BrokenGoMapSuppressed | maintainability | 5 | code-review | bruce | MEDIUM |
| 12 | [ ] | HIGH | internal/verify/syntaxguard_test.go:252 | Test name too long and unclear | Rename to TestValidateGoFixSyntax_BrokenGoMapSuppressed | maintainability | 5 | code-review | agent | MEDIUM |
| 12 | [ ] | HIGH | internal/verify/syntaxguard_test.go:266 | Test for looksLikeNonGoBraces misses edge cases | Add test for mixed Go/non-Go lines | maintainability | 5 | code-review | agent | MEDIUM |
| 12 | [x] | LOW | internal/verify/syntaxguard_test.go:271 | looksLikeNonGoBraces not tested with content having both JSON key line and Go decl keyword | Add table cases to TestLooksLikeNonGoBraces (syntaxguard_test.go:274) for "": 1 (expect true), "café": 1 (expect true), and "a\"b": 1 (pin the chosen behavior after the regex decision). Verify via go test ./internal/verify/. | maintainability | 15 | code-review | bruce | MEDIUM |
| 12 | [x] | LOW | internal/verify/syntaxguard_test.go:271 | The Epic 7.5 tests exercise only plain ASCII keys; they never probe escaped-quote, empty, or unicode keys — the regex boundary cases where jsonKeyLineRe behavior is non-obvious. Empty-key ("": 1) and unicode keys ("café": 1) suppress correctly, but the single-escaped-quote case silently regresses (see syntaxguard.go:62 finding) and nothing in the suite would catch a regression at this boundary. | Add table cases to TestLooksLikeNonGoBraces (syntaxguard_test.go:274) for "": 1 (expect true), "café": 1 (expect true), and "a\"b": 1 (pin the chosen behavior after the regex decision). Verify via go test ./internal/verify/. | maintainability | 15 | code-review | claude | MEDIUM |

### [2026-06-22] From Sprint: 7.5_syntax-guard-refinements

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 11 | [ ] | HIGH | internal/verify/syntaxguard.go:55 | jsonKeyLineRe (`^\s*"[^"]*"\s*:`) uses a non-escape-aware character class, so a JSON object whose ONLY member key contains an escaped quote (e.g. "a\"b": 1) does not match. looksLikeNonGoBraces then returns false, the block-brace signal fires, and the single-key escaped-quote JSON object is spuriously flagged invalid_syntax — the same false-positive class Epic 7.5 set out to close. Verified empirically: validateGoFixSyntax returns "illegal label declaration". Multi-key objects are unaffected because a sibling clean-key line still matches, so blast radius is narrow. (disagreement: LOW vs HIGH) | Widen jsonKeyLineRe to tolerate backslash-escapes in the key using an escape-aware character class (allow a non-quote/non-backslash char OR a backslash-escaped char between the quotes; RE2-safe, no backtracking). Add a single-key fixture {\n  "a\"b": 1\n} to syntaxguard_test.go asserting NoError. Alternatively accept and document the boundary as a known narrow limitation. | maintainability | 15 | code-review | agent, otto, greta, Residual FP for `{"a\"b":1}`; conservative-recall tolerant, bruce, claude, Regex `[^"]*` cannot match `\"` in keys like `"foo\"bar":` | HIGH |
| 11 | [x] | LOW | internal/verify/syntaxguard.go:167 | jsonKeyLineRe does not match keys with escaped quotes | Either accept limitation or use `"(?:[^"\\] | \\.)*"` | 5 | code-review | `[^"]*` won't match `"key\"with\"escapes"` which is rare but valid JSON | MEDIUM |
| 11 | [ ] | LOW | internal/verify/syntaxguard.go:167 | Inefficient regex re-execution | Either accept limitation or use `"(?:[^"\\] | \\.)*"` | 5 | code-review | bruce | MEDIUM |
| 11 | [ ] | HIGH | internal/verify/syntaxguard.go:167 | looksLikeGoCode logic unclear without comments | Either accept limitation or use `"(?:[^"\\] | \\.)*"` | 5 | code-review | agent | MEDIUM |
| 11 | [ ] | HIGH | internal/verify/syntaxguard.go:172 | looksLikeNonGoBraces may suppress valid Go with string keys | Order is correct per design but could be clearer | maintainability | 5 | code-review | agent | MEDIUM |
| 11 | [ ] | MEDIUM | internal/verify/syntaxguard.go:172 | looksLikeNonGoBraces called before block brace check but after decl keyword | Order is correct per design but could be clearer | maintainability | 5 | code-review | bruce | MEDIUM |
| 11 | [x] | LOW | internal/verify/syntaxguard.go:190 | The looksLikeNonGoBraces comment states it suppresses when there is "no Go declaration keyword", reading as if the JSON is structurally key-checked. In reality declKeywordRe is a coarse whole-input line-anchored scan, so a JSON value line beginning with a Go keyword (func/type/var/...) defeats suppression and re-arms the block-brace flagging. This is benign — it only re-arms pre-existing 7.1 behavior and never adds new flagging beyond 7.1 (AC2 intact) — but the comment over-promises and could mislead a future maintainer. | Tighten the comment at syntaxguard.go:183-189 to note the keyword check is a coarse whole-input line scan (not key-aware), so a JSON value line starting with a Go keyword defeats suppression — acceptable because it only re-arms 7.1 behavior, never adds flagging. No code change. | maintainability | 5 | code-review | claude | MEDIUM |
| 11 | [x] | LOW | internal/verify/syntaxguard.go:190 | Redundant declKeywordRe.MatchString — already checked at line 174 | Tighten the comment at syntaxguard.go:183-189 to note the keyword check is a coarse whole-input line scan (not key-aware), so a JSON value line starting with a Go keyword defeats suppression — acceptable because it only re-arms 7.1 behavior, never adds flagging. No code change. | maintainability | 5 | code-review | base-code-reviewer | MEDIUM |
| 11 | [ ] | HIGH | internal/verify/syntaxguard_test.go:218 | Missing header comment for Epic 7.5 tests | Add epic reference in test file header | maintainability | 2 | code-review | agent | MEDIUM |
| 11 | [ ] | HIGH | internal/verify/syntaxguard_test.go:238 | Missing test for JSON with trailing comma | Add test for trailing comma in JSON | maintainability | 5 | code-review | agent | MEDIUM |
| 11 | [x] | LOW | internal/verify/syntaxguard_test.go:252 | Test name references AC4 but doesn't assert conservative-recall policy | Rename to TestValidateGoFixSyntax_BrokenGoMapSuppressed | maintainability | 5 | code-review | bruce | MEDIUM |
| 11 | [ ] | HIGH | internal/verify/syntaxguard_test.go:252 | Test name too long and unclear | Rename to TestValidateGoFixSyntax_BrokenGoMapSuppressed | maintainability | 5 | code-review | agent | MEDIUM |
| 11 | [ ] | HIGH | internal/verify/syntaxguard_test.go:266 | Test for looksLikeNonGoBraces misses edge cases | Add test for mixed Go/non-Go lines | maintainability | 5 | code-review | agent | MEDIUM |
| 11 | [ ] | LOW | internal/verify/syntaxguard_test.go:271 | looksLikeNonGoBraces not tested with content having both JSON key line and Go decl keyword | Add table cases to TestLooksLikeNonGoBraces (syntaxguard_test.go:274) for "": 1 (expect true), "café": 1 (expect true), and "a\"b": 1 (pin the chosen behavior after the regex decision). Verify via go test ./internal/verify/. | maintainability | 15 | code-review | bruce | MEDIUM |
| 11 | [x] | LOW | internal/verify/syntaxguard_test.go:271 | The Epic 7.5 tests exercise only plain ASCII keys; they never probe escaped-quote, empty, or unicode keys — the regex boundary cases where jsonKeyLineRe behavior is non-obvious. Empty-key ("": 1) and unicode keys ("café": 1) suppress correctly, but the single-escaped-quote case silently regresses (see syntaxguard.go:62 finding) and nothing in the suite would catch a regression at this boundary. | Add table cases to TestLooksLikeNonGoBraces (syntaxguard_test.go:274) for "": 1 (expect true), "café": 1 (expect true), and "a\"b": 1 (pin the chosen behavior after the regex decision). Verify via go test ./internal/verify/. | maintainability | 15 | code-review | claude | MEDIUM |

### [2026-06-22] From Sprint: epic-7.5

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [ ] | LOW | internal/verify/syntaxguard.go:190 | looksLikeNonGoBraces suppresses any parse-failed unfenced fragment lacking a Go decl keyword whenever a line starts with a quoted key+colon — broader than the doc comment's "map/struct literals" wording (still within the AC4 false-negative-preferred policy). | Tighten the doc comment to state it suppresses any decl-keyword-free fragment with a quoted-key line, or pair jsonKeyLineRe with a leading-brace check so suppression requires actual JSON-object brace shape. | EDGE_CASES | 15 | execute-epic-independent |
| U | [ ] | LOW | internal/verify/syntaxguard.go:62 | AC1 closes only the quoted-key case: unfenced JSON5/YAML/TOML config with unquoted keys plus block braces is still flagged invalid_syntax (residual narrowed, not eliminated; consistent with the plan's deliberate quoted-key anchoring). | Note in the AC1 test file that unquoted-key config remains an accepted residual false positive so a future reader does not assume all unfenced config is covered. | EDGE_CASES | 20 | execute-epic-independent |

### [2026-06-22] From Sprint: 7.4_executor_agent_mode

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 9 | [ ] | MEDIUM | internal/registry/config.go:85 | Missing input validation | Add bounds check for integer input | security | 3 | code-review | bruce | MEDIUM |
| 9 | [ ] | MEDIUM | internal/registry/config.go:92 | Missing input validation | Add bounds check for integer input | security | 3 | code-review | bruce | MEDIUM |
| 9 | [ ] | HIGH | internal/registry/executor_config_test.go:510 | Missing input validation | Add bounds check for integer input | security | 3 | code-review | bruce | MEDIUM |
| 9 | [ ] | HIGH | internal/registry/executor_config_test.go:525 | Missing input validation | Add bounds check for integer input | security | 3 | code-review | bruce | MEDIUM |
| 9 | [ ] | HIGH | internal/registry/executor_config_test.go:615 | Missing input validation | Add bounds check for integer input | security | 3 | code-review | bruce | MEDIUM |
| 9 | [ ] | HIGH | internal/registry/executor_config_test.go:630 | Missing input validation | Add bounds check for integer input | security | 3 | code-review | bruce | MEDIUM |
| 9 | [ ] | MEDIUM | internal/registry/executor_config_test.go:685 | Incorrect error handling | Swallow errors without logging | error-handling | 5 | code-review | bruce | MEDIUM |
| 9 | [ ] | MEDIUM | internal/registry/executor_config_test.go:700 | Incorrect error handling | Swallow errors without logging | error-handling | 5 | code-review | bruce | MEDIUM |
| 9 | [ ] | LOW | internal/registry/executor_config_test.go:755 | Incorrect error handling | Swallow errors without logging | error-handling | 2 | code-review | bruce | MEDIUM |
| 9 | [ ] | HIGH | internal/registry/registry_test.go:55 | Missing input validation | Add bounds check for integer input | security | 3 | code-review | bruce | MEDIUM |
| 9 | [ ] | HIGH | internal/registry/registry_test.go:70 | Missing input validation | Add bounds check for integer input | security | 3 | code-review | bruce | MEDIUM |
| 9 | [ ] | MEDIUM | internal/registry/registry_test.go:125 | Incorrect error handling | Swallow errors without logging | error-handling | 5 | code-review | bruce | MEDIUM |
| 9 | [ ] | MEDIUM | internal/registry/registry_test.go:140 | Incorrect error handling | Swallow errors without logging | error-handling | 5 | code-review | brace | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:7 | Use of legacy math/rand package, prefer math/rand/v2 in Go 1.22+ | Replace math/rand import with math/rand/v2 and update rand.Uint32() call | maintainability | 5 | code-review | atcr-reviewer | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:71 | Misleading comment about cc availability | Fix comment: cc is non-nil only when skeptic harness built, not when fix eligible | maintainability | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/executor.go:127 | No concurrent agent-mode test exists. generateFixes runs each eligible finding's invokeExecutor in its own goroutine under a semaphore, each building its own fanout.NewEngine over the shared cc and disp, but every agent-mode test uses exactly one finding (eligibleFinding() returns a 1-element slice). A data race in shared completer/dispatcher state under concurrent agent-mode findings would not surface under -race. | Add an agent-mode test with multiple eligible findings and the (mutex-guarded) fake completer, asserting each finding gets its own fix and -race stays clean, confirming per-finding engine construction is goroutine-safe over shared cc/disp. | testing | 30 | code-review | claude | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:127 | Agent mode bypasses system_prompt/rules config | Add an agent-mode test with multiple eligible findings and the (mutex-guarded) fake completer, asserting each finding gets its own fix and -race stays clean, confirming per-finding engine construction is goroutine-safe over shared cc/disp. | testing | 30 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/executor.go:127 | Missing input validation | Add an agent-mode test with multiple eligible findings and the (mutex-guarded) fake completer, asserting each finding gets its own fix and -race stays clean, confirming per-finding engine construction is goroutine-safe over shared cc/disp. | testing | 30 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:133 | Fallback log doesn't specify missing component | Log whether cc or disp is nil for better debugging | maintainability | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/executor.go:133 | Missing input validation | Log whether cc or disp is nil for better debugging | maintainability | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | HIGH | internal/verify/executor.go:210 | Improper error handling | Swallow errors without logging | error-handling | 10 | code-review | bruce | MEDIUM |
| 10 | [ ] | HIGH | internal/verify/executor.go:235 | Improper error handling | Swallow errors without logging | error-handling | 10 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/executor.go:344 | Potential for predictable sentinel in prompt | Use a cryptographically secure random generator instead of math/rand | security | 15 | code-review | otto | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:356 | The len(results)==0 -> 'engine returned no result' guard in invokeExecutor is defensive never-fail-the-run code with no test, since the real engine always returns one result per slot. The contract it defends (no panic on zero results) is unverified. | Accept as documented untested defensive code, or refactor to inject a fake engine seam returning zero results and assert the warn string. Low blast radius. | testing | 15 | code-review | claude | MEDIUM |
| 10 | [x] | MEDIUM | internal/verify/executor.go:362 | invokeExecutor documents two distinct non-OK halts (provider error -> StatusFailed, deadline -> StatusTimeout) and joins TrippedBudgets into the warn string, but tests only drive the StatusFailed branch via {err: ...}. The StatusTimeout path and the TrippedBudgets join are entirely unexercised; a regression that dropped budget reporting or mis-classified a timeout would stay green. | Add an agent-mode test with a tiny effective timeout or a delayed chatTurn past a short ctx deadline, asserting the warn contains 'status: timeout' and the tripped-budget text, and that the run still continues (FixWarning set, no panic). | testing | 30 | code-review | claude | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:362 | math/rand used for prompt injection delimiter | Add an agent-mode test with a tiny effective timeout or a delayed chatTurn past a short ctx deadline, asserting the warn contains 'status: timeout' and the tripped-budget text, and that the run still continues (FixWarning set, no panic). | testing | 30 | code-review | greta | MEDIUM |
| 10 | [x] | MEDIUM | internal/verify/executor.go:376 | Tripped budget may be treated as failure | Confirm StatusOK is returned when budget is tripped | correctness | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:383 | Global math/rand used for prompt sentinel | Use crypto/rand or explicit rand.Source | maintainability | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:387 | Weak randomness for injection sentinel | Use crypto/rand for sentinel generation | security | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:410 | buildExecutorAgentPrompt doc (and the parallel skeptic comment) frames the random sentinel as making early-close 'impossible'. With rand.Uint32() -> 8 hex chars the guard is probabilistic (1 in ~4.29e9 per blind, unseeable guess), not impossible. Practically sound and identical to the accepted skeptic guard, but the absolute phrasing misrepresents the guarantee. | Soften 'cannot close it early' / 'impossible' to 'cannot predict the closing tag' in the buildExecutorAgentPrompt doc. Cosmetic, no behavior change. | security | 5 | code-review | claude | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:410 | 32-bit sentinel for prompt-injection defense | Soften 'cannot close it early' / 'impossible' to 'cannot predict the closing tag' in the buildExecutorAgentPrompt doc. Cosmetic, no behavior change. | security | 5 | code-review | atcr-reviewer | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:421 | The security-relevant sentinel injection guard is unpinned at every layer. buildExecutorAgentPrompt calls rand.Uint32() then delegates to the deterministic ...WithSentinel helper, but the only prompt test calls the non-deterministic public function and asserts sentinel-independent substrings, and the ...WithSentinel helper has no test. Reviewer text forging a closing tag is never asserted to stay inside the data block. | Add a test against buildExecutorAgentPromptWithSentinel with a fixed sentinel and a malicious finding.Problem containing a fake </sentinel>, asserting the real close tag still wraps the block and the injected text stays inside the data block. | security | 15 | code-review | claude | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:421 | 32-bit sentinel for prompt-injection defense | Add a test against buildExecutorAgentPromptWithSentinel with a fixed sentinel and a malicious finding.Problem containing a fake </sentinel>, asserting the real close tag still wraps the block and the injected text stays inside the data block. | security | 15 | code-review | atcr-reviewer | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor.go:447 | 32-bit sentinel for prompt-injection defense uses weak RNG | Use crypto/rand or math/rand/v2 for stronger entropy | security | 5 | code-review | atcr-reviewer | MEDIUM |
| 10 | [x] | MEDIUM | internal/verify/executor.go:453 | Partial Settings struct may miss fields | Pass full registry.Settings or verify EffectiveExecutorTimeoutSecs only uses TimeoutSecs | correctness | 10 | code-review | bruce | MEDIUM |
| 10 | [ ] | HIGH | internal/verify/executor.go:462 | Predictable prompt-injection sentinel | Use crypto/rand for sentinel generation | security | 2 | code-review | bruce | MEDIUM |
| 10 | [x] | LOW | internal/verify/executor_agent_test.go:125 | AC6 'logged warning' is half-covered and weakly asserted. FallsBackWhenDispatcherNil asserts the log buffer merely Contains 'agent_mode' (would pass on almost any agent-mode log line, not specifically executor_agent_mode_fallback), and the FallsBackWhenCCNil companion does not assert the log fires at all. | Assert the specific log key/message (executor_agent_mode_fallback or 'dispatcher/chat unavailable') instead of the bare word 'agent_mode', and add the same log assertion to the CC-nil companion so both fallback branches prove the warning fires. | testing | 15 | code-review | claude | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/executor_agent_test.go:129 | Missing input validation | Add length check on user input | security | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/executor_agent_test.go:163 | Missing input validation | Add length check on user input | security | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | HIGH | internal/verify/executor_agent_test.go:209 | Improper error handling | Swallow errors without logging | error-handling | 10 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/executor_agent_test.go:230 | Test uses Confidence: "HIGH" without Evidence: "confidence HIGH" — may fail eligibility check | Use ConfidenceVerified plus the "Found by bruce; confidence HIGH" evidence string that other eligible-finding tests use | correctness | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor_agent_test.go:238 | TestRunVerify_AgentMode_PopulatesFixEndToEnd injects okDispatcher() via the harness seam, so the 'end-to-end' claim does not cover the real buildDispatcher -> snapshot -> read-only jail wiring that production agent mode depends on. It validly proves runVerify threads cc into generateFixes and the fix lands in findings.json, but the actual tool-harness construction for the executor is untested. | Rename/scope the test comment to reflect it covers cc-threading + artifact landing, or add one test exercising the real buildDispatcher against a temp snapshot with agent_mode so the jail/read path is covered at least once in agent mode. | testing | 60 | code-review | claude | MEDIUM |
| 10 | [ ] | HIGH | internal/verify/executor_agent_test.go:251 | Improper error handling | Swallow errors without logging | error-handling | 10 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor_agent_test.go:265 | Test doesn't verify sentinel tags wrap finding | Add assertions that the open and close sentinel tags appear and the finding content sits between them | maintainability | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/executor_agent_test.go:295 | Incorrect error handling | Swallow errors without logging | error-handling | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/executor_agent_test.go:372 | AC5 (agent mode reuses the skeptics' dispatcher, no second buildDispatcher) is only structurally true, never asserted. TestRunVerify_AgentMode_PopulatesFixEndToEnd injects okDispatcher() via the harness closure but never counts harness invocations and the dispatcher is not a tagged/distinct instance, so changing production to build a second dispatcher for the executor would not fail any test. The reuse is real in pipeline.go (one disp threaded to skeptic + executor stages) but unverified. | Wrap the harness func in a call-counter and assert it is invoked exactly once across the run, or pass a sentinel dispatcher instance and assert the executor stage received that same instance. Add a variant with a skeptic also eligible, asserting one dispatcher served both skeptic reads and executor reads. | testing | 30 | code-review | claude | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/executor_test.go:102 | Incorrect error handling | Swallow errors without logging | error-handling | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/executor_test.go:132 | Incorrect error handling | Swallow errors without logging | error-handling | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/executor_test.go:182 | Incorrect error handling | Swallow errors without logging | error-handling | 2 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/log.go:45 | Unhandled error case | Log error and return default value | maintainability | 2 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/mock_test.go:110 | AC2 'executor reads the cited file first' is weakly pinned. TestGenerateFixes_AgentMode_ToolLoopThenFix asserts disp.count() >= 1, but the fake dispatcher only increments a counter; it does not record the tool name or arguments, so it never asserts read_file was actually dispatched (vs any tool) nor that the read preceded the JSON answer. Any non-zero dispatch satisfies the assertion. | Extend the fake dispatcher to record dispatched tool name(s) and assert read_file was among them; optionally assert ordering (read recorded before final content) so the 'first' in AC2 is genuinely pinned. | testing | 30 | code-review | claude | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/pipeline.go:260 | Resource leak | Unbounded goroutines | performance | 15 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/pipeline.go:275 | Resource leak | Unbounded goroutines | performance | 15 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/pipeline_test.go:105 | Resource leak | Unbounded goroutines | performance | 15 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/pipeline_test.go:120 | Resource leak | Unbounded goroutines | performance | 15 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/verify_test.go:102 | Incorrect error handling | Swallow errors without logging | error-handling | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/verify_test.go:132 | Incorrect error handling | Swallow errors without logging | error-handling | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/verify_test.go:182 | Incorrect error handling | Swallow errors without logging | error-handling | 2 | code-review | bruce | MEDIUM |
| 10 | [ ] | MEDIUM | internal/verify/verify_test.go:212 | Incorrect error handling | Swallow errors without logging | error-handling | 5 | code-review | bruce | MEDIUM |
| 10 | [ ] | LOW | internal/verify/verify_test.go:242 | Incorrect error handling | Swallow errors without logging | error-handling | 2 | code-review | bruce | MEDIUM |

### [2026-06-22] From Sprint: 7.3_github_action_pr_integration

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 6 | [ ] | MEDIUM | cmd/atcr/github.go:148-167 | Sequential inline comment posting is slow for PRs with many findings | Use GitHub's batch POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews endpoint | performance | 45 | code-review | bruce | MEDIUM |
| 6 | [ ] | LOW | cmd/atcr/github.go:148-167 | Conclusion is computed twice for the same findings/failOn inputs | Use GitHub's batch POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews endpoint | performance | 45 | code-review | bruce | MEDIUM |
| 6 | [ ] | HIGH | cmd/atcr/github.go:8 | missing import "path/filepath" | Add import "path/filepath" | correctness | 5 | code-review | atcr_bot | MEDIUM |
| 6 | [x] | LOW | cmd/atcr/github.go:33 | usageArgs function not defined in file | Verify usageArgs is imported/defined in package | maintainability | 5 | code-review | bruce | MEDIUM |
| 6 | [x] | MEDIUM | cmd/atcr/github.go:125 | No rate-limit handling for bulk comment posts | Add sleep/backoff between API calls | performance | 10 | code-review | bruce | MEDIUM |
| U | [ ] | LOW | cmd/atcr/github.go:139 | The exit-code gate compares the literal string conclusion == "failure"; Conclusion/BuildCheckOutput switch on raw "failure"/"success"/"neutral" literals across render.go:71,120,122 and github.go:139. These GitHub conclusion values are stringly-typed magic constants with no shared definition, so a typo at one site would silently never trigger exit 1 while still posting a check. | Define typed constants (conclusionFailure="failure", etc.) in the ghaction package and use them at every comparison and emission site. Verify no raw "failure"/"success"/"neutral" literals remain. | maintainability | 15 | code-review | claude | MEDIUM |
| U | [ ] | MEDIUM | cmd/atcr/github.go:150 | postInlineComments treats all errors as failures, including expected 422; should distinguish 422 and only count non-422 errors as failures for systemic problem detection | correctness | 30 | 0 | code-review | bob2 | MEDIUM |
| U | [ ] | MEDIUM | cmd/atcr/github.go:166 | postInlineComments treats posted==0 && failed>0 as a systemic error and exits 1, but the documented benign case (a 422 for a finding whose line is not in the PR diff) breaks this at small N: a review with exactly one anchorable finding that legitimately 422s off-diff fails CI, the precise case the function comment says must not fail the run. | Inspect the client error status code: treat 422/unprocessable (off-diff) as expected/skipped and only fail on systemic auth/permission errors (401/403) dominating. Verify with one off-diff finding: run should succeed "0 posted, 1 skipped", not exit 1. | error-handling | 60 | code-review | claude | MEDIUM |
| 7 | [x] | MEDIUM | internal/ghaction/client.go:80-115 | No retry on transient GitHub API failures (5xx/429/network) | Add exponential backoff retry for transient status codes and network errors | error-handling | 30 | code-review | bruce | MEDIUM |
| 7 | [x] | LOW | internal/ghaction/client.go:27 | Hard-coded 30s HTTP timeout may be insufficient on slow networks | Make timeout configurable via Client field with longer default | performance | 5 | code-review | bruce | MEDIUM |
| 7 | [ ] | LOW | internal/ghaction/client.go:32 | The default client uses a hard-coded per-request Timeout of 30s and runGithub passes cmd.Context() (no deadline), so there is no aggregate bound across the N inline-comment POSTs — a slow GitHub can keep the job alive for 30s*findings. The 30s value is also not configurable. | In baseURL() or runGithub validate the API URL parses as an absolute https URL (allow http only for 127.0.0.1/test) before attaching the Authorization header. | correctness | 30 | code-review | claude | MEDIUM |
| 7 | [x] | LOW | internal/ghaction/client.go:32 | baseURL() only trims trailing slashes; it does not validate that APIURL is https or a sane host before the bearer token is attached (client.go:87). Normally GITHUB_API_URL is trustworthy, but a non-empty-but-malformed api-url (e.g. http://...) would send the token over plaintext to an attacker-influenced endpoint with no guard. | In baseURL() or runGithub validate the API URL parses as an absolute https URL (allow http only for 127.0.0.1/test) before attaching the Authorization header. | correctness | 30 | code-review | claude | MEDIUM |
| 7 | [ ] | MEDIUM | internal/ghaction/client.go:70 | Missing User-Agent header required by GitHub API | Add User-Agent header to HTTP requests | security | 5 | code-review | bruce | MEDIUM |
| 7 | [ ] | MEDIUM | internal/ghaction/client.go:98 | post() treats every non-2xx identically as a terminal error with no retry/backoff. GitHub returns 429/403 with Retry-After for secondary rate limits and transient 502/503; with inline-comments enabled postInlineComments loops one POST per finding (cmd/atcr/github.go:157), so a large finding set can trip secondary rate limits and every subsequent comment fails, converting a transient throttle into "all comments failed → exit 1". No Retry-After/backoff handling exists in the package. | Add minimal retry-with-backoff honoring Retry-After / x-ratelimit-reset for 429/403-rate-limit and 5xx in post(), capped at a few attempts within the context deadline; at minimum distinguish rate-limit responses from genuine 4xx. | error-handling | 120 | code-review | claude | MEDIUM |
| 7 | [ ] | LOW | internal/ghaction/client.go:98 | On a non-2xx response only the body message is surfaced; GitHub's most actionable error context for 403/429 (x-ratelimit-remaining, retry-after, x-github-request-id) is discarded, so the resulting error cannot distinguish "missing checks:write permission" from "rate limited" — the diagnosis a consumer needs. | Add minimal retry-with-backoff honoring Retry-After / x-ratelimit-reset for 429/403-rate-limit and 5xx in post(), capped at a few attempts within the context deadline; at minimum distinguish rate-limit responses from genuine 4xx. | error-handling | 120 | code-review | claude | MEDIUM |
| 7 | [x] | MEDIUM | internal/ghaction/comments.go:30 | BuildInlineComments skips a finding only when File is empty or Line<=0 (comments.go:33); it never checks Problem. A finding with File/Line set but Problem=="" yields commentBody output "ATCR found: ." (comments.go:52) — a posted, content-free inline PR comment. There is no lower bound on body content. | In BuildInlineComments also skip findings whose trimmed Problem is empty (or have commentBody return "" and skip empties in the caller). Verify with {file:a.go, line:5, problem:""}: no comment produced. | error-handling | 15 | code-review | claude | MEDIUM |
| 7 | [x] | MEDIUM | internal/ghaction/comments.go:43 | Markdown injection via Problem/Fix/executor fields in PR comment body | Backtick-escape or strip markdown from user-controlled fields before rendering | security | 15 | code-review | atcr-reviewer | MEDIUM |
| 7 | [x] | MEDIUM | internal/ghaction/comments.go:50 | Unsanitized markdown in PR inline comments | Sanitize or escape f.Problem and f.Fix to prevent malicious markdown/HTML rendering | security | 10 | code-review | greta | MEDIUM |
| 7 | [x] | MEDIUM | internal/ghaction/render.go:114-115 | Truncation count can read "0 of N shown" when first row exceeds byte budget | Track a separate shownCount variable incremented after WriteString, not the loop index i | correctness | 10 | code-review | bruce | MEDIUM |
| 7 | [ ] | LOW | internal/ghaction/render.go:33 | FixAttribution reimplements the executor evidence-token grammar by splitting on "; " and CutPrefix("fix by "), duplicating internal/verify/executor.go serialization across a package boundary with no shared constant for the "; " separator. Any change to the executor join format silently breaks this consumer with no compile-time link. | Export the evidence join/parse from the producing package (internal/verify or reconcile) and have both sides use it, or at least share the "; " separator constant. Verify FixAttribution against the exact strings executor_test.go emits. | maintainability | 30 | code-review | claude, bruce | HIGH |
| 7 | [x] | LOW | internal/ghaction/render.go:40 | FixAttribution may parse evidence prose starting with "fix by " as executor name, causing misleading attribution | Require token to be the last semicolon-separated segment or validate against known executor names | correctness | 10 | code-review | reviewer | MEDIUM |
| 7 | [x] | MEDIUM | internal/ghaction/render.go:48 | isRefuted compares with strings.EqualFold(f.Verification.Verdict, VerdictRefuted) — case-insensitive but no whitespace trim — whereas the canonical refuted check (internal/reconcile/disagree.go:206,210) routes the verdict through canonVerdict (ToLower+TrimSpace). A verdict with surrounding whitespace from an externally produced findings.json slips past EqualFold and is counted as blocking. Same divergence-from-canonical root as the Conclusion gate. | Normalize before comparing, e.g. strings.EqualFold(strings.TrimSpace(f.Verification.Verdict), VerdictRefuted), or reuse the reconcile canonicalizer. Verify with Verdict:" refuted ": must be treated as refuted and excluded. | correctness | 15 | code-review | claude | MEDIUM |
| 7 | [ ] | HIGH | internal/ghaction/render.go:57 | ghaction.Conclusion is a second, divergent merge-gate implementation. The canonical gate reconcile.IsFailing (internal/reconcile/gate.go:96-98) excludes findings whose Category is "out-of-scope" and that exclusion takes precedence over any verdict; atcr reconcile gates on it via CountAtOrAbove (cmd/atcr/reconcile.go:168). Conclusion only checks isRefuted + AtOrAbove(f.Severity, failOn) and never inspects f.Category, so an out-of-scope finding at/above the threshold FAILS the atcr github check while atcr reconcile passes it — the same review yields two opposite merge verdicts, undermining AC2's gate. | Replace the hand-rolled loop in Conclusion with a per-finding call to reconcile.IsFailing(f.Severity, f.Category, f.Verification, failOn, false) so the Action gate cannot diverge from the canonical gate. Verify with a finding {severity:CRITICAL, category:out-of-scope} and --fail-on CRITICAL: expect success, currently failure. | correctness | 60 | code-review | claude | MEDIUM |
| 7 | [ ] | LOW | internal/ghaction/render.go:63 | Incomplete markdown sanitization for check output table | Escape additional markdown control characters to prevent formatting breaks | security | 5 | code-review | greta | MEDIUM |
| 7 | [ ] | MEDIUM | internal/ghaction/render.go:74 | Markdown injection in check-run table cells via Problem/Confidence | Sanitize markdown in cell() (e.g., wrap in backticks, strip links/emphasis) | security | 15 | code-review | and newlines; [text](url) renders as clickable link | MEDIUM |
| 7 | [x] | MEDIUM | internal/ghaction/render.go:114 | BuildCheckOutput is exported and calls reconcile.ParseSeverity(failOn) discarding the error (threshold, _ := ...). When failOn is non-empty but invalid, the title branch runs, ParseSeverity returns ("", err), and the title becomes "atcr — N finding(s), 0 at/above " with an empty threshold; Conclusion separately returns "success" because AtOrAbove is false for an unknown threshold — an invalid threshold silently degrades to a malformed-title pass instead of an error. runGithub validates failOn but the exported function does not. | Document BuildCheckOutput as requiring a pre-validated canonical failOn, or surface/handle the parse error and render the raw failOn when parse fails. Verify BuildCheckOutput(findings, "bogus") title. | correctness | 30 | code-review | claude | MEDIUM |
| 7 | [x] | LOW | internal/ghaction/render.go:131 | The findings table renders cell(f.Severity) and cell(f.Confidence) verbatim with no normalization, while the gate (Conclusion/AtOrAbove) normalizes severity internally. A finding with severity:"critical" (lowercase) or " HIGH" displays un-normalized in the table while still counting toward the gate, so the displayed Severity column and the gate's effective severity can visually disagree. | Normalize severity via the same canonicalizer the gate uses (reconcile.ParseSeverity best-effort) before rendering the cell. Verify a severity:"critical" finding displays as CRITICAL. | maintainability | 15 | code-review | claude | MEDIUM |
| 7 | [x] | LOW | internal/ghaction/render.go:140 | Check-run Summary duplicates Title verbatim | Use a distinct summary (e.g., gate verdict line) so the check UI shows different text | maintainability | 5 | code-review | atcr-reviewer | MEDIUM |
| 8 | [ ] | MEDIUM | Escape or inline-code-fence cell content (wrap each value in a backtick span after escaping embedded backticks) so findings text renders inert. Verify a finding whose Problem contains a markdown link and a fake | CRITICAL | " row renders as literal text. | security | 60 | code-review | claude | MEDIUM |
| 8 | [x] | MEDIUM | action.yml:28 | Required GitHub permissions (checks:write and, for inline comments, pull-requests:write) are documented only in the one-line github-token input description. A composite action cannot declare permissions:; a consumer who omits permissions:{checks:write} gets a late runtime 403 from the atcr github step with no early signal. | Document the required permissions: block prominently (docs + a comment at top of runs:) and/or add a preflight that fails fast with a clear "missing checks:write" message instead of relying on the API 403 surfacing late. | maintainability | 30 | code-review | claude | MEDIUM |
| 8 | [ ] | LOW | action.yml:37 | Cache path uses absolute action path | Use relative path like go.sum | maintainability | 2 | code-review | bruce | MEDIUM |
| 8 | [ ] | LOW | action.yml:51 | cache-dependency-path assumes go.sum exists at github.action_path | Pin to a full commit SHA to harden the composite action's supply chain | correctness | 10 | code-review | bruce | MEDIUM |
| 8 | [ ] | MEDIUM | action.yml:51 | BASE_REF fallback produces invalid ref when github.base_ref empty | Pin to a full commit SHA to harden the composite action's supply chain | correctness | 10 | code-review | bruce | MEDIUM |
| 8 | [x] | LOW | action.yml:51 | actions/setup-go pinned to floating major tag, not commit SHA | Pin to a full commit SHA to harden the composite action's supply chain | correctness | 10 | code-review | atcr-reviewer | MEDIUM |
| 8 | [ ] | HIGH | action.yml:70 | Invalid GitHub Actions expression syntax | Use conditional logic in shell step instead of |  | 0 | code-review | 10 | MEDIUM |
| 8 | [ ] | LOW | action.yml:74 | Potential shell injection via input interpolation | Use environment variables for all inputs in the `run` block to avoid script injection | security | 10 | code-review | otto | MEDIUM |
| 8 | [x] | MEDIUM | action.yml:78 | Malformed base ref origin/ when triggered on non-pull_request events | Fail fast with a clear error if github.event_name != pull_request and inputs.base-ref is empty | correctness | 10 | code-review | bruce | MEDIUM |
| 8 | [ ] | HIGH | action.yml:93 | HEAD_SHA prefers PR head commit which does not exist for fork PRs | Use github.sha merge commit SHA for all events | correctness | 5 | code-review | atcr-reviewer | MEDIUM |
| 8 | [x] | LOW | action.yml:99 | PR_NUMBER is set from github.event.pull_request.number, empty on non-pull_request triggers. With INLINE=true on such a trigger, --pr "" reaches the pr<=0 guard (cmd/atcr/github.go:93) and fails as a usage error (exit 2) — safe, but with a generic "requires --pr" message rather than a clear "inline-comments requires a pull_request event" diagnosis. | Guard INLINE=true against an empty PR_NUMBER in the action shell and emit an explicit error, mirroring the HEAD_SHA fallback pattern (action.yml:93-95), or document the constraint. | error-handling | 15 | code-review | claude | MEDIUM |
| U | [ ] | LOW | docs/ci-integration.md:23 | Out of scope reference to GitHub Action | Remove or update to reflect current implementation | maintainability | 1 | code-review | REVIEWER | MEDIUM |

### [2026-06-22] From Sprint: epic-7.4

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [ ] | LOW | internal/verify/executor.go:420 | Agent-mode buildExecutorAgent leaves ToolBudgetBytes unlimited, so max_tool_calls bounds turn count but nothing bounds total bytes read across the loop; a multi-turn loop over large files can accumulate a large model context (per-result size is still dispatcher-capped). | Forward a ToolBudgetBytes ceiling for agent mode, or document that max_tool_calls is the only agent-mode budget knob. | UNDER_ENGINEERING | 30 | execute-epic-independent |
| U | [ ] | LOW | internal/verify/executor.go:336 | Agent-mode success path emits no log line (only failures and the fallback are logged), so there is no positive signal that the tool loop actually ran for a given finding. | Add a debug-level log on agent-mode entry naming file:line and the resolved max_tool_calls budget, mirroring the skeptic debug discipline. | OBSERVABILITY | 15 | execute-epic-independent |

### [2026-06-22] From Sprint: 7.2_radar-renderer-consolidation

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 4 | [x] | LOW | internal/reconcile/disagree.go:476 | formatScore uses if s == float64(int64(s)) with no guard for NaN/+-Inf; int64(NaN) is implementation-defined and the equality is false, so a NaN score would render as "NaN" into the report via FormatFloat. Scores are never NaN today (no division in the scoring path), so this is theoretical and pre-existing, copied verbatim into the now-single source of truth. Recorded as a known non-issue, not an action item. | Only if the scoring path ever divides: add if math.IsNaN(s) or math.IsInf(s,0) handling in formatScore. No fix or verification needed today. | correctness | 5 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | internal/reconcile/disagree.go:413 | RadarTextRenderer func parameter lacks enforced escaping contract | Document requirement or wrap in interface to guarantee escaping and prevent future XSS | security | 10 | code-review | greta | MEDIUM |
| 4 | [x] | MEDIUM | internal/reconcile/disagree.go:436 | Missing nil check for renderText | Add one golden test in internal/reconcile/disagree_test.go that pins the FULL expected string of WriteRadarItems for the all-fields markerItem() (built at line 603) via assert.Equal on the exact rendered block. Verify by mutating a single space in the Fprintf at disagree.go:444 and confirming the test fails. | error-handling | 30 | code-review | bruce | MEDIUM |
| U | [ ] | MEDIUM | internal/reconcile/disagree.go:436 | The epic's stated parity oracle is "existing golden tests", but no byte-exact golden fixture exists for the rendered radar. Every radar test uses assert.Contains/ordering (TestWriteRadarSection_EmitsHeaderThenItemsVerbatim, TestRenderMarkdown_DisagreementsRadar at disagree_test.go:288), so a future edit to spacing, the "· score" separator, or a trailing newline in the now-single WriteRadarItems would pass CI silently. The byte-output-preserving claim is asserted, not proven by the suite. (intent_note: deferred per epic §Technical Approach) | Add one golden test in internal/reconcile/disagree_test.go that pins the FULL expected string of WriteRadarItems for the all-fields markerItem() (built at line 603) via assert.Equal on the exact rendered block. Verify by mutating a single space in the Fprintf at disagree.go:444 and confirming the test fails. | error-handling | 30 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | internal/reconcile/disagree.go:456 | WriteRadarItems and WriteRadarSection dereference the injected renderText RadarTextRenderer at lines 456/459/468 with no nil guard, and both functions are exported. The pre-refactor code hardcoded esc/escTrunc, so a nil renderer was structurally impossible; the consolidation converts that impossible state into a latent nil-call panic for any current or future caller passing nil. An item with no Problem/Detail/Positions skips the call, masking the bug until a populated item appears. | In WriteRadarItems guard at entry: if renderText == nil { renderText = esc } (sane verbatim default), or document the precondition explicitly. Verify with WriteRadarItems(&b, []DisagreementItem{markerItem()}, "### ", nil) asserting no panic. | error-handling | 15 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/reconcile/disagree.go:461 | Potential for inconsistent escaping if renderText doesn't escape | The `RadarTextRenderer` type is a generic `func(string) string`. If a caller passes a renderer that does not perform escaping (unlike `esc` or `escTrunc`), it could introduce markdown injection vulnerabilities in the Problem/Detail fields. | correctness | 15 | code-review | otto | MEDIUM |
| 4 | [x] | MEDIUM | internal/reconcile/disagree.go:468 | Missing nil check for renderText | Add nil check or panic | error-handling | 2 | code-review | bruce | MEDIUM |
| 4 | [x] | LOW | internal/reconcile/emit.go:469 | The consolidation collapsed the duplicated radar renderer but left two byte-identical esc functions (and identical newlineFlattener Replacers) in reconcile/emit.go and report/render.go. The radar renderer was unified; the escaper it relies on for parity was not. If one esc drifts (e.g. someone escapes "[" in only one package) the parity this epic just established silently breaks and no test catches it (see the golden-test finding). Pre-existing duplication the epic surfaced but left in place. | Have report.esc delegate to an exported reconcile.Esc (report already imports reconcile), or document why the duplication is intentional. Verify the report display escaping tests still pass. | maintainability | 30 | code-review | claude | MEDIUM |
| 5 | [ ] | LOW | internal/report/disagree.go:27 | escTrunc may truncate raw input before escaping causing malformed output | Ensure truncation occurs after escaping or on safe rune boundaries | security | 5 | code-review | greta | MEDIUM |
| 5 | [x] | MEDIUM | internal/report/disagree_test.go:0 | Sprint plan requires test updates but diff omits report test file | Update tests to remove references to deleted helpers (writeRadarSection, writeRadarItems, formatScore, reviewerOrUnknown) or confirm no test changes are needed | correctness | 10 | code-review | bruce | MEDIUM |
| 5 | [x] | MEDIUM | internal/report/render_test.go:1 | Sprint plan lists render tests as in-scope; diff updates internal/report/render.go which may have rendered output tests needing fixture updates | Verify test fixtures still produce byte-identical output with the new shared renderer; update if needed | correctness | 10 | code-review | bruce | MEDIUM |

### [2026-06-22] From Sprint: 7.1_local_syntax_guard

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 3 | [ ] | LOW | internal/verify/executor.go:154 | generateFixes sets f.FixWarning directly on []reconcile.JSONFinding and it persists only because the verify path re-emits JSONFinding in place. reconcile.JSONFindings() builds JSONFinding from Merged, which has no FixWarning field, so any future path that round-trips a fix-bearing finding back through the reconcile Merged path would silently drop the warning. Correct today, but the invariant is implicit and one refactor from a silent data-loss bug; the ownership reasoning at executor.go:145-149 is documented but unenforced. | Add a one-line comment in JSONFindings() (emit.go) noting FixWarning is intentionally not copied because verify owns it post-reconcile, to stop a future maintainer from re-adding the copy and changing behavior. No functional change. Verify ClearsStaleInvalidSyntaxOnValidFix still passes. | maintainability | 15 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/executor_test.go:92 | Dead code | Remove unused variable 'hadFence' | maintainability | 2 | code-review | bruce | MEDIUM |
| 3 | [ ] | MEDIUM | internal/verify/syntaxguard.go:99-103 | looksLikeGoCode false-positives on prose change-instructions starting with decl keywords (e.g. "Import the sync package", "Type the result into the buffer", "Const values should be defined at package level") | Require an additional code signal alongside the decl keyword, or only treat `package` as a code signal and require braces/`:=` for the others | correctness | 15 | code-review | code-reviewer | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/syntaxguard.go:29 | fenceRe requires `\n` immediately after the opening fence and rejects the CommonMark-permitted space (e.g. ` ``` go\n...`) — such fences fall through as unfenced, so a malformed fix in a spaced fence is never flagged (disagreement: LOW vs MEDIUM) | Allow optional whitespace between the opening fence and language tag, or trim a leading space in the capture | correctness | 20 | code-review | AutoReviewer, code-reviewer | HIGH |
| 3 | [x] | MEDIUM | internal/verify/syntaxguard.go:35 | fenceRe only accepts an opening fence line of `{3,}<tag>[ \t]*\n — the info string may be a language tag then whitespace only. A CommonMark fence opened with a trailing word (```go some note or ```python extra) does not match, so extractFencedCode returns hadFence=false and the ENTIRE raw string including backticks is passed to parseGoFix. The nonGoFenceLangs skip at syntaxguard.go:88 is bypassed, so a non-Go fenced block with an info string can be flagged as broken Go. | Tolerate trailing info-string text by changing the tag-trailing class from [ \t]* to [^\n]* (CommonMark allows it), OR document as an accepted fence gap alongside the existing fence WONTFIXes. Verify with a fence-with-info-string test using a non-Go tag plus trailing word asserting no flag. Tracked under epic 7.5_syntax-guard-refinements. | correctness | 30 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/syntaxguard.go:47 | fenceRe regex rejects valid markdown fences with trailing spaces after language tag | In syntaxguard.go require blockOpenRe to co-occur with a second code signal (a matching blockCloseRe or a declKeywordRe hit) before looksLikeGoCode treats unfenced text as code, OR explicitly document this as an accepted gap. Verify with a prose-ending-in-brace test asserting no invalid_syntax flag. Tracked under epic 7.5_syntax-guard-refinements. | correctness | 30 | code-review | bruce | MEDIUM |
| 3 | [ ] | MEDIUM | internal/verify/syntaxguard.go:47 | blockOpenRe (^[ \t]*[^{]*\{[ \t]*$) makes looksLikeGoCode return true for any unfenced line whose last non-whitespace char is a lone opening brace. A legitimate prose change-instruction whose line ends in { (e.g. "Wrap the body in the literal that opens with {") then fails go/parser and is stamped invalid_syntax. This is the exact trust-degrading false positive the guard header (lines 16-21, 148-156) claims to avoid, and it is NOT one of the three documented WONTFIXes (first-fence-only / OR-ed strategies / no-block-structure). | In syntaxguard.go require blockOpenRe to co-occur with a second code signal (a matching blockCloseRe or a declKeywordRe hit) before looksLikeGoCode treats unfenced text as code, OR explicitly document this as an accepted gap. Verify with a prose-ending-in-brace test asserting no invalid_syntax flag. Tracked under epic 7.5_syntax-guard-refinements. | correctness | 30 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/syntaxguard.go:71 | Unbounded input length for syntax validation | Add max length check before parsing | security | 5 | code-review | greta | MEDIUM |
| 3 | [x] | LOW | internal/verify/syntaxguard.go:83 | The size cap len(fix) > maxFixBytes is measured on the raw fix BEFORE normalizeNewlines and extractFencedCode, while the triple parser.ParseFile actually runs on the smaller extracted code. The check is conservative (can only over-protect) so it is benign, but the comment (lines 73-78) overstates precision and a small Go snippet wrapped in a huge prose blob is skipped entirely rather than validated. | Optional: move the cap check to after extractFencedCode so the bound applies to the code actually parsed, or soften the comment to match the raw-byte semantics. Verify the oversized-input short-circuit test still passes. | maintainability | 15 | code-review | claude | MEDIUM |
| 3 | [ ] | MEDIUM | internal/verify/syntaxguard.go:96 | Parser error positions in FixWarning are relative to the wrapped synthetic Go code, not the user's fix string | Adjust the line/column offset based on which strategy produced the error, or format the error to be relative to src | correctness | 20 | code-review | bruce | MEDIUM |
| 3 | [ ] | MEDIUM | internal/verify/syntaxguard.go:109 | Syntax errors from wrapped parses report misleading line numbers | Adjust positions by subtracting synthetic prefix lines or report message only | correctness | 15 | code-review | reviewer | MEDIUM |
| 3 | [ ] | MEDIUM | internal/verify/syntaxguard.go:115 | looksLikeGoCode can produce false positives for prose with code-like features (declaration keywords at line start, line-ending braces) (disagreement: LOW vs MEDIUM) | Refine the heuristic to require stronger signals (e.g., fenced code only), or document the trade-off more prominently | correctness | 15 | code-review | otto, bruce | HIGH |
| 3 | [x] | LOW | internal/verify/syntaxguard.go:143 | Unclear naming | Rename 's' parameter to 'src' | maintainability | 3 | code-review | bruce | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/syntaxguard.go:150 | Missing error handling | Check result of extractFencedCode | error-handling | 5 | code-review | bruce | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/syntaxguard.go:180 | Missing error handling | Check result of parseGoFix | error-handling | 5 | code-review | brice | MEDIUM |
| 3 | [x] | LOW | internal/verify/syntaxguard_test.go:72 | Test comment claims := triggers flag, but looksLikeGoCode actually flags via trailing brace | Update comment to reflect actual heuristic trigger | maintainability | 2 | code-review | bruce | MEDIUM |
| 3 | [x] | LOW | internal/verify/syntaxguard_test.go:166 | Tests cover trailing whitespace after the fence tag and a # in the tag, but not a trailing WORD (CommonMark info string) which silently disables fence detection (see syntaxguard.go:35 item). A regression in fenceRe info-string handling would go untested. | Add a test with ```go note or ```python extra asserting the intended fence-detection behavior. | testing | 15 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/syntaxguard_test.go:180 | The suite tests the inner-brace non-match case (x{y{ via TestValidateGoFixSyntax_InlineDoubleBraceLineNotFlagged) but has no test for a prose line whose LAST char is a lone {, which is the actual blockOpenRe-fires-on-prose case. That branch is the one regression a future blockOpenRe tweak would silently introduce, leaving the riskiest false-positive path uncovered. | Add a test pinning the contract for a prose change-instruction ending a line with { (assert current flag behavior or desired no-flag). Pairs with the syntaxguard.go:49 correctness item. | testing | 15 | code-review | claude | MEDIUM |

### [2026-06-22] From Sprint: epic-7.3

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [ ] | MEDIUM | cmd/atcr/github.go:postInlineComments | Inline comments post one-by-one via the PR comments API: N API calls + N notifications on large PRs and accumulating duplicates across re-runs (each push re-posts on the new head SHA) with no dedup | Switch to the batched reviews API (POST /pulls/{n}/reviews with a comments array) for a single review event, and dedup against existing atcr comments before posting | CROSS_CUTTING | 60 | execute-epic-stage3 |
| U | [ ] | MEDIUM | cmd/atcr/github.go:107 | readReconciledFindings failures (missing/empty/malformed reconciled data) return exit 2 (usage), but inside the action pipeline review+reconcile already ran, so a transient parse/IO error is misreported as misconfiguration | Map a present-but-unreadable reconciled file to exit 1 (operational) and reserve exit 2 for genuinely-absent data; align atcr report's readReconciledFindings the same way for contract consistency | ERROR_PATHS | 30 | execute-epic-independent |
| 1 | [x] | MEDIUM | cmd/atcr/github.go:132 | Partial inline-comment failures (some posted, some 422) are surfaced only on stderr/stdout, never in the check run, so the PR check does not reflect missing comments | Post comments before the check run and include the skipped/failed count in the check output text (or update the check after posting), so partial delivery is visible on the PR surface | ERROR_PATHS | 45 | execute-epic-independent |
| 1 | [x] | LOW | cmd/atcr/github.go:126 | No machine-readable output (GITHUB_OUTPUT) exposes the conclusion or finding counts, so downstream workflow steps cannot branch on the gate result except via job exit status | Emit conclusion and finding counts to $GITHUB_OUTPUT when running under Actions | OBSERVABILITY | 20 | execute-epic-independent |
| 2 | [x] | LOW | internal/ghaction/render.go:90 | BuildCheckOutput lists refuted findings at raw severity with no demoted/refuted marker while Conclusion correctly excludes them from the gate, so a reader may see a HIGH row under a passing gate | Render a refuted finding with a struck/annotated severity (e.g. (refuted)) or move it to a separate demoted section in the check table | EDGE_CASES | 30 | execute-epic-stage3 |
| 2 | [ ] | MEDIUM | internal/ghaction/comments.go:50 | Finding Problem/Fix (untrusted model output) are interpolated verbatim into the PR comment body, so crafted content can inject markdown, @mentions or #issue refs that GitHub renders/notifies on when inline-comments is enabled | Defang @ and # autolinking and strip HTML-comment sequences before posting, preserving AC3's plain sentence format (decision needed: defang vs backtick-wrap vs AC3 fidelity) | SECURITY | 45 | execute-epic-independent |
| 2 | [x] | LOW | internal/ghaction/render.go:30 | FixAttribution returns the FIRST "; "-segment beginning with "fix by ", but the executor token is always appended last; an earlier prose segment literally starting with "fix by " (rare; reviewer prose is not validated like executor names) would win | Scan segments in reverse / anchor on the last "fix by" segment since appendFixAttribution always appends last | EDGE_CASES | 10 | execute-epic-independent |
| 2 | [ ] | LOW | internal/ghaction/client.go:60 | CreateCheckRun discards the response body, so the check-run id is unavailable and re-runs create duplicate check runs on the PR rather than updating one | Parse and return the check-run id to enable update-in-place across re-runs (or document the per-run duplicate-check behavior) | EDGE_CASES | 45 | execute-epic-independent |
| U | [ ] | LOW | action.yml:atcr github | Composite action assumes a pull_request event (head.sha/base_ref/pull_request.number); a non-PR trigger fails deep inside atcr review with an empty --base rather than a clear up-front guard | Add an early step asserting github.event_name == pull_request (or that head.sha is non-empty) with an actionable error message | INTEGRATION | 15 | execute-epic-stage3 |
| U | [ ] | LOW | action.yml:60 | The atcr review step assumes origin/<base> was fetched; a consumer who omits fetch-depth:0 gets a deep atcr range-resolution failure instead of an early action-level error | Add a guard step that git rev-parse --verify origin/<base> and emits an explicit fetch-depth:0 error before invoking atcr review | UNDER_ENGINEERING | 15 | execute-epic-independent |

### [2026-06-22] From Sprint: epic-7.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [ ] | LOW | internal/reconcile/disagree.go:456 | WriteRadarItems/WriteRadarSection panic if the exported renderText (RadarTextRenderer) param is nil — no guard on the newly-exported API | Default a nil renderText to esc at the top of WriteRadarSection and WriteRadarItems | EDGE_CASES | 15 | execute-epic-stage3 |
| U | [ ] | LOW | internal/reconcile/disagree_test.go:171 | TestWriteRadarItems_TextRendererControlsTruncation uses an ad-hoc 80-rune truncator rather than the real report.escTrunc 500-rune cap (the production 500 boundary is pinned only indirectly via report's existing GrayZoneEscapesAndTruncates test) | Add an explicit report-side assertion exercising the real escTrunc 500-rune cap through reconcile.WriteRadarItems, or document that coverage lives in the existing report truncation test | EDGE_CASES | 15 | execute-epic-independent |

### [2026-06-22] From Sprint: epic-7.1

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | MEDIUM | internal/verify/syntaxguard.go:130 | An unfenced multi-line JSON/config snippet with block braces can still satisfy looksLikeGoCode and be parsed as Go, producing a spurious invalid_syntax flag on non-Go content (residual after heuristic hardening). | Detect obviously non-Go brace content (JSON object / key:value lines) before treating block braces as a Go signal; deferred as a separate design refinement (unfenced non-Go fixes are rare; fenced non-Go is already handled). [Deferred 2026-06-22 to Epic Plan 7.5 syntax-guard-refinements per clarification] | EDGE_CASES | 30 | execute-epic-independent |

### [2026-06-22] From Sprint: 7.0.1_executor_model_configuration

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 7 | [/] | LOW | internal/registry/config.go:524 | Potential prompt injection via system_prompt (Won't-fix: config.go:531–534 explicitly documents that control chars are intentionally NOT rejected in system_prompt; the --- delimiter added at executor.go:225 eliminates the CRLF metadata-forgery surface; otto's fix conflicts with the documented design decision) | Add control character validation to SystemPrompt | security | 15 | code-review | otto | MEDIUM |

### [2026-06-21] From Sprint: epic-6.0

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | MEDIUM | internal/debate/debate.go:165 | Gray-zone cluster merge/separate rulings are recorded in debate.json but not physically applied to findings.json; clusters still resolve via the existing adjudication path (Deferred: Epic Plan 6.1) | Wire the judge cluster decision into the reconcile adjudication application so unattended runs auto-merge gray-zone clusters inline | INTEGRATION | 60 | execute-epic-stage3 |

### [2026-06-20] From Sprint: epic-5.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/cache/store.go:115 | Eviction does a full ReadDir+Stat of the cache dir on every Put even when under the cap; O(n) per write scales poorly if the cache accumulates thousands of entries (Won't-fix 2026-06-21: scan runs serially under the store mutex and LLM calls dominate latency; no Epic 5.2 perf criterion requires O(1) Put — added state not justified at LOW) | Maintain a running total-size counter and skip the directory scan when it is under the cap, or evict only on a periodic/threshold basis | PERFORMANCE | 30 | execute-epic-cumulative |

### [2026-06-20] From Sprint: epic-5.0

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/report/render.go:317 | The "File not found" warning format string is duplicated across internal/reconcile/emit.go (writeFindingsList) and internal/report/render.go (writePathWarning) in separate packages | Extract a shared constant/helper only if a common low-level rendering package emerges; a cross-package dependency is not justified for one format string today (Won't-fix 2026-06-21: two independent format strings across packages with no shared dependency; the recorded fix confirms extraction is not justified at this scope) | CROSS_CUTTING | 15 | execute-epic-cumulative |
| U | [/] | MEDIUM | internal/reconcile/reconcile.go:26 | Validation root is hardcoded to "." at every call site, so "atcr reconcile <path>" for a review of another repo, or running from a non-repo-root CWD, falsely flags every finding as "file not found" | Thread the reviewed repo root explicitly or add a --repo flag, applied consistently with the verify stage which uses the same "." convention (Deferred 2026-06-21: the narrow Root: os.Getwd() variant is a no-op — filepath.Abs(".") already equals the CWD, so it would not fix the non-repo-root / other-repo case; the real fix is to plumb the reviewed-repo path explicitly via a --repo flag threaded through the reconcile and verify call sites, est 60) | EDGE_CASES | 60 | execute-epic-independent |

### [2026-06-20] From Sprint: 4.7.1_backup-swap-hardening

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 3 | [/] | MEDIUM | internal/fanout/reviewdir.go:318 | backupExisting() unconditionally RemoveAll's <path>.bak.old and <path>.bak.new at entry, but forceBackupOutputDir() only calls guardForeignBackup(dir+".bak") — the two staging siblings are not guarded. On an arbitrary --output-dir a user who owns <output-dir>.bak.old or <output-dir>.bak.new has it silently deleted by --force, re-opening the Epic 4.7 "never silently delete user data" contract. NOTE: epic clarification Q4 deliberately scoped the guard to .bak only (atcr-internal suffixes); this finding re-raises that decision as worth reconsidering, not a defect against the recorded scope. (intent_note: deferred per epic §Clarifications Q4 — extending guardForeignBackup to .bak.old/.bak.new is out of scope) (Won't-fix 2026-06-20: binding Epic 4.7.1 Q4 — guard stays scoped to .bak; .bak.old/.bak.new are atcr-owned staging names cleared by entry-time RemoveAll; confirmed via /sprint-clarification 95%) | In forceBackupOutputDir() (reviewdir.go:440) extend the guard to guardForeignBackup(dir+".bak.old") and guardForeignBackup(dir+".bak.new") before backupExisting, or refuse if either exists non-empty. Add a test pre-creating a foreign <output-dir>.bak.old with user content and assert --force errors instead of deleting it. | security | 60 | code-review | claude | MEDIUM |
| 3 | [/] | MEDIUM | internal/fanout/reviewdir.go:373 | The advertised "an interrupted swap never leaves the user with neither generation" invariant is conditional on a best-effort SILENT restore succeeding. If restorePriorBackup's os.Rename(backupOld,backup) fails (e.g. backup partially recreated, or perms change), the only surviving copy is stranded under .bak.old — which the very next run's entry-time RemoveAll(backupOld) (line 318) then deletes. Same pattern at the copy site swapStagedBackup (atomic.go:159-167) feeding atomic.go:97 RemoveAll(bakOld). (Won't-fix 2026-06-20: observability half done (restore failure now logged); the lone-.bak.old-as-generation redesign is rejected per Epic 4.7.1 Q3 — .bak.old is atcr-owned temp deleted at entry, one-generation contract + TestBackupExisting_CleansStaleStagingStragglers stand; confirmed via /sprint-clarification 95%) | Surface restore failures loudly (log/return) instead of dropping them, and/or do not auto-RemoveAll .bak.old at entry when .bak is absent — treat a lone .bak.old as the surviving generation to recover. Add a test where restore fails and a subsequent run is asserted not to delete the only surviving copy. | correctness | 120 | code-review | claude | MEDIUM |
| 3 | [/] | LOW | internal/fanout/reviewdir.go:388 | backupCrossDevice's inner os.Rename(backupNew,backup) relies on backupNew and backup sharing a filesystem, an invariant that holds only by naming coincidence (both are siblings of path). If anyone later relocates backupNew under path, the inner rename silently becomes cross-device and returns a raw EXDEV to the user. (Won't-fix 2026-06-20: same-fs invariant holds by construction — backup and backupNew are both siblings of path; a runtime guard would be unreachable dead code today and is already documented at reviewdir.go:415-419; confirmed via /sprint-clarification 90%) | Add a test that forces renameFn to return syscall.EXDEV and makes the copy fail (a copy seam, or unreadable src), staging a prior .bak first; assert the prior .bak content is restored intact, the live tree survives, and .bak.new is cleaned up. Cover the copy-failure leg at minimum. | testing | 120 | code-review | claude | MEDIUM |
| 3 | [/] | LOW | internal/fanout/reviewdir_test.go:458 | Test-coverage gaps in the failed-swap/cleanup paths: TestBackupExisting_FailedSwapPreservesPriorBak asserts no .bak.old straggler but not .bak.new; the non-ErrNotExist Lstat(backup) error branch (reviewdir.go:333-335) is untested; the entry-time RemoveAll straggler-cleanup failure legs (reviewdir.go:318-323) are untested. Each is a real error branch a regression could silently break. | Add assert.NoDirExists for src+".bak.new" at reviewdir_test.go:458; add a perms-based test forcing Lstat(backup) to fail with a non-ErrNotExist error; add a test where .bak.old cannot be removed and assert the typed "clearing stale staging backup" error. Skip on root/CI where perms are not enforced. (Partial 2026-06-21: gaps 1 (.bak.new assertion) and 3 (RemoveAll(.bak.old) failure) covered; gap 2 (non-ErrNotExist Lstat(backup)) deferred — needs an lstatFn production seam since the staging siblings share a parent dir so perms cannot isolate it) | testing | 30 | code-review | claude | MEDIUM |

### [2026-06-19] From Sprint: 4.7_idempotency

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 3 | [/] | MEDIUM | internal/fanout/reviewdir.go:284 | backupExisting() does os.RemoveAll(backup) then os.Rename(path, backup); BackupToDotBak() does os.RemoveAll(bak) then copyTree. If the rename/copy fails (cross-filesystem EXDEV when --output-dir and its .bak sibling are on different mounts, disk-full mid-copy, or SIGKILL), the single prior backup generation is already destroyed while the new backup is absent or partial. The live/original tree is preserved (good), but the one recoverable prior .bak is lost for no benefit — counter to the safe-to-retry goal. (disagreement: LOW vs MEDIUM) (Deferred: Epic Plan 4.7.1) | Stage the new backup first: copy/rename into <path>.bak.new (or rename old .bak aside to .bak.old), confirm it is complete, then swap and only then remove the old generation. For backupExisting, attempt os.Rename before RemoveAll so a failed rename leaves the old .bak intact; detect EXDEV and fall back to copy+remove. Add a fault-injection test asserting the prior .bak survives a failed swap. | correctness | 120 | code-review | code-reviewer, claude | HIGH |

### [2026-06-19] From Sprint: 4.5_circuit_breaker

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 2 | [/] | MEDIUM | internal/fanout/metrics.go:37 | recordAgentOutcome zeroes apiCalls for any result whose error unwraps to context.DeadlineExceeded/Canceled, assuming no request was made. But a per-agent timeout routinely fires AFTER real HTTP round-trips (and mid tool-loop after several Chat turns already hit the wire), so atcr_api_calls_total undercounts real provider traffic exactly when a provider is degraded. The no-request assumption is only provably safe for CircuitOpenError and pre-first-send cancellation. (Deferred: Epic Plan 4.11) | Use max(1, r.Turns) for the deadline case instead of a flat 0, or thread a real calls-attempted counter out of the client rather than inferring from the terminal error class. Keep the apiCalls=0 path only for CircuitOpenError. | performance | 60 | code-review | claude | MEDIUM |

### [2026-06-18] From Sprint: epic-4.3

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | LOW | internal/validation/validation.go:62 | System-dir denylist covers only /etc, /proc, /sys, leaving /boot, /dev, /root, /var and ~/.ssh writable via --output-dir / --output (deliberate Option-B permissive choice) (Won't-fix: intended Option-B policy per epic 4.3 clarifications 2026-06-18; revisit only on a concrete isolation requirement) | If stronger isolation is later required, switch to an allowlist anchored at the repo/.atcr root instead of a denylist | SECURITY | 30 | execute-epic-independent |
| 1 | [/] | LOW | internal/validation/validation.go:86 | Severity and Enum validators are shipped but wired to nothing (ParseSeverity/ValidFormat remain the live paths); they exist only to satisfy AC5/AC7 and future use (Won't-fix: intentionally public for AC5/AC7 per epic 4.3 clarifications; deletion breaks ACs, wire-in out of scope) | Revisit and delete if no caller adopts them within a release, or wire them in where duplication can be removed | OVER_ENGINEERING | 15 | execute-epic-independent |

### [2026-06-18] From Sprint: epic-4.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/registry/attribution.go:55 | attribute() recurses and reconstructs the join even for a single-error errors.Join (which still satisfies Unwrap() []error), a small avoidable cost on the load-time validation path | Optionally short-circuit when len(children)==1 if profiling ever flags it (WON'T-FIX 2026-06-18: trigger unmet — error-path only, never hit on normal load; no perf AC in epic 4.2) | INTEGRATION | 5 | execute-epic-independent |

### [2026-06-18] From Sprint: epic-4.1.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | MEDIUM | internal/fanout/review.go:361 | If a server shutdown (or CLI SIGINT) fires after all agents already succeeded but before ExecuteReview's interrupted := errors.Is(ctx.Err(), context.Canceled) check, a fully-completed run is stamped Interrupted=true and status.go:216 overrides RunCompleted to RunInterrupted (a false interrupted; inverse of AC4). Pre-existing in the CLI-shared path, newly reachable via MCP shutdown. | Gate the interrupted marker on at least one agent ending in StatusTimeout/cancelled rather than purely on parent ctx.Err()==Canceled. NOTE: touches CLI-shared review.go (out of scope for epic 4.1.2's MCP-only change); window is microscopic and outcome benign (resume no-ops a complete run) - separate design. (WON'T-FIX 2026-06-18: --resume self-healing via ClearInterrupted (resume.go:220) already recovers a stale interrupted-on-complete; revisit in a backlog sprint if insufficient) | CORRECTNESS | 30 | execute-epic-independent |

### [2026-06-17] From Sprint: epic-4.1

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | MEDIUM | internal/mcp/handlers.go:88 | Serve-mode background fan-out runs under context.WithoutCancel(ctx) so a SIGINT to the MCP server never cancels or marks an in-flight detached review interrupted; it is allowed to finish (intended MCP design) but never gets the interrupted marker CLI mode promises. (Deferred: Epic Plan 4.1.2) | Decide whether detached MCP reviews should be marked interrupted on server shutdown; if so, thread a cancellable/interrupt-aware context or post-hoc marker into the background review path — a separate design from this CLI-focused epic. | REGRESSION_RISK | 60 | execute-epic-independent |

### [2026-06-17] From Sprint: 4.0_structured_logging

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [/] | HIGH | cmd/atcr/review.go:172 | Both review entry points call `NewRedactor(root)` with ZERO configured secrets — cmd/atcr/review.go:172 and internal/mcp/handlers.go:87 pass only the path root, never the registry API keys. The exact-value secret-scrubbing loop in Redact is therefore dead in production; redaction relies entirely on the `sk-`/`Bearer` regexes, which miss raw provider keys that lack those prefixes (Google `AIzaSy...`, Azure `api-key`, JWTs `eyJ...`). The AC5 "no API key in log output" guarantee holds only for sk-/Bearer-shaped keys; the passing integration test likely uses an sk- key, masking the gap. (Deferred: Epic Plan 4.9 secret-value-redaction) | Thread resolved registry API key values into `NewRedactor(root, keys...)` at both review.go:172 and handlers.go:87 (keys are discoverable from prep.Slots / cfg.Registry). Add an integration test using a non-sk-shaped key (e.g. `AIzaSy...`) asserting it is redacted. | security | 120 | code-review | claude | MEDIUM |

### [2026-06-16] From Sprint: 3.5_severity-rank-consolidation

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:18 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestMergeNormalizesMixedCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:30 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestGrayZoneNormalizesMixedCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:42 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestMergeNoDisagreementOnCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |

### [2026-06-16] From Sprint: epic-3.5

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/stream/severity.go:20 | The canonical SeverityRank map carries a read-only-after-init invariant in a comment only, with no test guard preventing a future caller from mutating the shared map (which would race across concurrent fan-out agents). (Won't fix: structural guard — reconcile copy-on-init at merge.go:29-31 means no consumer writes stream.SeverityRank directly; grep confirms zero write sites across consumers. Snapshot test trips the over-simplification gate; Rank() accessor cascades to 14+ direct-lookup sites — both out of pure-consolidation scope.) | Add a stream test that snapshots the map and asserts it is unchanged after consumers run, or wrap it behind a Rank(sev) accessor. | OBSERVABILITY | 20 | execute-epic-independent |

### [2026-06-16] From Sprint: 3.4_scorecard-diagnostics-writer

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:118 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; the diagnostic already routes to the injectable writer, only fmt.Fprintf's own return is dropped; propagating it breaks Emit's never-fail contract) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:199 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; Append loop already surfaces firstErr; propagating the Fprintf return breaks the best-effort contract) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:235 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; verdictTallies already writes to injectable w; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:248 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; verification-read-failed diagnostic writes to injectable w; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:276 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; orphan-verdict diagnostic routes to injectable w, locked by TestEmit_OrphanVerdictDiagnosticRoutesToDiagWriter) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | LOW | internal/scorecard/scorecard_test.go:291 | Epic 3.4 added unit tests proving EmitOpts.Diag / ReadOpts.Writer routing at the scorecard layer, but there is no test asserting the wiring itself — that the MCP handler passes a non-default writer or that the three CLI entry points pass cmd.ErrOrStderr(). This plumbing can silently regress (a future refactor swaps cmd.ErrOrStderr() back to a default and no test fails). (Deferred: Epic Plan 3.6 scorecard-wiring-tests) | Add a CLI-level test that drives a scorecard command against a deliberately-malformed store and asserts the read diagnostic reaches the command's ErrOrStderr buffer; once MCP diagnostics route through e.log, add a handler-level test asserting against a captured logger buffer. | testing | 60 | code-review | claude | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:114 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; over-long-line warning uses injectable w via diagWriter; read continues; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:145 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; decodeRecord writes to injectable w then returns (Record,bool) — no error cascade) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:155 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; schema-version skip writes to injectable w; identical to malformed-record path) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | LOW | internal/scorecard/store.go:194 | Redundant call to diagWriter (Wontfix: FALSE POSITIVE — diagWriter is the required typed-nil guard for the nil-able opts.Writer interface, not redundant; removing it reintroduces the panic fixed by commit 476c6d1) | Remove diagWriter call and use opts.Writer directly since ReadRecords already resolves it | performance | 2 | code-review | otto | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:211 | Error from diagWriter is silently discarded (Wontfix: FALSE POSITIVE — `_, _ = fmt.Fprintf` is the documented best-effort never-panic diagnostics contract at store.go:22-24; returning the write error would regress a successful read on a broken sink and logging is circular; confirmed working as designed via /sprint-clarification 97%) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| U | [/] | MEDIUM | internal/mcp/handlers.go:220 | The MCP engine carries a structured *slog.Logger (e.log) used for every other diagnostic in handleReconcile (e.g. the require_verified warning at line ~225), but scorecard diagnostics are routed to raw os.Stderr, so MCP-path scorecard write-failures/malformed-record/orphan-verdict warnings emit as unstructured plaintext that bypasses the logger's level filtering, formatting, and sink redirection. NOTE: this was a DELIBERATE, documented epic decision (Clarifications Q2: supply os.Stderr at the call site; adapting e.log to an io.Writer was explicitly OUT of scope), so this is enhancement debt, not a regression. (intent_note: deferred per epic Clarifications Q2 — adapting e.log to an io.Writer is out of scope) (Wontfix: ACCEPTED ENHANCEMENT DEBT — handlers.go:220 is an unconditional EmitOpts{Diag: os.Stderr} call, not deferred logic; all five Epic 3.4 ACs are met and the comment at handlers.go:214-219 satisfies AC4; e.log→io.Writer adaptation is out of scope per Clarifications Q2; confirmed via /sprint-clarification 97%) | If MCP observability is later desired, adapt e.log into an io.Writer shim (slog-backed at Warn level) and pass it as Diag instead of os.Stderr, so MCP-path scorecard diagnostics flow through the same structured pipeline as the rest of the handler. Defer unless/until structured MCP diagnostics are required. | error-handling | 30 | code-review | claude | MEDIUM |

### [2026-06-15] From Sprint: 3.3_per-run_scorecard

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| U | [/] | LOW | internal/scorecard/store.go (FindByRunID adjacent-month notice; ReadRecords malformed-line notice); internal/scorecard/scorecard.go (Emit notices) | Store/emit diagnostics ("skipping malformed record", "run spans adjacent month files", "write failed") are written directly to the process-global `os.Stderr` rather than to a writer threaded from the cobra command (`cmd.ErrOrStderr()`). (Deferred: Epic Plan 3.4) | accept an `io.Writer` (or logger) on the store/emit entry points and have the CLI pass `cmd.ErrOrStderr()`, so warnings are capturable and redirectable. | ops | 0 | execute-sprint | execute-sprint | MEDIUM |

### [2026-06-14] From Sprint: 3.2_disagreement_radar

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| U | [/] | LOW | internal/reconcile/disagree.go:280 | Duplicate writeRadarSection rendering logic risks divergent escaping across packages (Deferred: .planning/epics/active/7.2_radar-renderer-consolidation.md) | Consolidate radar rendering into report package; reconcile should call report.writeRadarSection | security | 20 | code-review | greta | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/disagree.go:350 | Duplicated radar section rendering logic (Deferred: Epic Plan 7.2) | Extract shared writeRadarSection to a common package | maintainability | 10 | code-review | bruce | MEDIUM |
| 4 | [/] | MEDIUM | internal/reconcile/disagree.go:354 | Duplicated radar markdown rendering diverges (Deferred: Epic Plan 7.2) | Extract shared writer or make reconcile use report package's escTrunc | maintainability | 10 | code-review | bruce | MEDIUM |
| U | [/] | LOW | internal/reconcile/disagree.go:413 | Redundant implementation of writeRadarSection (Deferred: .planning/epics/active/7.2_radar-renderer-consolidation.md) | Remove duplicate function from internal/reconcile and use internal/report | maintainability | 15 | code-review | otto | MEDIUM |
| 5 | [/] | MEDIUM | internal/report/disagree.go:47 | The radar markup is rendered by two divergent copies: writeRadarSection + formatScore in internal/report/disagree.go and a structurally different copy in internal/reconcile/disagree.go:389. They are intentionally not identical (report copy truncates via escTrunc + uses joinReviewers/reviewerOrUnknown; reconcile copy uses uncapped esc + joinOrNone), so a future markup change (new field, reordered bullets) must be made in both or the live `atcr report` radar and the archival reconciled/report.md silently drift. (Deferred: .planning/epics/active/7.2_radar-renderer-consolidation.md) | Extract one shared item renderer parameterized by a truncate-vs-verbatim flag and heading prefix; have both call sites delegate. Add a test that diffs the rendered markup of both paths on the same DisagreementsFile asserting the only intended difference is truncation. | correctness | 60 | code-review | bruce, claude | HIGH |

### [2026-06-14] From Sprint: 2.2_code_review_fanout_hardening

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| U | [/] | MEDIUM | internal/fanout/postprocess.go:14 | The severity-rank rubric {CRITICAL:4,HIGH:3,MEDIUM:2,LOW:1} is independently redefined in fanout/postprocess.go:17, reconcile/merge.go, verify, report, plus a set-form copy reviewSeverities in registry/config.go. postprocess looks up severityRank[strings.ToUpper(...)] while reconcile looks up the raw value, so a future severity change or non-canonical casing silently desyncs fan-out truncation from reconcile merging. The postprocess copy was newly added by Epic 2.2. (disagreement: LOW vs MEDIUM) (Deferred: Epic Plan 3.5) | Extract a single canonical severity package (or export from internal/stream) exposing the ordered rank map plus normalizeSeverity, and have registry/fanout/reconcile/verify/report consume it. Verify by deleting the local maps and confirming the suite passes with one source of truth. | maintainability | 120 | code-review | claude | MEDIUM |

### [2026-06-14] From Review: llmclient OpenAI-compatible tool handling

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| U | [/] | LOW | internal/llmclient/chat.go:1 | No provider-conformance test matrix for the OpenAI-compatible surface. The client deliberately absorbs real wire divergence (string-encoded vs raw-object tool_call `arguments`, lenient finish_reason) but is exercised only against synthetic fixtures, so a regression against a specific provider's actual tool_call shape (OpenAI, litellm, Ollama, vLLM, Together) would not be caught. This is the robustness the official SDK is assumed to provide, achievable here without adopting it. | Add a recorded-fixture conformance suite: capture a real `tool_calls` response from each target provider and assert the parser (`ToolCallArguments`, `chatToolResponse` decode, finish_reason handling) yields identical engine-facing results. NOTE: scope is a few days, not a quick-win — consider promoting to a standalone test-remediation plan rather than resolving inline. | testing | 480 | review | claude | LOW |

### [2026-06-14] From Sprint: 3.0_adversarial_verification

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| 7 | [/] | LOW | internal/verify/pipeline.go:238 | The rich VerificationResult built in verifyFinding never populates TrippedBudgets (invokeSkeptic folds tripped budgets only into free-text Notes), base.Model is hard-coded to skeptics[0].Config.Model even when another skeptic produced the winning verdict, and the skip-already-verified rebuild (pipeline.go:199) re-synthesizes records from the compact on-disk block losing Model/DurationMs/TrippedBudgets — so verification.json's structured audit fields degrade or misattribute on multi-skeptic and no-op re-runs (extends the model-attribution gap of TD-011). Also: Model is empty on the no_eligible_skeptic / tool_harness_unavailable early-return paths. [intent: deferred per sprint-plan TD-011] | Thread the tripped-budget slice and the winning skeptic's model up from invokeSkeptic into base.TrippedBudgets/base.Model (join models when multiple voters agree, mirroring joinSkeptics), and carry Model/DurationMs/TrippedBudgets forward for skipped findings instead of synthesizing a lossy record. | correctness | 120 | execute-sprint | execute-sprint, claude | HIGH |

### [2026-06-13] From Sprint: 2.0_tool_using_reviewers

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| 5 | [/] | LOW | internal/tools/snapshot.go | AC 03-02 Scenario 5 and AC 03-03 Scenarios 4-5 require `manifest.json` `stages.review` to record `snapshot_mode` (live/worktree), `head_sha`, and `snapshot_worktree_path`. (intent_note: deferred per sprint-plan §2.5.A (manifest review-stage recording is Phase 5 work); Deferred to Epic Plan 2.1) | when wiring `SnapshotFor` into the agent loop, record `snapshot_mode`/`head_sha`/`snapshot_worktree_path` into `internal/payload/manifest.go` review stage and add the manifest assertion tests from AC 03-02/03-03. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
