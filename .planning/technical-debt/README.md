# Technical Debt Tracking

This file is a staging area for small technical debt items discovered during development. Items are triaged and moved to individual sprint documents in `sprints/` as they are prioritized.

## Stats

| Severity | Open | Deferred | Resolved |
|----------|------|----------|----------|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 0 | 3 | 1 |
| MEDIUM | 0 | 20 | 18 |
| LOW | 0 | 32 | 25 |


**Last Modified:** 2026-07-09 | **Open Items:** 0 | **Deferred Items:** 55 | **Resolved Items:** 44 | **Total Items:** 99

## Directory Structure

```
technical-debt/
├── README.md                    # This file (staging area)
├── CLAUDE.md                    # AI assistant guidelines
└── sprints/
    ├── active/                  # Currently being addressed
    ├── pending/                 # Prioritized, not yet started
    └── completed/               # Resolved items
```

## How to Use

1. **Small items**: Add to this README under "Staging Area" below
2. **Larger items**: Create a new document in `sprints/pending/`
3. **During sprint planning**: Move items from pending to active
4. **After resolution**: Move items from active to completed

## Sharded Storage Format (`items/`) — additive, Epic 12.1

As of Epic 12.1, every item in the dated table below is **also** stored as a
structured YAML file under [`items/`](items/), **sharded by source** — one file
per `### [date] From <Sprint|Review>: <label>` section (e.g.
`items/2026-06-26_epic-11.2.yaml`). A single review producing 50–100 findings is
therefore **one** shard file, not 50–100, and two concurrent review/sprint runs
each write their own new file, so they never merge-conflict on TD storage.

This is **additive and not yet canonical**:

- **The Markdown table below remains authoritative.** All existing tooling (the
  `td_*` MCP binaries and the TD skills) reads/writes this table unchanged. The
  shards are generated *alongside* it and are not yet machine-read by any tool.
- The cutover that makes the shards canonical (and updates the binaries/skills)
  is deferred to a follow-on epic (18.0 / 12.3). No tooling changed in 12.1.

**Tooling** — `cmd/td-migrate` (logic in `internal/tdmigrate/`):

| Command | Effect |
|---------|--------|
| `go run ./cmd/td-migrate migrate`  | Parse this README table → (re)write the shards under `items/`. Idempotent: prunes its own prior `*.yaml` output. |
| `go run ./cmd/td-migrate generate` | Read the shards → print a regenerated ToC table to **stdout** (never overwrites this README). |
| `go run ./cmd/td-migrate validate` | Strict-load + schema-check every shard; a malformed shard fails **loudly** (non-zero exit). |

The shard schema, field semantics, and the YAML-safety guarantees are documented
in [`items/SCHEMA.md`](items/SCHEMA.md). Round-trip fidelity (table → shards →
table with zero data loss) is proven by the Go test suite in
`internal/tdmigrate/`, not by a committed generated artifact.


### [2026-07-09] From Review: reasoning-model runaway generation truncates tool-assisted reviews

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| U | [x] | MEDIUM | personas/_base.md, personas/bruce.md, personas/greta.md, personas/kai.md, personas/mira.md, personas/dax.md, personas/otto.md, personas/sasha.md, personas/ingrid.md, personas/penny.md, ~/.config/atcr/personas/{archer,brad,vera,ronin,pace}.md | Every persona's `{{if .ToolsEnabled}}## Tool-Assisted Review` block gives an unbounded "verify before reporting, spend tool calls to verify not to browse" instruction with no stopping condition. On a reasoning-parser model (e.g. `--reasoning-parser qwen3`, now serving the `llm-large` alias as `ornate 1.0`), this lets `<think>` consume the entire output token budget with no bound, so generation is cut off mid-thought before the closing tag and zero findings parse out. Confirmed live: the `/execute-code-review --multi-agent` run against this exact sprint logged `"reviewer response truncated with zero findings; failing over"` for pace, bruce, archer, and mira in a single run — the existing 19.5 truncation-failover mechanism is masking the root cause rather than the root cause being fixed. 10 of the 15 affected files are embedded via `//go:embed *.md` (personas/personas.go:12) and require a rebuild + reinstall to take effect; the other 5 are local Registry-tier overrides at `~/.config/atcr/personas/*.md` read from disk with no rebuild needed. | Replace the "Spend tool calls to verify, not to browse" line with a bounded tool budget ("use at most 3 tool calls total; report at reduced confidence rather than continuing to investigate") and append a mandatory "## Reasoning Budget" section instructing the model to think efficiently, reserve its final ~500 tokens for pipe-delimited findings, and commit confirmed findings immediately rather than deferring all output to the end. Apply identically across all 15 files (10 embedded + 5 local). Verify by re-running the multi-agent fan-out against a large diff and confirming the previously-truncated agents (pace, bruce, archer, mira) now emit parseable findings instead of triggering the truncation-failover path. | correctness | 45 | review | claude | HIGH |

### [2026-07-09] From Sprint: 19.7_live_model_resolution

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| u | [x] | MEDIUM | cmd/atcr/init.go | Option B nil-roster path derives the install set from the fetched index's own `e.Name` values, which `FetchIndex` does not validate; an empty/invalid name is not skipped, fails inside `InstallUnit`→`validatePersonaName`, and trips the all-or-nothing rollback that aborts online init/quickstart for every user. (intent_note: deferred per sprint-plan §7.3 — all-or-nothing rollback is AC 07-03 EC1-mandated; only empty/invalid-name pre-filter is deferrable) | Future robustness pass: when building the derived roster from `entries`, pre-filter names failing `validatePersonaName` (skip-with-warning), distinct from the AC-mandated all-or-nothing rollback on genuine per-unit INSTALL failures; add a malformed-index-entry test. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| u | [x] | LOW | cmd/atcr/init.go | `rbCandidate{path, fileExists(path)}` entries are appended only AFTER the `if fileExists(yamlPath) // fileExists(mdPath) { continue }` guard, so both paths are known-absent and the re-stat always yields `preExisted: false` — dead, misleading bookkeeping. | Remove the always-false `preExisted` field from `rbCandidate` and simplify `rollback()` to remove only paths that still exist, since candidates are only ever recorded after the file-exists guard has already confirmed both paths absent. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| u | [x] | LOW | cmd/atcr/init.go | Online init/quickstart pin the 10 index-derived community personas into the resolver pin dir, but the generated `.atcr/config.yaml` roster and the synthetic registry stay `builtins.Names()` — the installed community set is a resolvable pool, not active on a default `atcr review` until the user hand-edits the roster. (intent_note: deferred per sprint-plan §7.6 — within locked Option B contract (TD-013); roster wiring is a future decision, not a defect) | Intentional deferral: the active `.atcr/config.yaml` roster stays on `builtins.Names()` per the locked AC7 Clarification in plan/plan.md (Option B). The online fetch-and-pin community set is a resolvable pool; roster wiring is a future decision, not a defect. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/init.go:50 | runInit fully writes the .atcr/ scaffold, then installCommunityPersonas runs; a transient fetch failure or empty index returns an error, so atcr init exits non-zero even though the on-disk scaffold is complete and usable (built-ins work). The scaffold is not rolled back, so a re-run of atcr init hits "existing files would be overwritten" (exit 2) and the user is funneled into --force --offline — the exit code contradicts the on-disk reality (workspace created, only the community pin failed). | Either roll the scaffold back on community-fetch failure, or downgrade a post-scaffold community-fetch failure to warn-and-continue (exit 0) with the offline hint, so exit status reflects whether the workspace is usable. | error-handling | 30 | code-review | claude | MEDIUM |
| u | [x] | MEDIUM | cmd/atcr/models.go | A whole-set enumeration error (`ListTiers` walk) and per-persona `LoadLock` failures are written only to stderr; neither affects the exit code or the stdout `--json` array, so `exit 0`/`[]` can mean a partially-failed enumeration rather than all-clear — a machine consumer cannot distinguish them. (intent_note: deferred per sprint-plan §5.6 — per-persona stderr-only/exit-unaffected is AC-mandated (TD-007); structured incomplete-check contract belongs to Epic 19.8 (out of scope)) | No code change: per-persona stderr-only/exit-unaffected enumeration behavior is AC-mandated (TD-007) and working as intended. A structured incomplete-check contract belongs to Epic 19.8 and is out of scope. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| u | [x] | LOW | cmd/atcr/models.go | The catalog GET is unauthenticated (`fetch()` adds no auth header) and `atcr models refresh` writes each fetched entry's `id`/`canonical_slug` into the committed fixture without running `validateResolvedSlug`, so a MITM or compromised upstream could persist misleading/garbage slugs into the resolver's source-of-truth snapshot. (intent_note: deferred per sprint-plan §8.6 — validate-and-skip would change the "faithfully snapshot live catalog" contract; captured as TD-014) | Future robustness pass: validate each fetched id/canonical_slug via validateResolvedSlug before WriteSnapshot/MarshalSnapshot persists it, skipping-with-warning or rejecting invalid entries, plus a hostile-slug test — deferred, not exploitable today per tech-debt-captured.md TD-014. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| u | [x] | LOW | cmd/atcr/models.go | AC 08-02's Security section states `models refresh` sends OPENROUTER_API_KEY as a Bearer token to api.openrouter.ai, but the code reads the key only as a local maintainer/CI presence-gate and never transmits it (the catalog GET is unauthenticated) — the AC prose overstates the transmission contract. (intent_note: deferred per sprint-plan §8.9 — code is correct/safer; AC prose is stale and reconciled post-execution (TD-016)) | Reconcile AC 08-02's Security Considerations section to state OPENROUTER_API_KEY is read only as a local maintainer/CI presence gate and is never transmitted; the catalog GET is unauthenticated per the Phase 1 spike. Do not add a Bearer header. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/models.go:97 | The never-run-in-CI guard uses os.Getenv("CI") != "", which treats CI=false or CI=0 as "in CI" and refuses to run. Some developers export CI=false locally (a documented convention, e.g. create-react-app), so a legitimate maintainer is blocked with the "must not run in CI" error even though nothing runs under CI. It fails safe (refuses) but is a misleading dead-end for the maintainer the command serves. | Treat only truthy values as in-CI (skip when the value is empty or one of ""/"false"/"0"), matching how CI systems populate the variable; add a test for CI=false. | error-handling | 15 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/models.go:124 | The empty-catalog guard uses substantiveModelCount (id-bearing entries only) but the success message reports len(models) — the raw count including blank-id entries — and WriteSnapshot persists ALL entries, blanks included. A payload like data:[{valid},{},{}] writes 3 entries (one junk) and reports "Wrote 3 models", so the reported number overstates the substantive catalog and blank entries silently enter the checked-in fixture. (Distinct from the pre-seed unvalidated-slug security row: this is a count/report mismatch plus blank-entry persistence.) | Filter to substantive entries before WriteSnapshot and report that filtered count in the success line so the message and the persisted fixture agree; add a test with blank-id entries in the payload. | maintainability | 15 | code-review | claude | MEDIUM |
| 1 | [x] | MEDIUM | cmd/atcr/personas.go:408 | The sprint's exit-code contract is clean=0/findings=1/usage-failure=2, and models refresh/check honor it (a live-fetch failure → usageError → exit 2). But runPersonaUpgrades maps the SAME OpenRouter fetch failure (bubbled from upgrade.go:245) to a plain fmt.Errorf → exit 1, so an infrastructure/network failure during personas upgrade returns exit 1 (indistinguishable from substantive changes) while the identical failure in models returns exit 2 — a CI/mechanical consumer keying on 1-vs-2 misreads an upgrade infra failure as a substantive result. | Classify pure fetch/environment failures in the upgrade loop as usage/command failures (wrap the terminal error with usageError or a coded error) so infra failures exit 2 and only substantive per-persona outcomes drive exit 1, matching the models commands. | error-handling | 30 | code-review | claude | MEDIUM |
| 1 | [x] | MEDIUM | cmd/atcr/quickstart.go:308 | appendExport tightens the profile to 0600 only when it CREATES the file; when the user names a pre-existing profile it leaves the mode untouched. Default shells ship ~/.bashrc/~/.zshrc at 0644, so appending the plaintext OpenRouter API key to an existing dotfile persists the secret in a group/other-readable file — an exposure on any shared/multi-user host. The stdout masking protects the echo but nothing protects the on-disk mode of the sink. (ORIGIN likely pre-19.7 key-export code, but quickstart.go is in the sprint diff.) | When the target already exists, stat its mode and, if group/other-readable, os.Chmod it to 0600 or refuse-with-warning and print the export line for manual placement; at minimum warn that the key is being written into a world/group-readable file. | security | 30 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/personas/drift.go | An alias-bound persona locks its `Model` to the synthetic `~vendor/…-latest` slug that `ResolveModel`'s alias passthrough returns; if a refreshed live catalog omits those ids, `models check`'s missing branch reports a false `missing` for every alias-bound persona. (intent_note: deferred per sprint-plan §5.5 — captured as TD-005 (Phase 8/19.8); snapshot currently lists ~ entries and Phase 1 spike confirmed aliases are real/routable) | Phase 8/later: when wiring `models refresh` against the live catalog, confirm the `~…-latest` ids are present; else skip the `missing` check for a lock slug equal to an `aliasTable` value (or resolve alias locks through the passthrough before the catalog lookup) and add a test. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | LOW | internal/personas/drift.go | `deriveFamilyPrefix` strips only a single TRAILING version-like hyphen segment, so a slug whose version is not last (e.g. `openai/gpt-5.4-mini`) derives to itself rather than a family key and never groups with a newer tier sibling — the bindingless newer-member fallback reports no drift forever. | Phase 8/later: when wiring `models refresh` against the live catalog, confirm the `~…-latest` ids are present; else skip the `missing` check for a lock slug equal to an `aliasTable` value (or resolve alias locks through the passthrough before the catalog lookup) and add a test. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [/] | LOW | internal/personas/drift.go | For a bound persona the `family` JSON field is the bare binding token (glm); for a bindingless persona it is the derived vendor/tier prefix (anthropic/claude-opus) — two shapes under one key that a 19.8 agent grouping on `family` would see inconsistently. (intent_note: deferred per sprint-plan §5.6 — shape documented as advisory; normalization deferred to 19.8 (TD-008)) | Phase 8/later: when wiring `models refresh` against the live catalog, confirm the `~…-latest` ids are present; else skip the `missing` check for a lock slug equal to an `aliasTable` value (or resolve alias locks through the passthrough before the catalog lookup) and add a test. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | LOW | internal/personas/snapshot.go | The `models check` default path reads a snapshot `//go:embed`-ed from `internal/personas/testdata/catalog_snapshot.json` compiled into the binary, so Phase 8's `models refresh` regenerating that file does not change an already-installed binary's default path without a recompile. (intent_note: acknowledged per sprint-plan §5.6 as Phase 8 refresh-consumption model (TD-009)) | Phase 8: decide and document how a refreshed snapshot reaches the default path (recompile-and-ship vs. a user-writable snapshot location read via the existing `ATCR_CATALOG_SNAPSHOT` seam), and whether the shipped snapshot should live outside `testdata/`. | ops | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | LOW | internal/personas/snapshot.go:35 | SnapshotModels's ATCR_CATALOG_SNAPSHOT env-override branch reads the file with a bare os.ReadFile(p) — no size cap — while the network catalog path is bounded by io.LimitReader (5 MiB) to prevent memory exhaustion from an untrusted body. An env override pointed at an unbounded source (/dev/zero, a FIFO, a multi-GB file) reads the entire stream into memory before parsing. | Read through a size-bounded reader (io.ReadAll(io.LimitReader(f, limit)) after os.Open, or os.Stat + size check) and return the existing "failed to load catalog snapshot" error when the cap is exceeded, mirroring the fetchBodyLimit guard; share one limit constant across both ingestion paths. | error-handling | 15 | code-review | claude | MEDIUM |
| 2 | [/] | LOW | internal/personas/upgrade.go | `resolveNewestInPrefix` selects the newest-by-`created` catalog member, but the lock gate advances only when the extracted semver is a version-advance (`isNewer`), so a newer-created-but-lower-semver hotfix slug is selected by the resolver yet blocked by the gate and silently not adopted. | Deferred (Story 6/later, semver philosophy): keep the semver isNewer gate authoritative for the lock-advance decision (consistent with the major-bump re-validation gate and the explicit-upgrade/reproducibility posture), but emit a visible signal in the personas upgrade before→after report or a models check finding when resolveNewestInPrefix's created-order pick diverges from what the gate accepts, rather than silently leaving the lock stale; cover the divergent hotfix case with a test. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | LOW | internal/personas/upgrade.go:179 | `parseBinding` validates only that a channel is non-empty (rejects a bare trailing @) but never that it is a known literal, so an invalid non-empty channel like anthropic/claude-opus@stabel is accepted. Because ResolveModel's alias passthrough (catalog.go:204) ignores the channel entirely, the typo resolves silently to the alias slug with no error, whereas a scan family with the same typo fails loudly at normalizeChannel — the alias-vs-scan asymmetry the empty-channel guard was written to close, applied only to the empty sub-case. (intent_note: accepted as designed per sprint-plan §3.5 — uniform channel validation contradicts AC 03-03/03-05 (pin/alias ignore channel)) | In parseBinding, after splitting on @, validate the channel via normalizeChannel (reject ok=false) so a bad channel fails closed uniformly for both families; add a test that anthropic/claude-opus@typo returns a parse error. | error-handling | 30 | code-review | claude | MEDIUM |
| 2 | [x] | HIGH | internal/personas/upgrade.go:213 | `versionFromSlug` extracts a version only from a standalone hyphen segment matching ^v?\d+(\.\d+)*$, but the qwen family glues the version to the vendor token (qwen3-coder-plus, qwen3.7-plus, qwen4-preview) and glm uses variant tokens (glm-5v-turbo), so it returns "" for all of them. The advance gate at upgrade.go:258 then computes !isNewer("","")=true → UpToDate, so a qwen/glm-BOUND persona can NEVER advance its lock after the initial establish even when resolveNewestInPrefix selected a newer model — a silent freeze. The same versionFromSlug call at drift.go:158 makes `models check` blind to qwen/glm newer-member drift. Not triggerable by a shipping persona today (all 10 are bindingless pinned slugs), but a real always-on gap in 2 of the 3 scan families once a qwen/glm binding is authored. | Make version extraction handle vendor-glued and variant-suffixed schemes (strip a known vendor/family stem then parse the leading numeric of the remainder, or extract an embedded /\d+(\.\d+)*/ from the first segment); add tests that a qwen-bound persona advances qwen3-coder-plus→a newer qwen and that CheckDrift reports newer-member for qwen, verified against the checked-in fixture. deepseek is unaffected (deepseek-v4-pro has a matching `v4` segment). | correctness | 120 | code-review | claude | MEDIUM |
| U | [/] | LOW | docs/personas-authoring.md | AC 08-03 Scenario 3 links the shipped persona doc sections to `../.planning/sprints/active/19.7_live_model_resolution/plan/documentation/*.md`; those links resolve now but `/finalize-sprint` moves the sprint active/→completed/, after which the active/ path 404s in the shipped docs. | Future docs-stabilization pass: update the two links to the completed/ path after archival, or promote the durable plan documentation into a stable home (docs/ or .planning/specifications/) and point the persona docs there; add a link-check that fails on a dangling docs→.planning link. | docs | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | LOW | internal/registry/config.go:298 | AgentConfig.Binding is the one new free-text agent string with no load-time guard, while validateAgent applies a control-char + U+2028/2029 + length guard to every sibling free-text field (Persona, Scope, Language). Binding is a string the resolver later parses into family/channel/pin (the pin path flows through validateResolvedSlug into a lock and an outbound request), so an unbounded/control-char value in registry.yaml is accepted silently at load and only fails far downstream, out of the config-error context the user is editing. | In validateAgent, add the same strings.IndexFunc(unicode.IsControl) rejection and a length cap (reuse MaxPersonaPromptLen or a dedicated MaxBindingLen) for a.Binding, mirroring the adjacent Persona guard, so a malformed binding fails fast at load. | security | 15 | code-review | claude | MEDIUM |

### [2026-07-09] From Review: llmclient hardcoded HTTP timeout

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| U | [x] | MEDIUM | internal/llmclient/client.go:33,107 | `Client.httpClient` is built with a hardcoded `Timeout: defaultHTTPTimeout` (120s, client.go:33) that competes with the per-request `context.Context` deadline already passed via `http.NewRequestWithContext` (client.go:659) and driven by `.atcr/config.yaml`'s `timeout_secs`. Since 120s is shorter than any realistic `timeout_secs` (600s in this repo's local config; a real code review commonly needs 15-25 min), the client's own hardcoded field fires first on every attempt, and the fan-out's own retry logic (`dispatch()`, up to 3 attempts) retries the resulting transport error, burning the full outer budget in repeated 120s slices before the properly-configured context deadline ever gets a chance to fire cleanly. Confirmed live in a `/execute-code-review --multi-agent` run: 4 of 11 reviewer agents (vera/greta/brad/kai) failed with `context deadline exceeded` at 500-600s instead of a clean single-attempt failure, masking the real upstream latency and making `timeout_secs` effectively unconfigurable in practice. | Delete `Timeout: defaultHTTPTimeout` from the `http.Client{}` construction in `New()` (client.go:104-121) and the now-dead `defaultHTTPTimeout` const (client.go:33) — the per-request context already fully bounds each attempt, so a second, shorter, competing client-level timeout is redundant and actively harmful. Update the stale `Client` doc comment (client.go:55-57) claiming "the HTTP transport timeout guards a single exchange". Verify with a test where a mock server responds just under the context deadline but over 120s, and assert the call succeeds. | correctness | 30 | review | claude | HIGH |

### [2026-07-09] From Review: persona prompt length cap too tight and improperly coupled

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| U | [x] | MEDIUM | internal/registry/config.go:83,88 | `MaxPersonaPromptLen` (config.go:88) is a literal Go const alias of `MaxExecutorSystemPromptLen` (config.go:83, value 4096), an unrelated Epic 7.0.1 limit for executor system-prompt overrides, reused in Sprint 19.6 to bound registry ("community"/fetched) persona prompts. `ValidateFetchedPersonaPrompt` (internal/registry/persona.go:166-167) enforces it at resolve-time via `validateCommunityPrompt` (persona.go:261-262, invoked from `ResolvePersona` persona.go:91-95), and only for the Registry tier — built-in and project-tier personas are exempt. The cap has no persona-specific derivation (no token-budget or security math, just a reused round number) and is demonstrably too tight: a normal, non-adversarial, non-bloated reviewer persona (`~/.config/atcr/personas/pace.md`, 51 lines — role, hard rules, focus, severity rubric, output-format contract, one example) is 4128 bytes, only 32 bytes over, with no content to trim. Confirmed live: a rebuilt `atcr` binary rejected `pace` outright with `persona prompt exceeds maximum length of 4096 bytes`, hard-blocking the `/execute-code-review --multi-agent` fan-out. Because the constant is a literal alias rather than an independent value, any future fix that raises the persona cap would silently raise the unrelated executor system-prompt cap too. | Split `MaxPersonaPromptLen` into its own independently-set constant (stop aliasing `MaxExecutorSystemPromptLen`), then raise the persona-specific cap to accommodate legitimate reviewer prompts — 8192 is a reasonable round number, or measure the largest existing built-in/community persona prompt and pick ~2x headroom over it. Verify: `pace.md`'s exact byte length now passes; `MaxExecutorSystemPromptLen` is unchanged after the split; a deliberately oversized/adversarial persona prompt still fails at the new persona-specific cap. | correctness | 30 | review | claude | HIGH |

### [2026-07-08] From Sprint: 19.6_community_registry_hub

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [x] | LOW | cmd/atcr/init.go:171-176 | The "config already exists at .atcr/config.yaml — use --force" gate trips on ANY existing init target (including a surviving `.atcr/personas/*.md` when config.yaml was deleted), so the message can be factually wrong about which file blocks the run. (intent_note: deferred per sprint-plan TD-004) | name the actual existing target(s), or narrow the trip to config.yaml since personas are always preserved. | ops | 0 | execute-sprint | execute-sprint | MEDIUM |
| 1 | [x] | MEDIUM | cmd/atcr/init.go:82 | `atcr init`'s RunE calls runInit (writes config.yaml + built-in personas scaffold) at init.go:36 BEFORE installCommunityPersonas at init.go:47. When the community fetch fails the scaffold is already persisted, but the returned error's offlineHint advises only "run with --offline". On the retry both `atcr init` and `atcr init --offline` re-run runInit (line 36, force=false) which trips the "config already exists" exists-gate BEFORE the offline short-circuit at line 39 — so the documented --offline recovery does not work; only `atcr init --force --offline` recovers. The scaffold's built-ins stay usable so the workspace is functional, but the shipped recovery guidance is wrong. | Change offlineHint to advise `--force --offline`, OR evaluate the offline flag / own-scaffold-resume before runInit's exists-gate on a resumed run, OR roll back the scaffold on community-fetch failure. Verify: point ATCR_PERSONAS_URL at an unreachable server, run `atcr init`, then confirm the hinted recovery command succeeds without "config already exists". | error-handling | 30 | code-review | claude | MEDIUM |
| 1 | [/] | HIGH | cmd/atcr/init.go:47 / cmd/atcr/quickstart.go:102 | `init`/`quickstart` fetch-and-pin the roster `builtins.Names()` (the 9 model-agnostic built-ins: bruce/greta/kai/mira/dax/otto/sasha/penny/ingrid), but the shipped `personas/community/index.json` publishes only the 10 model-indexed library personas (anthony…glenna) — a fully disjoint set. (intent_note: deferred per sprint-plan TD-011) (Deferred: Epic Plan .planning/epics/active/19.7_live_model_resolution.md — design-sprint decision; lean toward publishing built-ins into the community channel behind a model-agnostic tag/gate) | rebuild init/quickstart model enablement so online `init`/`quickstart` deliver a working, non-noisy persona set: either publish the built-in lenses into the community channel behind a model-agnostic gate carve-out (keeps C2), or align the roster with what the index publishes and drop the misleading skip-warning. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| u | [/] | LOW | cmd/atcr/init.go:142 | installCommunityPersonas records each candidate's preExisted flag then, on a later roster failure, rollback() removes any candidate path where !preExisted && fileExists. If another process creates <name>.yaml/.md between candidate recording (line 142) and the eventual rollback, the rollback deletes that not-ours file believing this run created it. The window is narrow and needs concurrency; the actual overwrite protection lives in InstallUnit. (Accepted LOW-severity risk — atcr is a single-user local CLI; if ever addressed, add an inode+mtime re-stat guard to the rollback rather than changing InstallUnit's return contract.) | Have rollback delete only files it observed itself successfully create (capture created paths from InstallUnit's result rather than re-deriving from candidates), or re-stat and skip if inode/mtime differs from what this run wrote. Verify with a concurrency test that plants a file mid-roster and asserts it survives rollback. | error-handling | 30 | code-review | claude | MEDIUM |
| u | [x] | LOW | cmd/atcr/personas.go (listPersonasWithScores → ListWithScores) | Plain `personas list` now routes through `ListTiers` (project > community > built-in), but the `--scores` variant still calls `List` (built-in + community only). (intent_note: deferred per sprint-plan TD-005) | A follow-up that adds a tier-aware `ListWithScores` (or an internal `ListTiersWithScores`) so both list variants agree on the Source column. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 1 | [x] | MEDIUM | cmd/atcr/personas.go (renderPersonaSearch / writeTable) | `renderPersonaSearch` writes community-index string fields (Name/Version/Provider/Model/Description) raw into tab-delimited `text/tabwriter` rows with no control-character sanitization. (intent_note: deferred per sprint-plan TD-008) | A follow-up that strips or escapes control characters (tab, newline, CR, ESC/ANSI) from each cell value in the shared `writeTable` path (covering `renderPersonaSearch` and `renderPersonaList` uniformly), or rejects index entries whose fields contain them at decode time. | security | 0 | execute-sprint | execute-sprint | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/quickstart.go:223 | keyEnvFlow re-prints the pasted secret verbatim as a copy-paste `export VAR=<key>` line to stdout. Echoing the plaintext key back writes it into terminal scrollback and into any log/CI capture of stdout, extending the secret's exposure surface beyond the transient paste and undercutting the file's stated "held only transiently" posture. | Print `export VAR=<paste-your-key-here>` guidance or a masked value instead of the literal key, or gate the echo behind an interactive-TTY check so piped/CI stdout never captures it. Verify by piping quickstart stdout to a file and grepping for the key. | security | 15 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/quickstart.go:288 | appendExport ends with an unconditional os.Chmod(profile, 0o600). When the user names an EXISTING shell profile (e.g. ~/.zshrc, ~/.bashrc) that was 0644, quickstart silently strips group/other read bits on a file it did not create and was only asked to append to — a side effect beyond "clean up your own mess" that can surprise tooling expecting the dotfile to be group-readable. | Only chmod when appendExport created the file (track the O_CREATE result or os.Stat before opening); leave an existing file's mode untouched. Verify by pre-creating a 0644 profile, running the append flow, and asserting the mode is unchanged. | security | 15 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/personas/bundles.go (InstallBundle → Install per member) | Task 3.15 routed the single-persona `personas install` and init/quickstart fetch-and-pin through `InstallUnit`, which delivers the complete unit (<name>.yaml + co-located <name>.md) and applies the C3 fetched-prompt guardrails. (intent_note: deferred per sprint-plan TD-006) | A follow-up that routes `InstallBundle` members through `InstallUnit` (or shares its fetch+validate+paired-write logic) so bundle installs deliver complete units uniformly. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | LOW | internal/personas/client.go:68 | The sprint-design risk profile lists an existing retry/backoff policy (429/5xx, ~500ms initial, 1.5x) as the expected defensive measure for transient registry-fetch failures, but fetch performs a single client.Do with NO retry — a transient blip or 5xx aborts the entire install/index/upgrade. The repo already has such backoff in internal/ghaction/client.go; the personas path has none. Anticipated-but-missed vs the risk profile. | Wrap fetch's client.Do in a bounded exponential-backoff retry for transient transport errors / 5xx / 429 mirroring ghaction, or explicitly document that a single-shot fetch is intended and drop the retry item from the risk profile. | error-handling | 60 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/personas/e2e_discover_test.go:80 | The 7.2 discover-by-model flow's final "test (fixture)" step calls `TemplateFixtureRunner{}.RunFixture("delia")`, which resolves the EMBEDDED community persona via `builtins.CommunityGet` — not the unit just installed to `destDir` in the install step. (intent_note: deferred per sprint-plan TD-013) | When on-disk community fixture support lands (the reserved `PersonasDir` seam), point the e2e fixture step at the installed unit so the full install→test path is exercised on disk. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | MEDIUM | internal/personas/install.go:32 | `Install` validates fetched YAML via `ValidateCommunityPersonaYAML` (strict `KnownFields(true)` over the community-persona schema), and `InstallBundle` (bundles.go:152) calls `Install` for every bundle member. (intent_note: deferred per sprint-plan TD-012) | A follow-up that either (a) confirms strict decode is intended for bundles and adds a bundle strict-decode reject test, or (b) keeps bundles on `ValidateAgentYAML` and reserves strict decode for the single-unit community path. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| u | [x] | LOW | internal/personas/install.go:43 | Install reimplements the exact symlink guard + CreateTemp + Chmod + Rename sequence that writeFileAtomic (unit.go:125) already provides. Two copies of a security-sensitive atomic-write routine invite drift — a future hardening of one (e.g. fsync, O_EXCL) silently leaves the other weaker. | Have Install call writeFileAtomic(dest, data) after MkdirAll and delete its inline temp-file block; behavior is identical. | maintainability | 15 | code-review | claude | MEDIUM |
| u | [x] | LOW | internal/personas/list.go:60 | ListTiers dedups tiers with a case-SENSITIVE map keyed on raw Name, and the built-in collision guard isBuiltin (remove.go:44) is likewise case-sensitive. On the default case-insensitive darwin/Windows filesystem, a project override Reviewer.md or community Reviewer.yaml yields name "Reviewer" which neither collides with built-in "reviewer" in isBuiltin nor merges with it in the byName dedup — so `personas list` shows two rows for what the filesystem/resolver treats as one persona, misrepresenting the resolvable set on the primary dev platform. | Normalize the dedup/collision key to a canonical case (strings.ToLower(m.Name)) in both ListTiers's byName/order and isBuiltin, keeping the original-cased name only for display. | maintainability | 30 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/personas/paths.go:19 | Phase 3 redefines `PersonasDir()` from `os.UserConfigDir()/atcr/personas` to `filepath.Dir(DefaultRegistryPath())/personas` so installs land on the resolver chain. (intent_note: deferred per sprint-plan TD-001) | either add a one-time migration (move/symlink old darwin dir to the new one on first run) or explicitly record in-code that no pre-public-launch back-compat is owed. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | MEDIUM | internal/personas/search_test.go (verifyCommunityIndex) | The AC7 gate cross-checks each index entry's `provider`/`model` against the YAML at the entry's `path`, but never asserts `path == name + ".yaml"`. (intent_note: deferred per sprint-plan TD-003) | once the layout is fixed, add a gate assertion deriving `path` from `name` (or vice-versa), or explicitly document that name/path coupling is out of the gate's scope and is verified per-persona during authoring. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| u | [x] | MEDIUM | internal/personas/test.go:105 | renderFixture decides pass/fail by scanning the RENDERED output for `{{`/`}}` (`!strings.Contains(out, "{{")`), but out includes the fixture patch injected verbatim via `{{.Payload}}` (payload/template.go). A persona whose fixture diff legitimately contains double-brace syntax (Go text/template, Handlebars, Vue, Jinja, Mustache — code these personas routinely review) makes the check false and falsely reports Passed:0 even though the template rendered correctly. The soundness signal is read off payload-controlled text, not the template. | Scan the template SOURCE `text` (pre-render) for surviving actions instead of the rendered output, or rely on RenderPrompt's successful Execute plus the AgentName interpolation check (RenderPrompt already errors on unknown fields/missing keys). Verify with a fixture patch containing `{{ }}` in the diff and assert Passed:1. | correctness | 30 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/personas/test.go:126 | TestPersona's docstring claims it "resolves ... a community persona installed under personasDir" and "errors if the persona is neither a built-in nor installed", but the implementation discards personasDir (`_ = personasDir`, line 130) and delegates to runner.RunFixture which only consults EMBEDDED content (builtins.Fixture/CommunityGet). An actually-installed on-disk community persona (the point of the fetch/install flow) is non-embedded, so `atcr personas test <installed-name>` silently returns HasFixture:false with exit 0 instead of testing it — masking a broken installed persona behind a misleading docstring. Related to TD-013 but this is the runtime `personas test` command manifestation. | Implement on-disk resolution using the injected personasDir/PersonasDir seam, or correct the docstrings and return a distinguishable "not testable/not found" signal for installed names rather than the same silent HasFixture:false used for genuinely fixtureless built-ins. | testing | 60 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/personas/unit.go:109 | On RE-install of an existing persona, InstallUnit writes the fresh <name>.yaml via writeFileAtomic (rename over the previously-valid file) at unit.go:103, then writes the co-located <name>.md. If the .md write fails (e.g. disk full), the rollback os.Remove(yamlDest) deletes the just-written YAML, leaving the user with NO persona where a working one existed moments earlier. The "roll back the partial unit" logic is only safe for a first-time install, not a re-install/upgrade-in-place. | Snapshot the prior <name>.yaml (and .md) before overwriting and restore on .md write failure; or stage both temp files and rename both only after both stage successfully; or at minimum do not os.Remove yamlDest when it pre-existed the call. Verify with a test that fails the .md write on re-install and asserts the prior YAML survives. | error-handling | 60 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/personas/unit.go:126 | Persona names may legitimately contain `/` (the name regex allows it; only `bundle/` is rejected) and personaPath/MkdirAll create nested dirs. The Lstat symlink guard checks only the final dest file, not intermediate directory components, and the name is attacker-influenced via the untrusted index.json fed to InstallUnit. A pre-planted symlink at an intermediate path segment would not be caught, contradicting the guard's own comment about writing outside the personas dir. Mitigated in practice by the 0700 personas dir. | Either forbid `/` in installed persona names (flatten to a single segment) or resolve/validate each intermediate path component against the base dir before writing. | security | 60 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/personas/upgrade.go:27-92 | `Upgrade` re-fetches and overwrites only `<name>.yaml`; it never re-fetches, refreshes, or removes the co-located `<name>.md`. (intent_note: deferred per sprint-plan TD-007) | A follow-up that routes `Upgrade` through the same unit logic as `InstallUnit` (re-fetch the `.md`, write-or-remove the sibling, enforce the fetched-prompt guardrails) alongside TD-006. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | MEDIUM | internal/personas/upgrade.go:43 | `Upgrade` re-fetches the remote community YAML and validates it with the NON-strict registry.ValidateAgentYAML, whereas the fresh-install path (install.go:32 / unit.go) validates the same untrusted source with the STRICT ValidateCommunityPersonaYAML (KnownFields(true)). A community persona carrying a smuggled/unknown field is therefore rejected on install but silently accepted on upgrade — the fail-closed guarantee for untrusted input is bypassed on the refetch path. ValidateAgentYAML's doc comment (validate.go:10-14) is also stale: it claims to be the seam the personas CLI uses to vet fetched YAML, but install no longer uses it. | Point Upgrade at ValidateCommunityPersonaYAML so install and upgrade apply the same strict schema to the same untrusted source; correct or remove the stale ValidateAgentYAML doc comment. Verify: refetch a persona YAML with an unknown key and assert upgrade rejects it. | security | 30 | code-review | claude | MEDIUM |
| U | [x] | LOW | internal/registry/config.go:83 | The untrusted reviewer-persona length cap is specified to "mirror" `MaxExecutorSystemPromptLen` (=4096), an unrelated Epic 7.0.1 executor/autofix limit. (intent_note: deferred per sprint-plan TD-002) | reference the constant directly, or define a dedicated `MaxPersonaPromptLen` aliased to `MaxExecutorSystemPromptLen`, and document that the shared value is intentional rather than coincidental. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | LOW | internal/registry/persona.go:206 | ValidateFetchedPersonaPrompt parses via parse.Parse with the default mode (no parse.ParseComments), so comments are discarded pre-tree and never emitted as nodes — the `*parse.CommentNode` arm of the allowlist switch is dead code. Behavior-neutral, but it misleads a future reader into thinking comment handling is load-bearing and masks that `{{/* */}}` acceptance depends on an implicit parser default a mode change could silently flip. | Drop *parse.CommentNode from the case (leaving *parse.TextNode only), or add a comment stating comments are stripped pre-tree by parse.Parse and the arm is defensive-only; optionally add a test asserting a `{{/* */}}`-bearing prompt passes. | maintainability | 15 | code-review | claude | MEDIUM |
| U | [x] | LOW | personas/community_test.go (TestCommunityPersonas_Differentiation) | The distinctness gate fails a persona pair only at token-set Jaccard > 0.85 over combined `## Role`+`## Focus`, but the observed max across all 45 pairs is ~0.168 — roughly 5x headroom. | A future AC 04-07 revision that re-locks the threshold nearer the observed distribution (e.g. 0.35-0.40) or replaces the single Jaccard gate with a lens-keyword-disjointness assertion. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |

### [2026-07-06] From Sprint: 19.5_response_truncation_failover

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [/] | LOW | internal/fanout/artifacts.go:214 | The TD-019 memo short-circuit only fires for parsedFindingCount==0, so a truncated-with-findings result (memo set to >=1 by the failover gate) re-runs stream.ParseModelOutput at line 217 — the redundant parse the refactor aimed to remove, though bounded to the rare truncated-with-findings path. | Cache the parsed []Finding slice on Result rather than only its length so findingsFor can reuse it, or accept the bounded cost. (Won't fix 2026-07-06: accepted the bounded re-parse cost — the parsedFindingCount memo already avoids the redundant parse on the common truncated+zero-finding path; caching the full slice risks aliasing via findingsFor's in-place Finding.Reviewer mutation and downstream grounding/constraint filtering.) | performance | 15 | code-review | claude | MEDIUM |

### [2026-07-05] From Sprint: 19.4_history_time_sharding

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 2 | [/] | HIGH | cmd/atcr/review.go:285 | Multiple concurrent atcr review processes will append to the same YYYY-MM.jsonl simultaneously without synchronization, causing interleaved JSON lines and ledger corruption (Accepted 2026-07-06: won't-fix — concurrent-append O_APPEND safety is a project-wide TD-004 tradeoff shared by all 5 append-only ledgers (audit, debate, scorecard, tools, history), outside epic 19.4's scope; matches the identical writer.go precedent accepted 2026-07-04 at README:112. Low probability (small per-batch writes are atomic on Linux/macOS O_APPEND) and low impact (a torn line is skipped by Load, no cross-data corruption); a cross-platform lock dependency is not justified for one ledger.) | Acquire a file lock (flock) on the shard or a dedicated lockfile before opening for append | concurrency | 40 | code-review | brad | MEDIUM |
| 3 | [/] | MEDIUM | internal/history/shard.go:32 | LoadAll/LoadShards read and JSON-unmarshal every monthly shard on disk, then Filter discards records older than the --since cutoff. The justification for monthly sharding is the YYYY-MM filenames, but the read path throws that structure away: a repo with years of history running the common --since 30d (or the 90d default) still parses every archived month it will immediately drop. (Deferred 2026-07-06: not worth doing now — LOW-value perf optimization; the epic's 1-2yr history bound caps the scan at 12-24 small files. If revisited, add an additive LoadShardsSince(dir, cutoff) variant rather than changing LoadShards/LoadAll signatures, which would ripple into 7+ existing test call sites for no measured benefit.) | Prune glob results in LoadShards by comparing each shard's YYYY-MM stem against the cutoff month before calling Load; pass a lower-bound month into LoadShards/LoadAll (or add a windowed variant) so shards entirely before the window are never opened. Verify with a benchmark over 60 seeded shards and --since 30d reading at most 1-2 files. | performance | 60 | code-review | claude, dax, brad | HIGH |
| 3 | [/] | LOW | internal/history/writer.go:39 | Append does an unlocked O_APPEND write and documents that a large buffer split across multiple write() syscalls can be torn by a concurrent append. Epic 19.4 does not change the mechanism but changes exposure: history is now git-tracked and the project is run as multiple concurrent sessions appending to the same YYYY-MM.jsonl within a month. A torn line is silently dropped by Load's malformed-line skip, so lost findings are invisible; sharding by month did not reduce intra-month contention. (Accepted 2026-07-06: won't-fix — same history.Append write site and same decision as cmd/atcr/review.go:285 (resolved in lockstep); no cross-platform lock dependency added. Mirrors the 2026-07-04 accepted precedent for this exact O_APPEND pattern.) | If a hard guarantee is wanted wrap the shard write in an advisory file lock (flock) consistent with the mkdir-flock pattern used in the planning tooling; otherwise document that concurrent same-month runs can lose records. Verify with a stress test of N concurrent Appends and a line-count assertion. | error-handling | 60 | code-review | claude | MEDIUM |

### [2026-07-05] From Sprint: epic-19.4

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | LOW | internal/history/shard.go:32 | LoadShards loads every shard into memory before Filter applies --since, so a narrow window still reads all shards (acceptable within the stated 1-2yr bound; no month-prefix pruning) (Deferred 2026-07-06: duplicate of the MEDIUM month-pruning item at this same line; same decision — defer, add LoadShardsSince additively if ever picked up.) | Skip shard files whose YYYY-MM stem is entirely older than now-since before loading, to keep long histories fast | UNDER_ENGINEERING | 30 | execute-epic-independent |

### [2026-07-05] From Sprint: epic-19.1

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | LOW | cmd/atcr/review.go:384 | The audit hook re-reads sources/pool/findings.txt that the history hook read moments earlier in the same review, so the pool file is parsed twice per review run | Parse the pool findings once in the CLI layer and pass the finding set to both history.RecordReview and audit.RecordReview, or expose a shared parse result (Won't-fix: accepted — the duplicate parse mirrors internal/history's independent capture pattern per epic design; not worth a cross-package signature refactor for a sub-ms, once-per-run gain.) | PERFORMANCE | 30 | execute-epic-cumulative |

### [2026-07-05] From Review: pr-105-recovery

Recovered from stale, never-merged PR #105 (`td/2026-06-26`, 46 commits, 9 days behind `main` and conflicting). Before closing that PR, each of its fixes was checked against current `main`; most were already independently re-implemented, but these were not — they are re-added here as fresh items for `/resolve-td` rather than merging the stale branch directly.

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | HIGH | internal/tdmigrate/shard.go:116 | WriteShards prunes every `*.yaml` file already present in dir before renaming staged shards into place, without checking whether a given file is actually shard output (Accepted 2026-07-05: won't-fix — PR #107 (merged, squash 9bd724d7) independently revisited this exact function on 2026-06-27 (adding the atomic tmp+rename staging), and reaffirmed rather than changed the "dir is owned entirely by this tool" doc comment; `TestWriteShards_IdempotentPrune` already encodes whole-directory ownership as the intended contract. `items/` is documented in SCHEMA.md as exclusively used for TD shards, so this is a deliberate design choice, not a gap.) | Skip files that don't parse as a valid shard (or otherwise verify a file is this tool's own prior output) before removing an existing `*.yaml` during the prune step | CORRECTNESS | 30 | pr-105-recovery |

### [2026-07-04] From Sprint: epic-19.0

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/history/reader.go:14 | Load reads the entire findings-history.jsonl into memory with no rotation/retention, so an unbounded ledger grows memory use over time (Deferred 2026-07-04: covered by epic 19.4 history_time_sharding; retention/rotation is out of scope for epic 19.0.) | Add optional retention/rotation or streaming aggregation if ledgers grow large (retention explicitly out of scope for epic 19.0) | PERFORMANCE | 60 | execute-epic-cumulative |
| U | [/] | LOW | internal/history/writer.go:35 | Append relies on a single O_APPEND write being atomic across concurrent atcr review runs; POSIX does not guarantee atomicity for multi-KB writes on regular files so simultaneous runs could interleave a JSONL line (Accepted 2026-07-04: won't-fix — cross-platform flock is out of scope for epic 19.0, which only requires sequential appends; the writer.go:38 doc-comment fix documents the risk.) | Take an advisory flock around the append (mirror the mkdir-flock pattern used for the TD README) or bound batch write size | EDGE_CASES | 45 | execute-epic-independent |

### [2026-07-04] From Sprint: epic-18.1

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | LOW | internal/reconcile/emit.go:211 | ambiguous.json gray-zone cluster Problem cells are not symbol-anchored while findings.json cells are - a now-harmless cosmetic asymmetry (debate correlation is anchor-insensitive as of the 18.1 HIGH fix) (Accepted: intentional by design — cluster members stay raw and StripSymbolAnchors normalizes for identity correlation, documented at symbol_anchor.go:82-90; no current ambiguous.json consumer needs the anchor. Revisit only if a TD ToC consumes ambiguous.json) | If the TD ToC ever consumes ambiguous.json, stamp toAmbiguousWire cluster problems via the same symbolResolver | CORRECTNESS | 30 | execute-epic-stage3 |
| 1 | [/] | LOW | internal/reconcile/symbol_anchor.go:56 | safeSymbolAnchor blacklists only table/parse-breakers (pipe, parens, whitespace), allowing markdown-active chars (backtick, brackets, angle) that render oddly but cannot corrupt the table or the parse (Accepted: leave permissive — no supported parser (Go, C-family, Python) can emit a name containing backtick/bracket/angle; generics and operators are stripped before Node.Name is set, so tightening guards unreachable input) | Leave permissive - a strict identifier whitelist would wrongly reject valid names like C++ Foo<T> or ~Dtor; revisit only if odd rendering is observed | SECURITY | 15 | execute-epic-independent |

### [2026-07-03] From Sprint: 18.0_technical_debt_tooling

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 3 | [/] | LOW | internal/debt/add.go:162 | A single AppendItem re-reads and re-parses the README three times and writes it twice, and SyncShards regenerates the entire shard store via a full migrate. Bulk-adding N items in a loop is effectively O(N) full-store rewrites and 2N shard-directory rewrites. (Deferred: .planning/epics/deferred/18.0.1_debt-bulk-add.md — was mis-cited as "Epic Plan 18.1", an unrelated epic; corrected 2026-07-05) | Expose a batch-append that mutates the README once, syncs shards once, and refreshes stats once for a batch; keep the single-item path for the interactive case. Verify by benchmarking a 100-item batch against 100 single adds. | performance | 120 | code-review | claude | MEDIUM |

### [2026-07-03] From Sprint: 17.0_auto_merged_fixes

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 4 | [/] | MEDIUM | internal/verify/localvalidate.go:81 | exec.CommandContext SIGKILLs only the direct child on timeout; a validation command that spawns subprocesses (e.g. sh -c make ...) leaves grandchildren orphaned and still running after the deadline, as cmd.WaitDelay only force-closes the parent pipe fds and does not reap the process group (Deferred: Epic Plan 22.0 process_group_reaping — TD-006/sprint-plan.md:296 was a bare TD-number reference with no backing plan; promoted to a real epic 2026-07-05) | Set cmd.SysProcAttr Setpgid true and kill the whole group (syscall.Kill(-pid, SIGKILL)) on the cancel/timeout path (unix build-tagged) so grandchildren are reaped too | performance | 0 | execute-sprint | execute-sprint | MEDIUM |

### [2026-07-02] From Sprint: 15.1_leaderboard-cost-na-rendering

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 2 | [/] | LOW | internal/scorecard/export.go:275-281 | Export groups records by the scrubbed persona/model, and scrubField's absolute-path regexes can reduce an entirely path-like input string to empty. Two genuinely distinct reviewer identities that both happen to be path-like scrub to the same empty string and silently merge into one aggregated group, blending the costPer/SurvivedSkepticRate/FindingsRaisedAvg values this epic's change is meant to make trustworthy. Adjacent to, not caused by, this epic, but feeds the same grouping key costPer's group totals depend on. (Won't fix: by-design -- scrubField intentionally merges identities that scrub-equal, locked in by TestExport_DistinctIdentitiesMergeWhenTheyScrubEqual, export_test.go:296) | By-design behavior, not a bug: grouping keys off the scrubbed identity is deliberate (scrubField strips PII/paths/secrets before export), and TestExport_DistinctIdentitiesMergeWhenTheyScrubEqual locks the merge-on-equal-scrub as an anti-regression invariant. No source change. | error-handling | 60 | code-review | claude | MEDIUM |

### [2026-06-30] From Sprint: epic-14.1

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | MEDIUM | internal/fanout/grounding.go:54 | out-of-scope category is a blanket grounding-gate exemption in all modes, so an adversarial or hallucinating model can launder a fabricated finding past AC2 by tagging category=out-of-scope in diff/blocks mode | Restrict the out-of-scope exemption to files mode (or require the cited FILE:LINE/EVIDENCE to anchor before honoring it in diff/blocks) (Won't-fix: binding epic 14.1 Clarifications Q2 — out-of-scope is a deliberate, AC-traced (AC 06-04) exemption, "annotated, not promoted"; relabeled from a bare DEFERRED with no plan citation 2026-07-05) | correctness | 30 | execute-epic-independent |
| 1 | [/] | MEDIUM | internal/fanout/grounding.go:47 | groundFindings ignores Result.Tools and Result.PayloadMode, so a tool-enabled or files-mode agent citing a real line it legitimately read outside the changed range is dropped unless it tags out-of-scope or quotes a changed line in EVIDENCE | Thread Tools/PayloadMode into the gate and relax the range/evidence requirement for tool/files agents (Won't-fix: binding epic 14.1 Clarifications Q2 — the out-of-scope prompt convention already instructs such agents to tag read-but-unchanged citations, which the gate exempts; relabeled from a bare DEFERRED with no plan citation 2026-07-05) | correctness | 45 | execute-epic-independent |
| 1 | [/] | LOW | internal/fanout/review.go:470 | computeGroundingData builds a fresh gitRunner and re-runs validateRange + --name-status + --unified=0 for a range the payload builder already diffed, duplicating ~4 git subprocesses per review and per resume | Reuse the payload builder's memoized rangeChunks/gitRunner for the same base..head instead of recomputing the zero-context diff (Deferred: Epic Plan 22.4 grounding_gitrunner_reuse) | performance | 45 | execute-epic-independent |

### [2026-06-29] From Sprint: 13.4_brace_language_parsers

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 2 | [/] | LOW | internal/astgroup/parsers/src/braceparser/main.go:1 | Wasm ABI (alloc/free/parse/emit/pins) duplicated across three parser modules | Extract shared ABI into common guest package (Deferred: Epic Plan 22.2 astgroup_shared_guest_abi) | maintainability | 60 | code-review | bruce | MEDIUM |

### [2026-06-28] From Sprint: epic-13.4

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | LOW | internal/astgroup/parsers/src/braceparser/main.go:20 | The alloc/free/emit/pins guest ABI is duplicated across three parser sources (goparser, pyparser, braceparser); the threshold the existing code documents for extraction (parser count > 2) is now crossed | Extract the shared guest ABI into a package referenced via go.mod replace directives coordinated in build.sh (Deferred: Epic Plan 22.2 astgroup_shared_guest_abi — duplicate of braceparser/main.go:1) | MAINTAINABILITY | 60 | execute-epic-stage3 |

### [2026-06-27] From Sprint: 13.1_ast_plugin_architecture

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| U | [/] | LOW | internal/astgroup/parsers/src/goparser/main.go:39 | The guest pins alloc'd buffers and hands the host int32(uintptr(unsafe.Pointer(&b[0]))) as a stable guest offset, valid only because the current Go GC is non-moving. This is an undocumented runtime invariant, not a language guarantee; a future moving GC makes the packed pointer a dangling offset with no detection. The unsafe alloc/free/emit/node boilerplate is copy-pasted across goparser, pyparser, and the 13.4 brace parsers, so any ABI fix must be applied N times. (Deferred: Epic Plan 22.2 astgroup_shared_guest_abi — the "revisit if parser count grows" trigger has fired (3 parsers); duplicate of braceparser/main.go:1) | Extract the shared alloc/free/emit/pins ABI into one internal guest package imported by every parser main.go, and add a build-time note pinning the GC assumption (or switch to an explicitly reserved arena). | maintainability | 120 | code-review | claude | MEDIUM |
| U | [/] | MEDIUM | internal/astgroup/parsers/src/pyparser/main.go:112 | scanTripleQuotes and stripComment are quote/escape-unaware. A triple-quote appearing inside a # comment, or a # appearing inside a string literal, flips the multi-line-string state machine the wrong way, so arbitrary spans of real code get classified as string content and dropped from significantLines, silently erasing blocks. The heuristic disclaimer documents the risk but does not bound it: on unusual source the structural hash diverges from the true AST. (Deferred: Epic Plan 22.3 pyparser_quote_aware_scanning — heuristic limitation already documented at pyparser/main.go:72-78 and 152-156, degrades to proximity grouping.) | Run scanTripleQuotes only over the code portion (skip after an unquoted #) and skip triple-quotes occurring inside single-line strings. Add regression fixtures: a # containing a triple-quote, and a string literal containing #. | security | 120 | code-review | claude | MEDIUM |

### [2026-06-23] From Sprint: 8.0_reconciler_library

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| U | [/] | MEDIUM | unknown:0 | [Story 01 / Story 06] DoD item not implemented: both CI jobs (root ci.yml + reconcile-module PR-time job) must be marked as REQUIRED status checks on the main branch-protection rule. The CI workflow deliverables they depend on are all present and verified; only the protection-rule toggle is unset. The two story-level [ ] boxes (AC 01-06, AC 06-02) are the same single external action. (External action — one-click GitHub repo Settings -> Branches toggle, not epic-sized code work; no epic plan needed. See dod-completion-summary.md for original context.) | Configure branch protection in GitHub repo Settings -> Branches: add the root CI job and the reconcile-module PR-time job as required status checks. External repo-admin UI action (post-merge), not a source-tree change. | docs | 15 | code-review | claude | MEDIUM |

### [2026-06-22] From Sprint: 7.0.1_executor_model_configuration

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 7 | [/] | LOW | internal/registry/config.go:524 | Potential prompt injection via system_prompt (Won't-fix: config.go:531–534 explicitly documents that control chars are intentionally NOT rejected in system_prompt; the --- delimiter added at executor.go:225 eliminates the CRLF metadata-forgery surface; otto's fix conflicts with the documented design decision) | Add control character validation to SystemPrompt | security | 15 | code-review | otto | MEDIUM |

### [2026-06-20] From Sprint: epic-5.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/cache/store.go:115 | Eviction does a full ReadDir+Stat of the cache dir on every Put even when under the cap; O(n) per write scales poorly if the cache accumulates thousands of entries (Won't-fix 2026-06-21: scan runs serially under the store mutex and LLM calls dominate latency; no Epic 5.2 perf criterion requires O(1) Put — added state not justified at LOW) | Maintain a running total-size counter and skip the directory scan when it is under the cap, or evict only on a periodic/threshold basis | PERFORMANCE | 30 | execute-epic-cumulative |

### [2026-06-20] From Sprint: epic-5.0

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/report/render.go:317 | The "File not found" warning format string is duplicated across internal/reconcile/emit.go (writeFindingsList) and internal/report/render.go (writePathWarning) in separate packages | Extract a shared constant/helper only if a common low-level rendering package emerges; a cross-package dependency is not justified for one format string today (Won't-fix 2026-06-21: two independent format strings across packages with no shared dependency; the recorded fix confirms extraction is not justified at this scope) | CROSS_CUTTING | 15 | execute-epic-cumulative |
| U | [/] | MEDIUM | internal/reconcile/reconcile.go:26 | Validation root is hardcoded to "." at every call site, so "atcr reconcile <path>" for a review of another repo, or running from a non-repo-root CWD, falsely flags every finding as "file not found" | Thread the reviewed repo root explicitly or add a --repo flag, applied consistently with the verify stage which uses the same "." convention (Deferred 2026-06-21: the narrow Root: os.Getwd() variant is a no-op — filepath.Abs(".") already equals the CWD, so it would not fix the non-repo-root / other-repo case; the real fix is to plumb the reviewed-repo path explicitly via a --repo flag threaded through the reconcile and verify call sites, est 60) (Deferred: Epic Plan 22.1 reconcile_verify_repo_root_threading) | EDGE_CASES | 60 | execute-epic-independent |

### [2026-06-20] From Sprint: 4.7.1_backup-swap-hardening

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 3 | [/] | MEDIUM | internal/fanout/reviewdir.go:318 | backupExisting() unconditionally RemoveAll's <path>.bak.old and <path>.bak.new at entry, but forceBackupOutputDir() only calls guardForeignBackup(dir+".bak") — the two staging siblings are not guarded. On an arbitrary --output-dir a user who owns <output-dir>.bak.old or <output-dir>.bak.new has it silently deleted by --force, re-opening the Epic 4.7 "never silently delete user data" contract. NOTE: epic clarification Q4 deliberately scoped the guard to .bak only (atcr-internal suffixes); this finding re-raises that decision as worth reconsidering, not a defect against the recorded scope. (intent_note: deferred per epic §Clarifications Q4 — extending guardForeignBackup to .bak.old/.bak.new is out of scope) (Won't-fix 2026-06-20: binding Epic 4.7.1 Q4 — guard stays scoped to .bak; .bak.old/.bak.new are atcr-owned staging names cleared by entry-time RemoveAll; confirmed via /sprint-clarification 95%) | In forceBackupOutputDir() (reviewdir.go:440) extend the guard to guardForeignBackup(dir+".bak.old") and guardForeignBackup(dir+".bak.new") before backupExisting, or refuse if either exists non-empty. Add a test pre-creating a foreign <output-dir>.bak.old with user content and assert --force errors instead of deleting it. | security | 60 | code-review | claude | MEDIUM |
| 3 | [/] | MEDIUM | internal/fanout/reviewdir.go:373 | The advertised "an interrupted swap never leaves the user with neither generation" invariant is conditional on a best-effort SILENT restore succeeding. If restorePriorBackup's os.Rename(backupOld,backup) fails (e.g. backup partially recreated, or perms change), the only surviving copy is stranded under .bak.old — which the very next run's entry-time RemoveAll(backupOld) (line 318) then deletes. Same pattern at the copy site swapStagedBackup (atomic.go:159-167) feeding atomic.go:97 RemoveAll(bakOld). (Won't-fix 2026-06-20: observability half done (restore failure now logged); the lone-.bak.old-as-generation redesign is rejected per Epic 4.7.1 Q3 — .bak.old is atcr-owned temp deleted at entry, one-generation contract + TestBackupExisting_CleansStaleStagingStragglers stand; confirmed via /sprint-clarification 95%) | Surface restore failures loudly (log/return) instead of dropping them, and/or do not auto-RemoveAll .bak.old at entry when .bak is absent — treat a lone .bak.old as the surviving generation to recover. Add a test where restore fails and a subsequent run is asserted not to delete the only surviving copy. | correctness | 120 | code-review | claude | MEDIUM |
| 3 | [/] | LOW | internal/fanout/reviewdir.go:388 | backupCrossDevice's inner os.Rename(backupNew,backup) relies on backupNew and backup sharing a filesystem, an invariant that holds only by naming coincidence (both are siblings of path). If anyone later relocates backupNew under path, the inner rename silently becomes cross-device and returns a raw EXDEV to the user. (Won't-fix 2026-06-20: same-fs invariant holds by construction — backup and backupNew are both siblings of path; a runtime guard would be unreachable dead code today and is already documented at reviewdir.go:415-419; confirmed via /sprint-clarification 90%) | Add a test that forces renameFn to return syscall.EXDEV and makes the copy fail (a copy seam, or unreadable src), staging a prior .bak first; assert the prior .bak content is restored intact, the live tree survives, and .bak.new is cleaned up. Cover the copy-failure leg at minimum. | testing | 120 | code-review | claude | MEDIUM |
| 3 | [/] | LOW | internal/fanout/reviewdir_test.go:458 | Test-coverage gaps in the failed-swap/cleanup paths: TestBackupExisting_FailedSwapPreservesPriorBak asserts no .bak.old straggler but not .bak.new; the non-ErrNotExist Lstat(backup) error branch (reviewdir.go:333-335) is untested; the entry-time RemoveAll straggler-cleanup failure legs (reviewdir.go:318-323) are untested. Each is a real error branch a regression could silently break. | Add assert.NoDirExists for src+".bak.new" at reviewdir_test.go:458; add a perms-based test forcing Lstat(backup) to fail with a non-ErrNotExist error; add a test where .bak.old cannot be removed and assert the typed "clearing stale staging backup" error. Skip on root/CI where perms are not enforced. (Partial 2026-06-21: gaps 1 (.bak.new assertion) and 3 (RemoveAll(.bak.old) failure) covered; gap 2 (non-ErrNotExist Lstat(backup)) deferred — needs an lstatFn production seam since the staging siblings share a parent dir so perms cannot isolate it) (Deferred: Epic Plan 22.5 backup_swap_lstat_seam_test) | testing | 30 | code-review | claude | MEDIUM |

### [2026-06-18] From Sprint: epic-4.3

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | LOW | internal/validation/validation.go:62 | System-dir denylist covers only /etc, /proc, /sys, leaving /boot, /dev, /root, /var and ~/.ssh writable via --output-dir / --output (deliberate Option-B permissive choice) (Won't-fix: intended Option-B policy per epic 4.3 clarifications 2026-06-18; revisit only on a concrete isolation requirement) | If stronger isolation is later required, switch to an allowlist anchored at the repo/.atcr root instead of a denylist | SECURITY | 30 | execute-epic-independent |
| 1 | [/] | LOW | internal/validation/validation.go:86 | Severity and Enum validators are shipped but wired to nothing (ParseSeverity/ValidFormat remain the live paths); they exist only to satisfy AC5/AC7 and future use (Won't-fix: intentionally public for AC5/AC7 per epic 4.3 clarifications; deletion breaks ACs, wire-in out of scope) | Revisit and delete if no caller adopts them within a release, or wire them in where duplication can be removed | OVER_ENGINEERING | 15 | execute-epic-independent |

### [2026-06-18] From Sprint: epic-4.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/registry/attribution.go:55 | attribute() recurses and reconstructs the join even for a single-error errors.Join (which still satisfies Unwrap() []error), a small avoidable cost on the load-time validation path | Optionally short-circuit when len(children)==1 if profiling ever flags it (WON'T-FIX 2026-06-18: trigger unmet — error-path only, never hit on normal load; no perf AC in epic 4.2) | INTEGRATION | 5 | execute-epic-independent |

### [2026-06-18] From Sprint: epic-4.1.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | MEDIUM | internal/fanout/review.go:361 | If a server shutdown (or CLI SIGINT) fires after all agents already succeeded but before ExecuteReview's interrupted := errors.Is(ctx.Err(), context.Canceled) check, a fully-completed run is stamped Interrupted=true and status.go:216 overrides RunCompleted to RunInterrupted (a false interrupted; inverse of AC4). Pre-existing in the CLI-shared path, newly reachable via MCP shutdown. | Gate the interrupted marker on at least one agent ending in StatusTimeout/cancelled rather than purely on parent ctx.Err()==Canceled. NOTE: touches CLI-shared review.go (out of scope for epic 4.1.2's MCP-only change); window is microscopic and outcome benign (resume no-ops a complete run) - separate design. (WON'T-FIX 2026-06-18: --resume self-healing via ClearInterrupted (resume.go:220) already recovers a stale interrupted-on-complete; revisit in a backlog sprint if insufficient) | CORRECTNESS | 30 | execute-epic-independent |

### [2026-06-16] From Sprint: 3.5_severity-rank-consolidation

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:18 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestMergeNormalizesMixedCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:30 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestGrayZoneNormalizesMixedCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:42 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestMergeNoDisagreementOnCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |

### [2026-06-16] From Sprint: epic-3.5

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/stream/severity.go:20 | The canonical SeverityRank map carries a read-only-after-init invariant in a comment only, with no test guard preventing a future caller from mutating the shared map (which would race across concurrent fan-out agents). (Won't fix: structural guard — reconcile copy-on-init at merge.go:29-31 means no consumer writes stream.SeverityRank directly; grep confirms zero write sites across consumers. Snapshot test trips the over-simplification gate; Rank() accessor cascades to 14+ direct-lookup sites — both out of pure-consolidation scope.) | Add a stream test that snapshots the map and asserts it is unchanged after consumers run, or wrap it behind a Rank(sev) accessor. | OBSERVABILITY | 20 | execute-epic-independent |

### [2026-06-16] From Sprint: 3.4_scorecard-diagnostics-writer

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:118 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; the diagnostic already routes to the injectable writer, only fmt.Fprintf's own return is dropped; propagating it breaks Emit's never-fail contract) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:199 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; Append loop already surfaces firstErr; propagating the Fprintf return breaks the best-effort contract) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:235 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; verdictTallies already writes to injectable w; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:248 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; verification-read-failed diagnostic writes to injectable w; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:276 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; orphan-verdict diagnostic routes to injectable w, locked by TestEmit_OrphanVerdictDiagnosticRoutesToDiagWriter) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:114 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; over-long-line warning uses injectable w via diagWriter; read continues; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:145 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; decodeRecord writes to injectable w then returns (Record,bool) — no error cascade) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:155 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; schema-version skip writes to injectable w; identical to malformed-record path) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | LOW | internal/scorecard/store.go:194 | Redundant call to diagWriter (Wontfix: FALSE POSITIVE — diagWriter is the required typed-nil guard for the nil-able opts.Writer interface, not redundant; removing it reintroduces the panic fixed by commit 476c6d1) | Remove diagWriter call and use opts.Writer directly since ReadRecords already resolves it | performance | 2 | code-review | otto | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:211 | Error from diagWriter is silently discarded (Wontfix: FALSE POSITIVE — `_, _ = fmt.Fprintf` is the documented best-effort never-panic diagnostics contract at store.go:22-24; returning the write error would regress a successful read on a broken sink and logging is circular; confirmed working as designed via /sprint-clarification 97%) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| U | [/] | MEDIUM | internal/mcp/handlers.go:220 | The MCP engine carries a structured *slog.Logger (e.log) used for every other diagnostic in handleReconcile (e.g. the require_verified warning at line ~225), but scorecard diagnostics are routed to raw os.Stderr, so MCP-path scorecard write-failures/malformed-record/orphan-verdict warnings emit as unstructured plaintext that bypasses the logger's level filtering, formatting, and sink redirection. NOTE: this was a DELIBERATE, documented epic decision (Clarifications Q2: supply os.Stderr at the call site; adapting e.log to an io.Writer was explicitly OUT of scope), so this is enhancement debt, not a regression. (intent_note: deferred per epic Clarifications Q2 — adapting e.log to an io.Writer is out of scope) (Wontfix: ACCEPTED ENHANCEMENT DEBT — handlers.go:220 is an unconditional EmitOpts{Diag: os.Stderr} call, not deferred logic; all five Epic 3.4 ACs are met and the comment at handlers.go:214-219 satisfies AC4; e.log→io.Writer adaptation is out of scope per Clarifications Q2; confirmed via /sprint-clarification 97%) | If MCP observability is later desired, adapt e.log into an io.Writer shim (slog-backed at Warn level) and pass it as Diag instead of os.Stderr, so MCP-path scorecard diagnostics flow through the same structured pipeline as the rest of the handler. Defer unless/until structured MCP diagnostics are required. | error-handling | 30 | code-review | claude | MEDIUM |

### [2026-06-14] From Review: llmclient OpenAI-compatible tool handling

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| U | [/] | LOW | internal/llmclient/chat.go:1 | No provider-conformance test matrix for the OpenAI-compatible surface. The client deliberately absorbs real wire divergence (string-encoded vs raw-object tool_call `arguments`, lenient finish_reason) but is exercised only against synthetic fixtures, so a regression against a specific provider's actual tool_call shape (OpenAI, litellm, Ollama, vLLM, Together) would not be caught. This is the robustness the official SDK is assumed to provide, achievable here without adopting it. | Add a recorded-fixture conformance suite: capture a real `tool_calls` response from each target provider and assert the parser (`ToolCallArguments`, `chatToolResponse` decode, finish_reason handling) yields identical engine-facing results. NOTE: scope is a few days, not a quick-win — consider promoting to a standalone test-remediation plan rather than resolving inline. (Deferred: .planning/epics/deferred/5.1_openai_compatible_conformance_suite.md — was uncited; corrected 2026-07-05) | testing | 480 | review | claude | LOW |
