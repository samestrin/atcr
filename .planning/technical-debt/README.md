# Technical Debt Tracking

This file is a staging area for small technical debt items discovered during development. Items are triaged and moved to individual sprint documents in `sprints/` as they are prioritized.

## Directory Structure

```
technical-debt/
├── README.md                    # This file (staging area)
├── CLAUDE.md                    # AI assistant guidelines
└── sprints/
    ├── active/                  # Currently being addressed
    ├── pending/                 # Prioritized, not yet started
    └── completed/               # Resolved items
```

## How to Use

1. **Small items**: Add to this README under "Staging Area" below
2. **Larger items**: Create a new document in `sprints/pending/`
3. **During sprint planning**: Move items from pending to active
4. **After resolution**: Move items from active to completed


### [2026-06-11] From Sprint: epic-1.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [ ] | LOW | internal/doctor/run.go:200 | StatusNetworkError detail surfaces bounded(err.Error()) which can embed the provider base_url host/path (never the API key, which lives only in the Authorization header) in the table and JSON output. | Document that base_url (never the key) may appear in network_error detail so users avoid embedding credentials in base_url. | SECURITY | 10 | execute-epic-independent |
| 1 | [ ] | LOW | internal/doctor/render.go:25 | RenderTable returns no error and discards the tabwriter Flush error, so a failed/truncated table write is silent while RenderJSON's error is checked. | Have RenderTable return an error like RenderJSON, or at least surface the Flush error to the caller. | CORRECTNESS | 15 | execute-epic-independent |
| 1 | [ ] | LOW | internal/doctor/render.go:35 | RenderTable folds Detail into the HINT column when Hint is empty, so a raw provider error snippet appears under the HINT header with no visual distinction from an actionable hint. | Keep Detail in its own column or prefix substituted detail (e.g. 'error: ...') so the table does not mislabel a raw snippet as a hint. | UNDER_ENGINEERING | 15 | execute-epic-independent |
| 1 | [ ] | LOW | internal/doctor/run.go:165 | base_url pre-flight accepts any http(s) URL with a non-empty host but does not reject a path already ending in /chat/completions or containing query/fragment, which llmclient then concatenates into a malformed endpoint reported only as network_error. | Warn on a base_url whose path extends beyond the version segment, or document the expected base_url shape in registry.md. | EDGE_CASES | 20 | execute-epic-independent |
| U | [ ] | MEDIUM | .gitignore:3 | Binary-ignore patterns 'atcr', 'atcr-mcp', 'atcr-mcp-ssee' are unanchored, so they also match the source dirs cmd/atcr/ and cmd/atcr-mcp/. New files under cmd/atcr/ are silently ignored (had to be force-added) and cmd/atcr-mcp/ source appears untracked entirely (git ls-files cmd/atcr-mcp is empty). | Anchor each binary pattern to repo root (/atcr, /atcr-mcp, /atcr-mcp-ssee) so only root-level build artifacts are ignored, then verify cmd/atcr-mcp/ source is tracked and add it if missing. | CONFIG_DRIFT | 15 | execute-epic-cumulative |
| U | [ ] | LOW | cmd/atcr/doctor.go:90 | doctor folds all problems into per-agent statuses with no overall stderr summary line, so a CI log scan that does not parse the table gets no signal; the plan mentioned logs to stderr. | Emit a one-line stderr summary (n ok / n failed) so non-table and CI consumers get a status-independent signal. | OBSERVABILITY | 20 | execute-epic-independent |

### [2026-06-11] From Sprint: epic-1.1

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [ ] | LOW | internal/fanout/status.go:122 | status.json reserved byte counter ToolBytes is *int64 while the sibling registry byte field tool_budget_bytes is *int — two byte-quantity fields modeled with different integer widths (note: existing payload_byte_budget is *int64), an inconsistency a future stage may trip over. | Make registry ToolBudgetBytes a *int64 to match status ToolBytes and the existing payload_byte_budget for a consistent byte-count type across artifacts. | EDGE_CASES | 15 | execute-epic-independent |
| U | [ ] | LOW | internal/registry/config.go:64 | yaml omitempty on input-only reserved fields (tools/max_turns/tool_budget_bytes/role) is inert because Registry is only decoded, never marshaled — the tag has no effect. | Drop omitempty from the reserved yaml input tags, or leave as-is; cosmetic only. | OVER_ENGINEERING | 5 | execute-epic-independent |
| U | [ ] | LOW | internal/reconcile/emit.go:30 | Verification.Verdict and Verification.Skeptic lack omitempty and have no enum validation, so a future stage emitting an empty verdict produces verdict:"" and bad verdict/role values are not caught at the contract boundary. | Document that Epic 3.0 must validate verdict against confirmed/refuted/unverifiable when it populates the block, since the loader does not. | UNDER_ENGINEERING | 15 | execute-epic-independent |
| U | [ ] | LOW | internal/registry/config.go:67 | role:"" is accepted and stays empty in 1.x while the documented planned default is reviewer, so a future Stage 3/4 must apply the default itself; the loader provides no safety net (intentional per recorded option-a decision). | Ensure Epic 3.0/4.0 applies the reviewer default when activating role. | UNDER_ENGINEERING | 5 | execute-epic-independent |

### [2026-06-11] From Sprint: 1.0_atcr_core

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [x] | LOW | cmd/atcr/report.go:15 | `--format` help promises md/json/checklist but no enum validation exists yet, so invalid values would only fail inside the future handler (intent_note: deferred per sprint-plan TD-003) | typed enum value or PreRunE validation mapping to exit 2 | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/report.go:62 | `--output` calls os.WriteFile on the user-supplied path with no validation — an absolute path, `..`, or a symlink target is written verbatim (writes through a symlink to its target) (intent_note: deferred per sprint-plan TD-021 (intended CLI behavior)) (closed 2026-06-11: resolved-by-design — v1 verbatim --output is intended, like shell redirection; revisit only if v2 sandboxed/CI hardening is requested) | open with O_NOFOLLOW and/or constrain the target under the review dir for sandboxed/CI contexts | security | 0 | execute-sprint | execute-sprint | MEDIUM |
| 1 | [x] | HIGH | cmd/atcr/flags.go:38 | validateRangeFlags requires --base and --head together (exit 2 otherwise), but every shipped CI integration passes --base alone: README Actions snippet runs `atcr review --base "origin/${{ github.base_ref }}"`, docs/ci-integration.md:37 does the same, and examples/ci-gate.sh's [base-ref] path execs `atcr review --base "$BASE_REF" --fail-on ...`. Verified empirically: `atcr range --base main` exits 2 with "--base and --head must be used together", so every user who copies the documented CI setup gets a guaranteed usage failure instead of a review. | Either allow base-only invocation (default head to HEAD in validateRangeFlags and gitrange.Resolve) or correct all three doc/example sites to add --head HEAD. Verify by running examples/ci-gate.sh with a base-ref argument and the README snippet command in a test repo, confirming exit 0 on a clean range. | integration | 30 | code-review | claude | MEDIUM |
| 1 | [x] | MEDIUM | cmd/atcr/main.go:84 | CLI documents exit semantics 0/1/2 and maps flag errors and positional-arg errors to 2, but cobra's unknown-subcommand path is unhandled: `atcr bogus` exits 1 (verified) while `atcr range --bogus-flag` exits 2. In CI, exit 1 specifically means "findings at/above threshold", so a typo'd subcommand in a pipeline is indistinguishable from a failed review gate. | Map cobra's unknown-command error to exit 2 — detect the unmatched-args case in main() or exitCode(), or give root a RunE returning usageError for unrecognized arguments. Verify with `atcr bogus; echo $?` printing 2. | patterns | 30 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/range.go:15 | The same range-resolution failure class maps to different exit codes: `atcr range` lets invalid-ref/shallow-clone/empty-range errors exit 1, while `atcr review` wraps the identical gitrange.Resolve failure as usageError exit 2. Pre-flighting with `atcr range` returns 1 for the exact condition that returns 2 from `atcr review`, and exit 1 from range conflates with the gate-failure meaning in the README exit table. | Pick one classification for resolution failures (exit 2 fits "usage or configuration") and apply it in both range.go and review.go, updating whichever AC disagrees. Verify with `atcr range --base x --head bogusref` and `atcr review --base x --head bogusref` returning the same code. | consistency | 15 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/anchor.go:21 | anchorDir discards the error from fanout.ReadLatest and always reports "no review specified and no .atcr/latest pointer", but ReadLatest distinguishes missing file from corrupt pointer (path separators, ".."). A corrupt/tampered latest pointer is silently misreported as absent. The identical drop-the-error pattern is duplicated in internal/mcp/handlers.go resolveReviewDir, violating the wrap-with-%w convention on both surfaces. | Wrap the underlying error with %w in cmd/atcr/anchor.go (CLI surface; MCP twin split to its own Group 4 row 2026-06-11) so a corrupt .atcr/latest surfaces its invalid-review-id message. Verify by writing "../escape" into .atcr/latest and running `atcr status` — error should mention the invalid id. | patterns | 15 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/anchor.go:27 | anchorDir classifies an argument as an explicit path only via strings.ContainsRune(arg, filepath.Separator). On Windows the separator is backslash, so a forward-slash relative path like reviews/2026-06-11_x is not recognized as a path, falls into bare-id validation, and is rejected with a misleading "invalid review id" error. Path-vs-id contract behaves differently per platform. | Treat '/' as a path marker too (strings.ContainsAny(arg, "/\\") or filepath.ToSlash before testing) so path detection is platform-uniform. Verify with a unit test asserting "a/b" resolves as a verbatim path on all platforms. | consistency | 15 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/report.go:63 | I/O-failure exit-class mapping is inconsistent: reconcile.go comments that an I/O failure during RunReconcile is exit 2 (never the gate's exit 1), yet report.go returns a --output WriteFile failure as a plain error (exit 1). The same disk-write failure class yields exit 2 from `atcr reconcile` but exit 1 from `atcr report --output`. Distinct from the captured report.go:62 symlink TD item, which concerns where the write lands. | Decide one classification for local I/O failures and apply it in report.go (wrap WriteFile error in usageError to match reconcile.go, or update reconcile.go's rationale). Verify by forcing a write failure (read-only dir) and comparing exit codes. | patterns | 15 | code-review | claude | MEDIUM |
| 1 | [x] | MEDIUM | cmd/atcr/reconcile.go:92 | fail_on precedence is implemented twice: registry.ResolveSettings resolves Settings.FailOn including the embedded default HIGH, but that value is never consumed — the reconcile gate hand-rolls its own precedence in resolveGateThreshold (flag > project > registry, deliberately no embedded default). docs/registry.md:83-95 documents fail_on=HIGH as an embedded default participating in resolution, true for ResolveSettings but false for the actual gate, so docs, registry package, and cmd layer disagree about whether an unconfigured project gates at HIGH. | Pick one source of truth: route the gate through ResolveSettings with an explicit no-default-gate sentinel, or delete FailOn from Settings/ResolveSettings and document that the embedded fail_on default applies only to the generated project config template. Verify with a test asserting gate behavior for an unconfigured project matches docs. | consistency | 60 | code-review | claude | MEDIUM |
| 1 | [x] | MEDIUM | cmd/atcr/review.go:84 | docs/ci-integration.md:17 promises exit 2 for missing API key env var and invalid config, and review.go:85 claims config errors map to exit 2. But a missing API key env is detected only at invoke time (llmclient client.go:115-117), surfacing as ErrAllAgentsFailed — a plain uncoded error mapped to exit 1. PrepareReview build failures (persona resolution, unknown provider, prompt render — explicitly configuration errors per fanout/review.go:277) also return uncoded errors exiting 1. | Pre-flight the roster's APIKeyEnv vars and wrap PrepareReview-stage errors with usageError in runReview (call PrepareReview and ExecuteReview separately in the CLI to distinguish phases), or correct the docs table. Verify with an e2e test running review with the key env unset, asserting the exit code matches docs. | risk-verification | 60 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/report.go:71 | cmd/atcr/report.go:71-88 and internal/mcp/handlers.go:299-313 implement the same reconciled-findings loader twice with already-divergent details: the CLI converts os.ErrNotExist into a guidance error ("failed to parse findings"), the MCP version returns raw ErrNotExist with different messages. Any future findings.json contract change must be applied twice and will silently drift. | Move the loader into the report or reconcile package (returning os.ErrNotExist sentinel) and have both the CLI command and MCP handler wrap it with layer-specific guidance. Verify both call sites compile against the single helper and tests pass. | patterns | 15 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/reconcile.go:93 | resolveGateThreshold (reconcile path) treats whitespace-only --fail-on as unset via TrimSpace and silently falls through to config tiers, while failOnThreshold (review one-shot path) treats any non-empty string as a value and hard-errors exit 2 via ParseSeverity. The same flag value produces a usage error in one command and a silent fallback to a possibly different threshold in the other. | Normalize both readers through one helper that trims first and applies one semantic (recommend: whitespace-only is unset in both). Add a table test covering "", "  ", and "high" through both commands. | consistency | 5 | code-review | claude | MEDIUM |
| 2 | [/] | MEDIUM | internal/fanout/engine.go:88 | Every non-serial slot spawns its own goroutine with no semaphore or worker-pool bound (intent_note: deferred per sprint-plan TD-017) (Deferred: Epic Plan 1.4) | bound the parallel lane with a buffered semaphore channel sized from a new max_parallel setting | performance | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | LOW | internal/fanout/engine.go:0 | (1) NewEngine(nil) would nil-panic inside invokeAgent rather than failing cleanly; (2) DurationMS is 0 on the ctx-short-circuit path even when real wall-clock elapsed before cancellation; (3) parallel lane passes slots[i] by arg while the serial goroutine closes over slots and indexes directly — divergent styles that invite a future closure-capture bug (intent_note: deferred per sprint-plan TD-018) | add a nil-completer guard, stamp elapsed time on early-return paths, and standardize slot capture to explicit parameters | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | LOW | internal/fanout/status.go:55 | atomicWriteFile renames a temp over the target (atomic w.r.t. readers) but never fsyncs the temp or the parent dir, so a power-loss crash between rename and metadata flush could lose the file on some filesystems (intent_note: deferred per sprint-plan TD-019) | fsync temp + parent dir in atomicWriteFile if durability is required; optionally stage the pool in a temp dir and rename it into place for set-atomicity | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [/] | MEDIUM | internal/fanout/status.go:48 | ReadReviewStatus reads manifest.json then sources/pool/summary.json in two separate steps while the background fan-out (started by the atcr_review handler) is writing both (intent_note: deferred per sprint-plan TD-023) (Deferred: Epic Plan 1.5) | write a single status.json snapshot at fan-out completion (or a completion sentinel) so status is read from one atomic file instead of two | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | MEDIUM | internal/fanout/reviewdir.go:153 | Review-id collision handling is TOCTOU-racy: ReviewID resolves collisions via an exists() stat probe and ScaffoldReviewDir then uses non-exclusive MkdirAll, so two reviews started concurrently for the same branch in the same second (two MCP atcr_review calls, or CLI plus MCP server) both pass the probe, scaffold the identical directory, and interleave payload/manifest/pool writes in a single review dir, silently corrupting provenance. ReviewExists also treats any os.Stat error as not-exists, widening the window. | Make directory creation the atomic claim: create the review root with os.Mkdir (fail if exists) and on EEXIST retry with the next collision candidate, instead of probing with Stat beforehand. Verify with a concurrent test launching two PrepareReview calls with identical branch/date/suffix asserting distinct dirs. | integration | 60 | code-review | claude | MEDIUM |
| 2 | [/] | MEDIUM | internal/fanout/status.go:48 | There is no terminal state for dead reviews: ReadReviewStatus treats absent summary.json as in_progress unconditionally. If ExecuteReview fails after fan-out (WritePool/WriteManifest I/O error returns nil summary, MCP background path only logs to stderr), or the MCP server dies / is killed past the 5s drain while a fan-out may run 600s, summary.json never appears and atcr status reports in_progress forever. The Skill polling loop cannot distinguish a running review from a dead one; .atcr/latest already points at the orphan, poisoning default-anchor calls. (Deferred: Epic Plan 1.5) | On post-fan-out persistence failure write a best-effort failure marker the status reader understands, and in ReadReviewStatus compare manifest StartedAt plus the global timeout (persist effective timeout in the manifest) against now to report a distinct stale/abandoned status. Verify with tests injecting a WritePool failure and a stale manifest with no summary.json. | integration | 60 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/fanout/review.go:197 | The partial flag has two sources of truth with opposite corruption policies: ExecuteReview writes summary.json (Partial) first then finalizes manifest.json (Partial); ReadReviewStatus reads Partial from summary.json while reconcile paths use ReadManifestPartial from manifest.json. If WriteManifest fails after WritePool, the manifest permanently says Partial=false while summary.json says true, so reconciled artifacts record partial:false for a partial run. ReadManifestPartial silently returns false on missing/corrupt manifest — the exact opposite policy of ReadReviewStatus which hard-errors on corruption. | Make summary.json the single source of truth for Partial: have ReadManifestPartial read sources/pool/summary.json (or fall back to it) and surface read/parse failures instead of silently defaulting false; or write the finalized manifest before summary.json so summary is the last artifact and sole completion signal. Verify with a test that fails WriteManifest after WritePool and asserts reconcile and status agree. | consistency | 60 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/fanout/status.go:12 | The status constants' doc contract states a malformed LLM response yields StatusFailed with the parse error recorded in AgentStatus.Error, but no code path implements this: stream.ParseModelOutput silently skips unparseable lines, findingsFor returns zero findings for pure-prose output, and invokeAgent marks any successful HTTP response StatusOK. An agent returning complete garbage is recorded as ok with findings_count 0 — indistinguishable from a genuinely clean review. | Either implement the contract (mark StatusFailed or a new status when content is non-empty but yields zero parsed findings, recording a diagnostic in AgentStatus.Error) or correct the doc comment and AC mapping to reflect silent-skip semantics. Verify with a test feeding prose-only content through WritePool. | consistency | 30 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/fanout/review.go:18 | The risk-profile performance measure "files-mode payload with deterministic byte-budget truncation" is effectively disabled: byteBudget is a hardcoded const 0 and ApplyByteBudget treats 0 as unlimited, with no config knob anywhere in cmd/, internal/registry/, or internal/mcp/. AgentStatus.Truncated is structurally always false, payload.ValidateBudget has zero production callers, the "every truncation recorded" defensive measure is vacuous, and a large changeset sends unbounded payloads to 6+ providers concurrently (cost blowout, opaque context-length 4xx failures for the agent AND its fallbacks which reuse the identical payload). | Expose a byte-budget setting through the registry/project/CLI precedence chain (plumbing exists per the comment) with a sane embedded default and feed it into buildPayloads in place of the const, calling ValidateBudget at load time — or explicitly record in the risk register that truncation is deferred and unbounded payloads are accepted for v1. Verify with an integration test that a configured budget produces Truncated=true in status.json. | risk-verification | 120 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/fanout/engine.go:179 | Slot attribution is slot-scoped — invokeSlot rewrites Agent, PayloadMode, Truncation to the primary's values when a fallback answers — but DurationMS is only the winning attempt's duration measured inside invokeAgent. A slot whose primary burned 120s before failing and whose fallback answered in 10s reports duration_ms 10000, understating slot wall time. Distinct from the captured engine.go DurationMS=0 ctx-short-circuit item. | Time the whole chain in invokeSlot (stamp time.Since on the returned Result regardless of which attempt won) or add a separate slot_duration_ms field. Verify with an engine test using a slow-failing primary plus fast fallback. | patterns | 15 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/fanout/engine.go:200 | classifyStatus maps context.Canceled to StatusTimeout, so a user Ctrl-C records every unfinished agent as "timeout" in status.json and the merged summary, and ReadReviewStatus reports the run as failed-by-timeout. Cancellation and deadline expiry are conflated in persisted artifacts, misleading post-hoc analysis about whether the 600s budget was exhausted. (intent_note: intentional per sprint-plan Phase 3 - canceled-as-timeout is a documented design decision) | Add a distinct StatusCancelled (or map Canceled to StatusFailed with a "cancelled" error string) in classifyStatus and handle it in summarize/statusFor, or document that timeout includes cancellation. Verify with an engine test cancelling the parent context. | patterns | 15 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/fanout/status.go:65 | ReadReviewStatus derives AgentsDone solely from summary.json, and WritePool writes all per-agent status files plus summary.json only after Engine.Run returns every result. During the entire fan-out AgentsDone is 0 and AgentsPending equals roster size, then both jump at once. The ReviewStatus shape (agent_count, agents_done, agents_pending) promises per-agent progress granularity the writer can never produce mid-run. | Either write each agent's status.json incrementally from the engine as results land (ReadReviewStatus counts sources/pool/raw/agent/*/status.json while summary.json is absent), or document agents_done/agents_pending as completion-only. Verify by polling status mid-fan-out with a slow fake completer. | integration | 120 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/fanout/review.go:124 | fanout treats the roster as the union of both lanes (rosterNames appends Agents and SerialAgents; ErrEmptyRoster fires only when both empty), but registry.LoadProjectConfig hard-rejects len(cfg.Agents)==0 with "no agents selected", so a config listing only serial_agents (legitimate when every provider is rate-limited) can never load, with a misleading error message. The two modules disagree on what constitutes an empty roster. | In LoadProjectConfig, reject only when both Agents and SerialAgents are empty (matching the fanout contract), or if parallel-lane-required is intentional, fix the message and document it. Verify with a load test for a serial-only config. | consistency | 15 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/fanout/review.go:326 | buildAgent does mp := payloads[mode] with no presence check; the invariant rests on neededModes independently re-deriving the same EffectivePayloadMode result over the same roster — duplicated logic with no guard. If they diverge, an agent is invoked with an empty payload and FileCount 0, producing a plausible-looking but vacuous review silently. Every other lookup in the function (registry agent, provider) errors explicitly on a miss. | Add the comma-ok form on payloads[mode] and return a wrapped error naming the agent and mode (consistent with adjacent unknown-agent/unknown-provider errors), or have buildSlots build payloads itself so the mode set cannot diverge. Verify with a unit test passing a payloads map missing the agent's mode. | patterns | 15 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/fanout/reviewdir.go:41 | ReviewID runs the exists/collision probe only for derived ids; an explicit override is returned verbatim after the traversal check, and ScaffoldReviewDir's MkdirAll succeeds on an existing directory. Over MCP, atcr_review with the same id called twice (a plausible client retry while the first shows running) launches a second detached background fan-out into the SAME review dir — two goroutines concurrently rewriting manifest, payloads, pool, and summary with no error surfaced. | In ReviewID or PrepareReview, reject an override whose review directory already exists, or have handleReview refuse when ReviewExists reports the id and run state is in_progress. Verify with a test calling PrepareReview twice with the same IDOverride asserting the second call errors. | integration | 30 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/fanout/review.go:127 | The risk profile says empty ranges hard-error before provider calls, and gitrange.ErrEmptyRange covers zero-commit ranges, but a range containing only merge commits or empty commits has CommitCount > 0 with zero changed files. BuildEntries returns nil entries, the payload text is empty, and PrepareReview scaffolds the review and ExecuteReview fires the entire provider pool at an empty payload — with no len(entries)==0 guard at any layer. | In PrepareReview (or buildPayloads), return a typed error when every needed mode produced zero entries, before ScaffoldReviewDir runs; map it to the same user guidance as ErrEmptyRange in CLI and MCP callers. Verify with a test using a range whose only commit is an empty commit. | integration | 30 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/llmclient/client.go:121 | If a provider base_url embedded userinfo (`https://user:pass@host`), a request-creation or transport error wraps the full URL via `%w`, which could surface the credential in logs — the same risk class as the (already-prevented) API-key leak (intent_note: deferred per sprint-plan TD-015) | defensively strip userinfo (or scrub the URL) before building the request, so the client is safe independent of registry validation | security | 0 | execute-sprint | execute-sprint | MEDIUM |
| 3 | [x] | LOW | internal/llmclient/client.go:186 | On any non-200, attempt drains the response body to io.Discard and reports only "provider returned HTTP <n>". OpenAI-compatible providers put the actionable reason (invalid model, quota exhausted, context-length exceeded) in the JSON error body, so all-agents-failed exits and per-agent status.json Error fields carry no root cause — inconsistent with the git runners which append captured stderr to wrapped errors. | Read a bounded snippet of the error body (io.LimitReader 2-4 KB), sanitize to a single line, and include it in the HTTP-status error for both retryable-exhausted and immediate-failure paths. Verify with an httptest server returning 400 with a JSON error body. | patterns | 15 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/llmclient/client.go:193 | The 200-path decodes resp.Body with no io.LimitReader, so a misbehaving or hostile base_url endpoint can stream an arbitrarily large body buffered into the content string. With 6+ agents fanning out concurrently this multiplies into unbounded memory in a long-lived atcr serve process. The payload side enforces byte budgets; the response side has no symmetric defense. | Wrap resp.Body in io.LimitReader with a generous cap (e.g. 32 MB) before json.Decode and fail with a clear "response exceeds size limit" error at the cap. Verify with an httptest handler emitting an oversized body. | risk-verification | 15 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/llmclient/client.go:142 | The retry loop returns immediately on status==0 (any transport-level error: connection reset, EOF, DNS blip), reserving backoff exclusively for HTTP 429/5xx. A momentary network glitch burns the primary agent instantly and silently promotes its fallback (a different provider/model), while a provider that returned a 500 gets three attempts — the most transient failure class gets the least resilience. | Retry transport errors on the same backoff schedule when ctx.Err() is nil (context cancellation/deadline must still return immediately so timeout classification stays correct). Verify with an httptest server dropping the first connection and succeeding on the second. | patterns | 30 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | internal/mcp/handlers.go:170 | handleReconcile calls RunReconcile (which runs Reconcile+Emit) before the `len(SourcesScanned)==0` guard, so a review whose sources/ exists but holds no findings files gets empty reconciled artifacts written to disk as a side effect, then the handler returns the "no agent results" error — whereas the CLI rejects earlier and writes nothing (intent_note: deferred per sprint-plan TD-025; split per clarification 2026-06-11) | pre-check the sources tree in handleReconcile (mirror resolveReviewDir) before RunReconcile so both layers fail-before-emit | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | LOW | internal/gitrange/resolver.go:118 | (closed 2026-06-11: ruled out as not-a-defect per /sprint-clarification — Resolve deliberately returns (nil, ErrEmptyRange) and handleRange already echoes in.Base/in.Head per AC 04-04 Edge Case 5; no contract change) handleRange echoes caller-supplied (possibly empty) base/head on ErrEmptyRange because gitrange.Resolve returns nil Resolution before refs are constructed (split from internal/mcp/handlers.go:170 row; sprint-plan TD-025) | have gitrange.Resolve return resolved refs alongside ErrEmptyRange so handleRange can echo them (coordinate with other internal/gitrange rows) | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 4 | [x] | MEDIUM | internal/mcp/handlers.go:155 | CLI `atcr reconcile` resolves the gate threshold via --fail-on then project config then registry config, but the MCP atcr_reconcile handler only gates when the fail_on argument is explicitly passed and never consults project or registry config. The same review reconciled through MCP silently reports pass=true where the CLI would gate on a configured threshold — a Skill or client orchestrating via MCP loses the project's configured CI gate. | In handleReconcile, when in.FailOn is empty, fall back to the same project/registry resolution the CLI uses (extract resolveGateThreshold into a shared package and call it from both layers). Verify with a handler test using a project config with fail_on set and no explicit arg. | integration | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/mcp/handlers.go:154 | Risk profile states MCP handlers honor ctx cancellation, but handleReconcile, handleReport, and handleStatus bind the context parameter to underscore and run RunReconcile, Render, and file reads with no cancellation point. For a large review a client cancel or server shutdown cannot interrupt the reconcile pipeline. | Plumb ctx into RunReconcile (ctx-checks between Discover, Reconcile, Emit stages) and pass the handler ctx through instead of discarding it. Verify with a handler test using a pre-cancelled context asserting context.Canceled. | risk-verification | 30 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | internal/mcp/handlers.go:83 | gitrange.Options documents that the CLI layer guarantees --base/--head travel together and never combine with --merge-commit, enforced in validateRangeFlags — but the MCP handlers (handleReview, handleRange) are a second caller with no such validation. A client passing base, head, AND merge_commit gets merge_commit silently ignored (explicit branch wins), reviewing a different range than requested with no error; merge_commit plus head yields a CLI-flavored error referencing flags that do not exist in the MCP arg vocabulary. | Add an argument-combination check at the top of handleReview/handleRange (or inside gitrange.Resolve so every caller is covered): reject merge_commit combined with base or head, phrasing the pairing error in json field names when raised through MCP. Verify with handler tests for the three-arg and merge_commit+head combinations. | integration | 30 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | internal/mcp/handlers.go:156 | atcr_review runs the fan-out asynchronously with summary.json as the completion signal, but handleReconcile never checks that signal before RunReconcile. A client calling atcr_reconcile while the fan-out is mid-WritePool can read a partially-written agent set and silently emit complete-looking reconciled artifacts and a pass verdict computed from a subset of agents — no error, no partial flag (manifest still Partial=false until finalization). Calling earlier yields the misleading "no agent results found; run atcr_review first" while the review is running. | In handleReconcile (and handleReport's missing-findings path), read run state via fanout.ReadReviewStatus first and reject with "review is still in_progress; poll atcr_status" when summary.json is absent. Grow the same guard on the CLI reconcile path for parity. Verify with a handler test scaffolding a review (manifest present, no summary.json) asserting the rejection. | integration | 60 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | internal/mcp/handlers.go:266 | changedFileCount calls payload.BuildEntries(ModeDiff), which runs one changedFiles call plus per-file isBinary numstat and a full per-file git diff (roughly 2N+3 git processes with all diff content materialized) only to return len(entries). atcr_range is documented as a cheap no-provider pre-flight, but on a 50+ file range it spawns 100+ git subprocesses where a single git diff --name-status yields the count. | Export a lightweight count helper from payload (wrapping the existing changedFiles name-status call) and use it in changedFileCount instead of BuildEntries. Verify RangeResult file_count is unchanged for added/deleted/renamed files via existing handler tests. | integration | 15 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/mcp/handlers.go:291 | resolveReviewDir discards the error from fanout.ReadLatest and reports "no reviews found; run atcr_review first", but ReadLatest distinguishes a missing file from a corrupt/tampered pointer (path separators, "..") — a corrupt .atcr/latest is misreported as absent (split from cmd/atcr/anchor.go:21 row per clarification 2026-06-11; CLI half resolved) | Wrap the underlying error with %w in internal/mcp/handlers.go resolveReviewDir so a corrupt pointer surfaces its invalid-review-id message. Verify with a handler test writing "../escape" into .atcr/latest and asserting the error mentions the invalid id. | patterns | 15 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/mcp/tools.go:39 | The CLI anchor accepts an explicit path verbatim by design, while MCP resolveReviewDir treats any non-empty anchor strictly as a review id and rejects every path. The strict containment is correct security for MCP, but the public contract field is named id_or_path across all three arg structs and tool descriptions, so a client doing what the name advertises gets "invalid review id". Tool names/shapes are declared a frozen v1 contract, so shipping the misnomer locks it in. | Before the contract freezes, rename the MCP field to id (descriptions saying "review id only; paths are not accepted") or document the id-only restriction in the jsonschema description text. Verify the tool descriptions no longer promise path support. | consistency | 15 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | internal/payload/builder.go:144 | `renderWithSentinels` emits head content verbatim (intent_note: deferred per sprint-plan TD-009) | neutralize content lines matching a sentinel (prefix-quote) or use a per-run nonce in the sentinel | security | 0 | execute-sprint | execute-sprint | MEDIUM |
| 5 | [x] | LOW | internal/payload/diff.go:122 | `functionContextFile` maps every git error to ok=false (fallback) and `isBinary` maps every git error to false (intent_note: deferred per sprint-plan TD-010) | inspect exit status to separate non-fatal (no diff) from fatal git errors | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 5 | [/] | LOW | internal/payload/builder.go:114 | blocks/files modes invoke up to 4-5 git processes per changed file (numstat, function-context, context, show, unified=0) (intent_note: deferred per sprint-plan TD-011) (Deferred: Epic Plan 1.6) | batch classification (`--numstat`/`--name-status` once) and split a single diff per file | performance | 0 | execute-sprint | execute-sprint | MEDIUM |
| 5 | [x] | LOW | internal/payload/budget.go:46 | `total += e.Size` could wrap negative for pathological/huge sizes, making `total <= budget` true and skipping truncation (a silent over-budget payload) (intent_note: deferred per sprint-plan TD-013) | reject negative sizes and detect `total > math.MaxInt64 - e.Size` before adding | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 5 | [x] | MEDIUM | internal/payload/manifest.go:51 | Two divergent copies of atomicWriteFile exist: the fanout copy (status.go:129) chmods the temp to 0644 citing the AC 01-03 artifact mode before renaming; the payload copy never chmods, so manifest.json — written via fanout.WriteManifest delegating to payload.WriteManifest — lands with os.CreateTemp's 0600 default while every other review artifact is 0644. The duplicated helper has already drifted (the fsync gap captured for status.go:55 also exists unrecorded in this copy). | Add the same Chmod(0o644) to payload's atomicWriteFile, or extract one shared atomic-write helper into a leaf package both import. Verify with a test asserting manifest.json mode equals status.json mode after a run. | consistency | 15 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | internal/payload/builder.go:120 | diff.go documents a verbatim contract — gitRunner.output exists because diff payloads must be shown to reviewers verbatim — but the per-file bodies that actually ship (fileBody ModeDiff via g.run, ModeBlocks via functionContextFile/contextFile which also use g.run) are TrimSpace'd. TrimSpace can strip a trailing blank context line from the last hunk, making the hunk header's line counts disagree with the body and silently diverging persisted payload artifacts from the documented contract. | Switch fileBody's diff-producing paths to g.output (raw bytes) plus ensureTrailingNewline, reserving g.run for parse-only commands (rev-parse, name-status, numstat, unified=0). Verify with a test diffing a file whose final hunk ends in a blank context line, asserting the line survives. | consistency | 30 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | internal/payload/diff.go:78 | changedFiles detects renames with -M and records oldPath, but every per-file git command in fileBody (diff, function-context, numstat, unified=0) passes only the head path as pathspec. Pathspec filtering excludes the old path before rename pairing, so a renamed file renders as a full-file addition: diff/blocks payloads bloat to the entire file as added lines, and in files mode changedHeadRanges wraps the whole file in one CHANGED sentinel even when only one line changed. | For kindRenamed files, pass both paths to the per-file git commands (git diff -M base..head -- oldPath newPath) so rename pairing survives pathspec limiting, or compute the per-file body by splitting the whole-range diff. Verify with a test renaming a file with a one-line edit, asserting blocks/files payloads mark only that line changed. | integration | 60 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | internal/payload/manifest.go:26 | Manifest.CompletedAt is declared, JSON-tagged, and documented but no code ever assigns it — fanout.ExecuteReview finalizes only the Partial flag before rewriting manifest.json. Because of omitempty the field silently never appears, so the provenance record promised to downstream tools permanently lacks completion time and run duration cannot be derived from the manifest. | Stamp p.manifest.CompletedAt = time.Now().UTC() in ExecuteReview just before the final WriteManifest call in internal/fanout/review.go:198-201. Verify with a test asserting completed_at is present and >= started_at in the re-read manifest.json. | integration | 15 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | internal/payload/budget.go:34 | ApplyByteBudget's doc says it keeps as many files as fit within budget, but the implementation drops smallest-first, which minimizes the number of kept files: for sizes [1,2,3,4,90] and budget 90 it drops the four small files and keeps only the 90-byte one. When the budget is wired, this preferentially keeps huge files (generated/lockfiles) while discarding many small source files — likely the inverse of review value. | Decide the intended policy: if keep-most-files is intended, sort drop candidates largest-first; if keep-largest is intended, rewrite the doc comment. Add a test pinning the chosen behavior with mixed sizes. | patterns | 15 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | internal/payload/builder.go:130 | The defensive measure "every truncation/fallback recorded" covers agent-chain fallback (FallbackUsed/FallbackFrom) and truncation (Truncated/FilesDropped), but the blocks-mode payload fallback — functionContextFile failing or yielding zero hunks and silently degrading to plain -U10 — leaves no record anywhere: no slog line, no payload marker, no status.json field. An operator cannot tell which files got function context versus the degraded fallback. | Emit a slog warning to stderr (and/or thread a per-file fallback list up through BuildEntries into the manifest or status.json) when contextFile is used in fileBody's ModeBlocks branch. Verify with a test on a brace-less file asserting the fallback record appears. | risk-verification | 30 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | internal/payload/builder.go:32 | The exported Build/BuildDiff/BuildBlocks/BuildFiles API has zero production callers — fanout.buildPayloads and the MCP handler use only BuildEntries — and BuildDiff's semantics diverge from joinEntries(BuildEntries(ModeDiff)): BuildDiff returns the verbatim whole-range diff including raw binary-diff lines, while the entries path substitutes binary markers and per-file trimmed bodies. A future caller picking the obvious top-level entry point produces payloads that differ from the persisted payload/<mode>.txt provenance artifacts. | Either delete the unused Build* string entry points (keeping BuildEntries as the single source) or reimplement them as thin joinEntries(BuildEntries(...)) wrappers so both paths are byte-identical. Add an equivalence test if both forms are kept. | consistency | 30 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | internal/payload/resolve.go:24 | payload.ParseMode and payload.ValidMode have no callers outside the payload package's own tests — the live path validates modes via registry at load time and fanout casts the raw string directly (payload.PayloadMode(mode) at fanout/review.go:339). The two validators disagree on empty-string semantics: ParseMode resolves empty to DefaultMode ("blocks") while registry treats empty as tier fall-through. A third copy of mode semantics beyond the enum duplication already captured for registry/payload.go:9. | Either delete ParseMode/ValidMode as dead code, or make fanout's buildAgent/neededModes go through ParseMode so the payload package owns final validation and defaulting. Verify with go build plus the enum-parity test proposed in the existing TD item. | consistency | 30 | code-review | claude | MEDIUM |
| 6 | [x] | LOW | internal/reconcile/discover.go:66 | Discover skips a source file that errors on read or has a bad header with only a stderr warning (intent_note: deferred per sprint-plan TD-020) | track a skipped-source count in summary.json and optionally fail closed (or warn loudly) when a `--sources`-requested entry yielded zero readable findings | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 6 | [x] | MEDIUM | internal/reconcile/gate.go:81 | (1) adjudication.json persists in reconciled/ and is re-applied on every reconcile run; it is keyed only by content-addressed cluster ids, with no binding to the ambiguous.json generation it was authored against — a stale decisions file from a prior session whose ids still match would re-merge silently (intent_note: deferred per sprint-plan TD-024) | record the baseline ambiguous.json content hash in adjudication.json and refuse to apply a decisions file whose baseline doesn't match; add strict decode + an explicit "no clusters to adjudicate" error | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 6 | [x] | HIGH | internal/reconcile/gate.go:53 | The risk register, internal/payload/scope.go's persona prompts, and docs/payload-modes.md:67 all state the reconciler annotates findings tagged CATEGORY out-of-scope (files mode) and that out-of-range findings will be flagged during reconciliation (diff/blocks mode). No code in internal/reconcile, internal/report, cmd/atcr, or internal/mcp references out-of-scope at all — the string appears only in prompt text. CountAtOrAbove (CLI gate and MCP gate) counts an out-of-scope pre-existing CRITICAL/HIGH finding toward fail_on, so a files-mode review fails CI on issues the change never touched, and no consumer-side category filter exists as docs claim. Also blocks AC 06-04 Scenario 4 (two Story-06 DoD items unmet). | Implement the promised annotation in reconcile: exclude Category=="out-of-scope" findings from CountAtOrAbove (with a summary.json count), or add an out-of-scope filter to the report/gate layers and correct docs/payload-modes.md and scope.go wording. Note merge.go modalCategory can silently drop the out-of-scope tag when merging with a real-category duplicate. Verify with a test where a CRITICAL out-of-scope finding does not trip --fail-on HIGH. | risk-verification | 120 | code-review | claude | MEDIUM |
| 6 | [x] | MEDIUM | internal/reconcile/emit.go:151 | Two parallel markdown renderers implement the same security defense divergently: internal/report/render.go:194-199 (codeSpan) guards against a model-controlled file path containing a backtick (would close the code span and let trailing text render as live markdown), falling back to HTML-escaping. internal/reconcile/emit.go:151 renders the same model-controlled FILE field as a backtick code span with no guard, so reconciled/report.md is exposed to the exact markdown-injection class AC 01-06 fixed in the report view. Grid/esc/newline helpers are also copy-pasted between the two files. | Extract the codeSpan guard (and ideally the shared grid/esc helpers) so emit.go's RenderMarkdown uses the same backtick-safe rendering as report/render.go. Verify with a test feeding a finding whose File contains a backtick plus markdown, asserting report.md renders it inert. | patterns | 30 | code-review | claude | MEDIUM |
| 6 | [x] | MEDIUM | internal/reconcile/dedupe.go:68 | dedupeCluster records every gray-zone pair as an AmbiguousCluster regardless of union-find state, so when findings A and B score gray against each other but each scores >=0.7 against a third finding C, A and B are transitively merged while ambiguous.json still lists the A-B pair as left unmerged. The sidecar contradicts findings.txt, a Skill adjudication of "merge" for that id is a silent no-op, and "distinct" cannot be honored (the pair is already collapsed) — corrupting the adjudication contract ambiguous.go documents. | After the pair loop, drop (or annotate) AmbiguousCluster entries whose two findings ended up under the same union-find root, before returning them for emission. Verify with a test: three findings where A~C and B~C merge but A~B is gray, asserting ambiguous.json is empty or marked already-merged. | consistency | 60 | code-review | claude | MEDIUM |
| 6 | [x] | LOW | internal/reconcile/ambiguous.go:151 | preserveOriginalAmbiguous writes ambiguous.original.json with plain os.WriteFile while every other artifact in reconciled/ goes through writeFileAtomic (temp + rename). The file is the adjudication validation baseline RunReconcile prefers; a crash mid-write leaves a truncated baseline that on the next run either hard-errors JSON parsing or validates decisions against a partial id set, and the stat guard prevents it ever being repaired from the true original. | Route the preservation write through the package's writeFileAtomic (same package) so the baseline is never partial. Verify with the existing adjudication re-run tests plus one asserting atomic write usage. | patterns | 15 | code-review | claude | MEDIUM |
| 7 | [x] | MEDIUM | internal/registry/precedence.go:53 | `payload_mode: bogus` and `fail_on: bogus` resolve into Settings silently; docs use lowercase `--fail-on high` while the embedded default is `HIGH` | Phase 2 task 2.25 and Phase 3 task 3.33 | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [x] | LOW | internal/registry/project.go:0 | The generated config bakes payload_mode/timeout_secs/fail_on explicitly, so registry-tier user defaults never apply to initialized projects unless the user removes those lines | document the inheritance behavior in docs/registry.md | docs | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [x] | LOW | internal/registry/payload.go:9 | The frozen {diff,blocks,files} enum is hand-duplicated in `registry.validPayloadModes` and `payload.validModes` because the package boundary forbids `registry` importing `payload` (intent_note: deferred per sprint-plan TD-012) | add an enum-parity test from a package permitted to import both registry and payload (e.g. fanout or an e2e test package) | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| 8 | [x] | LOW | internal/stream/parser.go:97 | (1) `# atcr-findings/v1x`, `v10`, `v1.2` all match the `# atcr-findings/` prefix and are reported as `ErrUnknownVersion` rather than a clean version-token comparison, so a consumer cannot distinguish a well-formed-but-unsupported version from a garbage header (intent_note: deferred per sprint-plan TD-014) | parse the version token exactly; optionally strip remaining control characters in escapeField | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 8 | [x] | MEDIUM | internal/stream/parser.go:106 | Personas are documented to emit 7 columns; the engine appends REVIEWER (intent_note: deferred per sprint-plan TD-016) | engine constructs Reviewer from the agent name; optionally parse model output as a 7-column shape that rejects a populated 8th field | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 8 | [x] | LOW | internal/stream/writer.go:77 | (1) `Finding.AsReconciled` and `stream.ParseReconciled` have only test callers — `reconcile.Merge` builds the 9-col finding from scratch and `report` reads findings.json, so neither helper is on the integration path | if Phase 4 MCP re-ingests reconciled findings.txt, add a DISAGREEMENT column or route Merge through AsReconciled; otherwise document EVIDENCE-folding as one-way | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| 9 | [x] | MEDIUM | skill/SKILL.md:43 | (resolved 2026-06-11: AC 05-02 amended to accept static format-equivalence verification — shared stream.ParseSource contract + TestSkill_HostFindingsFormat; live run downgraded to optional maintainer smoke test) [Story 05] DoD item not verified: "Host findings format verified against pool agent output in a real review run" (AC 05-02 manual gate). No .atcr/reviews/ run artifacts exist in the repo; the 8-column host findings format is verified only by static skill_test.go checks against SKILL.md text. | Execute a real atcr review run with at least one configured provider, perform the host review per SKILL.md, and verify sources/host/findings.txt parses identically to pool findings through stream.ParseSource. | incomplete | 30 | code-review | claude | MEDIUM |
| 9 | [/] | MEDIUM | skill/SKILL.md:33 | [Story 05] DoD item not verified: "Orchestration loop verified end-to-end with a real review run" (AC 05-03 manual gate). The range → review → poll → host review → reconcile → report sequence is verified only by static SKILL.md section tests; no real review run artifacts exist in the repo. (Deferred: Epic Plan 1.7) | Run the full Skill orchestration loop against a real branch with a configured provider and record the review directory path and outcome. | incomplete | 30 | code-review | claude | MEDIUM |
| 9 | [/] | MEDIUM | skill/SKILL.md:61 | [Story 05] DoD item not verified: "Adversarial tone of host review verified in a real review run" (AC 05-04 manual gate). The adversarial personality clause exists in SKILL.md:61-63 and is statically tested, but no real host review output exists to confirm the tone lands in practice. (Deferred: Epic Plan 1.7) | During the real-run verification, inspect sources/host/review.md for adversarial tone and absence of praise, per the AC. | incomplete | 15 | code-review | claude | MEDIUM |
| 9 | [/] | MEDIUM | skill/SKILL.md:96 | [Story 05] DoD item not verified: "Ambiguity adjudication produces sensible merge/distinct decisions" (AC 05-04 manual gate). The adjudication mechanics (stable cluster ids, adjudication.json, idempotent re-runs) are unit-tested, but no real review run with gray-zone clusters has exercised host adjudication quality. (Deferred: Epic Plan 1.7) | During a real review run that yields ambiguous.json clusters, perform host adjudication per SKILL.md:96-113 and review the merge/distinct decisions for sensibility. | incomplete | 30 | code-review | claude | MEDIUM |
| U | [x] | LOW | .github/workflows/ci.yml:34 | go.mod existence guards are dead code now that go.mod is committed, and two guard styles coexist (`[ -f go.mod ]` vs `hashFiles`) | drop the guards and the standalone vet step | ops | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | LOW | .github/workflows/ci.yml:49 | CI generates a coverage profile but never uploads, thresholds, or reports it, implying coverage enforcement that does not exist | add a coverage threshold check or artifact upload to ci.yml | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | LOW | internal/gitrange/resolver.go:99 | `--merge-commit SHA` resolves base as `SHA^` (first parent) (intent_note: deferred per sprint-plan TD-007) | note the first-parent assumption in docs and `--merge-commit` flag help | docs | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | LOW | internal/gitrange/resolver.go:177 | (closed 2026-06-11: ruled out as not-a-defect per /sprint-clarification — collapse to ErrInvalidRef is intentional and test-locked; env errors are typed upstream in run and shadowed by isShallow) `resolveRef` treats any non-empty error OR empty stdout as `ErrInvalidRef` (intent_note: deferred per sprint-plan TD-008) | distinguish `err != nil` (wrap raw git error) from `out == "" && err == nil` (true invalid ref) | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | MEDIUM | internal/report/render_test.go:30 | (resolved 2026-06-11: added testdata/ golden files for md/json/checklist with -update flag and byte-for-byte comparison via TestRender_GoldenFiles) [Story 01] DoD item not implemented as specified: "Golden file tests pass for each format" (AC 01-06). No testdata/ golden files exist in internal/report/ — all three formats (md/json/checklist) and the AC edge cases are covered by inline assertions in render_test.go:30-144 instead. Test substance is equivalent but the golden-file comparison mechanism the AC and sprint-design TDD strategy specify was not implemented. | Either add testdata/ golden files for md/json/checklist renderers with a -update flag pattern and compare render output byte-for-byte, or amend AC 01-06 to accept inline assertions as the verification mechanism. | incomplete | 60 | code-review | claude | MEDIUM |
| U | [x] | MEDIUM | examples/ci-gate.sh:1 | (resolved 2026-06-11: ci-gate.sh verified end-to-end in a CI-like env against a local OpenAI-shape stub provider — exit 1/0/2 across the three documented paths with real .atcr/reviews artifacts written; AC 03-02 manual box checked, evidence at code-review/verification/ci-gate-end-to-end-2026-06-11.log. The flags.go:38 "--base/--head mismatch" prerequisite was a false premise — base-only invocation passes validateRangeFlags by design) [Story 03] DoD item not verified: "CI example script tested manually in a CI-like environment" (AC 03-02 manual gate). ci-gate.sh passes bash -n and shellcheck and the README Actions snippet parses as YAML, but there is no record of the script executed against a live PR or CI run. Note: the documented base-only invocation currently exits 2 (see HIGH finding at cmd/atcr/flags.go:38), so this manual test would have caught that. | Run examples/ci-gate.sh in a CI-like environment (or a GitHub Actions workflow on a test PR) end-to-end and record the result; fix the --base/--head pairing mismatch first. | incomplete | 30 | code-review | claude | MEDIUM |

## Stats

| Severity | Open | Deferred | Resolved |
|----------|------|----------|----------|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 0 | 0 | 2 |
| MEDIUM | 1 | 6 | 25 |
| LOW | 9 | 1 | 41 |

**Last Modified:** 2026-06-11 | **Open Items:** 10 | **Deferred Items:** 7 | **Resolved Items:** 68 | **Total Items:** 85
