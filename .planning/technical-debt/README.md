# Technical Debt Tracking

This file is a staging area for small technical debt items discovered during development. Items are triaged and moved to individual sprint documents in `sprints/` as they are prioritized.

## Stats

| Severity | Open | Deferred | Resolved |
|----------|------|----------|----------|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 2 | 0 | 4 |
| MEDIUM | 12 | 1 | 29 |
| LOW | 22 | 4 | 24 |
| SEVERITY | 3 | 0 | 0 |


**Last Modified:** 2026-06-15 | **Open Items:** 39 | **Deferred Items:** 5 | **Resolved Items:** 57 | **Total Items:** 101

## Directory Structure

```
technical-debt/
├── README.md                    # This file (staging area)
├── CLAUDE.md                    # AI assistant guidelines
└── sprints/
    ├── active/                  # Currently being addressed
    ├── pending/                 # Prioritized, not yet started
    └── completed/               # Resolved items
```

## How to Use

1. **Small items**: Add to this README under "Staging Area" below
2. **Larger items**: Create a new document in `sprints/pending/`
3. **During sprint planning**: Move items from pending to active
4. **After resolution**: Move items from active to completed


### [2026-06-14] From Sprint: 3.2_disagreement_radar

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 4 | [x] | MEDIUM | internal/reconcile/disagree.go:100 | Exact problem match may double-surface gray-zone members with slightly different texts | Use file+line as key for gray-zone exclusion, drop problem | correctness | 10 | code-review | code-reviewer | MEDIUM |
| 4 | [x] | MEDIUM | internal/reconcile/disagree.go:118 | Inconsistent out-of-scope normalization: the per-finding check at disagree.go:120 uses exact f.Category == CategoryOutOfScope (case-sensitive, untrimmed) while allOutOfScope (disagree.go:166) lower-cases and trims before comparing. A finding categorized " Out-Of-Scope " is treated as in-scope by the per-finding tier (enters the radar) yet out-of-scope by the cluster guard -- contradictory decisions for the same string. (disagreement: LOW vs MEDIUM) | Route both checks through one normalized helper categoryIsOutOfScope(f.Category) (lower+trim); add a test with " Out-Of-Scope " asserting it is excluded from the radar. | correctness | 15 | code-review | bruce, claude | HIGH |
| 4 | [x] | LOW | internal/reconcile/disagree.go:118 | Solo finding condition includes zero reviewers | Route both checks through one normalized helper categoryIsOutOfScope(f.Category) (lower+trim); add a test with " Out-Of-Scope " asserting it is excluded from the radar. | correctness | 15 | code-review | bruce | MEDIUM |
| 4 | [x] | MEDIUM | internal/reconcile/disagree.go:130 | The switch treats len(f.Reviewers) <= 1 as a solo finding, so a malformed finding with an EMPTY Reviewers slice (len==0, data corruption) is silently promoted to a solo-tension radar item with a fabricated Independence of 1 via atLeastOne, misrepresenting reviewer count rather than being flagged. (disagreement: LOW vs MEDIUM) | Distinguish len==0 (skip or flag as malformed) from len==1 (true solo); add a test with an empty Reviewers slice asserting it is not surfaced as a normal solo with independence 1. | correctness | 15 | code-review | code-reviewer, claude | HIGH |
| 4 | [x] | LOW | internal/reconcile/disagree.go:159 | LoadDisagreements discards the ReadAmbiguousClusters error (clusters, _ := ...) with no log/metric/stderr, so a corrupt or truncated ambiguous.json silently drops every gray-zone cluster across the CLI default-report path and the entire MCP path with zero operator visibility. Compounding it, cmd/atcr/report.go is inconsistent: the --disagreements branch (report.go:59) surfaces the same corrupt-file error as exit 2 while the default md branch (report.go:81) silently degrades -- same file, same corruption, opposite behavior depending on an unrelated flag. | Plumb a logger (the MCP handler already has e.log) or return the error so call sites can warn on degradation; pick one corrupt-ambiguous.json policy and apply it to both report.go paths. | error-handling | 30 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | internal/reconcile/disagree.go:165 | Unsafe severity rank lookup in spread calculation (disagreement: LOW vs MEDIUM) | Add check for len(c.Findings) > 0 before accessing index 0 | correctness | 10 | code-review | bruce, otto | HIGH |
| 4 | [x] | MEDIUM | internal/reconcile/disagree.go:174 | Inconsistent out-of-scope filtering | Normalize JSONFinding.Category with ToLower/TrimSpace like allOutOfScope | correctness | 5 | code-review | bruce | MEDIUM |
| 4 | [x] | LOW | internal/reconcile/disagree.go:244 | scoreFor computes float64(spread * independence): the multiplication runs in int before the float conversion. independence is len(f.Reviewers), an unbounded slice length from external findings.json, so the widen-after-multiply ordering does not guard against overflow (unreachable for realistic inputs, but the type does not protect it). | Convert operands before multiplying: float64(spread) * float64(independence). Existing scoring tests should pass unchanged. | correctness | 5 | code-review | claude | MEDIUM |
| 4 | [ ] | LOW | internal/reconcile/disagree.go:280 | Duplicate writeRadarSection rendering logic risks divergent escaping across packages | Consolidate radar rendering into report package; reconcile should call report.writeRadarSection | security | 20 | code-review | greta | MEDIUM |
| 4 | [x] | MEDIUM | internal/reconcile/disagree.go:288 | spreadFromDisagreement accepts invalid severities | Add a defensive nil guard at function entry or document the precondition as a hard contract; add a unit test calling verificationItem with nil Verification asserting no panic. | correctness | 15 | code-review | bruce | MEDIUM |
| 4 | [x] | LOW | internal/reconcile/disagree.go:307 | verificationItem dereferences f.Verification.Skeptic with no nil guard of its own; safety is entirely positional (relies on the caller's prior isVerificationTie check in the switch). A future call path without that precondition panics with a nil dereference. | Add a defensive nil guard at function entry or document the precondition as a hard contract; add a unit test calling verificationItem with nil Verification asserting no panic. | correctness | 15 | code-review | claude | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/disagree.go:350 | Duplicated radar section rendering logic (Deferred: Epic Plan 15.0) | Extract shared writeRadarSection to a common package | maintainability | 10 | code-review | bruce | MEDIUM |
| 4 | [/] | MEDIUM | internal/reconcile/disagree.go:354 | Duplicated radar markdown rendering diverges (Deferred: Epic Plan 15.0) | Extract shared writer or make reconcile use report package's escTrunc | maintainability | 10 | code-review | bruce | MEDIUM |
| 4 | [ ] | LOW | internal/reconcile/disagree.go:413 | Redundant implementation of writeRadarSection | Remove duplicate function from internal/reconcile and use internal/report | maintainability | 15 | code-review | otto | MEDIUM |
| 4 | [x] | MEDIUM | internal/reconcile/disagree.go:430 | Missing import for html/template | Add "html/template" to imports | correctness | 5 | code-review | bruce | MEDIUM |
| 4 | [ ] | LOW | internal/reconcile/disagree_test.go:131 | Gray-zone out-of-scope exclusion is untested -- neither branch of allOutOfScope (disagree.go:135-143,166-176) has coverage. No test feeds a cluster where every member is out_of_scope (expect skipped) nor a mixed cluster with one in-scope member (expect kept), so the fail-closed semantics are unverified. | Add two cases: (1) all members out_of_scope -> zero gray_zone items; (2) mixed cluster -> the gray_zone item survives. | testing | 30 | code-review | claude | MEDIUM |
| 4 | [ ] | MEDIUM | internal/reconcile/disagree_test.go:142 | Determinism is under-tested against the actual risk. TestBuildDisagreements_DeterministicOrdering (and TestEmit_DeterministicOutput) call the builder twice on the SAME slice order, proving only referential transparency/idempotence -- not sort-stability under shuffled input, which is the AC3 determinism contract. A broken tie-break (dropping the kind-asc or problem-asc keys in sortDisagreements) would still pass; the lower tie-break tiers (line/kind/problem asc, disagree.go:449-456) are never exercised. | Build the same items in two different slice orders (or shuffle with a fixed seed) and assert identical df.Items, and add a case where two items share score+severity+file so line/kind/problem asc decide order. | testing | 30 | code-review | claude | MEDIUM |
| 4 | [ ] | LOW | internal/reconcile/disagree_test.go:240 | No test pins the documented load-bearing contract that the snapshot disagreements.json EXCLUDES the verification_disagreement tier (disagree.go:69-71 claims it is surfaced only by the live radar). TestEmit_WritesDisagreementsJSON feeds only a severity split, so a future change letting a verification tie leak into the persisted snapshot would not fail. The same gap leaves the snapshot-vs-live-rebuild equivalence (emit.go:114 BuildDisagreements over r.Ambiguous vs the live LoadDisagreements re-read) without any invariant. | Add a test running Emit with a merged finding carrying an unverifiable 2-skeptic Verification block, read back disagreements.json, assert no item has Kind==KindVerificationDisagreement, while the same input through the live RenderDisagreements path does surface it. | testing | 30 | code-review | claude | MEDIUM |
| 4 | [ ] | MEDIUM | internal/reconcile/disagree_test.go:286 | TestDisagreementsSchema_StableContract pins the Epic 6.0 handoff contract only with assert.Contains on each field-name substring. It does not pin JSON value types, nesting, or the positions/skeptics/detail keys, and cannot detect a field moving or being retyped (e.g. score becoming a string, line nesting under an object) -- Contains matches the quoted key regardless of structure. A downstream Epic 6.0 consumer could break while this test stays green. | Unmarshal into map[string]any (or a golden fixture) and assert the exact top-level and per-item key sets plus each value's Go type (score float64, line int, reviewers array). Drive a gray-zone input so the nested Position contract (reviewer/severity/problem) is pinned. | testing | 30 | code-review | claude | MEDIUM |
| 4 | [ ] | LOW | internal/reconcile/disagree_test.go:328 | TestBuildDisagreements_GrayZoneAllUnknownSeverityStillScoresAboveZero asserts only Score > 0.0, weaker than the impl/intent: the floor is exactly 1 (disagree.go:345-347) and the test comment claims it should rank "above a LOW solo (rank 1)" -- but a floor of 1 ties LOW rank 1, it is not above. The assertion would pass even at 0.0001, which would not achieve the stated ranking goal, and the comment is slightly inaccurate. | Assert the exact floor (Score == 1.0) and, better, add a LOW solo to the same input asserting the gray-zone cluster sorts at-or-above it; correct the comment to "at or above a LOW solo". | testing | 15 | code-review | claude | MEDIUM |
| 4 | [ ] | LOW | internal/reconcile/emit.go:113 | BuildDisagreements(r.JSONFindings(), r.Ambiguous) runs twice per Emit -- once for the DisagreementsJSON writer (emit.go:114) and again inside RenderMarkdown (emit.go:264) -- each rebuilding the identical radar (JSONFindings allocation + gray-key map build + sort), redundant O(n log n) work on the reconcile path. | Build the radar once at the top of Emit and pass the DisagreementsFile to both the DisagreementsJSON writer and RenderMarkdown. | performance | 30 | code-review | claude | MEDIUM |
| 4 | [ ] | LOW | internal/reconcile/emit.go:207 | ReadDisagreements unmarshals disagreements.json without ever checking SchemaVersion, so a future 2.0 file would parse into the 1.0 struct with silent field loss -- exactly the drift the version constant exists to guard against. The version is written but never validated on read, making it decorative for the Epic 6.0 consumer it was added for. | Have ReadDisagreements validate df.SchemaVersion against DisagreementsSchemaVersion (at least major-version compatibility) and error on mismatch. | maintainability | 30 | code-review | claude | MEDIUM |
| 5 | [/] | MEDIUM | internal/report/disagree.go:47 | The radar markup is rendered by two divergent copies: writeRadarSection + formatScore in internal/report/disagree.go and a structurally different copy in internal/reconcile/disagree.go:389. They are intentionally not identical (report copy truncates via escTrunc + uses joinReviewers/reviewerOrUnknown; reconcile copy uses uncapped esc + joinOrNone), so a future markup change (new field, reordered bullets) must be made in both or the live `atcr report` radar and the archival reconciled/report.md silently drift. (Deferred: .planning/epics/active/15.0_radar-renderer-consolidation.md) | Extract one shared item renderer parameterized by a truncate-vs-verbatim flag and heading prefix; have both call sites delegate. Add a test that diffs the rendered markup of both paths on the same DisagreementsFile asserting the only intended difference is truncation. | correctness | 60 | code-review | bruce, claude | HIGH |
| 5 | [x] | MEDIUM | internal/report/disagree.go:55 | esc (HTML-escape + newline-flatten) does not escape backticks, so reviewer-controlled fields rendered through esc in a normal bullet (reviewer names, severity, skeptics, disagreement) can contain a backtick that opens an inline code span affecting the rest of that line. codeSpan guards backticks only for the file path. Low impact (single line, HTML still escaped) but an un-neutralized markdown metacharacter in model-controlled fields. (disagreement: LOW vs MEDIUM) | Escape backticks in esc for non-codeSpan fields, or accept and document the single-line cosmetic risk. Verify with a reviewer name containing a backtick. | correctness | 15 | code-review | bruce, claude | HIGH |
| 5 | [x] | MEDIUM | internal/report/disagree.go:74 | Missing return statement in formatScore | Add return statement for the false case | correctness | 5 | code-review | bruce | MEDIUM |
| 5 | [x] | LOW | internal/report/disagree_test.go:14 | RenderDisagreementsJSON (the atcr report --disagreements --format json public entry point, disagree.go:36-43) has zero test coverage in report/disagree_test.go; a marshal regression or a missing trailing newline would ship undetected. | Add a TestRenderDisagreementsJSON that renders a built DisagreementsFile, asserts valid JSON round-trips back to an equal struct, and asserts the trailing newline the impl appends. | testing | 15 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | internal/report/disagree_test.go:105 | Radar injection/escape coverage is partial: TestRenderDisagreements_EscapesFreeText injects only into a Problem on a solo finding. The Positions block, Skeptics, Detail, and Disagreement fields each route through esc/escTrunc separately (disagree.go:62-85) and are untested for escaping; the report-layer escTrunc 500-rune truncation is also unpinned; and TestRenderDisagreements_GrayZoneShowsPositionsSideBySide asserts each token appears somewhere (bag-of-tokens) rather than that a reviewer is paired with its own severity/problem on one Positions line. | Inject script/backtick into a gray-zone member Problem+Reviewer and a Skeptics string asserting none render unescaped; add an escTrunc-cap assertion; and assert structural pairing (e.g. "greta -- HIGH: buffer overrun risk" on one line). | testing | 30 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | internal/report/render.go:412 | severityRank is defined separately in internal/report/render.go and internal/reconcile/merge.go; the radar sort (reconcile copy via sortDisagreements) and the report-view severity grouping (report copy) depend on different maps. A future severity-tier change added to only one map silently mis-ranks the radar in one view. Epic 3.2 deepened this coupling. | Export one canonical SeverityRank from reconcile and have report reference it; add a test ranking the same finding identically across both views. | maintainability | 30 | code-review | claude | MEDIUM |
| U | [ ] | SEVERITY | FILE:LINE | PROBLEM | FIX | CATEGORY | 0 | SOURCE | REVIEWERS | CONFIDENCE |
| U | [ ] | MEDIUM | cmd/atcr/report.go:68 | Silent error suppression on ReadAmbiguousClusters hides corrupted or tampered ambiguous.json | Log warning or annotate report output when ambiguous.json read fails | security | 10 | code-review | greta | MEDIUM |
| U | [ ] | LOW | cmd/atcr/report_test.go:70 | Incomplete ambiguous-sidecar test | Assert rendered output contains the gray-zone cluster content | maintainability | 3 | code-review | bruce | MEDIUM |
| U | [ ] | LOW | internal/mcp/handlers.go:267 | MCP handleReport surfaces the radar only embedded in format==md; there is no MCP equivalent of the CLI --disagreements flag, so an MCP client cannot request the focused/ranked radar view or its JSON machine-contract (RenderDisagreementsJSON). The format-to-radar dispatch is also duplicated between cmd/atcr/report.go:57-89 and handlers.go:267-279 (the CLI has a third branch the MCP lacks), the root cause of the parity gap. Out of this epic's ACs but a CLI/MCP-parity TD. | Extract a shared report.RenderReport(w, findings, df, opts) both call sites invoke, and add a disagreements option to ReportArgs mirroring the CLI; or explicitly document the radar as markdown-embed-only over MCP. | maintainability | 120 | code-review | claude | MEDIUM |
| U | [ ] | MEDIUM | internal/mcp/handlers.go:267 | Silent error suppression on ReadAmbiguousClusters hides corrupted or tampered ambiguous.json | Log warning or annotate report output when ambiguous.json read fails | security | 10 | code-review | greta | MEDIUM |

### [2026-06-14] From Sprint: 3.1_verify_audit_attribution

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 3 | [x] | LOW | internal/verify/emit_verification.go:25 | Docstring claim "Skeptic names every participating voter" is ambiguous | Clarify Skeptic: all voters on tie, lead on decisive, single name from on-disk block on carry-forward | maintainability | 5 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/invoke.go:72 | invokeSkeptic returns res.TrippedBudgets directly without copying; the slice is owned by the fanout.Result. No current caller mutates it, but the contract hands callers a reference into engine-owned memory with no documented read-only caveat, unlike SelectEligibleSkeptics which documents its read-only aliasing contract. Inconsistent contract documentation across the package. | Either copy the slice (append([]string(nil), res.TrippedBudgets...)) before returning, or add a read-only-aliasing note to the docstring matching select.go's convention. | maintainability | 15 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/invoke.go:150 | logSkepticFailure writes detail (which includes res.Err.Error() and is derived from the finding's reviewer-controlled File/Line/Problem merge text via the prompt) straight to stderr with no newline sanitization. A crafted finding problem containing a newline could forge an additional atcr: verify: log line (log injection). Low severity since stderr is operator-facing, but it muddies audit logs. | Strip or escape newlines in detail before the Fprintf (e.g. strings.ReplaceAll(detail, newline, space)). Verify with a finding whose Problem contains an embedded newline and confirm the log stays one line. | security | 15 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/invoke_test.go:30 | TestInvokeSkeptic_DegradesWhenNotFC drives the non-FC path with finalChat whose Complete returns empty, forcing unverifiable/empty_response. The test name claims it verifies degrade-to-single-shot, but it cannot distinguish a correct single-shot-empty from a broken path - it only asserts unverifiable, which is also the result of nearly every failure mode. The degrade is not positively confirmed. | Assert the tool loop was not entered (dispatcher count == 0) and/or make the fake Complete return a real verdict so a successful single-shot confirm is asserted. Currently the assertion is satisfied by too many unrelated failure modes. | testing | 15 | code-review | claude | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/invoke_test.go:162 | AC1 is invokeSkeptic returns tripped budgets separately. Only max_turns is exercised as a structurally-surfaced trip (TestInvokeSkeptic_SurfacesTrippedBudgets). tool_budget_bytes and timeout_secs are tested only for their Notes string, not asserted on the returned tripped []string, and no test asserts multiple budgets surfaced at once. The structural-surfacing contract is proven for one of three budget types. | Extend the byte-budget test and add a timeout-budget test to also assert Contains(tripped, tool_budget_bytes) / timeout_secs on the second return value, not just the Notes substring. Proves AC1 surfaces each budget class structurally. | testing | 30 | code-review | claude | MEDIUM |
| 3 | [x] | HIGH | internal/verify/pipeline.go:72 | The Verify/runVerify docstring (lines 72-76) states findings and skeptics are processed SERIALLY with no concurrency knob and that parallelizing across a bounded worker pool is tracked as TD-009. The actual runVerify body builds a MaxParallel-sized semaphore, a sync.WaitGroup, and fans every job into a goroutine (verifyFinding runs concurrently). TD-009 was already implemented. The lead doc on the stage's concurrency model is now actively false and will mislead anyone reasoning about determinism, ordering, or thread-safety. | Rewrite the lines 72-76 docstring to describe the bounded worker pool (reg.Verify.MaxParallel, default 4) and remove the SERIALLY / no-concurrency-knob / tracked-as-TD-009 claims. Grep the file for SERIALLY and TD-009 and confirm none remain misdescribing live behavior. | maintainability | 15 | code-review | claude | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:177 | The worker-pool fan-out has no context-cancellation short-circuit. If ctx is already cancelled when runVerify starts, every job still spawns and calls verifyFinding->invokeSkeptic, which checks only ctx==nil (not ctx.Err()). A cancelled or timed-out parent still dispatches every finding's provider calls instead of bailing, wasting the full provider-call budget on a doomed run. | Add a select on ctx.Done() before acquiring the semaphore (or at the top of the worker before verifyFinding) to record unverifiable/context_cancelled and skip the provider call. Verify with a test passing a pre-cancelled context that asserts zero completer invocations. | error-handling | 60 | code-review | claude | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:212 | Missing error handling for ReadVerificationResults | Check perr before logging | error-handling | 2 | code-review | bruce | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline.go:227 | priorByKey always built even when no findings will be skipped | Use logSkepticFailure-style logger or a structured log helper for consistency | performance | 5 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline.go:227 | Direct fmt.Fprintf to stderr bypasses codebase logger | Use logSkepticFailure-style logger or a structured log helper for consistency | performance | 5 | code-review | claude | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:242 | Case-insensitive verdict comparison may be fragile (disagreement: MEDIUM vs HIGH) | The priorByKey map is populated by reading the entire verification.json into memory for every runVerify call. While acceptable for small sets, this could scale poorly. Consider a streaming JSON decoder if the file grows significantly. | correctness | 30 | code-review | bruce | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:242 | TrippedBudgets may be nil (leading to null in JSON) (disagreement: MEDIUM vs HIGH) | The priorByKey map is populated by reading the entire verification.json into memory for every runVerify call. While acceptable for small sets, this could scale poorly. Consider a streaming JSON decoder if the file grows significantly. | correctness | 30 | code-review | reviewer | MEDIUM |
| 3 | [x] | HIGH | internal/verify/pipeline.go:242 | Nil pointer dereference on f.Verification for unverified findings (disagreement: MEDIUM vs HIGH) | The priorByKey map is populated by reading the entire verification.json into memory for every runVerify call. While acceptable for small sets, this could scale poorly. Consider a streaming JSON decoder if the file grows significantly. | correctness | 30 | code-review | bruce | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:242 | Potential memory leak or performance degradation with large prior files (disagreement: MEDIUM vs HIGH) | The priorByKey map is populated by reading the entire verification.json into memory for every runVerify call. While acceptable for small sets, this could scale poorly. Consider a streaming JSON decoder if the file grows significantly. | correctness | 30 | code-review | otto | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:242 | Skipped-finding verdict comparison uses case-insensitive trim which may match different strings (disagreement: MEDIUM vs HIGH) | The priorByKey map is populated by reading the entire verification.json into memory for every runVerify call. While acceptable for small sets, this could scale poorly. Consider a streaming JSON decoder if the file grows significantly. | correctness | 30 | code-review | case-reviewer | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline.go:350 | Explicit base.Model = "" is redundant | Remove redundant assignment | maintainability | 1 | code-review | bruce | MEDIUM |
| 3 | [x] | LOW | internal/verify/pipeline.go:356 | Explicit base.Model = "" is redundant | Remove redundant assignment | maintainability | 1 | code-review | bruce | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:379 | winningAttribution ignores nil skeptics check | Add nil check before accessing skeptics[i].Config.Model | correctness | 3 | code-review | bruce | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:386 | Missing TrippedBudgets initialization | Set base.TrippedBudgets = []string{} after base.Model assignment | correctness | 2 | code-review | reviewer | MEDIUM |
| 3 | [ ] | MEDIUM | internal/verify/pipeline.go:391 | Error path sets tripped to nil instead of empty slice, losing budget info from partial runs | Set tripped to a non-nil empty slice or nil consistently | error-handling | 3 | code-review | case-reviewer | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:395 | Missing TrippedBudgets initialization | Set base.TrippedBudgets = []string{} after base.Model assignment | correctness | 2 | code-review | reviewer | MEDIUM |
| 3 | [ ] | LOW | internal/verify/pipeline.go:421 | In verifyFinding, when invokeSkeptic returns a programming-fault error the code synthesizes an unverifiable verdict but emits no log line, so the impossible nil-ctx/cc/disp case silently becomes a normal unverifiable record indistinguishable from a real budget trip. The operational signal that a programming fault occurred is lost. | Add logSkepticFailure(sk.Name, "programming_fault", ierr.Error()) in the ierr != nil branch so the impossible case is visible if it ever fires. Verify by injecting the fault in a test and asserting the stderr line. | maintainability | 15 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/pipeline.go:435 | winningAttribution with empty winner records all voters silently | Amend the verifyFinding comment to winners on a decisive vote, all participants on a tie (see winningAttribution) so it matches the actual contract. | error-handling | 5 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/pipeline.go:435 | The verifyFinding comment says attribute Model/TrippedBudgets to the winning skeptics only - not all of skeptics[0..n]. This is only half-true: winningAttribution's tie branch deliberately records every participant's model. A reader of verifyFinding alone will wrongly conclude a tie records nothing or only the lone unverifiable voter. The two comments describe the same behavior inconsistently. | Amend the verifyFinding comment to winners on a decisive vote, all participants on a tie (see winningAttribution) so it matches the actual contract. | error-handling | 5 | code-review | claude | MEDIUM |
| 3 | [ ] | MEDIUM | internal/verify/pipeline.go:472 | winningAttribution re-derives the decisive/tie outcome independently from aggregateVerdicts instead of being told the result, so the two can silently diverge. aggregateVerdicts builds its valid set by filtering to canonical verdicts, while winningAttribution counts all non-nil entries including any non-canonical verdict string. If a skeptic ever returned a non-canonical verdict, the two would disagree on decisive vs tie, flipping which models get attributed and making the audit Model disagree with the recorded verdict. | Either have aggregateVerdicts return the winners set (or a decisive flag) and pass it to winningAttribution, or make winningAttribution filter to canonical verdicts using the same switch aggregateVerdicts uses before counting. Verify with a test feeding a non-canonical verdict among canonical ones, asserting attribution matches the aggregated verdict's voters. | maintainability | 60 | code-review | claude | MEDIUM |
| 3 | [x] | MEDIUM | internal/verify/pipeline.go:482 | winningAttribution indexes skeptics[i] and perTripped[i] while ranging over perSkeptic. The docstring asserts indices align as a precondition but the function does not defend it; a future caller passing a perSkeptic longer than skeptics or perTripped would panic. The verify stage is built on a never-panic / never-drop-a-finding failure-isolation contract, so an unguarded index that can panic contradicts the stage's own invariant. The sole current caller is safe but the function reads as reusable. | Guard the top of winningAttribution with a length check (if len(perSkeptic) != len(skeptics) or != len(perTripped) return "", nil) or bound the loop with i < len(skeptics) and i < len(perTripped). Add a unit test passing mismatched-length slices asserting no panic. | error-handling | 30 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/pipeline_test.go:208 | TestRunVerify_ToolHarnessUnavailable_RedactsDetail swaps os.Stderr to a pipe but never drains it in a goroutine while runVerify writes. If runVerify ever wrote more than the OS pipe buffer (~64KB) before w.Close(), the write would block and deadlock the test. Output is small today, but the pattern is fragile and duplicated in TestRunVerify_CorruptPriorNoWarningWhenNoSkippedFindings. | Drain the read end concurrently (start io.Copy in a goroutine before running, or use a bytes.Buffer-backed approach) so a larger-than-buffer stderr write cannot deadlock. Latent flake risk as logging grows. | testing | 15 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/pipeline_test.go:262 | alwaysChat and byModelChat both implement Complete, but every multi-skeptic test wires skeptics with SupportsFC=true so only the Chat path runs. The single-shot degrade path under multi-vote aggregation is never exercised; only the single-skeptic TestInvokeSkeptic_DegradesWhenNotFC covers degrade, and it returns empty->unverifiable. | Add a multi-skeptic test where one eligible skeptic has SupportsFC=false and the fake Complete returns a real verdict, asserting the non-FC skeptic's vote still participates in aggregation and its model is attributed. Confirms degrade interacts correctly with winningAttribution. | testing | 30 | code-review | claude | MEDIUM |
| 3 | [ ] | MEDIUM | internal/verify/pipeline_test.go:281 | TestRunVerify_ThoroughMultiSkepticRecordsAllModels (AC2) asserts only Contains(Model, m-s2) and Contains(Model, m-skep). Since all three skeptics confirm here, every model is a winner, so it does not exercise winner-vs-loser discrimination and would pass even if the impl naively recorded all participating models regardless of verdict. It also never pins the exact value or ordering of the joined Model string. (disagreement: LOW vs MEDIUM) | Assert the exact joined string (assert.Equal m-s2, m-skep) to lock ordering/dedup, and rely on the TwoConfirmOneRefute test for the discrimination property. Add an exact-equality assertion so a stray extra/duplicate model is caught. | testing | 15 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/pipeline_test.go:281 | The multi-skeptic tests hard-code an assumption about SelectEligibleSkeptics ordering in comments (Alphabetically returns [s2, skep]) and rely on it implicitly without asserting it. The AC2 never-default-to-skeptics[0] guarantee is only argued in a comment, not asserted via a controlled skeptics[0] that is a loser. (disagreement: LOW vs MEDIUM) | Assert the exact joined string (assert.Equal m-s2, m-skep) to lock ordering/dedup, and rely on the TwoConfirmOneRefute test for the discrimination property. Add an exact-equality assertion so a stray extra/duplicate model is caught. | testing | 15 | code-review | claude | MEDIUM |
| 3 | [ ] | MEDIUM | internal/verify/pipeline_test.go:330 | AC2 calls out both 2-confirm/1-refute AND 2-refute/1-confirm winning-model attribution, but only the confirmed-wins direction is tested. There is no test where two skeptics refute and one confirms asserting Model names only the two refuters and excludes the confirmer. The winningAttribution decisive/loser-exclusion logic is symmetric in code but only proven for one verdict polarity. | Add TestRunVerify_WinningModelAttribution_TwoRefuteOneConfirm mirroring the existing test with verdicts flipped, asserting verdict=refuted, Model contains the two refuters, NotContains the confirmer. Closes the untested polarity of AC2. | testing | 30 | code-review | claude | MEDIUM |
| 3 | [ ] | MEDIUM | internal/verify/pipeline_test.go:355 | TestRunVerify_WinningModelAttribution_TwoConfirmOneRefute asserts Contains/NotContains on the Model substring. Model names m-s2/m-s3/m-skep happen to avoid substring overlap, but the assertion style is fragile: a future rename (e.g. m-s or m-s30) would make NotContains(m-s3) silently wrong, and substring checks pin neither ordering nor reject duplicate emission of a winning model. | Assert the exact expected string (assert.Equal m-s2, m-skep) rather than substring Contains/NotContains. Exact equality is robust to model-name overlaps, locks selection-order dedup, and still proves the refuting model is excluded. | testing | 15 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/pipeline_test.go:362 | 1+1 tie case (1 confirmed + 1 refuted) not covered by tests | Add Skeptic assertions to lock the joinSkeptics contract the docstring promises | maintainability | 10 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/pipeline_test.go:362 | Multi-vote tests assert Model but not Skeptic field | Add Skeptic assertions to lock the joinSkeptics contract the docstring promises | maintainability | 10 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/pipeline_test.go:382 | No test exercises the pipeline-level rule that tripped budgets attach only to unverifiable winners and are dropped for confirmed/refuted winners. TestRunVerify_ThreeWayTieRecordsAllParticipantModels checks models but never asserts vf.Findings[0].TrippedBudgets content, and there is no live confirmed-finding test asserting TrippedBudgets is empty even when a losing skeptic tripped. | In the tie test, script one voter's unverifiable from a budget trip (tool-loop turns + low MaxTurns) and assert verification.json TrippedBudgets contains the tripped budget. Add a confirmed-winner test asserting TrippedBudgets is empty even when a loser tripped. | testing | 60 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/pipeline_test.go:397 | TestRunVerify_SkipDropsMismatchedPriorMetadata covers only a verdict-string mismatch. The carry-forward guard also gates on priorLoadFailed (corrupt prior) and on a missing prior entry for the key. There is no test that a SKIPPED finding with a corrupt prior verification.json drops to zero metadata without carrying garbage (the existing corrupt-prior test covers the no-skipped-findings branch, the opposite case). | Add a test: an already-verified finding (skipped, no --fresh) plus a corrupt prior verification.json; assert the rewritten finding keeps its verdict but has empty Model/zero DurationMs/empty TrippedBudgets and that the prior-unreadable warning DOES fire. | testing | 30 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/verify/pipeline_test.go:457 | TestRunVerify_SkipPreservesPriorMetadata asserts DurationMs==4242 is carried forward (skip path) but no test asserts the live (non-skip) path populates a non-zero DurationMs. Every live test uses scripted in-memory completers returning sub-millisecond, so int(time.Since(start).Milliseconds()) is routinely 0; a regression that always wrote 0 on the live path would pass every test. | Inject a clock into verifyFinding to assert a deterministic non-zero DurationMs, or add a mock turn delay and assert DurationMs > 0 on a live confirmed finding. Without one, live duration attribution is untested. | testing | 60 | code-review | claude | MEDIUM |
| 3 | [ ] | HIGH | internal/verify/pipeline_test.go:569 | TestRunVerify_WorkerPoolPreservesOrder claims to verify that a MaxParallel>1 pool produces the same per-finding verdict ordering as a serial run, but it uses alwaysChat which returns an identical confirmed verdict for every finding. A pool bug that swapped result indices would be undetectable because every finding still reads confirmed/VERIFIED. The test cannot fail on the ordering property it advertises. | Script distinct per-finding verdicts (byModelChat keyed on file/model, or distinct findings credited to distinct reviewers so each gets a different verdict) and assert a.go->confirmed, b.go->refuted, c.go->unverifiable land on the correct keys after the parallel run. Only distinguishable results can detect a mis-ordered pool. | testing | 30 | code-review | claude | MEDIUM |
| U | [ ] | SEVERITY | FILE:LINE | PROBLEM | FIX | CATEGORY | 0 | SOURCE | REVIEWERS | CONFIDENCE |

### [2026-06-14] From Sprint: 2.2_code_review_fanout_hardening

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| Solo | [ ] | HIGH | src/auth.go:42 | Missing input validation (intent_note: src/auth.go is not within epic 2.2 scope — epic covers internal/fanout, internal/registry, and internal/payload only; this finding is likely a reviewer hallucination; verify file existence before acting) | Add length check | security | 5 | code-review | bruce | MEDIUM |
| 1 | [x] | LOW | internal/fanout/artifacts.go:143 | findingsFor passes r.MinSeverity and r.MaxFindings directly to enforceConstraints — if Result fields are empty/nil, enforcement is no-op; but no validation that MinSeverity is a valid rubric level at the enforcement call site | Add validation in buildAgent or enforceConstraints that MinSeverity is a recognized level | correctness | 5 | code-review | dax | MEDIUM |
| 1 | [x] | LOW | internal/fanout/engine.go:284 | Every Result-building path threads MinSeverity/MaxFindings (single-shot engine.go:393-394, tool loop loop.go:98, fallbacks review.go:603-604) except the serial-lane cancellation short-circuit, which hand-builds a bare Result with no constraints. Benign today (timeout slot with empty Content), but the two cancellation lanes now construct Results differently — a future change stamping synthetic findings onto cancelled slots would skip guardrails only on the serial lane. | Route this branch through invokeSlot like the parallel cancellation case (engine.go:260) instead of hand-building a Result, or add the two constraint fields. Verify both cancellation lanes' Results are structurally identical. | correctness | 15 | code-review | claude | MEDIUM |
| 1 | [ ] | MEDIUM | internal/fanout/postprocess.go:14 | The severity-rank rubric {CRITICAL:4,HIGH:3,MEDIUM:2,LOW:1} is independently redefined in fanout/postprocess.go:17, reconcile/merge.go, verify, report, plus a set-form copy reviewSeverities in registry/config.go. postprocess looks up severityRank[strings.ToUpper(...)] while reconcile looks up the raw value, so a future severity change or non-canonical casing silently desyncs fan-out truncation from reconcile merging. The postprocess copy was newly added by Epic 2.2. (disagreement: LOW vs MEDIUM) | Extract a single canonical severity package (or export from internal/stream) exposing the ordered rank map plus normalizeSeverity, and have registry/fanout/reconcile/verify/report consume it. Verify by deleting the local maps and confirming the suite passes with one source of truth. | maintainability | 120 | code-review | claude | MEDIUM |
| 1 | [x] | MEDIUM | internal/fanout/postprocess.go:27 | Finding severity not trimmed before rank lookup — TrimSpace is absent at the finding side of the lookup even though MinSeverity is normalized at config load | Trim severity with strings.TrimSpace before severityRank lookup | correctness | 2 | code-review | atcr | MEDIUM |
| 1 | [x] | HIGH | internal/fanout/postprocess.go:35 | Severity normalization gap: severityRank map uses string keys but findings are compared with strings.ToUpper only — lowercase or whitespace-padded severity values map to rank 0 (below LOW) and are silently dropped. Model severity lookup uses ToUpper but not TrimSpace, inconsistent with MinSeverity normalization at config load. | Normalize finding severity consistently (strings.ToUpper + strings.TrimSpace) before severityRank lookup; add test with lowercase and mixed-case severities asserting no silent drop. | correctness | 15 | code-review | claude, dax | HIGH |
| 1 | [x] | MEDIUM | internal/fanout/postprocess.go:35 | enforceConstraints filters in place via kept := findings[:0], overwriting the caller's backing array, and sort.SliceStable reorders that same array. Safe today because the only caller (findingsFor) passes a freshly-parsed slice it never reuses, but any future caller holding the pre-enforcement slice would see its data silently clobbered and reordered. | Allocate kept := make([]stream.Finding, 0, len(findings)) to keep the input immutable, or assert the precondition at the call site. Add a test asserting the original slice is unchanged after enforceConstraints. | maintainability | 15 | code-review | claude, dax, otto | HIGH |
| 1 | [x] | LOW | internal/fanout/postprocess.go:35 | When min_severity is a non-empty but unknown value, severityRank[floor] returns 0; every real severity ranks >= 1, so the >= 0 comparison keeps everything — the floor silently becomes a no-op and emits no warning. Safe today only because registry load validates MinSeverity in a different package; the function itself fails open. | In enforceConstraints, add an explicit check: if severityRank[floor] == 0 (unknown level), log a warning and return early (no-op), rather than silently keeping all findings as if no floor were set. | error-handling | 10 | code-review | claude | MEDIUM |
| 1 | [x] | MEDIUM | internal/fanout/postprocess.go:35 | Variable `min` shadows Go 1.21+ built-in min(a,b) function | Rename local variable to avoid shadowing the built-in (e.g. minVal or floor) | correctness | 5 | code-review | claude | MEDIUM |
| 1 | [x] | MEDIUM | internal/fanout/postprocess.go:35 | severityRank is a package-level var with no sync protection — concurrent fan-out agents racing a future map write would trigger a data race. Currently read-only, but there is no const, comment, or compile-time guard documenting this invariant. | Mark severityRank with a comment documenting it as read-only after init. If mutation is ever needed, use a sync.Map or initialize a local copy. | correctness | 15 | code-review | dax | MEDIUM |
| 1 | [x] | MEDIUM | internal/fanout/postprocess.go:53 | enforceConstraints has no self-defense against max_findings <= 0. The guard len(findings) > *maxFindings fires for 0, all findings are sorted, then findings[:0] discards ALL of them while stderr reports "truncated to max_findings 0" — a silent total drop disguised as a cap. Relies entirely on registry validation in a different package; any direct caller or future config path that bypasses that guard silently destroys an agent's entire output. | In enforceConstraints, treat *maxFindings <= 0 as "no cap" (skip the truncation block), mirroring the absent-cap behavior. Add a unit test passing maxFindings=0 asserting all findings pass through with truncated==0. | error-handling | 15 | code-review | claude | MEDIUM |
| 1 | [x] | MEDIUM | internal/fanout/postprocess_test.go | Tests don't capture stderr to verify AC4 warning logs when findings are dropped or truncated — the warning path is executed but never asserted | Add stderr-capturing test asserting "dropped" and "truncated" messages are emitted when the respective constraints fire | maintainability | 10 | code-review | claude | MEDIUM |
| 1 | [x] | MEDIUM | internal/fanout/postprocess_test.go:60 | TestEnforceConstraints tests over-cap and under-cap but never maxFindings == len(findings) (the exact boundary where the guard flips). Also never feeds unknown/garbage or mixed-case severity (e.g. "BLOCKER", "high"), so the severityRank[...]==0 fallback and the finding-side strings.ToUpper are unexercised — a regression changing > to >= or deleting ToUpper would not be caught. | Add a case with maxFindings=len(in) asserting no truncation; add a case with a "BLOCKER" finding under a LOW floor asserting it is dropped; add a case with lowercase finding severity exercising normalization. | testing | 30 | code-review | claude | MEDIUM |
| 1 | [x] | HIGH | internal/fanout/postprocess_test.go:78 | Test asserts max_findings=1 but expects 2 findings in the result — the test assertion is incorrect and does not match the declared cap | Fix expected output to []string{"CRITICAL"} (the single highest-severity finding that should survive the cap) | correctness | 5 | code-review | bruce | MEDIUM |
| 1 | [ ] | MEDIUM | internal/fanout/review.go:600 | buildFallbackAgent overrides the fallback's MinSeverity/MaxFindings with the primary's (review.go:603-604) and the fallback's Scope never reaches it (Prompt is copied from primary.Prompt which already baked in the primary's scope). A user who sets scope/min_severity/max_findings on an agent that only ever runs as a fallback gets them silently ignored. Zero test coverage across all four Epic 2.2 test files. | Add a test that builds a slot whose fallback declares its own min_severity/max_findings/scope and asserts the primary's constraints win. Optionally emit a load-time warning when a fallback-only agent sets these fields so the silent-ignore is visible. | testing | 60 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | internal/fanout/review_scope_inject_test.go:1 | Reviewer flagged filename inconsistency with Go conventions but provided identical source and target names — finding text is self-contradicting; verify actual filename against Go test file naming conventions before acting | Renamed to review_scope_inject_test.go (buildAgent source is review.go; review_test.go already exists and is distinct; "inject" is load-bearing per the package's compound-descriptive-suffix pattern) | maintainability | 2 | code-review | dax | MEDIUM |
| 1 | [x] | LOW | internal/fanout/review_scope_inject_test.go:27 | TestBuildAgent_InjectsScopeFocus verifies the scoped agent's Invocation.Prompt carries the scope focus text, but for the unscoped agent only checks Prompt does NOT contain "Review Focus" — it never checks the unscoped Invocation.Prompt. Also, asserting NotContains on the exact string "Review Focus" is fragile to persona wording changes. | Add an assertion that the unscoped agent's Invocation.Prompt also does NOT contain "Review Focus", mirroring the scoped-agent assertion. Consider asserting the exact injected scope string for the scoped case to lock both wording and join correctness. | maintainability | 5 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/registry/config.go:243 | Whitespace-only min_severity values (e.g. "   ") trigger an invalid load error because validation checks the raw string rather than the normalized (trimmed) value, producing a confusing error message | Check the strings.TrimSpace normalized value instead of the raw string in min_severity validation | correctness | 5 | code-review | bruce | MEDIUM |
| 2 | [x] | MEDIUM | internal/registry/config.go:248 | Scope entries are not validated for newlines or control characters, enabling prompt injection via ScopeFocus — a scope entry containing \n can break out of the injected focus block and add arbitrary text to the agent persona prompt | Reject scope entries containing \n, \r, or other control characters at load validation | security | 10 | code-review | greta | MEDIUM |
| 2 | [x] | LOW | internal/registry/config.go:283 | validate() rejects max_findings <= 0 but accepts any positive value up to math.MaxInt, unlike every other bounded field in the file (timeout_secs, max_turns, tool_budget_bytes each have documented ceilings). max_findings is the odd one out with a floor but no ceiling — functionally harmless but inconsistent with the file's own validation conventions. | Add a documented ceiling constant (e.g. MaxFindingsCap) for consistency with other bounded fields, or add a comment explaining why max_findings is intentionally unbounded above. | correctness | 15 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/registry/config.go:317 | applyDefaults canonicalizes MinSeverity to upper-case so downstream comparisons are stable, but Scope entries are never trimmed/canonicalized at store — validation trims for the blank check without persisting the trimmed value, so " performance " survives into ScopeFocus. Asymmetric normalization between the two adjacent Epic 2.2 fields. | Trim-and-store scope entries in applyDefaults (mirroring MinSeverity canonicalization right above it), or document that scope is normalized lazily at render time. Add a test with whitespace-padded scope entries asserting the stored/rendered form. | correctness | 30 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/registry/config.go:344 | AgentsByRole godoc warns that returned AgentConfig values' Scope slice and pointer fields alias the registry backing memory and mutating them corrupts the shared registry, but no test asserts this contract. Scope is a newly load-bearing Epic 2.2 reference field. | Add a test documenting the aliasing (mutate a returned config's Scope element and assert the registry changed) as a regression anchor, or have AgentsByRole deep-copy Scope and the pointers and assert non-aliasing. | testing | 30 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/registry/config_review_constraints_test.go:75 | TestRegistryLoad_EmptyScopeEntry tests "" but not a whitespace-only entry "   ", even though validation uses strings.TrimSpace(s)=="" specifically to catch whitespace-only entries. The TrimSpace branch is unverified — it could be removed and only the "" case would still fail. | Add a case with scope: ["performance", "   "] asserting a load error mentioning scope, locking the TrimSpace branch. | testing | 5 | code-review | claude | MEDIUM |
| U | [ ] | SEVERITY | FILE:LINE | PROBLEM | FIX | CATEGORY | 0 | SOURCE | REVIEWERS | CONFIDENCE |
| U | [ ] | LOW | internal/payload/scope.go:22 | ScopeFocus concatenates each scope string verbatim into the persona prompt via strings.Join with no escaping or length bound. Validation rejects blank/whitespace entries but not content or length — a pathological multi-KB scope entry bloats every agent prompt, and the trust assumption (scope is operator-controlled input) is undocumented at the injection site. | Document at scope.go that scope is trusted operator input and intentionally not escaped. Optionally bound each entry's length (e.g. reject > 64 chars at config.go scope validation) to prevent accidental prompt bloat. | security | 30 | code-review | claude | MEDIUM |
| U | [ ] | LOW | internal/payload/scope_focus_test.go:16 | TestScopeFocus only checks category substring presence; the soft-not-hard contract ("not a hard limit — still report any genuinely critical issue") is never asserted and could be deleted with green tests. The blank-skip assertion checks only for literal ", ," patterns, so a trailing blank entry would pass undetected. | Assert the exact rendered string for a known input to lock wording and join correctness; assert the "not a hard limit" clause is present in the output. | testing | 15 | code-review | claude | MEDIUM |

### [2026-06-14] From Review: llmclient OpenAI-compatible tool handling

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| U | [/] | LOW | internal/llmclient/chat.go:1 | No provider-conformance test matrix for the OpenAI-compatible surface. The client deliberately absorbs real wire divergence (string-encoded vs raw-object tool_call `arguments`, lenient finish_reason) but is exercised only against synthetic fixtures, so a regression against a specific provider's actual tool_call shape (OpenAI, litellm, Ollama, vLLM, Together) would not be caught. This is the robustness the official SDK is assumed to provide, achievable here without adopting it. | Add a recorded-fixture conformance suite: capture a real `tool_calls` response from each target provider and assert the parser (`ToolCallArguments`, `chatToolResponse` decode, finish_reason handling) yields identical engine-facing results. NOTE: scope is a few days, not a quick-win — consider promoting to a standalone test-remediation plan rather than resolving inline. | testing | 480 | review | claude | LOW |

### [2026-06-14] From Sprint: 3.0_adversarial_verification

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| 7 | [/] | LOW | internal/verify/pipeline.go:238 | The rich VerificationResult built in verifyFinding never populates TrippedBudgets (invokeSkeptic folds tripped budgets only into free-text Notes), base.Model is hard-coded to skeptics[0].Config.Model even when another skeptic produced the winning verdict, and the skip-already-verified rebuild (pipeline.go:199) re-synthesizes records from the compact on-disk block losing Model/DurationMs/TrippedBudgets — so verification.json's structured audit fields degrade or misattribute on multi-skeptic and no-op re-runs (extends the model-attribution gap of TD-011). Also: Model is empty on the no_eligible_skeptic / tool_harness_unavailable early-return paths. [intent: deferred per sprint-plan TD-011] | Thread the tripped-budget slice and the winning skeptic's model up from invokeSkeptic into base.TrippedBudgets/base.Model (join models when multiple voters agree, mirroring joinSkeptics), and carry Model/DurationMs/TrippedBudgets forward for skipped findings instead of synthesizing a lossy record. | correctness | 120 | execute-sprint | execute-sprint, claude | HIGH |

### [2026-06-13] From Sprint: 2.0_tool_using_reviewers

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| 5 | [/] | LOW | internal/tools/snapshot.go | AC 03-02 Scenario 5 and AC 03-03 Scenarios 4-5 require `manifest.json` `stages.review` to record `snapshot_mode` (live/worktree), `head_sha`, and `snapshot_worktree_path`. (intent_note: deferred per sprint-plan §2.5.A (manifest review-stage recording is Phase 5 work); Deferred to Epic Plan 2.1) | when wiring `SnapshotFor` into the agent loop, record `snapshot_mode`/`head_sha`/`snapshot_worktree_path` into `internal/payload/manifest.go` review stage and add the manifest assertion tests from AC 03-02/03-03. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
