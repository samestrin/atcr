# Technical Debt Tracking

This file is a staging area for small technical debt items discovered during development. Items are triaged and moved to individual sprint documents in `sprints/` as they are prioritized.

## Stats

| Severity | Open | Deferred | Resolved |
|----------|------|----------|----------|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 0 | 0 | 1 |
| MEDIUM | 1 | 3 | 19 |
| LOW | 1 | 0 | 53 |

**Last Modified:** 2026-06-13 | **Open Items:** 2 | **Deferred Items:** 3 | **Resolved Items:** 73 | **Total Items:** 78

## Directory Structure

```
technical-debt/
├── README.md                    # This file (staging area)
├── CLAUDE.md                    # AI assistant guidelines
└── sprints/
    ├── active/                  # Currently being addressed
    ├── pending/                 # Prioritized, not yet started
    └── completed/               # Resolved items
```

## How to Use

1. **Small items**: Add to this README under "Staging Area" below
2. **Larger items**: Create a new document in `sprints/pending/`
3. **During sprint planning**: Move items from pending to active
4. **After resolution**: Move items from active to completed


### [2026-06-13] From Sprint: epic-1.9

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [ ] | MEDIUM | internal/fanout/status.go:152 | ReadReviewStatus reports Partial=ps.Partial and Status=completed for a FailureMarker summary with Succeeded>0 and Failed=0, contradicting the reconcile path which now treats the same on-disk summary as partial | Apply the same FailureMarker && Succeeded>0 -> partial correction in ReadReviewStatus (or factor partial-derivation into one shared helper used by both readers); epic 1.9 explicitly scoped the status path out, so deferred | INTEGRATION | 30 | execute-epic-cumulative |
| U | [ ] | LOW | cmd/atcr/review.go:149 | The one-shot in-process reconcile threads result.Summary.Partial directly (not ReadManifestPartial), bypassing the marker correction; this is safe only because ExecuteReview returns (nil, err) on WritePool failure so the line is unreachable post-fault, but the invariant is implicit and undocumented | Add a comment noting a WritePool failure short-circuits before this reconcile, so the in-process path never needs the marker correction (the out-of-process reconcile via ReadManifestPartial is the only reachable reconcile of a write-aborted review) | REGRESSION_RISK | 10 | execute-epic-independent |

### [2026-06-13] From Sprint: 1.8_output-dir-support

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| Solo | [x] | HIGH | internal/payload/budget.go:104 | ApplyByteBudget sets Truncation.AllDropped when a non-empty input has every file shed by the byte budget, and the field doc (budget.go:20-23) states callers should surface a distinct error instead of forwarding an empty payload. No production code reads AllDropped — only budget_test.go references it. buildPayloads (internal/fanout/review.go:318-325) captures trunc into modePayload.Truncation but never checks trunc.AllDropped, then forwards b.String() (empty) to the reviewer, so a too-small --byte-budget silently produces zero findings — the exact failure the flag was meant to prevent. | In buildPayloads at internal/fanout/review.go:318, after ApplyByteBudget, check if trunc.AllDropped and return a distinct error (wrap a sentinel e.g. ErrPayloadFullyDropped) so a fully-shed payload fails loudly rather than emitting empty text. Add a fanout test with a byte budget below the smallest file asserting the error path. | error-handling | 60 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/fanout/reviewdir.go:226 | ScaffoldOutputDir performs no containment or destination validation. Unlike --id (gated by reviewIDRe to a single path component), --output-dir accepts any absolute path resolved at flag time (review.go:53-59), so the review tree — which includes payload diffs containing full source — can be written anywhere the process can write (/tmp, a sibling repo, a network mount). If atcr review is driven from a less-trusted MCP/automation surface this is an arbitrary-write/exfiltration primitive. The trust boundary is undocumented. | Decide and document the trust boundary in the flag help. If outside-repo writes are intended, state it explicitly and accept the risk; if not, add a filepath.Rel(repoRoot, dir) containment check rejecting escaping paths. Verify with a test passing --output-dir=/tmp/atcr-escape asserting the chosen behavior. | security | 60 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/fanout/reviewdir.go:232 | TOCTOU + symlink hole in ScaffoldOutputDir. os.ReadDir follows symlinks, so an --output-dir that is a symlink to an empty directory passes the emptiness gate, then os.MkdirAll (line 238) writes through the symlink into the target — the refuse-non-empty guard is bypassable. There is also a check/use window between os.ReadDir (232) and os.MkdirAll (238) where a concurrent process can populate the dir, defeating the no-clobber guarantee. | os.Lstat the path first and reject symlinks (mirroring init.go Lstat use), or filepath.EvalSymlinks then re-check. To narrow the TOCTOU window, attempt os.Mkdir on the leaf and treat ErrExist plus an emptiness re-check as the only acceptable existing state. Add a test creating a symlink-to-empty-dir target and asserting rejection. | error-handling | 60 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/fanout/reviewdir.go:202 | Deliberate behavioral divergence between the two scaffolders with no enforced rationale. ScaffoldReviewDir (line 202) is exclusive-create (a pre-existing dir is rejected to stop two fan-outs sharing one dir), while ScaffoldOutputDir (226) allows an empty pre-existing dir, skipping the atomic-claim protection. Two concurrent --output-dir runs targeting the same empty path both succeed and interleave writes — the exact failure claimReviewDir was rewritten to prevent. | Document the concurrency contract on ScaffoldOutputDir (caller guarantees unique paths; no atomic claim is performed) and/or use os.Mkdir on the leaf for exclusive-create semantics, accepting the empty-existing case via a follow-up emptiness check. Verify by running two ScaffoldOutputDir calls on the same empty path. | maintainability | 30 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/fanout/review.go:175 | An --output-dir pointed at an existing empty .atcr/reviews/<id>/ directory is silently accepted (empty-existing allowed), writing a full review tree into the managed reviews location, but WriteLatest is intentionally skipped (review.go:221) so the review is invisible to atcr status/ReadLatest yet physically colocated with tracked reviews. The manifest records a derived id that maps to no discoverable directory — a confusing half-state with no warning. | Warn or reject when the resolved --output-dir is inside ReviewsRoot(root), or document that colocation is intentional and unsupported by status. Verify with a test setting OutputDir to an empty .atcr/reviews/foo. | error-handling | 30 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/fanout/review.go:169 | The --output-dir path still calls ReviewID (line 169) and threads the result into the manifest and PreparedReview.ID though it is unused for path construction, and the three-way switch couples path strategy with id strategy (OutputDir ignores the id, claimReviewDir mutates it) requiring a maintainer to read all three arms plus the downstream if req.OutputDir==\"\" (221). Works, but fragile coupling. | Add a comment at line 169 noting the id is provenance-only in the OutputDir arm, or extract path-resolution into a helper returning (id, dir) so the OutputDir special-case lives in one place. No behavior change; existing tests should still pass. | maintainability | 15 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/fanout/reviewdir_test.go:36 | ScaffoldOutputDir tests cover absent/empty/non-empty/file-at-path but omit the two highest-risk cases: a symlink target and an --output-dir resolving outside the repo. The tests lock in the current permissive behavior, giving false confidence the security/symlink dimensions were considered. | Add tests for a symlink-to-empty-dir target and (once the trust boundary is decided) an escaping absolute path. Confirm they fail against current code. | testing | 30 | code-review | claude | MEDIUM |
| U | [x] | MEDIUM | internal/payload/budget.go:20 | Scope creep: the four peripheral files in this --output-dir merge (budget.go, outcome.go, ambiguous.go, registry/project.go) are unrelated TD/doc fixes (commit log labels them fix:/docs:/chore(td): resolve item 4) folded into a feature PR, defeating reviewability and clean revert isolation. The AllDropped field specifically is half-landed: the producer shipped with a prescriptive doc comment but the consumer-side guard did not, leaving unconsumed API surface. | Bundle TD-resolution work in its own PR separate from feature flags going forward. For AllDropped specifically, either complete the wiring (see the HIGH budget.go:104 item) or drop the field until a consumer exists — an unconsumed flag with a prescriptive doc comment is dead surface. | maintainability | 30 | code-review | claude | MEDIUM |
| U | [x] | LOW | cmd/atcr/review.go:54 | outputDirFromFlags trims whitespace only for the emptiness check (strings.TrimSpace(dir)==\"\") but passes the untrimmed dir to filepath.Abs. An input like \"  ./out\" passes the non-empty gate and resolves to a path containing leading-space components, producing a directory named differently than intended — the value validated diverges from the value used. | Resolve filepath.Abs(strings.TrimSpace(dir)) so the validated value is the value used. Add a unit test passing \"  out\" and asserting the resolved path has no leading-space component. | error-handling | 15 | code-review | claude | MEDIUM |
| U | [x] | LOW | internal/reconcile/ambiguous.go:165 | AmbiguousHash was changed from returning \"\" on a render error to panic(...). The change is defensible (a silent empty hash would poison BaselineHash) and the path is currently unreachable since AmbiguousCluster marshals without error, but it converts a library function into one that can crash the process and the panic contract has zero test coverage. | Acceptable as-is given unreachability. If keeping the panic, optionally add a test documenting the contract; otherwise no code change required. Noted mainly as an unrelated fix that rode into a feature PR. | error-handling | 5 | code-review | claude | MEDIUM |

### [2026-06-12] From Sprint: epic-1.8

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [x] | LOW | internal/fanout/reviewdir.go:232 | ScaffoldOutputDir does a ReadDir empty-check then a separate MkdirAll (a check/use window) unlike claimReviewDir/ScaffoldReviewDir which claim atomically via os.Mkdir | Use os.Mkdir-first for the leaf and only MkdirAll the parents to match the codebase's atomic-claim discipline | CORRECTNESS | 15 | execute-epic-independent |
| 1 | [x] | LOW | internal/fanout/review.go:174 | OutputDir vs IDOverride mutual exclusion is enforced only at the CLI; PrepareReview's switch silently lets OutputDir win when both are set instead of rejecting | Add a guard at the top of PrepareReview returning an error when both req.OutputDir and req.IDOverride are non-empty | EDGE_CASES | 15 | execute-epic-independent |
| 1 | [x] | LOW | internal/fanout/reviewdir.go:235 | A dangling symlink at the --output-dir path makes os.ReadDir return fs.ErrNotExist so MkdirAll then runs on the symlink path with platform-dependent behavior | Lstat the path first or document/test that --output-dir must not be a dangling symlink | EDGE_CASES | 30 | execute-epic-independent |
| U | [x] | LOW | cmd/atcr/review.go:133 | outputDirFromFlags trims whitespace only for the empty-check but passes the untrimmed value to filepath.Abs so a leading-space value resolves a path with literal spaces | Pass strings.TrimSpace(dir) into filepath.Abs so the resolved path matches the non-empty check | EDGE_CASES | 5 | execute-epic-independent |

### [2026-06-12] From Sprint: epic-1.7

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [x] | LOW | internal/reconcile/ambiguous.go:166 | AmbiguousHash silently returns "" on a JSON render error; the swallow would write an empty ambiguous_hash to summary.json with no diagnostic | Return the render error or log to stderr instead of emitting an empty hash | error-handling | 15 | execute-epic-independent |
| U | [x] | LOW | internal/fanout/outcome.go:52 | summarize switch has no default arm; an unexpected Status is counted as neither Succeeded nor Failed so Total can exceed Succeeded+Failed and Partial can misreport | Add a default arm classifying unknown Status as Failed (or panic on the impossible value) | maintainability | 15 | execute-epic-independent |
| U | [x] | LOW | internal/payload/budget.go:81 | ApplyByteBudget can drop every file when one entry exceeds the budget, yielding an empty-but-Truncated payload reviewers receive as nothing while the run still succeeds | Surface a distinct signal or error when truncation leaves zero kept files | edge-case | 20 | execute-epic-independent |
| U | [x] | MEDIUM | internal/payload/budget.go:0 | Default 512KiB byte budget yields ~155k-token payloads that exceed common model context/plan limits (deepseek-4 capped at 49k; qwen-3.7-plus 360s timeout); atcr doctor probes with a trivial nonce so it does not catch this and a real review then fails the whole pool | Document a budget/model-context sizing guideline (or warn when payload size likely exceeds roster context) so large ranges do not silently fail every agent | robustness | 30 | execute-epic-cumulative |

### [2026-06-12] From Sprint: 1.6_payload-git-pipeline

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [x] | LOW | internal/payload/diff.go:202 | numstatNewPath reconstructs a renamed head path by finding the FIRST { and FIRST } in the numstat field, but a parent directory name may legitimately contain a literal {. For a binary rename like a{x/{old.bin => new.bin}, it parses the brace in the directory name as the rename segment and returns a wrong key (e.g. anew.bin}) instead of a{x/new.bin. The reconstructed key never matches the --name-status head path, so binarySet misses the file and isBinary returns false, leaking raw binary bytes into the diff/files payload for that file. | Match git's rename grammar: locate the {...} segment that itself contains ' => ' (scan for ' => ' first, then expand to the surrounding braces) rather than the first {/}. Add a unit test with a literal { in a parent dir plus a binary rename asserting the head key equals the --name-status new path. | correctness | 30 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | internal/payload/diff.go:225 | chunkKey loops over every head path for every chunk doing HasSuffix, so splitDiffByFile is O(N^2) in changed-file count (N chunks x N heads), once per diff variant (fc, plain, raw, range = 4 passes). For a changeset of thousands of files this is needless quadratic work where a basename index or single longest-suffix lookup built once per range would be linear, undercutting the epic's goal of collapsing per-file cost. | Build the head-path lookup once per range (map keyed by basename, or parse the exact b/<path> token from the header for a single map lookup with longest-suffix fallback only on miss). Add a benchmark over N=2000 files showing sub-quadratic scaling and re-run the splitter tests for correctness. | performance | 60 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | internal/payload/diff.go:265 | When chunkKey returns empty (a chunk matches no known head path), splitDiffByFile only emits slog.Warn and drops the chunk. The matching file then renders with an empty body and no error surfaces to the caller, so a reviewer payload silently loses a file's diff. The 'record rather than drop silently' mitigation degrades to silent data loss in a CLI where a log line is invisible and the build still returns success. | Return an error from splitDiffByFile/diffChunks when a chunk is unattributable, or have fileBody detect a file present in changedFiles but absent from chunks and fail loudly. Verify by injecting a heads set missing one path and asserting the build errors instead of returning an empty body. | error-handling | 30 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | internal/payload/builder_test.go:236 | BuildDiff now routes through joinEntries(BuildEntries(...)) (builder.go:60), so it is the per-file entries join, NOT a verbatim whole-range diff. TestBuildEntries_PerFileBridge skips the diff-mode join==flat assertion with the comment 'BuildDiff is the verbatim whole-range diff', while TestBuild_EquivalentToJoinedEntries asserts the opposite for ModeDiff and passes. The stale comment/skip hides a now-meaningful equivalence check and misleads the next maintainer about BuildDiff's actual behavior. | Remove the 'if mode != ModeDiff' guard at builder_test.go:236-242 so ModeDiff is also asserted to join byte-identically (already true per TestBuild_EquivalentToJoinedEntries) and delete the stale comment. Verify the test still passes. | testing | 15 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | internal/payload/builder_test.go:352 | splitDiffByFile (diff.go:277) and fileBody (builder.go:164) log via the package-global slog.Default(), and TestFileBody_BlocksFallbackLeavesRecord swaps slog.SetDefault globally to capture output. Safe today (no t.Parallel), but parallelizing any payload test turns this into a data race / cross-test output-capture bug, and the record-keeping is untestable in isolation without global mutation. | Thread an injectable *slog.Logger (or a logger field on gitRunner) so the fallback/unattributed records can be captured per-test without touching global state. Verify by capturing the warning through the injected logger and removing the slog.SetDefault swap. | maintainability | 30 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | internal/payload/builder.go:135 | In ModeFiles a deleted file is special-cased first (builder.go:172) rendering [deleted file: ...], but in ModeDiff and ModeBlocks a deleted binary is not: it flows through isBinary, and since numstat prints a -/- record for a deleted binary it renders [binary file changed: <path>]. A deleted binary is thus labeled 'changed' rather than 'deleted', an inconsistent marker semantics across modes for the same file. [intent_note: changing sentinel/marker contracts is out-of-scope per epic 1.6 Out of Scope; prefer documenting the divergence over altering marker text] | If deleted-binary should read consistently, special-case kindDeleted ahead of the binary check in the diff/blocks arms too, or accept the divergence and document it. Verify with a test deleting a binary file and asserting the marker text per mode. | correctness | 15 | code-review | claude | MEDIUM |

### [2026-06-12] From Sprint: epic-1.6

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [x] | MEDIUM | internal/fanout/engine_test.go:345 | TestRun_MaxParallelBoundsPeakConcurrency is flaky under go test -count>1 and parallel package load (peak observed 1 instead of 2); reproduces on main, so it is pre-existing and outside this epic's scope (internal/payload) (Resolved 2026-06-12 — gate all five agents so the cap fills regardless of admission order; verified -race -count=20) | Make peak deterministic by asserting after both cap agents are confirmed in-flight via the concurrency counter rather than onStart, and reset fake peak/state per iteration | DETERMINISM | 30 | execute-epic-cumulative |
| U | [x] | LOW | internal/payload/diff.go:101 | ensureRange guards five whole-range caches by one shared cacheKey; a future cache accessor that forgets to call ensureRange first would serve stale cross-range data — a fragile invariant relying on every helper remembering the call | Centralize range reconciliation behind a single lazy keyed-access path so no cache can be read without first reconciling cacheKey | INTEGRATION | 30 | execute-epic-independent |

### [2026-06-12] From Sprint: 1.5_review-status-lifecycle

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 8 | [x] | MEDIUM | internal/fanout/status.go:122 | ReadReviewStatus only infers stale when errors.Is(serr, os.ErrNotExist). A non-not-exist read error on summary.json (permission denied, EIO, dir-where-file-expected) falls through and reports in_progress forever — the exact eternal-in_progress failure the epic claims to remove. A dead fan-out whose pool dir is corrupt/permission-broken never reaches a terminal state. | In ReadReviewStatus, treat a persistent non-ErrNotExist read error past the deadline like absent for stale inference, or surface it as an explicit error rather than masking as in_progress. Verify with a test that chmods the pool dir unreadable after the deadline and asserts a terminal/error result. | error-handling | 60 | code-review | claude | MEDIUM |
| 8 | [x] | MEDIUM | internal/fanout/artifacts.go:109 | writeFailureSummary writes PoolSummary{Total: roster, Failed: roster, Agents: nil}, fabricating an all-failed record. The real per-agent results (in memory at the ExecuteReview call site) are discarded, so a run where some agents produced valid findings before the WritePool I/O error is reported as a total failure with zero findings and empty agents list, discarding recoverable work and corrupting post-hoc analysis. | Pass the real summarize(results) tally (or the results slice) into the failure marker so Succeeded/Failed/Partial reflect what actually happened instead of hard-coding all-failed. Verify with a test where WritePool fails after a partial-success run and assert the marker preserves the succeeded count. | error-handling | 30 | code-review | claude | MEDIUM |
| 8 | [x] | LOW | internal/fanout/status.go:90 | Torn-read invariant comment asserts the manifest fields are byte-identical across the finalizing rewrite, but ExecuteReview rewrites the manifest with mutated Partial and a fresh CompletedAt, so the file is NOT byte-identical. The claim survives only because the status path happens not to read those two fields — a fragile coupling the comment papers over; a future edit reading Partial/CompletedAt would silently violate the guarantee while the comment still claims safety. | Reword the comment to the actual invariant: the fields consumed here (roster, StartedAt, timeout_secs) are stable across the rewrite; Partial and CompletedAt change but are not read here. Verify by grep that no status-path code reads m.Partial/m.CompletedAt. | maintainability | 15 | code-review | claude | MEDIUM |
| 8 | [x] | LOW | internal/fanout/artifacts.go:112 | writeFailureSummary swallows both the writeJSON and os.MkdirAll errors with no logging. When the best-effort marker fails AND the manifest has no timeout_secs (old manifests / TimeoutSecs<=0), stale never fires, so the review is stuck in_progress permanently and silently with zero operator signal — an undiagnosable hang. | Emit at least one stderr/log line on marker-write failure naming the review dir, so a permanently-in_progress review is traceable. Verify the log fires by stubbing writeJSON to fail in a test. | error-handling | 30 | code-review | claude | MEDIUM |
| 8 | [x] | LOW | internal/fanout/status.go:52 | nowFunc is a package-level mutable var with no synchronization. The documented concurrent-read invariant and -race torn-read test call staleByDeadline -> nowFunc() from 8 goroutines; the withNow helper reassigns nowFunc from the test goroutine. Today no test overlaps a swap with live readers so -race passes, but the design is one test away from a data race on the package var. | Document that nowFunc may only be swapped before reader goroutines start and restored after they join, or guard it behind atomic.Value. Verify the concurrent test never overlaps a swap with live readers. | maintainability | 15 | code-review | claude | MEDIUM |
| 8 | [x] | LOW | internal/fanout/status.go:48 | staleGraceSecs=60 is a hard-coded magic constant unanchored to the write cost it claims to absorb. atomicWriteFile does a temp Sync + dir Sync per file; for a large roster with slow fsyncs, finalization could exceed 60s and falsely report a still-finishing run as stale. The grace is a guess not tied to roster size or measured latency. intent_note: deferred per epic clarification Q2 — flat 60s adjudicated as sufficient | Document the bound (max roster x per-file fsync budget < 60s) with justification, or scale the grace with roster size. Verify by timing WritePool finalization for the largest supported roster and confirming it stays under the grace. | error-handling | 30 | code-review | claude | MEDIUM |
| 8 | [x] | LOW | internal/fanout/review.go:239 | writeFailureSummary(poolDir, len(p.Slots)) uses len(p.Slots) as roster count, but WritePool derives Total from summarize(results) over the actual results slice. If results count differs from slot count, the failure marker Total/Failed disagree with what a successful WritePool would record — a second independent source of truth for the agent count instead of reusing results already in hand. | Pass results (or summarize(results)) to the failure path so the marker totals come from the same source as the success path. Verify by asserting marker Total equals len(results) in a forced-failure test. | error-handling | 15 | code-review | claude | MEDIUM |

### [2026-06-12] From Sprint: 1.4_max-parallel-concurrency-cap

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 7 | [x] | LOW | internal/fanout/engine_test.go:306 | TestRun_MaxParallelBoundsPeakConcurrency, MaxParallelZeroIsUnbounded, and LargerThanRosterIsUnbounded use fakeCompleter.delay=30ms to create an overlap window rather than the block channel + onStart barrier that TestRun_SerialAndParallelLanesRunConcurrently already uses. In principle a pathological scheduler could let agents complete before peak registers, producing a false-low flake. In practice the 30ms delay vs sub-millisecond goroutine spawn makes this robust (60+ iterations under GOMAXPROCS 1 and 2 with zero flakes), and mutation tests confirm the exact-equality assertion catches both a neutered semaphore and an off-by-one. | For the cap-bounding test specifically, gate the first N agents on a block channel (as the serial/parallel overlap test does) so peak deterministically reaches the cap before any agent is released, then close the gates — removing the timing dependence entirely. The unbounded variants are lower priority. | testing | 30 | code-review | claude | MEDIUM |
| 7 | [x] | LOW | internal/fanout/engine_test.go:345 | TestRun_MaxParallelDrainsUnderCancellation and TestRun_GlobalTimeoutCancelsAndDrains comment about "WaitGroup leaked"/"no leak", but the only leak guard is a 2s select timeout — they assert Run returns, not that no goroutine survives it. No goleak/runtime.NumGoroutine check exists in the repo. This is acceptable because Run uses wg.Wait() with defer wg.Done() on every goroutine (a goroutine cannot outlive Run by construction), so the comments are technically accurate — but the test does not independently verify that invariant. | Optionally add go.uber.org/goleak with defer goleak.VerifyNone(t) (or a NumGoroutine delta check) to one drain test so a future refactor that detaches a goroutine from the WaitGroup is caught directly rather than only when it manifests as a blocked Run. | testing | 30 | code-review | claude | MEDIUM |
| 7 | [x] | LOW | internal/fanout/engine_test.go:108 | TestRun_SerialLaneRunsSequentially asserts peak==1, but serial slots run in a single goroutine sequentially regardless of the semaphore, so it would pass even if max_parallel were entirely broken. The test claim is sound for the lane architecture, but it provides no coverage of the cap — a reader could mistake it for cap coverage. Labeling/scope-clarity point, not a correctness defect (the cap is covered by the MaxParallel tests). | Add a one-line comment clarifying it tests lane serialization (not the semaphore) to prevent future confusion. No behavioral change required. | maintainability | 5 | code-review | claude | MEDIUM |
| U | [x] | MEDIUM | internal/payload/manifest.go:38 | The manifest field MaxParallel is tagged json:"max_parallel,omitempty". The field's documented purpose (manifest.go:34-37) is post-hoc diagnosis — "a throttled run can be identified by max_parallel in the manifest." But 0 is the unbounded escape hatch, and omitempty drops zero values, so an explicitly-unbounded run (max_parallel: 0) is serialized identically to a pre-1.4 manifest that never had the field. Post-hoc diagnosis cannot distinguish "explicitly unbounded" from "field absent" — the exact ambiguity the field was added to remove. Contrast TimeoutSecs, which cannot legally be 0 (validated 1..max), so omitempty is harmless there. | Drop omitempty from the MaxParallel json tag in internal/payload/manifest.go:38 so the effective cap (including 0) is always serialized; or change it to *int if older manifests must stay parse-clean while still recording an explicit 0. Verify with a manifest round-trip test asserting max_parallel:0 survives serialization distinctly from absent. | correctness | 15 | code-review | claude | MEDIUM |
| U | [x] | LOW | internal/registry/precedence.go:115 | applyTier writes proj.MaxParallel/reg.MaxParallel into Settings with zero re-validation, relying entirely on LoadProjectConfig (project.go:145) and the registry validate (config.go:145) having rejected negatives at load. That invariant holds for the only production callers, but it is an implicit, undocumented coupling: a future caller that constructs a *ProjectConfig/*Registry directly (tests, an MCP path, a programmatic embed) and skips the loader can drive a negative past applyTier into the engine, where WithMaxParallel(n<=0) silently means UNBOUNDED — the inverse of a tight cap. The CLI tier defends itself (precedence.go:84) precisely because it bypasses load checks; the file tiers have the same theoretical exposure but no guard. | Add a single post-resolution sanity check on s.MaxParallel in ResolveSettings (return a usage error if < 0), or document on ResolveSettings that proj/reg MUST be loader-validated. Cheap insurance against a value that maps to the inverse of its intent. | error-handling | 15 | code-review | claude | MEDIUM |
| U | [x] | LOW | internal/registry/precedence.go:16 | Every user-facing surface advertises "0 = unbounded" (CLI help review.go:30, YAML template comment project.go:64, all three validation error strings), but the embedded default is 10, so a user who never sets the flag silently gets a cap of 10 — not the unbounded behavior v1 had. The DefaultMaxParallel comment justifies this as preserving effective v1 behavior, which is a deliberate, defensible decision, but nothing in the user-facing copy hints that the out-of-box default is a cap rather than the unbounded escape hatch the docs keep emphasizing. Documentation/expectation mismatch, not a logic bug. | Add one line to the YAML template comment (project.go:64) and/or CLI help (review.go:30) noting the default is 10 (a cap), and that 0 must be set explicitly for unbounded — aligning the "0 = unbounded" messaging with the fact that unset != unbounded. | maintainability | 5 | code-review | claude | MEDIUM |

### [2026-06-12] From Sprint: 1.3_project_registry_overlay

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 5 | [x] | MEDIUM | internal/doctor/run.go:245 | classify()'s network_error branch surfaces bounded(err.Error()) directly into AgentResult.Detail with no key scrubbing. HTTPStatusError.Snippet is redacted upstream (llmclient client.go:259 ReplaceAll(key,[redacted])), but a transport-level error (DNS/TLS/proxy/redirect) is wrapped as 'request failed: %w' and never passes through that scrub. The comment asserts 'never the API key' as an assumption about net/http error strings, not an enforced invariant — a misconfigured proxy URL with embedded user:pass@ credentials could leak into the report/JSON. | Apply the same redaction discipline as client.go:259: scrub the resolved key (and any embedded userinfo) out of transport errors at the llmclient boundary so every error returned to doctor is pre-sanitized, or pass the key into classify and ReplaceAll it out of the network-error detail. Do not rely on a comment as the control. Add a test with a synthetic error containing a secret asserting it is redacted. | security | 60 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | internal/doctor/render.go:40 | RenderTableError re-implements the source default ('if source == "" { source = "user" }'), duplicating logic that registry.AgentTier (overlay.go:153-158) already owns and that Resolve applies at resolve.go:91. For any report from the real pipeline AgentTier guarantees a non-empty tier, so this branch is dead except for hand-built test values, and the literal 'user' duplicates the registry.SourceUser constant — if that constant's value ever changes the table renderer silently diverges. | Drop the defaulting from the renderer and let Source flow through verbatim (the resolver guarantees it). If a presentation-boundary default is genuinely wanted, reference registry.SourceUser rather than a literal and document why. | maintainability | 15 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | internal/doctor/run.go:250 | bounded() truncates a detail string by raw byte count ('s[:maxDetailBytes]'). The input originates from arbitrary transport/error text, so for multi-byte UTF-8 (non-ASCII or IDN provider hostnames, localized OS errors) slicing at byte 512 can split a rune mid-sequence, producing an invalid-UTF-8 Detail that then flows into json.Encoder.Encode (render.go:22) and to any strict-UTF-8 downstream consumer. | Truncate on a rune boundary: back off to the previous valid boundary with utf8.DecodeLastRuneInString, or cap using a rune-aware slice. Mirror the whole-rune treatment the HTTPStatusError.Snippet path already uses. | correctness | 15 | code-review | claude | MEDIUM |
| 6 | [x] | MEDIUM | internal/registry/overlay.go:39 | ProjectRegistry declares only Providers and Agents and is parsed with the strict KnownFields decoder, so a contributor who writes a shared-settings key (timeout_secs, max_parallel, payload_mode) into .atcr/registry.yaml — expecting a project-tier override — gets a hard 'failed to parse .atcr/registry.yaml' error that does not say why a valid-looking key is rejected. The settings-vs-definitions split between registry.yaml and config.yaml is correct, but the strict decoder turns a common onboarding mistake into a cryptic failure. | In LoadProjectRegistry's decode-error path (overlay.go:85-91), detect a YAML unknown-field error naming a known shared-settings key and surface a targeted message: 'shared settings belong in .atcr/config.yaml, not .atcr/registry.yaml'. Add a test feeding a project registry with timeout_secs: and assert the helpful message. Verify the existing strict-parse tests still pass. | error-handling | 30 | code-review | claude | MEDIUM |
| 6 | [x] | MEDIUM | internal/registry/config.go:152 | validate() returns plain errors.New for an empty/whitespace provider name (config.go:153) and agent name (config.go:173). Over the merged view, attribute() (attribution.go:48-55) frames every non-entryError as 'registry.yaml: ...' (the USER file). So a .atcr/registry.yaml overlay with an empty provider key produces an error blaming the user registry, misdirecting the contributor to the wrong file. This breaks AC #3's contract that load errors name the defining file. | Change the empty-name checks at config.go:153 and config.go:173 to return providerErrf/agentErrf (entryError) so attribute() resolves the source tier from ProviderSource/AgentSource. Add a regression test: a project registry with an empty provider name must produce an error naming '.atcr/registry.yaml'. Consider asserting every entry-scoped branch in validate() returns an entryError so future fields cannot silently misattribute. | correctness | 30 | code-review | claude | MEDIUM |
| 6 | [x] | MEDIUM | internal/registry/trust.go:80 | LoadTrustStore indexes only e.Hash into the membership set and never recomputes providerTrustHash from each entry's stored base_url+api_key_env. The struct comment (trust.go:34-35) claims base_url/api_key_env are kept 'for human audit', but nothing enforces that the recorded hash matches those columns — so a hand-edited or restored trust file can display endpoint B in the audit columns while the authoritative hash pins endpoint A. The audit columns are decorative, weakening the file's reviewability (the user's own trust store is the trust anchor). | On load, recompute providerTrustHash from each entry's base_url+api_key_env and reject (or skip with a warning) any entry whose recorded hash disagrees, closing the audit-vs-authority gap the comment advertises. Add a test with a tampered entry (hash for A, columns for B) asserting it is rejected/ignored. | security | 30 | code-review | claude | MEDIUM |
| 6 | [x] | MEDIUM | internal/registry/trust.go:196 | The (project-registry path, trust-store path) pair is resolved independently in two places: the gate enforceProjectTrust uses DefaultTrustStorePath(filepath.Dir(regPath)) wired from overlay.go:124, while the `atcr trust` command rebuilds the same pair in cmd/atcr/trust.go:43-56. They agree only because both hardcode project root '.' and the user-registry dir. The coupling is implicit and undocumented: if review/doctor ever gain a --root/-C flag (they pass '.' literally today), `atcr trust` has no matching flag and would authorize providers for a different project than the gate checks. | Extract one helper, e.g. resolveTrustContext(root) (projectRegPath, storePath, err), called by both enforceProjectTrust and runTrust so the pairing cannot drift. At minimum, rename enforceProjectTrust's param to userRegDir and document that the store must live beside the USER registry, never under the repo root. Add a --root flag to `atcr trust` mirroring review/doctor if/when those gain one. | correctness | 30 | code-review | claude | MEDIUM |
| 6 | [x] | LOW | internal/registry/overlay.go:30 | EntrySource.Tier's doc comment lists 'SourceUser / SourceProject / SourceEmbedded' but no SourceEmbedded constant exists — the const block (overlay.go:13-16) defines only SourceUser/SourceProject and the block comment states there are no embedded provider/agent defaults. The comment is self-contradictory within the same file and sends a reader hunting for a non-existent tier. | Delete 'SourceEmbedded' from the comment at overlay.go:30. Verify by grepping the package for SourceEmbedded and confirming zero definitions. | maintainability | 5 | code-review | claude | MEDIUM |
| 6 | [x] | LOW | internal/registry/config.go:161 | The base_url check 'if err != nil || u.Scheme != http && u.Scheme != https || u.Host == ""' relies on Go's &&-binds-tighter-than-|| precedence. It is correct today, but an unparenthesized mixed boolean chain in a security-relevant URL validation is exactly where a future edit silently introduces a bug. The check also accepts surprising url.Parse outputs: a non-empty u.Opaque or '..' path segments pass, since only scheme/host/userinfo are constrained. | Parenthesize the scheme clause: '... || (u.Scheme != http && u.Scheme != https) || ...'. Optionally reject non-empty u.Opaque for defense in depth. Verify existing URL-validation tests pass. | security | 15 | code-review | claude | MEDIUM |
| 6 | [x] | LOW | internal/registry/graph.go:84 | In walkFallbacks, the gray-node branch trims the cycle via the for-loop at graph.go:88-92, then has a fallthrough 'return path, true' at line 93 commented as a fail-closed guard 'if that invariant ever breaks'. But because next is appended to path the moment it is colored gray, the loop is guaranteed to find it, making line 93 unreachable. Worse, if a true invariant break ever occurred (e.g. a foreign gray from a shared color map), it would return a WRONG untrimmed path silently — failing open on the error message, not closed. The reassuring comment is misleading. | Either delete the unreachable branch and let a genuine invariant violation panic loudly, or rewrite the comment to state it is provably dead because ValidateFallbacks only walks white roots so no foreign gray can occur. Do not leave a 'fail closed' comment over a silent fallthrough. | correctness | 15 | code-review | claude | MEDIUM |
| 6 | [x] | LOW | internal/registry/trust.go:110 | Save()'s doc comment claims the atomic temp+rename means 'a crash or a concurrent atcr trust can never truncate the file into a corrupt state' — true for corruption, but it overstates concurrency safety. LoadTrustStore -> Trust -> Save is a read-modify-write with no lock; two concurrent `atcr trust` runs each load the old store, add their own entry, and the second Rename clobbers the first's grant. The rename prevents a corrupt file, not a lost update. | Narrow the comment to the truncation/corruption guarantee it actually provides, OR add an advisory lock (flock on a sibling .lock) around load+save if concurrent trust grants are a real scenario. Given CLI one-shot usage, fixing the comment is likely sufficient. | maintainability | 15 | code-review | claude | MEDIUM |
| 6 | [x] | LOW | internal/registry/trust.go:183 | The 'name -> base_url=... api_key_env=...' provider line is formatted three times with inconsistent glyphs: untrustedProviderError uses '->' (trust.go:183), ProjectProviderBanner uses '->' (trust.go:229), and the trust command uses '→' (cmd/atcr/trust.go:84), with listProjectProviders rendering a fourth tabular variant. A reader cannot tell whether the -> vs → difference is intentional, and any change to how a provider is identified must be edited in several places. | Add one shared formatter, e.g. func (r ProviderRef) Line() string, and call it from the gate error, the banner, and the trust command; pick a single arrow glyph. Verify output snapshots/tests update accordingly. | maintainability | 30 | code-review | claude | MEDIUM |
| U | [x] | MEDIUM | cmd/atcr/trust.go:68 | `atcr trust --all <name>` silently overwrites want with sortedProviderNames(pr) (trust.go:70-72), discarding positional args — a user who types a specific name with --all believes they trusted that one provider when they trusted all. Separately, when args name multiple providers and a later name is unknown, the loop prints 'trusting X' for the earlier names and mutates the in-memory store, then returns the usageError at trust.go:77 BEFORE Save() — so stdout claims writes that never persist (the mutations are discarded). | Add a guard: if all && len(args)>0 return usageError('--all takes no provider arguments'). Validate the entire want set against pr.Providers in one pass up front, before printing any 'trusting' line or mutating the store, so output never claims a write that gets rolled back. Add tests for both cases. | error-handling | 15 | code-review | claude | MEDIUM |
| U | [x] | LOW | cmd/atcr/review.go:64 | The trust banner from cfg.Registry.ProjectProviderBanner() is printed right after LoadReviewConfig and says 'These endpoints will receive the named API keys'. The ordering is security-safe (the gate inside LoadMergedRegistry has passed and the first provider call is ExecuteReview at review.go:98), but the banner prints before PrepareReview/preflightAPIKeys, which can abort the run (exit 2) AFTER the banner showed — overstating what happened on the abort path. It also lists ALL project-tier providers, including ones no roster agent references, over-reporting the blast radius. | Soften the wording to 'are authorized to receive' / 'would receive', or move banner emission to just before ExecuteReview (the actual point keys flow). Optionally filter the banner to providers actually referenced by the resolved roster. The same property applies to doctor.go:62. | maintainability | 15 | code-review | claude | MEDIUM |

### [2026-06-12] From Sprint: 1.2_model_endpoint_selftest

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 4 | [x] | LOW | internal/doctor/run.go:208 | classify() verifies success with strings.Contains(content, Marker(nonce)). Because the prompt literally is "Reply with exactly: ATCR-OK-<nonce>", any endpoint that echoes the request prompt back verbatim (a common misconfig / wrong route returning the request) contains the marker and is falsely classified StatusOK. Content-based verification is defeated by prompt echo. Impact is low: doctor is a pre-flight aid, not a security gate. | Tighten the success check so the marker must appear OUTSIDE the prompt text — e.g. strip the known Prompt(nonce) substring from content before the Contains check, or require the marker as a standalone/trailing token rather than anywhere in the body. Add a test where the fake returns Prompt(nonce) verbatim and assert it does NOT classify as ok. | correctness | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/doctor/run_test.go:60 | The reasoning/thinking-model acceptance criterion (AC3) states the default token budget must be large enough that the marker appears, "verified against at least one reasoning-style mock." But fakeCompleter ignores inv.MaxTokens entirely and returns the marker (or empty) unconditionally — TestRun_MarkerAbsentIsWarning uses MaxTokens:8 yet the empty result is hard-coded, not a consequence of the small budget. No test conditions output on the token budget, so the AC's verification intent is only superficially met. | Add a fake whose fn inspects inv.MaxTokens: return Marker(nonce) only when *inv.MaxTokens >= a threshold (simulating reasoning tokens consuming the budget) and empty content below it. Assert default 2048 yields StatusOK while a tiny budget yields StatusOKWarning, faithfully exercising the thinking-model path. | testing | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/doctor/run.go:131 | exitVerdict() returns 0 when res.Paths is empty, so an effective roster that resolves to zero directly-listed agents (empty agents + serial_agents) produces an empty table and a passing exit 0 — "everything is healthy" when in fact nothing was tested. A user who mis-edits config to an empty roster gets a false green from doctor. | Treat an empty resolved roster as a usage/config condition: in runDoctor (or Run), detect len(res.Agents)==0 and emit a clear "no agents configured to test" message with a non-zero (usage, exit 2) result instead of a silent exit 0. Add a test asserting the empty-roster case does not report success. | correctness | 20 | code-review | claude | MEDIUM |
| U | [x] | LOW | cmd/atcr/doctor.go:96 | runDoctor renders the human table with doctor.RenderTable(), the variant that discards the tabwriter flush error, even though render.go deliberately exposes RenderTableError() for callers that need to detect truncated/short writes. On a broken pipe (e.g. `atcr doctor | head`) a partial table is emitted with no error signal, and the command can still exit 0, masking incomplete output. | Switch the CLI to RenderTableError() and propagate a non-nil flush error from runDoctor (or at least log it to stderr) so truncated table output is observable rather than silently swallowed. | 0 | 15 | code-review | claude |

### [2026-06-12] From Sprint: 1.1_schema_reservations

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 3 | [x] | low | internal/registry/config.go:197 | MaxTurns has a lower bound (>0) but NO upper bound, unlike TimeoutSecs which is capped at MaxTimeoutSecs (config.go:184). A reserved config with max_turns: 2000000000 loads cleanly, so the range-validation the epic promised is only half-enforced for this field. When Epic 2.0 activates the tool loop an unbounded turn cap becomes a runaway-cost/DoS vector that should have been rejected at load. | Add an upper-bound check mirroring MaxTimeoutSecs, e.g. a MaxAgentTurns const, and assert max_turns > MaxAgentTurns fails the load. Verify by adding a registry_test case with an absurd max_turns expecting a validation error. | correctness | 15 | code-review | claude | MEDIUM |
| 3 | [x] | low | internal/registry/config.go:200 | ToolBudgetBytes validation accepts 0 but the field comment ('cumulative tool-result budget') never defines what 0 means. PayloadByteBudget (config.go:142-143) documents 0=unlimited inline; this reserved field copies the >=0 check without the semantic note, so Epic 2.0 inherits an ambiguous sentinel (0 = block all tools vs 0 = unlimited) baked into accepted configs. | Add an inline comment on ToolBudgetBytes (config.go:66) defining the 0 sentinel to match PayloadByteBudget's convention, or reject 0 if it is meaningless. Verify the doc/registry.md reserved-fields table states the 0 semantics. | maintainability | 15 | code-review | claude | MEDIUM |
| 3 | [x] | low | internal/registry/config.go:64 | Tools is a value bool while MaxTurns/ToolBudgetBytes are pointers 'so the activating stage can tell an explicit value from an unset one' (struct doc config.go:49-50). Tools cannot make that distinction: tools: false is indistinguishable from absent. The stated unset-vs-explicit rationale is applied inconsistently across the four reserved fields. Harmless in 1.x (planned default is false) but the comment over-promises a capability Tools does not have. | Either make Tools a *bool for consistency with the pointer rationale, or amend the struct comment (config.go:49-52) to state Tools intentionally does NOT distinguish unset from false because its planned default is false. Verify the comment matches the field types. | maintainability | 15 | code-review | claude | MEDIUM |
| U | [x] | low | internal/reconcile/emit.go:33 | The Verification doc comment says 'The loader (reconcile/load.go) does NOT validate the enum' but no reconcile/load.go file exists; findings.json is read at emit.go:155 via plain json.Unmarshal. The cited path is wrong, so a future maintainer looking for the read-side validation hook will not find it where the contract points. | Update the comment at emit.go:33 to reference the actual reader (emit.go LoadFindings / json.Unmarshal at line 155) instead of the nonexistent reconcile/load.go. Verify with grep that the referenced symbol/path exists. | maintainability | 5 | code-review | claude | MEDIUM |
| U | [x] | low | internal/reconcile/emit.go:37 | Verification.Verdict has no omitempty and no load-time enum validation; a present-but-malformed block (verdict:"") round-trips silently. The comment pushes enum validation entirely onto the Epic 3.0 writer, but the epic contract says reserved fields must 'type/range-validate'. A tolerant reader that trusts verdict will mishandle an empty/unknown value with no guard at the schema boundary. | When Epic 3.0 lands, add read-side enum validation (confirmed/refuted/unverifiable) in the findings loader, not only the writer. For now, document in docs/findings-format.md that 1.x readers must treat an unknown/empty verdict as 'unverified' rather than trusting it. Verify a findings.json with verification.verdict:"" still loads and is handled defensively. | correctness | 15 | code-review | claude | MEDIUM |
| U | [x] | low | internal/payload/manifest.go:32 | Stages uses json:"stages,omitempty". omitempty drops a nil OR empty slice, so a manifest constructed without Stages (any future writer that forgets it, or a hand-assembled manifest) silently omits the field the epic says 1.x 'must carry'. The 1.x guarantee rests entirely on PrepareReview (review.go:200) remembering to set it; the schema does not enforce presence. Tolerant readers must therefore treat absent stages as implicitly ["review"], which is undocumented. | Document in docs/findings-format.md that readers MUST default absent stages to ["review"] for 1.x manifests, OR drop omitempty so the contract field is structurally always present. Verify the manifest-reader behavior for an absent stages key. | correctness | 15 | code-review | claude | MEDIUM |

### [2026-06-12] From Sprint: epic-1.5

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [x] | MEDIUM | internal/fanout/artifacts.go:109 | writeFailureSummary marks all agents failed even when an earlier agent already persisted findings (Deferred: Epic Plan 1.9) (Resolved: Epic Plan 1.9 — 2026-06-13; added PoolSummary.FailureMarker set only by writeFailureSummary, and ReadManifestPartial now forces partial when FailureMarker && Succeeded>0 so reconcile cannot emit a non-partial verdict from a write-aborted subset) during a mid-loop I/O fault, so reconcile can emit a non-partial verdict from the surviving subset that contradicts the failed marker | Have reconcile cross-check the failure-marker summary (or add a distinguishing marker field) so a partial-but-failed review is rejected rather than reconciled | INTEGRATION | 60 | execute-epic-independent |
| 1 | [x] | MEDIUM | internal/fanout/artifacts.go:109 | writeFailureSummary swallows MkdirAll/writeJSON errors with no log; when the marker write also fails there is no operator signal for the in_progress-to-stale gap (the primary WritePool error IS already logged by the ExecuteReview caller) | Thread a best-effort logger into the fanout failure path so the secondary marker-write failure is diagnosable, or accept the primary-error log as sufficient | ERROR_PATHS | 30 | execute-epic-independent |
| 1 | [x] | LOW | internal/fanout/review.go:239 | On the WritePool failure branch ExecuteReview writes the marker but never stamps manifest CompletedAt/Partial, so a failed-marked review's manifest is indistinguishable on disk from an unfinished scaffold for duration/partial-deriving tools | Stamp CompletedAt (and an explicit failed indicator) in the failure branch, or document the intentional absence | OBSERVABILITY | 20 | execute-epic-independent |
| 1 | [x] | LOW | internal/fanout/status.go:156 | staleByDeadline uses int arithmetic for (timeout_secs+grace)*1e9 ns; a pathological timeout_secs near math.MaxInt64/1e9 overflows Duration negative, yielding a false-positive stale (practically unreachable: needs a ~292-year timeout) | Bounds-check timeout_secs or compute the deadline with a saturating guard | EDGE_CASES | 15 | execute-epic-independent |
| 1 | [x] | LOW | internal/fanout/status.go:122 | report/reconcile route a stale review through EnsureReviewComplete and surface re-run guidance instead of poll guidance; intended per the epic (stale is terminal) but a slow-but-alive run past timeout+grace is told to re-run | Confirmed terminal-by-design; revisit only if grace-margin false positives are observed in practice | REGRESSION_RISK | 15 | execute-epic-independent |
| 1 | [x] | LOW | internal/fanout/status.go:52 | nowFunc is a mutable unsynchronized package var; a future parallel test swapping it while another goroutine calls ReadReviewStatus would data-race (current stale-clock tests are non-parallel so it does not trip) | Document that nowFunc must not be swapped concurrently, or inject the clock via a struct field/parameter | UNDER_ENGINEERING | 20 | execute-epic-independent |

### [2026-06-11] From Sprint: 1.0_atcr_core

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 2 | [/] | MEDIUM | internal/fanout/engine.go:88 | Every non-serial slot spawns its own goroutine with no semaphore or worker-pool bound (intent_note: deferred per sprint-plan TD-017) (Deferred: Epic Plan 1.4) | bound the parallel lane with a buffered semaphore channel sized from a new max_parallel setting | performance | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [/] | MEDIUM | internal/fanout/status.go:48 | ReadReviewStatus reads manifest.json then sources/pool/summary.json in two separate steps while the background fan-out (started by the atcr_review handler) is writing both (intent_note: deferred per sprint-plan TD-023) (Deferred: Epic Plan 1.5) | write a single status.json snapshot at fan-out completion (or a completion sentinel) so status is read from one atomic file instead of two | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [/] | MEDIUM | internal/fanout/status.go:48 | There is no terminal state for dead reviews: ReadReviewStatus treats absent summary.json as in_progress unconditionally. If ExecuteReview fails after fan-out (WritePool/WriteManifest I/O error returns nil summary, MCP background path only logs to stderr), or the MCP server dies / is killed past the 5s drain while a fan-out may run 600s, summary.json never appears and atcr status reports in_progress forever. The Skill polling loop cannot distinguish a running review from a dead one; .atcr/latest already points at the orphan, poisoning default-anchor calls. (Deferred: Epic Plan 1.5) | On post-fan-out persistence failure write a best-effort failure marker the status reader understands, and in ReadReviewStatus compare manifest StartedAt plus the global timeout (persist effective timeout in the manifest) against now to report a distinct stale/abandoned status. Verify with tests injecting a WritePool failure and a stale manifest with no summary.json. | integration | 60 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | internal/payload/builder.go:114 | blocks/files modes invoke up to 4-5 git processes per changed file (numstat, function-context, context, show, unified=0) (intent_note: deferred per sprint-plan TD-011) (Resolved: Epic Plan 1.6 — batched classification + split whole-range diff, blocks/diff now O(1) git processes) | batch classification (`--numstat`/`--name-status` once) and split a single diff per file | performance | 0 | execute-sprint | execute-sprint | MEDIUM |
| 9 | [x] | MEDIUM | skill/SKILL.md:33 | [Story 05] DoD item not verified: "Orchestration loop verified end-to-end with a real review run" (AC 05-03 manual gate). The range → review → poll → host review → reconcile → report sequence is verified only by static SKILL.md section tests; no real review run artifacts exist in the repo. (Resolved: Epic Plan 1.7 — 2026-06-12; review dir .atcr/reviews/2026-06-12_epic-1.7-realrun-local/, evidence .planning/epics/code-reviews/1.7_real-review-run-verification/run-evidence.md) | Run the full Skill orchestration loop against a real branch with a configured provider and record the review directory path and outcome. | incomplete | 30 | code-review | claude | MEDIUM |
| 9 | [x] | MEDIUM | skill/SKILL.md:61 | [Story 05] DoD item not verified: "Adversarial tone of host review verified in a real review run" (AC 05-04 manual gate). The adversarial personality clause exists in SKILL.md:61-63 and is statically tested, but no real host review output exists to confirm the tone lands in practice. (Resolved: Epic Plan 1.7 — 2026-06-12; independent tone inspection PASS, evidence .planning/epics/code-reviews/1.7_real-review-run-verification/run-evidence.md) | During the real-run verification, inspect sources/host/review.md for adversarial tone and absence of praise, per the AC. | incomplete | 15 | code-review | claude | MEDIUM |
| 9 | [x] | MEDIUM | skill/SKILL.md:96 | [Story 05] DoD item not verified: "Ambiguity adjudication produces sensible merge/distinct decisions" (AC 05-04 manual gate). The adjudication mechanics (stable cluster ids, adjudication.json, idempotent re-runs) are unit-tested, but no real review run with gray-zone clusters has exercised host adjudication quality. (Resolved: Epic Plan 1.7 — 2026-06-12; seeded-corpus fallback through real reconcile, merge+distinct + idempotent, evidence .planning/epics/code-reviews/1.7_real-review-run-verification/run-evidence.md) | During a real review run that yields ambiguous.json clusters, perform host adjudication per SKILL.md:96-113 and review the merge/distinct decisions for sensibility. | incomplete | 30 | code-review | claude | MEDIUM |

