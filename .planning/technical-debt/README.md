# Technical Debt Tracking

This file is a staging area for small technical debt items discovered during development. Items are triaged and moved to individual sprint documents in `sprints/` as they are prioritized.

## Stats

| Severity | Open | Deferred | Resolved |
|----------|------|----------|----------|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 0 | 2 | 7 |
| MEDIUM | 14 | 24 | 50 |
| LOW | 17 | 22 | 67 |

**Last Modified:** 2026-06-24 | **Open Items:** 31 | **Deferred Items:** 48 | **Resolved Items:** 124 | **Total Items:** 203

## Directory Structure

```
technical-debt/
├── README.md                    # This file (staging area)
├── CLAUDE.md                    # AI assistant guidelines
└── sprints/
    ├── active/                  # Currently being addressed
    ├── pending/                 # Prioritized, not yet started
    └── completed/               # Resolved items
```

## How to Use

1. **Small items**: Add to this README under "Staging Area" below
2. **Larger items**: Create a new document in `sprints/pending/`
3. **During sprint planning**: Move items from pending to active
4. **After resolution**: Move items from active to completed



### [2026-06-24] From Sprint: 9.0_persona_ecosystem

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [x] | MEDIUM | cmd/atcr/personas.go | The `test` subcommand's default runner (`noFixtureRunner`) always reports `HasFixture: false`, so in production `atcr personas test <name>` prints "No fixture defined" for every persona [deferred per sprint-plan TD-012 (production fixture runner out of phase scope)] | Implemented TemplateFixtureRunner in internal/personas/test.go: calls builtins.Fixture(name) + builtins.Get(name), renders via payload.RenderPrompt with a minimal PayloadContext, asserts no unresolved `{{` markers. Set as the personasFixtureRunner default in cmd/atcr/personas.go. Added payload to personas allowlist in boundaries_test.go. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| 1 | [x] | MEDIUM | cmd/atcr/personas.go | Both T6 and T8 carry a per-reviewer corroboration rate in a `map[string]float64`, but key it differently: T6 lowercases the reviewer name (`strings.ToLower(row.Reviewer)`), while T8 looks up by the raw registry agent name (case-sensitive) [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired)] | In SelectEligibleSkeptics (internal/verify/select.go), normalize score-map lookup key to lowercase: replace scores[matched[i]] / scores[matched[j]] with scores[strings.ToLower(matched[i])] / scores[strings.ToLower(matched[j])], matching the lowercase-keyed map that reviewerCorroborationRates already produces. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| 1 | [x] | MEDIUM | cmd/atcr/personas.go:11 | There are TWO distinct packages named `personas`: top-level `github.com/samestrin/atcr/personas` (embedded built-ins) and `github.com/samestrin/atcr/internal/personas` (community lifecycle). cmd/atcr/personas.go imports internal/personas while internal/personas/list.go imports the top-level one aliased `builtins`. A reader cannot tell which `personas` an import refers to without checking the path — a maintainability trap | rename one package to remove the collision (e.g. top-level personas → builtins, matching the existing alias), or at minimum alias the import in cmd/atcr/personas.go to make the distinction explicit | maintainability | 60 | code-review | claude | MEDIUM |
| U | [x] | LOW | cmd/atcr/personas.go:132 | `install` routes `bundle/`-prefixed args via strings.CutPrefix; a bare `bundle/` (empty name) passes through and is rejected as `unknown bundle: \"\"`, a confusing message for what is really a missing-argument error. No CLI guard distinguishes forgot-the-name from doesn-t-exist | after CutPrefix, if bundleName == \"\" return a usageError like `bundle name is required (e.g. bundle/security)` so the empty-input case reads as a usage mistake | error-handling | 15 | code-review | claude | MEDIUM |
| U | [x] | LOW | cmd/atcr/personas.go:153 | `installBundle` reports per-member outcome to stderr and exits non-zero on any failure, but the aggregate returned error `one or more bundle personas failed to install` discards the failed count, so a captured/piped run cannot distinguish 1-of-5 from 5-of-5 failures without scraping stderr | include the failed/total count in the summary error (e.g. `2 of 5 bundle personas failed to install`) so the returned error is self-describing | error-handling | 15 | code-review | claude | MEDIUM |
| U | [x] | MEDIUM | cmd/atcr/personas.go:199 | The graceful-degradation guarantee (`--scores` with no scorecard → all n/a + footer) holds only for a MISSING store. If scorecard.ReadAll returns a real error (corrupt records, permission denied), personasScores propagates it and listPersonasWithScores hard-fails the whole `list --scores` command, losing even the baseline persona table | on personasScores error, warn to stderr and fall through to rendering the table with all-n/a rates plus a scorecard-unreadable footer, mirroring the missing-store path, so `list --scores` is never worse than plain `list` | correctness | 30 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/personas.go:199 | newPersonasTestCmd: Total==0 edge case reports "PASS (0/0 cases)" | on personasScores error, warn to stderr and fall through to rendering the table with all-n/a rates plus a scorecard-unreadable footer, mirroring the missing-store path, so `list --scores` is never worse than plain `list` | correctness | 30 | code-review | bruce | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/personas.go:305 | `upgrade --all` calls installedCommunityNames (a full directory walk reading+unmarshalling every persona), then Upgrade per name re-reads the same file from disk again plus a network fetch, discarding the first walk's metadata. O(N) redundant local reads on top of the unavoidable sequential network round-trips [won't-fix: Network latency per persona dominates; local re-reads are O(N) microseconds vs O(N) × RTT. Revisit only if persona counts grow to hundreds or a bulk-version-check endpoint is added to the community registry.] | acceptable for a handful of personas; if it matters, pass the already-listed metas into the upgrade loop to skip per-persona local re-reads and/or fetch remote versions with a bounded worker pool | performance | 60 | code-review | claude | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/personas.go:366 | `renderPersonaList`, `renderScoredList`, and `renderPersonaSearch` are near-identical copy-paste: same bytes.Buffer + tabwriter boilerplate, same flush/write tail, same `lang := \"-\"`/strings.Join block duplicated verbatim. Any table-format change must be made in three places | extract a helper taking a header and a row-emitter (or [][]string rows) that owns buffer/flush/write, and a `formatLanguages([]string) string` helper used by both list renderers | maintainability | 30 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/personas/bundles.go | `InstallBundle` computes `personaPath(destDir, member)` for its pre-existence check, then `Install` recomputes `personaPath` for the same member — the traversal guard runs twice per bundle member [deferred per sprint-plan TD-015 (optional future cleanup)] | pass the resolved path into a lower-level install helper, or accept the double-compute and document it as intentional | docs | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | LOW | internal/personas/bundles.go:58 | `Resolve` builds the embed path via raw concat `\"bundles/\" + name + \".yaml\"`; traversal defense rests entirely on isValidBundleName having run first. Correct today but the safety is implicit in call ordering — a reorder or second caller bypasses the guard silently | keep the guard but make the dependency explicit (e.g. path.Join with a comment, or assert validity inside the read helper) so defense does not depend on caller discipline | security | 15 | code-review | claude | MEDIUM |
| U | [x] | LOW | internal/personas/bundles.go:70 | `isValidBundleName` hand-rolls a character-class loop duplicating the concept paths.go already expresses as compiled `personaNameRe`. Two validation idioms for the same safe-identifier concept invite drift when one is changed and the other is forgotten | replace the loop with a compiled regex mirroring personaNameRe (minus slash), or factor a shared isAllowedIdentRune helper — one canonical validation style per package | maintainability | 15 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/personas/bundles.go:129 | `InstallBundle` stats dest via `personaInstalled`, decides not-present, then `Install` stats/writes again with no lock. Two concurrent InstallBundle calls both report AlreadyPresent=false and both write; the idempotent-skip the doc comment promises is not atomic under concurrency | document that concurrent installs to the same dir are unsupported, or guard the personas dir with a lock file; do not advertise idempotency the implementation cannot guarantee under concurrency | security | 60 | code-review | claude | MEDIUM |
| U | [x] | MEDIUM | internal/personas/bundles.go:138 | `InstallBundle` passes each manifest member straight into `Install` without validating members against `validatePersonaName` first. A malformed or malicious community manifest (member named `bundle/foo`, or a traversal token) produces N opaque per-member errors instead of one clear bad-manifest failure; bundle authoring is community-controlled and reachable | validate every resolved member in Resolve/parseManifest with validatePersonaName and reject `bundle/`-prefixed members up front, returning a single manifest error before any install attempt | error-handling | 30 | code-review | claude | MEDIUM |
| U | [x] | MEDIUM | internal/personas/client.go:45 | HTTP fetch uses client with no timeout | Set http.Client Timeout or context deadline | error-handling | 10 | code-review | bruce | MEDIUM |
| 2 | [x] | HIGH | internal/personas/client.go:56 | fetch has no HTTP request timeout; default http.DefaultClient has no timeout, causing potential hangs (disagreement: LOW vs HIGH) | Set a timeout on the http.Client (e.g., 30s) or document that callers should inject a client with a timeout | security | 5 | code-review | bruce, code-reviewer | HIGH |
| 2 | [x] | HIGH | internal/personas/client.go:67 | `fetch` does `io.ReadAll(resp.Body)` with no size cap on every persona/bundle/index fetch. Two independent hostile reviewers rate this HIGH (DoS), escalating the sprint-captured MEDIUM (TD-007): a compromised or MITM'd community repo can return a multi-GB body and OOM the CLI before validateAgent ever sees the bytes. Affects Install, Upgrade and Search (index.json) (disagreement: MEDIUM vs HIGH) [deferred per sprint-plan TD-007 (LimitReader cap in follow-up)] | wrap the body in `io.LimitReader(resp.Body, maxBytes)` (1-5 MB) inside `fetch` so a single fix covers both YAML and index.json call sites; error on truncation so partial content is never validated/written. Add a test server streaming past the cap asserting a bounded-size error | security | 30 | execute-sprint | execute-sprint, claude, greta | HIGH |
| 2 | [x] | MEDIUM | internal/personas/client.go:71 | `FetchPersonaYAML` interpolates the raw `name` into the fetch URL without calling `validatePersonaName` [deferred per sprint-plan TD-008 (self-guard fetch boundary in follow-up)] | call `validatePersonaName(name)` at the top of `FetchPersonaYAML` and `url.PathEscape` each segment, so the fetch boundary is self-guarding | security | 15 | execute-sprint | execute-sprint, greta | HIGH |
| 2 | [x] | LOW | internal/personas/install.go:21 | The `bundle/` rejection in `Install` is a hardcoded string-prefix check that duplicates bundle-routing knowledge living in bundles.go. `personaNameRe` permits `/`, so `bundle/django` passes the name regex and is only stopped by this ad-hoc guard; any future reserved prefix must be remembered in both places | before writing, Lstat the dest (and created components) to reject symlinks, or write via a temp file with O_NOFOLLOW/O_EXCL then rename. Verify with a test that pre-creates a symlink at dest and asserts the write is refused | maintainability | 60 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/personas/install.go:21 | TOCTOU between path validation and write. `personaPath` does lexical filepath.Rel containment checks only and never Lstats components; `os.WriteFile` then follows whatever `dest` points to at write time. An attacker who plants a symlink at `~/.config/atcr/personas/<name>.yaml` (or an intermediate dir) between resolution and write redirects the write outside the personas dir. The risk profile explicitly named symlink as a vector; the lexical-only guard does not cover it [deferred per sprint-plan TD-010 (atomic temp-file + rename in follow-up)] | before writing, Lstat the dest (and created components) to reject symlinks, or write via a temp file with O_NOFOLLOW/O_EXCL then rename. Verify with a test that pre-creates a symlink at dest and asserts the write is refused | maintainability | 60 | code-review | claude | MEDIUM |
| U | [x] | LOW | internal/personas/install.go:30 | Persona YAML is written via `os.WriteFile` [deferred per sprint-plan TD-010 (atomic temp-file + rename in follow-up)] | write to a temp file in the same dir and `os.Rename` into place (or reuse `internal/atomicfs`) for an atomic replace | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | MEDIUM | internal/personas/install.go:35 | `Install` writes persona YAML with `os.WriteFile(dest, data, 0o644)` and creates the parent dir at 0o755. Personas are remotely-fetched reviewer instructions; world-readable mode plus a world-traversable `~/.config/atcr` dir lets any other user on a shared host read every installed persona — broader than necessary (disagreement: LOW vs MEDIUM) [deferred per sprint-plan TD-010 (atomic temp-file + rename in follow-up)] | write personas at 0o600 and create the dir at 0o700 in both install.go:35-38 and upgrade.go:55. Verify with a stat-mode assertion in the install test | correctness | 15 | code-review | bruce, claude, greta | HIGH |
| U | [x] | LOW | internal/personas/list.go:71-83 | In `listCommunity`, a YAML file that fails `os.ReadFile` or fails to unmarshal is silently listed with `Version: "-"` and no language — a corrupt installed persona is indistinguishable from one legitimately lacking a version field [deferred per sprint-plan TD-011 (surface per-row warnings in follow-up)] | accumulate per-row read/parse failures in listCommunity and return them alongside valid rows so the CLI caller can warn on stderr | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | LOW | internal/personas/list.go:71-83 | The comparator assumes finite rates; a NaN rate would violate strict-weak ordering | guard NaN at the `ScoredPersona` boundary in `ListWithScores` (treat NaN as nil/"n/a") so the package is self-protecting regardless of caller | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | MEDIUM | internal/personas/list.go:123 | `listCommunity` uses `filepath.WalkDir`, which does not follow top-level symlinks but a symlinked directory inside the user-writable personas dir pointing at `/` (or a deep tree) makes List walk arbitrary filesystem locations and ReadFile/Unmarshal every `.yaml` it finds, leaking unrelated YAML into the persona list and doing unbounded work [deferred per sprint-plan TD-011 (surface per-row warnings in follow-up)] | skip entries where `d.Type()&fs.ModeSymlink != 0`, or resolve each path and confirm it stays within personasDir before reading. Treat the personas dir as untrusted input | security | 30 | code-review | claude | MEDIUM |
| U | [x] | LOW | internal/personas/list.go:130 | Swallows corrupt persona read/unmarshal errors | accumulate per-file read/parse errors in listCommunity and return them alongside valid rows; CLI caller writes them to stderr | error-handling | 10 | code-review | bruce | MEDIUM |
| U | [x] | LOW | internal/personas/list.go:139 | Community persona Name is derived from the file path with no collision check against the nine built-in names. A community file matching a built-in name (e.g. `architect.yaml`) produces a duplicate list row, and downstream consumers keying by name (the score map in ListWithScores) conflate them | skip community files whose names collide with built-ins and accumulate a warning so the CLI caller can emit it to stderr; built-in takes priority | error-handling | 30 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/personas/paths.go:14 | `personaNameRe` (`^[a-zA-Z0-9_/-]+$`) accepts separator-only names like `-`, `_`, or `//`; safety rests entirely on the secondary segment-emptiness loop. The defense currently holds but is fragile — a refactor of either guard could expose it | tighten the regex to require an alphanumeric first char (`^[a-zA-Z0-9][a-zA-Z0-9_/-]*$`) and add explicit reject tests for `-`, `_`, `//` | security | 15 | code-review | claude | MEDIUM |
| U | [x] | LOW | internal/personas/remove.go:34 | `isBuiltin` linearly scans `builtins.Names()` on every call, and `Names()` re-allocates the slice fresh each invocation (used in Remove, TestPersona, List). Trivial at 9 names but an avoidable O(n) re-alloc per lookup, copy-pasted across remove.go and test.go | build a package-level `var builtinSet map[string]struct{}` once at init and make isBuiltin a single map lookup | performance | 15 | code-review | claude | MEDIUM |
| U | [x] | LOW | internal/personas/search.go:22 | When keyword is empty/whitespace, `strings.Contains(x, \"\")` is always true so `Search(\"\")` returns the entire index. May be intended but is undocumented and untested; a caller expecting empty-keyword to be a usage error gets a full dump | either document empty-keyword-returns-all explicitly with a test, or reject empty keyword with a usage error at the cmd layer — make the empty-string semantics a deliberate contract | error-handling | 15 | code-review | claude | MEDIUM |
| U | [x] | LOW | internal/personas/test.go:24 | `TestPersona` resolves and stat-checks the persona file, then discards `dest` and calls `runner.RunFixture(name)` with only the name, so the runner re-resolves independently. The pre-check exists only to produce an error and could validate a different file than the runner ultimately reads (same TOCTOU shape) | remove the stat pre-check from TestPersona and let the injected runner own resolution and error reporting; keeps FixtureRunner interface unchanged per TD-012 | maintainability | 30 | code-review | claude | MEDIUM |
| U | [x] | MEDIUM | internal/personas/upgrade.go:46 | `versionOf` swallows the YAML unmarshal error (`_ = yaml.Unmarshal(...)`). The remote data is validated, but the LOCAL file read off disk is not re-validated — a corrupt or hand-edited local persona that fails to parse silently yields version `-`, which compares unequal to remote and triggers an overwrite, clobbering a user's customized persona with no warning [deferred per sprint-plan TD-011 (surface per-row warnings in follow-up)] | surface the unmarshal error from versionOf (or validate localData before comparing) and abort the upgrade with a descriptive error when the local file is unparseable. Test: install garbage at dest, assert Upgrade errors rather than overwriting | error-handling | 30 | code-review | claude | MEDIUM |
| U | [x] | LOW | internal/personas/upgrade.go:76 | Non-atomic persona upgrade write [deferred per sprint-plan TD-010 (atomic temp-file + rename in follow-up)] | replace os.WriteFile in Upgrade with atomic temp-file+rename at 0o600, mirroring the install.go fix; guard symlinks at dest before writing | correctness | 30 | code-review | bruce | MEDIUM |
| U | [x] | LOW | internal/personas/upgrade.go:76 | `isNewer` falls back to `local != remote` for non-semver versions, so any difference (including a downgrade like local `2.0` vs remote `1.0`, both non-semver) reports as an upgrade and overwrites the local file [accepted per sprint-plan AC 02-06 EC1 / TD-009 (specified non-semver behavior)] | accepted/specified behavior per AC 02-06 EC1 / TD-009 — no code change | correctness | 30 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | LOW | internal/personas/upgrade.go:76 | Non-semver version fallback treats downgrades as upgrades [accepted per sprint-plan AC 02-06 EC1 / TD-009 (specified non-semver behavior)] | duplicate of adjacent row — accepted/specified behavior per AC 02-06 EC1 / TD-009 — no code change | correctness | 30 | code-review | greta | MEDIUM |
| U | [x] | MEDIUM | internal/personas/upgrade.go:76 | `isNewer` only takes the semver comparison branch when BOTH local and remote parse as valid semver; when exactly one side is valid (e.g. local `v1.5.0` vs remote `latest`/`2.0-beta`) it falls through to `local != remote` and returns true, silently overwriting a newer local persona with a non-comparable remote. Distinct angle from TD-009 (both-non-semver): the mixed-validity case is the common real-world one [accepted per sprint-plan AC 02-06 EC1 / TD-009 (specified non-semver behavior)] | when exactly one of lv/rv is valid semver, return false (treat as up-to-date) rather than falling through to string inequality. Add a table test covering (v1.0.0,latest),(latest,v1.0.0),(v2.0.0,v1.0.0) | correctness | 30 | code-review | claude | MEDIUM |
| U | [ ] | LOW | internal/registry/config.go | `NormalizeLanguageToken` only trims surrounding whitespace and strips a single leading dot; interior whitespace or extra dots (`"g o"`, `".."` → `"."`) store as junk tokens that silently never match any finding extension [deferred per sprint-plan TD-001 (document interior-space tokens in follow-up)] | document in `docs/personas-authoring.md` that only surrounding-whitespace + single-leading-dot are canonicalized; interior-space tokens are accepted verbatim and will not route | security | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [ ] | MEDIUM | internal/registry/config.go:270 | The executor block guards Persona/Name/Rules against control-char prompt injection and caps length, and the Epic 9.0 Language guard rejects control chars, but `AgentConfig.Persona` (config.go:270) — interpolated into the reviewer prompt by fanout the same way — has NO control-char or length guard in validateAgent. An untrusted community persona YAML (the exact ValidateAgentYAML threat model) can embed CR/LF to forge prompt lines via `persona` | apply the same control-char + length guard to AgentConfig.Persona in validateAgent that the executor persona already has, closing the prompt-injection gap on the community-install path | security | 30 | code-review | claude | MEDIUM |
| 3 | [x] | HIGH | internal/registry/config.go:705 | Language entries (Epic 9.0) follow the Scope guard: reject blank entries and entries with control characters. What *is* rejected (a load error) is an entry that is empty, whitespace-only, just a dot (`"."`), or contains control characters; control characters are rejected too. There is **no** allow-list of known languages — third-party persona authors may declare any extension. |  |  | 0 | code-review | archer-backup | MEDIUM |
| 3 | [x] | MEDIUM | internal/registry/config.go:720 | normalizeExt canonicalizes a file extension to the same dotless, lowercased form AgentConfig.Language entries are canonicalized to at load, so "go", ".go", and " .GO " all store as "go" and match identically against a finding's normalized extension in one form. prefer writing the canonical form directly (`["go"]`). |  |  | 0 | code-review | archer-backup | MEDIUM |
| 3 | [x] | MEDIUM | internal/registry/config.go:733 | A persona author can declare a compound extension like `language: [tar.gz]` or `[d.ts]`. NormalizeLanguageToken stores it verbatim and validateAgent passes it, but filepath.Ext only ever returns the LAST segment (`.gz`, `.ts`), so the entry can never match any finding — silent dead routing on a token that looks plausible to the author [deferred per sprint-plan TD-001 (document interior-space tokens in follow-up)] | document that only single trailing extensions are supported, or strip up to the last interior dot in the canonicalizer, and flag interior-dot tokens at validateAgent time so the author learns the entry is dead | maintainability | 30 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/registry/config.go:733 | `strings.TrimPrefix(s, \".\")` strips only ONE leading dot, so a `..go` typo canonicalizes to `.go` — non-empty, passes the validateAgent empty guard — and is stored as `.go`, which never equals a finding's canonical `go`. The canonical-empty guard comment claims to reject bad dot-tokens but `..go` slips through as dead routing | document that only single trailing extensions are supported, or strip up to the last interior dot in the canonicalizer, and flag interior-dot tokens at validateAgent time so the author learns the entry is dead | maintainability | 30 | code-review | claude | MEDIUM |
| 3 | [x] | LOW | internal/registry/config.go:733 | nil/omitted = no constraint (the persona is eligible for every review), with silent fallback when no skeptic matches the finding's language (routing-only). This is the default and keeps every pre-9.0 registry loading and routing unchanged. | document that only single trailing extensions are supported, or strip up to the last interior dot in the canonicalizer, and flag interior-dot tokens at validateAgent time so the author learns the entry is dead | maintainability | 30 | code-review | archer-backup | MEDIUM |
| 3 | [x] | LOW | internal/registry/config_test.go | The idempotency assertion covers only ASCII inputs (`.Go`, ` .TS `, `GO`) [deferred per sprint-plan TD-002 (future test-hardening pass)] | add a Unicode case-fold idempotency case to lock the canonicalization contract | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| 3 | [x] | MEDIUM | internal/registry/validate.go:23 | ValidateAgentYAML parses a single community-persona document and runs the registry's agent validation against it, returning a joined error describing every fault (or nil when valid). It is the exported seam the personas CLI (internal/personas) uses to vet fetched YAML BEFORE writing it to disk, so malformed or malicious community configs never reach the registry. |  |  | 0 | code-review | archer-backup | MEDIUM |
| 3 | [x] | LOW | internal/registry/validate.go:45 | Unlike LoadRegistry, the unmarshal is NON-strict: a community persona file is a AgentConfig superset that also carries persona-file metadata (version, description, fixture) that the strict registry decoder does not define, and those extra keys are intentionally ignored rather than rejected. The agent fields that DO matter (provider/model/role/payload/temperature/scope/language/...) are validated by the same unexported validateAgent the registry uses at load, so the rules can never drift from a hand-maintained duplicate — notably the Epic 9.0 Language guard added this sprint stays in one place. |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/pipeline.go:162 | `SelectEligibleSkeptics`'s `scores` map is keyed by skeptic registry name (`reg.Agents` key), but T6's source `scorecard.Aggregate()` is described as "reviewer-name → rate" [deferred per sprint-plan TD-005 (document keyspace when T6 wires scores)] | when wiring `scorecard.Aggregate()` into `pipeline.go:162`, add a one-line comment/assertion that the map is keyed by skeptic registry name (canonicalized per the Phase 5 `strings.ToLower` join convention) so a keyspace mismatch surfaces in review | testing | 60 | execute-sprint | execute-sprint | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/pipeline.go:162 | `SelectEligibleSkeptics` takes a `scores map[string]float64` and contains a full score-based sort.SliceStable block, but the only production caller passes nil (`nil until T6`). The entire scored-ordering branch is dead in production, exercised only by tests, and carries the NaN-ordering hazard (TD-003) for zero current benefit | when wiring `scorecard.Aggregate()` into `pipeline.go:162`, add a one-line comment/assertion that the map is keyed by skeptic registry name (canonicalized per the Phase 5 `strings.ToLower` join convention) so a keyspace mismatch surfaces in review | testing | 60 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/verify/pipeline.go:408 | `FindingsProcessed` counts jobs where len(skeptics)>0 && disp != nil, computed from the plan not the outcome, so it over-reports when a dispatcher exists but every skeptic later collapsed to unverifiable. The doc comment claims it counts findings sent through a live skeptic | tighten the comment to planned-not-completed semantics, or derive the count from jobResults outcomes rather than the pre-run plan | error-handling | 15 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go | The matched-partition comparator sorts by `scores[name]` descending | sanitize NaN to the lowest rank (`math.IsNaN(s)` → treat as -Inf) before the sort and add a NaN-score test | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:123 | `filepath.Ext(finding.File)` on a dotfile like `.gitignore`/`.env` returns the whole name (`.gitignore` → normalizes to `gitignore`), and a file literally named `.go` yields ext `go`. This is a silent semantic mismatch between file-extension and what filepath.Ext returns for dotfiles, producing surprising routing | guard against dotfile inputs (when the base name has no dot before the extension, treat as extensionless) or document the behavior, and add a `.gitignore`/`.env` routing test so the behavior is intentional | error-handling | 60 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:123 | The two-partition reorder pre-allocates and is O(n) as anticipated, but SelectEligibleSkeptics is called once per finding and re-derives `reg.AgentsByRole(RoleSkeptic)` (a full map copy of every skeptic AgentConfig) on every call. For a 500-finding review that is 500 full role-map rebuilds — the real hot-path cost, not the partition | Accepted as-is at current registry scale (~20 agents, 500 findings → ~10,000 comparisons, sub-millisecond). Revisit only if a benchmark shows regression; if hoisting is needed, add the skeptics map as a caller parameter to SelectEligibleSkeptics. | error-handling | 60 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:123 | The empty-findingLang fallthrough is correctly implemented, but correctness depends on a cross-package invariant (validateAgent rejects canonical-empty Language entries so no skeptic stores `\"\"`). If that registry guard regresses, a skeptic with a stored `\"\"` token would match every extensionless finding here | guard against dotfile inputs (when the base name has no dot before the extension, treat as extensionless) or document the behavior, and add a `.gitignore`/`.env` routing test so the behavior is intentional | error-handling | 60 | code-review | claude | MEDIUM |
| 4 | [x] | HIGH | internal/verify/select.go:132 | Two-partition language reorder: split the alphabetical names into those whose language scope matches the finding's extension and those that don't. Matched names lead, so the per-finding skeptic cap favors a language-matched skeptic. Within the matched partition, higher score sorts first, ties breaking alphabetically. A nil scores map is a valid "no score data" signal — the matched partition then orders alphabetically. Fallback is silent and automatic: when no skeptic matches the finding's language (none declared, or a different extension), selection proceeds exactly as it did pre-9.0: the review is never blocked for lack of a language-scoped skeptic, and no warning is emitted. A registry with only generalist (unscoped) skeptics routes precisely as before. |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:167 | SECTOR_PERFORMANCE: When a finding's file extension matches a persona's `language` scope, that persona is **preferred** over an unscoped one during skeptic selection (Epic 9.0). The routing algorithm now has a two-partition reorder: language-matching skeptics first, non-matching after; the existing n-cap then favors matched. This makes domain personas (security, performance, Go idioms) immediately valuable to vertical teams and becomes the primary lever for vertical market adoption. |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:190 | NaN corroboration score breaks the matched-partition total order → non-deterministic routing. Deferred → TD-003 (not active; scores nil until T6 wires it). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-003] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:214 | score-map key convention (`strings.ToLower(reviewerName)`) differs between T6 (`scores[name]`) and the routing path (`normalizeExt(filepath.Ext(finding.File))`). No live bug (T8 passes nil; not wired to scores), but a future merge conflict is possible. Deferred → TD-013 (reconcile when scores wire into routing; per Phase 2 clarification this lowercasing was a deliberate Phase 5/T6 handoff). [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:247 | One reviewer, two models → one collapsed rate of (3+1)/(4+6) = 0.4, not a last-wins single-model rate (3/4 or 1/6). Deferred → TD-003 (AC-mandated dedup in 04-04 EC3). [deferred per sprint-plan TD-003] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:279 | Reviewer that ran under multiple models yields multiple rows sharing one Reviewer name; this sums corroborated/raised across those rows and recomputes the ratio (matching scorecard's own formula) so the rate is a true per-reviewer aggregate rather than whichever model's row sorted last. |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:311 | score-map key (`strings.ToLower(reviewerName)`) diverges from the routing path's key convention (`normalizeExt(filepath.Ext(finding.File))`). No live bug (T8 passes nil; not wired to scores), but a future merge conflict is possible. Deferred → TD-013. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | HIGH | internal/verify/select.go:349 | multi-model score collapse: one reviewer that ran under several models yields multiple rows sharing one Reviewer name, and the loop lets the last row overwrite → arbitrary/misleading rate. Fixed inline in 5.5: extracted `reviewerCorroborationRates` that sums corroborated/raised across models per lowercase reviewer and recomputes the ratio. |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:367 | Comparator couples to upstream no-NaN invariant; a non-CLI caller could pass NaN. Deferred → TD-014 (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:391 | clamp branches (<0, >1) untested. Fixed inline in 5.5: added clamp tests (`-0.5`→`0.0%`, `1.5`→`100.0%`). |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:417 | NaN rate would break strict-weak ordering. Deferred → TD-014 (non-CLI caller could pass NaN; CLI caller sources finite rates). [deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:451 | Unbounded `io.ReadAll` on the community response (persona YAML + index.json) → OOM via a multi-GB body from a compromised repo/MITM. Deferred → TD-007 (HTTPS trust-on-first-use source; `io.LimitReader` cap in follow-up). [deferred per sprint-plan TD-007 (LimitReader cap in follow-up); deferred per sprint-plan TD-007] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:485 | Extracts `reviewerCorroborationRates` that sums corroborated/raised across models per lowercase reviewer and recomputes the ratio (matching scorecard's own formula) so the rate is a true per-reviewer aggregate rather than whichever model's row sorted last. |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:521 | Defensive `bundle/` reject is not a sentinel (test asserts only "not ErrPersonaNotFound"). By design — unreachable in practice (members come from trusted embedded manifests). No action. [accepted per sprint-plan (defense-in-depth on trusted embedded manifests); accepted per sprint-plan (marked by-design / no action)] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:557 | `list` warns + exits 0 even on a real dir I/O error, not just absent dir. **Not a defect** — AC 02-02 Error Scenario 1 mandates exit 0 + stderr warning on an unreadable personas dir (graceful degradation). Spec-compliant; no change. [accepted per sprint-plan AC 02-02 (graceful degradation on unreadable personas dir); accepted per sprint-plan (explicitly marked spec-compliant / not a defect)] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:593 | `test` FAIL emits "FAIL" on stdout AND a returned error on stderr (double output). **Accepted**: stdout carries the report, stderr the error — a reasonable convention. No action. [accepted per sprint-plan (stdout report + stderr error convention); accepted per sprint-plan (marked by-design / no action)] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:653 | Non-semver version diff treated as upgrade (downgrade masquerade). **Deferred → TD-009** (matches AC 02-06 EC1 — specified behavior). [accepted per sprint-plan AC 02-06 EC1 / TD-009 (specified non-semver behavior); deferred per sprint-plan TD-009] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:689 | score-map keyspace (reviewer name) vs T6's `scorecard.Aggregate()` "reviewer-name" source could silently mismatch when wired. **Fixed inline in 5.5**: extracted `reviewerCorroborationRates` that sums corroborated/raised across models per lowercase reviewer and recomputes the ratio. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired)] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:725 | NaN rate → non-deterministic comparator. **Deferred → TD-014** (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:761 | Unbounded `io.ReadAll` on the community response. **Already captured Phase 4 → TD-007** (not a Phase 5 file; no re-capture). [deferred per sprint-plan TD-007 (LimitReader cap in follow-up); deferred per sprint-plan TD-007] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:795 | score-map key (`strings.ToLower(reviewerName)`) diverges from the routing path's key convention (`normalizeExt(filepath.Ext(finding.File))`). **Already captured Phase 4 → TD-013**. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:831 | NaN rate → non-deterministic comparator. **Already captured Phase 4 → TD-014** (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:875 | Defensive `bundle/` recompute redundant with `Install`. **Deferred → TD-015** (harmless defense-in-depth on a network-bound path). [accepted per sprint-plan (defense-in-depth on trusted embedded manifests); deferred per sprint-plan TD-015] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:913 | score-map key (`strings.ToLower(reviewerName)`) diverges from the routing path's key convention (`normalizeExt(filepath.Ext(finding.File))`). **Already captured Phase 4 → TD-013**. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:957 | NaN rate → non-deterministic comparator. **Already captured Phase 4 → TD-014** (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:1001 | Defensive `bundle/` recompute redundant with `Install`. **Already captured Phase 4 → TD-015** (harmless defense-in-depth on a network-bound path). [accepted per sprint-plan (defense-in-depth on trusted embedded manifests); deferred per sprint-plan TD-015] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:1045 | score-map key (`strings.ToLower(reviewerName)`) diverges from the routing path's key convention (`normalizeExt(filepath.Ext(finding.File))`). **Already captured Phase 4 → TD-013**. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:1089 | NaN rate → non-deterministic comparator. **Already captured Phase 4 → TD-014** (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:1133 | Defensive `bundle/` recompute redundant with `Install`. **Already captured Phase 4 → TD-015** (harmless defense-in-depth on a network-bound path). [accepted per sprint-plan (defense-in-depth on trusted embedded manifests); deferred per sprint-plan TD-015] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:1177 | score-map key (`strings.ToLower(reviewerName)`) diverges from the routing path's key convention (`normalizeExt(filepath.Ext(finding.File))`). **Already captured Phase 4 → TD-013**. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:1221 | NaN rate → non-deterministic comparator. **Already captured Phase 4 → TD-014** (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:1265 | Defensive `bundle/` recompute redundant with `Install`. **Already captured Phase 4 → TD-015** (harmless defense-in-depth on a network-bound path). [accepted per sprint-plan (defense-in-depth on trusted embedded manifests); deferred per sprint-plan TD-015] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:1309 | score-map key (`strings.ToLower(reviewerName)`) diverges from the routing path's key convention (`normalizeExt(filepath.Ext(finding.File))`). **Already captured Phase 4 → TD-013**. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:1353 | NaN rate → non-deterministic comparator. **Already captured Phase 4 → TD-014** (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:1397 | Defensive `bundle/` recompute redundant with `Install`. **Already captured Phase 4 → TD-011** (harmless defense-in-depth on a network-bound path). [accepted per sprint-plan (defense-in-depth on trusted embedded manifests); deferred per sprint-plan TD-011] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:1441 | score-map key (`strings.ToLower(reviewerName)`) diverges from the routing path's key convention (`normalizeExt(filepath.Ext(finding.File))`). **Already captured Phase 4 → TD-013**. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:1485 | NaN rate → non-deterministic comparator. **Already captured Phase 4 → TD-014** (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:1529 | Defensive `bundle/` recompute redundant with `Install`. **Already captured Phase 4 → TD-015** (harmless defense-in-depth on a network-bound path). [accepted per sprint-plan (defense-in-depth on trusted embedded manifests); deferred per sprint-plan TD-015] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:1573 | score-map key (`strings.ToLower(reviewerName)`) diverges from the routing path's key convention (`normalizeExt(filepath.Ext(finding.File))`). **Already captured Phase 4 → TD-013**. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | MEDIUM | internal/verify/select.go:1617 | NaN rate → non-deterministic comparator. **Already captured Phase 4 → TD-014** (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:1661 | Defensive `bundle/` recompute redundant with `Install`. **Already captured Phase 4 → TD-015** (harmless defense-in-depth on a network-bound path). [accepted per sprint-plan (defense-in-depth on trusted embedded manifests); deferred per sprint-plan TD-015] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [ ] | MEDIUM | internal/verify/select.go:1705 | score-map key (`strings.ToLower(reviewerName)`) diverges from the routing path's key convention (`normalizeExt(filepath.Ext(finding.File))`). **Already captured Phase 4 → TD-013**. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [ ] | MEDIUM | internal/verify/select.go:1749 | NaN rate → non-deterministic comparator. **Already captured Phase 4 → TD-014** (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:1793 | Defensive `bundle/` recompute redundant with `Install`. **Already captured Phase 4 → TD-015** (harmless defense-in-depth on a network-bound path). [accepted per sprint-plan (defense-in-depth on trusted embedded manifests); deferred per sprint-plan TD-015] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [ ] | MEDIUM | internal/verify/select.go:1837 | score-map key (`strings.ToLower(reviewerName)`) diverges from the routing path's key convention (`normalizeExt(filepath.Ext(finding.File))`). **Already captured Phase 4 → TD-013**. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [ ] | MEDIUM | internal/verify/select.go:1881 | NaN rate → non-deterministic comparator. **Already captured Phase 4 → TD-014** (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:1925 | Defensive `bundle/` recompute redundant with `Install`. **Already captured Phase 4 → TD-015** (harmless defense-in-depth on a network-bound path). [accepted per sprint-plan (defense-in-depth on trusted embedded manifests); deferred per sprint-plan TD-015] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [ ] | MEDIUM | internal/verify/select.go:1969 | score-map key (`strings.ToLower(reviewerName)`) diverges from the routing path's key convention (`normalizeExt(filepath.Ext(finding.File))`). **Already captured Phase 4 → TD-013**. [deferred per sprint-plan TD-013 (score-map key convention reconciled when wired); deferred per sprint-plan TD-013] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [ ] | MEDIUM | internal/verify/select.go:2013 | NaN rate → non-deterministic comparator. **Already captured Phase 4 TD-014** (CLI caller sources finite rates; precondition documented). [deferred per sprint-plan TD-003 (sanitize NaN when scores wired); deferred per sprint-plan TD-014] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [x] | LOW | internal/verify/select.go:2057 | Defensive `bundle/` recompute redundant with `Install`. **Already captured Phase 4 TD-015** (harmless defense-in-depth on a network-bound path). [accepted per sprint-plan (defense-in-depth on trusted embedded manifests); deferred per sprint-plan TD-015] |  |  | 0 | code-review | archer-backup | MEDIUM |
| 4 | [ ] | LOW | internal/verify/select_test.go | `Skeptic.Config.Language` aliases the registry's backing array (same read-only contract as `Scope`), but only `Scope` has an independence test (`TestSelectEligibleSkeptics_ScopeSliceIsIndependent`) [deferred per sprint-plan TD-004 (future test-hardening pass)] | add a `Language`-slice independence test mirroring the Scope one, or document that `Language` shares the Scope read-only guarantee | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| 5 | [x] | LOW | personas/personas.go | The package doc comment and `Names`' doc comment state the literal count ("nine embedded ... personas") [deferred per sprint-plan TD-006 (future doc-tidy pass)] | replace the hardcoded count with a count-agnostic phrasing ("the embedded default reviewer personas") so the comment cannot go stale | docs | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | MEDIUM | personas/personas.go:17 | `//go:embed *.md` pulls in EVERY `.md` in the package dir, but Names() exposes only the hardcoded 9-entry slice and Get rejects unregistered names. A stray `.md` is embedded but unreachable (binary bloat, no signal); a name in the slice missing its `.md` only fails at Get-call time, not init. No test asserts embedded set == names ∪ {_base} | add a package-init or test-time invariant that the embedded `.md` files exactly match names plus _base.md, failing loudly on drift — converts a latent runtime internal-error into a build/test failure | testing | 30 | code-review | claude | MEDIUM |
| U | [x] | LOW | personas/personas.go:26 | `isRegistered` does a linear scan of `names` on every Get call. Get is on the prompt-resolution hot path and the slice is immutable after init, so a map lookup would be both faster and clearer | build a `var nameSet map[string]struct{}` once (populate in init from names) and have isRegistered do a map lookup | performance | 15 | code-review | claude | MEDIUM |

### [2026-06-23] From Sprint: 8.0_reconciler_library

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 2 | [x] | HIGH | internal/reconcile/adapter/adapter.go:19 | The internal/reconcile/adapter package (ToFinding/FromFinding/ToJSONFinding) has ZERO non-test callers — the live path (RunReconcile in gate.go:220) uses lib.go toLibFinding/fromLibFinding, not the adapter. The documented permanent public boundary is unused scaffolding today and its conversion logic duplicates lib.go (TD-006) while exercised only by a self-asserting test. A dead exported boundary that drifts from the live one is worse than no boundary. (intent_note: deferred per sprint-plan Phase 3 Clarification Q1/Q2 (TD-006)) | Phase 3 deferral (TD-006): wire RunReconcile/emit.go JSONFindings() to consume the adapter (collapsing duplication), OR mark the package explicitly as a transitional Phase-3 staging artifact so it is not mistaken for live boundary code. Extends pre-seeded TD-006. | maintainability | 60 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/reconcile/adapter/adapter.go:62 | THREE independent JSONFinding field maps must agree but are maintained separately: lib.go toLibFinding/fromLibFinding, emit.go JSONFindings() (emit.go:140-156), and adapter ToJSONFinding (adapter.go:63-79). TD-006 names only the lib.go<->adapter pair; the emit.go<->adapter pair is a third undocumented copy. Adding a ClusterID-class field to the merged record would silently diverge the builders with no compiler or test catch. (intent_note: deferred per sprint-plan Phase 3 Clarification Q1/Q2 (TD-006)) | Collapse to one conversion helper (have JSONFindings() call adapter.ToJSONFinding or vice versa) so there is a single field map; at minimum add a reflection-based table test that fails when a field exists in one builder but not the other. Extends TD-006. | maintainability | 120 | code-review | claude | MEDIUM |
| 2 | [x] | MEDIUM | internal/reconcile/adapter/adapter_test.go:99 | The require.Same *Verification pointer-identity assertion only covers the DEAD adapter path. The LIVE path debate/gate depend on is Merged.Verification -> JSONFindings() (emit.go:152) -> cache -> IsFailing(f.Verification) (gate.go:77,123) plus findings[i].Verification mutation in debate. Identity is in fact preserved (struct copy of pointer), but untested on the path that matters — a future refactor of JSONFindings() to deep-copy would pass the adapter test and silently break gate.go. (intent_note: deferred per sprint-plan Phase 3 Clarification Q1/Q2 (TD-006)) | Add a test on Result.JSONFindings() (emit.go) asserting require.Same(merged.Verification, jsonFindings[i].Verification) — cover the live path, not only the unused boundary helper. | testing | 30 | code-review | claude | MEDIUM |
| 2 | [/] | HIGH | internal/reconcile/discover.go:25 | `internal/reconcile` keeps its discovery `Source` (Name + `[]stream.Finding` + Skipped + SkippedFiles); the library now defines a public `Source` (Name + `[]Finding`) | moved `Reconcile` takes the library `Source`; discovery output (`discover.Source`) is converted to `reconcile.Source` in the adapter/discovery layer | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | LOW | internal/reconcile/emit.go:121 | Subtle cached-vs-derived behavior split in JSONFindings | Add a code comment near the cache check explicitly noting derivation is intentional | correctness | 5 | code-review | bruce | MEDIUM |
| 2 | [x] | LOW | internal/reconcile/emit.go:360 | Misleading/truncated comment about FixWarning/path-validation fields | Remove or correct comment to accurately explain why path-validation fields are omitted | maintainability | 5 | code-review | reviewer | MEDIUM |
| 2 | [x] | MEDIUM | internal/reconcile/emit.go:410 | Missing output encoding in markdown report generation | Apply HTML/Markdown escaping to user-controlled fields before writing | security | 15 | code-review | greta | MEDIUM |
| U | [x] | LOW | internal/reconcile/emit.go:439 | ClusterID doc comment instructs maintainers to grep internal/debate/cluster.go symbols by name (manual coupling, no compile guarantee); same fragility applies to stream.PathNotFoundWarning used at emit.go:446 to special-case the "File not found" label — a rename in internal/stream silently changes report rendering with no test asserting the label mapping. | Add a focused test asserting m.PathWarning == stream.PathNotFoundWarning renders the "File not found" label and any other warning renders verbatim, locking the contract the comment only describes. | maintainability | 15 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/reconcile/gate.go:229 | RunReconcile + Emit serialize toAmbiguousWire(r.Ambiguous) twice per run over the same data: ambiguousHash already renders it in lib.go:140, and Emit renders the same wire slice again at emit.go:220 — duplicated O(n log n)+marshal work on every reconcile. | Cache the rendered ambiguous.json bytes (or the wire slice) on the Result alongside jsonFindings and have both ambiguousHash and Emit reuse it. | performance | 30 | code-review | claude | MEDIUM |
| 2 | [x] | LOW | internal/reconcile/lib.go | The stream.Finding <-> reconcile.Finding conversion exists in two places: `internal/reconcile/lib.go` (`toLibFinding`/`fromLibFinding`, used by the Phase-2 Reconcile wrapper) and `internal/reconcile/adapter` (`ToFinding`/`FromFinding`, the public boundary) (intent_note: deferred per sprint-plan Phase 3 Clarification Q1/Q2 (TD-006)) | when consumers flip to the library, route discovery/conversion through `internal/reconcile/adapter` and delete `lib.go`'s `toLibFinding`/`fromLibFinding` (and the transitional Reconcile wrapper), collapsing to one conversion implementation | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | MEDIUM | internal/reconcile/lib.go:87 | Missing defensive copy for SeverityRank | Add a defensive map copy like the previous merge.go init | maintainability | 5 | code-review | bruce | MEDIUM |
| 2 | [x] | HIGH | internal/reconcile/merge.go:56 | The pure logic Phase 2 must relocate is hard-coupled to `internal/stream` — `Merged` embeds `stream.Finding`, and `Cluster`/`Merge`/`DedupeCluster`/`Reconcile`/`BuildDisagreements` take or return `[]stream.Finding` (merge.go ~21 `stream.` refs, disagree.go ~11, cluster.go ~7, dedupe.go ~6) | execute as a PORT: rewrite `stream.Finding` -> `reconcile.Finding` in each moved file; relocate `SeverityRank`/`NormalizeSeverity` into the library (internal/stream/severity.go is self-contained + stdlib-only, so mechanical) | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 2 | [x] | LOW | internal/reconcile/validate_test.go:54-66 | TestValidateFindingPaths_FlowsIntoJSONRecord doesn't exercise validateFindingPaths | Add a real call to validateFindingPaths over a temp dir | maintainability | 10 | code-review | bruce | MEDIUM |
| U | [x] | LOW | unknown:0 | Phase-2 task 2.2.7 lists `AmbiguousCluster` under `ambiguous.go`, but the `AmbiguousCluster` type is declared in `dedupe.go` (ambiguous.go holds `AmbiguousID`/`AmbiguousHash`) (intent_note: deferred per sprint-plan Final Phase / dod-completion-summary.md (external repo-admin action)) | Configure branch protection in GitHub repo Settings -> Branches: add the root CI job and the reconcile-module PR-time job as required status checks. External repo-admin UI action (post-merge), not a source-tree change; documented deferred in dod-completion-summary.md. | docs | 15 | execute-sprint | execute-sprint | MEDIUM |
| U | [/] | MEDIUM | unknown:0 | [Story 01 / Story 06] DoD item not implemented: both CI jobs (root ci.yml + reconcile-module PR-time job) must be marked as REQUIRED status checks on the main branch-protection rule. The CI workflow deliverables they depend on are all present and verified; only the protection-rule toggle is unset. The two story-level [ ] boxes (AC 01-06, AC 06-02) are the same single external action. (intent_note: deferred per sprint-plan Final Phase / dod-completion-summary.md (external repo-admin action)) | Configure branch protection in GitHub repo Settings -> Branches: add the root CI job and the reconcile-module PR-time job as required status checks. External repo-admin UI action (post-merge), not a source-tree change; documented deferred in dod-completion-summary.md. | docs | 15 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/ambiguous.go:23 | The \x00 field delimiter in AmbiguousID is not escaped, so a NUL byte embedded in PROBLEM text aliases the field boundary. Empirically confirmed AmbiguousID("a.go",10,"x","y\x00z") == AmbiguousID("a.go",10,"x\x00y","z") — two distinct gray pairs collapse to one id, the one outcome the design comment says it forbids, reachable via canonicalization not just SHA brute force. | Length-prefix each field (write len(s) then s) or hex/base64-encode the variable-length segments before hashing so no input byte can impersonate the separator; alternatively reject/strip NUL from PROBLEM text at the boundary. | security | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/ambiguous.go:23 | AmbiguousID truncates SHA-256 to 128 bits (h[:16]); the inline comment defends this as "spend the bytes" against collision but reads as arguing for MORE bits while actually halving the digest. Reasoning is sound (128-bit collision resistance is ample) but the justification text is self-contradictory and will mislead a maintainer into thinking truncation is the safety measure. | Length-prefix each field (write len(s) then s) or hex/base64-encode the variable-length segments before hashing so no input byte can impersonate the separator; alternatively reject/strip NUL from PROBLEM text at the boundary. | security | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/ambiguous.go:30 | AmbiguousHash stability hinges entirely on renderIndentedJSON using MarshalIndent with fixed two-space indent + trailing newline. Correct and deterministic today (structs use no maps), but nothing in the hash path documents or guards that adding a map[string]... field to any nested struct would break byte-stability across Go versions. | Add a test that round-trips a fixed []AmbiguousCluster fixture and asserts the exact hash string (golden) so any field/ordering change that perturbs bytes fails CI loudly. | testing | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/ambiguous.go:44 | AmbiguousHash panics on any json.MarshalIndent error, justified as unreachable — true only because every current field is JSON-safe. The type is public and exported; a future field of func/chan/cyclic type makes this a live panic in a hashing helper callers reasonably assume is total. A library function should not panic on serialization of a public struct. (Same pattern in internal emit.go:196 ambiguousHash, also on the main path via lib.go:140, where reviewer-controlled non-UTF8 free-text could trigger it as a DoS.) | Return (string, error) from AmbiguousHash or tolerate the error with a sentinel; at minimum add a compile-time/test guard asserting Finding stays JSON-marshalable. For the internal copy, ToValidUTF8 the free-text fields before hashing. | error-handling | 60 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/attribution.go:21 | HasFixAttribution/AppendFixAttribution do not handle an empty name: AppendFixAttribution("foo","") appends "fix by " (trailing space, no name) and HasFixAttribution then matches the empty-name token. No guard, so an upstream bug passing an empty executor name silently writes a malformed attribution that reports as "already attributed" on the next pass, masking the bug. | Replace the Split loop with an index-based scan checking segment boundaries against EvidenceSep without materializing the slice, or accept the allocation as negligible and document it. Low priority unless attribution is genuinely hot. | error-handling | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/attribution.go:21 | HasFixAttribution calls strings.Split(evidence, EvidenceSep) which allocates a full slice of every segment just to find one match; AppendFixAttribution then calls HasFixAttribution again (re-splitting). For a hot executor path appending attribution to many findings this is O(segments) allocation per call where a streaming strings.Index-with-boundary-check would be zero-alloc. | Replace the Split loop with an index-based scan checking segment boundaries against EvidenceSep without materializing the slice, or accept the allocation as negligible and document it. Low priority unless attribution is genuinely hot. | error-handling | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/confidence.go:18 | ConfidenceForVerdict normalizes the verdict (case-insensitive, trimmed) but passes prior through completely unvalidated in the default branch, so ConfidenceForVerdict("high","garbage") returns "high" (lowercase) which then fails closed in ConfidenceAtOrAbove because confidenceRank["high"] is 0 — a silently-degraded gate. The two functions disagree on whether confidence tokens are canonicalized. | Either normalize prior to canonical upper-case before returning it from the pass-through branch, or document that prior must already be canonical. Add a test for non-canonical prior flowing through an unrecognized verdict into ConfidenceAtOrAbove. | error-handling | 15 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/confidence.go:18 | The switch compares strings.ToLower(verdict) against VerdictConfirmed/VerdictRefuted, correct ONLY because those constants happen to be lowercase literals. If the verdict enum is ever recanonicalized to upper-case (plausible — severity/confidence are upper-case), every case arm silently stops matching and all verdicts fall through to pass-through: a catastrophic-but-green failure coupled to an unstated convention in verification.go. | Either normalize prior to canonical upper-case before returning it from the pass-through branch, or document that prior must already be canonical. Add a test for non-canonical prior flowing through an unrecognized verdict into ConfidenceAtOrAbove. | error-handling | 15 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/confidence.go:29 | confidenceRank keys on ConfLow/ConfMedium/ConfHigh (defined in merge.go) plus ConfidenceVerified (defined here) — the confidence-tier constants are split across two files in the same package. A maintainer adding a tier must touch two files and remember the rank map lives apart from three of its four keys. | Co-locate all four Conf* tier constants with confidenceRank in confidence.go (or a dedicated confidence_tiers.go) so the tier set and its ranking are defined together. | maintainability | 15 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/dedupe.go:142 | The gating cross-multiply (inter*10 >= union*7) is correctly integer-only — float-drift risk IS addressed for the decision boundary. But the advisory sim := float64(inter)/float64(union) is stored in AmbiguousCluster.Similarity and serialized through AmbiguousHash's MarshalIndent, so a float64 DOES enter the hashed sidecar bytes. Byte-identity holds (deterministic IEEE-754 division + Go float encoding) but the stated "never depend on float rounding" intent is partially undermined. | If strict float-free hashing is desired, serialize similarity as an integer ratio (inter/union fields or fixed-scale integer) instead of float64, or add a comment documenting the hashed float relies on deterministic float division + Go encoding. Add a cross-run hash-stability test. | maintainability | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/dedupe.go:157 | jaccard() is dead code in production — the only caller is tests; classify does its own inter/union computation inline. It duplicates the intersection/union logic, creating two parallel implementations that can drift, and tests validate a function the library never executes. | Either delete jaccard and have tests assert against classify's returned sim, or refactor classify to call a shared jaccardSets(a,b) helper so there is one implementation. | maintainability | 15 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | reconcile/finding.go | Library `Finding` intentionally omits `PathValid`/`PathWarning`/`PathSuggestion`, but `Merged`/`JSONFinding` and report.md rendering (emit.go renderMarkdown reads `m.PathWarning`/`m.PathSuggestion`) depend on them | `FromFinding` + the ATCR I/O layer re-stamp path-validation onto `stream.Finding` AFTER the library round-trip; lock it down with `TestBoundaryAdapter_FindingConversionRoundTrip` (task 2.1) | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 4 | [x] | LOW | reconcile/finding.go:25 | omitempty policy is asymmetric and undocumented: Disagreement/Verification/Reviewer/Reviewers/Confidence carry omitempty while the first eight wire fields (Severity..Evidence) do not, so a bare input finding emits zero-value core fields (line:0, est_minutes:0) while a reconciled finding omits the trailing five. Fine for stability but the asymmetry isn't documented and a reader might assume omitempty is uniform. | Document the omitempty policy explicitly in the struct doc (core nine always emitted; reconciled-only fields omitempty), or add a golden serialization test covering both a bare input finding and a fully-populated reconciled finding. | testing | 15 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | reconcile/merge.go:47 | The pre-identified edge "verdict/Verification mutation across boundary" was a flagged risk yet there is ZERO test coverage exercising Verification through Merge/Reconcile (grep of merge_test.go/reconcile_test.go finds nothing). The TD-005 Verification drop went undetected precisely because no test asserts the field survives merge. An untested field on a byte-identity contract is a gap. (intent_note: deferred per sprint-plan Phase 2 gate / TD-005) | Add a test feeding a Finding with populated Verification through Reconcile and asserting the documented behavior (propagated, or deliberately nil per TD-005 disposition). Pairs with TD-005. | testing | 30 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/merge.go:54 | The library `Merge(group []Finding) Merged` builds the merged `Finding` without copying any input `Finding.Verification` (intent_note: deferred per sprint-plan Phase 2 gate / TD-005) | either carry `Verification` through `Merge` with winning-verdict precedence (mirroring the internal `mergeVerification`), or document on `Merge`/`Finding` that core merge intentionally does not propagate input Verification (it is stamped post-reconcile) | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 4 | [x] | LOW | reconcile/merge.go:112 | In MergeSeverity, when no group member has a known severity, max falls back to NormalizeSeverity(group[0].Severity); sortMerged then ranks unknown tokens via SeverityRank returning 0, so all unknown-severity findings collapse to rank 0 and sort among themselves by file/line. Deterministic and correct, but an unknown severity silently sorts below LOW with no signal. | No code change required for determinism; optionally emit a disagreement/annotation when the group max severity is unrecognized so downstream gates can flag malformed input. Confirm intentional against the AC. | maintainability | 15 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/merge.go:183 | MaxEstMinutes initializes max:=0 and only updates when f.EstMinutes > max. If every finding in a group carries a negative EstMinutes (malformed input), it returns 0 rather than the actual max, masking bad data on a library whose inputs come from external reviewer streams. | Initialize from group[0].EstMinutes (group is guaranteed non-empty by Merge precondition) instead of 0, or document that negatives clamp to 0. Add a unit test with a group of negative estimates. | maintainability | 15 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | reconcile/reconcile.go:41 | Reconcile ignores Options.Merges | Call dedupeCluster(c, opts.Merges) | correctness | 2 | code-review | bruce | MEDIUM |
| 4 | [x] | MEDIUM | reconcile/reconcile.go:110 | sortMerged's comparator chain is severity->file->line with no content/Problem tiebreak despite the doc comment claiming a total order. Two distinct un-merged findings sharing the same severity+file+line compare equal both ways; byte-identity holds ONLY because SliceStable + deterministic upstream cluster iteration order. It is a stable sort over a partial order, not a strict total order — a future refactor of dedupeCluster root-iteration order would silently reorder output. | Add a final deterministic tiebreak (e.g. Problem string, then a fully discriminating field) so the comparator is a strict total order independent of input order, OR correct the doc comment to state the dependency on stable upstream ordering. Add a test feeding two co-located distinct findings in both input orders asserting identical output. | maintainability | 30 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | reconcile/severity.go:19 | SeverityRank is an exported package-level map[string]int documented read-only-after-init, aliased (not copied) by internal/stream/severity.go and internal/reconcile/lib.go — dual-ownership is genuinely eliminated (single source of truth). BUT an exported mutable map is a footgun: any consumer can do reclib.SeverityRank["CRITICAL"]=0 and desync every package at once, the exact failure extraction meant to prevent. Only a comment prevents it. | Replace the exported map with an unexported map behind an exported accessor func SeverityRank(s string) (int, bool) or func RankOf(string) int; makes the rubric truly read-only and removes the alias-mutation race without materially changing call sites. | maintainability | 60 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/source.go:10 | Source.Name and Source.Findings have no JSON tags, unlike every other type in the package (Finding/Verification/AmbiguousCluster tag every field). If Source is ever serialized it emits "Name"/"Findings" (Go default capitalization), inconsistent with the lower_snake wire convention. Either deliberately non-wire (then a latent trap) or simply missed. | If Source is purely an in-memory input shape, add a comment stating it is never serialized; if it can hit the wire, add json:"name"/json:"findings" tags for consistency with the schema. | maintainability | 5 | code-review | claude | MEDIUM |
| 4 | [x] | MEDIUM | reconcile/verification.go:11 | The Verdict enum-validation invariant is documented (writing stage MUST validate; readers do not re-validate) but the type provides NO enforcement mechanism — Verdict is a bare string, no constructor, no Validate() method. *Verification pointer semantics are correct, but an unvalidated empty/garbage Verdict is a prose-only contract; ConfidenceForVerdict even silently tolerates it by passing prior through. | Add func (v Verification) Valid() bool or NewVerification(verdict, skeptic string) (*Verification, error) that rejects out-of-enum verdicts, and route the verify stage through it — turns a prose contract into an enforced one. | error-handling | 60 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | reconcile/verification.go:29 | Verdict enum values are lowercase string constants ("confirmed") while Severity ("CRITICAL") and Confidence ("VERIFIED") tiers in the same package are upper-case — three casing conventions for three enum families in one small library, which is exactly what makes the confidence.go ToLower coupling fragile. New contributors cannot infer the convention. | Document the casing convention per enum family at the package level (doc.go) or unify; if unification is too invasive, add an assertion test pinning each family's casing. | maintainability | 15 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | reconcile/adapter/json/adapter.go:60 | Decode takes a fully-buffered []byte with no size cap and double-allocates on the array path (Unmarshal builds the full []decodeEnvelope graph, then make([]Source,...) copies it). Depth-DoS is covered by stdlib (~10000 nesting cap) and total size is documented as the caller's job — but "documented" is the entire mitigation; a careless caller who skips io.LimitReader gets unbounded allocation on untrusted input. | Consider an optional maxBytes guard or a DecodeReader(io.Reader, limit) variant so the safe path is the default rather than opt-in. Acceptable as-is given the explicit doc contract, but the safety depends entirely on every caller reading the doc. | security | 60 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | reconcile/adapter/json/adapter.go:66 | Unbounded JSON input may cause memory exhaustion | Enforce input size limit before unmarshaling or document caller responsibility | security | 5 | code-review | greta | MEDIUM |
| 5 | [x] | LOW | reconcile/adapter/json/adapter.go:76 | The [-sniff dispatches array-vs-object on the first significant byte; a JSON literal that is neither (bare "string", 42, true, null) routes to the single-object path. Top-level null Unmarshals with no error into a zero-value envelope, then fails the version check producing the misleading error 'version must be reconcile-json/v1, got ""' rather than 'expected object or array'. | Still errors (caught by the version gate), so no security impact; optionally reject non-object/non-array first bytes explicitly for a clearer message. Low priority, diagnostic quality only. | error-handling | 15 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | reconcile/adapter/json/adapter.go:84 | Decode validates version but performs ZERO validation on finding contents — a decoded finding can carry severity:"", line:0, empty file/problem and sail through as a valid Source. The package doc strategy claims "validate required fields after unmarshal" but only the envelope version is checked; the doc overpromises validation the code does not perform. | Either explicitly document that finding-field validation is the reconcile pipeline's responsibility (not the adapter's) and remove the overpromise from the package doc, OR add a minimal required-field check per finding (non-empty file+problem, severity present). | error-handling | 30 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | reconcile/adapter/json/adapter.go:91 | Missing required-field validation in Decode | Add severity/file/line checks | correctness | 10 | code-review | bruce | MEDIUM |
| U | [x] | LOW | reconcile/adapter/json/adapter.go:118 | The encoded summary block leaks zero/null fields the adapter cannot populate from a bare Result: golden shows per_source_counts:null, skipped_sources:null, reconciled_at:"", ambiguous_hash:"". The envelope top-level reconciled_at is RFC3339 but nested summary.reconciled_at is empty — two reconciled_at fields, one valid one blank, in the same v1 document: schema noise/inconsistency in the public surface. | Driven by reconcile.Summary lacking omitempty (pre-existing type). At minimum document that summary is passed through verbatim and the envelope-level reconciled_at is authoritative, or normalize the duplicate; flag the dual reconciled_at as a deliberate decision in the package doc. | maintainability | 30 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | reconcile/adapter/json/adapter.go:129 | firstNonSpace reinvents whitespace-skipping that bytes.TrimLeft does in one line and conflates "empty input" with "all-whitespace input" by returning sentinel 0; a genuine input whose first significant byte is NUL (0x00) is misreported as "empty input" rather than a JSON syntax error. | Use bytes.TrimLeft for the whitespace strip, then branch on len(trimmed)==0 for emptiness and trimmed[0]=='[' for the array sniff — removes the NUL-vs-empty ambiguity and the hand-rolled loop. | maintainability | 15 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | reconcile/adapter/json/adapter_test.go:138 | Untrusted-input failure-mode coverage is thin for the highest-risk new surface. Missing: all-whitespace/whitespace-only input, BOM-prefixed input (despite utf8BOM stripping being core), leading-whitespace+valid-array (exercising firstNonSpace sniff), a mixed array where one element has the wrong version (does the indexed source[%d] error fire with the right index?), deeply-nested rejection. Single TestDecode_MalformedJSON checks one truncated object only. | Add table-driven decode tests covering BOM, leading-whitespace+array, whitespace-only, and a 3-element array whose middle element has a bad version (assert error names source[1]). | testing | 60 | code-review | claude | MEDIUM |
| 5 | [x] | LOW | reconcile/adapter/json/adapter_test.go:207 | The byte-stability test encodes the SAME struct twice — can never differ for a pure function, proving little. The real risk is map-key ordering in summary.per_source_counts (map[string]int); sampleResult() leaves that map nil, so the one nondeterministic field is never exercised. | Add a PerSourceCounts map with 3+ keys to a fixture and assert byte-equality across encodes (or against a golden). encoding/json sorts keys so it passes, but the test must cover the field that could regress. | testing | 30 | code-review | claude | MEDIUM |
| U | [x] | LOW | reconcile/adapter/json/adapter_test.go:260 | TestNoPathValidationFieldsInOutput greps for Go field names PathValid/PathWarning, but if those leaked they would serialize as snake_case JSON tags (path_valid/path_warning), not Go identifiers — the test would pass even if a leak occurred under a snake_case tag. The check is matched against the wrong casing for the actual failure mode, giving false confidence. | Assert absence of the JSON wire names path_valid/path_warning/path_suggestion/cluster_merged (snake_case) in addition to or instead of the Go identifiers. | testing | 15 | code-review | claude | MEDIUM |
| U | [x] | LOW | .githooks/pre-push | `.githooks/pre-push` mirrors root CI (`golangci-lint run` + `go test -race ./...`) but, like root `go test ./...`, does NOT cross the nested `./reconcile` `go.mod` boundary — so a `./reconcile` regression passes pre-push locally even though the new `ci.yml` `reconcile-module` job catches it at PR time | add `(cd reconcile && go test ./...)` to `.githooks/pre-push` (and mirror in the tracked `.githooks/` per git-hooks-mirror-ci) so the local mirror matches the new `ci.yml` `reconcile-module` job and the boundary gap cannot reopen locally | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
| U | [x] | MEDIUM | .github/workflows/ci.yml:58-86 | New reconcile-module job lacks explicit permissions block | Add `permissions: contents: read` block to the new job for least-privilege consistency with reconcile-module.yml | security | 3 | code-review | bruce | MEDIUM |

### [2026-06-22] From Sprint: 7.3_github_action_pr_integration

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 6 | [/] | MEDIUM | cmd/atcr/github.go:148-167 | Sequential inline comment posting is slow for PRs with many findings (Deferred: Epic Plan 7.6) | Use GitHub's batch POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews endpoint | performance | 45 | code-review | bruce | MEDIUM |
| 6 | [/] | LOW | cmd/atcr/github.go:148-167 | Conclusion is computed twice for the same findings/failOn inputs (Deferred: Epic Plan 7.6) | Use GitHub's batch POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews endpoint | performance | 45 | code-review | bruce | MEDIUM |

### [2026-06-22] From Sprint: epic-7.1

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | MEDIUM | internal/verify/syntaxguard.go:130 | An unfenced multi-line JSON/config snippet with block braces can still satisfy looksLikeGoCode and be parsed as Go, producing a spurious invalid_syntax flag on non-Go content (residual after heuristic hardening). | Detect obviously non-Go brace content (JSON object / key:value lines) before treating block braces as a Go signal; deferred as a separate design refinement (unfenced non-Go fixes are rare; fenced non-Go is already handled). [Deferred 2026-06-22 to Epic Plan 7.5 syntax-guard-refinements per clarification] | EDGE_CASES | 30 | execute-epic-independent |

### [2026-06-22] From Sprint: 7.0.1_executor_model_configuration

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 7 | [/] | LOW | internal/registry/config.go:524 | Potential prompt injection via system_prompt (Won't-fix: config.go:531–534 explicitly documents that control chars are intentionally NOT rejected in system_prompt; the --- delimiter added at executor.go:225 eliminates the CRLF metadata-forgery surface; otto's fix conflicts with the documented design decision) | Add control character validation to SystemPrompt | security | 15 | code-review | otto | MEDIUM |

### [2026-06-21] From Sprint: epic-6.0

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | MEDIUM | internal/debate/debate.go:165 | Gray-zone cluster merge/separate rulings are recorded in debate.json but not physically applied to findings.json; clusters still resolve via the existing adjudication path (Deferred: Epic Plan 6.1) | Wire the judge cluster decision into the reconcile adjudication application so unattended runs auto-merge gray-zone clusters inline | INTEGRATION | 60 | execute-epic-stage3 |

### [2026-06-20] From Sprint: epic-5.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/cache/store.go:115 | Eviction does a full ReadDir+Stat of the cache dir on every Put even when under the cap; O(n) per write scales poorly if the cache accumulates thousands of entries (Won't-fix 2026-06-21: scan runs serially under the store mutex and LLM calls dominate latency; no Epic 5.2 perf criterion requires O(1) Put — added state not justified at LOW) | Maintain a running total-size counter and skip the directory scan when it is under the cap, or evict only on a periodic/threshold basis | PERFORMANCE | 30 | execute-epic-cumulative |

### [2026-06-20] From Sprint: epic-5.0

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/report/render.go:317 | The "File not found" warning format string is duplicated across internal/reconcile/emit.go (writeFindingsList) and internal/report/render.go (writePathWarning) in separate packages | Extract a shared constant/helper only if a common low-level rendering package emerges; a cross-package dependency is not justified for one format string today (Won't-fix 2026-06-21: two independent format strings across packages with no shared dependency; the recorded fix confirms extraction is not justified at this scope) | CROSS_CUTTING | 15 | execute-epic-cumulative |
| U | [/] | MEDIUM | internal/reconcile/reconcile.go:26 | Validation root is hardcoded to "." at every call site, so "atcr reconcile <path>" for a review of another repo, or running from a non-repo-root CWD, falsely flags every finding as "file not found" | Thread the reviewed repo root explicitly or add a --repo flag, applied consistently with the verify stage which uses the same "." convention (Deferred 2026-06-21: the narrow Root: os.Getwd() variant is a no-op — filepath.Abs(".") already equals the CWD, so it would not fix the non-repo-root / other-repo case; the real fix is to plumb the reviewed-repo path explicitly via a --repo flag threaded through the reconcile and verify call sites, est 60) | EDGE_CASES | 60 | execute-epic-independent |

### [2026-06-20] From Sprint: 4.7.1_backup-swap-hardening

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 3 | [/] | MEDIUM | internal/fanout/reviewdir.go:318 | backupExisting() unconditionally RemoveAll's <path>.bak.old and <path>.bak.new at entry, but forceBackupOutputDir() only calls guardForeignBackup(dir+".bak") — the two staging siblings are not guarded. On an arbitrary --output-dir a user who owns <output-dir>.bak.old or <output-dir>.bak.new has it silently deleted by --force, re-opening the Epic 4.7 "never silently delete user data" contract. NOTE: epic clarification Q4 deliberately scoped the guard to .bak only (atcr-internal suffixes); this finding re-raises that decision as worth reconsidering, not a defect against the recorded scope. (intent_note: deferred per epic §Clarifications Q4 — extending guardForeignBackup to .bak.old/.bak.new is out of scope) (Won't-fix 2026-06-20: binding Epic 4.7.1 Q4 — guard stays scoped to .bak; .bak.old/.bak.new are atcr-owned staging names cleared by entry-time RemoveAll; confirmed via /sprint-clarification 95%) | In forceBackupOutputDir() (reviewdir.go:440) extend the guard to guardForeignBackup(dir+".bak.old") and guardForeignBackup(dir+".bak.new") before backupExisting, or refuse if either exists non-empty. Add a test pre-creating a foreign <output-dir>.bak.old with user content and assert --force errors instead of deleting it. | security | 60 | code-review | claude | MEDIUM |
| 3 | [/] | MEDIUM | internal/fanout/reviewdir.go:373 | The advertised "an interrupted swap never leaves the user with neither generation" invariant is conditional on a best-effort SILENT restore succeeding. If restorePriorBackup's os.Rename(backupOld,backup) fails (e.g. backup partially recreated, or perms change), the only surviving copy is stranded under .bak.old — which the very next run's entry-time RemoveAll(backupOld) (line 318) then deletes. Same pattern at the copy site swapStagedBackup (atomic.go:159-167) feeding atomic.go:97 RemoveAll(bakOld). (Won't-fix 2026-06-20: observability half done (restore failure now logged); the lone-.bak.old-as-generation redesign is rejected per Epic 4.7.1 Q3 — .bak.old is atcr-owned temp deleted at entry, one-generation contract + TestBackupExisting_CleansStaleStagingStragglers stand; confirmed via /sprint-clarification 95%) | Surface restore failures loudly (log/return) instead of dropping them, and/or do not auto-RemoveAll .bak.old at entry when .bak is absent — treat a lone .bak.old as the surviving generation to recover. Add a test where restore fails and a subsequent run is asserted not to delete the only surviving copy. | correctness | 120 | code-review | claude | MEDIUM |
| 3 | [/] | LOW | internal/fanout/reviewdir.go:388 | backupCrossDevice's inner os.Rename(backupNew,backup) relies on backupNew and backup sharing a filesystem, an invariant that holds only by naming coincidence (both are siblings of path). If anyone later relocates backupNew under path, the inner rename silently becomes cross-device and returns a raw EXDEV to the user. (Won't-fix 2026-06-20: same-fs invariant holds by construction — backup and backupNew are both siblings of path; a runtime guard would be unreachable dead code today and is already documented at reviewdir.go:415-419; confirmed via /sprint-clarification 90%) | Add a test that forces renameFn to return syscall.EXDEV and makes the copy fail (a copy seam, or unreadable src), staging a prior .bak first; assert the prior .bak content is restored intact, the live tree survives, and .bak.new is cleaned up. Cover the copy-failure leg at minimum. | testing | 120 | code-review | claude | MEDIUM |
| 3 | [/] | LOW | internal/fanout/reviewdir_test.go:458 | Test-coverage gaps in the failed-swap/cleanup paths: TestBackupExisting_FailedSwapPreservesPriorBak asserts no .bak.old straggler but not .bak.new; the non-ErrNotExist Lstat(backup) error branch (reviewdir.go:333-335) is untested; the entry-time RemoveAll straggler-cleanup failure legs (reviewdir.go:318-323) are untested. Each is a real error branch a regression could silently break. | Add assert.NoDirExists for src+".bak.new" at reviewdir_test.go:458; add a perms-based test forcing Lstat(backup) to fail with a non-ErrNotExist error; add a test where .bak.old cannot be removed and assert the typed "clearing stale staging backup" error. Skip on root/CI where perms are not enforced. (Partial 2026-06-21: gaps 1 (.bak.new assertion) and 3 (RemoveAll(.bak.old) failure) covered; gap 2 (non-ErrNotExist Lstat(backup)) deferred — needs an lstatFn production seam since the staging siblings share a parent dir so perms cannot isolate it) | testing | 30 | code-review | claude | MEDIUM |

### [2026-06-19] From Sprint: 4.7_idempotency

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 3 | [/] | MEDIUM | internal/fanout/reviewdir.go:284 | backupExisting() does os.RemoveAll(backup) then os.Rename(path, backup); BackupToDotBak() does os.RemoveAll(bak) then copyTree. If the rename/copy fails (cross-filesystem EXDEV when --output-dir and its .bak sibling are on different mounts, disk-full mid-copy, or SIGKILL), the single prior backup generation is already destroyed while the new backup is absent or partial. The live/original tree is preserved (good), but the one recoverable prior .bak is lost for no benefit — counter to the safe-to-retry goal. (disagreement: LOW vs MEDIUM) (Deferred: Epic Plan 4.7.1) | Stage the new backup first: copy/rename into <path>.bak.new (or rename old .bak aside to .bak.old), confirm it is complete, then swap and only then remove the old generation. For backupExisting, attempt os.Rename before RemoveAll so a failed rename leaves the old .bak intact; detect EXDEV and fall back to copy+remove. Add a fault-injection test asserting the prior .bak survives a failed swap. | correctness | 120 | code-review | code-reviewer, claude | HIGH |

### [2026-06-19] From Sprint: 4.5_circuit_breaker

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 2 | [/] | MEDIUM | internal/fanout/metrics.go:37 | recordAgentOutcome zeroes apiCalls for any result whose error unwraps to context.DeadlineExceeded/Canceled, assuming no request was made. But a per-agent timeout routinely fires AFTER real HTTP round-trips (and mid tool-loop after several Chat turns already hit the wire), so atcr_api_calls_total undercounts real provider traffic exactly when a provider is degraded. The no-request assumption is only provably safe for CircuitOpenError and pre-first-send cancellation. (Deferred: Epic Plan 4.11) | Use max(1, r.Turns) for the deadline case instead of a flat 0, or thread a real calls-attempted counter out of the client rather than inferring from the terminal error class. Keep the apiCalls=0 path only for CircuitOpenError. | performance | 60 | code-review | claude | MEDIUM |

### [2026-06-18] From Sprint: epic-4.3

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| 1 | [/] | LOW | internal/validation/validation.go:62 | System-dir denylist covers only /etc, /proc, /sys, leaving /boot, /dev, /root, /var and ~/.ssh writable via --output-dir / --output (deliberate Option-B permissive choice) (Won't-fix: intended Option-B policy per epic 4.3 clarifications 2026-06-18; revisit only on a concrete isolation requirement) | If stronger isolation is later required, switch to an allowlist anchored at the repo/.atcr root instead of a denylist | SECURITY | 30 | execute-epic-independent |
| 1 | [/] | LOW | internal/validation/validation.go:86 | Severity and Enum validators are shipped but wired to nothing (ParseSeverity/ValidFormat remain the live paths); they exist only to satisfy AC5/AC7 and future use (Won't-fix: intentionally public for AC5/AC7 per epic 4.3 clarifications; deletion breaks ACs, wire-in out of scope) | Revisit and delete if no caller adopts them within a release, or wire them in where duplication can be removed | OVER_ENGINEERING | 15 | execute-epic-independent |

### [2026-06-18] From Sprint: epic-4.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/registry/attribution.go:55 | attribute() recurses and reconstructs the join even for a single-error errors.Join (which still satisfies Unwrap() []error), a small avoidable cost on the load-time validation path | Optionally short-circuit when len(children)==1 if profiling ever flags it (WON'T-FIX 2026-06-18: trigger unmet — error-path only, never hit on normal load; no perf AC in epic 4.2) | INTEGRATION | 5 | execute-epic-independent |

### [2026-06-18] From Sprint: epic-4.1.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | MEDIUM | internal/fanout/review.go:361 | If a server shutdown (or CLI SIGINT) fires after all agents already succeeded but before ExecuteReview's interrupted := errors.Is(ctx.Err(), context.Canceled) check, a fully-completed run is stamped Interrupted=true and status.go:216 overrides RunCompleted to RunInterrupted (a false interrupted; inverse of AC4). Pre-existing in the CLI-shared path, newly reachable via MCP shutdown. | Gate the interrupted marker on at least one agent ending in StatusTimeout/cancelled rather than purely on parent ctx.Err()==Canceled. NOTE: touches CLI-shared review.go (out of scope for epic 4.1.2's MCP-only change); window is microscopic and outcome benign (resume no-ops a complete run) - separate design. (WON'T-FIX 2026-06-18: --resume self-healing via ClearInterrupted (resume.go:220) already recovers a stale interrupted-on-complete; revisit in a backlog sprint if insufficient) | CORRECTNESS | 30 | execute-epic-independent |

### [2026-06-17] From Sprint: epic-4.1

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | MEDIUM | internal/mcp/handlers.go:88 | Serve-mode background fan-out runs under context.WithoutCancel(ctx) so a SIGINT to the MCP server never cancels or marks an in-flight detached review interrupted; it is allowed to finish (intended MCP design) but never gets the interrupted marker CLI mode promises. (Deferred: Epic Plan 4.1.2) | Decide whether detached MCP reviews should be marked interrupted on server shutdown; if so, thread a cancellable/interrupt-aware context or post-hoc marker into the background review path — a separate design from this CLI-focused epic. | REGRESSION_RISK | 60 | execute-epic-independent |

### [2026-06-17] From Sprint: 4.0_structured_logging

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [/] | HIGH | cmd/atcr/review.go:172 | Both review entry points call `NewRedactor(root)` with ZERO configured secrets — cmd/atcr/review.go:172 and internal/mcp/handlers.go:87 pass only the path root, never the registry API keys. The exact-value secret-scrubbing loop in Redact is therefore dead in production; redaction relies entirely on the `sk-`/`Bearer` regexes, which miss raw provider keys that lack those prefixes (Google `AIzaSy...`, Azure `api-key`, JWTs `eyJ...`). The AC5 "no API key in log output" guarantee holds only for sk-/Bearer-shaped keys; the passing integration test likely uses an sk- key, masking the gap. (Deferred: Epic Plan 4.9 secret-value-redaction) | Thread resolved registry API key values into `NewRedactor(root, keys...)` at both review.go:172 and handlers.go:87 (keys are discoverable from prep.Slots / cfg.Registry). Add an integration test using a non-sk-shaped key (e.g. `AIzaSy...`) asserting it is redacted. | security | 120 | code-review | claude | MEDIUM |

### [2026-06-16] From Sprint: 3.5_severity-rank-consolidation

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:18 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestMergeNormalizesMixedCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:30 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestGrayZoneNormalizesMixedCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:42 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestMergeNoDisagreementOnCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |

### [2026-06-16] From Sprint: epic-3.5

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/stream/severity.go:20 | The canonical SeverityRank map carries a read-only-after-init invariant in a comment only, with no test guard preventing a future caller from mutating the shared map (which would race across concurrent fan-out agents). (Won't fix: structural guard — reconcile copy-on-init at merge.go:29-31 means no consumer writes stream.SeverityRank directly; grep confirms zero write sites across consumers. Snapshot test trips the over-simplification gate; Rank() accessor cascades to 14+ direct-lookup sites — both out of pure-consolidation scope.) | Add a stream test that snapshots the map and asserts it is unchanged after consumers run, or wrap it behind a Rank(sev) accessor. | OBSERVABILITY | 20 | execute-epic-independent |

### [2026-06-16] From Sprint: 3.4_scorecard-diagnostics-writer

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:118 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; the diagnostic already routes to the injectable writer, only fmt.Fprintf's own return is dropped; propagating it breaks Emit's never-fail contract) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:199 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; Append loop already surfaces firstErr; propagating the Fprintf return breaks the best-effort contract) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:235 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; verdictTallies already writes to injectable w; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:248 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; verification-read-failed diagnostic writes to injectable w; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:276 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; orphan-verdict diagnostic routes to injectable w, locked by TestEmit_OrphanVerdictDiagnosticRoutesToDiagWriter) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | LOW | internal/scorecard/scorecard_test.go:291 | Epic 3.4 added unit tests proving EmitOpts.Diag / ReadOpts.Writer routing at the scorecard layer, but there is no test asserting the wiring itself — that the MCP handler passes a non-default writer or that the three CLI entry points pass cmd.ErrOrStderr(). This plumbing can silently regress (a future refactor swaps cmd.ErrOrStderr() back to a default and no test fails). (Deferred: Epic Plan 3.6 scorecard-wiring-tests) | Add a CLI-level test that drives a scorecard command against a deliberately-malformed store and asserts the read diagnostic reaches the command's ErrOrStderr buffer; once MCP diagnostics route through e.log, add a handler-level test asserting against a captured logger buffer. | testing | 60 | code-review | claude | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:114 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; over-long-line warning uses injectable w via diagWriter; read continues; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:145 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; decodeRecord writes to injectable w then returns (Record,bool) — no error cascade) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:155 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; schema-version skip writes to injectable w; identical to malformed-record path) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | LOW | internal/scorecard/store.go:194 | Redundant call to diagWriter (Wontfix: FALSE POSITIVE — diagWriter is the required typed-nil guard for the nil-able opts.Writer interface, not redundant; removing it reintroduces the panic fixed by commit 476c6d1) | Remove diagWriter call and use opts.Writer directly since ReadRecords already resolves it | performance | 2 | code-review | otto | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:211 | Error from diagWriter is silently discarded (Wontfix: FALSE POSITIVE — `_, _ = fmt.Fprintf` is the documented best-effort never-panic diagnostics contract at store.go:22-24; returning the write error would regress a successful read on a broken sink and logging is circular; confirmed working as designed via /sprint-clarification 97%) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| U | [/] | MEDIUM | internal/mcp/handlers.go:220 | The MCP engine carries a structured *slog.Logger (e.log) used for every other diagnostic in handleReconcile (e.g. the require_verified warning at line ~225), but scorecard diagnostics are routed to raw os.Stderr, so MCP-path scorecard write-failures/malformed-record/orphan-verdict warnings emit as unstructured plaintext that bypasses the logger's level filtering, formatting, and sink redirection. NOTE: this was a DELIBERATE, documented epic decision (Clarifications Q2: supply os.Stderr at the call site; adapting e.log to an io.Writer was explicitly OUT of scope), so this is enhancement debt, not a regression. (intent_note: deferred per epic Clarifications Q2 — adapting e.log to an io.Writer is out of scope) (Wontfix: ACCEPTED ENHANCEMENT DEBT — handlers.go:220 is an unconditional EmitOpts{Diag: os.Stderr} call, not deferred logic; all five Epic 3.4 ACs are met and the comment at handlers.go:214-219 satisfies AC4; e.log→io.Writer adaptation is out of scope per Clarifications Q2; confirmed via /sprint-clarification 97%) | If MCP observability is later desired, adapt e.log into an io.Writer shim (slog-backed at Warn level) and pass it as Diag instead of os.Stderr, so MCP-path scorecard diagnostics flow through the same structured pipeline as the rest of the handler. Defer unless/until structured MCP diagnostics are required. | error-handling | 30 | code-review | claude | MEDIUM |

### [2026-06-15] From Sprint: 3.3_per-run_scorecard

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| U | [/] | LOW | internal/scorecard/store.go (FindByRunID adjacent-month notice; ReadRecords malformed-line notice); internal/scorecard/scorecard.go (Emit notices) | Store/emit diagnostics ("skipping malformed record", "run spans adjacent month files", "write failed") are written directly to the process-global `os.Stderr` rather than to a writer threaded from the cobra command (`cmd.ErrOrStderr()`). (Deferred: Epic Plan 3.4) | accept an `io.Writer` (or logger) on the store/emit entry points and have the CLI pass `cmd.ErrOrStderr()`, so warnings are capturable and redirectable. | ops | 0 | execute-sprint | execute-sprint | MEDIUM |

### [2026-06-14] From Sprint: 3.2_disagreement_radar

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| U | [/] | LOW | internal/reconcile/disagree.go:280 | Duplicate writeRadarSection rendering logic risks divergent escaping across packages (Deferred: .planning/epics/active/7.2_radar-renderer-consolidation.md) | Consolidate radar rendering into report package; reconcile should call report.writeRadarSection | security | 20 | code-review | greta | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/disagree.go:350 | Duplicated radar section rendering logic (Deferred: Epic Plan 7.2) | Extract shared writeRadarSection to a common package | maintainability | 10 | code-review | bruce | MEDIUM |
| 4 | [/] | MEDIUM | internal/reconcile/disagree.go:354 | Duplicated radar markdown rendering diverges (Deferred: Epic Plan 7.2) | Extract shared writer or make reconcile use report package's escTrunc | maintainability | 10 | code-review | bruce | MEDIUM |
| U | [/] | LOW | internal/reconcile/disagree.go:413 | Redundant implementation of writeRadarSection (Deferred: .planning/epics/active/7.2_radar-renderer-consolidation.md) | Remove duplicate function from internal/reconcile and use internal/report | maintainability | 15 | code-review | otto | MEDIUM |
| 5 | [/] | MEDIUM | internal/report/disagree.go:47 | The radar markup is rendered by two divergent copies: writeRadarSection + formatScore in internal/report/disagree.go and a structurally different copy in internal/reconcile/disagree.go:389. They are intentionally not identical (report copy truncates via escTrunc + uses joinReviewers/reviewerOrUnknown; reconcile copy uses uncapped esc + joinOrNone), so a future markup change (new field, reordered bullets) must be made in both or the live `atcr report` radar and the archival reconciled/report.md silently drift. (Deferred: .planning/epics/active/7.2_radar-renderer-consolidation.md) | Extract one shared item renderer parameterized by a truncate-vs-verbatim flag and heading prefix; have both call sites delegate. Add a test that diffs the rendered markup of both paths on the same DisagreementsFile asserting the only intended difference is truncation. | correctness | 60 | code-review | bruce, claude | HIGH |

### [2026-06-14] From Sprint: 2.2_code_review_fanout_hardening

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| U | [/] | MEDIUM | internal/fanout/postprocess.go:14 | The severity-rank rubric {CRITICAL:4,HIGH:3,MEDIUM:2,LOW:1} is independently redefined in fanout/postprocess.go:17, reconcile/merge.go, verify, report, plus a set-form copy reviewSeverities in registry/config.go. postprocess looks up severityRank[strings.ToUpper(...)] while reconcile looks up the raw value, so a future severity change or non-canonical casing silently desyncs fan-out truncation from reconcile merging. The postprocess copy was newly added by Epic 2.2. (disagreement: LOW vs MEDIUM) (Deferred: Epic Plan 3.5) | Extract a single canonical severity package (or export from internal/stream) exposing the ordered rank map plus normalizeSeverity, and have registry/fanout/reconcile/verify/report consume it. Verify by deleting the local maps and confirming the suite passes with one source of truth. | maintainability | 120 | code-review | claude | MEDIUM |

### [2026-06-14] From Review: llmclient OpenAI-compatible tool handling

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| U | [/] | LOW | internal/llmclient/chat.go:1 | No provider-conformance test matrix for the OpenAI-compatible surface. The client deliberately absorbs real wire divergence (string-encoded vs raw-object tool_call `arguments`, lenient finish_reason) but is exercised only against synthetic fixtures, so a regression against a specific provider's actual tool_call shape (OpenAI, litellm, Ollama, vLLM, Together) would not be caught. This is the robustness the official SDK is assumed to provide, achievable here without adopting it. | Add a recorded-fixture conformance suite: capture a real `tool_calls` response from each target provider and assert the parser (`ToolCallArguments`, `chatToolResponse` decode, finish_reason handling) yields identical engine-facing results. NOTE: scope is a few days, not a quick-win — consider promoting to a standalone test-remediation plan rather than resolving inline. | testing | 480 | review | claude | LOW |

### [2026-06-14] From Sprint: 3.0_adversarial_verification

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| 7 | [/] | LOW | internal/verify/pipeline.go:238 | The rich VerificationResult built in verifyFinding never populates TrippedBudgets (invokeSkeptic folds tripped budgets only into free-text Notes), base.Model is hard-coded to skeptics[0].Config.Model even when another skeptic produced the winning verdict, and the skip-already-verified rebuild (pipeline.go:199) re-synthesizes records from the compact on-disk block losing Model/DurationMs/TrippedBudgets — so verification.json's structured audit fields degrade or misattribute on multi-skeptic and no-op re-runs (extends the model-attribution gap of TD-011). Also: Model is empty on the no_eligible_skeptic / tool_harness_unavailable early-return paths. [intent: deferred per sprint-plan TD-011] | Thread the tripped-budget slice and the winning skeptic's model up from invokeSkeptic into base.TrippedBudgets/base.Model (join models when multiple voters agree, mirroring joinSkeptics), and carry Model/DurationMs/TrippedBudgets forward for skipped findings instead of synthesizing a lossy record. | correctness | 120 | execute-sprint | execute-sprint, claude | HIGH |

### [2026-06-13] From Sprint: 2.0_tool_using_reviewers

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| 5 | [/] | LOW | internal/tools/snapshot.go | AC 03-02 Scenario 5 and AC 03-03 Scenarios 4-5 require `manifest.json` `stages.review` to record `snapshot_mode` (live/worktree), `head_sha`, and `snapshot_worktree_path`. (intent_note: deferred per sprint-plan §2.5.A (manifest review-stage recording is Phase 5 work); Deferred to Epic Plan 2.1) | when wiring `SnapshotFor` into the agent loop, record `snapshot_mode`/`head_sha`/`snapshot_worktree_path` into `internal/payload/manifest.go` review stage and add the manifest assertion tests from AC 03-02/03-03. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
