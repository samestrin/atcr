# Technical Debt Tracking

This file is a staging area for small technical debt items discovered during development. Items are triaged and moved to individual sprint documents in `sprints/` as they are prioritized.

## Stats

| Severity | Open | Deferred | Resolved |
|----------|------|----------|----------|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 1 | 0 | 0 |
| MEDIUM | 2 | 0 | 10 |
| LOW | 10 | 2 | 17 |


**Last Modified:** 2026-06-14 | **Open Items:** 13 | **Deferred Items:** 2 | **Resolved Items:** 27 | **Total Items:** 42

## Directory Structure

```
technical-debt/
├── README.md                    # This file (staging area)
├── CLAUDE.md                    # AI assistant guidelines
└── sprints/
    ├── active/                  # Currently being addressed
    ├── pending/                 # Prioritized, not yet started
    └── completed/               # Resolved items
```

## How to Use

1. **Small items**: Add to this README under "Staging Area" below
2. **Larger items**: Create a new document in `sprints/pending/`
3. **During sprint planning**: Move items from pending to active
4. **After resolution**: Move items from active to completed


### [2026-06-14] From Sprint: 3.0_adversarial_verification

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 4 | [x] | MEDIUM | cmd/atcr/review.go:89 | The one-shot review gate resolves its threshold via failOnThreshold (flag-only), whereas standalone atcr reconcile uses resolveGateThreshold (flag > project-config fail_on > registry fail_on). A repo with fail_on:HIGH configured is gated by atcr reconcile but NOT by atcr review (no --fail-on flag), so review silently exits 0 on findings the configured gate should fail; the two entry points disagree on what "the gate" is, and --require-verified's precondition (review.go:104-111) keys off the same wrong source. | Have runReview call resolveGateThreshold(cmd) instead of failOnThreshold(cmd) so configured project/registry fail_on participates on the review path (the resolver returns "" when nothing is configured, so the threshold!="" or verifyFlag guard still holds). Add a test with config fail_on:HIGH and no flag asserting review gates. | correctness | 60 | code-review | claude | MEDIUM |
| 4 | [x] | LOW | cmd/atcr/reconcile.go:95 | verifyStageRan accepts directories named verification.json | Add !info.IsDir() check after os.Stat | correctness | 2 | code-review | bruce | MEDIUM |
| 4 | [x] | MEDIUM | cmd/atcr/review.go:198 | boolFlag silently swallows flag lookup errors | Check error and fail on undefined flag names | correctness | 3 | code-review | bruce | MEDIUM |
| 4 | [x] | LOW | cmd/atcr/verify.go:72 | A non-`ErrNoReconciledFindings` failure from `verify.Verify` is mapped to a bare `usageError(err)` by `atcr verify`, but wrapped as `usageError(fmt.Errorf("review failed: %w", err))` by `atcr review --verify` — same underlying failure, two different stderr message shapes for scripts keying on text. Both still map to exit 2. [intent: deferred per sprint-plan TD-012] | Route both through one shared `verifyFailureError` helper so the non-reconciled-findings failure reads identically regardless of entry point. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 5 | [x] | LOW | internal/reconcile/emit.go:73 | JSONFindings() converts Merged -> JSONFinding but omits the Verification field, and Merge() (merge.go:73) never populates Merged.Verification, so on the standalone atcr reconcile / MCP reconcile path both res.Findings[].Verification and the emitted findings.json carry nil verdicts. Today this is benign — the verify gate reads the ReEmitFindings output via CountFailingJSON (correct) and standalone reconcile runs before verify (the --require-verified-without-verify case is already tracked as the gate.go:80 item). But the moment Merged.Verification is ever populated (the reserved Epic 3.0 contract at merge.go:44), findings.json would silently drop it. | Add Verification: m.Verification to the JSONFinding literal in JSONFindings(), and add a round-trip test asserting a Merged with a non-nil Verification survives RenderJSON/ReadReconciledFindings, so the reserved field is wired before any consumer relies on it. | correctness | 30 | code-review | claude | MEDIUM |
| 5 | [x] | MEDIUM | internal/reconcile/gate.go:80 | `CountAtOrAbove(..., requireVerified=true)` excludes every finding whose `Verification` is nil, so if the verify stage never ran a real CRITICAL yields count 0 and CI passes — the strictest gate becomes the most permissive when verdicts are absent. [intent: deferred per sprint-plan TD-004 — Phase 4 added a --verify flag guard at review.go:109, but the manifest-Stages/verification.json existence check remains open] | Refuse `--require-verified` unless the manifest `Stages` contains "verify" (or `reconciled/verification.json` exists), failing fast with a "verification has not run" usage error rather than silently passing. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 5 | [x] | MEDIUM | internal/reconcile/gate.go:86 | IsFailing normalizes severity but not threshold | Normalize threshold with strings.ToUpper/TrimSpace before AtOrAbove | correctness | 5 | code-review | bruce | MEDIUM |
| 5 | [x] | LOW | internal/reconcile/merge.go:143 | The gate predicate `IsFailing` normalizes the out-of-scope category, but `modalCategory`, the summary `out_of_scope` count (reconcile.go:80), and the report out-of-scope section (emit.go:182) still compare `category == CategoryOutOfScope` exact-match, so a non-canonical casing (`Out-Of-Scope`) is excluded from the gate but mis-counted in summary.json and mis-rendered in report.md. [intent: deferred per sprint-plan TD-014] | Canonicalize category once at parse or in `modalCategory` (lower+trim) so the gate, the summary out_of_scope count, and the report out-of-scope section all agree; add a fixture with non-canonical category casing. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 6 | [x] | MEDIUM | internal/report/render.go:93 | In v2 mode "Total findings: N" prints len(findings) including refuted, but the main ## Findings body demotes every refuted finding into the collapsed Refuted <details> section, so the headline total exceeds the count of findings actually listed with no "of which M refuted" reconciliation. A reader summing visible entries never reaches the stated total. Distinct from the unknown-severity grid skip (TD-015). | Annotate the total (e.g. "Total findings: N (M refuted, shown below)") or print the main-list count alongside the grand total so the headline and the visible list reconcile. Add a fixture with at least one refuted finding asserting the parenthetical. | maintainability | 30 | code-review | claude | MEDIUM |
| 6 | [x] | LOW | internal/report/render.go:109 | The main-list severity headers are driven by f.Severity != lastSev over findings in arrival order; renderMarkdown never sorts, trusting reconcile's ordering. A hand-edited or externally-produced findings.json (which ReadReconciledFindings accepts unvalidated) that interleaves severities emits duplicate "### HIGH" headers and fragmented grouping. The grid stays correct but the human findings list becomes incoherent. | Sort the main slice by canonical severity rank before the header loop, or bucket per-severity and render in fixed order, rather than relying on upstream ordering for a view layer that also accepts external JSON. | maintainability | 30 | code-review | claude | MEDIUM |
| 6 | [x] | LOW | internal/report/render.go:166 | `writeSummaryGrid` iterates only the known severities (CRITICAL/HIGH/MEDIUM/LOW) and skips any finding whose `Severity` is outside that set, yet still counts it in "Total findings" and renders its own body header — so the grid's per-row sum can be less than the stated total. The Phase 5 VERIFIED column inherits the same skip. [intent: deferred per sprint-plan TD-015] | Add an "other" severity bucket/row (or fold unknown severities into LOW) so the grid's per-row sum always reconciles with "Total findings"; add a fixture with a non-canonical severity. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 6 | [x] | MEDIUM | internal/report/render.go:181 | The summary-grid confidence tally switch routes any unrecognized Confidence value into the LOW bucket via default, and matches VERIFIED with exact string equality (case confVerified) while isRefuted/writeSkepticBlock use strings.EqualFold. A finding with empty, mixed-case ("Verified"/"High"), or garbage confidence — which ReadReconciledFindings accepts without validation — is silently miscounted as LOW, and a non-canonical "Verified" is dropped from the VERIFIED column though the refuted/skeptic paths handle it. The gate normalizes casing precisely to defend against this; the report grid does not. | Normalize f.Confidence (ToUpper+TrimSpace via one shared canonicalize helper used by the grid, isRefuted, and writeSkepticBlock) before the switch, and add an explicit "other" bucket instead of silently folding unknowns into LOW. Add a fixture with mixed-case/unknown confidence. | maintainability | 30 | code-review | claude | MEDIUM |
| 6 | [x] | LOW | internal/report/render.go:313 | `joinReviewers` joins reviewer names with ", " and the whole string is `esc()`'d at the call site, so a reviewer name containing a comma renders ambiguously — a reader cannot tell where one name ends and the next begins. Not an injection (escaping is applied to the joined result); purely a display-correctness ambiguity. [intent: deferred per sprint-plan TD-016] | Escape/join reviewer names per-element (or use a non-comma delimiter) if comma-bearing names ever become legitimate; otherwise document the no-comma assumption in `joinReviewers`. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [ ] | LOW | internal/verify/verify_test.go:111 | itoa reinvents strconv.Itoa | Replace with strconv.Itoa | maintainability | 1 | code-review | bruce | MEDIUM |
| 7 | [x] | LOW | internal/verify/emit_findings.go:34 | `ReEmitFindings` matches verdicts by exact `FindingKey{File,Line,Problem}`, but `Merge` sets `Problem` to the longest variant across a cluster, so a verdict whose key was built from a pre-merge or skeptic-echoed problem string silently fails to attach (finding keeps v1 confidence, nil Verification) with no diagnostic. [intent: deferred per sprint-plan TD-007] | Build keys from the same findings slice and/or log/return a count of verdicts that matched zero findings so a key-construction mismatch is visible rather than silent. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [x] | MEDIUM | internal/verify/emit_findings.go:41 | ReEmitFindings computes confidenceV2(findings[i].Confidence, verdict) from the CURRENT confidence, not the original v1 tier. On a --fresh re-run a previously-confirmed finding already carries Confidence="VERIFIED"; if the new verdict is unverifiable/empty it hits confidenceV2's default branch and passes "VERIFIED" through unchanged, so a finding a fresh skeptic could no longer confirm stays permanently VERIFIED. The v1 baseline is destroyed on the first write and unrecoverable. | Persist the original v1 confidence in the verification block (or a separate field) and compute v2 from that immutable baseline on every re-emit; or enforce single-pass semantics and reject re-running over a file already containing VERIFIED blocks. Add a --fresh re-run test: confirmed-then-unverifiable must not remain VERIFIED. | error-handling | 60 | code-review | claude | MEDIUM |
| 7 | [x] | LOW | internal/verify/emit_manifest.go:46 | UpdateManifestStage round-trips the manifest through the typed payload.Manifest struct via payload.WriteManifest, which does not preserve manifest JSON fields absent from the struct — unlike UpdateSummaryVerdicts which decodes to map[string]any to keep unknown fields. The asymmetry is latent data loss: summary is round-trip-safe, manifest silently drops any future/unknown field on every verify run. | Decode the manifest to a generic map and set only the stages key (matching emit_summary's field-preserving approach), or document that payload.Manifest is the complete manifest schema and enforce it. | error-handling | 30 | code-review | claude | MEDIUM |
| 7 | [x] | LOW | internal/verify/emit_summary.go:31 | UpdateSummaryVerdicts preserves unknown fields by decoding summary.json into map[string]any, but json.Unmarshal decodes every existing numeric field to float64; re-marshaling large integers (epoch timestamps, est_minutes totals, int64 counts) round-trips through float64 and can lose precision or emit scientific notation, corrupting fields the stage promises to leave untouched. | Decode with json.Decoder.UseNumber() so existing numeric fields round-trip as json.Number and re-serialize byte-identically. Add a test with a large integer field in summary.json asserting it survives unchanged. | error-handling | 30 | code-review | claude | MEDIUM |
| 7 | [x] | LOW | internal/verify/emit_verification.go:89 | VerifiedAt is formatted with time.RFC3339 (whole-second precision), so two verify runs within the same second produce identical verifiedAt stamps, defeating any consumer ordering runs by it; the per-finding DurationMs is millisecond-precise, an inconsistency. | Use time.RFC3339Nano (or add a monotonic run id) so the file-level stamp is unique and orderable per run. | maintainability | 5 | code-review | claude | MEDIUM |
| 7 | [x] | MEDIUM | internal/verify/emit_verification.go:101 | The verify-stage `writeFileAtomic` (and peers `internal/reconcile/emit.go:314`, `internal/payload/manifest.go:120`) omit `tmp.Sync()` before `Close()` and never fsync the parent dir after `os.Rename`, so a power loss / kernel crash can leave a truncated or stale findings.json / verification.json / manifest.json. [intent: deferred per sprint-plan TD-005] | Factor the three duplicated `writeFileAtomic` copies into one shared helper that calls `tmp.Sync()` before close and fsyncs the containing directory after rename. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [x] | LOW | internal/verify/emit_verification.go:108 | On SIGKILL the `defer os.Remove(tmpName)` never runs, leaving `.<name>.tmp-*` files in `reconciled/` and the review-dir root that accumulate as litter across killed runs. Impact is low: readers open exact filenames so orphans are never consumed. [intent: deferred per sprint-plan TD-006] | Add a best-effort sweep of stale `.*.tmp-*` in the target dir at start of a run via the shared atomic-writer helper, or document the orphan as accepted. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [x] | LOW | internal/verify/pipeline.go:147 | When the snapshot/dispatcher cannot be built, `runVerify` writes the `buildDispatcher` error verbatim to stderr, which can include the absolute `repoRoot`/snapshot path and git internals captured into CI logs. [intent: deferred per sprint-plan TD-013] | Surface a generic "tool harness unavailable" line to stderr and log the path-bearing detail at debug level only. | ops | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [x] | MEDIUM | internal/verify/pipeline.go:159 | `runVerify` iterates findings (and the skeptics within each finding) strictly serially with no goroutines/errgroup/concurrency cap, so a `--thorough` 50-finding × 3-skeptic verify is 150 sequential LLM round-trips (minutes of wall-clock) with no knob to speed it up. [intent: deferred per sprint-plan TD-009] | Process jobs through a bounded worker pool honoring a `--max-parallel`-style limit (reuse fanout's semaphore pattern), preserving deterministic artifact order by keying results back to findings rather than append order. | performance | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [x] | MEDIUM | internal/verify/pipeline.go:167 | `runVerify` writes findings.json (`ReEmitFindings`), then verification.json, the manifest stage, and summary verdictCounts as four separate atomic-per-file steps, so a failure after `ReEmitFindings` leaves findings.json mutated while verification.json/summary.json are stale or absent — an inconsistent on-disk reconciled tree. [intent: deferred per sprint-plan TD-010] | Stage all reconciled artifacts to temp files and rename them as a group, or build verification.json from the in-memory verdict map to drop the second findings.json read, folding into the shared atomic-writer work. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [x] | LOW | internal/verify/pipeline.go:229 | Result.FindingsProcessed is len(jobs), but jobs includes findings with no eligible skeptic and findings processed while the tool harness was unavailable (which emit no_eligible_skeptic / tool_harness_unavailable without a skeptic actually investigating). The CLI/MCP summary's "findings sent through verification" therefore overcounts versus reality. | Compute FindingsProcessed as the count of jobs with a non-empty eligible-skeptic list AND a live dispatcher, or rename/redocument the field to "findings considered". Add a test with a no-eligible-skeptic finding asserting it is not counted as processed. | maintainability | 30 | code-review | claude | MEDIUM |
| 7 | [/] | LOW | internal/verify/pipeline.go:238 | The rich VerificationResult built in verifyFinding never populates TrippedBudgets (invokeSkeptic folds tripped budgets only into free-text Notes), base.Model is hard-coded to skeptics[0].Config.Model even when another skeptic produced the winning verdict, and the skip-already-verified rebuild (pipeline.go:199) re-synthesizes records from the compact on-disk block losing Model/DurationMs/TrippedBudgets — so verification.json's structured audit fields degrade or misattribute on multi-skeptic and no-op re-runs (extends the model-attribution gap of TD-011). Also: Model is empty on the no_eligible_skeptic / tool_harness_unavailable early-return paths. [intent: deferred per sprint-plan TD-011] | Thread the tripped-budget slice and the winning skeptic's model up from invokeSkeptic into base.TrippedBudgets/base.Model (join models when multiple voters agree, mirroring joinSkeptics), and carry Model/DurationMs/TrippedBudgets forward for skipped findings instead of synthesizing a lossy record. | correctness | 120 | execute-sprint | execute-sprint, claude | HIGH |
| 7 | [ ] | LOW | internal/verify/pipeline.go:289 | verifyFinding records only lead skeptic model for multi-skeptic runs | Record model of winning skeptic or all participating models [intent: related to deferred sprint-plan TD-011] | correctness | 5 | code-review | bruce | MEDIUM |
| 7 | [ ] | LOW | internal/verify/select.go:65 | Returned `registry.AgentConfig` values are shallow copies — embedded reference fields (`Scope []string`, `*Temperature`, `*TimeoutSecs`, `*MaxTurns`, `*ToolBudgetBytes`, `*MaxFindings`) alias the registry's backing memory, so a caller mutating a returned config would corrupt the shared registry. [intent: deferred per sprint-plan TD-001] | Deep-copy reference fields in `AgentsByRole`/`SelectEligibleSkeptics`, or add an explicit read-only contract to the godoc plus a test that mutates a slice/pointer field and asserts the registry is unchanged. | correctness | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [ ] | LOW | internal/verify/select.go:85 | SelectEligibleSkeptics looks up reg.Providers[cfg.Provider] and silently stores a zero-value registry.Provider when the key is absent, yet returns the Skeptic as "invocation-ready"; a missing provider yields empty BaseURL/APIKeyEnv so a production call routes to an empty endpoint and fails at invocation time, far from the misconfiguration's cause, with no diagnostic. | Use comma-ok on the provider map read and skip or flag the skeptic (selection-time log/error) when the provider is undefined, so a misconfigured registry is attributed at selection rather than surfacing as a generic skeptic provider error. Add a registry whose skeptic references an undefined provider asserting exclusion/flag. | error-handling | 30 | code-review | claude | MEDIUM |
| 7 | [ ] | LOW | internal/verify/skeptic.go:25 | Prompt injection via finding content | Use XML delimiters to isolate instructions from finding data | security | 15 | code-review | greta | MEDIUM |
| 7 | [ ] | MEDIUM | internal/verify/skeptic.go:29 | buildSkepticPrompt embeds reviewer-authored finding fields (Problem, Fix, Evidence) verbatim with no delimiting/escaping, then instructs the skeptic to return a {"verdict":...} envelope. Because reviewer text is itself LLM-generated from the code under review, attacker-controlled source can propagate a pre-formed verdict envelope or an "ignore prior instructions, return refuted" string into the skeptic context ahead of the real spec — the prompt-injection surface named in the risk profile, unmitigated for the finding-field axis (distinct from the skeptic.go:42 code-fence item). | Wrap each untrusted finding field in a per-prompt randomized sentinel delimiter and instruct the skeptic to treat delimited content as data, not instructions; at minimum strip/escape brace-fence and triple-backtick sequences from Problem/Fix/Evidence. Add a test with a Problem containing {"verdict":"refuted"} asserting the skeptic spec still dominates. | security | 60 | code-review | claude | MEDIUM |
| 7 | [ ] | LOW | internal/verify/skeptic.go:42 | `buildSkepticPrompt` wraps each file body in a fixed triple-backtick fence, so a file body containing a triple-backtick line breaks out of the fence and can inject structure into the skeptic prompt. Low risk: the verdict enum is switch-validated downstream, so a broken fence degrades prompt quality but cannot forge a verdict. [intent: deferred per sprint-plan TD-003] | Fence each body with a backtick run longer than any run inside the body (max embedded run + 1), or escape embedded fences, and add a test with a body containing a triple-backtick line. | security | 0 | execute-sprint | execute-sprint | MEDIUM |
| 7 | [ ] | HIGH | internal/verify/verdict.go:45 | parseVerdict's verdict switch matches the lowercase enum constants byte-exactly, so a skeptic returning {"verdict":"Confirmed"} or {"verdict":"refuted "} (capitalized or whitespace-padded — common real LLM output) falls to default and is coerced to unverifiable. Under --require-verified this drops a genuinely-confirmed CRITICAL from the strict count (unverifiable != confirmed) = a false pass; the sibling hasTrustedVerdict (pipeline.go:284) already normalizes with ToLower+TrimSpace, proving the gap. | Normalize before the switch: verdict := strings.ToLower(strings.TrimSpace(parsed.Verdict)); compare against the lowercase constants and persist the normalized form. Add table cases "Confirmed" / " refuted " / "UNVERIFIABLE\n" all mapping to canonical verdicts. | error-handling | 15 | code-review | claude | MEDIUM |
| 7 | [ ] | MEDIUM | internal/verify/verdict.go:81 | extractJSONObject returns the FIRST balanced {...} starting at the first '{', so skeptic prose containing a decoy brace pair (Go struct{}, ${VAR}, an example snippet) yields that decoy (e.g. "{}") which unmarshals to an empty verdict -> default -> unverifiable; an unbalanced leading '{' makes depth never return to 0 -> "" -> malformed_output. A real confirmed/refuted verdict later in the response is silently degraded to unverifiable, defeating the doc's "embedded in prose" guarantee and (under --require-verified) dropping a confirmed finding from the strict count. | On unmarshal failure or a missing/empty verdict key, retry extraction from the next '{' after start (iterate candidate objects), or prefer the last balanced object, requiring the "verdict" key before accepting. Add tests for prose-with-decoy-braces and unbalanced-leading-brace followed by a valid envelope. | error-handling | 60 | code-review | claude | MEDIUM |
| 7 | [ ] | LOW | internal/verify/votes.go:67 | In the clear-majority branch aggregateVerdicts sets Skeptic: joinSkeptics(valid) (all voters incl. dissenters) but Notes: combineReasonings(filterByVerdict(valid, winner)) (winners only), so a reader sees a dissenting skeptic's name with no corresponding reasoning, making it look like the dissenter agreed; the disagreement is partially erased on a non-tie majority (fully preserved only on ties). | Either restrict Skeptic to joinSkeptics(filterByVerdict(valid, winner)) for consistency, or include all reasonings as the tie branch does. Add a 2-confirmed/1-refuted test asserting the refuter's name and reasoning are both present or both absent. | maintainability | 15 | code-review | claude | MEDIUM |
| U | [ ] | LOW | internal/mcp/handlers.go:434 | The registryPath containment check blocks absolute paths, any "../" substring, and the bare "..", but a balanced traversal like "a/.." or "a/b/.." slips through (Contains("a/..","../") is false and it isn't ".."); these resolve back to/inside root so there is no escape today, but the predicate is fragile string-matching rather than resolved-path containment, and a future change to the join base could turn a slip-through into a real escape. | Replace substring matching with canonical containment: filepath.Join(root, p), then verify filepath.Clean(result) is still prefixed by filepath.Clean(root)+separator — escape-proof regardless of how .. segments balance. Add tests for "a/..", "../x", and "sub/../../etc". | security | 30 | code-review | claude | MEDIUM |

### [2026-06-13] From Sprint: epic-2.2

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [ ] | LOW | internal/fanout/postprocess.go:1 | Dropped/truncated finding counts from min_severity/max_findings enforcement are logged to stderr but not persisted in the per-agent status.json | Add dropped_by_min_severity and truncated_by_max_findings counters to AgentStatus so the post-processing volume reduction is observable after the run, not just in transient stderr | observability | 30 | execute-epic-stage3 |
| U | [ ] | LOW | internal/fanout/postprocess.go:34 | Local variable `min` shadows the Go 1.21+ builtin min/max inside enforceConstraints | Rename the local to floorRank to avoid shadowing the builtin (readability) | style | 5 | execute-epic-independent |

### [2026-06-13] From Code Review: epic-2.1

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|-----------|------------|
| 3 | [x] | LOW | internal/payload/manifest.go | Unnecessary blank lines throughout file | Remove unnecessary blank lines between assertions/declarations | maintainability | 1 | ace | MEDIUM |
| 3 | [x] | LOW | internal/payload/manifest.go:65 | Missing comment explaining why SnapshotWorktreePath is not omitempty | Add comment referencing AC 03-03 Scenario 5 | maintainability | 2 | ace | MEDIUM |
| 3 | [x] | LOW | internal/payload/manifest.go:68 | Comment typo: "omitted (via omitempty)" should be "omitted (via omitempty)" | Fix typo in comment | maintainability | 1 | ace | MEDIUM |


### [2026-06-13] From Sprint: 2.0_tool_using_reviewers

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 5 | [/] | LOW | internal/tools/snapshot.go | AC 03-02 Scenario 5 and AC 03-03 Scenarios 4-5 require `manifest.json` `stages.review` to record `snapshot_mode` (live/worktree), `head_sha`, and `snapshot_worktree_path`. (intent_note: deferred per sprint-plan §2.5.A (manifest review-stage recording is Phase 5 work); Deferred to Epic Plan 2.1) | when wiring `SnapshotFor` into the agent loop, record `snapshot_mode`/`head_sha`/`snapshot_worktree_path` into `internal/payload/manifest.go` review stage and add the manifest assertion tests from AC 03-02/03-03. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
