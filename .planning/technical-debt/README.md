# Technical Debt Tracking

This file is a staging area for small technical debt items discovered during development. Items are triaged and moved to individual sprint documents in `sprints/` as they are prioritized.

## Stats

| Severity | Open | Deferred | Resolved |
|----------|------|----------|----------|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 5 | 0 | 0 |
| MEDIUM | 11 | 13 | 0 |
| LOW | 26 | 13 | 2 |


**Last Modified:** 2026-06-17 | **Open Items:** 42 | **Deferred Items:** 26 | **Resolved Items:** 2 | **Total Items:** 70

## Directory Structure

```
technical-debt/
├── README.md                    # This file (staging area)
├── CLAUDE.md                    # AI assistant guidelines
└── sprints/
    ├── active/                  # Currently being addressed
    ├── pending/                 # Prioritized, not yet started
    └── completed/               # Resolved items
```

## How to Use

1. **Small items**: Add to this README under "Staging Area" below
2. **Larger items**: Create a new document in `sprints/pending/`
3. **During sprint planning**: Move items from pending to active
4. **After resolution**: Move items from active to completed


### [2026-06-17] From Sprint: 4.0_structured_logging

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [ ] | LOW | cmd/atcr/main.go:setupLogger | `setupLogger` passes `LOG_LEVEL` to `log.New`; an invalid value yields `log: invalid level %q` (internal/log/log.go:43) which `main()` echoes verbatim to stderr. `LOG_LEVEL` is externally influenceable in some CI contexts. | emit a fixed message listing the valid level values without echoing the raw input (or cap echoed length), with a covering test | ops | 0 | code-review | execute-sprint | MEDIUM |
| 1 | [x] | LOW | cmd/atcr/main.go:newRootCmd | cobra's `--help`/`--version`/`-h` short-circuit before `PersistentPreRunE`, so no logger is stored in context for those paths. Correct today only because every consumer uses `log.FromContext`, which returns the shared discard logger on a miss. | document (package or PersistentPreRunE comment) that bypass paths rely on the discard fallback so future code keeps using `FromContext` rather than asserting logger presence | docs | 0 | code-review | execute-sprint | MEDIUM |
| 1 | [ ] | HIGH | cmd/atcr/review.go:172 | Both review entry points call `NewRedactor(root)` with ZERO configured secrets — cmd/atcr/review.go:172 and internal/mcp/handlers.go:87 pass only the path root, never the registry API keys. The exact-value secret-scrubbing loop in Redact is therefore dead in production; redaction relies entirely on the `sk-`/`Bearer` regexes, which miss raw provider keys that lack those prefixes (Google `AIzaSy...`, Azure `api-key`, JWTs `eyJ...`). The AC5 "no API key in log output" guarantee holds only for sk-/Bearer-shaped keys; the passing integration test likely uses an sk- key, masking the gap. | Thread resolved registry API key values into `NewRedactor(root, keys...)` at both review.go:172 and handlers.go:87 (keys are discoverable from prep.Slots / cfg.Registry). Add an integration test using a non-sk-shaped key (e.g. `AIzaSy...`) asserting it is redacted. | security | 120 | code-review | claude | MEDIUM |
| 1 | [ ] | MEDIUM | cmd/atcr/reconcile.go:98 | `runReconcile` passes `Diag: cmd.ErrOrStderr()` to scorecard.EmitForReconcile (and the MCP path uses `e.diagWriter()` -> os.Stderr at handlers.go:272), routing scorecard diagnostics straight to raw stderr — NOT through the context logger. scorecard docs (scorecard.go:98-106) admit these messages embed absolute ~/.config/atcr store paths (username disclosure) and raw %v error strings, so they bypass BOTH the redactor (no relativization/secret scrub) AND LOG_LEVEL (emit unconditionally). This is the task-15 incomplete criterion plus a security dimension. Path-bearing scorecard details also lack Debug-level gating. | Route scorecard diagnostics through an io.Writer adapter over the context logger (or wrap Diag with the Redactor) at reconcile.go:98 and handlers.go:272, holding path-bearing detail to Debug. At minimum reduce ~/.config/atcr store paths to base names before an unredacted sink. | security | 120 | code-review | claude | MEDIUM |
| 1 | [ ] | MEDIUM | cmd/atcr/serve.go:39 | The serve-mode engine base logger (`e.logger()`) is NOT redacted — WithRedactor is attached only per-review inside reviewContext (handlers.go:87). Any MCP server log line emitted outside a review fan-out — the panic recover (handlers.go:194), the fan-out-error log (handlers.go:198), handleReconcile's e.logger().Warn (handlers.go:277) — uses the bare unredacted logger, so secret-shaped tokens or absolute paths there bypass the redactor. The TD-007 single-sink redaction contract is honored only on the review goroutine's context, not the engine's base logger. | Construct the engine base logger already wrapped in WithRedactor(root) so EVERY engine log line is scrubbed, then layer review_id per-review. Relativization needs the absolute root, known at engine construction. | security | 60 | code-review | claude | MEDIUM |
| 1 | [ ] | LOW | cmd/atcr/review.go:169 | `redactRoot := prep.Repo` then filepath.Abs, but the Abs error is silently swallowed (`if abs, err := ...; err == nil`). If Abs fails, redactRoot stays prep.Repo which is "." (req.Repo hardcoded "." at review.go:132); relativizePaths no-ops on "." (redact.go:76), so AC6 path relativization silently does nothing for the entire review with no warning. Same swallow at handlers.go:84. | Emit a debug/warn log line when filepath.Abs fails so the silent loss of path redaction is observable rather than failing closed silently. | error-handling | 15 | code-review | claude | MEDIUM |
| 1 | [ ] | LOW | cmd/atcr/review.go:162 | review_id is bound (correlateReviewID, review.go:162) BEFORE WithRedactor wraps the handler (review.go:172), so the review_id attr value flows through redactAttr on every line. If a branch name ever contained an absolute-path-looking or sk--looking substring it would be redacted in the correlation key, breaking grep-by-review_id (AC9 vs AC5/6 tension). | Bind review_id AFTER the redactor, or exempt the review_id/agent_name correlation keys from redaction (internally generated, not secret-bearing). | maintainability | 30 | code-review | claude | MEDIUM |
| 1 | [ ] | LOW | cmd/atcr/serve_test.go:39 | golangci-lint errcheck: the return values of r.Close() (serve_test.go:39) and w.Close() (serve_test.go:40) are not checked. Lint gate is FAILING on these test-only deferred Close calls. | Assign to _ explicitly or wrap in a checked closure (e.g. `defer func(){ _ = r.Close() }()`), or add the errcheck exclusion for deferred Close in test files. | testing | 5 | code-review | claude | MEDIUM |
| 1 | [ ] | HIGH | cmd/atcr/main.go:95 | Missing input validation | Add length check | security | 5 | code-review | bruce | MEDIUM |
| 1 | [ ] | HIGH | cmd/atcr/reconcile.go:120 | SQL injection vulnerability | Use parameterized queries | security | 10 | code-review | bruce | MEDIUM |
| 1 | [ ] | MEDIUM | cmd/atcr/serve.go:45 | Error handling | Missing checks for nil return values | error-handling | 3 | code-review | bruce | MEDIUM |
| 1 | [ ] | LOW | cmd/atcr/main.go:131 | Ignoring error from GetString | Check error and return usageError if non-nil | error-handling | 2 | code-review | reviewer | MEDIUM |
| 2 | [ ] | LOW | internal/errors/errors.go:IsRetryable | `IsRetryable` resolves the outermost `*ClassifiedError` via `errors.As`. If a permanent failure (e.g. a 401) were re-wrapped as `NewTransient(NewPermanent(err))`, `IsRetryable` would report true, forging retryability and risking repeated calls against a permanent auth error. | if a double-classification path is ever introduced (e.g. a generic retry wrapper), add a "Permanent poisons the chain" guard: have constructors detect an inner non-retryable `*ClassifiedError` and refuse to escalate it to Transient, with a regression test asserting an inner Permanent stays non-retryable | security | 0 | code-review | execute-sprint | MEDIUM |
| 2 | [x] | LOW | internal/errors/errors.go:49 | `ClassifiedError.Error()` falls back to the classification label string when Err == nil (reachable only by direct struct construction, which the contract forbids). The fallback produces a message like "transient" that looks like real error text to log scrapers and callers doing string matching, papering over a programming error. | Keep the nil guard but make the fallback unmistakably diagnostic, e.g. "classified error (transient) with nil cause", so misuse is visible in logs. | maintainability | 15 | code-review | claude | MEDIUM |
| 2 | [ ] | LOW | internal/errors/errors_test.go:154 | golangci-lint staticcheck ST1012: error sentinel var `sentinel` should be named in the errFoo form (e.g. errSentinel). Lint gate FAILING. | Rename the test sentinel var to errSentinel (or similar errFoo form) to satisfy ST1012. | testing | 5 | code-review | claude | MEDIUM |
| 2 | [ ] | LOW | internal/errors/errors.go:75 | Inconsistent error handling | Error message not wrapped with context | error-handling | 1 | code-review | bruce | MEDIUM |
| 3 | [ ] | LOW | internal/fanout/review.go:296 | Five `fmt.Fprintf(os.Stderr, ...)` warnings (tool-harness/snapshot disabled, transcript dir mkdir, fallback-agent config override) still write directly to stderr, bypassing the single-sink contract and the new sink-level redaction. | replace the four ExecuteReview sites with `log.FromContext(ctx).Warn(...)` (they then ride the correlated, redacted sink); thread a build-time logger into `buildFallbackAgent`/`PrepareReview` for site 604, or accept it as a pre-correlation build warning | ops | 0 | code-review | execute-sprint | MEDIUM |
| 3 | [ ] | MEDIUM | internal/fanout/engine.go:224 | `logger()` allocates a brand-new `slog.NewTextHandler(io.Discard, nil)` wrapped in a fresh *slog.Logger on EVERY call when e.log == nil, and invokeAgent calls it once per agent (engine.go:421). internal/payload/diff.go:206 (gitRunner.log()) has the identical defect, called per-file in blocks mode. The log package already defines a shared discardLogger singleton (internal/log/log.go:19) built for exactly this purpose. | Export a `log.Discard()` accessor returning the package singleton and have both engine.go:logger() and diff.go:log() return it instead of constructing a new discard handler per call. Verify allocation count with testing.AllocsPerRun. | performance | 30 | code-review | claude | MEDIUM |
| 3 | [ ] | LOW | internal/fanout/engine.go:421 | invokeAgent does `agentLogger := log.WithAgent(e.logger(), a.Name)`. e.logger() never returns nil so this is safe today, but log.WithAgent returns nil when its input is nil; a future change making logger() returnable-nil would silently produce a nil agentLogger, then agentLogger.Debug(...) at :423 panics. The invariant is split across two functions with no test pinning it. | Make WithAgent return the discard logger instead of nil on nil input, or add a test asserting logger() is never nil so the contract dependency is explicit. | error-handling | 30 | code-review | claude | MEDIUM |
| 3 | [ ] | MEDIUM | internal/fanout/engine.go:165 | Race condition | Shared counter accessed without synchronization | correctness | 8 | code-review | bruce | MEDIUM |
| 4 | [ ] | LOW | internal/llmclient/client.go:339 | `skKeyPattern` in llmclient is `sk-\S+` (case-sensitive), the same gap fixed in `internal/log/redact.go` during 1.2.A. A `SK-`/`Sk-` shaped foreign token echoed in a provider error body would bypass the llmclient scrub. | add `(?i)` to `skKeyPattern` in client.go to match the log package, with a regression test mirroring `TestRedact_SKKeyCaseInsensitive` | security | 0 | code-review | execute-sprint | MEDIUM |
| 4 | [ ] | LOW | internal/llmclient/client.go:262-267 | The retry loop uses a fixed exponential backoff (initialBackoff * factor^attempt) and never inspects a `Retry-After` header on a 429/503 response. A rate-limited provider that advertises a cooldown can be retried before that window elapses. | parse `Retry-After` (delta-seconds or HTTP-date) from 429/503 responses and override the backoff delay when present; fall back to fixed backoff otherwise. Add a covering test asserting the honored delay | correctness | 0 | code-review | execute-sprint | MEDIUM |
| 4 | [ ] | LOW | internal/llmclient/client.go:268 | Retry backoff has no maximum cap and no jitter: `delay = delay * backoffFactor` grows unbounded, so a caller-supplied WithRetry(20, 1s, 2.0) yields a ~17-minute final sleep. The lack of jitter means many agents hitting a 429 at the same instant retry in lockstep (thundering herd), amplifying the rate-limit. Defaults are safe today but the knob is public and unguarded. | Clamp delay to a maxBackoff constant after each multiplication and add bounded jitter (delay/2 + rand(delay/2)). Add a test asserting the clamp. | performance | 30 | code-review | claude | MEDIUM |
| 4 | [ ] | LOW | internal/llmclient/client.go:260 | When maxRetries is configured negative via WithRetry(-1, ...), the loop `for attempt := 0; attempt <= c.maxRetries` never executes, lastErr stays nil, and the function falls through to return NewTransient(fmt.Errorf("exhausted retries: %w", nil)) — a transient error wrapping a nil cause with a misleading "exhausted retries" message despite zero attempts. No validation rejects nonsensical budgets. | Validate maxRetries >= 0 in WithRetry (clamp to 0 or reject), or guard the fallthrough so a zero-attempt loop returns a clear configuration error. | error-handling | 15 | code-review | claude | MEDIUM |
| 4 | [ ] | LOW | internal/llmclient/client.go:229 | When both msg.Content and msg.ReasoningContent are empty (provider returned an empty assistant message), CompleteWithUsage returns ("", usage, nil) — success with empty content. Downstream consumers cannot distinguish "model said nothing" from a silent failure; the len(Choices)==0 guard does not cover empty-content. | After the ReasoningContent fallback, if content == "" return a classified permanent/system error describing an empty completion so the caller fails loudly instead of propagating an empty review. | error-handling | 15 | code-review | claude | MEDIUM |
| 4 | [ ] | LOW | internal/llmclient/client.go:62 | The CheckRedirect guard (returning ErrUseLastResponse, preventing the Authorization: Bearer header from being forwarded to a redirect target) is set only in New(). A caller using WithHTTPClient to inject their own *http.Client silently loses this protection — Go's default client follows up to 10 redirects and re-sends the Authorization header to the redirected host, leaking the key to an attacker-controlled Location. | Re-apply the CheckRedirect guard onto any injected client in WithHTTPClient (or wrap its transport) so the no-redirect/no-header-forwarding invariant cannot be bypassed by dependency injection. | security | 30 | code-review | claude | MEDIUM |
| 4 | [ ] | LOW | internal/llmclient/client.go:373 | `readErrorSnippet` drains the remaining error body with `io.Copy(io.Discard, r)` with no byte limit. On a non-200 from a hostile/malfunctioning endpoint the body can be arbitrarily large; the snippet read is capped at 4KB but the subsequent drain reads the entire rest into discard to "enable connection reuse", blocking and consuming bandwidth indefinitely on an error path. | Bound the drain with io.CopyN(io.Discard, r, limit) or close the body without draining on the error path — connection reuse is a weak justification for an unbounded read after the size cap is already breached. | performance | 15 | code-review | claude | MEDIUM |
| 4 | [ ] | LOW | internal/llmclient/client.go:170 | `clampNonNegative` silently returns 0 for any count above 1e15. A provider reporting a genuinely large but valid token count is reported as zero tokens, which flows into cost computation as a free request — masking data rather than clamping to a maximum. | Clamp to a documented ceiling (math.MaxInt) instead of returning 0; reserve the 0 return for NaN/Inf/negative/non-numeric only. Document the behavior. | maintainability | 15 | code-review | claude | MEDIUM |
| 5 | [ ] | LOW | internal/log/log.go:New | `New` accepts a nil `io.Writer` without validation. A nil writer passes construction silently and panics only on the first log write, producing a deferred failure far from the call site. | add `if w == nil { return nil, fmt.Errorf("log: nil writer") }` before building the handler, with a covering test | correctness | 0 | code-review | execute-sprint | MEDIUM |
| 5 | [ ] | LOW | internal/log/log.go:LevelFromString | `LevelFromString` and `New` return generic `fmt.Errorf` string errors. Callers cannot distinguish "invalid level" from "invalid format" programmatically via `errors.Is`. | define exported `ErrInvalidLevel`/`ErrInvalidFormat` sentinels and wrap with `%w` if a caller ever needs to branch | correctness | 0 | code-review | execute-sprint | MEDIUM |
| 5 | [ ] | MEDIUM | internal/log/redact.go:bearerTokenPattern | `bearerTokenPattern` requires literal whitespace (`Bearer\s+\S+`), so a URL-encoded `Bearer%20<token>` appearing in a logged URL or error body is not scrubbed. | extend the pattern to `(?i)Bearer(\s+/%20)\S+` or percent-decode messages before applying token regexes, with a covering test | security | 0 | code-review | execute-sprint | MEDIUM |
| 5 | [ ] | LOW | internal/log/redact.go:Redact | Configured secrets are removed via case-sensitive `strings.ReplaceAll`; a secret echoed by an upstream provider with altered casing would not match the exact-secret pass (bearer/sk- regex only backstops those shapes, not arbitrary configured secrets). | if a provider is found to alter secret casing, fold case for the exact-secret comparison or document verbatim-only matching | security | 0 | code-review | execute-sprint | MEDIUM |
| 5 | [ ] | LOW | internal/log/correlation.go:WithReviewID | `slog.Logger.With` appends attributes rather than replacing them, so wrapping a logger that already carries `review_id`/`agent_name` emits the key twice (last-wins for most JSON parsers, but the raw line carries both). There is no handler-level dedup guard. | if double-wrap misuse appears, add a custom handler (or `ReplaceAttr` collapse) in `log.New` that enforces single-occurrence for correlation keys, with a test asserting one key after a double wrap | correctness | 0 | code-review | execute-sprint | MEDIUM |
| 5 | [ ] | HIGH | internal/log/handler.go:54 | `redactAttr` only redacts KindString, KindGroup, and error-typed KindAny. A non-error struct/slice/map attribute passed via `slog.Any("x", v)` whose String()/JSON marshaling contains a secret is returned UNCHANGED (KindAny default at handler.go:70, outer default at :72) and the base handler marshals it to the sink with the secret intact. The handler-level guarantee "no log line at any level can bypass it" is false for any non-string, non-error value type. | For KindAny that is neither error nor recognized, render via fmt.Sprint(v.Any()), run through Redact, and emit the redacted string; or document that callers must never pass secret-bearing structs as slog.Any. Add a test logging a struct containing a configured secret and asserting it is scrubbed. | security | 60 | code-review | claude | MEDIUM |
| 5 | [ ] | MEDIUM | internal/log/redact.go:48 | `Redact` scrubs configured secrets in literal and `url.QueryEscape` form only. Secrets appearing base64-encoded (Authorization: Basic base64(user:pass)), JSON-escaped, or percent-encoded via url.PathEscape bypass redaction. The risk profile anticipated "transformed secret" leaks; only one transform is covered. | Narrow the comment's "cannot leak even when transformed" claim to the forms actually handled, or add base64 and PathEscape variants to the per-secret loop. Verify with a test passing base64.StdEncoding.EncodeToString(secret) through Redact. | security | 30 | code-review | claude | MEDIUM |
| 5 | [ ] | MEDIUM | internal/log/redact.go:71 | `relativizePaths` strips only the single exact review-root prefix with a plain substring strip — no symlink resolution and no coverage of other sensitive roots. On macOS a symlink-resolved real path (/private/var/.../repo for /tmp) leaks because it lacks the root prefix; review log lines also carry paths NOT under the repo root — the scorecard store under ~/.config/atcr (contains username), temp snapshot worktrees, $TMPDIR payload paths — none relativized. AC6 is technically met for repo-root paths but username/path disclosure is only partially mitigated. | Resolve root via filepath.EvalSymlinks at NewRedactor time and strip both raw and resolved; add a second relativization pass for known sensitive roots (home dir -> ~, tmpdir -> $TMPDIR). Add a darwin test using a symlinked temp dir. | security | 60 | code-review | claude | MEDIUM |
| 5 | [ ] | MEDIUM | internal/log/redact.go:58 | The `sk-\S+` and `Bearer\s+\S+` patterns use greedy `\S+`. In JSON log format a token followed by non-whitespace punctuation (e.g. `sk-abc","key":"...`) is all non-whitespace, so `\S+` consumes the closing quote, comma, and following key — over-redacting adjacent structured fields into `[redacted]` or mangling the line. Per-attr redaction bounds cross-field bleed, but within one string value containing JSON-ish content the greedy match corrupts it. | Anchor the patterns to token charsets, e.g. `sk-[A-Za-z0-9_-]+` and `Bearer\s+[A-Za-z0-9._-]+`, instead of `\S+`. Add a test feeding a JSON-shaped line containing an sk- token and asserting only the token is replaced. | security | 30 | code-review | claude | MEDIUM |
| 5 | [ ] | MEDIUM | internal/log/handler.go:26 | Redaction runs unconditionally on the message plus every attribute on EVERY emitted record; Redact does N+2 full-string passes (one strings.ReplaceAll per configured secret, two regex ReplaceAllString, one path ReplaceAll). With several attrs this is O(secrets x attrs x msgLen) per line. There is no benchmark proving the design's <0.1ms-per-record target and no fast-path skip when the redactor has no secrets/root or the line contains no sk-/Bearer/root substring. | Add a Benchmark for Handle/Redact to prove the <0.1ms claim, and add an early return / cheap strings.Contains prefilter in Redact before each regex/ReplaceAll so common no-match lines skip the regex engine. | performance | 60 | code-review | claude | MEDIUM |
| 5 | [ ] | LOW | internal/log/handler.go:50 | `redactAttr` never redacts attribute KEYS (handler.go:53 assumes keys are never secret-bearing). slog group/attribute keys can be caller-supplied (e.g. a dynamic key derived from a URL path or header name containing a token); the blanket "keys always static" assumption is an unverified invariant for a sink that claims total enforcement. | Downgrade the claim from "enforced-by-construction, no bypass" to "values redacted; keys assumed static", and grep all slog call sites for dynamically-constructed keys to confirm. | security | 15 | code-review | claude | MEDIUM |
| 5 | [ ] | LOW | internal/log/log_test.go:200 | golangci-lint staticcheck SA4000: identical expressions on both sides of `!=` (`FromContext(context.Background()) != FromContext(context.Background())`). The assertion is intentional (verifying the cached discard-logger identity) but trips the linter, keeping the lint gate FAILING. | Restructure the identity assertion to avoid SA4000 (assign both calls to vars then compare) or add a targeted //nolint:staticcheck comment with rationale. | testing | 15 | code-review | claude | MEDIUM |
| 5 | [ ] | LOW | internal/log/log.go:42 | Dead code | Unreachable code after return statement | maintainability | 2 | code-review | bruce | MEDIUM |
| 5 | [ ] | HIGH | internal/log/correlation.go:3 | Non-exported constants used by external package | Make constants exported (Uppercase) to allow external access | maintainability | 5 | code-review | bruce | MEDIUM |
| U | [ ] | LOW | internal/verify/pipeline.go:31 | `logPipelineWarning` (pipeline.go:31) writes `key.File:key.Line` directly to os.Stderr at the default level (orphan_verdict at :307), and the harness-unavailable warning (pipeline.go:161) also writes raw os.Stderr — both bypass internal/log entirely, contradicting the single-sink and path-at-debug discipline that logSkepticFailure follows. The comment claiming it "mirrors logSkepticFailure's pattern" is false. | Thread a context logger into logPipelineWarning and the line-161 warning and emit via log.FromContext at Warn (path-bearing detail at Debug) so all verify-stage diagnostics share one sink and honor LOG_LEVEL/--log-format. | maintainability | 30 | code-review | claude | MEDIUM |
| U | [ ] | MEDIUM | internal/payload/diff.go:88 | Resource leak | File handles not closed properly | performance | 5 | code-review | bruce | MEDIUM |

### [2026-06-16] From Sprint: 3.5_severity-rank-consolidation

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:18 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestMergeNormalizesMixedCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:30 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestGrayZoneNormalizesMixedCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/severity_consolidation_test.go:42 | Test name too long (Won't fix: rejected by maintainer — breaks file's TestSubject_Behavior naming convention) | Shorten to TestMergeNoDisagreementOnCase | maintainancy | 2 | code-review | Reviewer | MEDIUM |

### [2026-06-16] From Sprint: epic-3.5

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source |
|-------|---|----------|------|---------|-----|----------|-------------|--------|
| U | [/] | LOW | internal/stream/severity.go:20 | The canonical SeverityRank map carries a read-only-after-init invariant in a comment only, with no test guard preventing a future caller from mutating the shared map (which would race across concurrent fan-out agents). (Won't fix: structural guard — reconcile copy-on-init at merge.go:29-31 means no consumer writes stream.SeverityRank directly; grep confirms zero write sites across consumers. Snapshot test trips the over-simplification gate; Rank() accessor cascades to 14+ direct-lookup sites — both out of pure-consolidation scope.) | Add a stream test that snapshots the map and asserts it is unchanged after consumers run, or wrap it behind a Rank(sev) accessor. | OBSERVABILITY | 20 | execute-epic-independent |

### [2026-06-16] From Sprint: 3.4_scorecard-diagnostics-writer

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:118 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; the diagnostic already routes to the injectable writer, only fmt.Fprintf's own return is dropped; propagating it breaks Emit's never-fail contract) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:199 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; Append loop already surfaces firstErr; propagating the Fprintf return breaks the best-effort contract) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:235 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; verdictTallies already writes to injectable w; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:248 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; verification-read-failed diagnostic writes to injectable w; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/scorecard.go:276 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; orphan-verdict diagnostic routes to injectable w, locked by TestEmit_OrphanVerdictDiagnosticRoutesToDiagWriter) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | LOW | internal/scorecard/scorecard_test.go:291 | Epic 3.4 added unit tests proving EmitOpts.Diag / ReadOpts.Writer routing at the scorecard layer, but there is no test asserting the wiring itself — that the MCP handler passes a non-default writer or that the three CLI entry points pass cmd.ErrOrStderr(). This plumbing can silently regress (a future refactor swaps cmd.ErrOrStderr() back to a default and no test fails). (Deferred: Epic Plan 3.6 scorecard-wiring-tests) | Add a CLI-level test that drives a scorecard command against a deliberately-malformed store and asserts the read diagnostic reaches the command's ErrOrStderr buffer; once MCP diagnostics route through e.log, add a handler-level test asserting against a captured logger buffer. | testing | 60 | code-review | claude | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:114 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; over-long-line warning uses injectable w via diagWriter(opts.Writer); read continues; AC2 satisfied) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:145 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; decodeRecord writes to injectable w then returns (Record,bool) — no error cascade) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:155 | Error from diagWriter is silently discarded (Wontfix: intentional best-effort `_, _ =` diagnostics discard; schema-version skip writes to injectable w; identical to malformed-record path) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| 1 | [/] | LOW | internal/scorecard/store.go:194 | Redundant call to diagWriter (Wontfix: FALSE POSITIVE — diagWriter is the required typed-nil guard for the nil-able opts.Writer interface, not redundant; removing it reintroduces the panic fixed by commit 476c6d1) | Remove diagWriter call and use opts.Writer directly since ReadRecords already resolves it | performance | 2 | code-review | otto | MEDIUM |
| 1 | [/] | MEDIUM | internal/scorecard/store.go:211 | Error from diagWriter is silently discarded (Wontfix: FALSE POSITIVE — `_, _ = fmt.Fprintf` is the documented best-effort never-panic diagnostics contract at store.go:22-24; returning the write error would regress a successful read on a broken sink and logging is circular; confirmed working as designed via /sprint-clarification 97%) | Log or return the error from fmt.Fprintf | error-handling | 5 | code-review | dax | MEDIUM |
| U | [/] | MEDIUM | internal/mcp/handlers.go:220 | The MCP engine carries a structured *slog.Logger (e.log) used for every other diagnostic in handleReconcile (e.g. the require_verified warning at line ~225), but scorecard diagnostics are routed to raw os.Stderr, so MCP-path scorecard write-failures/malformed-record/orphan-verdict warnings emit as unstructured plaintext that bypasses the logger's level filtering, formatting, and sink redirection. NOTE: this was a DELIBERATE, documented epic decision (Clarifications Q2: supply os.Stderr at the call site; adapting e.log to an io.Writer was explicitly OUT of scope), so this is enhancement debt, not a regression. (intent_note: deferred per epic Clarifications Q2 — adapting e.log to io.Writer is out of scope) (Wontfix: ACCEPTED ENHANCEMENT DEBT — handlers.go:220 is an unconditional EmitOpts{Diag: os.Stderr} call, not deferred logic; all five Epic 3.4 ACs are met and the comment at handlers.go:214-219 satisfies AC4; e.log→io.Writer adaptation is out of scope per Clarifications Q2; confirmed via /sprint-clarification 97%) | If MCP observability is later desired, adapt e.log into an io.Writer shim (slog-backed at Warn level) and pass it as Diag instead of os.Stderr, so MCP-path scorecard diagnostics flow through the same structured pipeline as the rest of the handler. Defer unless/until structured MCP diagnostics are required. | error-handling | 30 | code-review | claude | MEDIUM |

### [2026-06-15] From Sprint: 3.3_per_run_scorecard

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| Solo | [/] | LOW | internal/scorecard/store.go (FindByRunID adjacent-month notice; ReadRecords malformed-line notice); internal/scorecard/scorecard.go (Emit notices) | Store/emit diagnostics ("skipping malformed record", "run spans adjacent month files", "write failed") are written directly to the process-global `os.Stderr` rather than to a writer threaded from the cobra command (`cmd.ErrOrStderr()`). (Deferred: Epic Plan 3.4) | accept an `io.Writer` (or logger) on the store/emit entry points and have the CLI pass `cmd.ErrOrStderr()`, so warnings are capturable and redirectable. | ops | 0 | execute-sprint | execute-sprint | MEDIUM |

### [2026-06-14] From Sprint: 3.2_disagreement_radar

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| Solo | [/] | LOW | internal/reconcile/disagree.go:280 | Duplicate writeRadarSection rendering logic risks divergent escaping across packages (Deferred: .planning/epics/active/15.0_radar-renderer-consolidation.md) | Consolidate radar rendering into report package; reconcile should call report.writeRadarSection | security | 20 | code-review | greta | MEDIUM |
| 4 | [/] | LOW | internal/reconcile/disagree.go:350 | Duplicated radar section rendering logic (Deferred: Epic Plan 15.0) | Extract shared writeRadarSection to a common package | maintainability | 10 | code-review | bruce | MEDIUM |
| 4 | [/] | MEDIUM | internal/reconcile/disagree.go:354 | Duplicated radar markdown rendering diverges (Deferred: Epic Plan 15.0) | Extract shared writer or make reconcile use report package's escTrunc | maintainability | 10 | code-review | bruce | MEDIUM |
| Solo | [/] | LOW | internal/reconcile/disagree.go:413 | Redundant implementation of writeRadarSection (Deferred: .planning/epics/active/15.0_radar-renderer-consolidation.md) | Remove duplicate function from internal/reconcile and use internal/report | maintainability | 15 | code-review | otto | MEDIUM |
| 5 | [/] | MEDIUM | internal/report/disagree.go:47 | The radar markup is rendered by two divergent copies: writeRadarSection + formatScore in internal/report/disagree.go and a structurally different copy in internal/reconcile/disagree.go:389. They are intentionally not identical (report copy truncates via escTrunc + uses joinReviewers/reviewerOrUnknown; reconcile copy uses uncapped esc + joinOrNone), so a future markup change (new field, reordered bullets) must be made in both or the live `atcr report` radar and the archival reconciled/report.md silently drift. (Deferred: .planning/epics/active/15.0_radar-renderer-consolidation.md) | Extract one shared item renderer parameterized by a truncate-vs-verbatim flag and heading prefix; have both call sites delegate. Add a test that diffs the rendered markup of both paths on the same DisagreementsFile asserting the only intended difference is truncation. | correctness | 60 | code-review | bruce, claude | HIGH |

### [2026-06-14] From Sprint: 2.2_code_review_fanout_hardening

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|----------|
| Solo | [/] | MEDIUM | internal/fanout/postprocess.go:14 | The severity-rank rubric {CRITICAL:4,HIGH:3,MEDIUM:2,LOW:1} is independently redefined in fanout/postprocess.go:17, reconcile/merge.go, verify, report, plus a set-form copy reviewSeverities in registry/config.go. postprocess looks up severityRank[strings.ToUpper(...)] while reconcile looks up the raw value, so a future severity change or non-canonical casing silently desyncs fan-out truncation from reconcile merging. The postprocess copy was newly added by Epic 2.2. (disagreement: LOW vs MEDIUM) (Deferred: Epic Plan 3.5) | Extract a single canonical severity package (or export from internal/stream) exposing the ordered rank map plus normalizeSeverity, and have registry/fanout/reconcile/verify/report consume it. Verify by deleting the local maps and confirming the suite passes with one source of truth. | maintainability | 120 | code-review | claude | MEDIUM |

### [2026-06-14] From Review: llmclient OpenAI-compatible tool handling

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| U | [/] | LOW | internal/llmclient/chat.go:1 | No provider-conformance test matrix for the OpenAI-compatible surface. The client deliberately absorbs real wire divergence (string-encoded vs raw-object tool_call `arguments`, lenient finish_reason) but is exercised only against synthetic fixtures, so a regression against a specific provider's actual tool_call shape (OpenAI, litellm, Ollama, vLLM, Together) would not be caught. This is the robustness the official SDK is assumed to provide, achievable here without adopting it. | Add a recorded-fixture conformance suite: capture a real `tool_calls` response from each target provider and assert the parser (`ToolCallArguments`, `chatToolResponse` decode, finish_reason handling) yields identical engine-facing results. NOTE: scope is a few days, not a quick-win — consider promoting to a standalone test-remediation plan rather than resolving inline. | testing | 480 | review | claude | LOW |

### [2026-06-14] From Sprint: 3.0_adversarial_verification

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| 7 | [/] | LOW | internal/verify/pipeline.go:238 | The rich VerificationResult built in verifyFinding never populates TrippedBudgets (invokeSkeptic folds tripped budgets only into free-text Notes), base.Model is hard-coded to skeptics[0].Config.Model even when another skeptic produced the winning verdict, and the skip-already-verified rebuild (pipeline.go:199) re-synthesizes records from the compact on-disk block losing Model/DurationMs/TrippedBudgets — so verification.json's structured audit fields degrade or misattribute on multi-skeptic and no-op re-runs (extends the model-attribution gap of TD-011). Also: Model is empty on the no_eligible_skeptic / tool_harness_unavailable early-return paths. [intent: deferred per sprint-plan TD-011] | Thread the tripped-budget slice and the winning skeptic's model up from invokeSkeptic into base.TrippedBudgets/base.Model (join models when multiple voters agree, mirroring joinSkeptics), and carry Model/DurationMs/TrippedBudgets forward for skipped findings instead of synthesizing a lossy record. | correctness | 120 | execute-sprint | execute-sprint, claude | HIGH |

### [2026-06-13] From Sprint: 2.0_tool_using_reviewers

| Group | | Severity | File | Problem | Fix | Category | Est Minutes | Source | Reviewers | Confidence |
|-------|---|----------|------|---------|-----|----------|-------------|--------|---------|------------|
| 5 | [/] | LOW | internal/tools/snapshot.go | AC 03-02 Scenario 5 and AC 03-03 Scenarios 4-5 require `manifest.json` `stages.review` to record `snapshot_mode` (live/worktree), `head_sha`, and `snapshot_worktree_path`. (intent_note: deferred per sprint-plan §2.5.A (manifest review-stage recording is Phase 5 work); Deferred to Epic Plan 2.1) | when wiring `SnapshotFor` into the agent loop, record `snapshot_mode`/`head_sha`/`snapshot_worktree_path` into `internal/payload/manifest.go` review stage and add the manifest assertion tests from AC 03-02/03-03. | testing | 0 | execute-sprint | execute-sprint | MEDIUM |
