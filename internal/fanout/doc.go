// Package fanout runs the reviewer pool: parallel and serial lanes, fallback
// chains, global-timeout context handling, partial-success semantics, and
// crash-safe per-agent artifact writes.
package fanout
