// Package payload builds per-agent reviewer input in three modes (diff,
// blocks, files), enforces byte budgets with deterministic truncation, and
// renders persona prompt templates with payload variables.
package payload
