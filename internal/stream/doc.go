// Package stream implements the atcr-findings/v1 stream format: the strict
// severity-prefix extraction regex, pipe escaping, short-row padding, and the
// parser/writer shared by fan-out and reconcile.
package stream
