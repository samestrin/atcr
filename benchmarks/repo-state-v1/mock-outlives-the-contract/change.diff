diff --git a/billing/rates.py b/billing/rates.py
index d97f07a..ef9c68a 100644
--- a/billing/rates.py
+++ b/billing/rates.py
@@ -2,6 +2,10 @@
 
 DEFAULT_RATE = 1.0
 
+
+class UnknownPlan(Exception):
+    """Raised when a plan has no rate entry."""
+
 RATES = {
     "free": 0.0,
     "team": 2.5,
@@ -12,7 +16,9 @@ RATES = {
 def lookup_rate(plan):
     """Return the per-unit rate for plan.
 
-    An unrecognised plan falls back to DEFAULT_RATE, so a new plan name rolled
-    out ahead of its rate entry still bills at something sane.
+    An unrecognised plan is a provisioning bug, so it raises rather than
+    billing a default nobody chose.
     """
-    return RATES.get(plan, DEFAULT_RATE)
+    if plan not in RATES:
+        raise UnknownPlan(plan)
+    return RATES[plan]
