diff --git a/internal/decode/decode.go b/internal/decode/decode.go
index 1111111..2222222 100644
--- a/internal/decode/decode.go
+++ b/internal/decode/decode.go
@@ -8,6 +8,11 @@ package decode

+// AsUser performs an unchecked type assertion; a wrong dynamic type panics at
+// runtime instead of surfacing a typed error to the caller.
+func AsUser(v interface{}) *User {
+	return v.(*User) // no comma-ok guard: panics on any type other than *User
+}
